# Паттерны остановок 09.09 — (ff), s37 «harness-fields»: поле, оставшееся в руках оператора, — ритуал с именем поля

| # | остановка | класс | корень | предвидимо файлом фазы? | что вошло |
|---|---|---|---|---|---|
| (ff) | не остановка — девиация `harness-permission` внутри принятого айтема: s37 шла в auto-режиме (классификатор отклонил `cp` в `.claude/` и read-only `sed -n`), хотя (ee) объявила строку запуска `claude --dangerously-skip-permissions` полем; исполнитель положил файл инструментом Write и доказал байт-в-байт; плюс вопрос тимлиду «останавливает ли deny `Edit(…)` также Write» | процесс — тимлида | «поле» (ee) осталось в руках оператора — значение, которое надо повторять при каждом запуске, ритуал по факту; отказало в первой же сессии; ответ на вопрос был в доках (permissions: «Edit rules apply to all built-in tools that edit files»), но в аудите не назван | да — v3.16 §5 уже говорил «поле, не ритуал», но не сказал, ГДЕ поле живёт | режим — дефолт в пользовательских настройках (`permissions.defaultMode = bypassPermissions`; доки: из проектного файла игнорируется), флаг лишний; штамп `permission_mode` хуком в `.claude/session_mode` + первый гейт §0 ранбука c3 (обе стороны продрилены на сохранённой команде); PROCESS v2.5 «Harness fields», PHASE v22; скилл v3.17 §5: поле — там, где платформа читает его без рук; ответ про Write — рулинг (ff)2, тест/гард не нужен |

Итог дня (полдень): 1 сессия исполнителя (s37, $0, принят), 0 остановок, 1 девиация (процесс, тимлида, закрыта полем + гейтом); закон исполнителя: +1 хук-штамп (не гард — никогда не роняет вызов), +1 строка `.gitignore`, +1 гейт в ранбуке c3; закон тимлида: PROCESS v2.4 → v2.5, PHASE v21 → v22, скилл v3.16 → v3.17. Деньги: $0; цикл-3 REMAINING $2.1178.

## §2 (14:00) — (gg), s38 «c3-prep»: стоп (l)3 верный; три дефекта записи тимлида — неизмеренная предпосылка рулинга, инертное поле, отказ без механизма из доков

| # | остановка | класс | корень | предвидимо файлом фазы? | что вошло |
|---|---|---|---|---|---|
| (gg)-1 | стоп исполнителя (l)3 «оба канала несут ОДИН chain id `marketopt_promo`»: у требования нет производителя — `chain_key(owner().name)` не сворачивает НИ ОДИН из двух каналов Маркетопта, а две из трёх форм правки двигают отгруженный слой P1 с опубликованным числом (0.7411 / 0.7937) | развилка — тимлида (предвидимая) | пункт (l)3 написан из комментария в `chain_aliases.yaml` («marketopt_private — тот же chain»), а не из прогона `chain_key` по именам реестра: предпосылка «существующий id, ничего запечатанного не движется» была верой, не замером | да — §6.7 v20 уже говорит: фильтр/слой продукта, которого не видел грейдер, = замер за $0 функциями продукта; рулинг должен был назвать это шагом, а не «нулевой» правкой | форма (b) как отдельный $0-шаг «chain-fold» ПОСЛЕ c3 и тома: R2/R3 пишут chain ID владельца через карту `chain_of_channel`, один читатель `chain_key` не меняется, число продукта перемеряется его же функциями (sha до/после, по наборам), сдвиг публикуется с файлом по (dd); тест в обе стороны; до этого экран показывает приватный канал под несвёрнутым ключом — названо в записи |
| (gg)-2 | девиация записи: третий гейт §0 ранбука c3 отказывает по построению — allow-правила (ee) `pod create`/`pod delete`, а нога создаёт `template create` и `serverless create`; и это в режиме bypass, где allow-правила не действуют вовсе | процесс — тимлида | правило (ee) писалось под команду симптома s33 (`pod create`), не под строки ранбука; после (ff) (режим — поле, читаемое первым гейтом) правило стало инертным, а инертное поле, которое надо поддерживать на каждой ноге, — ритуал | да — v3.16 §5 «поле, не ритуал» — но не сказал: поле, которое не может сработать, снимается, а не чинится | allow-правила сняты (файл `docs/reviews/2026-09-09-harness-allow/settings.json`, `"allow": []`), третий гейт удалён, однострочник v2.6 проверяет `allow == []` + хук-штамп; обе стороны прогнаны мной (OK на выпущенном, MISSING на живом) |
| (gg)-3 | девиация `harness-permission` ПОВТОРИЛАСЬ при работающем лечении (ff): штамп говорит `bypassPermissions`, а `cp <docs/reviews/…> .claude/settings.json` и `sed -n … docs/reviews/…` отклонены; исполнитель сделал пять проб, положил файл через Write | процесс — тимлида | (ff)2 приписал отказы s37 классификатору без цитаты доков — та же ошибка «из симптома», что и в (ee), в другую сторону; механизм в доках: «Read and Edit deny rules apply … to file commands Claude Code recognizes in Bash, such as cat, head, tail, and sed, and to the targets of Bash redirections» — в любом режиме; CHANGELOG 2.1.261 называет `cp -r` | да — v3.16 §5 «правило харнеса пишется из доков» — (ff)2 нарушил его в самой формулировке | PROCESS v2.6 «The deny rules reach into Bash»: deny-путь никогда не операнд Bash-файловой команды; файлы тимлида читаются Read, харнес кладётся Write (записи в `.claude` в bypass разрешены — «Protected paths»), `diff` — доказательство; один отказ документированного вида → документированный путь, без проб |
| (gg)-4 | не стоп — «готово» оператора: сессия «c3-prep» шла ПРОДОЛЖЕНИЕМ процесса s37 (компакция), а не свежим запуском; поле режима на старте процесса не испытано | процесс — тимлида (инструкция в чате) | «выйди и запусти заново» было просьбой в сообщении — ритуал в руках оператора; запись же (первая строка PROGRESS + штамп) поймала это сама, за $0 | — | «c3-prep-2» и «c3» — каждая в СВЕЖЕМ процессе; первая строка PROGRESS = режим из шапки + штамп живого хука; стоп при ином — мой |

Итог дня (14:00): 2 сессии исполнителя (s37 принят утром; s38 «c3-prep» $0, принят — нога c3 собрана и оценена: 30 страниц + 8 постов, dry run $0.1864 при капе $0.50), 1 стоп (верный, развилка тимлида), 3 девиации записи (все — тимлида); закон исполнителя: −2 allow-правила, −1 гейт, +1 строка ранбука (том), +1 правило чтения (Read вместо `sed`/`cp` на deny-путях); закон тимлида: PROCESS v2.5 → v2.6, PHASE v22 → v23, скилл v3.17 → v3.18 (три пункта: предпосылка рулинга измеряется до записи; инертное поле снимается; отказ приписывается только механизму из доков). Деньги: $0; цикл-3 REMAINING $1.4859 (13:32Z), том ≈ $0.23/день до удаления в сессии c3.

## §3 (15:30) — (hh), s39 «c3-prep-2» принят; верификатор до покупки — NOT SAFE: три непроизошедших стопа (все — тимлида)

| # | остановка | класс | корень | предвидимо файлом фазы? | что вошло |
|---|---|---|---|---|---|
| (hh)-1 | находка верификатора (RISK 1): при капе $0.50 внутренний гейт эмиттера (`run_promo_c2.py:681, :732–735`: пачка сайзится по времени 540 с / разогрев, комната = кап − резерв $0.2843 на зависший джоб − хвост $0.0184 − счёт) закончил бы ногу c3 ДО первой страницы, оплатив буст и текстовую ногу; rung 0 dry run этот гейт не моделирует | деньги — тимлида (стоп, которого не случилось) | гейт написан под ногу C2 в 3 008 страниц и ни разу не перечитан на популяции c3 в 30 страниц; (gg)/(hh) prep-шаги считали ногу «оценённой», потому что FITS был зелёным — а FITS считает всю ногу, не комнату перед первой пачкой | да — скилл §6 «правило, снимающее или заменяющее границу, проверяется на КАЖДОЙ ноге, которая её читает» — не применено к денежному гейту новой популяции | кап $0.80 твоим словом (min($0.80, REMAINING − $0.30)); код денег до покупки не трогаем; названо на ретро: пачка по оставшимся строкам + dry run прогоняет внутренние гейты; скилл v3.19 §6: денежный гейт перечитывается на популяции новой ноги до покупки |
| (hh)-2 | находка верификатора (RISK 2): §3 ранбука c3 запускает платный `--run` в переднем плане (`| tee`) — форму, которую в C2 харнес убил дважды (`run_promo_c2.log:113, :171`), после чего нога C2 доработала под `nohup`; убитый процесс не пишет запись, пачка в полёте перекупается | процесс — тимлида | ранбук C2 так и остался с формой первого запуска, а не с той, что доработала; приёмка C2 не заменила строку; c3 унаследовал её как «то же, что и C2» | да — §6 «охота за тем, что добавлено и что выпало» — но не сказано: строка, заменённая ПО ХОДУ платного рана, заменяется в ранбуке на приёмке | §3 = detached запуск + короткие опросы PID и записи; скилл v3.19 §6: запись несёт то, что РАБОТАЛО |
| (hh)-3 | ниты верификатора: `--close` без `--tolerance` (`runpod_guard.py:697`), нет строки ключа API, том до закрытия (walk спрашивает `billing network-volume`), `register()` называет файлы C2 (класс (gg)5), кап и пол $0.15 — арифметика руками; и моя (gg)5: контроль «байт в байт» недостижим (запись C2 содержит живое чтение гарда) | процесс — тимлида | ранбук c3 «перенацелен» из C2 без прогона его закрывающих строк на коде; контроль в рулинге сформулирован без прогона | частично — PROCESS «Closing a line» уже называет флаги закрытия; C2 §0 уже нёс ключ | «c3-prep-3»: §0 ключ + кап командой + гейт пола; §5 по PROCESS; том ПОСЛЕ закрытия; `register()` из флагов; скилл v3.19 §3.5: контроль, названный рулингом, прогоняется до выпуска или формулируется как «показать дельту» |

Итог дня (15:30): 3 сессии исполнителя (s37, s38, s39 — все $0, приняты), 1 стоп (верный, (l)3), 3 девиации записи (gg), 3 непроизошедших стопа (hh) — все корни у тимлида, $0 потеряно; поле режима доказано на свежем старте; закон исполнителя: −2 allow-правила, −1 гейт, ранбук c3 по находкам верификатора; закон тимлида: PROCESS v2.6, PHASE v22 → v24, скилл v3.17 → v3.19 (v3.19: денежный гейт перечитывается на популяции новой ноги; ранбук несёт то, что работало; контроль рулинга прогоняется до выпуска). Деньги: $0; цикл-3 REMAINING $1.4762 (15:05Z); кап c3 $0.80, ожидаемо $0.1864.

## §4 (16:50) — (ii), s40 «c3-prep-3» принят; третий заход верификатора — ещё два непроизошедших стопа, оба в строках ранбука

| # | остановка | класс | корень | предвидимо файлом фазы? | что вошло |
|---|---|---|---|---|---|
| (ii)-1 | RISK A: `export RUNPOD_API_KEY` в вызове §0, `nohup … --run` в вызове §3 — между вызовами Bash среда не живёт (ранбук сам это пишет в строке 60 про `$CAP`), `run_promo_c2.py:1015` читает ключ только из окружения → `--run` отказал бы после создания эндпоинта ($0 простоя, но стоп в платной сессии) | процесс — тимлида | (hh)4 велел «§0 — экспорт первым», не сказав «в том же вызове, что и запуск»; ранбук дрелили по гейтам, но не РЕПЕТИРОВАЛИ по границам вызовов; исполнитель сам применил правило к `$CAP`, но не к переменной окружения | частично — правило «shell state не переживает вызов» уже было в ранбуке, для одной переменной из двух | §3 — один вызов: экспорт `&&` `CAP=$(…)` `&&` `nohup … &` `&&` pid; правило репетиции каждой строки (скилл v3.20 §7) |
| (ii)-2 | RISK B: `--close --expect-ms $MS` из Σ wall записи против walk биллинга — serverless считает uptime воркера (буст, хвост 60 с, убитые раны): на реальной записи C2 5,84 млн мс против walk 9,82 млн → `complete()` False; C2 закрывалась с `expected_ms: null`; плюс `--note` под заголовком «через 30–40 мин», а полоса 5 % (капля тома против записанного расхода) закрывается ≈ через час после якоря | процесс — тимлида | (hh)4(iii) велел «закрытие по PROCESS «Closing a line» (`--expect-ms …`)» — по ФОРМЕ записи, не по реальному закрытию прошлой ноги; PROCESS писался под поды (wall = billed), serverless — другая арифметика; никто не открыл `spend_promo_pulse_1.json` | нет — PROCESS «Closing a line» сам вводит в заблуждение для serverless; правится на ретро (в PROCESS: «для serverless — без `--expect-ms`, по образцу C2») | §5 по образцу C2: `--note` сразу после teardown; close без `--expect-ms`, `--until "$(date -u …)"`, `--tolerance 0.05`; том после закрытия; скилл v3.20 §7: закрытие пишется по реальному закрытию прошлой ноги |
| (ii)-3 | ниты: `python3` вместо `python3.11` в строках гарда (без шима pyenv — 3.9, ImportError); `cap_from` = «рулинг (b)» для капа (hh)3 — поле, которое говорит, кто задал кап; `CAP=$(…)` и `nohup` разделены пустой строкой (два вызова → пустой `$CAP`, argparse exit 2); `--until` руками | процесс — тимлида | те же корни: строки, не прогнанные в границах вызовов; C2-литерал, не выведенный из флага | — | «c3-prep-4»: `python3.11` везде; `cap_from` из флага; одна строка-вызов; `--until` из часов |

Итог дня (16:50): 4 сессии исполнителя (s37–s40, все $0, приняты), 1 стоп (верный, (l)3), 3 девиации записи (gg), 5 непроизошедших стопов ((hh) 3 + (ii) 2) — все корни у тимлида, $0 потеряно; поле режима доказано дважды на свежих стартах; закон исполнителя: ранбук c3 переписан трижды по находкам верификатора; закон тимлида: PROCESS v2.6, PHASE v22 → v25, скилл v3.17 → v3.20 (v3.20: каждая строка платного ранбука, кроме самой покупки, репетируется за $0 в границах вызовов платной сессии; закрытие пишется по реальному закрытию прошлой ноги). Деньги: $0; цикл-3 REMAINING $1.4567 (16:35Z); кап c3 $0.80, ожидаемо $0.1864.

## §5 (18:00) — (jj), s41 «c3-prep-4» ПРИНЯТ; ТРЕТИЙ проход верификатора — SAFE TO BUY, только fail-safe ниты; v3.20 подтверждён

| # | остановка | класс | корень | предвидимо файлом фазы? | что вошло |
|---|---|---|---|---|---|
| (jj)-1 | не остановка — плановое чтение (верификатор до покупки): третий проход по §3/§5/`cap_from` вернул SAFE TO BUY. Ни одного пост-покупочного риска: любой отказ, который он смог построить (пустой ключ, чужой endpoint, битое чтение CAP), роняет `&&`-цепь ДО загрузки воркера | плановое чтение (не стоп) | — | — | ничего в закон: v3.20 (репетиция каждой платной строки в границах вызовов + закрытие по реальному закрытию прошлой ноги) СРАБОТАЛА — исполнитель сам продрилил §3/§5 по вызовам, риски (hh)/(ii) исчезли |
| (jj)-2 | нит верификатора: `--endpoint E` / `-T` — плейсхолдеры ручной подстановки (§2 создаёт и печатает id); незаполненный → `assert_serving` падает, воркер не грузится, $0 | процесс — тимлида (fail-safe) | плейсхолдер id create-шага неизбежен; ранбук печатает `-> id E`, но не помечает строку как «подставить из §2» | нет — это норма любого ранбука с create | напоминание «подставить E/T из §2» — $0 doc-правка в «chain-fold»; НЕ гейт покупки |
| (jj)-3 | нит верификатора: §3 `export RUNPOD_API_KEY=$(…)` маскирует статус подстановки (в отличие от §0, где есть проверка длины); нечитаемый `config.toml` между §0 и §3 → пустой ключ → отказ аутентификации → $0 | процесс — тимлида (fail-safe) | §0 и §3 симметричны по смыслу, но не по проверке; §0 доказал файл минутами раньше в той же сессии | частично — «каждая платная строка репетируется» уже в v3.20; проверка непустоты — не отдельная строка, а укрепление | гард `&& [ -n "$RUNPOD_API_KEY" ]` в §3 — продрилен мной за $0 (пустой ключ роняет цепь до create); фолд в «chain-fold»; НЕ пятая prep-сессия |
| (jj)-4 | не стоп — девиация `unauthorised-create` (§2 запущен по ошибке выбором блока по номеру фенса) — проверена реверснутой: мои листинги облака чисты (2 доисторических шаблона, serverless/pod `[]`, том на месте), гард REMAINING $1.4470 = чтение ДО create → `template create` не биллит, endpoint не существовал, $0 | процесс — исполнителя, закрыт им же в той же сессии | выбор блока `sed -n '5p'` по ординалу в списке фенсов указал на §2 | — (девиация исполнителя, не тимлида) | каждый блок теперь по своей первой/последней строке; названо исполнителем, $0 |

Итог дня (18:00): **5 сессий исполнителя (s37–s41, все $0, все приняты)**, 1 стоп (верный, (l)3), 3 девиации записи тимлида (gg), 5 непроизошедших стопов ((hh) 3 + (ii) 2 — все тимлида), 1 плановое чтение верификатора (jj, SAFE) + 1 девиация исполнителя (unauthorised-create, закрыта им, $0). $0 потеряно за день. Поле режима доказано трижды на свежих стартах. Закон исполнителя: ранбук c3 переписан трижды по находкам верификатора, теперь SAFE TO BUY; два fail-safe нита — в «chain-fold». Закон тимлида: НИЧЕГО нового — v3.20 подтверждён третьим проходом (процесс поймал два денежных бага в (hh)/(ii) и теперь чист, всё за $0). Метрика ретро §7 «preventable stops»: профилактика (v3.20) на месте, не «нужна ещё prep». Честно: четыре prep-сессии на одну платную ногу — цена, которую это купило; больше prep не должно. Деньги: $0; цикл-3 REMAINING $1.4470 (15:50Z); c3 GO, кап $0.80, ожидаемо $0.1864.

## §6 (18:35) — (kk), s42 «c3» ПРИНЯТ как ПОЛНАЯ платная нога ($0.2577); открытый стоп — ЛОЖНЫЙ отказ полосы: 5% относительный допуск не годится для ноги < $1

| # | остановка | класс | корень | предвидимо файлом фазы? | что вошло |
|---|---|---|---|---|---|
| (kk)-1 | **стоп: §5 отказывает по ПОЛОСЕ** — `promo-c3` settles at $0.257668 против записанного $0.227200, 13.4% при допуске 5%. Нога ПОЛНАЯ (30 стр. + 8 постов, `stopped false`), дёшево ($0.2577 из $0.80); отказ мешает только закрыть линию | деньги — тимлида (верный стоп) | §4-ссылка $0.2272 снята в 16:46 = ровно `serverless`, ДО того как поздний вид `pods` $0.0305 (GPU-секунды serverless-воркера, под `8f7itos72efemh`) сел на баланс → ссылка теряет ЦЕЛЫЙ вид биллинга; 5% × $0.2272 = $0.0114, а один поздний вид = $0.0305 | нет — денежная секция файла фазы несла относительную полосу, но не абсолютный пол; C2 дрейфовала 2.73%/1.29% только потому, что $2.99 впитывали те же центы | рулинг (a): АБСОЛЮТНЫЙ пол рядом с относительной полосой — паттерн `complete()` для мс (`max(MS_FLOOR, MS_BAND×expect)`): `:897` → `abs(settled−recorded) > max(DOLLAR_FLOOR, tol×recorded)`, `DOLLAR_FLOOR=$0.05` (выше замеренного $0.0305 + капля тома, ниже кроссовера 5%×$1.00 — вяжет только ноги <$1, крупные без изменений); НЕ расширение `--tolerance`; тест в обе стороны; скилл §7, PROCESS |
| (kk)-2 | не остановка — находка: вариант (b) «перекадрировать ссылку после walk» НЕ чинит | — | ссылка тогда читает balance-delta $0.2771 (капля тома внутри), а settled том исключает → off = 7.0%, всё равно >5% | — | (b) отклонён в рулинге на моих числах; полоса неверна независимо от времени ссылки, т.к. ссылка и settled меряют разное (always-on вид внутри/снаружи) |
| (kk)-3 | не остановка — находка: биллинг-walk запоздал на ~70 мин (ранбук: 30–40); проекция страницы 3.369 с/стр., а нога шла 19.828 с/стр. (5.9×) | — (замер, не стоп) | RunPod эмитит строки с переменным лагом; проекция dear-угла оптимистична по времени страницы | частично — §6.5/§7 знают про лаг; про 5.9× промах проекции — нет | лаг 60–90 мин — в PROCESS «Closing a line» (доп. к 30–40); промах проекции — ретро-находка: будущие ноги оцениваются по РЕАЛИЗОВАННОЙ ставке; кап $0.80 (hh) впитал промах — оправдан задним числом |

Итог дня (18:35): **6 сессий исполнителя (s37–s42; s37–s41 $0, s42 ПЛАТНАЯ $0.2577)**, 2 стопа (верных: (l)3 днём; (kk) — денежный, полоса), 3 девиации записи тимлида (gg), 5 непроизошедших стопов ((hh)3+(ii)2), 1 плановое чтение верификатора (jj SAFE) + 1 девиация исполнителя (unauthorised-create, $0). Платная нога c3 — **УСПЕХ**: полная, дёшево ($0.2577 из $0.80), 0 нечитаемых; закрытие линии застряло на ложном отказе полосы — рулинг (kk) разблокирует за $0. Закон исполнителя: «kk-close» — пол в гард + тест обе стороны + закрытие + том. Закон тимлида: скилл §7 (+пол рядом с полосой; предлагаю карточку), PROCESS (DOLLAR_FLOOR $0.05, лаг 60–90 мин). Метрика §7: 2 стопа/день — оба верные и денежные, не превентивные-в-моменте; (kk) превентивна-в-прошлом (в денежном шаблоне не было пола). Честно: платная нога прошла с первого раза, дёшево и полно; единственная заминка — калибровка гейта закрытия под мелкую ногу, чинится за $0. Деньги: цикл-3 $8.84 из $10.00, REMAINING $1.1602; том ждёт закрытия ≈$0.24/день; гейт 12.09 держится.
