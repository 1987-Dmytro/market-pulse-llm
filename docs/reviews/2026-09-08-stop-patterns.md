# Паттерны остановок 08.09 — (dd), s35 «p1-ship»: инструмент ≠ продукт (граница инструмента не была названа файлом фазы)

| # | остановка | класс | корень | предвидимо файлом фазы? | что вошло |
|---|---|---|---|---|---|
| (dd) | s35: айтем сделан целиком ($0); открытый стоп — петля продукта подаёт P1 строки ПОСЛЕ хуков (`promo_hooks.screen`: цитата — точная подстрока), а замер (cc) подавал P1 СЫРОЙ ответ модели (`predicted_rows`); исполнитель нашёл 1 сдвинутую строку holdout-2 (0.7500 → 0.7411) и остановился, не расширяя запись C3; моё чтение тем же путём (`data/annotation/tl_loop_replay.py`, функции самой петли) добавило дельту сигналов 0.8021 → 0.7937 (3 сброшенные строки), dev-40 / dev-2 — без изменений | развилка (дизайн) — тимлида | §2 файла фазы задал грейдер над `predicted_rows` (сырьё), §2 S4 — хуки в продукте, и ни одна строка не сказала, входит ли фильтр продукта в измеряемый инструмент; все пять итераций и два холдаута мерили МОДЕЛЬ, а не продукт | да — принципом §2 скилла «надёжность считается на продукте»; конкретная строка «инструмент = пайплайн продукта целиком» отсутствовала и в файле фазы, и в карточке | рулинг (dd): (c) — число ПРОДУКТА меряется его же функциями за $0 (`grade_promo_loop_readings.json`), печатается первым как отгруженное; замеры бара не переписываются; запись C3 не расширяется; PHASE v20 §2 + §6.7 (каталог развилок: «фильтр продукта вне инструмента → $0-чтение функциями продукта, не стоп»); скилл v3.15 §2: граница измеряемого инструмента = пайплайн продукта от модели до экрана, названа в файле фазы ДО первой покупки |

Что ещё в отчёте s35 (не остановки): тимлид ответил на «определение ничьих» (16 по правилу файла, любая ось — правило файла бьёт ручной счёт 15); `build_readme_results.py` без вызывающего → `make promo-screen` (в айтеме «s2-loop»); контракт `@` в `channel` записи C3 → строка в «c3-prep». Исполнитель назвал сдвиг субъекта, но не дельту сигналов — урок исполнителю (в PROCESS не нужен, покрыт (dd)5(i): файл считает обе оси).

Итог дня (утро): 1 сессия исполнителя (s35, $0), 1 остановка — развилка, предвидимая, тимлида (граница инструмента); закон исполнителя: +0 тестов, +0 гардов (два «показано, не утверждено» остаются названными); закон тимлида: PHASE v19 → v20 (149 строк, кап 150 — §7 ужат, устаревшая строка `/plan-phase` снята), скилл v3.15 предложен. Деньги: $0; цикл-3 REMAINING $2.1178.
