# Паттерны остановок 06.09 — (y), s29: регистрация ЯКОРИТ линию — в prep-сессии или в платной?

| # | остановка | класс | корень | предвидимо файлом фазы? | что вошло |
|---|---|---|---|---|---|
| (y) | s29: prep (x) сделан целиком, кроме регистрации — `--register --step promo-iter5` зовёт гард с `--step`, а тот СОЗДАЁТ и якорит `spend_promo_iter5.json`; (x)2 велел регистрацию в prep-сессии, PROCESS «Деньги» v2 — открыть линию «перед подом» в платной; правая часть закрытия — дельта баланса с каплей тома, левая — без неё → возраст якоря = дрейф (iter4: $0.019444 = строка тома ровно, 2.73 % за 2.45 ч; полоса 5 % закрывается за ≈ 4 ч) | процесс (рулинг против стоящего закона) + деньги | тимлида: (x)2 захотел «прочитать зарегистрированную запись до покупки» и посадил якорь на сессию раньше, не спросив «что ещё делает `--register`»; PROCESS v2 нёс два порядка (регистрация с `--step» и «открыть перед подом»), не сказав, что это ОДИН момент | да — тимлидом (пятый денежный промах за два дня); класс «якорь стареет, линия не закрывается» уже назван в (r)(b) и (w)+ | рулинг (y): регистрация = открытие линии → §0a ВНУТРИ платной сессии, минуты до create; чтение тимлида до покупки = dry-run + HEAD (сделано в этой сессии), регистрация читается на приёмке против dry-run; PHASE v14 §6.1; PROCESS «Деньги» v2.1 + «стоящий закон бьёт рулинг по механике — без остановки»; скилл v3.11 §3.4(g)/§3.5/§6 |

Итог дня (полдень): 1 сессия исполнителя (s29), 1 остановка — предвидимая, тимлида, деньги/процесс. Верификатор ДО покупки (свежий агент, HEAD `31a6bc5`): 7 чтений подтверждены, 1 укус (порядок `--pack`/commit в §0a ранбука — отказ за $0) + 2 риска, названы в PROGRESS, не построены. Закон исполнителя: +1 тест по (x)3 (дефект §4, обе стороны), +0 гардов. Рулинг (y) — 17 строк вместо 12: приёмка prep + ответ стопу + §0a платной сессии в одном рулинге; шаблон «Деньги» (brain-init) несёт СЕДЬМОЙ дефолт: «регистрация открывает линию и живёт в платной сессии; чтение до покупки — dry-run + HEAD».

## 2. s30 — остановки НЕТ ($0, ранбук §0a по укусу верификатора). s31 — платная итерация 5 ЗЕЛЁНАЯ; открытый стоп — плановое чтение (z)
| # | событие | класс | корень | предвидимо? | что вошло |
|---|---|---|---|---|---|
| (z) | s31: 80/80, 0 нераспарсенных; dev-40 0.8857 / 0.8667 — оба бара; dev-2 0.8883 / 0.8296 (чтение); под $0.9396, чтение $0.9495 из $1.40, дрейф 1.04 % (правило (y) измерено); `--close` отказал на ЧАСТИЧНОМ walk — биллинг отстаёт, повтор на старте; вопрос стопа «кто стартует holdout-2» — §7 (эталон тимлида) и §6.2 (нотис оператору) уже отвечают | плановое чтение (§6.2) + задержка биллинга | — ; риск 3 верификатора выстрелил как предсказано (ложное `OVER` в run record, гейтов нет) — починка в первом $0-айтеме | нет предвидимого; вопрос внутри стопа файл фазы отвечал | PHASE v15 §2 (зелёное чтение и 4 пина замороженного инструмента названы); рулинг (z): следующий $0-айтем «holdout-2 prep» заканчивается на dry-run, регистрация — в платной сессии; скилл — без изменений |

Итог дня: 3 сессии исполнителя, 1 остановка (s29, предвидимая, тимлида), 1 плановое чтение; закон исполнителя: +1 тест (дефект §4), +0 гардов; ≥2 сессии/день и ≤1 остановка/день — выполнены.

## 3. 17:00 — сессия тимлида: эталон holdout-2 вслепую ($0, остановки НЕТ)
| # | событие | класс | корень | предвидимо? | что вошло |
|---|---|---|---|---|---|
| — | 112 строк / 40 тредов за одну сессию; валидатор 0 ошибок; 36 `unsure` — дро из популяции продукта короче и разговорнее dev-40 (реплики между читателями, «ВАУ», «Ну такое», планы друзей): ничьи «шум ↔ спрос/похвала» и «post ↔ chain» в реплике-споре — честно названы, в знаменателе | плановая работа тимлида | — | — | (z) addendum 2 (файл, sha, «коммит по пути, не править»); PHASE v16 §2; STATUS 17:00. Паттерн инструмента (в PROCESS не нужен — тимлид-сторона): дамп тредов для эталона читает АРМ ИЗ ФАЙЛА ДРО (`draw.<stratum>.holdout`), а не захардкоженный список (03.09 список dev-40 был вбит руками) — `data/annotation/dump_holdout2_threads.py`, счётчик текстовых комментов сверяется с `n_comments − n_wordless` дро до разметки |

## 4. 17:45 — s32 принят (aa); верификатор ДО покупки holdout-2: остановки, которых не случилось (тот же разбор, §10 скилла)
| # | находка | класс | корень | предвидимо файлом фазы? | что вошло |
|---|---|---|---|---|---|
| (aa)-1 | РИСК: вторая `--register` на якорённой линии отказывает, как только линия проглотила копейку (капля тома через границу часа, убитый рунгом-1 под): `cap = min(cap, step_remaining, remaining)`; пути восстановления самого ранбука (сдвиг цены в §0 → «перечитать rung 0 регистрацией», повтор STOP через час) уперлись бы в него | деньги/процесс | (v)/(y) назвали «линия на ран» и «регистрация открывает линию», но не «регистрация — одна на линию»: якорь делает остаток линии, а не кап, её мерой | да — каталог развилок (b) «второй экземпляр чего угодно» покрывает и вторую регистрацию; не было записано | PROCESS «Деньги» v2.2 (повтор регистрации = новая линия `<line>-r2` или `--cap` = остаток линии, слово оператора); PHASE v17 §6.1; ранбук — 1 строка в начале платной сессии; скилл v3.12 §3.4(g) |
| (aa)-2 | НИТ: пин-чек §0a печатает «PIN MOVED — STOP» с exit 0, не сцеплен с коммитом и стоит ПОСЛЕ якорящей `--register` | процесс | «гейт» написан как заметка человеку; в платной сессии человека нет — есть только код возврата | да — §7 «гард, которого никто не видел отказывающим, проверяется в обе стороны» — проверки ранбука не считались гардами | PROCESS v2.2: гейт ранбука = команда с exit ≠ 0 и `&&`, ДО шага, который сторожит; PHASE v17; скилл v3.12 §7 |
| (aa)-3 | НИТ: при spent == cap (сработал жёсткий стоп) пост-рановая `--note` отказывает; ранбук молчит, что закрытие тогда идёт по walk (v13) | деньги | ветка «жёсткий стоп сработал» не была прописана как штатная | да — (f) «линия без пост-ранового чтения закрывается по walk» уже в каталоге, ветка не названа в ранбуке | PROCESS v2.2 (не повторять `--note`, `--close` по walk); PHASE v17; ранбук — 1 строка |

Итог дня (17:50): 4 сессии исполнителя (s29–s32), 1 остановка (s29, предвидимая, тимлида), 2 плановых чтения ((z), (aa)); верификаторы до покупки: 1 укус + 2 риска (утро), 1 риск + 3 нита (вечер) — все в закон проекта до покупки, ни один не стоил сессии; закон исполнителя: +0 тестовых файлов (один тест расширен), +0 гардов; тимлид: эталон holdout-2 вслепую (112 строк). Паттерн для карточки (v3.12): «регистрация — одна на линию: повтор после якоря = новая линия», «гейт ранбука — команда, способная уронить сессию, до шага», и то, что уже есть в (f): «жёсткий стоп сработал → чтение может отказать, закрытие по walk — прописано ДО покупки».

## 5. 20:05 — s33, платный выстрел holdout-2: одна остановка внутри сессии (харнес) + плановое чтение (bb), КРАСНЫЙ по субъекту
| # | событие | класс | корень | предвидимо файлом фазы? | что вошло |
|---|---|---|---|---|---|
| s33-1 | классификатор auto-mode харнеса дважды отклонил `runpodctl pod create` (команду и скрипт с ней); открытый стоп в PROGRESS, снят словом оператора «запускай» — $0, ≈ 16 мин возраста якоря при ОТКРЫТОЙ линии | харнес / процесс | разрешение на платную необратимую команду ни разу не проверялось за $0: prep-сессия делала dry-run и регистрацию, но не «прогон разрешений» самой команды create | да — §5 «до первой покупки всё прогоняется за $0» покрывает и разрешения харнеса; не было записано | PROCESS «Executor conventions»: разрешение на команду create (allow-list харнеса) проверяется в prep-сессии за $0 — `--help`/заведомо отказывающий вызов, — и ранбук §0 показывает, что оно есть; скилл v3.13 §5 |
| (bb) | второй холдаут подряд ≈ 0.71 по субъекту (0.7181 → 0.7054) при dev-баре 0.8857: закон пять раз тюнился на промахах dev-40 — бар стал фитом, а не прогнозом; 15 из 33 промахов — ничьи самого эталона (`unsure`), где кодбук допускает два чтения («нет в наличии» — товар или магазин; свой канал — пост или сеть) | плановое чтение | (1) эталон с 32 % ничьих измеряет двусмысленность кодбука не меньше, чем инструмент; (2) шестая итерация того же закона — не ответ; другой класс инструмента (детерминированный слой) меряется за $0 до покупки | частично — §6 триаж (2) «эталон и грейдер» велит смотреть на эталон, но не велит СЧИТАТЬ ничьи до вердикта; каталог развилок не имел ветки «красный холдаут второй раз» | рулинг (bb) + addendum: P1 за $0, holdout-3 только при зелёном $0-чтении; PHASE v18 §2/§6.2; скилл v3.13 §6: ничьи эталона считаются ДО вердикта, вердикт с двумя числами; второй красный холдаут → другой класс инструмента, замер за $0 до покупки |

Итог дня (20:15): 5 сессий исполнителя, 2 остановки (s29 деньги/процесс; s33 харнес — обе предвидимые, обе в закон), 3 плановых чтения; деньги дня: итерация 5 $0.9396 + holdout-2 $0.4232 = $1.36, ноль потерянных; закон исполнителя: +0 гардов, один тест расширен; закон тимлида: PROCESS v2.2, PHASE v16→v18, скилл v3.12 (сохранён) → v3.13 (предложен).

## 6. 21:45 — s34 «p1-prep»: плановая развилка ($0-гейт покупки не пройден) + красный гейт на КАРТУ («объясни, я запутался»)
| # | событие | класс | корень | предвидимо? | что вошло |
|---|---|---|---|---|---|
| s34 | таблица решений PHASE v18 §6.2 сработала как записана: dev-3 0.7500 < 0.80 → выстрел holdout-3 не куплен, развилка вернулась к оператору за $0; исполнитель ничего не купил и остановился ровно там, где велено | плановое чтение | — | да, по построению — это и есть работа таблицы | рулинг (cc): принято; слово оператора (а) — отгрузить как измерено |
| карта | оператор на вторую таблицу решений подряд ответил «объясни мне детально, я запутался»: таблицы были написаны на языке тимлида (K8 v3, dev-3, «ничьи», «$0-гейт») — три ветки, у каждой цена и срок, но ни одна не объясняла, ЧТО измеряется и почему число такое | процесс (карта, §8) | STATUS-строка «Честно» описывала механику (сожжённый холдаут, популяция r2), а не смысл чисел; сложный день (три чтения, две развилки) ушёл в рулинги, а не в карту | да — §8 «дезориентация — красный гейт на процесс, чинить карту» | «Честно» в STATUS переписана простым языком (что измеряем, откуда 0.71/0.75/0.80, почему правилами не дотянуть); объяснение с нуля отправлено оператору перед повторным вопросом; скилл v3.14 §8: таблица решений — на языке оператора (что измеряется, что покупает каждая ветка, чего стоит, рекомендация первой); таблица, о которой оператор спрашивает, переписывается до ответа |

Итог дня (21:50): 6 сессий исполнителя (s29–s34), 2 остановки (s29 деньги/процесс; s33 харнес), 4 плановых чтения/развилки ((z), (aa), (bb), (cc)), 1 красный гейт на карту (тимлида); деньги дня $1.36 (итерация 5 $0.94 + holdout-2 $0.42), $0 потеряно; S2 закрыт как измерено; закон исполнителя: +1 модуль P1 с одним тестом, +0 гардов; закон тимлида: PROCESS v2.2–2.3, PHASE v16→v19, скилл v3.12 сохранён, v3.13/v3.14 предложены.
