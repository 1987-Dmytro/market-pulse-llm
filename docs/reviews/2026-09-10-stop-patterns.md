# Разбор остановок 10.09.2026 — паттерны (скилл v3.21 §10: один файл в день, ≤5 строк на рулинг)

## §1 (11:40) — (mm), спека «w3» по ИЗМЕРЕННОМУ слою; находка №2 — лента реакций на экране ПУСТА

| # | остановка | класс | корень | предвидимо файлом фазы? | что вошло |
|---|---|---|---|---|---|
| (mm)-1 | не остановка — плановое чтение тимлида: `windows` (w1/w2), позиции по окнам (145/1113, 1253 уникальных, 5 общих), c3 = 57 строк в `data/derived_w2/position_rows/+Ejz…jsonl`, `build_aggregates.py` читает их и `keep()` выбрасывает (окно = pinned population ids регистрации; у c3 нет addendum и пина реестра), `tick.py --window` строит ОДНО окно | плановое чтение | — | — | фича `w3` в PHASE v29 §2 с проверкой (адденда по ноге → третье окно через свою печать → экран по ВСЕМ окнам, дедуп по `(carrier,row_id)`, паузные каналы вне экрана, плохой id → exit ≠ 0) |
| (mm)-2 | девиация тимлида (09.09, (kk)5): `--window all` взят из §4 файла фазы — обещание механики, которую никто не построил и не проверил | процесс (тимлида) | §4 «constraints» нёс команду без фичи и без проверки | да — §5 скилла «каждое утверждение о файле проверено открытием» относится и к файлу фазы | обещание стало фичей `w3`(iii); паттерн уже в скилле (v3.21 §5/§6) — нового нет |
| (mm)-3 | предвидимая развилка (не сработала — поймана до выпуска): `register_addendum()` пишет `WINDOW_ID = "w2"` и `dated = "2026-09-03"` константами — `repoint()` их не перевязывает; для c3 адденда была бы ЛОЖНОЙ записью | развилка (c) каталога, второй раз в проекте (s38: `register()` называл файлы C2) | поле решения записи не ветвится по ноге | да — §3.4 (c) | в фиче `w3`(i): четыре поля ветвятся по `--step`, чужой шаг → отказ; дефолт каталога подтверждён, в скилл ничего |
| (mm)-4 | **находка продукта (тимлида): `results/promo_signals/` не существует, шесть таблиц 0/0/0/0/401/0, `screen.feed = []`; экран и `draw_truth_20` печатают «not bought», хотя 120 тредов куплены dev-петлёй под отгруженным законом (`d598573`) и `promo_p1_apply.loop()` уже строит записи в форме, которую читает `tick.signal_records`, — но никуда не пишет** | «куплено ≠ на экране», второй экземпляр (слой S2) | замер (s36) шёл функциями продукта, но мимо хранилища продукта | да — §2 скилла «graded instrument = pipeline END TO END» уже требовал этого 08.09; файл фазы не нёс фичу «лента наполняется купленным» | фича `s2-promote` ($0) ПОСЛЕ `chain-fold` (id режутся один раз под свёрнутым законом); гейт 12.09 увидит реальные реакции там, где треды прочитаны; в скилл — ничего нового (v3.21 §2/§6 держат) |
| (mm)-5 | ловушка, предупреждённая в спеке: ключ `rollup` без окна + `INSERT OR IGNORE` — первый тик по `all` на старом хранилище молча оставил бы значения «только w2» | дефект (латентный, продукт) | «per-week fact» без окна в ключе | да — теперь в `w3`(iii): первый тик по `all` — на свежепостроенном хранилище | механика проекта → файл фазы; в скилл — нет |

Уроки дня (11:40): один паттерн, два экземпляра — **«куплено» и «на экране» — два шага, и файл фазы должен нести второй как фичу с проверкой** (w3 для позиций, s2-promote для реакций); скилл v3.21 это уже говорит (§2/§6) — сегодня он не меняется, меняется файл фазы. Предвидимых стопов исполнителя: 0 (обе развилки закрыты до выпуска).

## §2 (12:50) — (nn), ПЕРЕСПЕКА словом оператора: «к пятнице проект полностью готов и рабочий, React-фронт, презентабельно»

| # | остановка | класс | корень | предвидимо? | что вошло |
|---|---|---|---|---|---|
| (nn)-1 | красный гейт на КАРТУ (скилл §8): «мы опять возвращаемся на исходную точку» — оператор не видел в карте финиша ПРОДУКТА: блок FINISH отвечал «когда гейт», а гейт был чтением 20 строк со статичного экрана; заказчик же считает финишем то, что запускает и показывает (PRODUCT.md + план фазы 6 + упаковка для LinkedIn, ни один из них не был в карте) | процесс (тимлида) | определение «готово» в файле фазы = промежуточный артефакт (экспорт + статичный экран), а не продукт в его рантайме; план фазы 6 (командный центр, рулинги 15.08) лежал в docs и не попал в карту после переспеки 27.08 | да — §2 скилла «phase-close gate: the operator SEES the system's output» читалось как «экран из файлов», а не «то, что заказчик запускает» | новый файл фазы `PHASE-ship-1.md` с тремя запускаемыми артефактами в §1 и датой; STATUS «Финиш» называет команду запуска и адрес показа; **скилл v3.22 (предложен): §2 «артефакт закрытия фазы — то, что заказчик ЗАПУСКАЕТ/ОТКРЫВАЕТ в его рантайме; гейт на промежуточном артефакте — веха», §8 «FINISH называет команду запуска и адрес показа»** |
| (nn)-2 | не остановка — три развилки закрыты за один клик (хостинг, объём фронта, смысл «vision») до записи спеки; дизайн-бриф написан ДО первой фронт-сессии, и §4 файла фазы объявляет UI-развилку НЕ стопом | предвидимый стоп, не случившийся | фронт без брифа = десяток развилок в первой же сессии | да | паттерн уже в скилле (§3.4 «форк-каталог до первого элемента»); проектная механика — `docs/DESIGN-ship-1.md` + PROCESS v2.7 |

Урок дня (12:50): **определение «готово» — это то, что заказчик запускает, а не то, что тимлид принимает**; карта, в которой нет команды запуска и адреса показа, дрейфует в гейты. Предвидимых стопов исполнителя: 0.

## §3 (14:10) — (oo), s44 «w3» ПРИНЯТ ($0, 34 мин, 0 стопов); одна девиация — тимлида

| # | остановка | класс | корень | предвидимо? | что вошло |
|---|---|---|---|---|---|
| (oo)-1 | не остановка — зелёная приёмка на моих прогонах (145/1113/57; 1 301; K10; отказ на `w9`; дро S1 не тронуто) | плановое чтение | — | — | галочка в PHASE-ship-1 §2, STATUS |
| (oo)-2 | девиация ТИМЛИДА, пойманная исполнителем и названная в PROGRESS («named, not built»): порог chain-fold «79 = 22 + 57» — 22 было измерено на w2, а проверка бежит на объединении окон (w1 несёт ещё 4 пост-позиции `marketopt_promo` → 26 → 83) | развилка (число в проверке), тимлида | число для проверки выведено на ОДНОЙ популяции, а проверка бежит на ДРУГОЙ | да — §3.5 скилла «предпосылка рулинга измерена на реальных входах» относится и к числам в проверках | порог исправлен в тот же день; паттерн уже в скилле (§3.5: измерять на входах, на которых побежит проверка) — в карточку ничего нового; проектная механика: PHASE-ship-1 v2 |
| (oo)-3 | не остановка — два решения исполнителя по §4.2 (исключение паузных каналов и в трендах; читатели-«братья» `promo_by_chain`/`coverage` пусты на `all` — латентно) | развилка, закрытая без стопа (§4.2 работает) | — | да | подтверждено рулингом; починка братьев и определение очереди — в «serve-loop» |

Урок (14:10): каталог §4 «UI/дизайн-развилка — не стоп» сработал в первой же сессии: исполнитель решил две развилки сам, назвал их и пошёл дальше — 0 стопов. Порог в проверке считается на той популяции, на которой побежит проверка.

## §4 (15:40) — (pp), s45 «chain-fold» ПРИНЯТ ($0, 25 мин, 0 стопов); девиация — тимлида (чтение набора на живом дереве)

| # | остановка | класс | корень | предвидимо? | что вошло |
|---|---|---|---|---|---|
| (pp)-1 | не остановка — зелёная приёмка (83/0; числа качества не сдвинулись; K10; `truth_20` байт в байт; тест в обе стороны) | плановое чтение | — | — | галочка, STATUS |
| (pp)-2 | девиация ТИМЛИДА: мой `make check` по w3 бежал на ЖИВОМ дереве 14:05–14:18, оператор запустил s45 в 14:09 — старый загрузчик реестра встретил новый `chain_aliases.yaml` посреди прогона → 8 ложных падений `test_tick`; чтение пустое | процесс (тимлида) | второе чтение снято на дереве, которое пишет другой писатель | да — PROCESS уже говорил «never beside the executor's», но не говорил КАК: рабочее дерево принадлежит исполнителю с момента вставки промта | PROCESS v2.8: набор тимлида бежит на снимке (`rsync -a` С `.git` → `/tmp/mp-snap`, `make check`; снимок без `.git` дал 297 ложных «not tracked» — гейты `git ls-files` продюсеров); **паттерн в скилл (v3.23, батчем в конце дня): второе чтение снимается на СНИМКЕ принятого коммита, никогда на дереве, которое может тронуть другой писатель** |
| (pp)-3 | не остановка — исполнитель назвал 5 долгов (гард карты, охват `chain_key`, пере-свёртка хранимых субъектов, sha P1-файлов, братья-читатели) и пошёл дальше | §4.2 сработал | — | — | (pp) 2: один долг не дефект, два приняты как форма, два уехали в «serve-loop» |
| (pp)-4 | девиация ИСПОЛНИТЕЛЯ (процесс): `sed -n` по трём файлам тимлида до перечтения §4.5 — отказа не было (deny только на Edit), ничего не записано | процесс (исполнителя) | правило известно, порядок чтения нарушен | да — §4.5 файла фазы | без действия; отчёт `docs/reports/chain-fold.md` 59 строк > кап 30 — метрика ретро |

Урок (15:40): второе чтение тимлида — на снимке; живое дерево после вставки промта — чужое.

## §5 (16:40) — (qq), s46 «s2-promote» ПРИНЯТ ($0, 34 мин, 0 стопов); девиация — тимлида (порог напечатан, не измерен)

| # | остановка | класс | корень | предвидимо? | что вошло |
|---|---|---|---|---|---|
| (qq)-1 | не остановка — зелёная приёмка (118 записей; 414/184/281/100/0/571; K10; лента 281; фраза на экране; числа качества на месте) | плановое чтение | — | — | галочка, STATUS |
| (qq)-2 | девиация ТИМЛИДА (второй раз за день после «79»): в проверке стояло «unsure > 0» — число, которое никто не измерял; на этих 120 тредах читатель не выдал ни одного `unsure`; исполнитель назвал это девиацией проверки и пошёл дальше (§4.2) | развилка (число в проверке), тимлида | порог там, где должен стоять счётчик | да — §3.5 (измерять на реальных входах) и (oo)-2 сегодня же | клауза «counted»; **паттерн в скилл (v3.23, батч): число в проверке — либо измерено на входах, на которых проверка побежит, либо это СЧЁТЧИК без порога; порог, которого никто не мерил, — девиация тимлида** |
| (qq)-3 | не остановка — продуктовая правда для гейта: прочитанные треды (с комментариями: Varus, агрегатор) почти не пересекаются с позициями из листовок (1 из 1 301) → 20 строк гейта скажут «в очереди», реакции — на вкладке Реакції | плановое чтение | два слоя продукта живут на разных популяциях | да — сказано оператору заранее, в STATUS «Честно про гейт» | STATUS; гейт читает и 20 строк, и ленту |
| (qq)-4 | средовое падение в моём снимке (`test_baselines`, путь репо) — не дефект, названо в PROCESS v2.8 | процесс (тимлида) | инструмент завязан на абсолютный путь | да | PROCESS: одно известное средовое падение снимка |

Урок (16:40): два раза за день число в проверке было напечатано, а не измерено (79, «unsure > 0») — правило одно: измерь на входах проверки или пиши счётчик.

## §6 (17:15) — (rr), «positions-gold» сделан (эталон 180 строк, sha `44242b2a…`); бар S1 КРАСНЫЙ (0.2333 · 0.9524 на 42); дефект — исполнителя (03.09): грейдер ни разу не встретил свой файл предсказаний

| # | остановка | класс | корень | предвидимо? | что вошло |
|---|---|---|---|---|---|
| (rr)-1 | не остановка — эталон записан вслепую по закону извлечения (кодбук позиций v1 в шапке файла: одна строка на ПРЕДЛОЖЕНИЕ, бренд как напечатан, продукт = линейка или категория закона, объём как напечатан; связки «tie:» в note), 46 страниц за ~2 ч 40 мин, sha и число строк — в рулинге ДО открытия предсказаний | плановое чтение | — | — | файл эталона, рулинг (rr) 1 |
| (rr)-2 | ДЕФЕКТ продукта (исполнителя, s6 03.09): `grade_positions.py` сопоставляет по `volume`, а `positions_50_predicted.jsonl` пишет `size_value`/`size_unit` (и `badge_pct` вместо `discount_pct_printed`, без `price_old`) → грейдер как есть даёт 0.0 и «205 не сопоставлено» без единого слова о причине; грейдер писали и тестировали ТОЛЬКО на синтетическом эталоне | продукт (исполнителя) | читатель, написанный до появления входа, ни разу не прогнан по реальному входу — «прочитал образец, не схему» | да — одна команда 03.09 (`--gold <пустой файл>` → «205 unmatched») показала бы поле | пункт «s1-grade» (один тест в обе стороны, файл оценки по пути); **паттерн в скилл (v3.23, батч): читатель, написанный раньше своего входа, прогоняется по РЕАЛЬНОМУ входу в день, когда вход появился — он читает схему, а не образец** |
| (rr)-3 | не остановка — честное красное измерение: полнота 0.2333 (42/180), цена 0.9524 на найденных, по бренду+объёму 0.756; потеря — в поле «продукт» (свободный текст вместо линейки/категории), дрейф написания бренда, выдумки на мелком шрифте; новой вычитки в фазе нет → бар публикуется КРАСНЫМ со своим файлом; ни эталон, ни правило, ни бар не ослаблены | плановое чтение | модель не держит формат поля `product` закона | да — S1 был «ещё не измерен» в STATUS с 30.08 | STATUS «Честно, по-простому»; Якість-вкладка покажет красное число (front-1) |
| (rr)-4 | не остановка — снимок s46: 4333 + 2 skip + известное средовое падение; исполнитель ещё не запустил «serve-loop» (ps/HEAD/PROGRESS в 16:40 и 16:58) | процесс | — | — | рулинг (rr) 4; промт v2 без изменений |

Урок (17:15): красное число со своим файлом лучше зелёного без файла — эталон, правило и бар остаются как были; дефект грейдера — один тест и запуск по реальному входу, не ослабление сопоставления.

## §7 (20:15) — (ss), s47 «serve-loop» ПРИНЯТ ($0, 34 мин, 0 стопов); девиация — процессная, повтор (pp)-4 → промт v3

| # | остановка | класс | корень | предвидимо? | что вошло |
|---|---|---|---|---|---|
| (ss)-1 | не остановка — зелёная приёмка на моих прогонах (`/api/status`, 422 ×3 с файлом байт в байт, тик-кнопка K10, экспорт по имени / `..` 404, только 127.0.0.1, демон `--once`, plist, облако `[]`, эталон в HEAD с тем же sha) | плановое чтение | — | — | галочка, STATUS |
| (ss)-2 | девиация ИСПОЛНИТЕЛЯ (процесс), ВТОРОЙ раз за день: `cat`/`sed`/`awk` по файлам тимлида до перечтения §4.5 — правило «читать через Read tool» лежит в файле, который читается ТРЕТЬИМ по списку промта | процесс (тимлида — по корню) | ограничение на КАК читать стоит после списка ЧТО читать | да — после (pp)-4 | **промт v3: правило чтения — первая фраза; коммит по пути перечисляет файлы тимлида (в т.ч. `docs/labels-*.jsonl`); паттерн в скилл (v3.23, батч): стандартный промт называет дисциплину чтения ДО списка чтения** |
| (ss)-3 | не остановка — «две трубы в одной клаузе»: строка фазы говорила и «из счётчиков тика», и «= `screen.threads`»; исполнитель выбрал (qq) 6 и назвал | развилка (тимлида — устаревшая формулировка) | правка (qq) 6 не дошла до всех мест строки | да | PHASE v6: первая формулировка вычеркнута |
| (ss)-4 | не остановка — `make check` дольками (5 срезов, объединение доказано равным списку файлов), потому что один вызов > потолка харнеса | процесс (исполнителя, названо) | набор растёт (4339) быстрее потолка вызова | да | PROCESS v2.9: дольки — механика исполнителя при доказанном объединении; один вызов — снимок тимлида |
| (ss)-5 | не остановка — «named, not built»: демон без юнит-теста (проверен `--once`, две ветви вызваны вручную), устаревшая фраза в `tick.SCHEDULE_DEFAULT`, якорь w1 — datetime, w2/w3 — date | плановое чтение | — | — | PHASE v6: вкладка Петля не рендерит note, фраза правится в front-1 прозой, адаптер форматирует обе даты |

Урок (20:15): одна и та же девиация дважды — это не девиация исполнителя, а место в промте; ограничение «как» ставится перед списком «что».
