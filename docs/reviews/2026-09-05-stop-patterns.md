# Паттерны остановок 05.09 — (q), s19: нотис холдаута (плановая остановка §6.2, деньги + слово оператора)

| # | остановка | класс | корень | предвидимо файлом фазы? | что вошло |
|---|---|---|---|---|---|
| (q) | s19: dev-бар взят (0.8714 / 0.9104), открытый стоп — «холдаут: стрелять ли, кто морозит закон, за сколько» | плановая (§6.2) + деньги | забор холдаута $0.30 — оценка §6.1 по заимствованной ставке 23.76 с/тред, ни разу не переоценённая по измеренному темпу (2402 с на медленном поде; holdout-40 на 34 % длиннее dev); (k)2 планировал сдвиг закона МЕЖДУ dev-баром и холдаутом | остановка — нет (по правилу §6.2 обязательна); её ДВА вопроса — да | PHASE v7 §6.1–6.2: забор будущего платного шага — оценка, переоценивается при его регистрации по СОБСТВЕННОМУ темпу самого медленного пода (хосты одной карты разнятся в 1.5–2.3× на тех же ответах), кап = жёсткий стоп; закон на заморозке = байт-в-байт инструмент, взявший dev-бар, косметика — после выстрела. Скилл v3.3 §7/§9 — те же два правила общими словами |

Итог: 1 остановка, 0 предвидимых (плановое чтение); два вопроса внутри неё файл фазы мог снять заранее — сняты v7. Закон исполнителя не вырос (+0 тестов, +0 гардов).

## 2. Вторая остановка дня — (r), s21: «holdout-prep» сделан, выстрел заблокирован на первой команде
| # | остановка | класс | корень | предвидимо? | что вошло |
|---|---|---|---|---|---|
| (r)(a) | `--register --part holdout` запечатал бы таблицу решений dev-петли (полосы GO/STOP, авторство «ruling 03.09 (b)… run iteration 1 now»); `project()` решает по литералам и дал бы KILL на ране, выпущенном FITS | развилка: продюсер переиспользован, а поля записи с решениями — нет | (q)5 параметризовал популяцию и пины, но не поля записи, несущие решения; каталог развилок §4 говорил «пути — параметрами» и молчал о решениях | да — рулингом (q)5 | PHASE §4 v8: каждое поле записи с решением ветвится по ноге или эмиттер отказывает; рулинг (r)2: гейт холдаута = FITS рунга 0 + жёсткий стоп, полосный гейт не запускается |
| (r)(b) | `--close` dev-петли отказал (33.7 % vs 5 %) | процесс: гейт закрытия построен для одноподового шага в один день | правая сторона — дельта баланса ДО последнего пода; многодневный шаг с каплей тома не закрывается по построению | частично (харнес, не файл фазы) | рулинг (r)4: леджер открыт по построению, число шага — сумма сегментов run-record; долг харнеса (brain-init): RHS закрытия = run record |

Итог дня: 2 остановки, 1 предвидимая ((r)(a) — тимлида). Скилл v3.4: §3.4(c)/§5 — «поля записи с решениями ветвятся по ноге», §7 — «гейт закрытия шага сравнивает счёт платформы с run record шага, не с дельтой баланса до последнего пода».

## 3. Третье чтение дня — (s), s22: холдаут куплен и прочитан (плановое чтение, остановки нет)
| # | событие | класс | корень | предвидимо? | что вошло |
|---|---|---|---|---|---|
| (s) | субъект 0.7181 красный при полном чтении; 21 из 53 промахов — один внедоменный тред `@matusi_ukr` (канал приостановлен в r2, но остался в замороженной популяции v1); 11 — партнёры акций без типа в кодбуке; 7 — формы слова | плановое чтение + два пробела спецификации | популяция S2 «все 678» не вычитала приостановленные каналы; кодбук не описал партнёров, внедоменные треды и каноническую форму товара вне поста | да — файлом фазы (популяция) и кодбуком | PHASE v9 §2: holdout-2 из популяции продукта (r2 `collect: true`), K8 v2 по идентичности товара; кодбук v1.2 — четыре правила; скилл: без изменений (правило «эталон = популяция продукта» — в шаблон файла фазы при следующем kickoff) |

Итог дня: 3 сессии исполнителя, 1 остановка (предвидимая, (r)), 1 плановое чтение с двумя пробелами спецификации тимлида. Закон исполнителя не вырос.

## 4. Четвёртая остановка дня — (t), s23: итерация 4 не регистрируется под кап $0.60
| # | остановка | класс | корень | предвидимо? | что вошло |
|---|---|---|---|---|---|
| (t) | `--register --part dev --cap 0.60` отказал: dear $1.31, mean $1.57 (по смок-ставке 88.8), заимствованная ставка ещё жива на dev-ноге, полосы $0.80/$1.20 над капом | деньги + процесс | (s)5 назвал кап ЧИСЛОМ без dry-run, который его ценит (§9 — команда, не число); (o)4 «заимствование в отставку» принято, хотя `rate_for("dev")` его не тронул — верификатор это написал, тимлид счёл dev-ногу закрытой; смок из трёх с самым длинным тредом — максимум, а не среднее | да — тимлидом (два раза) | PHASE v10 §6.1: на любой ноге — dear, иначе mean по среднему ЦЕЛОГО рана самого медленного пода + жёсткий стоп; кап в рулинге — цитата dry-run; скилл v3.6 §6/§9: правило, отменяющее границу, прогоняется на КАЖДОЙ ноге, которая её читает; кап — цитата из инструмента |

Итог дня: 4 остановки исполнителя (s21 (r), s23 (t)) + 2 плановых чтения; предвидимых — 2, обе тимлида. Закон исполнителя не вырос (+0 тестов сверх §4-дефектов, +0 гардов).

## 5. Пятая остановка дня — (v), s26: леджер шага отказывает итерации 4 при обоих числах
| # | остановка | класс | корень | предвидимо? | что вошло |
|---|---|---|---|---|---|
| (v) | `--step promo-dev-loop --step-cap 1.20` → $1.85 потрачено из $1.20 (поды $1.46 + капля тома $0.38 с якоря 03.09); при $2.50 остаток $0.65 < mean-угол $0.87; регистрация читала FITS, потому что `guard_reading()` зовёт гард без `--step` (видел остаток ЦИКЛА) | деньги + процесс | кап ноги записан в открытую многорановую линию; в каталоге развилок не было «денежной линии платного рана»; приёмка (u) прочитала «гард перед записью», не спросив КАКОЙ гард | да — тимлидом (третий раз за день по деньгам) | PHASE v11 §6.1: один платный ран = одна линия шага (свой `--note`, кап, закрытие по run record в той же сессии); регистрация читает гард с `--step`; скилл v3.7 §6/§7 |

Итог дня: 5 остановок исполнителя (s21 (r), s23 (t), s26 (v)) + 2 плановых чтения; предвидимых — 3, все тимлида; три из них — деньги. Диета по метрике §7 (>1 остановка/день): следующая правка — не фича, а шаблон файла фазы (brain-init): раздел «Деньги» с четырьмя дефолтами (линия на ран · ставка по целому рану · кап из dry-run · гард с --step).

## 6. Шестая остановка дня — (w), s26: итерация 4 куплена и НЕПОЛНАЯ — CUDA OOM на самом длинном рендере в смоке
| # | остановка | класс | корень | предвидимо? | что вошло |
|---|---|---|---|---|---|
| (w) | смок: 8647 (14 281 знаков) упал в `gemma4._norm`, 23.19 из 23.52 ГиБ занято; GO не выписан; $0.69, из них $0.62 — ожидание третьего ответа до дедлайна после падения раннера | дефект транспорта (сервинг) + деньги | закон v1.2 длиннее v1.1 (+правила, +примеры) — самый длинный рендер ушёл за 24 ГБ карты; раннер не сообщает о падении юнита, Мак ждёт дедлайн; каталог не говорил, что сервинг ≠ инструмент | частично: OOM — нет (памяти никто не мерил), ожидание — да (класс (a) «полнота» без «крах виден сразу») | PHASE v12 §6.5: сервинг-отказ двигает сервинг, не инструмент; ошибка юнита = ответ-ошибка, раннер выходит ненулевым, Мак удаляет сразу; перекупка — следующий номер на своей линии; скилл v3.8 §3.4(a) |

Итог дня: 6 остановок исполнителя, предвидимых 3 (все тимлида, деньги) + 1 наполовину (ожидание после краха); 2 плановых чтения. Смок-правило «самый длинный рендер первым» окупилось: $0.69 вместо ~$1.
