# HANDOFF-teamlead — вход новой тимлид-сессии (team-lead file; ПЕРЕЗАПИСЫВАЕТСЯ тимлидом при закрытии каждой его сессии)

Механика (стандарт с 14.09): тимлид-сессия в Cowork при закрытии сама переписывает ЭТОТ файл через
Desktop Commander; новая тимлид-сессия читает его ПЕРВЫМ, затем docs/STATUS.md и новейшую секцию
лога решений. Ничего искать не надо.

## Состояние на 15.09 (день) — ВСЕ исполнительские айтемы закрыты, ВИТРИНА В СЕТИ
- PHASE v24; рулинги дня: (kkk) три слова оператора · (lll) dark-default принят (поведение жило с
  `a44cad6`, s65 добавил доказательство; моя (kkk)-посылка исправлена) · (mmm) print-light принят
  (печать читана МОИМ headless print-to-pdf), закон одного писателя в PROCESS §One-writer.
- **Публичная витрина: https://market-pulse-three-zeta.vercel.app** (аккаунт `334477334477-1227`,
  проект `market-pulse`; артефакт `bf17dba3ac91fb27`; передеплой: `vercel deploy --prod --yes` из
  `dashboard/app/`). Тёмный первый рендер у каждого свежего посетителя; печать светлая.
- Suite: python не двигался с 5782a53 (замерено); vitest 49/49 мой; сеалы `e45860c6…` ·
  `eff8ba5b…` целы; экспортные пины staged = tracked. Деньги: REMAINING $1.1602; облако RunPod пусто.
- Эпизод дня: две живые исполнительские сессии на одном дереве (одна отступила образцово, вторая
  закрыта оператором после fix-коммита; ритуал закрыт тимлидом, полётный PROGRESS-блок сохранён в
  `/tmp/tl_progress_inflight_s66.diff` и по сути процитирован в (mmm)). PROGRESS не несёт s66 —
  правда s66 в рулинге (mmm).

## Осталось до гейта (всё — ТИМЛИД)
linkedin-pack → тик de-deck (PDF уже собран и выдан оператору 15.09: market-pulse-pitch-de.pdf,
9 слайдов, инвестор/немецкий) → ГЕЙТ (оператор): ср 16.09 вечером (резерв чт 17.09 12:00).

## Следующая тимлид-сессия делает
1. «linkedin-pack»: пост + скриншоты (docs/reports/screens/ + при желании кадр публичной витрины) +
   легенда — ТОЛЬКО из README и файлов результатов; в посте можно давать ЖИВОЙ адрес витрины.
2. Тики linkedin-pack и de-deck в PHASE (→v25), строка в лог решений, STATUS, перезапись этого файла.
3. STATUS всегда отвечает КОГДА: гейт ср 16.09 вечером.
4. Исполнитель не нужен; если понадобится — стендинг-промт печатает `make session`; ПЕРЕД запуском
   предыдущий процесс закрыт (`/exit`) — PROCESS §One-writer.

## Заметки среды тимлида
- NODE_ENV=production в шелле DC → `env -u NODE_ENV make front`; suite ждать порциями sleep ≤175 s,
  итог греппить по `=+ [0-9]+ passed`; brew-обнова python лечится `pip install -e '.[serve]'`.
- Печать мерить headless-хромом: `--headless --print-to-pdf --virtual-time-budget=10000`
  (virtual-time морозит count-up — нули в KPI суть артефакт замера).
- Vercel CLI залогинен device-flow (аккаунт выше); сессия оператора в браузере Arc — другой аккаунт
  (`dmytro-hordiienkos-projects`), витрина сознательно на chrome-аккаунте.

## Долги пост-гейта (до гейта не трогать)
crossfade 200 мс для сохранённой светлой · glob-асимметрия `required()` · emptiness-guard `report()` ·
«—» chip · error boundary вкладок · s1-bakeoff · добор 2 692 региональных тредов · платное чтение
очереди (548) после dry-run · перенос запечатанных цитат STATUS · даунскейл 88 МБ JPEG (витрина
живёт с оригиналами — решено «целиком сейчас») · чистка results/ (3,5 ГБ) после снятия пинов.

## Решённое, что легко забыть
- Скилл-строка v3.23 одобрена 14.09; вкладка в карту — на стороне оператора (патч в чате 14.09;
  при вкладке снять «no separate handoff documents» из §8 карты).
- `config/region_brands.yaml` оператор правит сам (ggg 5). Firebase-конфиг остаётся в репо как есть.
