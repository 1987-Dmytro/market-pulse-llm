# PHASE — `ship-1` (the finish: a working, packaged, presentable product) — v8 12.09 (ruling (vv)): the operator read front-1 as the customer and returned RED on the LOOK — the brief v1 named no reference, no flyer media, no motion (a team-lead spec defect; the technical acceptance (uu) stands); DESIGN v2 §11 (dark-first «antigravity», flyer JPEGs of the current week per chain, four mandatory animations, Якість out of the static client); NEW item «design-pass» before «front-2»; v7 11.09 (ruling (tt)): «s1-grade» ACCEPTED — the S1 bar is a committed file, RED, reproduced field by field; the Якість tab shows it with the file and the two limits in words; v6 10.09 (ruling (ss)): «serve-loop» ACCEPTED — the product runs at $0; the Петля tab never renders the schedule file's note (its «out of scope» sentence is corrected in front-1); v5 10.09 (ruling (rr)): «positions-gold» DONE on the team lead's side (180 rows, sha `44242b2a…`), the S1 bar read RED (0.2333 · 0.9524 on 42) and PUBLISHED as RED; the grader's field defect → new item «s1-grade» after «serve-loop»; «design-brief» ticked; v4 10.09 (ruling (qq)): «s2-promote» ACCEPTED; `unsure` counted, not thresholded; the status line reads `screen.threads`; v3 10.09 (ruling (pp)): «chain-fold» ACCEPTED; the team lead's suite runs on a snapshot (PROCESS v2.8); v2 10.09 (ruling (oo)): «w3» ACCEPTED; chain-fold's count is 83 (26 + 57); the sibling readers and the queue definition folded into «serve-loop»; v1 10.09 (ruling (nn)): the operator's re-spec of 10.09 12:00 — «к пятнице проект полностью готов и рабочий, React-фронт, презентабельно, упаковано»; ONE phase file for everything that remains, the tail of `promo-pulse-1` (w3 · chain-fold · s2-promote) INSIDE it as its first items; `docs/PHASE-promo-pulse-1.md` stays as the backend contract's history and its §8 DONE list (its clauses are bytes); the decisions log is the same rulings file. Every item is $0; the loop's paid reading stays WIRED and GATED (no endpoint, no purchase before the gate).

Team-lead file (process v3: launched by the standing prompt of §8, never `/goal`). Mechanics named below are CONSTRAINTS; the goal is the question and the checks. Design constraints for the front end live in `docs/DESIGN-ship-1.md` (team-lead file, read by the front items).

## 1. The operator's question and the artifact that answers it

«Проект закончен и работает?» — answered by THREE things the operator can run and show on Friday 11.09:
(a) `make serve` on his Mac → `http://localhost:8000/` opens the React command centre (T0–T8 of `docs/PLAN-phase6-command-center.md` + the promo pulse: positions over ALL windows, trends, reactions, quality/provenance, the loop with its schedule editable) with EVERY number read from result files and named with its file; `make loop` keeps the store ticking on `data/schedule.json`;
(b) `make front` → `dashboard/app/` — a static build of the same app (data embedded as files, served-mode features degrade to read-only with a visible note), deployable as the public showcase with `firebase deploy` (config in the repo, the deploy is the operator's own command);
(c) `README.md` as the product's front page (what it is, screenshots of the app, how to run, the quality numbers from result files, the fine-tuned-vs-base block, the dataset card link) — and `docs/reports/ship-1.md` ≤30 lines.
The gate: the operator opens (a), runs the 10-second quiz (each of the 7 questions of `docs/PRODUCT.md` answered from a tab in ≤10 s), reads the 20 rows of `results/truth_20.json`, and says in his own words what is right and wrong.

## 2. FEATURE LIST — the checks (ordered; ONE item per executor session, each $0; the phase is done when every box is ticked by a shown check)

- `[x] w3` — ACCEPTED 10.09 (oo), `6fb8d1b`: w1|145 · w2|1113 · w3|57, the screen 1301 rows over all windows, `--window w9` refuses — c3's 57 Маркетопт positions on the screen beside w2's 1113 — check: (i) `run_promo_c2.py --register-addendum` with the c3 flags of `knowledge/runbooks/promo_c3_paid_leg.md` §1 (`--prereg results/prereg_promo_c3.json --census results/promo_census_c3.json --pagecount results/promo_pagecount_c3.json --projection results/promo_projection_c3.json --manifest results/post_media_promo_c3.json --record results/run_promo_c3.json --step promo-c3`) → `results/prereg_promo_c3.json :: addendum[0].window_id = "w3"`, `pinned_inputs["config/registry.yaml"]` = `eff8ba5b…` (the registry has not moved since C2's pin), `dated` = today, `authority` = ruling (mm) — the four fields BRANCH on `--step` (`promo-c2 → w2`, `promo-c3 → w3`, any other step → refuse, nothing written); the existing two-way addendum test extended to both legs; (ii) `scripts/build_aggregates.py` builds `w3` through c3's OWN seal exactly like w2 (`selection_c2` with its manifest/census as parameters; `promo_census_c3.json :: window` → anchor 2026-09-05 · 28 d · since 2026-08-08; populations comment 0 · leaflet_page 30 · post_text 8; `main()` prints w3) → `sqlite3 data/derived/pulse.db "SELECT window_id, COUNT(*) FROM positions GROUP BY 1"` → `w1|145 · w2|1113 · w3|57`; (iii) `make tick` renders ALL windows — `--window all` is the DEFAULT ((d)'s `w2` superseded): positions = the union deduped by `(carrier, row_id)` (newest window wins; the 5 rows in w1∩w2 once), rows of channels the LIVE registry marks `collect: false` excluded and counted in the export (`@telegraf_kremenchuk`'s 9), `rollup`/`weeks` over the same union on a FRESHLY REBUILT store (the rollup key has no window — `INSERT OR IGNORE` would keep w2-only values), `export.window_id = "all"` + the windows with their anchors, `--window <id>` still one window, `--window <an id the table lacks>` → exit ≠ 0, a named error, NOTHING written (one test both ways — §3's standing permission); K10 holds → `results/promo_screen_data.json :: screen.positions` = **1301** rows (1244 distinct over w1∪w2 minus the paused channel + 57 — the query decides), 57 with `evidence.channel == "+Ejz6ubzm21IyMTQy"` under `chain.id = "marketopt_private"` (named, until chain-fold); `make promo-screen` green; `draw_truth_20.py --json` → `population 1301`; the S1 draw (`positions_draw_50.json`, w2, 1113, seed 42) and its gold stay FROZEN; a test that pinned `window_id == "w2"` or a count reads the record (fork (e)); `make check` green; the export and `truth_20.json` committed by path.
- `[x] chain-fold` — ACCEPTED 10.09 (pp), `b26da6d`: 83 under `marketopt_promo`, `marketopt_private` 0, no graded number moved — ONE Маркетопт on the screen and in P1 — ruling (gg)2 shape (b): `chain_of_channel` in `config/chain_aliases.yaml` (`marketopt_private → marketopt_promo`, every other own channel → its own id), read before the spellings by the one reader `chain_key`; R2/R3 write the owner's chain ID through it; the screen's `chain.id` folds through the same map — check: `promo_screen_data.json` carries NO `chain.id == "marketopt_private"` and `marketopt_promo` = **83** rows (26 + 57 — the union carries w1's 4 post positions beside w2's 22; ruling (oo) 2 corrected the 79 that was w2 alone); `results/grade_promo_loop_readings.json` re-measured before/after with shas per set (a moved number re-published with its file per (dd), the bar readings never rewritten); one test both ways; (jj)3's two runbook nits; `make check` green.
- `[x] s2-promote` — ACCEPTED 10.09 (qq), `161a901`: 118 threads, 281 feed rows, «118 of 678 read · 548 in the queue · 12 not collected», no graded number moved — the reactions the paid dev loop bought reach the screen's feed (today `feed = []`, the screen says «not bought») — check: a producer calling `promo_p1_apply.loop()`'s own functions writes the 120 thread records (dev-40 + dev-2 of `promo_dev40_iter5.jsonl`, holdout-2 of `promo_holdout2.jsonl`; the four pins of `d598573`) into `results/promo_signals/<channel>_<root>.json`, threads of `collect: false` channels excluded and counted; `make tick` → the six tables' counts printed (attribution · signal · evidence > 0, `unsure` COUNTED — 0 on these answers by measurement, ruling (qq) 2; digests for the cooled threads read), a second tick → `new 0` in every table (K10); `screen.feed` non-empty, its rows = the loop reading's rows on the same threads (`grade_promo_loop_readings.json` unchanged from chain-fold's reading — the file decides); the screen's and the gate's «not bought» sentence becomes «N of 678 threads read · M in the queue»; `make check` green; `results/promo_signals/` + the export committed by path.
- `[x] serve-loop` — ACCEPTED 10.09 (ss), `8d85ec0`: the product RUNS at $0 — `/api/status` three windows · «118 read · 548 queue» · $1.1602 from the ledger's last line; 422 on both 30-minute spellings with the file byte-identical; the tick button `new 0` ×6, export sha unmoved; the daemon's one line «queue 548 threads · reading unbought»; the sibling readers refuse `all` by name and fold the chain (one test both ways); pods/serverless/volumes `[]` — the product RUNS: `make serve` and `make loop` — check: `scripts/serve.py` (FastAPI + uvicorn, deps under `[project.optional-dependencies] serve`, pinned) serves `dashboard/app/` at `/` (a stub page until front-1 lands) and the API: `GET /api/status` (last tick from `results/promo_tick.json`, the windows with anchors from the store, `threads_read`/`queue` = the export's `screen.threads` (the earlier «from the tick's digest counts» was this file's stale wording — struck by (ss)), `money.remaining_usd` = the LAST recorded line reading of `results/spend_cycle3.json` — a file read through the guard's own ledger parser, never a live balance call, never re-spelled), `GET /api/schedule` · `PUT /api/schedule` (validated: `min_interval_hours ≥ 1`, `default_interval_hours ≥ min`; writes `data/schedule.json`; a bad body → 422 and the file unchanged — both ways tested), `POST /api/tick` (runs `scripts/tick.py` in a subprocess at $0, returns its counters and exit code; never the paid pass), `GET /api/exports/{name}` for the JSON exports the app reads; the two sibling readers `aggregates.promo_by_chain` and `coverage` (their own `WHERE window_id = ?`, silently EMPTY on `all` — ruling (oo) 3) are routed through `positions_source` or refuse `all`, one test both ways; the status line's `queue`/`read`/`population` = the export's own `screen.threads` (118 read · 548 queue · 666 product population of 678, written by `tick.thread_population()` off the draw files — ruling (qq) 6), never re-derived and never the tick's «cooled and not yet read» counter; `scripts/loop_daemon.py` = `make loop`: sleeps until `data/schedule.json` says due, runs `tick.py --if-due`, logs one line per wake to `results/loop.log`; the PAID pass is NEVER started by the daemon — when `data/loop.json :: endpoint` is empty it logs «queue N threads · reading unbought» and sleeps (a configured endpoint + the guard's FITS is the only path, and it is the operator's word, not this phase's); `ops/com.marketpulse.loop.plist` (launchd template, install steps in the README — not installed by the executor); tests: the schedule validation both ways, the three GET endpoints on a temp copy of the results, `POST /api/tick` on a temp store; check: `make serve` in the background → `curl -s localhost:8000/api/status | python3.11 -m json.tool` shows the windows and the queue; `curl -X PUT` with a 30-min interval → 422 and the file unchanged; `make check` green.
- `[x] s1-grade` — ACCEPTED 11.09 (tt), `3ba018d`: `results/grade_positions_50.json` (sha `6669663b…`) = the team lead's reading field by field — completeness 0.2333 (42/180) RED · price accuracy 0.9524 (40/42) · badge 40/42 · `price_old` not carried · 163 · 138; the pre-fix grader still reads 0.0 on the same files; one test both ways; the gold unchanged in HEAD — the S1 bar is READ by the grader on its own prediction file and the reading lands as a file (ruling (rr)): `scripts/grade_positions.py` builds the volume key from `size_value`/`size_unit` when a row carries no `volume` (a row with neither stays UNMATCHED and is counted, never a crash), reads the printed badge from `badge_pct` (fallback `discount_pct_printed`), reports `price_old` as «not carried» when the prediction has none; ONE test both ways (§3's standing permission — a row with size fields matches its gold row; a row with no size at all matches nothing and is counted); `docs/labels-positions-50.jsonl` committed by path FIRST (sha256 `44242b2acc036446fe100b4d1e60a2416a43aa82c5907d08c16228757a16d8d9`, 180 lines — printed in the commit message; the gold is the team lead's, never edited); check: `python3.11 scripts/grade_positions.py --gold docs/labels-positions-50.jsonl --predicted results/positions_50_predicted.jsonl` → `results/grade_positions_50.json` committed by path, its numbers = the team lead's snapshot reading **completeness 0.2333 (42/180) RED · price_accuracy 0.9524 (40/42) · badge 40/42 · unmatched predictions 163 · gold rows unreached 138** or the difference named in PROGRESS with its cause; the bar stays RED and is published as RED (README quality block + the Якість tab) — nothing loosened; `make check` green.
- `[ ] front-1` — the React app exists, the promo half is on it, both modes work — check (the brief `docs/DESIGN-ship-1.md` decides every UI fork; a fork it does not settle is NOT a stop: the simplest reading, named in PROGRESS): `frontend/` = Vite + React + TypeScript (versions pinned in `package.json`, `package-lock.json` committed, `npm ci` offline-safe), charts by Recharts, no CSS framework (custom properties from the brief), i18n UA/EN (dictionary file + the export's `dictionary`), light/dark (`data-theme` + `prefers-color-scheme`), URL-hash routing (`#/promo/positions?chain=…`); data adapters over `results/promo_screen_data.json` and `results/front_data.json` (NEW $0 producer `scripts/export_front_data.py`: the w1 command-centre export by reference + the code-rule «three readings» of `build_dashboard.py` (its nested rule function LIFTED to a module-level callable in the same file — moved, not re-spelled; the static page keeps calling it) + the i18n dictionary + `status`; a missing source → the producer exits ≠ 0 by name, the app renders a red «source missing: <file>» panel, never a blank or a zero); tabs Промо · Позиції (all windows; filters chain/week/brand/carrier; sort; the row's provenance ⓘ → channel/msg_id + t.me link) · Тренди (SKU price by week per chain — lines; depth by chain/brand — horizontal bars) · Реакції (the feed: signal · quote · msg_id · thread, filter by signal/chain; the S2 grade block via `build_promo_screen.s2_readings()` through `front_data.json :: s2_readings`) · Якість (S1 from `results/grade_positions_50.json` — completeness 0.2333 against 0.90 RED, price accuracy 0.9524 against 0.95, the gold's size 180 rows / 46 pages, the two limits in words per ruling (tt) 2; S2 readings with their files; ties; the honest limits — every number from a file, none typed) · Петля (status, windows, queue, schedule form → `PUT /api/schedule` in served mode; read-only + note in static mode; a «Tick now» button → `POST /api/tick`; the tab never renders `data/schedule.json :: note` — and `tick.SCHEDULE_DEFAULT`'s sentence «the schedule UI is out of scope (§6)» is corrected in the same commit, prose only (ruling (ss)); the windows' anchors arrive as a datetime for w1 and a date for w2/w3 — the adapter formats both as a date, the app rewrites no source); `make front` = `npm ci && npm run check && npm run build` → `dashboard/app/` (`base: './'`, the exports copied to `dashboard/app/data/` with a manifest of sha256s); vitest on the adapters: positions 1301 · weeks · the KPI values equal the JSON's (numbers from files, never typed); check: `make front` green, `make serve` → the five promo tabs render at `http://localhost:8000/#/promo/positions` (a screenshot of each tab in `docs/reports/screens/`), `npm run check` (tsc) clean, `make check` green (Python untouched or green).
- `[ ] design-pass` — the approved look of DESIGN v2 §11 lands on the accepted front-1 app — check: the dark theme is the DEFAULT and carries §11's tonality; `export_front_data.py` carries `positions[].media` and `flyers` (current week, one entry per chain) and `make front` stages ONLY the referenced JPEGs into `dashboard/app/media/` with their count in the manifest; the Позиції tab renders the mockup grammar (chips, four KPI cards with count-up, «Свіжі листівки» one card per chain with the real cover JPEG, row thumbnails, lightbox); the four animations of §11 run and `prefers-reduced-motion` disables them (toggle it in devtools, say so in PROGRESS); a static build hides Якість from navigation while `make serve` shows it unchanged (one test both ways on the mode flag if shaped as code); vitest: every staged media path exists and the staged set equals the referenced set (one test both ways); screenshots of Позиції dark + light and the gallery in `docs/reports/screens/`; `make front` and `make check` green.
- `[ ] front-2` — the command centre T0–T8 on the app — check: tabs T0 Огляд (KPI cards: volume · NSR · negative share sarcasm-adjusted · SoV · promo depth · coverage — each value + context line + status + ⓘ; the «3 висновки вікна» from the export's code-rule conclusions), T1 Тональність і бренди, T2 Аспекти, T3 Сегменти, T4 Конкуренти, T5 Промо і ціни (Маркетопт mandatory — links to the promo tabs), T6 Категорії (honest stub from `not_computable.category_layer`), T7 Алерти (design preview from `not_computable.alert_baselines`, no invented numbers), T8 Методологія (glossary UA/EN from the dictionary, the model and its gates with numbers, the window and populations, the provenance manifest «which screen ← which file», the limits) — every figure from `results/front_data.json :: command_center` (= `dashboard_data_w1.json` by reference), drill-down from a brand/aspect figure to the rows of its cut (the original comment beside the verdict, t.me link) where the export carries the rows, else the ⓘ says «rows not exported»; vitest: every KPI on T0 equals the export's field; check: `make front` green, the nine tabs render, the 10-second quiz list (`docs/DESIGN-ship-1.md` §9) is answered tab by tab in PROGRESS, screenshots in `docs/reports/screens/`.
- `[ ] e2e-ship` — the phase closes on a CLEAN CLONE — check: `git clone <repo> /tmp/mp-e2e && cd /tmp/mp-e2e && make check && make front && make tick && make promo-screen` green (the store absent on a clone → the tick says so and exits 0, the screen renders from the committed exports); a removed source (one JSON renamed) → `make front`'s data step exits ≠ 0 with the file's name; `make serve` smoke (`curl /api/status` 200) then stopped; `draw_truth_20.py` prints 20 rows from the committed export; `make session` prints the fenced block of `docs/PROMPT-standing.md`; `firebase.json` + `.firebaserc.example` (hosting `public: dashboard/app`, SPA rewrite) present and `npx firebase-tools hosting:channel:deploy` NOT run (the operator deploys); README rewritten as the front page (§1 c) with screenshots and the run/deploy steps; `docs/reports/ship-1.md` ≤30 lines answering §1's question in its first ten lines, every number naming its file; `make check` ≥ 4330 + the new tests; §8's porcelain empty.
- `[x] design-brief` (TEAM LEAD) — DONE 10.09 12:45 (ruling (nn)): `docs/DESIGN-ship-1.md` v1, 82 lines — tokens, layout, components, chart rules, i18n, a11y, the tab-by-data-source map, the 10-second quiz.
- `[x] positions-gold` (TEAM LEAD) — DONE 10.09 16:45 (ruling (rr)): `docs/labels-positions-50.jsonl` over the 46 pages of `positions_draw_50.json` — 180 rows on 44 pages, written WHOLE and blind, sha256 `44242b2a…` — and the S1 bar (completeness ≥ 0.90 · promo-price accuracy ≥ 0.95, promo-pulse-1 §8 (d)) READ on a snapshot: **RED — 0.2333 · 0.9524 on the 42 matched** (the item is the gold + the grader + the tab showing the reading WITH its bar; the bar's colour is the measurement, not the item); the grade file and the Якість tab land with «s1-grade» and «front-1».
- `[ ] linkedin-pack` (TEAM LEAD, 11.09) — the LinkedIn post + the screenshot set + the «how it was built» legend, from the README and the result files; nothing typed that a file does not carry.
- `[ ] gate` (OPERATOR, 11.09 evening; reserve 12.09 12:00) — `make serve`, the 10-second quiz on the app, the 20 rows of `truth_20.json`: right / wrong in his own words; then `firebase deploy` if he wants the public showcase.

## 3. Constraints (files and interfaces — not steps)

- PINNED and never moved: `src/market_pulse/prompts.py`, `scorer.py`, `brands.py`, `local_llm.py`, `window_summary_5c2.py`, the pod runners, `config/registry.yaml`/`lexicon.yaml`, every sealed record under `results/` (`dashboard_data_w1.json`, the preregs, the run records, the grades) — `make preflight` before touching anything pinned; a new export is a NEW file (`results/front_data.json`), never an edit of a sealed one. `data/derived/pulse.db` and `data/derived*/` are local derived stores, gitignored; the app ships from the committed exports.
- Numbers come from result files through the functions that already compute them (`build_dashboard.py`'s conclusions, the guard's reading, the tick's counters) — a second spelling of a function is a defect. The app computes NO figure of its own: sums, shares and readings are the exports'; the app formats, filters, sorts and links.
- Ownership: `frontend/`, `dashboard/app/`, `ops/`, `scripts/serve.py`, `scripts/loop_daemon.py`, `scripts/export_front_data.py`, `firebase.json` are the executor's; `docs/DESIGN-ship-1.md`, this file, `docs/STATUS.md`, `docs/PROMPT-standing.md`, the rulings file are the team lead's (read with the Read tool, committed by path, never edited). Node 24 / npm 11 on the Mac; every npm dependency pinned; `npm ci` from the committed lockfile; no dependency that phones home at build.
- Out of scope, one line each: model training or any pod/endpoint create; new sources or a registry revision; the alerts LOGIC (T7 stays a preview); the categories layer (T6 stays an honest stub); a schedule UI beyond the two intervals; user accounts or auth on the API (localhost only; the public build has no API); a redesign of the Python pipeline.
- **Asked for here, once for all:** a caught PRODUCT defect gets ONE test, both directions, in the same commit as its fix — no authorisation needed; every other new test, pin, guard or ledger stays forbidden (name the need in PROGRESS). Front-end tests are vitest on the data adapters and `tsc`; no browser-automation suite in this phase — screenshots are taken by hand or by a one-off script and are documentation, not a test.

## 4. Stop-points — and the forks that are NOT stops (decide, name it in PROGRESS, continue)

1. Any paid or irreversible step: a pod/serverless/template create, a `firebase deploy`, a registry revision, a move of a sealed record — STOP (the operator's word). The loop daemon never creates anything; the endpoint field of `data/loop.json` is written by the operator alone.
2. A UI/design fork `docs/DESIGN-ship-1.md` does not settle → NOT a stop: the simplest reading that keeps every number from its file, named in PROGRESS with the line the brief lacks; the team lead revises the brief the same day.
3. A library choice the brief names is unavailable or broken at the pinned version → the nearest maintained version, pinned, named in PROGRESS — not a stop; a switch to ANOTHER library is a stop.
4. A test that pinned a literal of a record that legitimately moved (a window id, a row count, the default window) → rewritten to read the record, both directions, same commit (fork (e) of promo-pulse-1 §6.6) — not a stop; weakening an invariant stays a stop.
5. The harness's deny rules on team-lead paths reach into Bash file commands: read them with the Read tool; never `cat`/`sed`/`cp` a `docs/` team-lead path — a refusal met once is handled by the documented path, never by a series of probes (PROCESS «Harness fields»).
6. `make check` runs ≥ 12 min: run it ONCE at the end of an item (unit-test the touched files as you go); `npm run check` + `vitest` are the front's fast loop.
7. Money: $0 on every item; the guard's reading is read, never re-anchored; REMAINING $1.1602 is a display value, not a budget.

## 5. Decisions log

Rulings live in `docs/reviews/2026-08-30-plan-promo-pulse-1.md` (one decisions log for the project; the newest dated section is the executor's first read). Ruling (nn) 10.09 opens this phase.

## 6. The team lead's dependencies

`docs/DESIGN-ship-1.md` before «front-1» (done 10.09 12:45); `docs/labels-positions-50.jsonl` (the S1 bar's gold, done 10.09 16:45, sha `44242b2a…`) — «s1-grade» commits it by path first and the Якість tab of «front-1» reads the grade file; the LinkedIn pack 11.09. Ownership of the gold: the team lead's (`docs/labels-*.jsonl` — written whole, never edited in place, committed by the executor by path).

## 7. Progress file and launch

`docs/plans/ship-1.PROGRESS.md` — the executor's one file (done / next / open stop, ≤60 lines), created at the first ship-1 session FROM `docs/plans/promo-pulse-1.PROGRESS.md` (carry its DONE tail and «named, not built»; the promo-pulse-1 file is then left as history, unedited). The standing prompt of `docs/PROMPT-standing.md` (v3 — ship-1 paths; the Read-tool clause first, ruling (ss)) launches every session; a fresh process each time.

## 8. DONE WHEN (the ship-1 predicate — read, never pasted into `/goal`)

Every box of §2 ticked by its shown check; `make check` green with passed ≥ 4330 + the new tests and no test file deleted; `make front` and `make serve` green on a clean clone; `git status --porcelain src tests scripts config results docs/plans docs/reports frontend dashboard` prints nothing; the operator has run the 10-second quiz and read the 20 rows and said what is right and wrong.
