# PROMPT-5c1-captions-full — the full captioning run and yield screen v2

> **VOID — never executed.** Superseded by SPEC amendment 3.13 (operator
> ruling 2026-08-08, second evening session): captions move to the project's
> own Gemma-4 vision path; the $0.35 OpenRouter cap below was never touched.
> Kept as provenance of the authorization it recorded. Do not execute.

**Contract:** team-lead authorization 08.08 evening, on the pilot's
measured numbers (ATB 0 → 13 of 25, bar A PASS on the untouched
prereg; survives brand-ablation with 10 pure-category posts; honest
measured rate $0.000948/caption). **One paid step, cap $0.35.**
The launch signing and EVERY registry action stay frozen — they are
the operator's, next session, on the v2 numbers.

## Steps

1. **Caption the remaining ~232 no-text media posts** from
   `results/image_census_5c1.json`, `caption_post` prompt, same model
   as the pilot. Own spend anchor (three constants, never imported);
   **cap $0.35** (232 × $0.000948 ≈ $0.22 + margin); one attempt,
   STOP over cap — a partial run reports what it bought.

2. **Yield screen v2** over text+captions for the WHOLE registry →
   `results/yield_screen_5c1_v2.json` (v1 record stays; `--out`
   discipline). Bars UNCHANGED — prereg sha `1aa89818…` cited.
   Controls as in v1 (ATB window control expected to pass now; Сільпо
   + negative fixture unchanged). The operator's noise ruling stands:
   @polyakova_fitness and @myrhorodtown count below bar A.

3. **Report:** v1 → v2 movement (pass_A / pass_B / below_both, per
   audience), below_both v2 VERBATIM, one quoted evidence line per
   newly-passing channel, spend from the anchor, Deviations. Then
   STOP.

## Verify · DO NOT
`make check` green. Team-lead files read-and-commit only; no registry
/ watchlist / bars edits; no Stars; never `git add -A`. Batch 1
stands.
