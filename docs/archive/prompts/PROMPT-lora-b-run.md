# PROMPT — lora-b-run: D3a tightenings at $0, then the ONE paid session (D3–D4)

**THE OPERATOR'S WORD TO START D3 IS GIVEN** (ruling 19.08 evening, relayed by
the team lead, who accepted D0–D2 and `reader-topup` with an own suite run of
**3 114 / 2** and an own guard reading: cycle-2 **$3.4171 of $20.00**,
remaining **$16.5829**; cloud listed empty from the team-lead side).

**This prompt runs in a FRESH session.** Read, in order: this file →
`docs/PROMPT-lora-b.md` (the parent contract: D3/D4 bodies, kill-clock,
DO NOT — all still binding) → `results/prereg_lora_b.json` (the commitments)
→ the handoff block in `knowledge/hot.md`. Deviations from **Dv575**, closed
enum v2, `[[wiki-name]]` lessons, five-line Process signals.

## D3a — tightenings from the fresh-context review ($0, BEFORE any pod;
## direction strictly SAFER: bars, cap and gold untouched)

1. **The mask boundary.** `learn_chars` extends THROUGH the closing quote of
   the `subject_type` value (+1 char), so a value-final token merged with
   `"` can no longer straddle out of supervision — today the straddle-mask
   erodes the tail of the one field the gate scores. Regenerate datasets and
   `results/prereg_lora_b.json`; check the commitment blocks block-by-block
   (the Dv573 pattern) and print the diff: `bars` · `population.gold` ·
   `instruments.prompt_sha256` · `money.cap_usd_all_in` ·
   `return_to_the_sitting` must be byte-identical; `go_no_go` may ONLY gain
   the rungs below; the prereg notes «tightened before any pod, bars
   untouched».
2. **The clock is CUMULATIVE.** The 7.5 h ceiling and every budget check
   count across ALL pods of this attempt, never per pod. Per-pod
   `--terminate-after` is sized so cumulative billed ≤ **5.5 h ≈ $4.40** at
   the $0.80/h ceiling — the reader-topup lesson: a hard stop must sit UNDER
   the cap it defends ($4.40 < $6.00), and it protects even a hung unit the
   between-checks gate cannot see.
3. **Training projection gate**, every log (5 steps): projected attempt
   total at the MEASURED s/step — elapsed + steps_remaining × measured +
   (arm B's fitted 5 005.9 s if not yet trained) + eval 660.7 s — must fit
   $6.00 at the live price, else KILL (the prereg's arm-A-only branch then
   applies). This closes the compliant-slow path (121 s/step never trips the
   122 s watchdog yet doubles the bill).
4. **Re-creation budget check.** The ONE allowed pod re-creation happens
   only if guard reading + worst-case-remaining at measured rates ≤ $6.00;
   otherwise the session closes with what exists. Register the honest worst
   cases beside it: **$2.63** no-incident · **$3.44** with the sanctioned
   re-creation (boot 450 + A 3 907 + B wasted 5 006 + boot 450 + B 5 006 +
   eval 661 s = 4.30 h × $0.80) · **$4.40** the cumulative hard stop.
5. **Format smoke per arm, before its eval:** ONE TRAINING-set prompt (never
   a gold or eval-pack row) through the adapter; the reply must parse as one
   balanced four-key object. Failure → that arm is NOT evaluated. If NO arm
   passes the smoke, the session closes and **the attempt is NOT spent** —
   no eval output was seen — and it returns to the team lead. This is a
   transport-format check, not a bar peek; head-only supervision can in
   principle un-teach the four-key shape, and this rung makes that a cheap
   KILL instead of an invisible zero at the bar. **The attempt is SPENT at
   the first gold-row reply generated** — write that line into the prereg.
6. `make check` green; commit before any pod.

## D3 — the ONE paid session (parent contract's body + the rungs above)

Price gate ($0.80/h ceiling, price recorded) → create → ssh dead-man 180 s →
boot-to-training ≤450 s → **arm A** (watchdog 122 s/step · projection gate
every log) → **milestone: guard reading ≤ $2.50 or arm B never starts,
pasted** → **arm B** (same rungs) → format smoke per arm → eval arm A and
arm B, EACH INTO ITS OWN OUT-FILE (the resume-skip defect Dv560), gold-14 +
census-50, base never re-run → **scp EVERYTHING before any verdict**
(replies, adapters + hashes, loss logs, pod log) → delete pod → deletion
proven by LISTING with the volume as positive control. Guard reading pasted
at every milestone. Gate records append-only. Never two billing endpoints.

## D4 — the verdict ($0)

`score_pass1_probe.bar_p1` grades; **max(gold14(A), gold14(B)) ≥ 12/14**,
one attempt, multiplicity named; tie ships arm B; red closes line B →
sitting C. Ablation table: base 9/14 (sealed per-row) / arm A / arm B,
paired on the same 14; census-50 profiles beside it as observation.
Provenance: adapter shas, seeds, config, sampler weights, dataset digests.
ADR: registration + outcome + what ships. Report
`docs/reports/lora-b-run.md`, path-only in chat. Read back FIRST, one line
each: the one-attempt gate and when it is SPENT; the smoke is not a bar
peek; the cumulative clock and the $4.40 hard stop; the projection gate; the
re-creation budget check; scp before verdict; base never re-run.

## DO NOT

Everything in `docs/PROMPT-lora-b.md`'s DO NOT, plus: never edit the
`bars` / `population.gold` / `instruments` / `money.cap_usd_all_in` blocks;
no eval output before both trainings and smokes are done; a red bar is an
answer — no retry, no tuning, no second draw. Team-lead files
(docs/STATUS.md, docs/SPEC.md, docs/PROMPT-*): commit verbatim, never edit.
