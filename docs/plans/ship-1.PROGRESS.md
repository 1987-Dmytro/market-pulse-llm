# PROGRESS — ship-1 (ruling 03.09 (e): the executor's one file — done / next / open stop, ≤60 lines)

## Done — 12.09 s51 «design-pass» ($0, §2 item 7, ACCEPTED (ww)): `f783af4` (producer) · `a44cad6` (app) · `a1bdb46` (screens)
Dark is the DEFAULT (`data-theme="dark"` in `index.html`), §11's tonality written from ONE string into both dark blocks. `media` joined out of `results/post_media_*.json` by GLOB — 402 pages · 11 flyer sets · 25 rows with no photo, read from the RECORDS, two runs byte-identical (sha `aaf9f792…`); `--stage` reported referenced 402 · staged 402 · missing 0 · 90.9 MB. Ten defects fixed before the commit (five measured in the browser, five from a read-only review). `make check` 4340/2.

## Done — 12.09 s52 «prices-and-region» ($0, operator-commissioned, ACCEPTED (xx)): `0761a70` · `a4f87d8` · `e7e3ef6` · `bea3fcb`
Three blocks in `results/front_data.json` (NOT pinned; the byte-pin `dashboard_data_w1.json` and the position row's own fields untouched): `category_prices`, `regions`, `chains.by_channel`. ONE new number — ₴/кг · ₴/л; median, quartiles and the two ends are `aggregates.spread`/`quartiles` BY CALL, no second median in the repo. Current week = 311 rows of 1301 in 13 «category × unit» groups; Полтавщина 47 cards (Маркетопт 7 · АТБ 38 · ЕКО 2 · Сільпо a sentence, never a zero). NO threshold introduced. ADR `the-price-block-is-cut-per-unit-and-the-region-is-a-chain-list`; report `docs/reports/prices-and-region.md`.

## Done — 13.09 s53 «price-fix», its READING ($0, §2 item 9, ACCEPTED (yy)): `c9ae6cb`
`read_price_plausibility.py` → 13 of 303 priced rows at x3 of their own category × unit median (the DIAGNOSTIC's band, no shipped module imports it). The three deterministic layers measured CLEAN (size units, pack markers 0 in all 308 traced answers, all 8 `rows_without_a_unit_price` are model ABSENCES), so **the figure was already wrong when it was stored**. 14 rows read × 2 independent readers, unanimous; 8 rows on 6 pages carry a figure the page does not print. Detail: the ruling and `config/price_corrections.yaml`'s own `reads:` lines.

## Done — 13.09 s54 «price-fix» BUILD ($0, §2 item 9 of PHASE v10, ruling (yy) option (c)): `5ce9499` (team-lead files) · `f6e2345` · `5b986bc` · `f655e9f` · `0f2db69` · ACCEPTED (aaa) at `821fd51`
`config/price_corrections.yaml` (6 corrections + 2 exclusions, each naming its page and its readers) applied ONCE by `export_front_data.page_true()` between the screen export and every block that shows a price. Identity is **`(row_id, carrier)`** — `row_id` repeats for 7 ids of the 1301 — and an entry matching anything but EXACTLY ONE row refuses, as does an unknown key. Export `dabcaa95…` byte-identical twice; three blocks moved and no others. The four evidence cards are page-true: йогурти 12.2571 → 49.75 · сир твердий 4993.3333 → 1247.92 · морозиво 2006.6667 → 1073.913 · сир кисломол. 100.0 → 162.5714. Window stays **311**, unit-priceless **8**, excluded **2**. ONE test both ways in `tests/test_export_front_data.py`; `read_price_plausibility.py` still reads 13 of 303. `make check` on `0f2db69`: **4341 passed / 2 skipped**. Report `docs/reports/price-fix.md`.
**Dv4 (APPROVED by (aaa) 2):** `@ATB_FANatik:4657:2` is the 750 г tube, not the 75 г сендвіч — one header, two tags; applying the accepted sentence literally would have duplicated row `:1` and lost the tube. Both that row and its sibling copy `@atb_aktsiyi:3420:2` read 200.6667 ₴/кг; no published figure moved.

## Both open stops of s53 are CLOSED — neither by me weakening anything
Stop 1 (the 8 model misreads) was ruled option (c) by (yy) 2 and is BUILT above. Stop 2 was the team lead's STATUS rewrite: the 19 sentences are restored in STATUS's machine archive, and on `5ce9499` I re-ran the four guard families myself — **156 passed**, the team lead's own number. No guard weakened, no sealed record moved, `docs/SPEC.md` untouched.

## Done — 13.09 s55 «insight-2» ($0, §2 item «insight-2» of PHASE v12, ruling (aaa) 3 — it runs BEFORE «insight-1»): `91a2447` (team-lead files) · `ca8294f` (producer) · `3b8e7e8` (app)
Start ritual: the four modified team-lead files committed by path FIRST — `91a2447`.
**The table is page-true.** `front_data.json` gains TWO blocks the producer writes through the same `page_true()`: `positions` (**1299 rows · 6 corrected · 2 excluded** of the sealed 1301) and `reporting_week`. The Позиції table's adapter reads `front_data.json :: positions`; the sealed `promo_screen_data.json` is byte-identical before and after the build — `e45860c6…` both times, K10 pin untouched — and **no other block of the export moved** (compared block by block against the pre-change file: `positions` and `reporting_week` are the only new keys, `category_prices`/`regions`/`media`/`status`/`command_center` unchanged, so no published median moved). A corrected row's ⓘ says «виправлено вручну за сторінкою листівки · було: 4,29 грн → 42,99 грн · сторінку прочитали: reader-1, reader-2, team-lead» (my own browser, served, UA/dark); the two excluded rows are gone from the table and it prints «вилучено 2: сторінка без ціни · @VARUS_channel:11300:0, @VARUS_channel:11239:0» — the category block's own sentence out of the now-shared `excluded_rows()`, one law over two populations.
**The weekly frame.** `reporting_week` = **2026-W36 · 01.09–04.09** — the newest ISO week ANY chain has leaflet pages in, and that is **Маркетопт alone: 1 chain of 11, 2 pages, 7 of the 311 current-week rows**. The banner carries exactly that reading in its own ⓘ on Позиції and Тренди; every flyer card and every regional chain prints ITS OWN week and says «тиждень раніший за звітний» when it is not the banner's (9 of the 11 do). **No aggregate was re-cut to the banner's week** — `category_prices` and `regions` still read each chain's own current week (the s52 rule), and the medians are unchanged.
Header **1 301** vs table **1 299** is the record's own law — an excluded row stays in the window's population — and the «вилучено 2» sentence reconciles them on the screen.
Fork taken (§4.2, named here): the export carries the corrected rows WHOLE (+540 KB; `front_data.json` 453 → 1303 KB) instead of a patch the app merges. `page_true()` is applied ONCE, in the producer; a second application site in TypeScript would be the second spelling §3 forbids.
Product defect caught in my own browser and fixed in the same commit (§3's standing permission): §11's staggered reveal leaves a `transform` on every row, so each row is its own stacking context and the new ⓘ panel was painted UNDER the rows below it — the row holding an open panel is now lifted (`tr:has(.popover:not([hidden]))`).
Checks: `make front` green, producer byte-identical on three runs (`b980c659…`); `npm run check` (tsc) clean; **vitest 23/23** — the two new ones are the check line's own, both ways, and each was shown RED by a producer-side negative control (`kept = list(rows)` → 1301 ≠ 1299; `min` for `max` → 2026-W33 ≠ 2026-W36); no console error from the app; the STATIC build through `python3.11 -m http.server` renders both blocks (light, Якість absent from nav per §11); the schedule grep — `dashboard/app/data/*.json` **0 hits**, the bundle's 5 + 5 + 2 hits all come from `frontend/src/tabs/Loop.tsx`, the only source file that spells them; `read_price_plausibility.py` still reads **13 of 303**. Screens: `docs/reports/screens/insight-2-{positions-week,corrected-row,trends-week,chain-weeks,static-light}.png`. `make check` on `3b8e7e8`: ruff **All checks passed!** · **4341 passed / 2 skipped in 712.13 s**, exit 0 — the same count as s54, because this item's two tests are vitest (what its check line asks for) and no Python test was added.

## Done — 13.09 s56 «insight-1» ($0, §2 item «insight-1», ruling (bbb) 3): `bb2b50e` (team-lead files) · `8fc14b5` (producer) · `5127401` (app) · `15a3331` · `71794a7` (what my own adversarial review of the diff returned)
Start ritual: the three modified team-lead files committed by path FIRST — `bb2b50e` (ruling (bbb), the §2 tick, STATUS).
**The message is the export's.** `front_data.json` gains `trends`: three readings («Три висновки тижня») and three exhibit ACTION TITLES, written by code rules in `export_front_data.py` over the very block the exhibits draw — the app renders sentences and words none. Each carries `stands_on`, the path of every figure it states, and its ⓘ prints those paths beside the file. On screen: Морозиво — медіана **287,00 ₴/кг** на 95 позиціях, найнижча **106,00** (Рудь, MSUa) −63%; найширший розкид Сир твердий **253,33 → 1 247,92**, різниця 994,59 (n=62); найнижча медіана в морозиві **227,14** проти ринкової 287,00 — MSUa (11 каналів). Every rule states the population it compared BEFORE it names what it found, and a tie is LISTED, never picked by an id.
**Two new exhibits.** The min–median–max strip — one row per (category × unit), 13 rows in 2 groups, the axis shared INSIDE one unit and never across two; the chain ranking over the week's biggest basis — 11 collected channels on their own medians inside морозиво, the market median a dashed reference line, `n` beside every bar (NO floor: a channel with one row prints its 1). `category_prices…bases[].chains` is the ONE place a per-chain median is computed — `aggregates.spread`, cut inside the basis, never across categories.
**Colour follows the chain** (s52 finding-1, measured in my browser): brand «Рудь» → «Ласунка» is a different series set with a different first row, and АТБ stays `rgb(57,135,229)`, Varus `rgb(217,89,38)`, Близенько `rgb(0,131,0)`; the fold-and-colour step is one callable (`chains.handleColour`) and `colourByIndex` is deleted with its last caller.
**Nothing leaks sideways** (s52 finding-2; both builds served and measured — elements painted past the right edge, excluding the one scroll wrapper §4 allows): BEFORE at 500 px the nav was a scroll row hiding **622 px** of tabs (`.shell` 1122 → 500); AFTER **0** on Тренди and Позиції at 500 px AND at 390 px, `documentElement.scrollWidth` = the viewport. Two defects of my own found in the browser and fixed in `15a3331`: `.chart-head` did not wrap (390 px: 6 elements past the edge → 0) and the ⓘ of the FIRST reading opened 228 px off-screen at 1440 px (1 clipped of 9 → 0 of 9 at 1440 and 390), both closed the way `.sku`/`.extreme` already close it — by the block that bounds the panel.
**My own review of the diff** (six read-only lenses, every finding put to a refuter; 30 filed, 5 survived): two of MY guards held over nothing — the colour check compared `seriesColour(id)` with `seriesColour(fold[handle])`, which is the same call, and it passed with the fold dropped and with one colour for every chain; the exhibit-id check compared the export against a list typed in the test. Both repaired in `71794a7` and each shown RED by its mutation (fold dropped · one colour for all · a renamed exhibit id). The third: the ranking's rows are collected CHANNELS and the sentence said «мереж» — corrected below.
Checks: producer twice byte-identical (`dcef7ec5…`); sealed `promo_screen_data.json` untouched (`e45860c6…`); block by block against the pre-change export — `trends` the only new key, `category_prices` gains only `chains`, medians unmoved (287 · 390,42 · 103,48); `npm run check` clean; **vitest 27/27**, the four new checks each shown red by its own negative control (the first caught a REAL defect before the commit: an exhibit title standing on a figure it does not print); `make front` green; no console error from the app. `make check` on `71794a7`: ruff **All checks passed!** · **4341 passed / 2 skipped in 727.70 s**, exit 0 (on `5127401` it read the same, 736.06 s) — the count is s55's, because this item's tests are vitest. Screens: `docs/reports/screens/insight-1-{trends-dark,spread-and-ranking-dark,ranking-light,conclusion-info,nav-500px,spread-500px,ranking-390px}.png`.
(1) **A SHRINK, not a fork — the check line is met on 3 of the 6 exhibits, and it needs a RULING.** §12's «every exhibit's title» is unqualified and §12:99 legislates the FORM of two of the three reader-filtered charts by their data's job, so the brief does reach them; I did not title them because a title per (metric, brand) pair is 272 messages in one exhibit, which §12's own «one message per exhibit» forbids — the three keep descriptive titles BELOW the exhibit block. On the word, the shape is a titles map keyed by the filter (272 + 9 rows) and I build it. Forks taken (§4.2 — named here, the line the brief owes): (2) The ranking is ONE series over a channel axis, so §12's message/context colouring applies there (the lowest median accented, the rest grey); `chains.by_channel` binds the handle-keyed charts, where s52's finding was measured. (3) DESIGN §4's «nav scrolls under 720 px» is superseded by this item's «wraps at 500 px» — dropped at every width.

## Done — 13.09 s57 «data-shape» ($0, §2 item «data-shape»): `00317c1` (the reading + the weekly artifact) · `a954cac` (the three checks) · `24e6e07` · `5c9f766` (`docs/DATASETS.md`)
**The reading is OVERWRITE.** A rerun of the weekly pipeline does not append a week to the derived rows: `build_aggregates.settle()` unlinks the database and backs a freshly built one over it, so the store holds exactly the windows the run's OWN record files describe. Shown, not read — `PYTHONPATH=src python3.11 scripts/build_aggregates.py --out $S/probe.db --quiet` → `w1|145 · w2|1113 · w3|57`; a window `w0` (57 rows) and a `sentinel` table planted with `sqlite3`; the same command again → `w1|145 · w2|1113 · w3|57`, the sentinel GONE, the two files byte-identical (`c79a365e…`). Week N+1's collection would leave week N nowhere on disk.
**So the week leaves with the tick.** `scripts/weekly_positions.py` (one line in the `tick:` target) writes `data/derived/weekly/positions_<ISO-week>.jsonl` from `aggregates.positions_source` — the one statement every reader of the positions table shares, so the weekly files and the screen are one population. **1301 of 1301 rows dated into 10 weeks, W27–W36** (28 · 34 · 23 · 39 · 6 · 296 · 276 · 286 · 306 · 7). The week is the row's own MESSAGE date: a leaflet page IS a message with its own `msg_id`, so its date lives in the post-media records and NOWHERE ELSE — no raw root can carry it, because a page's id is the image's and a post's id is the post's. `export_front_data.media_index` is their one reader and it is IMPORTED, not spelled a second time (§3); the raw store alone dates 346 of the 1301 and all 955 misses are `leaflet_page`.
Checks shown: two runs byte-identical over the 10 files; a week the store no longer carries (a planted `positions_2026-W20.jsonl`) survives untouched — that is the whole of the accumulation; a damaged week (`W33` truncated to one row) was the ONLY file the next run rewrote, every other sha unchanged and W33 back to its own bytes; `--db /nonexistent` prints and exits 0, the clean-clone case «e2e-ship» will meet. `make tick` green end to end, K10 unmoved (0 new rows in all six promo tables) and the sealed `promo_screen_data.json` still `e45860c6…` after it; `git status --porcelain results/` empty.
**The three checks the item names**, in the ONE authorized file `tests/test_export_front_data.py`: the multipack divisor both ways (6×100 г @ 89,99 ₴ → **149,9833 ₴/кг**, the same row without `pack_count` → **899,90**, six times dear) · a missing source refuses BY NAME and a complete set passes · no silent zero-row category (the cards are the REGISTRY's, so a category whose rows carry an uncuttable size keeps its card with `positions: 1, bases: []`, and one nobody promoted keeps its «немає в даних»). Each shown RED by its own mutation — the pack dropped from the divisor · the refusal returning instead of raising · the card list filtered to the observed categories. `docs/DATASETS.md`, **71 lines**: raw → derived → exports, each dataset's key, week field, provenance columns and the model that would read it; the week stated ONCE for every layer; and the two things a future model must not learn the hard way are on the page — the completeness bar is RED 0.2333, and the six corrections are an export-time overlay the weekly files do not carry.
Fork taken (§4.2, named here): the writer is a SCRIPT of its own called by `make tick`, not a function inside `tick.py` — `export_front_data` imports `tick`, so the page-date reader cannot be reached from inside it, and the alternatives were a second spelling of the manifest join (§3's defect) or a dataset missing 73% of its rows. `make check` on `24e6e07`: ruff **All checks passed!** · **4344 passed / 2 skipped in 728.60 s**, exit 0 — 4341 + this item's three.

## Next — «front-2» (PHASE §2): the command centre T0–T8 on the app — every figure from `front_data.json :: command_center`, drill-down where the cut carries rows and «rows not exported» where it does not, vitest that every KPI on T0 equals the export's field, the §1 ten-second quiz answered tab by tab in PROGRESS, screenshots. Then «e2e-ship» → the gate Wed 16.09 evening. **Open stop: none.**

## Named, not built (the phase file forbids adding what it did not ask for)
- **The weekly artifact has no test, no shrink guard, and no committed home.** The item authorised ONE test file with three named checks, so `weekly_positions.py`'s proof is the shown run, not a suite. A PARTIAL rebuild (a store carrying only some of a week's rows) would rewrite that week's file SMALLER — a refusal-to-shrink is a guard the phase did not ask for. The rows are the STORE's figures, not the page-true ones (`page_true()` is an export-time overlay over a different row shape). And `data/derived/weekly/` is gitignored, as §3 rules that whole tree: today's ten weeks are still reconstructible on a clean clone from `results/promo_screen_data.json` + `results/post_media_*.json`, a FUTURE week's would not be — a committed weekly dataset is the team lead's call, not mine.
- **The ranking's rows are collected CHANNELS, not retailers.** АТБ reaches the морозиво ranking through three (atb 17 · atb_fanatik 14 · atb_aktsiyi 13 — 44 of the 95 rows) and `msuaaaa` (an aggregator, 11) reprints АТБ's own leaflet. **The crowned median 227,1429 IS that reprint**: Рудь 70 г @ 15,90 ₴, the same offer the record also carries at `@atb_market_official:4759` and `@ATB_FANatik:4656`, while АТБ's own channel reads **257,65** in the same basis — so the published finding names the aggregator as the holder of АТБ's own price. The sentence now counts channels, the block's `reading` says what a row is and the note names both classes — FOLDING them would move a published population (POST-GATE, (yy) 5; the ADR's rule 5 already ruled this shape for the region block).
- **The «інші» grey collapses the five chains the brief gives no slot** (msuaaaa · sim23 · atb_aktsiyi · atb_fanatik · xochydeshevshe): §6 names eight and the promo data carries sixteen sources, so two named series can share one grey swatch in the week chart. msuaaaa holds this week's cheapest ice cream — a slot for it is the brief's call.
- **The pageless note's two populations.** «без фото: 25 з 1 299» — the 25 was counted by `media()` over the sealed 1301 and the denominator is the corrected block. It is whole TODAY because both excluded rows do carry a page (`VARUS_channel_11300.jpg`, `VARUS_channel_11239.jpg`); an exclusion landing on a pageless row would make the note count a row its own denominator drops.
- **No Python test for the two new producer blocks**: the item's check line asks for vitest, and §3 forbids the rest — `positions_block` and `reporting_week` are covered by the two vitest checks over the SHIPPED export, not by a unit test on synthetic rows. `adapters.test.ts`'s header rule went from two literals to three (`RECORDED_ROWS = 8`, the record's own size, with its reason).
- **The same leaflet page is collected three times** (`@atb_market_official:4767` · `@ATB_FANatik:4657` · `@atb_aktsiyi:3420`), so that tile's two offers are counted three times each in морозиво ₴/кг n=95. Pre-existing; de-duplicating moves a published population.
- `media.flyers[].positions` still counts the two excluded rows — the gallery joins pages, not figures, and the cover in the page population is POST-GATE ((yy) 5).
- **A row off a page that prints NO price is invisible to any magnitude band.** Two of the 8 were that class and both landed mid-distribution — only a hand-widened pass found them. The record closes the eight that were read; it cannot enumerate the class.
- **One offer counted twice in a price median**: `@ekomarket_shop:1469:0` and `@forainfo:6056:0` are each read off the page AND off the post text; `row_id` is NOT unique (7 ids), and a leaflet COVER sits in the page population (`@VARUS_channel:11300`). All three move published populations — POST-GATE per (yy) 5.
- Carried (two of this list are CLOSED by s56 — the by-position colour and Тренди's phone overflow): the static/served cold-start race (`load.ts`'s 1 s guard) · `quartiles()` returned q1 below min at n=2, which is why the strip draws min–median–max and no quartile band · `status.money.from` names `sessions[-1]` for a root `cap_usd` · the two id spaces · no test for `loop_daemon.py`, `promote_signals.py`, `thread_population`, the gate's third state · `spend_promo_c3.json` says `"tolerance": 0.05` where the FLOOR closed it · `promo_projection_c2.json` not reproducible · the fold map has no guard ((pp) 2(a)).
