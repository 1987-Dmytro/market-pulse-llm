# PROGRESS — ship-1 (ruling 03.09 (e): the executor's one file — done / next / open stop, ≤60 lines)

## Done and ACCEPTED earlier (detail: the rulings file and `docs/reports/`)
**s51 «design-pass»** (ww) `f783af4` · `a44cad6` · `a1bdb46` — dark by default, §11's tonality from ONE string; `media` by GLOB: 402 pages · 11 flyer sets · 25 rows with no photo, staged 402/402, ten defects fixed before the commit.
**s52 «prices-and-region»** (xx) `0761a70` · `a4f87d8` · `e7e3ef6` · `bea3fcb` — `category_prices` · `regions` · `chains.by_channel`; ONE new number (₴/кг · ₴/л) over `aggregates.spread`/`quartiles` BY CALL; 311 rows of 1301 in 13 «category × unit» groups; Полтавщина 47 cards; NO threshold introduced.
**s53 «price-fix» READING** (yy) `c9ae6cb` — 13 of 303 flagged at ×3 of their own median; the three deterministic layers measured CLEAN, so the figure was already wrong when STORED; 8 rows on 6 pages, two readers unanimous.
**s54 «price-fix» BUILD** (aaa) `5ce9499` · `f6e2345` · `5b986bc` · `f655e9f` · `0f2db69` — `config/price_corrections.yaml` applied ONCE by `page_true()`, identity `(row_id, carrier)`, an entry matching ≠1 row refuses; four evidence cards page-true; Dv4 approved. **Both open stops of s53 closed**; no guard weakened, no sealed record moved.
**s55 «insight-2»** (bbb) `91a2447` · `ca8294f` · `3b8e7e8` — the Позиції table is page-true out of a NEW `positions` block (1299 of the sealed 1301; 6 corrected with page + readers on the row, 2 excluded and COUNTED by the category block's own sentence); `reporting_week` 2026-W36 · 01.09–04.09 banners both tabs and every chain card keeps its own week; the sealed export kept its bytes and no published median moved; vitest 23/23, `make check` 4341/2.
**s56 «insight-1»** (ccc) `bb2b50e` … `71794a7` — Тренди opens with «Три висновки тижня», sentences written by code rules that carry `stands_on`; the min–median–max strip and the chain ranking with the market median as reference; colour follows the CHAIN and `colourByIndex` is deleted; 0 elements past the edge at 500 px and 390 px. My own review of the diff filed 30 findings and kept 5 — two of MY OWN guards held over nothing and were repaired. The 3-of-6 shrink is now DESIGN §12's amendment.
**s57 «data-shape»** (ccc) `00317c1` … `5c9f766` — the aggregate store is REBUILT by the tick, so a week leaves with it unless it is written out: `weekly_positions.py` lands `positions_<ISO-week>.jsonl`, **1301 rows into 10 weeks W27–W36**, two runs byte-identical, a damaged week the only file rewritten, a week the store no longer carries untouched. `DATASETS.md` 71 lines; the three authorized tests; `make check` 4344/2.
**s58+s59 «front-2»** (ddd) `1a3e0e5` · `6347058` · `0ea6bb6` · `2d8afb9` — the command centre T0–T8 over ONE block (`front_data.json :: command_center`; `src/data/cc.ts` the only reader, the app computes no figure), nine tabs answering §1's ten-second quiz. Three laws: one window is a POINT (no arrow anywhere), the headline population is the FILE's, a reading this window cannot carry is a SENTENCE. The lawful close caught **two on-screen figures no file holds** — T5's browser-divided «73,1 %» (now the count the file holds) and T8's «[object Object]» (now the arm the verdict SELECTED) — each fixed with a test shown red by its own mutation; vitest 30/30, `make check` 4344/2, export `dcef7ec5…` and sealed pin `e45860c6…` unmoved.

**s60 «weekly-home»** (fff) `d262d02` — the ten weeks W27–W36 (**1 301 rows**) live in the committed `results/weekly/` (content digest `d4fb6a44…` twice; the accepted combined digest hashed PATHS and a lawful move moved it), and T4's «Δ до свого» is the producer's `command_center_derived` field against the named own brand — the app subtracts nothing; vitest 31/31, `make check` 4344/2.
**s61+s62 «region-collect»** (hhh) `4c1c2d2`…`117e10b` — the s61 stop was ruled shape (c) and the registry NEVER moved (`eff8ba5b…`, sealed screen `e45860c6…`, 1301): `config/region_channels.yaml` carries the 18 handles, `collect_region.py` + `region_sentinel.py` are its only readers, `data/raw_region/` holds **15 889 posts · 8 232 comments**, and the baseline is an honest printed ZERO that holds under a stricter matcher; `make check` 4348/2. The comment population is a SAMPLE (1 008 of 3 700 threads) — ruled onto the screen by (hhh) 3.

## Done — 14.09 s63 «reactions-region» ($0): `99b7bc5` (team-lead files) · `4408377` (brain) · `3b20bb3` (i+ii) · `064fbf0` (iii+iv) · `fbd1f03` (screens) · `7ff8c26` (chips) · `1f3e100` (my review's fixes)
**(i) The comment SAMPLE is a file the screen reads** (ruling (hhh) 3). `collect_region.py` writes `results/region_collect_report.json` on EVERY mode, `--plan` included, so the three figures it already printed — **threads_total 3 700 · threads_read 1 008 · outstanding 2 692** — exist per channel and in total without a single Telegram call ($0). Built over all eighteen side-file channels WHATEVER `--only` names: they are properties of the store, not of one invocation, and a filtered run that rewrote the committed artifact would shrink it silently ([[a_shrunk_population_is_a_test_change]]) — shown: two `--plan` runs and a `--only @zinkivnews` run all write the same `8cf7563b…`.
**(ii) `reactions_v2` and `region`, joined out of COMMITTED result files ONLY.** 281 signals of `results/promo_signals/` (the type × subject matrix, the complaint pair **110 на мережу vs 37 на товар**, the per-channel mix, 18 monthly points, 30 thread cards of the 100 that drew a signal, 95 product-voice rows, the coverage line) and the sentinel's baseline + mentions + the new report by reference. **The dates and the post texts are NOT read from `data/raw/`**: that root is gitignored, so a producer that joined it would stop being reproducible on the clean clone «e2e-ship» runs `make front` on — the month is the PROMO POST's (`promo_threads_draw*.json :: post_date`) and the text is the pack's (`promo_*_pack.json :: items[].post`), 100 of 100 threads covered by both, and the export's own sentence says whose month it is.
**(iii) Реакції answers its question, and Промо · Регіон is a tab.** §13's page order on Реакції — question, ONE muted coverage line, three exhibits the EXPORT titles, the matrix, «Промо → реакція» cards with their root post, «Голос товару», then the full feed under chips carrying the export's own margins (pressing «sku 95» leaves «показано 95 з 281» — measured on the served page); the queue and «не збирається» moved under the answers. The feed table reads `reactions_v2.feed` — the same 281 rows `screen.feed` holds plus the subject fields §13 (5)'s toggle needs, one list and not two. The S2 block front-1 shipped stays. **Регіон** (`#/promo/region`): the instrument sentence, the baseline card whose ZERO is the producer's own sentence, the sentinel feed's three-state line, the coverage table under «коментарі — вибірка: прочитано 1 008 із 3 700 тредів (2 692 у черзі)», the link to Маркетопт. `region` is NOT `regions` one letter over — that block is the chain cut at the foot of Позиції and nothing joins them.
**Checks (mine, on the final tree `1f3e100`).** `npm run check` (tsc, strict) clean · **vitest 37/37** (31 + this item's six) · `make front` green and the producer byte-identical twice (`25566dff…`) · the sealed `promo_screen_data.json` still **`e45860c6…`** and `config/registry.yaml` **`eff8ba5b…`** · six frames dark+light in `docs/reports/screens/` (the Регіон frame carries the sample sentence IN it, not below its fold) · no console error from the app. **13 mutations of the export, 13 red**, restored green. `make check`: ruff **All checks passed!** · **4348 passed / 2 skipped in 742.44 s**, exit 0, HEAD `1f3e100` at both ends — the same 4348 (hhh) accepted, because this item authorises vitest and adds no Python test. **$0 — no pod, no volume, nothing created.**
**My own adversarial review of `117e10b..HEAD`** (6 read-only lenses → 42 findings → 3 skeptics each) earned **ten fixes** (`1f3e100`), one of which changed an exhibit: the monthly chart was a **DUAL AXIS**, the one form DESIGN §12 forbids outright — and §13 (1) makes §12's grammar this tab's. The greyed context is now a SHARE too (what part of the year's signals a month carried), so both series ride one axis. Also: `(confidence or 1) < 1` read a measured **0.0 as certainty** — the flag is now the producer's, decided once; a second draw or pack could overwrite a date it had already found with a missing one; the footer called 184 tick rows «сигнал» on a page whose own signal count is **281** (the tick's six counters are already on Петля, and the dead label went with them); a one-month series named a «highest» and a «lowest» over the same point; and the complaint pair's title could INVERT with all three of its numbers in place — the test now holds it to its ORDER in both languages, which is what caught it.

## Next — «e2e-ship» (PHASE §2, $0): the phase closes on a CLEAN CLONE — `git clone <repo> /tmp/mp-e2e` then `make check && make front && make tick && make promo-screen` green (a store-less clone's tick says so and exits 0), a renamed source → `make front` exits ≠ 0 BY NAME, `make serve` smoke, `draw_truth_20.py`, `make session`, `firebase.json` + `.firebaserc.example` present and NOT deployed, README rewritten as the front page with the screenshots, `docs/reports/ship-1.md` ≤30 lines, §8's porcelain empty. Then «linkedin-pack» (team lead) → the gate Wed 16.09 evening. **No open stop.**

## Named, not built (the phase file forbids adding what it did not ask for)
- **The month on the Реакції line is the PROMO POST's, not the comment's** — the comment's own date lives only in `data/raw/comments/`, which git does not carry, and an export that joined it would build differently on a clean clone. The two distributions differ by three rows of 281 (measured); the block's sentence says whose month it is, on the screen.
- **DESIGN §13 (4)'s «comments-first-collected date» is not on the coverage table**, because no file carries it: `region_baseline.json` holds ONE window per channel over posts ∪ comments. The table prints that window and says so; a comments-only first date is a sentinel change nobody ordered.
- **`promo.ts :: feed()` and `tableRows()` are now read only by the suite** — the tab reads `reactions_v2` instead, so one screen has one reader. Both adapters stay (deleting accepted code is not this item's business) and `adapters.test.ts` still holds them against the sealed export. The label `positions.col.evidence` DID go: it had no reader left and the orphan check says so.
- **The producer's empty-signals refusal is the file's own missing-source idiom**, not a guard the item ordered: a directory cannot be spelled in `required()`, and a page printing «118 тредів прочитано» over no rows is the blank §7 forbids. Same class as s62's empty-`own_brands` line.
- **No Python test for the two producer blocks or for the run report** — the check line authorises vitest, and §3 forbids the rest. `reactions_v2`, `region` and `region_collect_report.json` are covered over the SHIPPED export by the six vitest and by the mutations, not by unit tests on synthetic rows (the same shape s55's producer blocks stand in).
- **The comment sweep is capped at 80 newest threads per channel — 1 008 of 3 700 threads read, 2 692 outstanding**, printed per channel. Uncapped it is ~2.5 h; the store resumes for free because `todo` is «has replies, no stored comment». The baseline's COMMENT population is therefore a sample of the newest threads; the post population is complete.
- **`region_source.comments_enabled` is measured BEFORE the post walk**, so one run combining `--posts --comments` would stamp comment provenance with the pre-walk answer. The two phases were run separately here; named because the combined run is legal and one flag away.
- **Two brand blocks sharing one `brand_id` would make one mention read as two** — `mentions_in` appends per block and `mentions_per_brand` collapses the key. It is the twin of the duplicate-HANDLE case I did refuse, in a file the operator edits freely; not built because no defect was caught on it and a third refusal is not something the item asked for. Named so the asymmetry is a decision, not an oversight.
- **An overlapping dictionary yields one row per BRAND named, not per phrase** — «Яготинське для дітей» names both `yagotynske` and `yahotynske-dlia-ditei`, measured in the test. The keyword matcher behaving, not a double count; it matters the moment the operator adds a brand whose name contains another's.
- **The mention row carries a `matched` field the check's list does not name** (which spelling hit). It is what lets a human judge a false positive on «Гармонія», which is also an ordinary Ukrainian noun — the same reason every row carries its quote.
- **No stemmer, cost measured not assumed**: «миргородської корівки» is not a hit for «Миргородська корівка». On the frozen archive the stemmed variant's WHOLE delta is the 2 false hits above, so it would have bought only noise — but that does not prove an inflected REAL mention («купив Гармонію») would be caught. That miss is the rule §2 (iii) names, and it is this item's known cost.
- **The empty-`own_brands` refusal in the producer is the file's OWN missing-source idiom**, not a guard the phase asked for: a watchlist with no own brand would otherwise IndexError with no sentence, where every other refusal in `export_front_data.py` names what is missing. Named here because it is a line the item did not order.
- **Every other printed figure was grepped for arithmetic** across `frontend/src/tabs/*.tsx` and `components/*.tsx` after the Δ moved: what remains is bar geometry (`value / ceiling`), sort comparators, pagination slices and the count-up's interpolation — no second printed figure is computed in the app. Ruling (ddd) 2 states the law tab-wide but folds only T4 into this item, so nothing else was touched.
- **`SovReading.share` is typed `number` while the producer can write null** (a sample with zero mentions has no shares at all), so such a window would die at the share column BEFORE the Δ cell's null branch is reached. The branch is the new type's own consequence, not a claim that the tab survives that window.
- **The app has NO error boundary, so a named refusal never reaches the screen from inside a tab.** `must()` throws `SourceMissing(file, field)` and `App.tsx` catches only the BOOT promise; a required field missing while a tab renders unmounts the root and the page comes up blank — where DESIGN §5/§7 want a red panel naming the file. Found by s59's review and PRE-EXISTING (the same shape stood at `1a3e0e5^`, and `load.ts`'s own comment records it happening once); a boundary is not a thing this phase asked for. The overclaiming doc-comment in `cc.ts` is corrected to what the code does.
- **The second population reaches the screen on ONE of four sampled readings.** NSR prints «куплені: 7,70 %» beside its headline; T1's negative share (bought 5,38 % vs payable 7,35 %), T2's `rows_with_no_aspect` (3 944 vs 2 583) and T3's segment rows (mothers_kids 2 726 vs 1 554) name the population in the ⓘ and print one reading. No figure is wrong — `headline_sample` is obeyed everywhere — but the commit's own «and the other beside it» was true of NSR only, and the header comment now says so.
- **The weekly artifact has no test and no shrink guard.** The item authorised ONE test file with three named checks, so `weekly_positions.py`'s proof is the shown run. A PARTIAL rebuild would rewrite that week's file SMALLER — a refusal-to-shrink is a guard the phase did not ask for; now that the files are COMMITTED that shrink lands as a `git diff` instead of silently, and a store-less clone's tick writes nothing at all. Its rows are the STORE's figures, not the page-true ones (`page_true()` is an export-time overlay over a different row shape).
- **The ranking's rows are collected CHANNELS, not retailers.** АТБ reaches the морозиво ranking through three (atb 17 · atb_fanatik 14 · atb_aktsiyi 13 — 44 of the 95) and `msuaaaa` (an aggregator, 11) reprints АТБ's own leaflet. **The crowned median 227,1429 IS that reprint** — Рудь 70 г @ 15,90 ₴, the same offer at `@atb_market_official:4759` and `@ATB_FANatik:4656`, while АТБ's own channel reads 257,65. The sentence counts channels and the note names both classes — FOLDING them would move a published population (POST-GATE, (yy) 5).
- **The «інші» grey collapses the five chains the brief gives no slot** (msuaaaa · sim23 · atb_aktsiyi · atb_fanatik · xochydeshevshe): §6 names eight and the promo data carries sixteen sources. msuaaaa holds this week's cheapest ice cream — a slot for it is the brief's call.
- **The pageless note's two populations.** «без фото: 25 з 1 299» — the 25 was counted by `media()` over the sealed 1301 and the denominator is the corrected block. Whole TODAY because both excluded rows do carry a page; an exclusion landing on a pageless row would make the note count a row its own denominator drops.
- **No Python test for the two producer blocks of s55**: the item's check line asks for vitest and §3 forbids the rest — `positions_block` and `reporting_week` are covered over the SHIPPED export, not by a unit test on synthetic rows.
- **The same leaflet page is collected three times** (`@atb_market_official:4767` · `@ATB_FANatik:4657` · `@atb_aktsiyi:3420`), so that tile's two offers are counted three times each in морозиво ₴/кг n=95. Pre-existing; de-duplicating moves a published population.
- `media.flyers[].positions` still counts the two excluded rows — the gallery joins pages, not figures (POST-GATE, (yy) 5).
- **A row off a page that prints NO price is invisible to any magnitude band.** Two of the 8 were that class and both landed mid-distribution — only a hand-widened pass found them. The record closes the eight that were read; it cannot enumerate the class.
- **One offer counted twice in a price median**: `@ekomarket_shop:1469:0` and `@forainfo:6056:0` are each read off the page AND off the post text; `row_id` is NOT unique (7 ids), and a leaflet COVER sits in the page population (`@VARUS_channel:11300`). All three move published populations — POST-GATE per (yy) 5.
- Carried: the static/served cold-start race (`load.ts`'s 1 s guard) · `quartiles()` returned q1 below min at n=2, which is why the strip draws min–median–max and no quartile band · `status.money.from` names `sessions[-1]` for a root `cap_usd` · the two id spaces · no test for `loop_daemon.py`, `promote_signals.py`, `thread_population`, the gate's third state · `spend_promo_c3.json` says `"tolerance": 0.05` where the FLOOR closed it · `promo_projection_c2.json` not reproducible · the fold map has no guard ((pp) 2(a)).
