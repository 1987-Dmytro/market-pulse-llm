# PROGRESS — ship-1 (ruling 03.09 (e): the executor's one file — done / next / open stop, ≤60 lines)

## Done and ACCEPTED earlier (detail: the rulings file and `docs/reports/`)
**s51 «design-pass»** (ww) `f783af4` · `a44cad6` · `a1bdb46` — dark by default, §11's tonality from ONE string; `media` by GLOB: 402 pages · 11 flyer sets · 25 rows with no photo, staged 402/402, ten defects fixed before the commit.
**s52 «prices-and-region»** (xx) `0761a70` · `a4f87d8` · `e7e3ef6` · `bea3fcb` — `category_prices` · `regions` · `chains.by_channel`; ONE new number (₴/кг · ₴/л) over `aggregates.spread`/`quartiles` BY CALL; 311 rows of 1301 in 13 «category × unit» groups; Полтавщина 47 cards; NO threshold introduced.
**s53 «price-fix» READING** (yy) `c9ae6cb` — 13 of 303 flagged at ×3 of their own median; the three deterministic layers measured CLEAN, so the figure was already wrong when STORED; 8 rows on 6 pages, two readers unanimous.
**s54 «price-fix» BUILD** (aaa) `5ce9499` · `f6e2345` · `5b986bc` · `f655e9f` · `0f2db69` — `config/price_corrections.yaml` applied ONCE by `page_true()`, identity `(row_id, carrier)`, an entry matching ≠1 row refuses; four evidence cards page-true; Dv4 approved. **Both open stops of s53 closed**; no guard weakened, no sealed record moved.
**s55 «insight-2»** (bbb) `91a2447` · `ca8294f` · `3b8e7e8` — the Позиції table is page-true out of a NEW `positions` block (1299 of the sealed 1301; 6 corrected with page + readers on the row, 2 excluded and COUNTED by the category block's own sentence); `reporting_week` 2026-W36 · 01.09–04.09 banners both tabs and every chain card keeps its own week; the sealed export kept its bytes and no published median moved; vitest 23/23, `make check` 4341/2.
**s56 «insight-1»** (ccc) `bb2b50e` … `71794a7` — Тренди opens with «Три висновки тижня», sentences written by code rules that carry `stands_on`; the min–median–max strip and the chain ranking with the market median as reference; colour follows the CHAIN and `colourByIndex` is deleted; 0 elements past the edge at 500 px and 390 px. My own review of the diff filed 30 findings and kept 5 — two of MY OWN guards held over nothing and were repaired. The 3-of-6 shrink is now DESIGN §12's amendment.
**s57 «data-shape»** (ccc) `00317c1` … `5c9f766` — the aggregate store is REBUILT by the tick, so a week leaves with it unless it is written out: `weekly_positions.py` lands `positions_<ISO-week>.jsonl`, **1301 rows into 10 weeks W27–W36**, two runs byte-identical, a damaged week the only file rewritten, a week the store no longer carries untouched. `DATASETS.md` 71 lines; the three authorized tests; `make check` 4344/2.
**s58+s59 «front-2»** (ddd) `1a3e0e5` · `6347058` · `0ea6bb6` · `2d8afb9` — the command centre T0–T8 over ONE block (`front_data.json :: command_center`; `src/data/cc.ts` the only reader, the app computes no figure), nine tabs answering §1's ten-second quiz. Three laws: one window is a POINT (no arrow anywhere), the headline population is the FILE's, a reading this window cannot carry is a SENTENCE. The lawful close caught **two on-screen figures no file holds** — T5's browser-divided «73,1 %» (now the count the file holds) and T8's «[object Object]» (now the arm the verdict SELECTED) — each fixed with a test shown red by its own mutation; vitest 30/30, `make check` 4344/2, export `dcef7ec5…` and sealed pin `e45860c6…` unmoved.

## Done — 13.09 s60 «weekly-home» (clauses 1+2, $0): `19bfcf7` (team-lead files) · `29c972a`·`ec88cba`·`208cd65` (clause 1) · `6231349` (clause 2) · `b502484`+`5b5d34c` (T4 re-shot) · `d262d02` (my review's repairs)
**(1) The weekly dataset has a home git carries.** `weekly_positions.py :: OUT` = `results/weekly/`; the ten weeks W27–W36 (**1 301 rows**) land in ONE commit by path and are byte-identical to the set (ccc) 3 accepted (`cmp` 10 of 10); `git check-ignore` exits 1 on the path — the tree really is tracked, not silently ignored. Idempotence re-shown ON the moved home: two runs `diff -r` clean (content digest `cat positions_*.jsonl | shasum -a 256` = `d4fb6a44…` both times), and in a scratch `--out` a damaged W33 is the ONLY file rewritten (44 B → the store's 196 215 B, byte-equal to the committed one) while a planted W01 the store does not carry survives untouched and W35's bytes do not move. **The accepted combined digest `72d9d46e…` is `shasum -a 256 <paths> | shasum -a 256` — it hashes the PATHS, so a lawful move moves it by construction (`b636e842…`); the content-only digest is the one that held.** `DATASETS.md`'s row names the new home and its new price (a collected week dirties `results/` until the operator commits it), the Makefile comment says the same where the tick is defined, and the old gitignored `data/derived/weekly/` is deleted after the identity check — one home, not two.
**(2) T4's «Δ до свого» is a field (ruling (ddd) 2).** `export_front_data.command_center_derived()` subtracts from the sealed `dashboard_data_w1.json`'s own fields and the app renders it. TWO halves of the old line were the app's own: the arithmetic AND the reference — the record lists two own brands (`garmonija`, `mgarske`) ordered `ORDER BY brand_id` (`aggregates.share_of_voice`), so `own_brands[0]` was the alphabet's pick; DESIGN §7 names «Гармонія» in words, but nothing in the code or the data said which id the column subtracted, and now `reference` does (the served note reads «проти свого бренду garmonija»). The block MIRRORS `metrics.sov` — one reading per sample under the record's own `headline_sample` — so `headline()` picks the population for the Δ exactly as it does for the shares, and a null share subtracts to null, never to a 0 that would read as a measured tie.
**Checks (mine, on the final tree `d262d02`).** `npm run check` (tsc, strict) clean · **vitest 31/31** (30 + this item's one) · `make front` green · `make tick` green end to end with the sealed `promo_screen_data.json` still **`e45860c6…`** and `dashboard_data_w1.json` only READ; `front_data.json` moved as the new block requires — **`dcef7ec5…` → `742013c1…`**, disclosed — and `git status --porcelain results/` is empty after the tick. The one vitest is RED under both of its own mutations: the producer subtracting from `max(share)` instead of the named reference («expected -0.4 to be close to +0») and `row.value - ownShare` put back into the tab («expected … not to contain»). **Today Гармонія's share is 0.0000, so every Δ EQUALS the share beside it** — the digits cannot separate them and the test says so where it asserts the reference. T4 re-shot (served, light, 1440 px; no console error from the app) — and a SECOND frame, because the tab's note and its sources line sit below the 689 px fold the first frame ends at, so the shot committed as evidence of «проти свого бренду garmonija» did not contain it. `make check`: ruff **All checks passed!** · **4344 passed / 2 skipped in 686.07 s**, exit 0, HEAD `d262d02` at both ends — `d262d02` is the LAST commit of this item that touches code or data; everything after it (`547495d` PROGRESS · `5b5d34c` a PNG · `796ef75` the brain · this line) is documentation, so the reading's tree is named rather than implied ([[a_review_that_verifies_a_moving_tree]]). **$0 — no pod, no volume, nothing created.**
**My own adversarial review of `afadbda..HEAD`** (5 read-only lenses → 10 findings → 3 skeptics each, 30 votes cast, none survived) still earned three repairs of MY OWN text (`d262d02`): `DATASETS.md` claimed the ten weeks were «reconstructible on a clean clone» — MEASURED, the committed files restore each row's identity, its week and 15 of its 28 columns, and **13 nothing committed carries** (`price_old` · `pack_count` · `price_qualifier` · `discount_footnote` · `depth_disagrees_with_printed` · the five `presence_*` · `brand_raw` · `ordinal` · `window_id`); the new docstring cited `aggregates.sov_block`, a function that does not exist; and the test compared the block to itself per brand one line after asserting it IS that object. **A unanimous 30-of-30 refutation is itself a reading to distrust** — those three came from re-checking the refuted list by hand.

## Done — 14.09 s61 «region-collect» (i) READ-ONLY half, $0: `5a76196` (team-lead files by path) · `9d3ea59` (the brain sweep s60 left dirty)
**The phase-B set is PRINTED with its count and its diff (check (i), first half).** `paused_by == "deferred to phase B (ruling (ц) 30.08)"` matches **17 of the registry's 74 sources** — every one `audience: regional`, `collect: false`, `comments_enabled: false`. The list differs from §2 (i)'s expected set by ONE substitution: the phase names `dikankaa`, the registry carries **`mo3ambik`** («МОЗАМБІК.МЕДІА: Лубни, Пирятин, Хорол, Гребінка, Оржиця»); `dikankaa` is not a registry source at all, while `data/raw/posts/dikankaa.jsonl` holds 30 posts. §2 (i) rules a differing list named with its diff, not a stop — named here.
**The archive those 17 already carry (`data/raw/posts/`, my count):** 6 739 posts, 10.07–08.08, **zero comment rows**; 12 of the 17 carry `reply_count > 0` for **12 189 replies upstream** (the team lead's 13.09 reading is the same 12-of-17 shape at ~6 030 — mine counts every stored post, said here so the difference is visible, not reconciled by me). The five with none: `poltava_informue` · `suspilnepoltava` · `piryatingromada` · `LHVC_info` (1 post) · `kremen_news`.
**Two collector facts measured, NOT changed (the flip is the stop below):** `collect_r2.py :: a1_sources()` selects `source.collect and bucket(source) == "A"` and regional IS bucket B — un-pausing alone collects nothing; and `--join`/`--comments` gate on `source.comments_enabled`, `false` on all 17 while the stored posts say 12 carry replies. Both are the collector's to answer, not the registry's, and §2 (ii) already names the criterion it wants («comments arrive where posts carry `reply_count > 0`»).

## OPEN STOP — the (eee) un-pause moves the registry's LIVE bytes, and two SEALED registrations pin exactly those
**Stop-point (§4.1 «a move of a sealed record» · §4.4 «weakening an invariant stays a stop»).** The flip itself is authorized; its CONSEQUENCE is priced nowhere. Measured by applying the edit and reverting it — tree clean again, `config/registry.yaml` back at `eff8ba5b…`, `test_registry.py`+`test_aggregates.py` 47 passed:
- `config/registry.yaml` carries **24 pins inside sealed records, in four sha families** (`make preflight config/registry.yaml`). After the flip r1 (`d4e3b237…`, 7 records) and pre-(13)(b) (`920c7f20…`, 8) still reconstruct **byte-exactly** — every touched line keeps `# (r2)` at its END, which is the filter `registry_before_r2` drops by. The **LIVE-r2 family `eff8ba5b…`** (`results/prereg_promo_c2.json` + `results/prereg_promo_c3.json :: pinned_inputs`) goes **UNREACHABLE**: `registry_revisions()` has three entries and its newest IS today's bytes.
- `window_summary_5c2.registry_through_the_seal` then refuses by name and `build_aggregates.build` reaches it: **3 failed + 9 errors** over `moved_pins`+`test_registry`+`test_aggregates`+`test_window_summary_5c2`+`test_promo_tables`+`test_run_promo_c2`+`test_registry_revision_proposal`. `make check` goes RED on the flip alone.
- **Option (a), priced:** a 4th revision `registry_before_r3()` + one line in `registry_revisions()` (~25 lines) makes `eff8ba5b…` reachable again as «r2, through the r3 undo» — measured: the same subset reads **107 passed / 1 failed**, and that one is `test_the_paused_rows_are_loaded_and_are_not_collected` asserting `len(paused) == 57` where the flip reads 40 — a literal that legitimately moved, §4.4-rewritable in the same commit. No sealed record moves, no invariant weakens, and `src/market_pulse/registry.py` is pinned by **no** sealed record (preflight: 0 pins).
- **Why (a) is still yours:** ruling 04.09 (g) met this same question on a different registry edit and REFUSED a fourth revision (`R3_MARK`), taking a new one-reader file instead — «no pin moved, no revision, no preflight change». Taking (a) would overturn a standing ruling. (b) re-pinning the two sealed preregs is §4.1. (c) loosening `registry_through_the_seal` is §4.4.
**The question:** (a) add the r3 revision undo, ruling 04.09 (g) amended for this file — or (b) re-pin the two sealed preregs — or (c) a shape neither of us has named? Nothing else in «region-collect» can start: (ii) and (iii) both read the un-paused set.
**Tree state:** `git status --porcelain` EMPTY at `9d3ea59` + this commit; registry `eff8ba5b…` unmoved; **$0 — no pod, no volume, no Telegram call opened, nothing created.**

## Next — «region-collect» (PHASE §2 v16, ruling (eee), $0): PRINT the registry channels whose pause cites «phase B (ruling (ц) 30.08)» with their count (the 17 district set is expected; a different list is named with its diff, not a stop) and un-pause ONLY those, the diff shown line by line — the ONE authorized registry move, and §4.1 says it already carries the operator's word. Then `collect_r2.py` over that set (posts AND comments into `data/raw_r2/`, per-channel counts and date ranges printed, a second run appending only new rows, a comments-disabled channel a printed zero), then the NEW `scripts/region_sentinel.py` ($0, reads NO model) over v1∪r2 with `config/region_brands.yaml` → `results/region_mentions.jsonl` + `results/region_baseline.json` (a ZERO is a printed number, never an empty file), ONE test both ways. Then «reactions-region» → «e2e-ship» → the gate Wed 16.09 evening. **Open stop: the one above — (i)'s PRINT is done, (i)'s FLIP is blocked on it.**

## Named, not built (the phase file forbids adding what it did not ask for)
- **The empty-`own_brands` refusal in the producer is the file's OWN missing-source idiom**, not a guard the phase asked for: a watchlist with no own brand would otherwise IndexError with no sentence, where every other refusal in `export_front_data.py` names what is missing. Named here because it is a line the item did not order.
- **Every other printed figure was grepped for arithmetic** across `frontend/src/tabs/*.tsx` and `components/*.tsx` after the Δ moved: what remains is bar geometry (`value / ceiling`), sort comparators, pagination slices and the count-up's interpolation — no second printed figure is computed in the app. Ruling (ddd) 2 states the law tab-wide but folds only T4 into this item, so nothing else was touched.
- **`SovReading.share` is typed `number` while the producer can write null** (a sample with zero mentions has no shares at all), so such a window would die at the share column BEFORE the Δ cell's null branch is reached. The branch is the new type's own consequence, not a claim that the tab survives that window.
- **The app has NO error boundary, so a named refusal never reaches the screen from inside a tab.** `must()` throws `SourceMissing(file, field)` and `App.tsx` catches only the BOOT promise; a required field missing while a tab renders unmounts the root and the page comes up blank — where DESIGN §5/§7 want a red panel naming the file. Found by s59's review and PRE-EXISTING (the same shape stood at `1a3e0e5^`, and `load.ts`'s own comment records it happening once); a boundary is not a thing this phase asked for. The overclaiming doc-comment in `cc.ts` is corrected to what the code does.
- **The second population reaches the screen on ONE of four sampled readings.** NSR prints «куплені: 7,70 %» beside its headline; T1's negative share (bought 5,38 % vs payable 7,35 %), T2's `rows_with_no_aspect` (3 944 vs 2 583) and T3's segment rows (mothers_kids 2 726 vs 1 554) name the population in the ⓘ and print one reading. No figure is wrong — `headline_sample` is obeyed everywhere — but the commit's own «and the other beside it» was true of NSR only, and the header comment now says so.
- **The weekly artifact has no test and no shrink guard.** The item authorised ONE test file with three named checks, so `weekly_positions.py`'s proof is the shown run. A PARTIAL rebuild would rewrite that week's file SMALLER — a refusal-to-shrink is a guard the phase did not ask for; now that the files are COMMITTED that shrink lands as a `git diff` instead of silently, and a store-less clone's tick writes nothing at all. Its rows are the STORE's figures, not the page-true ones (`page_true()` is an export-time overlay over a different row shape).
- **The ranking's rows are collected CHANNELS, not retailers.** АТБ reaches the морозиво ranking through three (atb 17 · atb_fanatik 14 · atb_aktsiyi 13 — 44 of the 95) and `msuaaaa` (an aggregator, 11) reprints АТБ's own leaflet. **The crowned median 227,1429 IS that reprint** — Рудь 70 г @ 15,90 ₴, the same offer at `@atb_market_official:4759` and `@ATB_FANatik:4656`, while АТБ's own channel reads 257,65. The sentence counts channels and the note names both classes — FOLDING them would move a published population (POST-GATE, (yy) 5).
- **The «інші» grey collapses the five chains the brief gives no slot** (msuaaaa · sim23 · atb_aktsiyi · atb_fanatik · xochydeshevshe): §6 names eight and the promo data carries sixteen sources. msuaaaa holds this week's cheapest ice cream — a slot for it is the brief's call.
- **The pageless note's two populations.** «без фото: 25 з 1 299» — the 25 was counted by `media()` over the sealed 1301 and the denominator is the corrected block. Whole TODAY because both excluded rows do carry a page; an exclusion landing on a pageless row would make the note count a row its own denominator drops.
- **No Python test for the two producer blocks of s55**: the item's check line asks for vitest and §3 forbids the rest — `positions_block` and `reporting_week` are covered over the SHIPPED export, not by a unit test on synthetic rows.
- **The same leaflet page is collected three times** (`@atb_market_official:4767` · `@ATB_FANatik:4657` · `@atb_aktsiyi:3420`), so that tile's two offers are counted three times each in морозиво ₴/кг n=95. Pre-existing; de-duplicating moves a published population.
- `media.flyers[].positions` still counts the two excluded rows — the gallery joins pages, not figures (POST-GATE, (yy) 5).
- **A row off a page that prints NO price is invisible to any magnitude band.** Two of the 8 were that class and both landed mid-distribution — only a hand-widened pass found them. The record closes the eight that were read; it cannot enumerate the class.
- **One offer counted twice in a price median**: `@ekomarket_shop:1469:0` and `@forainfo:6056:0` are each read off the page AND off the post text; `row_id` is NOT unique (7 ids), and a leaflet COVER sits in the page population (`@VARUS_channel:11300`). All three move published populations — POST-GATE per (yy) 5.
- Carried: the static/served cold-start race (`load.ts`'s 1 s guard) · `quartiles()` returned q1 below min at n=2, which is why the strip draws min–median–max and no quartile band · `status.money.from` names `sessions[-1]` for a root `cap_usd` · the two id spaces · no test for `loop_daemon.py`, `promote_signals.py`, `thread_population`, the gate's third state · `spend_promo_c3.json` says `"tolerance": 0.05` where the FLOOR closed it · `promo_projection_c2.json` not reproducible · the fold map has no guard ((pp) 2(a)).
