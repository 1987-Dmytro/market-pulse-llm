# PROGRESS — ship-1 (ruling 03.09 (e): the executor's one file — done / next / open stop, ≤60 lines)

## Done — 12.09 s51 «design-pass» ($0, §2 item 7, ACCEPTED (ww)): `f783af4` (producer) · `a44cad6` (app) · `a1bdb46` (screens)
Dark is the DEFAULT (`data-theme="dark"` in `index.html`), §11's tonality written from ONE string into both dark blocks. `media` joined out of `results/post_media_*.json` by GLOB — 402 pages · 11 flyer sets · 25 rows with no photo, read from the RECORDS, two runs byte-identical (sha `aaf9f792…`); `--stage` reported referenced 402 · staged 402 · missing 0 · 90.9 MB. Ten defects fixed before the commit (five measured in the browser, five from a read-only review). `make check` 4340/2.

## Done — 12.09 s52 «prices-and-region» ($0, operator-commissioned, ACCEPTED (xx)): `0761a70` · `a4f87d8` · `e7e3ef6` · `bea3fcb`
Three blocks in `results/front_data.json` (NOT pinned; the byte-pin `dashboard_data_w1.json` and the position row's own fields untouched): `category_prices`, `regions`, `chains.by_channel`. ONE new number — ₴/кг · ₴/л; median, quartiles and the two ends are `aggregates.spread`/`quartiles` BY CALL, no second median in the repo. Current week = 311 rows of 1301 in 13 «category × unit» groups; Полтавщина 47 cards (Маркетопт 7 · АТБ 38 · ЕКО 2 · Сільпо a sentence, never a zero). NO threshold introduced. ADR `the-price-block-is-cut-per-unit-and-the-region-is-a-chain-list`; report `docs/reports/prices-and-region.md`.

## Done — 13.09 s53 «price-fix», its READING ($0, §2 item 9, ACCEPTED (yy)): `c9ae6cb`
`read_price_plausibility.py` → 13 of 303 priced rows at x3 of their own category × unit median (the DIAGNOSTIC's band, no shipped module imports it). The three deterministic layers measured CLEAN — size units (`г` 296 · `мл` 13 · none 2), pack markers 0 in all 308 traced answers, all 8 `rows_without_a_unit_price` are model ABSENCES — so **the figure was already wrong when it was stored**. 14 rows read × 2 independent readers, unanimous; 8 rows on 6 pages carry a figure the page does not print (the team lead read two of the JPEGs themselves). Detail: the ruling and `config/price_corrections.yaml`'s own `reads:` lines.

## Done — 13.09 s54 «price-fix» BUILD ($0, §2 item 9 of PHASE v10, ruling (yy) option (c)): `5ce9499` (team-lead files) · `f6e2345` · `5b986bc` · `f655e9f`
Start ritual: the five modified team-lead files committed by path FIRST — `5ce9499`.
`config/price_corrections.yaml` (6 corrections + 2 exclusions, each naming its page and its readers) applied ONCE by `export_front_data.page_true()` between the screen export and every block that shows a price. Identity is **`(row_id, carrier)`** — `row_id` repeats for 7 ids of the 1301 and `@VARUS_channel:11300:0` is one of them, so the id alone would patch a product nobody read; an entry matching anything but EXACTLY ONE row refuses, and so does an unknown key (the `!= 1` predicate is the fork I took for «names a row the data lacks», named here). The record is in `required()`.
Export `5a6f5750…`, byte-identical twice; three blocks move (`category_prices`, `regions`, `sources`) and no others — `media` (referenced 402 · staged 402), `status`, the command centre and `promo_screen_data.json` all unchanged. The four evidence cards are page-true: йогурти 12.2571 → 49.75 · сир твердий 4993.3333 → 1247.92 · морозиво 2006.6667 → 1073.913 · сир кисломол. 100.0 → 162.5714; the 8 rows land on 122.8286 ×3 · 499.3333 · 200.6667 · 345.3333 + the two with no price. Window stays **311**, unit-priceless stays **8**, excluded **2** counted («вилучено 2: сторінка без ціни»). ONE test both ways in `tests/test_export_front_data.py` (+ negative control by hand); `read_price_plausibility.py` still reads 13 of 303 — the accepted reading reproduces. `make front` green · tsc clean · vitest 21/21 · own browser (served, UA and EN): the corrected card's ⓘ opens complete and the block's sentence renders. `make check` on `f655e9f`: ruff clean · **4341 passed / 2 skipped in 728.82 s** (4340 + this item's one test), exit 0. Report `docs/reports/price-fix.md` (Dv1 verified_by · Dv2 the test file · Dv3 one CSS rule).

## Both open stops of s53 are CLOSED — neither by me weakening anything
Stop 1 (the 8 model misreads) was ruled option (c) by (yy) 2 and is BUILT above. Stop 2 was the team lead's STATUS rewrite: the 19 sentences are restored in STATUS's machine archive, and on `5ce9499` I re-ran the four guard families myself — **156 passed**, the team lead's own number. No guard weakened, no sealed record moved, `docs/SPEC.md` untouched.

## Next — «insight-1» (Тренди in DESIGN §12's grammar: «Три висновки тижня» from code rules inside `export_front_data.py`, action titles, the dot-range strip, the chain ranking with the market median, colour-follows-chain, the 500 px wrap). Then «insight-2» → «data-shape» → «front-2» → «e2e-ship» → the gate Wed 16.09 evening.

## Named, not built (the phase file forbids adding what it did not ask for)
- **The Позиції TABLE still shows the misread figure.** It reads `promo_screen_data.json` directly (DESIGN §7), so the three Млековіта rows print 4,29 ₴ beside price cards that now print 42,99 ₴. Correcting that export is a §4.1 stop (a K10-pinned committed artifact), so the screen carries two readings of one row until the team lead rules.
- **The correction makes one pair identical**: `@ATB_FANatik:4657:1` and `:2` are now the same row (Хрещатик, 75 г, 25.90) — one offer counted twice in морозиво ₴/кг n=95, the class (yy) 5 ruled POST-GATE. De-duplicating moves a published population; not done here.
- `media.flyers[].positions` still counts the two excluded rows — the gallery joins pages, not figures, and the cover in the page population is POST-GATE ((yy) 5). The regional cut drops an excluded row from its cards; **0 fall in the cut this week**, so no count was added there.
- **A row off a page that prints NO price is invisible to any magnitude band.** Two of the 8 were that class and both landed mid-distribution — only a hand-widened pass found them. The record closes the eight that were read; it cannot enumerate the class.
- **One offer counted twice in a price median**: `@ekomarket_shop:1469:0` and `@forainfo:6056:0` are each read off the page AND off the post text; `row_id` is NOT unique (7 ids), and a leaflet COVER sits in the page population (`@VARUS_channel:11300`). All three move published populations — POST-GATE per (yy) 5.
- Carried: the static/served cold-start race (`load.ts`'s 1 s guard) · `colourByIndex` colours by POSITION · «Тренди» overflows on a phone · `quartiles()` returned q1 below min at n=2 · `status.money.from` names `sessions[-1]` for a root `cap_usd` · the two id spaces · no test for `loop_daemon.py`, `promote_signals.py`, `thread_population`, the gate's third state · `spend_promo_c3.json` says `"tolerance": 0.05` where the FLOOR closed it · `promo_projection_c2.json` not reproducible · the fold map has no guard ((pp) 2(a)).
