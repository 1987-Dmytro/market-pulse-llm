# PROGRESS — ship-1 (ruling 03.09 (e): the executor's one file — done / next / open stop, ≤60 lines)

## Done and ACCEPTED earlier (detail: the rulings file and `docs/reports/`)
**s51–s54** (ww)(xx)(yy)(aaa) `f783af4`…`0f2db69` — §11's dark look and the flyer media staged by GLOB (402 pages · 11 sets); `category_prices` per unit (13 groups, 311 rows) + the Полтавщина `regions` cut (47 cards); the 8 implausible rows READ (deterministic layers measured clean — the model's misread) and FIXED by a human-verified `config/price_corrections.yaml` applied ONCE by `page_true()`, identity `(row_id, carrier)`.
**s55–s57** (bbb)(ccc) `91a2447`…`0a6eba7` — the Позиції table page-true out of a NEW `positions` block (1299 of the sealed 1301, 2 excluded and COUNTED), ONE weekly banner per tab; Тренди in DESIGN §12's grammar, sentences written by code rules carrying `stands_on`, `colourByIndex` deleted; and the weekly dataset proven to ACCUMULATE — `weekly_positions.py`, 1301 rows into 10 weeks W27–W36.
**s58+s59 «front-2»** (ddd) `1a3e0e5`…`2d8afb9` — the command centre T0–T8 over ONE block (`front_data.json :: command_center`), nine tabs, three laws (a window is a POINT; the headline population is the FILE's; a reading this window cannot carry is a SENTENCE). The lawful close caught two on-screen figures no file holds — T5's browser-divided «73,1 %» and T8's «[object Object]» — each fixed with a test shown red by its own mutation.
**s60 «weekly-home»** (fff) `d262d02` — the ten weeks live in the committed `results/weekly/` (content digest `d4fb6a44…` twice) and T4's «Δ до свого» is the producer's `command_center_derived` field; the app subtracts nothing.
**s61+s62 «region-collect»** (ggg)(hhh) `4c1c2d2`…`117e10b` — the s61 stop ruled shape (c) and the registry NEVER moved: `config/region_channels.yaml` carries the 18 handles, `data/raw_region/` holds 15 889 posts · 8 232 comments, and the baseline is an honest printed ZERO that holds under a stricter matcher. The comment population is a SAMPLE (1 008 of 3 700 threads).
**s63 «reactions-region»** (iii) `99b7bc5`…`7c207f1` — Реакції rebuilt in §13's page order (complaints 110 chain / 37 sku, 30 thread cards of 100 with their root promo post, SKU voice 95, 18 monthly points) and **Промо · Регіон** as a new tab whose ZERO is the producer's own sentence; the run report `results/region_collect_report.json` (3 700 / 1 008 / 2 692) writes on EVERY mode, `8cf7563b…` from three runs. Dates and texts joined from COMMITTED draw and pack files, never from gitignored `data/raw/`. vitest 37/37, `make check` 4348/2.
**s64 «e2e-ship»** (jjj) `1851113`…`5782a53` — the clean clone RUNS the product (`make front`/`make tick`/`make promo-screen` exit 0, `/api/status` and `/` 200, the removed-source probe exits 2 naming `results/region_collect_report.json` at the PRODUCER, `draw_truth_20.py` 20 rows, `make session`, `firebase.json` + `.firebaserc.example` present and nothing deployed, porcelain empty); the README is the product's front page with four generated sections and not one typed number. My open stop — `make check` ON the clone — was ruled **(a)**: §8's predicate governs, the suite's input is the local store git lawfully omits, and §2's clone-`make check` clause was struck by revision (PHASE v21). (jjj) addendum: the Mac's `serve` extra was reinstalled after a brew python re-link and the serve clause re-proven live.

## Done — 15.09 s65 «dark-default» ($0, ruling (kkk) 2): `f8dc66f` (team-lead addendum by path) · `c1621cc` (brain) · `ebfe2ce` (the test + the frame) · this file
**Clause 1 — «with the OS in light mode `/` renders the dark tokens on first load»: GREEN, measured on the rebuilt app.** The Mac is in light mode (`defaults read -g AppleInterfaceStyle` → the key is absent) and the page was opened on `http://127.0.0.1:8000/` — the same `make serve` process on an origin whose storage the app has never written, so the reading is a FIRST visit and not the operator's remembered one: `localStorage` empty · `prefers-color-scheme: dark` **false** · `data-theme="dark"` · `--surface-0 #141413` · `--text-1 #ffffff` · body `rgb(20, 20, 19)` · `color-scheme: dark only`. Frame: `docs/reports/screens/dark-default-first-load.jpg`.
**Clause 2 — «the toggle still reaches light and system»: GREEN, and the fork named.** One click on the header toggle → `data-theme="light"` · `--surface-0 #fcfcfb` · stored `light` · the label flips to «темна»; `#/promo/positions?theme=system` → the attribute is REMOVED (`null`) and this light-mode Mac gets `#fcfcfb`, i.e. `prefers-color-scheme` decides again. **Fork, §4.2 — decided, not a stop:** the control stays TWO-state (dark↔light) and «system» stays an explicit choice through the link param FOR THE CURRENT PAGE LOAD — `?theme=…` is deliberately not remembered, so it does not survive a reload (the first bullet below). It has been exactly that since `dd3c110`: the pre-`a44cad6` toggle is the same `theme === 'dark' ? 'light' : 'dark'` ternary, so «still reaches» is a no-regression clause, not a request for a third button, and DESIGN §11 says only «the toggle and the light theme stay». The line the brief lacks: whether «system» is owed a place IN the header control. A third state is new UI on the approved §11 header plus two dictionary keys × two languages — not built, named here.
**Clause 3 — «one vitest both ways if the default is shaped as code»: it is, so the test is in.** `frontend/test/theme.test.ts`, vitest **45 passed** over 4 files, 8 of them this file's (measured here; the 37 of ruling (iii) was read at another HEAD and is not re-run, so the delta is not arithmetic I can stand on). It holds BOTH lines that decide a first paint — `rememberedTheme()` (nothing stored / the retired «system» / storage that throws → `dark`; a reader's own `light` still wins) and `index.html`'s `data-theme`, read off disk, which no unit test on the module can see go missing. Shown red by its own mutation TWICE: the default back to `'system'` reds the three first-visit cases and leaves the two reader-choice controls green; `index.html` without the attribute reds the first-paint case alone.
**Clause 4 — «`make front` green and the rebuilt app staged»: green, and the rebuild is byte-identical — measured on the ARTIFACT.** `make front` exit **0** (`/tmp/s65_make_front2.log`) and run twice: the digest over the built page, bundle and manifest (`find dashboard/app -name '*.html' -o -name '*.js' -o -name '*.css' -o -name manifest.json | sort | xargs shasum -a 256 | shasum -a 256`) is **`86acc266f354718e`** both times, `assets/index-BB5UmYX7.css` and `index-BIc22Rm6.js` unmoved, media referenced 402 · staged 402 · missing 0, and the staged `data/front_data.json` · `data/promo_screen_data.json` are byte-identical to the TRACKED `results/` files (`25566dff…` · `e45860c6…`). The artifact Vercel will serve carries the default in itself: `dashboard/app/index.html` → `<html lang="uk" data-theme="dark">`, the minified bundle → `e==="light"||e==="dark"?e:"dark"`. **Correction to my own first reading of this clause:** I cited `git status --porcelain dashboard/` as the proof; `dashboard/app/` is gitignored (`.gitignore:127`), so that command is empty under EVERY state of the build and can report nothing about it — my own adversarial review of this diff caught it (three lenses, both skeptics) and the digest above replaces it.
**What the ruling measured, and the two lines that decide.** (kkk) 1 read `tokens.css:68` — the `@media (prefers-color-scheme: dark)` block, guarded `:root:not([data-theme='light'])` — and concluded «system» is the default. The deciding lines are `frontend/index.html:4` (`data-theme="dark"`, on the element BEFORE the bundle runs) and `frontend/src/app-state.ts:67` (`rememberedTheme()` → `'dark'`), both landed at `a44cad6` (12.09 «design-pass»). The dark first load was therefore already shipped; this item adds its PROOF, not its behaviour — and no working line was rewritten to manufacture a fix.
**MEASURED vs INFERRED (the gate reads this tomorrow).** MEASURED: a first visit is dark, above. INFERRED and NOT provable retroactively: the operator's light window came from a light choice remembered at HIS origin — `localStorage` is per-origin, the app writes the key only from the toggle, and his `localhost:8000` carries `market-pulse.theme` today (`dark`, by whose click and when cannot be shown). The mechanism was reproduced end to end: toggle → light → reload → STILL light; clear the key → reload → dark. Consequence: the Vercel showcase, where every visitor is a fresh origin, opens DARK; and a light window on the operator's own Mac tomorrow is his remembered choice, one click from dark, not a regression of this item.
**Checks (mine).** vitest **45 passed** (4 files) · `npm run check` (tsc) clean · `make front` green · `make check`: ruff **All checks passed!** · **4348 passed, 2 skipped in 710.70 s**, exit 0 (`/tmp/s65_make_check.log`) — the same 4348 ruling (jjj) accepted, this item adding no Python test; HEAD `ebfe2ce` at both ends, only this file moved under the run and no test reads it. **$0 — nothing created anywhere.**

## Next — the team lead's «linkedin-pack» and «de-deck», then the Vercel deploy → the GATE Wed 16.09 evening
(kkk) 3's order is dark-default → rebuild → ONE deploy: the rebuild is done and staged, `vercel login` is the operator's own hand and the deploy command is the team lead's. **I have no open stop.**

## Named, not built (the phase file forbids adding what it did not ask for)
- **The header's theme control has no «system» state** (clause 2's fork) and a link's `?theme=…` is deliberately NOT remembered, so «system» lasts until the next reload. Both are one decision for the brief, not for me.
- **PRINT comes out white-on-white since dark became the default** (found by this diff's own review, both skeptics confirming; the most serious of the two). `tokens.css:98` selects the dark palette by ATTRIBUTE, so it survives into the print medium where the browser drops the `#141413` background but keeps `--text-1 #ffffff`; the only `@media print` block, `app.css:621-627`, hides the chrome and resets no token. Harmless while the OS picked the theme, a defect since `a44cad6` — and DESIGN §8 calls print non-negotiable, so the remedy (an `@media print` block re-pointing the tokens to the light values, ~8 lines) is the team lead's call — BEFORE the (kkk) 3 deploy, or knowingly after it — not mine to add unasked.
- **A reader whose stored choice is LIGHT gets a dark first paint and a 200 ms crossfade on every load** — `index.html:4` hard-codes the attribute, the stored choice is applied only after mount (`App.tsx:41` → `App.tsx:51`) and `app.css:1027-1038` transitions `background-color`/`color` for 200 ms. It arrived with `a44cad6`, not with this item, and a first visit — every showcase visitor — has nothing stored and never sees it. The remedy is an inline head script reconciling storage before the first paint: new code the check line does not ask for.
- **POST-GATE, ruled (iii) 2 and (ddd):** the `required()` GLOB cannot refuse by name (a checkout missing a globbed draw/pack builds dateless cards instead of refusing) · `collect_region.py :: report()` has no emptiness guard, so a store-less tree prints «прочитано 0 із 0» as a MEASUREMENT · the «—» `subject_type` chip never fires today (3 + 180 + 3 + 95 = 281) · the ⓘ-as-badge fork, APPROVED · the app has NO error boundary, so `must()` inside a tab unmounts the root where DESIGN §5/§7 want a red panel · `SovReading.share` is typed `number` while the producer can write null.
- **Промо · Якість has no §11-era frame**, so the README shows five tabs and the flyer gallery and says where the sixth lives: `promo-quality.png` was shot 12.09 13:06, before the design pass landed that afternoon. Re-shooting a tab is new work the check line does not ask for.
- **The 90.9 MB of full-size leaflet JPEGs**: ruling (kkk) 3 resolved the parked (ww) decision — they deploy WHOLE, a downscaled variant is post-gate if the operator wants it. The row thumbnails are the originals (1499×2136) and decode under scroll.
- **`build_readme_results.py` crashes rather than refuses on a half-written source** — `load()` checks existence, so a missing KEY inside a committed file is a traceback rather than a named refusal. The three refusals my own review earned (an empty `results/weekly/` glob · two gold versions in `rescores_v3.json` · a gate row the file no longer carries) are in, each shown firing with the control beside it; a key-by-key contract over four files is not something this item asked for.
- **No test for `make session`, for the README's new sections, or for the hosting config.** §3 authorises a test only for a caught PRODUCT defect; the session refusal was shown both ways in the session log instead, and the README block is proven by regenerating it byte-identically on two trees.
- **The clone's `make front` stages 0 of 402 leaflet photos** — the JPEGs live under gitignored `data/annotation/`, so the public showcase must be built on the machine that collected them. Named because the README says so and the operator deploys from his Mac.
- **The month on the Реакції line is the PROMO POST's**, not the comment's: the comment's own date lives only in gitignored `data/raw/comments/`. Two distributions differ by three rows of 281; the block's sentence says whose month it is.
- **DESIGN §13 (4)'s «comments-first-collected date» is not on the coverage table** — `region_baseline.json` holds ONE window per channel over posts ∪ comments, and a comments-only first date is a sentinel change nobody ordered.
- **`promo.ts :: feed()` and `tableRows()` are read only by the suite** now that the tab reads `reactions_v2`; both adapters stay and `adapters.test.ts` still holds them against the sealed export.
- **The comment sweep is capped at 80 newest threads per channel** — 1 008 of 3 700 read, 2 692 outstanding, printed per channel; uncapped it is ~2.5 h and the store resumes for free. `region_source.comments_enabled` is measured BEFORE the post walk, so one combined `--posts --comments` run would stamp comment provenance with the pre-walk answer.
- **Two brand blocks sharing one `brand_id` would make one mention read as two**, and an overlapping dictionary yields one row per BRAND named, not per phrase («Яготинське для дітей» names two). No stemmer: «миргородської корівки» is not a hit — measured cost, 2 false hits avoided, an inflected REAL mention missed.
- **The ranking's rows are collected CHANNELS, not retailers** (АТБ reaches морозиво through three, 44 of 95; `msuaaaa` reprints АТБ's leaflet and its crowned median 227,1429 IS that reprint), the same leaflet page is collected three times, one offer is counted twice (`row_id` is NOT unique), a leaflet COVER sits in the page population, and `media.flyers[].positions` counts the two excluded rows. Every one moves a PUBLISHED population — POST-GATE per (yy) 5.
- **A row off a page that prints NO price is invisible to any magnitude band** — two of the 8 were that class; the record closes the eight that were read, it cannot enumerate the class. The pageless note's «25 з 1 299» counts over two populations, whole only because both excluded rows carry a page.
- **The second population reaches the screen on ONE of four sampled readings** (NSR prints «куплені: 7,70 %»); `headline_sample` is obeyed everywhere and the header comment now says so. `report()` over an empty store writes eighteen ZEROS and the screen prints them as a measurement.
- Carried: the static/served cold-start race (`load.ts`'s 1 s guard) · `quartiles()` returned q1 below min at n=2 · `status.money.from` names `sessions[-1]` for a root `cap_usd` · the two id spaces · no test for `loop_daemon.py`, `promote_signals.py`, `thread_population`, the gate's third state · `spend_promo_c3.json` says `"tolerance": 0.05` where the FLOOR closed it · `promo_projection_c2.json` not reproducible · the fold map has no guard ((pp) 2(a)) · the weekly artifact has no shrink guard (a partial rebuild now lands as a `git diff`).
