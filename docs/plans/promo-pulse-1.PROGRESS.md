# PROGRESS — promo-pulse-1 (executor's one file: done / next / open stop, ≤60 lines)

Ruling 03.09 (e) made this file the phase's state; §8 of the phase file stays the DONE list.

## The money, live
`promo-dev-loop`: **$0.7493 of the registered $2.4469** (iteration 1 $0.5254 + B $0.2239), **$1.6977
left**, the step OPEN. Cycle 3 after B, the guard's own lines: **$4.5265 of $7.00, REMAINING
$2.4735** — a balance delta, and the pessimistic reading, because the billing walk still says
$4.2920 and posts 30–40 min late. c3's cap under (l)4 = `min($0.50, REMAINING − $0.30)` = **$0.50**,
far above the $0.15 that would have been a STOP for the operator's word. Holdout $0.30 untouched.

## Done
- **03.09 s9 iteration 1** ($0.4358): signal 0.8792, subject 0.7929 — **0.8214 re-read under the
  v1.1 gold**, and that is the only number iteration 2 is comparable to; **s10/s11 A(1)**; **04.09
  s12 «A-rest», $0** — ACCEPTED by (k); **s14 «sources-r3», $0** — ACCEPTED by (l)1.
- **04.09 s15 B — the paid iteration 2 RAN WHOLE** (`8d4108d`, `90996cc`, `20fdeb9`). Pod
  `ej1dxfcpe0etff` at the registered $0.74/h, created 14:42:45Z, deleted 15:00:54Z: **1089 s,
  $0.223850**, 56/56 units, every reply balanced, finish stop, ONE boot (167.0 s off the warm
  volume) across the GO. The smoke measured **38.816 s/thread** (max 88.237) against iteration 1's
  81.212, so `--project` read **GO** at $0.4097 on the mean corner and $0.8465 on the max. All three
  numbers pre-declared before the smoke held with room: the cap tightened to $1.8826, the KILL read
  against it instead of the frozen $2.4469, and the 90-min stop `--open` prints its $1.11 for.
  **K8 on dev-40: signal 0.8854 HOLDS (bar 0.75, up from 0.8792) · subject 0.7500 RED (bar 0.80,
  DOWN from 0.8214 under the SAME gold).** Before it: the runbook repointed off every paid
  `…_iter1.*` — K8's `--predicted` was the last one, and the smoke's own units had moved too.

## Open stop — «iteration 2 read»: a RED bar with one cause, and a fork about what B bought
**Stop-point.** Subject is RED at 0.7500 and the whole gap is ONE thread: `@VARUS_channel:6216`
went 18 → **0** rows and lost all 18 of its gold comments; on the other 122 iteration 2 misses
**17** where iteration 1 missed **25**, and with 6216 answered as before it would read **0.8786**.
That answer came back a ```json-fenced ARRAY, and `promo_dev_pod_runner.balanced_prefix` dispatches
on `text.lstrip().startswith("[")` — the fence makes it False, so the SHIPPED object rule ran and
**stopped GENERATION at the first complete object** (213 chars, 5.9 s, finish stop): the other 17
answers were never written and no parse-time repair can recover them. Checked, not assumed — **1
of 40** answers is array-shaped, and `9014` 11→1, `9999` 6→5 are whole objects costing 0 gold.
**Q1.** Is repairing that dispatch a DEFECT FIX (same `extractor_version`, iteration 2 re-bought)
or a NEW INSTRUMENT under 03.09 (c) 3 — iteration 3? It decides whether B's $0.2239 bought a
reading of the LAW or of the TRANSPORT. **Q2.** Gain or plateau, on two facts: 0.8214 → 0.7500 on
the registered population, 25 → 17 misses on the 122 comments outside 6216.
**Tree state.** Clean; pod deleted, both listings `[]`; step OPEN at $0.7493 of $2.4469 (no guard
`--close` — iterations 3–5 must fit). `make check` **4319 passed, 2 skipped**, exit 0; an earlier
ERROR in `test_zero_spend_45g5.py` was mine — the verifier ran while results were being written.

## Named, not built (the standing prompt forbids adding what the phase did not ask for)
- **Leg B bought nothing new:** 16 of 16 registered posts answered `[]` in BOTH iterations, so S4's
  leftover posts still have no evidence row and a third purchase buys the same empty array.
- **Gold covers 140 of the 208 comments in the 40 threads**, so a model that answers fewer rows can
  score better; iteration 2 wrote 122 rows against iteration 1's 151.
- **A unit test for `resolvable()` with a `+` handle is AUTHORISED by (l)1**, not yet written.
- **`project()` prices its KILL against the whole step cap, never the step's remainder.**
- **`check_law` compares `codebook_version` only**; **`committed_registration()` ignores
  `pinned_inputs`** — checked by hand this session, all five held.
- **(k)2** holds the near-quote «Шикарно…» and the codebook doc's 11 quotes to the law FREEZE.
- **c3 stays as (l) 2–4 wrote it**; `@ON_LINE_MO` after c3, `1925810730` unaddressable.
