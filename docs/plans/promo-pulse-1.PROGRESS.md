# PROGRESS — promo-pulse-1 (executor's one file: done / next / open stop, ≤60 lines)

Ruling 03.09 (e) made this file the phase's state: it replaces STOP files and per-session reports
until the phase closes. `docs/PHASE-promo-pulse-1.md` §8 stays the DONE list; `/goal` is retired.
The 2026-09-03 STOP file is folded in here and deleted (its full text: `git show 49860d1`).

## The money, live
`results/promo_dev_loop_run.json :: spent_all_segments_usd` = **$0.089622** of the step's $2.50 —
two pods of 2026-09-03 that were killed at 180 s by a dead-man read from a PROBE's record before
either published an ssh port. Nothing was generated; both listings `[]` after every delete. Cycle 3:
$3.6322 of $7.00 by the guard's balance delta, its billing walk 30–40 min behind. Holdout $0.30 untouched.

## Done
- **2026-09-03 s8** — the paid attempt that bought nothing: create line of ruling (d) VINDICATED
  (`--image runpod/pytorch:1.0.2-cu1281-torch280-ubuntu2404`, EU-RO-1/SECURE/`--ssh`/disk 30, both
  pods at the registered $0.74/h); `--terminate-after` is a DATETIME and was derived from the cap.
  The defect was the gate: 180 s from `prereg_reader_probe_v5b.json` against a production sibling
  that registers 500 s with six readings 14.5 → 262.5 s. The second recreate was refused on the
  record — an instrument set below its own span gives the same answer twice.

## Next (this session's ONE item — ruling 03.09 (e))
Run `scripts/runbook_promo_dev_1.md` end to end with the dead-man at 500 s: create → smoke → the
decision table of ruling 03.09 (b) → 40 threads + 16 posts → fetch → delete → settle → K8 → error
table; end the turn with the open stop «iteration 1 read» and the three iteration-1 files named.

## Open stop
None — the eighth pause is closed by ruling 03.09 (e).

## Needs named, not built (the standing prompt forbids adding what the phase did not ask for)
- `results/prereg_promo_dev_loop.json :: gates.terminate_after_minutes` (90) is voided by ruling (d)
  and still printed beside every rung-1 GO (`usd_at_the_backstop` $1.11 where the platform stop the
  runbook actually sets is the cap's 12 162.2 s ≈ $2.50). Report-only — nothing derives the flag
  from it, checked by grep — so it was left as the ruling left it.
- Money question 1 is still unanswered: does the step ledger of attempt 2 open carrying the
  $0.089622 already spent, or is that attempt closed against the cycle alone? The run record counts
  it against the cap either way (`close_segment` sums every segment).
