# PROGRESS — promo-pulse-1 (executor's one file: done / next / open stop, ≤60 lines)

Ruling 03.09 (e) made this file the phase's state: it replaces STOP files and per-session reports.
`docs/PHASE-promo-pulse-1.md` §8 stays the DONE list; `/goal` is retired. The 2026-09-03 STOP file
is folded in here and deleted (its full text: `git show 49860d1`).

## The money, live
`results/promo_dev_loop_run.json :: spent_all_segments_usd` = **$0.5254** of the step's $2.50 —
$0.089622 for the two pods a probe's dead-man killed on 2026-09-03, $0.435778 for iteration 1.
**$1.9746 left.** Cycle 3: $4.0631 of $7.00, REMAINING $2.9369; holdout $0.30 untouched. The step
ledger is anchored (20:17:16Z) and **OPEN**: the close refused because the billing walk had not
landed (0 ms against 2 120 000). The pod-priced figures above are the quotable ones.

## Done
- **2026-09-03 s8** — two pods bought and killed at 180 s by a dead-man read from a PROBE's record.
  The create line of ruling (d) was vindicated; the gate was the defect.
- **2026-09-03 s9 — ITERATION 1 RAN WHOLE.** Dead-man 500 s from the production sibling (ruling (e)
  item 1); the port published at 37 s. Pod `0z95ve2n570f0t`, 2 120 s at the registered $0.74/h.
  56 of 56 units answered, all balanced, all `finish stop`, 1 717.7 s of generation.
  **signal-type agreement 0.8792 — HOLDS the 0.75 bar. Subject agreement 111/140 = 0.7929 against
  0.80 — RED by ONE comment.** Strata (readings): currency 0.7377/0.8583, decimal_only 0.8354/0.9000.
  Leg B: 16 empty arrays, and correct — those posts carry no dairy and no ice cream.
  Files: `results/promo_dev40_predicted_iter1.jsonl` · `results/grade_promo_dev40_iter1.json` ·
  `results/promo_dev40_errors_iter1.json` (+ the raw replies `promo_dev40_iter1.jsonl` and pod log).
- Built at $0 before the money, because the runbook could not otherwise run: `--project` (the
  decision gate, GO at $0.7844 against its $0.80 band, all four bands driven), `--score` (the join
  the plan named and nobody had written, plus the error table), the array-closing prefix leg B's
  registered prompt needs, and eight defects an adversarial audit confirmed — see `git show 4e75e04`.

## Next
The team lead's read of iteration 1, then iteration 2 by the ruling it produces.

## Open stop — «iteration 1 read»
1. **Subject is 0.7929 and the bar is 0.80: one comment.** The 29 misses are one shape — the model
   answers the CHAIN where the codebook wants the item (sku→chain 4 of the named ten), and confuses
   `post` with `chain` under rule 2's noise clause. That is a CODEBOOK question, and the codebook is
   the team lead's file. Iteration 2 may vary template, rendering, decoding and answer repair
   (ruling 03.09 (c) item 3) — none of those is where these 29 rows live.
2. **A deviation you must rule on: gemma-4 fenced all 40 answers in ```json.** Scored as they stood
   they were 40 parse failures and iteration 1 would have reported subject 0.0. `promo_prompts.parse`
   now reads the fence off and NOTHING else — a malformed or cut object still fails, counted by cause
   (`git show 36ef74f`). The codebook never forbids a fence; leg B's registered prompt is the one
   that does. The raw replies are on disk, so this is re-scorable either way at $0.
3. **The step ledger is open.** `--close` refused on an unlanded walk rather than settle ~$0.00, and
   the anchor (20:17:16Z) cannot reach the $0.089622 of the earlier session — money question 1 from
   the eighth pause is still unanswered, and a one-note session also makes `--tolerance 0.05`
   structurally vacuous. Tree: clean; `make check` green; both listings `[]`.

## Needs named, not built (the standing prompt forbids adding what the phase did not ask for)
- No committed test covers `--score`, `--project` or the array-closing prefix; all three were driven
  at $0 and their output shown, but the suite does not pin them.
- `gates.terminate_after_minutes` (90) is voided by ruling (d) and still printed beside every rung-1
  GO; `usd_at_the_backstop` read $1.11 where the stop this session set buys $2.4102. Report-only.
- `rung_0.dear_usd` $1.3193 prices a dead segment at 279 s where the gate now allows 560, and its
  135.2 s/thread was exceeded by the measured max of 178.3. The corner still FITS; the meaning drifted.
- `--go-deadline 1800` is unpriced in rung 0 (it cost ~14 s here, worst case $0.37).
- A relaunch after the GO would abort in `already_answered` — the smoke phase sees the full run's
  ids as foreign. It never fired; the recovery path is unbuilt.
