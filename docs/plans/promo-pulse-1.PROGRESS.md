# PROGRESS — promo-pulse-1 (executor's one file: done / next / open stop, ≤60 lines)

Ruling 03.09 (e) made this file the phase's state; §8 of the phase file stays the DONE list.

## The money, live
`promo-dev-loop`: **$0.749250 of the re-registered cap $2.1638** (it fell $2.4469 → $2.1638 on the
volume's drip), **$1.4146 left**, the step OPEN — **s18 spent $0 as s17 did: no pod created, no
ledger line taken.** Cycle 3 **$4.5362 of $7.00**; holdout $0.30 untouched; c3 cap $0.50.

## Done
- **Bought and accepted:** s9 iteration 1 ($0.5254) · s15 B = iteration 2 ($0.2239,
  `ej1dxfcpe0etff`, 1 089 s, 56/56) by (m)1 — **signal 0.8854 HOLDS, subject 0.7500 RED**,
  `…_iter2.*` never rewritten. Plateau **0 of 2**. $0 slices s10–s17 accepted.
- **04.09 s17 — the whole $0 half of iteration 3** (`8efb0c8` `48537b9` `1962ee5`): `ITERATION = 3`;
  `pinned_inputs` gains the runner `102fa524…` and `promo_prompts.py` `dd260cb5…` ((m)5, (n)2); leg
  B closed by (m)4, its 16 ids NAMED under `not_bought`; rung 0 over 43 threads + 0 posts at $0.74/h
  — cheap $0.2674, priced $0.3247, **dear $1.3100 FITS**; the pack 40 leg-A units; the runbook off
  every `…_iter2.*`. All 40 renders byte-identical to iteration 2's at a moved module sha
  (`8d52ee79…` → `dd260cb5…`) — the whole reason the pins exist; `make preflight` 0 of 21.
- **04.09 s18 — the stop of (o) closed, $0.** `ec16e82` the four team-lead files by path (ruling
  (o), PHASE v6 §6.6, STATUS, the stop-pattern review). `2f2b62c7`
  `test_the_pack_pins_exactly_what_the_pod_re_derives` stops pinning ONE registration's population:
  the count is `leg_a.threads + leg_b.posts`, the set is `leg_a.order` + `leg_b.by_channel` (the map
  `build_pack` itself iterates, so re-opening leg B needs no second edit), «exactly» is stated in
  BOTH directions with no id twice, leg B's render-pin claim is a loop over the record's own units —
  empty while (m)4 holds — and `pack["registration"]["sha256"]` is checked against `PREREG` FIRST,
  or the two reads could be two populations. (o)2 refused the synthetic unit;
  `test_leg_b_pins_ids_and_not_a_count` untouched. **`make check`: 4320 passed, 2 skipped, 692.53 s,
  exit 0** — §8 (j)'s floor 4266, the run before 1 failed / 4319; no file deleted, diff in the
  commit. **Negative control at $0** (a guard that only ever passes proves nothing): a registered
  unit dropped, an unregistered one shipped, one shipped twice — each REFUSED. The invariant itself
  never moved: `check_requests` re-derived all 40 pinned shas and refused none.

## Next — the PAID item: iteration 3, one session, nothing before it
No stop is open; `make check` is green at `2f2b62c7`; the tree is clean but for this file. Order of
(o)5: buy iteration 3 → K8 → the error table naming the diff against iteration 2 ((c)3), by
`scripts/runbook_promo_dev_1.md` §0–§6 — create → dead-man 500 s → bundle → **DETACHED** launch
(`os.setsid`) → smoke → decision table → teardown — then `--score --iteration 3`. Subject ≥ 0.80 →
the holdout NOTICE is the next stop, END; RED → the error table, END. «c3» after, by (l) 2–4.
**The three numbers, declared BEFORE the smoke ((m)5), unmoved by s18:** (1) iteration 3 has
**$1.4146** = cap $2.1638 less the step's own $0.749250 over four segments; (2) `project()` reads
KILL against the WHOLE cap, so KILL is read BY HAND against $1.4146 — dear $1.3100 fits at **−7.4%**,
its own line saying −39.5%; (3) `--terminate-after` = min(114.7 min runway, registered 90) = **90
min = $1.1100**. `--close` NOT called. One ledger line, taken BEFORE the pod exists.

## Open stop — NONE. (o) closed the only one; PHASE §6.6 (v6) keeps its class from stopping again.

## Named, not built (the standing prompt forbids adding what the phase did not ask for)
- **Rung 0 still prices at the BORROWED max** (135.232 s, §9a 1) though this instrument has two
  measured smoke maxima — **178.298 s** (iter. 1) and **88.237 s** (iter. 2). (o)4 retires the borrow
  BEFORE iteration 4's re-emission (own measured maximum; missing that dear corner, FITS on the MEAN
  one with the cap as hard stop, in one line). Iteration 3 as registered — a ruling now, not my debt.
- **`items[:3] == pack["smoke_ids"]` is pack-vs-pack**, no external anchor; the record carries the
  same fact at `leg_a.smoke.units[*].unit_id`. (o) did not name that line, so s18 did not touch it.
- **`pod.main` installs `close_arrays_too` on `reader_v5` and never lifts it.**
  **`committed_registration()` ignores `pinned_inputs`**, so iteration 3's pins are witnessed by git
  and not by it. **`check_law` compares `codebook_version` only.** **`project()` prices KILL against
  the whole step cap.** A `resolvable()` test with `+` is AUTHORISED by (l)1, not written. **Gold
  covers 140 of 208 comments.** **(k)2** holds «Шикарно…» and the 11 quotes to the law FREEZE.
  `@ON_LINE_MO` after c3; `1925810730` unaddressable.
