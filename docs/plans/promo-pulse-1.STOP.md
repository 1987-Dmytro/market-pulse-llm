# STOP — promo-pulse-1, 2026-09-03 (the seventh). The $0 half of S9 is BUILT and PROVEN; the pod was not created.

**Stop-point:** the money boundary, and it is not a fork — the registration FITS and the sequence
was clear. What ran out is the session, and the honest reason is below rather than dressed as one
of §8's listed causes. **Nothing was bought. No pod exists. No rung fired.** Every listing in this
session's transcript is `[]` and stayed `[]`.

## What the ruling asked for, and what is done

Ruling 03.09 (c): «the flags and their stub tests at $0 (`--register` and the projection testable
without a pod), `make check` green, `--register` shown with `fits` at the dear corner → **then, in
the same session if the registration fits, the pod**». The first three are DONE and shown:

* **§9a is committed** (`bf21167`) — the five amendments folded in before any code, and one mechanic
  §9 left loose fixed by a FACT: the transport is a POD, because `local_llm.batch` renders from
  `prompts.py`, which is pinned. Serving this prompt over an endpoint would move a pinned file.
* **`--register` FITS at the dear corner: $1.3193 of $2.50** (`results/prereg_promo_dev_loop.json`).
  Priced in the POD's own unit — seconds of existence × the day's $/h — not in a serverless
  per-second rate borrowed from another transport. Cap from the guard's own `REMAINING $3.4591`
  less the holdout's $0.30; price from `runpodctl gpu list` (the DEARER of the two clouds, EU-RO-1,
  RTX 4090, stock Medium, $0.74/h); overhead from the sibling that SETTLED — `pass2-signals-r2`
  billed 2 061 s for 75 threads, so 279 s of it was boot, the 31B load, the scp and the delete.
  Transport gates borrowed from v5b's frozen record, the 180 s dead-man DERIVED from the two fields
  that bound it. Nothing typed.
* **The pack is built and every unit re-derives** (`results/promo_dev40_pack.json`): 56 units, leg A
  40 promo threads, leg B the 16 C2 posts enumerated from the store by S4's own subtraction (ids,
  not a count), the smoke's three first — `@msuaaaa:6523` (4 895 c), `@VARUS_channel:9006` (5 202 c),
  `@VARUS_channel:6009` (8 170 c), the shortest/median/longest rule.
* **The pod runner is written and its preamble is proven at $0**: `scripts/promo_dev_pod_runner.py`
  swaps ONE function of `reader_v5_pod_runner` — the render — and dispatches on the item's own task,
  so both legs ride one boot with the GO between them. Driven on the Mac with no GPU, the whole
  preamble passes on the REAL pack: the codebook handshake, `check_instrument`, and all 56 per-item
  shas.
* **12 new tests, each with its negative control.** `make check` at `ae99c7a`: **4 313 passed,
  2 skipped in 751.97s, exit 0** — (j) holds, and no assertion of anyone else's moved.

**The dry contact earned its keep.** It refused at `@ekomarket_shop:1457`: the pack pinned the sha of
the PAYLOAD where the pod re-derives the RENDERED request, and the two differ by the whole positions
instruction. On a live pod that is a boot and a session thrown away for a diff of one function call.
It is fixed and it is now a test.

## Why the pod was not created, plainly

The sequence's last step needs the pod session ITSELF — create, rung 1 on the response, the ssh
dead-man, the bundle and the staging, the launch, the smoke, the decision table, the GO, 40 threads
and 16 posts, the fetch, the teardown, K8, the error table, the settlement. The sibling's own pod
billed 34 minutes of wall clock for 75 threads. What was left of this session could have started
that and could not have finished it, and a paid run abandoned mid-flight is the one outcome no rung
protects against: `--terminate-after` bounds the money, nothing bounds the loss of the evidence.

So the boundary was drawn where the money is, not where the work stopped. `scripts/runbook_promo_dev_1.md`
is the whole session end to end — create through settlement, the decision table quoted, the teardown
listings in it — so the next session's first command is a paste and its last is the STOP «iteration 1 read».

**This is not on §8's list of PAUSE causes and I am not going to pretend it is.** Rule on it: (1) the
next session runs the runbook as written; or (2) something in the $0 half is wrong and must move
first. One thing genuinely does need your word before the create: **the runbook's `--image-name`**,
which the sibling's records do not carry and which I will not guess on a billing pod.

## Money, unchanged

`CYCLE 3 SPENT $3.5409 of $7.00 · REMAINING $3.4591` (the guard, this session, twice). The dev
loop's cap is the full **$2.50**, floor $2.00 clear, holdout $0.30 untouched. Step
`promo-dev-loop` has no anchor yet — the ledger is written by the first `--note`, at the create.
