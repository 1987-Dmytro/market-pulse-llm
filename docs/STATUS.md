# STATUS — market-pulse-llm (2026-08-20; компактная карта)

> Операторский документ, по-русски по решению оператора. Правит только тимлид,
> после приёмок. Полная хроника фаз 0–5 и дней 15–17.08 — в git-истории этого
> файла и в docs/reports/; здесь — карта и живые решения.

## Что это за проект (одним абзацем)

Ключевой инструмент маркетинговых исследований компании (ТМ «Гармонія»):
мониторим Telegram-каналы украинских ритейл-сетей (Сільпо, АТБ, Varus,
Маркетопт) и агрегаторов, находим анонсы новинок и промо по молочке/мороженому,
извлекаем бренды и позиции с ценами, классифицируем реакции покупателей
(тональность, сарказм, скрытые интенции), и превращаем это в командный центр
директора по маркетингу. Ядро — открытая LLM (Gemma-4-31B), дообученная QLoRA;
GPU только арендные, между сессиями консоль пуста. Северная звезда — семь
вопросов директора в `docs/PRODUCT.md`.

## Карта фаз — ГДЕ МЫ

| Фаза | Что | Статус |
|---|---|---|
| 0–4.5 | Спека (rev 3.24) · скелет · данные · бейзлайны · QLoRA · программа качества | ✅ (хроника в git) |
| 5 | Продакшн-петля 5a…5c2: окно-1 куплено целиком, сидение ратифицировало обе половины | ✅ 14.08 |
| **6** | **Командный центр + слой сигналов из комментариев** | **🔄 ТЫ ЗДЕСЬ — v1 центра построен; однопроходный промпт-читатель ЗАКРЫТ stop-rule'ом 17.08; каркас — гибрид D (сидение 17.08); проход-1 ИЗМЕРЕН 18.08: база 9/14 при пороге 12 → сидение выбрало линию B (разметка subject_type + LoRA); ЛИНИЯ B ИЗМЕРЕНА 20.08: арм A 9/14 → КРАСНЫЙ, линия B ЗАКРЫТА (рулинг оператора на приёмке) → СИДЕНИЕ C (другая база) — следующее; эталон цели — `docs/REFERENCE-signals-w1.md`** |
| 7 | Валидация на ≥2 окнах + упаковка | — |

## Модель сейчас (деливерабл)

Классификация: Gemma-4-31B NF4 + несмёрженный QLoRA-адаптер 4.5h2, батч 1,
гейты v4: 3/5 (G1a 0.9214 · G1b 23/38 · G1c 0.8478 FAIL на 0.0005 · G1d 0.9586
· G1e 0.9610). Сигналы из комментариев — каркас «гибрид D»: v5-читатель треда
(сущности 4/4×2, шум, цитаты — на сданном), проход-1 — короткая per-comment
классификация с контекстом сущностей, проход-2 — сборка сигналов. Проход-1
ИЗМЕРЕН (pass1-probe-b): инструмент и транспорт зелёные, чтение subject_type
не взято; LoRA линии B (lora-b-run, 20.08) бар не взяла — **ничего не шипится,
адаптер лежит как evidence**. Позиции листовок: инструмент v2, промо+бейдж —
зелёные ноги; зачёркнутая цена не печатается нигде (законы 3.17/3.18/3.22).

## Дни 18–19.08 — сжато (полные записи: git-история этого файла, отчёты, ADR)

**18.08.** Недельное ретро (`docs/reviews/2026-08-18-weekly-retro.md`): enum
cause-тегов v2 ЗАКРЫТ, чеклист приёмки 6 пунктов, формула у каждой константы
(H6 — отказный гейт шага 1), kill-правило на биллинговом часе ДО milestone.
`pass1-probe-b` ПРИНЯТ — проход-1 ИЗМЕРЕН, база **9/14 при пороге 12**, все 5
потерь — subject_type; $0.135 из $0.20; звонок 5.162 с/коммент, проход-1 окна
= $1.095 генерации. `guard-until` ПРИНЯТ (гард `--until`, preflight H1;
45h2/srv2b/srv2d закрыты в полосе 7%). `pass1-data-prep` ПРИНЯТ (пак разметки
500, кодбук = закон v5+F2a+r2). Разметка r1 выполнена ТИМЛИДОМ; «наших»
классов 38.

**19.08.** `vault-dream` · `labels-boot-audit` (метки запломбированы
`8d2e1ba`) · `boot-debloat` (census 13.2K→9.7K, TARGET_KTOK 10.7) — ПРИНЯТЫ,
$0. **Сидение-2 (LoRA линии B):** два арма (A = 500·вес, B = 500 + top-up),
кап $6.00, бар gold r2 ≥12/14, одна попытка, красный → вариант C.
`pass1-redraw` ПРИНЯТ: премиса «целевого» дорозыгрыша МЕРТВА (ценз), арм B
переименован в top-up; разметка r2-150 выполнена тимлидом («наших» 38→49).
`lora-b` D0–D2 + `reader-topup` ПРИНЯТЫ (suite 3 114/2; рулинги оператора в
сессии исполнителя зарегистрированы: ветка C — топик в конверт купленного;
ветка B — reader-topup кап $2.00, бил $1.2259, entity-блоки арма B 39→282).
Талли дней: contract-health 6/3/7/8/11+4 · paid 2/2/1/1/3+3 — два спайка
авторства подряд → инструмент `make baselines` (исполнен шагом 0.5 lora-b).
Долги, живые: строка индекса `a-checker-whose-failure-is-silence` и
`the-argmax-and-the-max-are-two-rows`; дом механизма Dv521; Dv541 (hot.md
vs цель 10.7); прозовый дрейф hot.md (Dv536).

## День 20.08 — lora-b-run ИЗМЕРЕН: КРАСНЫЙ 9/14, линия B ЗАКРЫТА → сидение C

**`lora-b-run` (D3a–D4) исполнен и ПРИНЯТ 20.08 новой тимлид-сессией**
(`docs/reports/lora-b-run.md`, ADR `lora-b-red-and-line-b-closes`; suite
**3 169/2 — свой прогон тимлида**). D3a ($0, до пода): маска +1 символ через
закрывающую кавычку (гард ужесточён, тест меняет смысл, не цвет), кумулятивный
час с хард-стопом 5.5 ч = $4.40 < капа, projection-гейт на каждом логе,
бюджет-чек пересоздания, format-smoke на тренировочной строке; prereg —
commitment-блоки байт-в-байт к D2 (мой дифф по `8e3807a`), kill-clock только
ПРИБАВИЛ рунги 7–10. Первая попытка D3: **rung 1 STOP — A6000 нет в EU-RO-1**
(том пинует ДЦ), $0; рулинг оператора «ждать» — окно открылось через ~1 ч.
**Платная сессия:** под `0cnth2accoyj0n` A6000 $0.53/ч, 13:21:44Z→17:38:23Z =
15 399 с; ssh 31.8 с · бут 353 с · арм A 62 шага × 68.44 с · smoke GO ·
eval 64/64, 0 отказов · scp 10 файлов (хэши сошлись на Mac) · удаление
доказано листингом (мой листинг: `[]`, том — позитивный контроль).
**Бар: арм A 9/14 — те же пять промахов, что у базы (21601 · 47902 · 48283 ·
579457 · 580124); ничего не перевернул, ничего не потерял. Арм B НЕ ОБУЧЕН.**
Мой прогон скорера воспроизвёл вердикт байт-в-байт (кроме одного пина
provenance — Dv579, аддендум); построчная сверка — независимо.

**Почему арм B не обучен — простой исполнителя, не деньги:** арм A закончил
14:38Z, исполнитель вернулся ~17:20Z — **9 720 с (~$1.43) холостого биллинга**;
рунг 5 ($2.1938 ≤ $2.50) пропустил, рунг 7 (кумулятивный час) отказал: арм B
нужно 6 668 с, платформенный backstop оставлял 4 787 с. Без простоя арм B влез
бы с запасом >2 ч. Ни один рунг не смотрит на под, который НЕ обучается —
урок в скилл (дельта показана оператору). **Рулинг оператора на приёмке:
«Красный = ответ, линия B закрыта»** — по букве prereg (попытка тратится первым
gold-ответом; арм без ответов = not_evaluated, не 0/14). Арм B не измерен —
это честно входит в брифинг сидения C.

**Находка для сидения C (механизм, не «мало данных»):** адаптер выучил
маргинал тренировочного набора (52% не_наш_рынок): на census-50 `None` 25→9,
`не_наш_рынок` 8→23; loss 0.689→0.0095 на 1–2 токенах головы; две из пяти
потерь сменили неверный ответ (21601 null→сеть_ритейлер, 48283
null→не_наш_рынок). Супервизия одного `subject_type` на таком перекосе учит
prior, взвешивание не удержало. Формат не рассыпался, транспорт чист.

**Деньги (мой гард на приёмке):** LORA-B **$2.3624 из $6.00** (часы
15 399 с × $0.53 = $2.2671 + капель тома; биллинг-история отстаёт на ~32 мин —
класс Dv504); цикл-2 **$5.7892 из $20.00, остаток $14.2108, баланс $16.72**.
Свежий субагент-опровергатель: RED и деньги СТОЯТ; мелочи (5 437→5 389 с,
«79 шагов» ≠ 80, boot-GO записан на 480-й с при событии на 353-й) — в
Deviations аддендума, ни один гейт не переворачивается.

**Аддендум ($0, промпт `docs/PROMPT-lora-b-run-addendum.md`, выдан):** таблица
Deviations Dv575–586 на enum v2 (в отчёте её НЕ БЫЛО — протокольный долг),
коммит хвоста vault, перепрогон скорера, чтобы пин run_record совпал с git.
Талли финальное (после аддендума): contract-health 7 · paid 5 — см. очередь п. 2.

## Очередь контрактов

1. ✅ ПРИНЯТЫ 18–19.08: `guard --until` · `pass1-data-prep` · `vault-dream` ·
   `labels-boot-audit` · `boot-debloat` · `pass1-redraw` · `lora-b` D0–D2 ·
   `reader-topup` — см. сжатые дни выше и отчёты в `docs/reports/`.
2. ✅ **`lora-b-run` ПРИНЯТ 20.08** — КРАСНЫЙ 9/14, линия B ЗАКРЫТА (рулинг
   оператора на приёмке). **Аддендум ПРИНЯТ 20.08 (вечер), $0** — моя
   проверка: verdict.json 262 листа, сдвинут РОВНО один (пин run_record →
   sha коммита `1da4c29`, сверен тремя чтениями), arm_a_rows байт-в-байт,
   бар RED неподвижен; таблица Dv575–586 — 12/12 на enum v2, три числа
   приёмки исправлены по артефактам (простой 9 778 с / $1.4396; «5 437» —
   строка этого же отчёта; снимок arm-b-fit БЕЗ продюсера, но rung 8 даёт тот
   же STOP) — арифметика пересчитана мной; запечатанные файлы не тронуты;
   свип владения чист (83d1bf1 verbatim); `make check` **3 169/2 — свой
   прогон**. Грязь в дереве — только Stop-hook (index.md/daily log) —
   хвост следующей сессии. **Итоговое талли lora-b-run: contract-health 7
   (c-gap 3 · s-gap 1 · v-gap 3) · paid 5 (process 4 · env 1)** — спайк
   контракта ПЛАТНОГО прогона: каждый из 7 — про регистрацию (рунг без
   продюсера, рунг цены вместо наличия, гейт-проекция мягче своей цели) →
   в pass1-fewshot уже заложено: каждый рунг с продюсером и прогоном на
   фейк-клиенте до пода, рунг живости держит скрипт.
3. **`pass1-fewshot` — СЛЕДУЮЩИЙ, промпт НАПИСАН: `docs/PROMPT-pass1-fewshot.md`
   (свежая сессия ПОСЛЕ приёмки аддендума).** Рулинг 20.08 (п. 0 повестки) снял
   сидение C; меню тимлида (Gemma-4 остаётся): 0 холдаут-100 из меток ($0) →
   2 кодбук-клауза «о чём, а не что упоминает» + по одному размеченному соседу
   на класс (без обучения) → 1 проход-1 окна + проход-2 против референса; 3
   синтетика+LoRA и 4 LoRA «рассуждение→метка» — ступень ниже, если 2 не
   сдвинул. Три ответа оператора в совместном ревью: dev-200 (все 49 «наших» +
   151 по стратам) · выстрел по 14 в той же сессии по пре-регистрированному
   dev-гейту · гейт = v2 берёт ≥+10 строк категории/бренда против базы И общее
   согласие не хуже базы более чем на 5 (парно). **Кап $1.50**, worst $1.18 при
   потолке $0.80/ч (формулы в промпте), хард-стоп 1.75 ч = $1.40 < капа, рунг
   ЖИВОСТИ (600 с без события → KILL, держит скрипт `--watch`, не привычка).
   Третий выстрел по тем же 14 — множественность названа в регистрации.
4. **prep-b** — коллекция Сільпо/Varus/Маркетопт ($0, контракт согласован) —
   после читательского тракта (слово оператора 16.08).
5. `money-anchors` ($0) — по слову оператора (см. Открытые решения, п. 2).

**Сидение B/C 18.08: линия B, разметку делает ТИМЛИД.** Рамка честности
(судья не двигается · метки = тренировочные, аблируются · контаминация
исключена · кодбук = закон v5+F2a+r2) исполнена; ADR
`sitting-b-line-b-and-the-team-lead-labels`. Линия B закрыта 20.08 — ADR
`lora-b-red-and-line-b-closes`. Слепые 40 — опция, не блокирует.

## Деньги (чтение гарда тимлидом на приёмке 20.08, вечер)

Фаза 4 ЗАКРЫТА $32.4708 из $33.00 (финальное чтение 16.08, не растёт).
**Цикл-2: $5.7892 из $20.00, остаток $14.2108; баланс $16.72** (моё чтение на
приёмке lora-b-run; внутри — lora-b $2.3624 и reader-topup $1.2551 по
пессимистичной руке, пробы читателя/прохода-1 и капель тома ~$0.0116/ч
отдельной строкой). Влезает решённое (окно-2 ≈$4.8 · батч-пробник $2.13 ·
том ≈$2/нед); сидение C получит свой кап регистрацией; история 11 143 — нет.
Деньги читаются ИЗ ГАРДА (`scripts/runpod_guard.py`), не из этого файла и не
из отчётов.

## Открытые решения (повестка брифинга)

0. **РУЛИНГ ОПЕРАТОРА 20.08 (вечер, в тимлид-сессии) — ЗАРЕГИСТРИРОВАН ЭТОЙ
   ЗАПИСЬЮ:** база НЕ меняется — остаёмся на Gemma-4-31B; отдельное сидение C
   не нужно — слово оператора здесь = рулинг сидения; цель слоя — «такой же
   анализ комментариев, как ручной разбор тимлида» (`docs/REFERENCE-signals-w1.md`:
   F1–F5 · E1–E4 · N); разметку и дальше делает тимлид. Тимлид предлагает
   варианты в рамках этих ограничений (меню — в чате 20.08, выбранное — сюда).
1. **Следующая линия прохода-1** — выбирается из меню тимлида (п. 0); вход —
   этот файл + ADR закрытия линии B + `docs/reports/lora-b-run.md` §13. **Скилл team-lead: две дельты
   ПРИМЕНЕНЫ 20.08 по «да» оператора** (рунг ЖИВОСТИ — дедлайн от последнего
   события, не от create; Names: «пункт назначения — тоже имя» + «два списка
   покрывают друг друга», Dv539/544); эталон — проект Claude,
   `team-lead-SKILL-2026-08-20.md`. **Триггер размера СРАБОТАЛ: 705 строк ≥
   700 → сессия дистилляции с оператором ДО следующей фазы** (секционные
   правки, кейсы — в ADR/ревью репо, без переписывания целиком).
2. **Денежная гигиена (рулинги + контракт `money-anchors`, $0):** правая часть
   закрытия шага (записанное чтение отстаёт от биллинга — Dv504; чем сверять
   settled?), судьба зависшего walk'а pass1-probe (300k/427k мс), дисциплина
   `anchored_at` для 19 отказанных леджеров. Словом оператора.
3. **Дизайн командного центра** — отвергнут 15.08; направления на выбор —
   после установки Design-плагина.
4. Мелочи step-0.5 (ждут контракта, трогающего их файлы): `t5_depth`-бейдж ·
   валюта ₴ в шапке промо · fix-b(2) «две колонки — одно число».

## Живые рулинги читательского тракта (сидения 15–17.08, сжато)

- Эталон цели: `docs/REFERENCE-signals-w1.md` (ручной разбор тимлида 15.08:
  51 кандидат → ~дюжина тредов с сигналом); «система должна выдавать
  аналогичный ответ».
- Популяция тракта читателя окна-1: **129 тредов / ~1 032 платных коммента**
  (varto-глушитель снят для ТРАКТА ЧИТАТЕЛЯ; гейт оплаты классификации и
  бренд-атрибуция r1 не тронуты).
- Голд: ревизия **r2** (референс не правится; рулинг «категория» =
  «категория_личное» — отдельной записью; пины не двигаются зря).
- Транспорт (выстрадан v3→v5b, живёт в коде и суите): один JSON-объект, `[]`
  для пустого; расхождение двух top-level объектов = отказ; раннер режет
  генерацию на первом сбалансированном объекте; эхо msg_id из запроса,
  недостача печатается рядом с барами; потолок вывода 4000 (один на обе ноги);
  gate-0 «ssh жив к 180 с или KILL», ≤2 пересозданий, третий мёртвый под =
  STOP; лог пода scp'ится вместе с jsonl перед каждым гейтом; gate-записи —
  только append; поды, не serverless (рулинг 16.08: холодный старт 20 мин
  съел кап v3).
- F2a-carve-out (рулинг 17.08): событие о доступности/статусе предмета поста —
  сигнал о предмете поста; мнения без имени не переносятся.
- Рулинг «пак по убыванию payable» ОТОЗВАН 17.08 до всякого пода (гейтова
  таблица показала STOP на юните 1; урок: рулинг-фикс прогоняется через гейт,
  которому служит, ДО регистрации).
- **Однопроходная промпт-линия ЗАКРЫТА stop-rule'ом** (пре-регистрирован в v5,
  сработал на завершённом v5b: бар-1 2/5, бар-4 0.4286); ADR закрытия
  связывает тимлида; проход-1-классификатор = «two passes» из самого правила,
  не «v6».

## Отложено СОЗНАТЕЛЬНО (что · критерий развязки)

- **Serving-оптимизация (латенси)** — разбор 16.08 (git-хроника): холодный
  старт serverless ~239 с + декод ~5 ток/с — принятая цена отладки; критерий
  развязки: слово оператора «готовим прод» (ожидаемо — вход Фазы 7). Пути
  лечения известны (пре-квант база → старт ~1 мин; merge+vLLM новой
  прережкой → строка <1 с).
- **История 11 143 строк** — ждёт батч-решения И денег; вотермарк-люк известен
  (очередь читает 0; контракт истории сам сбрасывает поле `inference`).
- **Батч-паритет** — прережка пишется вместе с решением гнать историю (там
  окупается: $4.35 при −30%); проекция в
  `results/batch_cycle2_projection.json`.
- **5c3: категорийный слой** (таксономия 13+2 утверждена, механика не
  строена), сайдкар v2 (реакции/просмотры).
- **G1a/G1c: новая фаза качества или потолок принят** — на свежих данных
  цикла; сцеплено с судьбой читательского тракта (стенс-слой может изменить,
  что нужно от классификационных голов).
- **Алерты (вопрос 6)** — дизайн порогов словом оператора + нужно окно-2 для
  базовой линии. **Тренды** — окно-2. **Продажи ТПК** — по слову собственника,
  архитектура (SQLite по окнам) готова принять.
- **Мёрдж адаптера в bf16** — только после замера в прод-конфигурации.
- **LLM-нарратив отчёта (5d)** — модель выбирается на живых агрегатах.

## Правила, которые легко забыть

- Все eval гейтов — батчом 1; v2/v4-голды и запечатанные вердикты неизменны.
- Числа — только из result-файлов через скорер; в презентационные артефакты
  руками не вписываются (закон 3.20, отравленный тест).
- **Деньги читаются из гарда, не из леджера и не из прозы** (Dv33); у шагового
  чтения пока нет верхней границы окна — чинится контрактом «--until».
- **Прережка замерзает при первом `pod create` попытки**; правка после —
  только новой регистрацией; кап живёт в регистрации, его поднятие — слово
  оператора ДО эндпоинта.
- **Пины — потребители:** `prompts.py`, `brands.py`, `local_llm.py`,
  `window_summary_5c2.py`, PLAN-файлы пинуются запечатанными записями; правка
  требует MOVED-манёвра, не перепина. Префлайт любого контракта: греп пинов
  на всё, что он трогает (с «--until» — через preflight-инструмент H1).
- `registry.yaml` и `lexicon.yaml` запечатаны — правила матчинга в
  `config/watchlist_rules.yaml`; правки только новыми ревизиями.
- Никогда `git add -A`; файлы тимлида исполнитель коммитит, не правит;
  отчёты — файлами в `docs/reports/`, в чат — только путь.
- Впрыснутые треды проб (`injected: true`) никогда не входят в
  продакшн-агрегаты и цену окна.
- Пре-регистрации не переоткрываются; провал закрывает вопрос (one attempt);
  сработавший stop-rule закрывает ЛИНИЮ, не попытку.
- **Платный под не оставляют без опроса:** все рунги смотрят на МЕДЛЕННОЕ
  обучение, ни один — на под, который закончил и ждёт (9 720 с = $1.43 и
  потерянный арм B, 20.08). Рунг живости — в каждую следующую регистрацию.

## Урок недели (ретро 18.08)

Узкое место — тимлидский ярус (P1–P7, ревью ретро). Пилоты СРАБОТАЛИ: H1/H6
(«--until»), H5b (vault-dream), H4 (enum 18/18 →6/6 →8/8 →5/5), таймстемпы
базлайнов (спайк 6→3). H5a/H7 и split-талли фазы — на закрытии фазы 6.
