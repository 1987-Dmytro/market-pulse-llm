# STATUS — 14.09.2026 (обновлён тимлидом после приёмки (jjj) — «e2e-ship» закрыт, исполнительские айтемы фазы ВСЕ приняты)

## Миссия одним абзацем
Система читает промо-каналы ритейла (17 каналов, листовки + посты + комментарии), извлекает ценовые
позиции и реакции покупателей и показывает их директору по маркетингу в React-приложении: позиции с
фото листовок, аналитика цен по категориям (min/медиана/max с доказательствами), срез Полтавщины,
тренды, реакции, качество и петля с расписанием. Каждая цифра на экране — поле файла результата.

## Карта фазы `ship-1` — ты ЗДЕСЬ
Сделано: … · insight-2 · insight-1 · data-shape · front-2 · weekly-home (принят (fff): контентный
дайджест `d4fb6a44…` дважды на моих запусках; Δ на T4 — поле; экспорт `742013c1…`) ·
**region-collect (принят (hhh) моими прогонами: реестр `eff8ba5b…` и печать `e45860c6…` не
двигались, экран 1301; 18 каналов вне реестра (`config/region_channels.yaml`), корень
`data/raw_region/` — 15 889 постов · 8 232 коммента; baseline: «Гармонія» 0 · zarih 0 ·
myrhorodska-korivka 0 — нуль побайтово стабилен на моём перегоне сторожа; suite 4348/2)** ·
**reactions-region (принят (iii) моими прогонами: жалобы 110 сервис / 37 товар, 30 карточек
«промо→реакция» с корневыми постами, голос товара 95, помесячная линия 18 точек, вкладка Регіон
с нулевым baseline и строкой выборки «прочитано 1 008 із 3 700 тредів»; vitest 37/37)**.
**e2e-ship (принят (jjj) моими прогонами: чистый клон — front/tick/promo-screen exit 0, serve 200×2,
проба отказывает по имени файла ДО npm ci, truth 20 строк, session-блок; suite на рабочем дереве
4348/2, мой лог `/tmp/tl_make_check_s64.log`; три пина `e45860c6…` · `eff8ba5b…` · `25566dff…` целы;
стоп по suite-на-клоне рулен (a): §8 — предикат, клон доказывает ПРОДУКТ, suite живёт на рабочем
дереве со стором; 372 скипа в 65 файлах отклонены как ослабление)**.
Осталось: LinkedIn-пак (тимлид, свежая тимлид-сессия) → **ГЕЙТ (ты): среда 16.09 вечером**
(честный резерв: чт 17.09 12:00).

## Проверенные числа (файл рядом с каждым)
- Suite: **4348 passed / 2 skipped** — мой прогон 14.09 при приёмке e2e-ship
  (`/tmp/tl_make_check_s64.log`, 703.93 s); фронт: мой vitest **37/37**. Экспорт `742013c1…`→ движется с
  новыми блоками (раскрыто в рулингах); недельные файлы:
  контентный дайджест `d4fb6a44…` (пометь: принятый ранее `72d9d46e…` хешировал ПУТИ и штатно
  сменился при законном переезде — держится контентный).
- Категории, текущая неделя, ПОСЛЕ ручных исправлений по листовкам (`config/price_corrections.yaml`,
  8 строк, каждая с указанием страницы и читателей): морозиво ₴/кг 106/**287**/1074 (n=95), сир
  253/**390**/1248 (n=62), йогурт 50/**103**/214 (n=39); исключено 2 строки «сторінка без ціни» —
  `results/front_data.json :: category_prices` (sha `742013c1…`; медианы держатся — мой прогон 14.09).
- Полтавщина: Маркетопт 7 · АТБ 38 · ЕКО Маркет 2 · Сільпо «нет свежей листовки» — `:: regions`.
- S1 (извлечение позиций): completeness 0.2333 КРАСНАЯ · цена 0.9524 — `results/grade_positions_50.json`,
  опубликовано честно на вкладке Якість.
- S2 (сигналы): subject 0.75 (с P1) · signals 0.80 — файлы грейдов; 118 из 678 тредов прочитано.
- Реакции (моё чтение 13.09, файлы `results/promo_signals/` + `data/raw`): 281 сигнал с атрибуцией
  «о чём» — жалобы на СЕРВИС сети 110 против жалоб на товар 37; голос товара 95 строк; у всех 118
  прочитанных тредов корневой промо-пост с датой лежит на диске — теперь ВСЁ это на экране
  (принято (iii) 14.09): вкладка Реакції открывается ответом, а не лентой.
- Регион (после s62, `results/region_baseline.json`): 18 каналов вне реестра, 15 889 постов ·
  8 232 коммента (комменты собраны ВПЕРВЫЕ; выборка — новейшие 80 тредов/канал, 1 008 из 3 700,
  2 692 в черзі — добор бесплатными перегонами); упоминаний «Гармонія»/zarih/myrhorodska-korivka —
  0/0/0 (нуль защищён: stem-толерантный матчер добавляет ровно два ложных «гармонію смаків»); честная
  строка выборки уходит на экран в «reactions-region» (рулинг (hhh) 3).
- Деньги: цикл-3 $8.8398 из $10.00, REMAINING $1.1602; облако пусто; впереди только $0-шаги.

## Живые решения (твои, уже принятые 13.09)
Принят s52 · недельная рамка — правило (б) · парс-артефакты ПОЧИНЕНЫ руками по страницам (s54,
Dv4 утверждён: туба 750 г, страница решает) · таблица Позиції page-true (s55) — противоречие
экрана снято, «вилучено 2: сторінка без ціни» напечатано под таблицей · гейт ср 16.09 вечер ·
(eee, вечер): редизайн Реакцій + срез Полтавщины — одной задачей ДО гейта; единственное разрешённое
движение реестра — снятие паузы с районных каналов; словарь сторожа: «Гармонія» + региональные
конкуренты из реестра, файл `config/region_brands.yaml` правишь сам без рулинга · (ggg, 14.09):
стоп s61 закрыт формой (c) — реестр НЕ двигается вообще (флип ломал два сеала и, по замеру
исполнителя, ничего не собирал); региональный набор живёт в one-reader `config/region_channels.yaml`
(17 + dikankaa), сбор — в собственный корень `data/raw_region/`, экран остаётся 1301, оба пина
(`eff8ba5b…`, `e45860c6…`) целы; словарь сторожа = Гармонія + zarih + myrhorodska-korivka.

## Отложенные решения
- После гейта: публичный деплой `firebase deploy` (твоя команда); миграция/чистка `results/` (3,5 ГБ)
  — только после снятия байтовых пинов; **«s1-bakeoff» ($0, решено 13.09)** — готовые локальные OCR
  (PaddleOCR · Surya · docTR) против текущего чтения на том же эталоне тем же грейдером, выбор
  инструмента S1 для этапа 2 — по таблице; перенос запечатанных цитат из STATUS в архивный файл; error boundary внутри вкладок (именованный отказ вместо пустой страницы, находка s59).

## FINISH — что считается «готово» и когда
ВСЕ исполнительские айтемы фазы ПРИНЯТЫ ((jjj) 14.09 — e2e-ship закрыл список); критический путь
теперь у ТИМЛИДА: LinkedIn-пак (пост + скриншоты + легенда, только из README и файлов результатов;
свежая тимлид-сессия 15.09) — исполнитель больше не нужен до гейта.
**Ответ на «когда»: продукт готов к твоему гейту в среду 16.09 вечером** (честный резерв чт 17.09
12:00; резерв больше не зависит от сбора — он лишь на случай твоего времени).
«Готово» = ты запускаешь: `make serve` → открываешь `http://localhost:8000/` (тёмная тема,
листовки, категории, Полтавщина, T0–T8), проходишь 10-секундный квиз, читаешь 20 строк
`truth_20.json` — и говоришь своими словами, что верно и что нет. **Публичная витрина УЖЕ в сети
(рулинг (mmm), 15.09): https://market-pulse-three-zeta.vercel.app — тот же артефакт `bf17dba3…`,
тёмный первый рендер, печать светлая; передеплой = `vercel deploy --prod` из `dashboard/app/`.**
Недельный режим: интервал 168 ч ты выставляешь сам
на вкладке Петля — это настройка, не код.

## Архив запечатанных цитат рулингов (машинная секция — гарды регистраций греппят эти предложения ДОСЛОВНО; не редактировать, не перефразировать, не удалять; рулинг (yy) 13.09)

(в) **Затем — (4) LoRA «рассуждение→метка» поверх v2 с (3) синтетикой как армом B** одной регистрации; цель
обучения — из (б).

(к) **РУЛИНГ 22.08 (вечер) — дизайн `lora-c`:** промпт v3 = v2 + поле `rationale` ПЕРЕД меткой, одним
рендерингом для обучения и инференса; таблица парно в четыре колонки (база v2 · база v3 · адаптер A · адаптер
B) на одном пуле соседей; рационали для обучающих строк пишет Claude Code по кодбуку и метке тимлида, тимлид
ревьюит все «наши» + ~40 пограничных; синтетика ~160 (по 40 на класс ошибки) — арм B, тег в provenance,
абляция армом A, никогда в холдаут/голд; из обучения и из пула соседей исключены холдаут-100 и ВСЕ строки 16
референсных тредов; **бар на арм (один выстрел): холдаут-100 ≥ 64 И сквозной бар 1 = 5/5 И бар 3 = 0 — все три
или КРАСНЫЙ**; отчётно: ячейка «упоминание → категория» на холдауте (18 → ?), dev-200 (fit), 14 золотых
(шестой взгляд). Два контракта: `lora-c-prep` ($0, с ревью-гейтом тимлида) → `lora-c-run` (один A6000, кап
**$4.00**).

синтетический запрос рендерится ТЕМ ЖЕ правилом на ТОМ ЖЕ пуле 515 (закон уже запрещает синтетику только как
СОСЕДА; запрет двойника действует)

проход-2 в ране — ТОЛЬКО для армов (2 ноги, не 4): база v2 сквозной уже измерена (r2: 4/5 · 2), база v3 бара
не берёт — экономия ~$0.47 по заряду.

кап lora-c-run **остаётся $4.00** (оба существующих замера s/step влезают по фактической цене; смок решает,
рунг проекции убивает честно)

карта — **RTX PRO 4500, 32 GB, $0.72/ч, EU-RO-1** (A6000 в датацентре тома none; 32 GB — запас против OOM при
замороженном micro_batch; фолбэк 4090 преавторизован)

ложная строка топика в синтетических заголовках заменяется выделенной константой (продовая строка NO_POST_TEXT
в синтетике запрещена); конфаунд маркера принят и именован; в ран добавлен отчётный маркер-ценз (~$0.05) —
отделяет «не помогло» от «выучил маркер»

ONE attempt per stage; no re-runs.

the same registered prompts, the same instances, Gemma-4-31B thinking ON

Nothing is tuned: no prompt edit, no pack edit, no adapter.

ONE pod (RTX PRO 4500 32 GB, EU-RO-1, `mp-srv2`; fallback 4090 $0.74), cap $8.00

Система бесполезна, пока не находит сигналы в комментах и тредах на уровне ручного разбора тимлида; **только
локальная Gemma-4-31B, thinking ОБЯЗАТЕЛЬНО**; API-модели отклонены.

читатель с thinking ON ни разу не измерен; регистрации сами пишут «thinking-ON reader = НОВАЯ регистрация»

Решение о дальнейшем пути — на этой таблице.

(0) price ≤$0.80/h at create

(1) liveness — 900 s from the LAST reply or `nvidia-smi` activity (thinking is silent longer than JSON)

(2) projection after the smoke and after every stage at MEASURED s/call — over the cap by ≤20% → **ASK**: hold
the pod ≤10 min for the operator's typed word (quoted verbatim in the report), silence = KILL; over by more →
KILL;

(3) platform hard stop from the cap at the observed price

Свидетель D2 шага 0 (тест греппит дословно): D1 (инструмент, $0)
