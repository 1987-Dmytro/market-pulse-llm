# STATUS — market-pulse-llm (2026-08-16; компактная карта)

> Операторский документ, по-русски по решению оператора. Правит только тимлид,
> после приёмок. Полная хроника фаз 0–5 и решений 1–23 (до 14.08) — в
> git-истории этого файла; здесь — карта и живые решения.

## Что это за проект (одним абзацем)

Ключевой инструмент маркетинговых исследований компании (ТМ «Гармонія»):
мониторим Telegram-каналы украинских ритейл-сетей (Сільпо, АТБ, Varus,
Маркетопт) и агрегаторов, находим анонсы новинок и промо по молочке/мороженому,
извлекаем бренды и позиции с ценами, классифицируем реакции покупателей
(тональность, сарказм, скрытые интенции), и превращаем это в командный центр
директора по маркетингу. Ядро — открытая LLM (Gemma-4-31B), дообученная QLoRA;
GPU только арендные, между сессиями консоль пуста. Северная звезда — семь
вопросов директора в `docs/PRODUCT.md`.

## Карта фаз — ГДЕ МЫ

| Фаза | Что | Статус |
|---|---|---|
| 0–4.5 | Спека (rev 3.22) · скелет · данные · бейзлайны · QLoRA · программа качества | ✅ (хроника в git) |
| 5 | Продакшн-петля: 5a…5c2 — окно-1 куплено целиком, сидение ратифицировало обе половины | ✅ 14.08 |
| **6** | **Командный центр + слой сигналов из комментариев** | **🔄 ТЫ ЗДЕСЬ — v1 центра построен; probe-b принята, v2-инструмент закрыт провалом; сидением 16.08 назначен v3 (A+B), эталон цели — `docs/REFERENCE-signals-w1.md`** |
| 7 | Валидация на ≥2 окнах + упаковка | — |

## Модель сейчас (деливерабл)

Классификация: Gemma-4-31B NF4 + несмёрженный QLoRA-адаптер 4.5h2, батч 1,
гейты v4: 3/5 (G1a 0.9214 · G1b 23/38 · G1c 0.8478 FAIL на 0.0005 · G1d 0.9586
· G1e 0.9610). Читатель тредов (новая задача): база БЕЗ адаптера, промпты
reader v1/v2 зарегистрированы, способность доказана качественно, бары НЕ
взяты; v2 закрыт провалом по правилу, v3 (A+B) — к регистрации (сидение 16.08). Позиции листовок: инструмент v2, промо+бейдж — зелёные
ноги; зачёркнутая цена не печатается нигде (законы 3.17/3.18/3.22).

## День 16.08, сессия 2 — probe-b принята после свежей верификации; сидение по читателю

Тимлид перепроверил отчёт с нуля по артефактам: `make check` **2562 / 2 skipped**
(свой прогон); скорер баров перепрогнан из сохранённых строк — вердикт
воспроизвёлся **байт-в-байт**; популяция 23/134/4 и дайджест `ccef35fa…`
сошлись; облако перелистано с позитивным контролем (serverless `[]` · pods `[]`
· том виден); ownership-свип чист. **Приёмка probe-b подтверждена.** Эталон
цели найден и подтверждён оператором: `docs/REFERENCE-signals-w1.md` (ручной
разбор тимлида 15.08, 51 кандидат → ~дюжина тредов с сигналом) — «система
должна выдавать аналогичный ответ».

**Перечитка гарда утром (требование Dv407/Dv411):** шаговый гард ОТКАЗЫВАЕТ —
«probe-b $0.4396 из $0.35», но разложение честное: с удаления эндпойнта
(12.9 ч, в облаке пусто) баланс тает ровно на ставке тома ($0.0116/ч), чистая
проба ≈ **$0.29 — ВНУТРИ капа**. Новый дефект в семью Dv411: у шагового чтения
нет ни конца, ни атрибуции по ресурсам (том капает в «шаг» вечно) — чинится
денежным контрактом, не раньше.

**Решения оператора на сидении 16.08 (его словом):**
1. **Линия цикла-2: $20.** Амендмент в SPEC — штатным манёвром следующего
   контракта, ДО первого платного шага против линии; Фаза-4-леджер закрывается
   финальным чтением. *Уточнение после приёмки:* $20 — деньги, УЖЕ лежащие на
   счету (пополнение 15.08); порог «баланс ≥ $40», попавший в 3.23 (2), — ошибка
   тимлида, снимается 3.24.
2. **Инструмент читателя: A+B одной новой регистрацией (v3)** — толерантность
   парсера к КОНТЕЙНЕРАМ явным рулингом (домены строгие; расхождение двух
   top-level объектов = отказ, не last-wins) + v3-промпт «один JSON-объект,
   `[]` для пустого» + вопросы разрыва чтения (F1b/F1c/F2a) в промпте;
   перечитка тех же 23 тредов ~$0.08, бары против голда-r2.
3. **Varto-глушитель снят для ТРАКТА ЧИТАТЕЛЯ:** популяция читателя окна-1 —
   129 тредов (плюс-спам/скам тредов не снимают — измерено цензом); гейт
   оплаты классификации и бренд-атрибуция r1 НЕ тронуты. Новая ячейка ценза
   (narrow · varto off · plus-spam/scam on) меряется в контракте, не выводится.
4. **Словарь-рулинг (адъюдикация):** «категория» референса = «категория_личное»
   таксономии. Голд перевыводится ИМЕНОВАННОЙ ревизией (r2); сам референс НЕ
   правится — рулинг живёт отдельной записью, пины не двигаются зря.

**Очередь контрактов:** ✅ `cycle2-money` исполнен и ПРИНЯТ 16.08 (отчёт
`docs/reports/cycle2-money.md`, Dv413–423): Фаза-4 закрыта, 3.23 приземлён,
гард читает шаг двумя чтениями с атрибуцией по ресурсам, probe-b закрыт
$0.3132. ✅ `reader-v3-prep` исполнен и ПРИНЯТ 16.08 (отчёт
`docs/reports/reader-v3-prep.md`, Dv424–434, суита 2623/2): 3.24 приземлён,
**линия цикла-2 ЗАЯКОРЕНА** ($22.5098, потрачено $0.02 — только том); парсер v3
(три контейнерных ремонта, расхождение = отказ; рулинг тимлида по Dv426 —
остаёмся на узком чтении «map keyed by msg_id»), промпт v3 (6 правок _swap,
sha `22533644…`, полный текст в приложении отчёта), голд-r2 (12 клеток
словаря, референс не тронут), ячейка читателя ИЗМЕРЕНА (129 тредов · 1 032
платных · окно ~$1.72; все 23 треда пробы production-reachable — впрыск
кончился, Dv427), регистрация v3 заморожена ($0.3132 прогноз-ПОЛ, кап $0.35
держит 1.165× замедление; риск потолка токенов на ОКНЕ — замер в run, Dv433).
⚠️ `reader-v3-run` исполнен и ПРИНЯТ 16.08 как ОТРИЦАТЕЛЬНЫЙ результат
(`docs/reports/reader-v3-run.md`, Dv436–445): serverless-бут 20,2 мин съел кап
($0.3918; биллинг подтвердил 1 211 с исполнения — настоящий холодный старт,
не srv-2b-очередь), ни один тред не прочитан, бары UNSCORED; всё удалено с
доказательством, том остался застейдженным на нужном коммите. Куплено за $0:
два дефекта замороженной регистрации (бар-3 молча разросся 5→6 с потерей
исключения N1; словарная склейка после r2 бьёт в обратную сторону) и честная
арифметика ворот (реальный стоп — 69.6 с варм-апа, не «1.165×»). Решение
оператора 16.08: **v4 на ПОДЕ** (бут виден в логе, kill-правило, ~$0.20–0.30).
Дальше: **`reader-v4`** (`docs/PROMPT-reader-v4.md`) — регистрация v4 (бар-3
на 5 тредах с исключением · склейка словаря в обе стороны · подовая
арифметика с kill-правилом) + прогон на поде 4090, кап $0.35. prep-b — после
читателя.

## День 16.08 — латенси разобрана по записям ($0, сессия тимлида, без исполнителя)

Разбор из `results/serving_5b.json` и `results/serving_srv2d_smoke.json`:
«космическая» задержка = **холодный старт serverless ~239 с** (из них ~214 с —
загрузка 62 ГБ bf16 + NF4-сжатие на лету; `idleTimeout: 60` убивает воркер через
минуту простоя, поэтому каждый одиночный запрос платит старт заново) + **медленный
декод ~5 ток/с** (стек bnb-NF4 + несмёрженный LoRA + HF generate, батч 1 по
3.11 (2) — исследовательский, выбран в 5b сознательно ради чистоты паритета).
Тёплая строка одинакова на поде и serverless (4.07 против 4.10 с) — runtime и
карта НЕ виновники; 4090 — измеренный лидер цена/скорость (probe-b, 2.64× L4);
очередь тёплая ~1 с. Пути лечения объяснены оператору (пре-квант база → старт
~1 мин; merge+vLLM новой прережкой → строка <1 с, тред ~5 с).

**Решение оператора 16.08:** лечение **отложено до готовности системы к проду**
(«пока отлаживаем»); runtime и карты остаются как есть (3.14 serverless;
классификация — класс AMPERE_48; читатель — 4090); бюджетная линия в ТОЙ
сессии не открывалась (открыта позже, сидением сессии 2: $20). ADR этого
решения — шагом 0.5 следующего исполнительского контракта.

## День 15.08 — что построено и решено (шесть контрактов + две пробы, всё принято)

1. **prep-a ($0):** правило 3.19 в очереди — платная популяция окна 3 714 из
   5 075; бестекстовые (1 361) остаются собранными, но не покупаются. Находка:
   ВСЕ бестекстовые были «нейтральны» → честная чистая тональность окна
   **+10.5%** (была +7.7%). Проекция батча: окупается только на истории —
   **отложено с критерием** «вместе с решением об истории 11 143».
2. **6a ($0):** слой агрегатов SQLite (окна накапливаются) + детерминированный
   экспорт (сходимость с печатью окна **902/902 листа**) + словарь метрик
   UA/EN. Находки: retail_official — единственный сегмент в минусе (−11%,
   «жалуются там, где сеть слушает»); regional — улики есть, разговора нет.
3. **6b ($0):** командный центр v1 — один HTML, 9 вкладок, UA/EN, свет/тьма,
   ⓘ-справки, дрилл-даун seed-42 до оригиналов, «ни одного числа руками»
   (доказано отравленным экспортом). Амендмент 3.20.
4. **КРАСНЫЙ ГЕЙТ сидения (оператор):** бренд-атрибуция мусор, промо без
   конкретики, дизайн отвергнут. Разбор по строкам подтвердил: 7 из 11
   «бренд-строк» — слово «варто», 1 — детский центр «Гармонія».
5. **fix-a ($0):** правила r1 в НОВОМ файле `config/watchlist_rules.yaml`
   (registry/lexicon запечатаны пинами — не правятся) — бренд-строк 11 → 3;
   **SoV Гармонії = 0.00%** — честно: наш бренд в комментах окна не звучит.
   Ценз гейта: релевантно **9.5%** платных строк (111 тредов узкого гейта).
6. **fix-b ($0):** промо-таблица на T5 — **145 позиций**: бренд × позиция ×
   сеть × промо-цена × печатный % × глубина × ссылка; «наших» позиций в промо
   окна — ноль (честный факт). Бонус: «Селянське» разрешилось контекстом
   листовки = линейка Молокії (масло 180 г, −28% в АТБ).
7. **fix-c + закон 3.22:** строчные поверхности печатают только бейдж-глубину —
   арифметическая восстанавливала запрещённую старую цену до копейки.
8. **Архитектура комментариев ратифицирована (3.21 + PLAN-comment-signals):**
   гейт на полноту (лексикон+глушители) → LLM-читатель треда целиком (тема →
   сущности КОНТЕКСТОМ → сигналы с цитатами-доказательствами) → лента
   «Сигналы окна». Таксономия, бары (флагманы 5/5 · сущности 4/4 · шум 0 ·
   согласие ≥0.80 · ≤30 мин/окно) — словом оператора.
9. **probe-a ($0.094 по гарду):** читатель ЧИТАЕТ — 3/3 вердикта содержательно
   верны (на рецептном треде сам разрешил три молочные марки контекстом), но
   интерфейс отбил все три (схема), а L4 дала 54.8 с/тред → ворота честно
   сказали STOP до основной массы. Кап поднимался оператором $0.20 → $0.45 до
   создания эндпоинта.
10. **probe-b ($0.32 из капа $0.35, ещё settle-ится):** карта решена — **4090
    (ADA_24) в 2.64× быстрее L4 по той же цене**; ворота GO; все 23 треда
    прочитаны; **бары впервые посчитаны и НЕ взяты**: флагманы 2/5 (с
    контейнерными починками 3/5), сущности 2/4 → 3/4 (E1 «Гармонія-центр» и
    оба «варто» отвечены — на впрыснутых тредах), шум «0» — но над пустотой
    (4 из 5 тредов не распарсились), согласие 0.357 → 0.643 при пороге 0.80.
    Причина №1 — КОНТЕЙНЕРЫ: 10 из 23 ответов отбиты шестью новыми формами
    (пустой объект вместо списка, ответ двумя JSON-объектами…), ни одна не из
    закрытых v2. Причина №2 — настоящий гэп чтения (флагманы F1b/F1c/F2a
    пропущены в распарсенных тредах). **Попытка v2-инструмента закрыта
    провалом по правилу** — дальше только новая регистрация после
    дизайн-разбора. Окно 111 тредов на 4090 стоит $0.71–1.58.

## Деньги (после приёмки `cycle2-money`, 16.08)

**Фаза 4 ЗАКРЫТА: $32.4708 из $33.00** финальным чтением 11:09 UTC — больше не
растёт. probe-b закрыт честно: **$0.3132 из $0.35** (том — отдельной строкой).
Баланс **$22.52** — и это **деньги линии цикла-2**: пополнение $20 легло ещё
**15.08** (доказано закрывающей записью: разрыв двух чтений фазы = $19.99).
**Порог $40 в 3.23 (2) — ошибка тимлида** (порог построен на непроверенном
допущении «пополнение будет новым, поверх»); снимается амендментом **3.24**
шагом 0.5 `reader-v3`, якорь линии берётся осознанным прогоном от текущего
баланса. Новых платежей НЕ требуется. Потрачено из линии к вечеру 16.08: **$0.43**
(несостоявшийся serverless-прогон v3 $0.39 + том), остаток **$19.57**; шаг
`reader-v3` стоит нижней границей и закрывается первым прогоном гарда
(биллинг запостился: 1 211 с serverless + 300 с стейджинг-пода). Влезает всё
решённое (окно-2 ≈$4.8 · читатель на поде ~$0.2–0.35 · батч-пробник $2.13 ·
том ≈$2/нед); история 11 143 — нет.

## Открытые решения (повестка следующего брифинга)

1. **Дизайн командного центра** — отвергнут оператором; Design-плагин НЕ
   установлен (карточка в чате); направления на выбор — после установки.
2. **prep-b — коллекция Сільпо/Varus/Маркетопт** ($0, контракт согласован) —
   в очередь ПОСЛЕ читателя: слово оператора 16.08 «мы не закончили с
   комментариями».
3. Мелочи step-0.5: заголовок `t5_depth` (бейдж, не пара цен), валюта ₴ в
   шапке промо-таблицы, fix-b(2) «две колонки — одно число»; пачка закрытий
   22 старых шаговых леджеров — при первом следующем контракте, трогающем гард.

_Дизайн-разбор читателя и деньги цикла-2 РЕШЕНЫ сидением 16.08 — четыре
рулинга в «Дне 16.08, сессия 2» выше. Батч-паритет по-прежнему отложен с
критерием (вместе с историей 11 143)._

## Отложено СОЗНАТЕЛЬНО (что · критерий развязки)

- **Serving-оптимизация (латенси)** — разбор 16.08 в дне выше; критерий
  развязки: слово оператора «готовим прод» (ожидаемо — вход Фазы 7/упаковки).
  До того окна и история идут на текущем стеке (~4.1 с/строка, тред 21–32 с),
  а одиночные запросы несут холодный старт ~4 мин — это принятая цена отладки,
  не дефект конфигурации.
- **История 11 143 строк** — ждёт батч-решения И денег; вотермарк-люк известен
  (очередь читает 0; контракт истории сам сбрасывает поле `inference`).
- **Батч-паритет** — прережка пишется вместе с решением гнать историю (там
  окупается: $4.35 при −30%); проекция в `results/batch_cycle2_projection.json`.
- **5c3: категорийный слой** (таксономия 13+2 утверждена, механика не строена),
  сайдкар v2 (реакции/просмотры), правила ВАРТО/Селянське именованной ревизией
  прод-гейта — частично исполнено раньше срока в r1 (fix-a).
- **G1a/G1c: новая фаза качества или потолок принят** — решение на свежих
  данных цикла; вопрос теперь сцеплен с судьбой читателя (стенс-слой может
  изменить, что вообще нужно от классификационных голов).
- **Алерты (вопрос 6)** — дизайн порогов твоим словом + нужно окно-2 для
  базовой линии. **Тренды** — окно-2. **Продажи ТПК** — трек по слову
  собственника, архитектура (SQLite по окнам) готова его принять.
- **Мёрдж адаптера в bf16** — только после замера в прод-конфигурации.
- **LLM-нарратив отчёта (5d)** — модель выбирается на живых агрегатах.

## Правила, которые легко забыть

- Все eval гейтов — батчом 1; v2/v4-голды и запечатанные вердикты неизменны.
- Числа — только из result-файлов через скорер; в презентационные артефакты
  руками не вписываются (на дашборде это закон 3.20 и отравленный тест).
- **Деньги читаются из гарда, не из леджера и не из брифа** (Dv33; у
  step-чтения одно чтение — Dv411, чинится следующим денежным контрактом).
- **Прережка замерзает в момент создания эндпоинта**; правка после — только
  новой регистрацией. Кап живёт в регистрации; поднятие капа — слово
  оператора, амендмент регистрации ДО эндпоинта (прецедент probe-a).
- **Пины — потребители:** `prompts.py`, `brands.py`, `local_llm.py`,
  `window_summary_5c2.py`, PLAN-файлы пинуются запечатанными записями; правка
  (даже тимлидская, даже верная — Dv397) требует MOVED-манёвра, не перепина.
  Префлайт любого контракта: греп пинов на всё, что он трогает.
- `registry.yaml` и `lexicon.yaml` запечатаны — правила матчинга живут в
  `config/watchlist_rules.yaml`; правки только новыми ревизиями.
- Никогда `git add -A`; файлы тимлида исполнитель коммитит, не правит;
  отчёты — файлами в `docs/reports/`, в чат — только путь.
- Впрыснутые треды пробы (`injected: true`) никогда не входят в
  продакшн-агрегаты и в цену окна.

## Урок процесса дня (в ретро фазы, талли)

Семейство «пин-как-потребитель» сработало четырежды за день (Dv330-класс:
Dv335/337, Dv380, Dv397 — последний на правке САМОГО тимлида). Правило
эскалировано: греп пинов — обязательный шаг префлайта для обеих ролей, включая
правки тимлида в собственных файлах. Второе семейство дня — «контейнер против
домена» у читателя (Dv393/394 → Dv404): формулировка схемы чинит поле, но не
класс; решение о толерантности парсера к контейнерам — разовое и явное, на
дизайн-разборе. → Принято 16.08 (A+B). Третье семейство: денежный инструмент —
шаговое чтение гарда без конца и без атрибуции по ресурсам (том капает в «шаг»
вечно, вчерашняя «нижняя граница» стала сегодняшней верхней без предела);
эскалация прозы в код — контрактом `cycle2-money`.
