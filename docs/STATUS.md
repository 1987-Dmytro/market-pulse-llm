# STATUS — market-pulse-llm (2026-08-22, вечер; компактная карта)

> Операторский документ, по-русски по решению оператора. Правит только тимлид,
> после приёмок. Полная хроника фаз 0–5 и дней 15–17.08 — в git-истории этого
> файла и в docs/reports/; здесь — карта и живые решения.

## Что это за проект (одним абзацем)

Ключевой инструмент маркетинговых исследований компании (ТМ «Гармонія»):
мониторим Telegram-каналы украинских ритейл-сетей (Сільпо, АТБ, Varus,
Маркетопт) и агрегаторов, находим анонсы новинок и промо по молочке/мороженому,
извлекаем бренды и позиции с ценами, классифицируем реакции покупателей
(тональность, сарказм, скрытые интенции), и превращаем это в командный центр
директора по маркетингу. Ядро — открытая LLM (Gemma-4-31B), дообученная QLoRA;
GPU только арендные, между сессиями консоль пуста. Северная звезда — семь
вопросов директора в `docs/PRODUCT.md`.

## Карта фаз — ГДЕ МЫ

| Фаза | Что | Статус |
|---|---|---|
| 0–4.5 | Спека (rev 3.24) · скелет · данные · бейзлайны · QLoRA · программа качества | ✅ (хроника в git) |
| 5 | Продакшн-петля 5a…5c2: окно-1 куплено целиком, сидение ратифицировало обе половины | ✅ 14.08 |
| **6** | **Командный центр + слой сигналов из комментариев** | **🔄 ТЫ ЗДЕСЬ — v1 центра построен; однопроходный промпт-читатель ЗАКРЫТ stop-rule'ом 17.08; каркас — гибрид D (сидение 17.08); проход-1 ИЗМЕРЕН 18.08: база 9/14 при пороге 12 → сидение выбрало линию B (разметка subject_type + LoRA); ЛИНИЯ B ИЗМЕРЕНА 20.08: арм A 9/14 → КРАСНЫЙ, линия B ЗАКРЫТА; рулинг 20.08: база остаётся Gemma-4; ПРОМПТ v2 ИЗМЕРЕН 21.08: dev-гейт КРАСНЫЙ +7 из +10 → рулинг: v2 = базовый промпт, сквозной замер окна; ПРОХОД-1 ПО ОКНУ-1 КУПЛЕН ЦЕЛИКОМ 21.08 (1 032/1 032, $0.742) → СЛЕДУЮЩИЙ: `pass2-signals` — сборка сигналов против `docs/REFERENCE-signals-w1.md`** |
| 7 | Валидация на ≥2 окнах + упаковка | — |

## Модель сейчас (деливерабл)

Классификация: Gemma-4-31B NF4 + несмёрженный QLoRA-адаптер 4.5h2, батч 1,
гейты v4: 3/5 (G1a 0.9214 · G1b 23/38 · G1c 0.8478 FAIL на 0.0005 · G1d 0.9586
· G1e 0.9610). Сигналы из комментариев — каркас «гибрид D»: v5-читатель треда
(сущности 4/4×2, шум, цитаты — на сданном), проход-1 — короткая per-comment
классификация с контекстом сущностей, проход-2 — сборка сигналов. Проход-1
ИЗМЕРЕН (pass1-probe-b): инструмент и транспорт зелёные, чтение subject_type
не взято; LoRA линии B (lora-b-run, 20.08) бар не взяла; промпт v2
(pass1-fewshot r2, 21.08) dev-бар не взял (+7 из +10) — **ничего не шипится**;
v2 — ПРОВИЗОРНЫЙ базовый промпт прохода-1 (рулинг (а)), окно-1 им пройдено;
проход-2 — смок измерен (день 22.08). Позиции листовок: инструмент v2, промо+бейдж —
зелёные ноги; зачёркнутая цена не печатается нигде (законы 3.17/3.18/3.22).

## Дни 18–19.08 — сжато (полные записи: git-история этого файла, отчёты, ADR)

**18.08.** Недельное ретро (`docs/reviews/2026-08-18-weekly-retro.md`): enum
cause-тегов v2 ЗАКРЫТ, чеклист приёмки 6 пунктов, H6 — отказный гейт шага 1.
`pass1-probe-b` ПРИНЯТ — база **9/14 при пороге 12**, все 5 потерь — subject_type;
5.162 с/коммент. `guard-until` · `pass1-data-prep` ПРИНЯТЫ. **19.08.** `vault-dream`
· `labels-boot-audit` (метки `8d2e1ba`) · `boot-debloat` (TARGET_KTOK 10.7) ·
`pass1-redraw` (целевой дорозыгрыш МЁРТВ — ценз) · `lora-b` D0–D2 · `reader-topup`
($1.2259) — ПРИНЯТЫ. Разметка 650 строк ТИМЛИДОМ: **«наших» 49** — вся популяция.
Долги, живые: строки индекса `a-checker-whose-failure-is-silence`,
`the-argmax-and-the-max-are-two-rows`; дом механизма Dv521; Dv541; Dv536.

## День 20.08 — lora-b-run ИЗМЕРЕН: КРАСНЫЙ 9/14, линия B ЗАКРЫТА

**`lora-b-run` + аддендум ПРИНЯТЫ 20.08** (`docs/reports/lora-b-run.md`, ADR
`lora-b-red-and-line-b-closes`). Под A6000, 15 399 с, $2.3624 из $6.00. **Бар:
арм A 9/14 — те же пять промахов, что у базы (21601 · 47902 · 48283 · 579457 ·
580124). Арм B НЕ ОБУЧЕН** — простой исполнителя 9 720 с (~$1.43) → рунг
ЖИВОСТИ в скилл и в каждую регистрацию. **Рулинг оператора: «Красный = ответ,
линия B закрыта»** (арм без ответов = not_evaluated, не 0/14). **Механизм (не
«мало данных»):** адаптер выучил маргинал тренировочного набора (52%
не_наш_рынок): на census-50 `None` 25→9, `не_наш_рынок` 8→23; loss 0.689→0.0095
на 1–2 токенах головы — супервизия одного `subject_type` на таком перекосе учит
prior. Талли: contract-health 7 · paid 5 — каждый из 7 про регистрацию.

## День 21.08 — промпт v2 ИЗМЕРЕН (pass1-fewshot r1→r2): dev-гейт КРАСНЫЙ +7 из +10

**r1** (`docs/reports/pass1-fewshot.md`, Dv587–606): D0 ПРИНЯТ; D1 не состоялся
(два 4090 убиты рунгом 2, $0.1178, попытка цела) → рулинг: r2, бюджет шага $1.50.
**r2** (`docs/reports/pass1-fewshot-r2.md`, Dv607–622) **ПРИНЯТ 21.08**: четыре
поправки (рунг 2 → 500 с; рунг 3 от `launched_at` + бэкстоп 1 100; пины; кап
$1.38); один 4090, 1 359 с = **$0.279**; ssh ≤50 с · загрузка 142.7 с · 2.29/2.73
с/звонок. Моя приёмка: suite свой; r1 байт-в-байт; git-часы впритык (47 с);
dev-гейт пересчитан МОИМ парсером; опровергатель 8/8. **Талли r2: contract-health
11 (c-gap 3 · s-gap 1 · v-gap 7) · paid 5.**

**Что измерено (пересчитано тимлидом по out-файлам и меткам, dev-200):**

| | база v1 | v2 |
|---|---:|---:|
| согласие на 200 | 87 (43.5%) | **136 (68%)** |
| «наши» 49 строк верно | 31 | **38** (+7; бар +10 → КРАСНЫЙ) |
| молчание (`не_наш_рынок → None`, из 85) | **48** | 12 |
| упоминание принято за суть (`не_наш_рынок → категория`) | 22 | **24** |
| `категория → не_наш_рынок` (новая ошибка) | ~0 | 6 |
| фильтр «наших»: полнота / точность | 65% / 44% | **80% / 55%** |

Чтение: база молчала — v2 научил отвечать; граница «упоминает vs о чём» не
сдвинулась и стала главной ошибкой v2. По 14 золотым не стреляли. Шаг $0.397.
**Ценз всего окна (отчётные строки, НЕ бары):** v2 на 1 032: не_наш_рынок 460 ·
None 291 · категория 207 · сеть 59 · бренд 15; 650 размеченных → **395 (60.8%)**,
«наших» 38/49; **холдаут-100 → 64/100, «наших» 6/8, 18 строк «упоминание →
категория»** («до»-число LoRA-линии); dev-200 → 136/200 — те же 200 ответов на
трёх подах (детерминизм); 14 золотых → 11/14 (пятый взгляд, не бар). Фильтр
прохода-2: 281 строка в 79 тредах.
**Факт данных:** «наших» строк 49 из 650 (вся популяция ~7.5%); в холдауте-100
их 8, золото 14 → любая обучающая линия учится на ≤41 и меряется на 22 строках.

**`pass1-window` r1 (`docs/reports/pass1-window.md`, Dv623–658) — ПРИНЯТ как
КРАСНЫЙ:** 845 с = $0.174, **131 из 1 032**; рунг 4 убил под правильно — скорость
пода 4.498 с/звонок против 2.726 × 1.25, на которых ТИМЛИД построил цену при
уже измеренном разбросе 2.25× (нарушено мной). Аддендум: ценз 650/450 по паре
112/68/21 и 64/34. Талли r1: contract-health 29 · paid 7 — авторский спайк.
**`pass1-window r2` (`docs/reports/pass1-window-r2.md`, Dv659–679) — ПРИНЯТ
ЗЕЛЁНЫМ: ОКНО-1 КУПЛЕНО ЦЕЛИКОМ, 1 032/1 032.** Один 4090, 2 765 с = **$0.568**;
901/901, 0 отказов; скорость 2.694 с/звонок при заряженных 6.14 (разброс v2 —
2.69…4.50, заряд стоит); хвост с тома 134 строки, 3 куплены дважды ($0.0017).
Окно: $0.742 из $2.17. Моя приёмка: suite свой; r1-артефакты байт-в-байт; пак
r2 = комплемент; git-часы впритык второй раз (47 с); ценз объединения, 14
золотых, фильтр — МОИМ парсером; опровергатель 12/12. **Талли r2:
contract-health 11 (c-gap 4 · s-gap 1 · v-gap 6) · paid 10.**

## День 22.08 — pass2-signals: смок ИЗМЕРЕН, рунг S′ STOP, F1 взят 3/3

**`pass2-signals` r1 (`docs/reports/pass2-signals.md`, Dv681–703) — ПРИНЯТ как
STOP:** 526 с = $0.108, смок 5 F-тредов; декод прохода-2 **45.6 с/тред (макс
58.1)**; рунг S′ STOP по моей формуле (1.5 × среднее по пяти самым богатым
тредам) — STOP о выборке. F1 — все 3 сигнала (v5b: 0/3); F2 отказан парсером на
`"note": ""` (отчётное поле с правом отказа — сработало). Ревью: 13 дефектов,
два фатальных в КОМАНДАХ. Аддендум (принят в r2): enum 23/23 → contract-health
16 · paid 7; запас пака 144; порог 43.087.
**`pass2-signals r2` (`docs/reports/pass2-signals-r2.md`, Dv704–727) — ПРИНЯТ:
СЛОЙ СИГНАЛОВ ОКНА-1 ИЗМЕРЕН ЦЕЛИКОМ, 79/79, все три бара посчитаны — все три
КРАСНЫЕ.** Один 4090, 2 061 с = **$0.424** из $2.50 (заряд 97 с/тред, измерено
23.8; тяжёлый тред 26 строк — 135 с, 2.3× смока); 0 отказов, 0 перемаркировок;
F2 перекуплен байт-в-байт (разброс подов на декоде 1.008); r1-парсер отказал
бы 52/79 на пустом `note` — Dv702 окупился. **Бар 1: 4/5** (6/7 сигналов; F2
недостижим: 580124 «сеть» по проходу-1); **бар 2: 3/4** (E1 не вызван —
артефакт конструкции); **бар 3: КРАСНЫЙ, 2 сигнала из розыгрыша VARUS 10366**
(проход-1 пометил 4 коммента «сеть»; мои метки всех 12 строк треда — `null`).
DROP 21/281 (сеть 11.9% · категория 6.3% · бренд 6.7%); `subject_doubt` 15/260,
на бренде **21%** (кафе, канцелярия, спрей → «молочный бренд»). **Все три
красных — ошибки атрибуции прохода-1; проход-2 под строгой властью отработал.**
Слой: **$1.27/окно** против $0.31 у v5b — вдвое больше флагманов на 3.4×
тредах. Моя приёмка: suite **3 608/2 свой**; r1-артефакты байт-в-байт; пак r2 =
рендеринги r1; 4 перенесённых ответа байт-в-байт; 16 пинов = живые; git-часы
12:02Z < 12:07Z; все 79 ответов, бары, DROP, doubt — МОИМ парсером; деньги/
облако свои; enum 24/24; свип чист; опровергатель 10/10. **Талли r2:
contract-health 12 (c-gap 6 · s-gap 1 · v-gap 5) · paid 12 (tooling 7 · process 3
· env 1 · model 1)**; ревью по замороженному дереву: 10/11 вместо 3/12.

## Очередь контрактов

1. ✅ ПРИНЯТЫ 18–19.08: `guard --until` · `pass1-data-prep` · `vault-dream` ·
   `labels-boot-audit` · `boot-debloat` · `pass1-redraw` · `lora-b` D0–D2 ·
   `reader-topup` — см. сжатые дни выше и отчёты в `docs/reports/`.
2. ✅ **`lora-b-run` + аддендум ПРИНЯТЫ 20.08** — КРАСНЫЙ 9/14, линия B
   ЗАКРЫТА; талли contract-health 7 (c-gap 3 · s-gap 1 · v-gap 3) · paid 5
   (process 4 · env 1) — см. день 20.08.
3. ✅ **`pass1-fewshot` r1+r2 ПРИНЯТЫ 21.08** — dev-гейт КРАСНЫЙ +7/+10, линия
   «клауза + сосед» ЗАКРЫТА (талли 15/5 · 11/5). Меню 20.08: 0 холдаут ✅ → 2
   клауза ✅ КРАСНЫЙ → 1 проход-1 окна + проход-2 ✅ ИЗМЕРЕНО → 4 LoRA
   «рассуждение→метка» + 3 синтетика армом B — ТЕКУЩЕЕ.
   ✅ **`pass1-window` r1+r2 ПРИНЯТЫ 21.08 — окно-1 куплено за $0.742.**
   ✅ **`pass2-signals` r1+r2 ПРИНЯТЫ 22.08 — слой измерен, три бара красные по
   проходу-1.** **СЛЕДУЮЩИЙ — `lora-c-prep` ($0): промпт НАПИСАН
   `docs/PROMPT-lora-c-prep.md` (свежая сессия, Dv с 728)** — рационали,
   синтетика, SFT/eval-паки v3, исключения, черновик регистрации; ревью-гейт
   тимлида внутри. За ним — `lora-c-run` (A6000, кап $4.00, рулинг (к)).
4. **prep-b** — коллекция Сільпо/Varus/Маркетопт ($0, контракт согласован) —
   после читательского тракта (слово оператора 16.08).
5. `money-anchors` ($0) — по слову оператора (см. Открытые решения, п. 2).

Рамка честности разметки — ADR `sitting-b-line-b-and-the-team-lead-labels`.

## Деньги (чтение гарда тимлидом на приёмке 22.08, вечер)

Фаза 4 ЗАКРЫТА $32.4708 из $33.00 (финальное чтение 16.08, не растёт).
**Цикл-2: $7.89 из $20.00, остаток $12.11** (моё чтение гарда 22.08 вечером на
приёмке pass2-signals r2; внутри — lora-b $2.3624, reader-topup $1.2551,
pass1-fewshot $0.397, pass1-window $0.742, pass2-signals r1+r2 $0.532 по часам
подов; капель тома ~$0.24/сутки; четыре шага ОТКРЫТЫ до `money-anchors`).
Сигнальный слой окна-1: **$1.274** против $0.313 у v5b. Влезает: LoRA-линия
≈$4–5 (обучение + сквозная оценка) · том ≈$2/нед; окно-2 ≈$4.8 — после LoRA.
Деньги читаются ИЗ ГАРДА (`scripts/runpod_guard.py`), не из этого файла и не
из отчётов.

## Открытые решения (повестка брифинга)

0. **РУЛИНГ ОПЕРАТОРА 20.08 (вечер, в тимлид-сессии) — ЗАРЕГИСТРИРОВАН ЭТОЙ
   ЗАПИСЬЮ:** база НЕ меняется — остаёмся на Gemma-4-31B; отдельное сидение C
   не нужно — слово оператора здесь = рулинг сидения; цель слоя — «такой же
   анализ комментариев, как ручной разбор тимлида» (`docs/REFERENCE-signals-w1.md`:
   F1–F5 · E1–E4 · N); разметку и дальше делает тимлид. Тимлид предлагает
   варианты в рамках этих ограничений (меню — в чате 20.08, выбранное — сюда).
1. **РУЛИНГ ОПЕРАТОРА 21.08 (в тимлид-сессии, после брифинга) — ЗАРЕГИСТРИРОВАН
   ЭТОЙ ЗАПИСЬЮ.** (а) **v2 (`pass1_comment_gm4_v2`) — базовый промпт прохода-1**:
   следующая линия строится поверх него и меряется против него («до»-числа
   136/200 · 38/49 на dev-200). Регистрация r2 закрыла итерацию промпта, не
   принятие измеренного; по 14 золотым v2 НЕ стрелял — и не стреляет без новой
   регистрации. (б) **Следующий контракт — (1) сквозной прогон окна-1 на v2:**
   проход-1 по ~1 032 платным комментам + проход-2 (сборка сигналов) против
   `docs/REFERENCE-signals-w1.md` с пре-регистрированным баром по F1–F5 /
   E1–E4 / N; ~$1–2, без обучения. Это рулинг ПОВЕРХ следствия регистрации r2
   (v2 шипится как ПРОВИЗОРНЫЙ фильтр для сквозного замера, не как принятый
   деливерабл). Цель замера — какая ошибка прохода-1 (мусор 45% / пропуски
   20%) ломает проход-2. (в) **Затем — (4) LoRA «рассуждение→метка» поверх v2 с
   (3) синтетикой как армом B** одной регистрации; цель обучения — из (б).
   Отклонены: (4) первым (инструмент оценки 22 строки); стоп и упаковка.
   (г) **Дизайн прохода-2 = (A):** один LLM-вызов на ТРЕД по комментам, которые
   проход-1 пометил «наши»/`сеть_ритейлер`, с entity-контекстом → сигналы
   `{тип · субъект · аспект · цитаты msg_id}`; **рулинг по ADR 17.08: такой
   вызов НЕ «однопроходный читатель»** (не делает per-comment атрибуцию, не
   отвечает на всё по сырому треду) — записать в ADR, связывает обе ступени.
   (B) детерминированная сборка отклонена. (д) **Два контракта, один платный
   прогон на каждый:** `pass1-window` (кап $1.50) → `pass2-signals` (кап $1.50).
   14 золотых в окне — строка ценза с множественностью, НИКОГДА не бар. ADR
   записан (`v2-is-the-base-and-pass-2-is-not-a-one-shot-reader`).
   (е) **РУЛИНГ 21.08 (вечер), после красного pass1-window:** дозаказ 901
   коммента регистрацией `pass1-window r2` с капом **$2.00** — ИСПОЛНЕН,
   ЗЕЛЁНЫЙ, $0.568. (ж) **РУЛИНГ 22.08 (утро) — дизайн `pass2-signals`:** схема
   и скорер v5-читателя, голд r2 как есть; проход-2 СТРОГО не перемаркирует
   (может выбросить; `subject_doubt` отчётный); вход = фильтр + пост +
   entity-блок; бар референса как есть + таблицы; смок первым; кап $1.50 —
   ИСПОЛНЕН, STOP на смоке. (з) **РУЛИНГ 22.08 (день):** `pass2-signals r2`, 75
   тредов, кап **$2.50** — ИСПОЛНЕН, $0.424, бары посчитаны. (и) **РУЛИНГ 22.08
   (вечер):** фаза 6 ОСТАЁТСЯ открытой, следующий контракт — LoRA-линия сразу;
   **дистилляция скилла и ретро фазы ОТЛОЖЕНЫ словом оператора** до результата
   LoRA (триггер 705 ≥ 700 стоит, долг назван); адъюдикация референса (E1 для
   прохода-2; S1 в N1 — машина нашла референсный вторичный сигнал 21420)
   ОТЛОЖЕНА до результата LoRA — вердикт r2 не трогается. (к) **РУЛИНГ 22.08
   (вечер) — дизайн `lora-c`:** промпт v3 = v2 + поле `rationale` ПЕРЕД меткой,
   одним рендерингом для обучения и инференса; таблица парно в четыре колонки
   (база v2 · база v3 · адаптер A · адаптер B) на одном пуле соседей; рационали
   для обучающих строк пишет Claude Code по кодбуку и метке тимлида, тимлид
   ревьюит все «наши» + ~40 пограничных; синтетика ~160 (по 40 на класс ошибки)
   — арм B, тег в provenance, абляция армом A, никогда в холдаут/голд; из
   обучения и из пула соседей исключены холдаут-100 и ВСЕ строки 16 референсных
   тредов; **бар на арм (один выстрел): холдаут-100 ≥ 64 И сквозной бар 1 =
   5/5 И бар 3 = 0 — все три или КРАСНЫЙ**; отчётно: ячейка «упоминание →
   категория» на холдауте (18 → ?), dev-200 (fit), 14 золотых (шестой взгляд).
   Два контракта: `lora-c-prep` ($0, с ревью-гейтом тимлида) → `lora-c-run`
   (один A6000, кап **$4.00**). **Скилл team-lead:** дельты 20.08 применены;
   эталон `team-lead-SKILL-2026-08-20.md`; **триггер 705 ≥ 700 стоит, дистилляция
   ОТЛОЖЕНА (и)** — шесть дельт-кандидатов в handoff проекта.
2. **Денежная гигиена (контракт `money-anchors`, $0):** закрытие шагов по
   settled-биллингу (walk отстаёт на 30–40 мин — Dv504/658/678), зависший walk
   pass1-probe, `anchored_at` для 19 отказанных леджеров. Словом оператора.
3. **Дизайн командного центра** — отвергнут 15.08; направления на выбор —
   после установки Design-плагина.
4. Мелочи step-0.5 (ждут контракта, трогающего их файлы): `t5_depth`-бейдж ·
   валюта ₴ в шапке промо · fix-b(2) «две колонки — одно число».

## Живые рулинги читательского тракта (сидения 15–17.08, сжато)

- Эталон цели: `docs/REFERENCE-signals-w1.md` (ручной разбор тимлида 15.08);
  голд — ревизия r2 («категория» ≡ «категория_личное»); референс не правится.
- Популяция тракта читателя окна-1: **129 тредов / 1 032 платных коммента**
  (varto-глушитель снят для ТРАКТА ЧИТАТЕЛЯ; классификация не тронута).
- Транспорт (v3→v5b, живёт в коде и суите): один JSON-объект, `[]` для
  пустого; эхо msg_id; потолок вывода 4 000 ТОКЕНОВ; kill-clock по рунгам
  (ssh 500 с · launch→ответ 450 с · проекция · живость 600 с · хард-стоп
  платформы), одно пересоздание, третий под = STOP; gate-записи append-only;
  поды, не serverless. **ID коммента по окну — только пара (тред, msg_id)**.
- F2a-carve-out (рулинг 17.08): событие о доступности предмета поста — сигнал о
  предмете поста; мнения без имени не переносятся.
- **Однопроходная промпт-линия ЗАКРЫТА stop-rule'ом** (v5b: бар-1 2/5); ADR
  связывает тимлида; проход-2 по атрибутированным комментам — НЕ он (ADR 21.08).

## Отложено СОЗНАТЕЛЬНО (что · критерий развязки)

- **Serving-оптимизация (латенси)** — по слову оператора «готовим прод» (вход
  Фазы 7); пути известны (пре-квант база; merge+vLLM новой прережкой).
- **История 11 143 строк** и **батч-паритет** — ждут батч-решения И денег
  ($4.35 при −30%; `results/batch_cycle2_projection.json`).
- **5c3: категорийный слой** (таксономия 13+2, механика не строена), сайдкар
  v2. **G1a/G1c** — новая фаза качества или потолок принят, на свежих данных.
- **Алерты (вопрос 6)** — пороги словом оператора + окно-2 для базовой линии.
  **Тренды** — окно-2. **Продажи ТПК** — по слову собственника (SQLite готов).
- **Мёрдж адаптера в bf16** — после замера в прод-конфигурации; **LLM-нарратив
  отчёта (5d)** — модель выбирается на живых агрегатах.

## Правила, которые легко забыть

- Все eval гейтов — батчом 1; v2/v4-голды и запечатанные вердикты неизменны.
- Числа — только из result-файлов через скорер; в презентационные артефакты
  руками не вписываются (закон 3.20, отравленный тест).
- **Деньги читаются из гарда, не из леджера и не из прозы** (Dv33); часы пода
  — число для цитаты, walk отстаёт (Dv504).
- **Прережка замерзает при первом `pod create` попытки**; правка после —
  только новой регистрацией; кап живёт в регистрации, его поднятие — слово
  оператора ДО эндпоинта.
- **Пины — потребители:** `prompts.py` (38 записей), `scorer.py`, `brands.py`,
  `local_llm.py`, `window_summary_5c2.py`, PLAN-файлы; правка = MOVED-манёвр или
  новый модуль, не перепин. Префлайт контракта: `make preflight` на всё, что он
  трогает. `registry.yaml`/`lexicon.yaml` запечатаны — правки новыми ревизиями.
- Никогда `git add -A`; файлы тимлида исполнитель коммитит, не правит;
  отчёты — файлами в `docs/reports/`, в чат — только путь. Впрыснутые треды
  проб (`injected: true`) не входят в продакшн-агрегаты и цену окна.
- Пре-регистрации не переоткрываются; провал закрывает вопрос (one attempt);
  stop-rule закрывает ЛИНИЮ. **Платный под не оставляют без опроса** — рунг
  живости в каждой регистрации (9 720 с холостых = $1.43, 20.08).
- **Ревью пятью линзами ДО первого `pod create`, по ЗАМОРОЖЕННОМУ дереву** —
  строка шаблона; тесты гоняют КОМАНДЫ (точки входа), не только функции (два
  фатальных дефекта pass2 жили в `main`); отчётное поле не может отказать.

## Урок недели (ретро 18.08)

Узкое место — тимлидский ярус (P1–P7); пилоты H1/H6, H5b, H4 СРАБОТАЛИ. H5a/H7 и
split-талли фазы — на закрытии фазы 6.
