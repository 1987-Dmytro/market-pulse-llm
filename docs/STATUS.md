# STATUS — market-pulse-llm (04.09.2026) — карта этапа 1 «промо-пульс сетей»: что за чем, где мы, когда конец

> Операторский документ, по-русски; правит только тимлид, после приёмок. Хроника — git этого файла, `docs/reviews/`, ADR.
> Потолок — 60 строк карты (скилл v3 §8); машинный блок в конце не считается и не редактируется.

## Что это за проект (одним абзацем; переспека 27.08, рулинг (х))
Инструмент маркетинговых исследований ТМ «Гармонія» по украинскому Telegram (`docs/PRODUCT.md`). Замер 27.08: бренды
watchlist названы ≈ в 40 комментах за 18 мес., Гармонія — в 0; зато 2 102 промо-поста сетей с ценами и 1 092 коммента под ними.
Поэтому **этап 1 = «промо-пульс сетей»: что и почём сети промоутируют по молочке и мороженому, и как покупатели реагируют
под промо-постами — неделя за неделей**, непрерывной петлёй с расписанием из UI (`docs/SPEC-v2-promo-pulse.md`). GPU арендные.

## План этапа 1 — что за чем идёт (фаза `PHASE-promo-pulse-1`, все числа — из result-файлов)
| # | слой | что это для тебя | состояние | держит |
|---|---|---|---|---|
| S0 | источники | 17 каналов сетей собраны, все страницы листовок куплены (3 008 стр., $2.99) | ✅ C2 закрыт; Маркетопт (@marketopt_promo, Кременчук) внутри — 22 позиции. **Собственный промо-канал Маркетопта (приватный, разрешён 30.08) — НЕ собран: следующий пункт исполнителя, рулинг (i)** | исполнитель (1 сессия, ≤$0.50) |
| S1 | цены | таблица промо-позиций: бренд · товар · объём · промо-цена · −N% | ✅ **1 113 позиций, 12 сетей** в `pulse.db :: w2`; Гармонія — 0 позиций в листовках окна. **Бар не измерен**: нужен эталон тимлида по 46 страницам | **тимлид** (2 сессии) |
| S2 | реакции | под каждым промо: о чём коммент (сеть / бренд / товар) и что говорит (жалоба · похвала · спрос · привычка · цена) | итерация 1 (03.09, $0.44): **сигналы 0.879 ✅ (бар 0.75), субъект 0.793 (бар 0.80, −1 коммент)**. Итерация 2: подготовка A(1) сделана, остаток A → B (платно ≤$1.97) | исполнитель (2 сессии) |
| S3 | тренды | цена SKU по неделям и сетям, глубина скидок по сети и бренду — SQL | ✅ 14 тестов зелёные, пересчёт дважды идентичен | — |
| S4 | петля | `make tick` по расписанию, экран из result-файлов | ✅ идемпотентна (второй прогон — 0 новых строк); экран рендерится | — |
| гейт | ты | смотришь 20 строк под сидом 42 (`draw_truth_20.py`) и говоришь, что верно/неверно | после S1-бара и холдаута S2 | **оператор** (30 мин) |
| дальше | C6 | фронт: петля + расписание в UI; рост источников (приватный Маркетопт, оптовики) — параллельным треком, как только есть список | после гейта / параллельно | слово оператора |

**Где мы сейчас, одной строкой:** измеряем, правильно ли система читает комменты (S2, итерация 2 из ≤5), и тимлид должен два эталона
(46 страниц для S1, holdout-40 для S2). Всё остальное этапа 1 сделано или ждёт этих трёх вещей.

## Ближайшие шаги — по порядку, кто что делает
1. Исполнитель, сессия «A-rest» ($0): канонический id сети, примеры из кодбука в шаблон, тесты, регистрация под v1.1 → NEXT = Маркетопт.
1a. Исполнитель, сессия «Маркетопт» (≤$0.50): вступить в приватный канал, собрать, ценз, страницы, третье окно, строки на экране → NEXT = B.
2. Исполнитель, сессия B (платно ≤$1.97): итерация 2 → «iteration 2 read» → тимлид читает таблицу ошибок; при субъекте ≥ 0.80 —
   нотис прережки холдаута; при плато (две без роста) — кодбук v1.2 тимлидом.
3. Тимлид: эталон позиций по 46 страницам (2 сессии) → грейдер K6 → бары S1; holdout-40 после нотиса → один выстрел.
4. Оператор: вставляет стандартный промт в каждую сессию исполнителя; даёт список оптовиков (или «найди») для параллельного трека; гейт.
**Оценка: гейт этапа 1 — 08–09.09**, если итерация 2 берёт 0.80; иначе +1 день на кодбук v1.2 и итерацию 3.

## Деньги (из гарда и pod-priced записей)
Цикл-3: потрачено $4.06 из $7.00, остаток $2.94; шаг dev-петли $0.53 из $2.50 (осталось $1.97); холдаут $0.30 неприкосновенен.
Шаг S4 закрыт settlement $2.9867/$3.95. История: фаза 4 $32.47/$33; цикл-2 $17.68/$20; lora-c $1.12; D2 $5.84. Тома: `mp-srv2` (EU-RO-1) остаётся
до дизайна C6; `mp-lora-c` удаляется в `phase7-a1`.

## Что доказано и остаётся в продукте
Адаптер 4.5h2 (`results/verdict_45h2.json`): тональность macro-F1 0.9214, сарказм 23/38, аспекты 0.8478, тип поста 0.9586, бренды 0.9610.
Позиции 5c2 (`run_5c2_positions.json`): 145 из 159 стр.; vision 7.872 с/стр. (n=30). Сигналы в брендовой постановке закрыты (х): проход-1 68%,
thinking +0 (`think_zero_shot_table.json`; D1 (инструмент, $0) принят 26.08). Закрытые линии: однопроходный читатель, lora-b, lora-c,
think-zero-shot, харнес v1. Витрина A1–A6 — параллельно, $0, не гейт.

## Живые решения (последние; полные тексты — `docs/reviews/2026-08-30-plan-promo-pulse-1.md`, история — git этого файла)
- (i) 04.09 — приватный промо-канал Маркетопта собирается СЕЙЧАС (пункт после A-rest, до B; кап $0.50; третье окно); `@ON_LINE_MO` — через
  боковой файл реестра; оптовики — по списку оператора или «найди».
- (h) 04.09 — A(2)–A(5) + переиздание регистрации = один пункт одной сессии; примеры в шаблон — дословно из кодбука v1.1; затем B.
- (g) 04.09 — id админов в `config/channel_admins.yaml` (реестр пиннят 21 печать — не растить); бестекстовые комменты вне рендера.
- (f) 03.09 — итерация 1 прочитана: промахи — атрибуция, не смысл; кодбук v1.1 (3 строки эталона → post); итерация 2 = метка + id сети + примеры.
- (e) 03.09 — процесс v3: без `/goal`, стандартный промт, `PROGRESS.md`, рулинг ≤12 строк; dead-man 500 с; рунги 1–2 сняты для шагов ≤$3.
- (я)(a)(b) 03.09 — раскол derived-стора (w1 побайтно, C2 в `data/derived_w2/`), `carrier` в ключе, план S9 принят, §8 v4.
- (х)(ц)(ч)(ш) 27–30.08 — переспека «промо-пульс», ценз 17 каналов, v1 архив/r2 живой, дро 20/20, бары S1/S2 — в файле рулингов.

## Отложено сознательно (что · развязка)
Проектная цель одной `/goal` поверх `make done` — после гейта C5. Окно-2, мёрдж bf16, 5c3, G1a/G1c — по слову оператора.
`think-adapter` — при +0 ценность упала. Долги исполнителя — в `docs/plans/promo-pulse-1.PROGRESS.md` («needs named»).

## Уроки (03.09, ретро `docs/reviews/2026-09-03-retro-process-v3.md`)
Восемь пауз фазы — восемь правильных отказов, $0 потеряно; корни у тимлида: закон рос и не убывал (проверка : продукт = 15 : 1), деньги
рулились по интуиции, план принимался без инструмента, сессия резалась то слишком крупно, то слишком мелко. Лекарство — v3: один файл
фазы, один файл прогресса, стандартный промт, рулинг ≤12 строк, диета закона. Итерации/день: 03.09 — 4; 04.09 — 2 (сессии 10–11).

<!-- MACHINE-READ BLOCK — quoted verbatim by sealed producers (`scripts/write_lora_c_prereg.py :: quoted()`).
     The diet of 26.08 deleted these and reddened 5 tests. Bytes below are law: never reword, never re-flow.
     This block is NOT map prose and does not count toward the 120-line diet. -->
## Архив рулингов, которые читают запечатанные записи (не редактировать)

- (в) **Затем — (4) LoRA «рассуждение→метка» поверх v2 с (3) синтетикой как армом B** одной регистрации; цель обучения — из (б).
- (к) **РУЛИНГ 22.08 (вечер) — дизайн `lora-c`:** промпт v3 = v2 + поле `rationale` ПЕРЕД меткой, одним рендерингом для обучения и инференса; таблица парно в четыре колонки (база v2 · база v3 · адаптер A · адаптер B) на одном пуле соседей; рационали для обучающих строк пишет Claude Code по кодбуку и метке тимлида, тимлид ревьюит все «наши» + ~40 пограничных; синтетика ~160 (по 40 на класс ошибки) — арм B, тег в provenance, абляция армом A, никогда в холдаут/голд; из обучения и из пула соседей исключены холдаут-100 и ВСЕ строки 16 референсных тредов; **бар на арм (один выстрел): холдаут-100 ≥ 64 И сквозной бар 1 = 5/5 И бар 3 = 0 — все три или КРАСНЫЙ**; отчётно: ячейка «упоминание → категория» на холдауте (18 → ?), dev-200 (fit), 14 золотых (шестой взгляд). Два контракта: `lora-c-prep` ($0, с ревью-гейтом тимлида) → `lora-c-run` (один A6000, кап **$4.00**).
- (н) синтетический запрос рендерится ТЕМ ЖЕ правилом на ТОМ ЖЕ пуле 515 (закон уже запрещает синтетику только как СОСЕДА; запрет двойника действует)
- (н) проход-2 в ране — ТОЛЬКО для армов (2 ноги, не 4): база v2 сквозной уже измерена (r2: 4/5 · 2), база v3 бара не берёт — экономия ~$0.47 по заряду.
- (о) кап lora-c-run **остаётся $4.00** (оба существующих замера s/step влезают по фактической цене; смок решает, рунг проекции убивает честно)
- (о) карта — **RTX PRO 4500, 32 GB, $0.72/ч, EU-RO-1** (A6000 в датацентре тома none; 32 GB — запас против OOM при замороженном micro_batch; фолбэк 4090 преавторизован)
- (о) ложная строка топика в синтетических заголовках заменяется выделенной константой (продовая строка NO_POST_TEXT в синтетике запрещена); конфаунд маркера принят и именован; в ран добавлен отчётный маркер-ценз (~$0.05) — отделяет «не помогло» от «выучил маркер»
- (ф) **(ф) 26.08, вечер — ПРИОРИТЕТ: СЛОЙ СИГНАЛОВ.** Система бесполезна, пока не находит сигналы в комментах и тредах на уровне ручного разбора тимлида; **только локальная Gemma-4-31B, thinking ОБЯЗАТЕЛЬНО**; API-модели отклонены. Находка: `enable_thinking: false` стояло во ВСЕХ запусках проекта — читатель с thinking ON ни разу не измерен; регистрации сами пишут «thinking-ON reader = НОВАЯ регистрация». Решение о дальнейшем пути — на этой таблице. [цитируется `scripts/write_think_zero_shot_prereg.py :: quoted()`; рулинг закрыт (х) 27.08, байты — закон]
