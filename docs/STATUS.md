# STATUS — market-pulse-llm (2026-08-22, вечер; компактная карта)

> Операторский документ, по-русски по решению оператора. Правит только тимлид,
> после приёмок. Полная хроника фаз 0–5 и дней 15–17.08 — в git-истории этого
> файла и в docs/reports/; здесь — карта и живые решения.

## Что это за проект (одним абзацем)

Ключевой инструмент маркетинговых исследований компании (ТМ «Гармонія»):
мониторим Telegram-каналы украинских ритейл-сетей (Сільпо, АТБ, Varus,
Маркетопт) и агрегаторов, находим анонсы новинок и промо по молочке/мороженому,
извлекаем бренды и позиции с ценами, классифицируем реакции покупателей
(тональность, сарказм, скрытые интенции), и превращаем это в командный центр
директора по маркетингу. Ядро — открытая LLM (Gemma-4-31B), дообученная QLoRA;
GPU только арендные, между сессиями консоль пуста. Северная звезда — семь
вопросов директора в `docs/PRODUCT.md`.

## Карта фаз — ГДЕ МЫ

| Фаза | Что | Статус |
|---|---|---|
| 0–4.5 | Спека (rev 3.24) · скелет · данные · бейзлайны · QLoRA · программа качества | ✅ (хроника в git) |
| 5 | Продакшн-петля 5a…5c2: окно-1 куплено целиком, сидение ратифицировало обе половины | ✅ 14.08 |
| **6** | **Командный центр + слой сигналов из комментариев** | **🔄 ТЫ ЗДЕСЬ — v1 центра построен; однопроходный промпт-читатель ЗАКРЫТ stop-rule'ом 17.08; каркас — гибрид D (сидение 17.08); проход-1 ИЗМЕРЕН 18.08: база 9/14 при пороге 12 → сидение выбрало линию B (разметка subject_type + LoRA); ЛИНИЯ B ИЗМЕРЕНА 20.08: арм A 9/14 → КРАСНЫЙ, линия B ЗАКРЫТА; рулинг 20.08: база остаётся Gemma-4; ПРОМПТ v2 ИЗМЕРЕН 21.08: dev-гейт КРАСНЫЙ +7 из +10 → рулинг: v2 = базовый промпт, сквозной замер окна; ПРОХОД-1 ПО ОКНУ-1 КУПЛЕН ЦЕЛИКОМ 21.08 ($0.742); слой сигналов ИЗМЕРЕН 22.08 — три бара красные по проходу-1; `lora-c-prep`/`apply`/`close` ПРИНЯТЫ 23.08 (потолок 3 072 по (м)); `lora-c-run` STOP и `lora-c-armb` ПРИНЯТЫ 24.08; **run r2 ПРИНЯТ 25.08: KILL по рунгу — 32 GB не тянет обучение на 3 072, базы куплены ($0.76: v2 61/98 · v3 66/98), попытка цела**, проба 25.08: PRO 4500 ЗАКРЫТА ($0.075); миграция в EU-SE-1 ОПРОВЕРГНУТА платформой ((р)→(с)); **migrate r2 ПРИНЯТ 26.08: том mp-lora-c в CA-MTL-3 построен, A100 грузит модель за 40 с ($0.287)** → СЛЕДУЮЩИЙ: `lora-c-run r3` — смок A100 покупает s/step, кап словом, армы, бары** |
| 7 | Валидация на ≥2 окнах + упаковка | — |

## Модель сейчас (деливерабл)

Классификация: Gemma-4-31B NF4 + несмёрженный QLoRA-адаптер 4.5h2, батч 1,
гейты v4: 3/5 (G1a 0.9214 · G1b 23/38 · G1c 0.8478 FAIL на 0.0005 · G1d 0.9586
· G1e 0.9610). Сигналы — «гибрид D»: v5-читатель треда, проход-1 per-comment
(v2 — ПРОВИЗОРНЫЙ базовый промпт, рулинг (а); линия B и «клауза+сосед» закрыты
красным; **ничего не шипится**), проход-2 — сборка под строгой властью, измерен
целиком 22.08. Позиции листовок: инструмент v2, промо+бейдж — зелёные ноги;
зачёркнутая цена не печатается нигде (законы 3.17/3.18/3.22).

## Дни 18–19.08 — сжато (полные записи: git-история этого файла, отчёты, ADR)

**18.08.** Недельное ретро (`docs/reviews/2026-08-18-weekly-retro.md`): enum v2
ЗАКРЫТ, чеклист приёмки 6 пунктов. `pass1-probe-b` ПРИНЯТ — база **9/14 при
пороге 12**; `guard-until` · `pass1-data-prep` ПРИНЯТЫ. **19.08.** `vault-dream` ·
`labels-boot-audit` · `boot-debloat` · `pass1-redraw` · `lora-b` D0–D2 ·
`reader-topup` — ПРИНЯТЫ. Разметка 650 строк ТИМЛИДОМ: **«наших» 49** — вся
популяция. Долги: строки индекса; дом Dv521; Dv541; Dv536.

## День 20.08 — lora-b-run ИЗМЕРЕН: КРАСНЫЙ 9/14, линия B ЗАКРЫТА

**`lora-b-run` + аддендум ПРИНЯТЫ** (`docs/reports/lora-b-run.md`, ADR
`lora-b-red-and-line-b-closes`). A6000, $2.3624 из $6.00; **арм A 9/14 — те же
пять промахов, что у базы; арм B НЕ ОБУЧЕН** (простой 9 720 с ≈ $1.43 → рунг
ЖИВОСТИ в каждую регистрацию). Рулинг: «Красный = ответ, линия B закрыта»; арм
без ответов = not_evaluated. Механизм — адаптер выучил маргинал набора (52%
не_наш_рынок), супервизия одного поля на перекосе учит prior. Талли 7/5.

## День 21.08 — промпт v2 ИЗМЕРЕН (pass1-fewshot r1→r2): dev-гейт КРАСНЫЙ +7 из +10

**r1** (Dv587–606): D0 ПРИНЯТ; D1 не состоялся (два 4090 убиты рунгом 2,
$0.1178) → рулинг: r2, бюджет шага $1.50. **r2** (`docs/reports/pass1-fewshot-r2.md`,
Dv607–622) **ПРИНЯТ**: один 4090 = **$0.279**; 2.29/2.73 с/звонок; приёмка —
suite свой, r1 байт-в-байт, dev-гейт МОИМ парсером, опровергатель 8/8. Талли 11/5.

**Что измерено (пересчитано тимлидом, dev-200; «до»-числа текущей LoRA-линии):**

| | база v1 | v2 |
|---|---:|---:|
| согласие на 200 | 87 (43.5%) | **136 (68%)** |
| «наши» 49 строк верно | 31 | **38** (+7; бар +10 → КРАСНЫЙ) |
| молчание (`не_наш_рынок → None`, из 85) | **48** | 12 |
| упоминание принято за суть (`не_наш_рынок → категория`) | 22 | **24** |
| `категория → не_наш_рынок` (новая ошибка) | ~0 | 6 |
| фильтр «наших»: полнота / точность | 65% / 44% | **80% / 55%** |

Чтение: база молчала — v2 научил отвечать; граница «упоминает vs о чём» стала
главной ошибкой v2. **Ценз окна (отчётно, НЕ бары):** 650 размеченных → 395
(60.8%), «наших» 38/49; **холдаут-100 → 64/100, «наших» 6/8, 18 строк
«упоминание → категория»**; 14 золотых → 11/14 (пятый взгляд); фильтр
прохода-2: 281 строка в 79 тредах. **Факт данных:** «наших» 49 из 650 (~7.5%);
любая обучающая линия учится на ≤41 и меряется на 22 строках.
**`pass1-window` r1 КРАСНЫЙ ($0.174, 131/1 032; цена на среднем при измеренном
разбросе — нарушено мной; талли 29/7) → r2 ЗЕЛЁНЫЙ: ОКНО-1 КУПЛЕНО ЦЕЛИКОМ,
1 032/1 032, $0.568; окно $0.742 из $2.17** (талли 11/10; отчёты r1/r2).

## День 22.08 — pass2-signals: слой сигналов окна-1 ИЗМЕРЕН, все три бара красные

**r1 STOP** ($0.108, смок; рунг S′ о выборке; 13 дефектов ревью, два фатальных
в КОМАНДАХ; талли 16/7) → **r2 ПРИНЯТ: 79/79, $0.424 из $2.50. Бар 1: 4/5 (F2
недостижим — 580124 «сеть» по проходу-1); бар 2: 3/4 (E1 — артефакт
конструкции); бар 3: КРАСНЫЙ (2 сигнала из розыгрыша VARUS 10366, мои метки
всех 12 строк — null).** DROP 21/281; `subject_doubt` на бренде 21% (кафе,
канцелярия, спрей). **Все три красных — ошибки атрибуции прохода-1; проход-2
под строгой властью отработал.** Слой: **$1.27/окно** против $0.31 у v5b.
Приёмка вся моим парсером; талли 12/12; ревью по замороженному дереву 10/11.

## День 23.08 — lora-c-prep ПРИНЯТ (STOP-отчёт) · рулинги (л)/(м) · lora-c-apply ПРИНЯТ

**`lora-c-prep` (Dv728–764, талли 24/13 — спайк = расщепление) — ПРИНЯТ, $0.**
Три стопа до денег (нет пятого соседа → у арма A ноль позитивов, потолок
холдаута 98; 506/506 мимо max_seq_len 1 408; запиненный тренер отказывает v3)
+ течь на бар (22/198 own-text, 5/98 — пересчитано мной). Гейты закрыты моими
вердиктами (4 именные строки, r3-дельта 49685, 2 паттерна; синтетика 0 дропов,
6 рулингов). **`lora-c-apply` (Dv765–780, 8/8) — ПРИНЯТ, $0:** вердикты
применены (48+51 правок, меток 0), own-text 0/198 и 0/506 моим сканом,
**реальный токенайзер ОТКАЗАЛ потолку 2 816 (макс 2 975; ratio-модель −228) →
рулинг (м) 3 072**; line B байт-в-байт. Второй скептик молчал ВТОРОЙ раз →
эскалация в числовой дифф-аудит. **`lora-c-close` (Dv781–785, 3/2) — ПРИНЯТ:**
посадка 3.26 в реестр стрипа + находка единственного пина сырого SPEC;
суита 3 669/2. Хроника — отчёты в docs/reports/, приёмки в git этого файла.

## День 24.08 — run STOP ПРИНЯТ; (н); armb ПРИНЯТ; (о) — кап $4.00, PRO 4500; run r2 ВЫДАН

**`lora-c-run` (Dv786–792, 4/3) — ПРИНЯТ как STOP на $0, попытка цела:** кап
$4.00 не покрывал четыре ноги (брейк-ивен 48–59 с/шаг против замеров 61/68 на
вдвое коротких строках; вывод — неравенством, без выдуманного s/step), и **у
арма B не было датасета** (666 — арифметика без продюсера; create = заморозка).
Сиблинг-тренер построен, энкод-ценз 506/506 реальным токенайзером. →
рулинг (н). **`lora-c-armb` (Dv793–802, 6/4) — ПРИНЯТ, $0:** арм B — файл
(506 байт-в-байт + 160), энкод 666/666 запас 97, изоляция чиста моим грепом;
деньги на 2 ноги прохода-2 (брейк-ивен 63.3/73.4 — замеры влезали). Находки:
**маркер в заголовках синтетики** (160/160 против 0/506, в них все 32 позитива
класса — конфаунд, отрулен в (о)) и check-stamped, забраковавший собственное
зелёное чтение. Суиты 3 682→3 701/2. Хроника — отчёты.

## День 25.08 — lora-c-run r2 ПРИНЯТ: KILL по рунгу 4, $0.76, попытка цела; (п)

**`lora-c-run r2` (Dv803–811, талли 6/3) — ПРИНЯТ как STOP по рунгу.** Один под
PRO 4500, 3 803 с = **$0.7606 из $4.00 по часам пода** (дельта баланса $0.7338 —
нижняя граница; строки биллинга опаздывают — долг). **Смок: OOM на 3 072 даже
при micro_batch 1** (тренер сам понизил по замороженному закону) — 32 GB мало
для СТРОК (паддинг по самой длинной реальной строке 2 975), не для потолка;
понижение потолка не освобождает памяти. **Куплены и остаются: обе базовые
ноги** — v2 61/98 · **v3 66/98** на холдауте (мой парсер: сходится построчно);
весь прирост v3 — не_наш_рынок (+6); mention-ячейка не движется (20→19/66);
**v3 = 9.20 с/звонок (1.50× заряда)**. До пода за $0 закрыты 7 транспортных
дыр (4 подняли бы на оплаченном поде); pass-2 потолок ограничен худшим случаем
(флагманский тред 12 399 > 12 000 → оба конца на 15 569). Рунг 3 (реализованная
ставка) стрелял дважды GO во время денег. Моя приёмка: согласия и поклассовые —
своим парсером из сырых out-файлов (совпали); деньги гардом и листингом сам;
дозамер 580124/10366 (см. (п)); регистрация ЗАМОРОЖЕНА создёй r2, попытка цела.
Суита исполнителя 3 771/2; моя — хвостом этой приёмки.
**`lora-c-vramprobe` (Dv812–819, талли 3/5) — ПРИНЯТ: OOM снова, карта ЗАКРЫТА,
$0.0752 из $0.30.** Настройка ДОКАЗАННО дошла до процесса (/proc/environ,
артефакт) и сработала — а дефицит вырос 0.06→0.64 GiB: структурный, «чуть
добавить» мертво. Смоки спарены до первого батча ([231, 4] на обоих подах).
Чекер отчёта: 61/61 числа пересчитываются (мой прогон). Замороженная
регистрация нетронута (sha тот же), попытка цела. §5: в EU-RO-1 нет 48 GB;
A6000 — EU-SE-1/US-TX-1 по $0.53. Долг: два `--close` гарда (r2 + проба);
формат-дрифт двух файлов при запиненном продюсере — make fmt запрещён.
## День 26.08 — migrate r2 ПРИНЯТ: том построен, модель грузится; готов r3

**`lora-c-migrate r2` (Dv829–836, талли 6/2) — ПРИНЯТ 26.08.** Том **mp-lora-c
100 GB в CA-MTL-3** построен; веса на пиненной ревизии; **A100 PCIe 80 GB
грузит модель за 40.22 с при 17.0 из 79.1 GiB** (карта занята на 21.5% — класс
памяти закрыт с запасом). **$0.287 из капа $1.50** (744 с пода). Два новых
замера: **venv 132 с** (первый в проекте) и закачка 62.58 GB за 184 с
(340 MB/s). KILL рунга 5 — перестраховка правила (du сдвинулся на 40 байт
бухгалтерии HF: refs/main + 4 нулевых маркера отсутствия; блобов записано 0);
правило честно НЕ правлено после чтения — ремеди r3: инвариант «ни один блоб
не растёт». Сток мерцает поминутно (три чтения A100 за 17 минут — три разных
строки; мой вопрос оператору в сессии: ответ «CA-MTL-3, как рулено» —
исполнено). Долг close пробы отказан ПОВТОРНО — структурный, уходит в
money-anchors. Моя приёмка: гард/листинги/чекер 79/79/талли — свои; оба тома
на месте; замороженная регистрация нетронута; venv привязан к /workspace/repo
(r3: свежий бандл КЛОНИРУЕТСЯ ПОВЕРХ, не в новый каталог). Суита 3 905/2.
**Следующее — контракт `lora-c-run r3` (пишет новая тимлид-сессия):** смок на
A100 покупает s/step на 3 072 → слово оператора по капу (вилка $6.5–8) → армы.

**`lora-c-migrate` (Dv820–828, талли 7/2) — ПРИНЯТ как STOP на $0.0000.**
Первая же бесплатная проверка (отказ create) опровергла (р): EU-SE-1 томов не
держит, 21 томопригодный ДЦ перечислен самой платформой, $0.53-A6000 нет ни в
одном. Попутно: r2-close ЗАКРЫТ ($0.765 осело против $0.761 часов пода — часы
честны); close пробы гард отказал структурно (7%-полоса не грейдит шаг $0.075
при лаге ~3 цента — названо, не подкручено); 4 дефекта рунгов миграции найдены
за $0 (границы стадий крест-накрест с хард-стопом; venv никто не мерил;
liveness закачки по строкам = ложный KILL; парсер цены прочёл бы «6000» из
имени карты). Чекер 64/64 моим прогоном; суита 3 843/2 stamped. Рулинг (с):
A100 CA-MTL-3, миграция r2 капом $1.50.

## Очередь контрактов

1. ✅ ПРИНЯТЫ 18–19.08: `guard --until` · `pass1-data-prep` · `vault-dream` ·
   `labels-boot-audit` · `boot-debloat` · `pass1-redraw` · `lora-b` D0–D2 ·
   `reader-topup` — см. сжатые дни выше и отчёты в `docs/reports/`.
2. ✅ **`lora-b-run` + аддендум ПРИНЯТЫ 20.08** — КРАСНЫЙ 9/14, линия B
   ЗАКРЫТА; талли contract-health 7 (c-gap 3 · s-gap 1 · v-gap 3) · paid 5
   (process 4 · env 1) — см. день 20.08.
3. ✅ **`pass1-fewshot` r1+r2 ПРИНЯТЫ 21.08** (талли 15/5 · 11/5). ✅
   **`pass1-window` r1+r2 — окно-1 куплено за $0.742.** ✅ **`pass2-signals`
   r1+r2 ПРИНЯТЫ 22.08.** ✅ **`lora-c-prep` ПРИНЯТ 23.08 как STOP-отчёт** (день
   23.08). ✅ **`lora-c-apply` и `lora-c-close` ПРИНЯТЫ 23.08.** ✅
   **`lora-c-run` ПРИНЯТ 24.08 как STOP на $0; `lora-c-armb` ПРИНЯТ 24.08.**
   ✅ **`lora-c-run r2` ПРИНЯТ 25.08 как KILL по рунгу 4** ($0.76, базы
   куплены, OOM, попытка цела). ✅ **`lora-c-vramprobe` ПРИНЯТ 25.08** ($0.075,
   PRO 4500 закрыта). ✅ **`lora-c-migrate` ПРИНЯТ 25.08 как STOP на $0** —
   (р) опровергнут платформой. ✅ **`lora-c-migrate r2` ПРИНЯТ 26.08** —
   том mp-lora-c построен, загрузка доказана (40.22 с, 17/79 GiB), $0.287.
   **СЛЕДУЮЩИЙ — `lora-c-run r3` ВЫДАН 26.08 (рулинг (т), кап $7.00):
   промпт `docs/PROMPT-lora-c-run-r3.md`** — смок (первый s/step на 3 072) →
   рунг проекции → арм A → eval A → арм B → eval B + маркер-ценз → бары (к),
   один выстрел; клон ПОВЕРХ /workspace/repo; отчёт — в НОВУЮ тимлид-сессию.
4. **prep-b** — коллекция Сільпо/Varus/Маркетопт ($0, контракт согласован) —
   после читательского тракта (слово оператора 16.08).
5. `money-anchors` ($0) — по слову оператора (см. Открытые решения, п. 2).

Рамка честности разметки — ADR `sitting-b-line-b-and-the-team-lead-labels`.

## Деньги (чтение гарда тимлидом на приёмке 26.08)

Фаза 4 ЗАКРЫТА $32.4708 из $33.00 (финальное чтение 16.08, не растёт).
**Цикл-2: $10.0070 из $20.00, остаток $9.9930** (моё чтение гарда 26.08 на
приёмке migrate r2; линия lora-c по часам подов: r2 $0.7606 [осело $0.765] +
проба $0.0752 [close структурно ОТКАЗАН → money-anchors] + миграция $0.287;
**ДВА тома теперь: ~$0.47/сутки ≈ 21 день остатка** — mp-lora-c удаляется по
закрытии линии, слово оператора). Вперёд: run r3 на A100 $1.39 — фикс ≈$3.1,
вилка пакета $6.5–8, кап словом оператора НА ЧИСЛАХ СМОКА; **окно-2 ($4.8) в
этот цикл НЕ влезает — сказано 25.08.** Деньги читаются ИЗ ГАРДА.

## Открытые решения (повестка брифинга)

0. **РУЛИНГ ОПЕРАТОРА 20.08 (вечер, в тимлид-сессии) — ЗАРЕГИСТРИРОВАН ЭТОЙ
   ЗАПИСЬЮ:** база НЕ меняется — остаёмся на Gemma-4-31B; отдельное сидение C
   не нужно — слово оператора здесь = рулинг сидения; цель слоя — «такой же
   анализ комментариев, как ручной разбор тимлида» (`docs/REFERENCE-signals-w1.md`:
   F1–F5 · E1–E4 · N); разметку и дальше делает тимлид. Тимлид предлагает
   варианты в рамках этих ограничений (меню — в чате 20.08, выбранное — сюда).
1. **РУЛИНГ ОПЕРАТОРА 21.08 (в тимлид-сессии, после брифинга) — ЗАРЕГИСТРИРОВАН
   ЭТОЙ ЗАПИСЬЮ.** (а) **v2 (`pass1_comment_gm4_v2`) — базовый промпт прохода-1**:
   следующая линия строится поверх него и меряется против него («до»-числа
   136/200 · 38/49 на dev-200). Регистрация r2 закрыла итерацию промпта, не
   принятие измеренного; по 14 золотым v2 НЕ стрелял — и не стреляет без новой
   регистрации. (б) **Следующий контракт — (1) сквозной прогон окна-1 на v2:**
   проход-1 по ~1 032 платным комментам + проход-2 (сборка сигналов) против
   `docs/REFERENCE-signals-w1.md` с пре-регистрированным баром по F1–F5 /
   E1–E4 / N; ~$1–2, без обучения. Это рулинг ПОВЕРХ следствия регистрации r2
   (v2 шипится как ПРОВИЗОРНЫЙ фильтр для сквозного замера, не как принятый
   деливерабл). Цель замера — какая ошибка прохода-1 (мусор 45% / пропуски
   20%) ломает проход-2. (в) **Затем — (4) LoRA «рассуждение→метка» поверх v2 с
   (3) синтетикой как армом B** одной регистрации; цель обучения — из (б).
   Отклонены: (4) первым (инструмент оценки 22 строки); стоп и упаковка.
   (г) **Дизайн прохода-2 = (A):** один LLM-вызов на ТРЕД по комментам, которые
   проход-1 пометил «наши»/`сеть_ритейлер`, с entity-контекстом → сигналы
   `{тип · субъект · аспект · цитаты msg_id}`; **рулинг по ADR 17.08: такой
   вызов НЕ «однопроходный читатель»** (не делает per-comment атрибуцию, не
   отвечает на всё по сырому треду) — записать в ADR, связывает обе ступени.
   (B) детерминированная сборка отклонена. (д) **Два контракта, один платный
   прогон на каждый:** `pass1-window` (кап $1.50) → `pass2-signals` (кап $1.50).
   14 золотых в окне — строка ценза с множественностью, НИКОГДА не бар. ADR
   записан (`v2-is-the-base-and-pass-2-is-not-a-one-shot-reader`).
   (е) **РУЛИНГ 21.08 (вечер), после красного pass1-window:** дозаказ 901
   коммента регистрацией `pass1-window r2` с капом **$2.00** — ИСПОЛНЕН,
   ЗЕЛЁНЫЙ, $0.568. (ж) **РУЛИНГ 22.08 (утро) — дизайн `pass2-signals`:** схема
   и скорер v5-читателя, голд r2 как есть; проход-2 СТРОГО не перемаркирует
   (может выбросить; `subject_doubt` отчётный); вход = фильтр + пост +
   entity-блок; бар референса как есть + таблицы; смок первым; кап $1.50 —
   ИСПОЛНЕН, STOP на смоке. (з) **РУЛИНГ 22.08 (день):** `pass2-signals r2`, 75
   тредов, кап **$2.50** — ИСПОЛНЕН, $0.424, бары посчитаны. (и) **РУЛИНГ 22.08
   (вечер):** фаза 6 ОСТАЁТСЯ открытой, следующий контракт — LoRA-линия сразу;
   **дистилляция скилла и ретро фазы ОТЛОЖЕНЫ словом оператора** до результата
   LoRA (триггер 705 ≥ 700 стоит, долг назван); адъюдикация референса (E1 для
   прохода-2; S1 в N1 — машина нашла референсный вторичный сигнал 21420)
   ОТЛОЖЕНА до результата LoRA — вердикт r2 не трогается. (к) **РУЛИНГ 22.08
   (вечер) — дизайн `lora-c`:** промпт v3 = v2 + поле `rationale` ПЕРЕД меткой,
   одним рендерингом для обучения и инференса; таблица парно в четыре колонки
   (база v2 · база v3 · адаптер A · адаптер B) на одном пуле соседей; рационали
   для обучающих строк пишет Claude Code по кодбуку и метке тимлида, тимлид
   ревьюит все «наши» + ~40 пограничных; синтетика ~160 (по 40 на класс ошибки)
   — арм B, тег в provenance, абляция армом A, никогда в холдаут/голд; из
   обучения и из пула соседей исключены холдаут-100 и ВСЕ строки 16 референсных
   тредов; **бар на арм (один выстрел): холдаут-100 ≥ 64 И сквозной бар 1 =
   5/5 И бар 3 = 0 — все три или КРАСНЫЙ**; отчётно: ячейка «упоминание →
   категория» на холдауте (18 → ?), dev-200 (fit), 14 золотых (шестой взгляд).
   Два контракта: `lora-c-prep` ($0, с ревью-гейтом тимлида) → `lora-c-run`
   (один A6000, кап **$4.00**). **Скилл team-lead:** дельты 20.08 применены;
   эталон `team-lead-SKILL-2026-08-20.md`; **триггер 705 ≥ 700 стоит, дистилляция
   ОТЛОЖЕНА (и)** — шесть дельт-кандидатов в handoff проекта.
   (л) **РУЛИНГ 23.08 (приёмка lora-c-prep) — ЗАРЕГИСТРИРОВАН ПОПРАВКОЙ SPEC
   3.25:** `max_seq_len` 1 408 → **2 816** новой ревизией `config/qlora.yaml`
   (при импортируемом токенайзере — реальный счёт всех 506 строк, >2 800 = STOP
   к оператору, следующий рунг 3 072); правило соседей получает ЕДИНСТВЕННЫЙ
   отказ — кандидат с идентичным (нормализация пробелов/регистра) текстом
   запроса; СТОП №1 принят как есть (506 строк, потолок холдаута 98, бар не
   занижен). Кап lora-c-run $4.00 стоит до вывода цены — поднятие отдельным
   словом. Вердикты обоих гейтов — файлы тимлида в `docs/reviews/`.
   (м) **РУЛИНГ 23.08 (в сессии исполнителя, подтверждён на приёмке) —
   ЗАРЕГИСТРИРОВАН ПОПРАВКОЙ SPEC 3.26:** «поднимай max_seq_len до 3072» —
   рунг, преавторизованный 3.25; `config/qlora.yaml` ревизия 3; запас над
   максимумом 97 токенов (81 со слэком); порог STOP 2 800 читается ИСТИННЫМ
   счётом (без TEMPLATE_SLACK — Dv777); ratio-модель для потолков в отставке
   (недооценка 228 токенов, Dv765).
   (т) **РУЛИНГ 26.08 (после приёмки migrate r2) — ЗАРЕГИСТРИРОВАН ЭТОЙ
   ЗАПИСЬЮ:** **кап `lora-c-run r3` = $7.00 all-in**, преавторизован — сессия
   проходит смок → армы → бары БЕЗ паузы на слово, рунг проекции исполняет кап
   (KILL = соблюдение); покрытие ~91 с/шаг по фактическим ставкам (~61 по
   заряженным); отчёт принимает НОВАЯ тимлид-сессия. Промпт:
   `docs/PROMPT-lora-c-run-r3.md` (Dv с 837; step 0.5 — инвариант «ни один
   блоб не растёт», долг пробы → money-anchors, греп [-1]-селекторов).
   (с) **РУЛИНГ 25.08, вечер (на приёмке lora-c-migrate) — ЗАРЕГИСТРИРОВАН
   ЭТОЙ ЗАПИСЬЮ; ОТМЕНЯЕТ (р):** рулинг (р) ОПРОВЕРГНУТ платформой за $0 —
   EU-SE-1 (и все 4 ДЦ из таблицы пробы) не поддерживают сетевые тома; вопрос
   способности «где живёт том» на приёмке пробы не задал ТИМЛИД — мой промах,
   урок в ретро (таблица стока обязана нести JOIN всех способностей плана).
   Новый путь: **A100 PCIe 80 GB, ≤$1.50/ч, CA-MTL-3** (томопригоден по отказу
   create; тот же выбор runbook_4a §1). Миграция r2:
   `docs/PROMPT-lora-c-migrate-r2.md`, кап $1.50 (при $1.39 старый $1.00 =
   2 590 с — границы не влезали), закачка ужата до 1 200 с (замер 240 с/62 GB),
   НОВЫЙ рунг venv 900 с (первый замер стадии), liveness закачки ПО БАЙТАМ,
   rung-0 price-first. Окно-2 в этот цикл не влезает — сказано оператору.
   Кап r3 — словом оператора на числах миграции и смока A100 (вилка $5–6.5).
   (р) **РУЛИНГ 25.08 (приёмка vramprobe) — ОПРОВЕРГНУТ платформой, см. (с):**
   переезд в EU-SE-1 на A6000 $0.53 (PRO 4500 закрыта пробой — дефицит
   структурный). EU-SE-1 томов не поддерживает; хроника — отчёты
   vramprobe §5 / migrate §1.
   (п) **РУЛИНГ 25.08 (на приёмке lora-c-run r2) — ЗАРЕГИСТРИРОВАН ЭТОЙ
   ЗАПИСЬЮ:** после OOM на 32 GB — **проба `expandable_segments` на том же
   PRO 4500, кап $0.30** (`docs/PROMPT-lora-c-vramprobe.md`; замороженную
   регистрацию не трогает, свой микро-рекорд; оба исхода — ответы: выжил →
   есть и s/step на 3 072, нет → карта закрыта, вопрос датацентра ко мне).
   **Правило тимлида:** во всех будущих выводах v3 заряжается ИЗМЕРЕННЫМИ
   9.20 с/звонок (не 6.14); PRO 6000 $2.09/ч признан невлезающим (~$11/сессия).
   Ответ на вопрос отчёта «нужен ли адаптер»: ДА — мой дозамер за $0: v3 НЕ
   чинит ни 580124 (F2a: по-прежнему «сеть»), ни бар 3 (те же 4 ложных «сеть»
   в 10366); прирост v3 (+5 холдаут) — весь в не_наш_рынок, мимо красных целей.
   (о) **РУЛИНГ 24.08 (на приёмке lora-c-armb, на числах вывода) —
   ЗАРЕГИСТРИРОВАН ЭТОЙ ЗАПИСЬЮ:** кап lora-c-run **остаётся $4.00** (оба
   существующих замера s/step влезают по фактической цене; смок решает, рунг
   проекции убивает честно); карта — **RTX PRO 4500, 32 GB, $0.72/ч, EU-RO-1**
   (A6000 в датацентре тома none; 32 GB — запас против OOM при замороженном
   micro_batch; фолбэк 4090 преавторизован). **Дизайн-рулинги тимлида (Dv795,
   Dv802):** ложная строка топика в синтетических заголовках заменяется
   выделенной константой (продовая строка NO_POST_TEXT в синтетике запрещена);
   конфаунд маркера принят и именован; в ран добавлен отчётный маркер-ценз
   (~$0.05) — отделяет «не помогло» от «выучил маркер»; вайтлист check-stamped
   расширен на knowledge/index.md (проходит зарегистрированный критерий),
   hot.md остаётся вне списка. Промпт: `docs/PROMPT-lora-c-run-r2.md`.
   (н) **РУЛИНГ 23.08 (в сессии исполнителя, на STOP-отчёте lora-c-run;
   ЗАРЕГИСТРИРОВАН ЭТОЙ ЗАПИСЬЮ):** «сначала $0-контракт на датасет арма B» —
   отдельный преп, тимлид рулит рендеринг синтетической строки v3-запросом,
   продюсер рендерит 666, регистрация пинит файл; затем ОДИН lora-c-run с
   выведенным капом и обоими армами. «Кап оставить $4.00 — решать после
   выбора объёма»: слепого поднятия нет, кап перевыводится контрактом,
   знающим объём. **Дизайн-рулинги тимлида на приёмке 24.08 (в рамках (0)):**
   синтетический запрос рендерится ТЕМ ЖЕ правилом на ТОМ ЖЕ пуле 515 (закон
   уже запрещает синтетику только как СОСЕДА; запрет двойника действует);
   проход-2 в ране — ТОЛЬКО для армов (2 ноги, не 4): база v2 сквозной уже
   измерена (r2: 4/5 · 2), база v3 бара не берёт — экономия ~$0.47 по заряду.
2. **Денежная гигиена (контракт `money-anchors`, $0):** закрытие шагов по
   settled-биллингу (walk отстаёт на 30–40 мин — Dv504/658/678), зависший walk
   pass1-probe, `anchored_at` для 19 отказанных леджеров. Словом оператора.
3. **Дизайн командного центра** — отвергнут 15.08; направления на выбор —
   после установки Design-плагина.
4. Мелочи step-0.5 (ждут контракта, трогающего их файлы): `t5_depth`-бейдж ·
   валюта ₴ в шапке промо · fix-b(2) «две колонки — одно число» · кириллические
   написания сетей в registry («Varus» ≠ «варусі», Dv732) — новой ревизией.

## Живые рулинги читательского тракта (сидения 15–17.08, сжато)

- Эталон цели: `docs/REFERENCE-signals-w1.md` (ручной разбор тимлида 15.08);
  голд — ревизия r2 («категория» ≡ «категория_личное»); референс не правится.
- Популяция тракта читателя окна-1: **129 тредов / 1 032 платных коммента**
  (varto-глушитель снят для ТРАКТА ЧИТАТЕЛЯ; классификация не тронута).
- Транспорт (v3→v5b, живёт в коде и суите): один JSON-объект, `[]` для
  пустого; эхо msg_id; потолок вывода 4 000 ТОКЕНОВ; kill-clock по рунгам
  (ssh 500 с · launch→ответ 450 с · проекция · живость 600 с · хард-стоп
  платформы), одно пересоздание, третий под = STOP; gate-записи append-only;
  поды, не serverless. **ID коммента по окну — только пара (тред, msg_id)**.
- F2a-carve-out (рулинг 17.08): событие о доступности предмета поста — сигнал о
  предмете поста; мнения без имени не переносятся.
- **Однопроходная промпт-линия ЗАКРЫТА stop-rule'ом** (v5b: бар-1 2/5); ADR
  связывает тимлида; проход-2 по атрибутированным комментам — НЕ он (ADR 21.08).

## Отложено СОЗНАТЕЛЬНО (что · критерий развязки)

- **Serving-оптимизация (латенси)** — по слову оператора «готовим прод» (вход
  Фазы 7); пути известны (пре-квант база; merge+vLLM новой прережкой).
- **История 11 143 строк** и **батч-паритет** — ждут батч-решения И денег
  ($4.35 при −30%; `results/batch_cycle2_projection.json`).
- **5c3: категорийный слой** (таксономия 13+2, механика не строена), сайдкар
  v2. **G1a/G1c** — новая фаза качества или потолок принят, на свежих данных.
- **Алерты (вопрос 6)** — пороги словом оператора + окно-2 для базовой линии.
  **Тренды** — окно-2. **Продажи ТПК** — по слову собственника (SQLite готов).
- **Мёрдж адаптера в bf16** — после замера в прод-конфигурации; **LLM-нарратив
  отчёта (5d)** — модель выбирается на живых агрегатах.

## Правила, которые легко забыть

- Все eval гейтов — батчом 1; v2/v4-голды и запечатанные вердикты неизменны.
- Числа — только из result-файлов через скорер; в презентационные артефакты
  руками не вписываются (закон 3.20, отравленный тест).
- **Деньги читаются из гарда, не из леджера и не из прозы** (Dv33); часы пода
  — число для цитаты, walk отстаёт (Dv504).
- **Прережка замерзает при первом `pod create` попытки**; правка после —
  только новой регистрацией; кап живёт в регистрации, его поднятие — слово
  оператора ДО эндпоинта.
- **Пины — потребители:** `prompts.py` (38 записей), `scorer.py`, `brands.py`,
  `local_llm.py`, `window_summary_5c2.py`, PLAN-файлы; правка = MOVED-манёвр или
  новый модуль, не перепин. Префлайт контракта: `make preflight` на всё, что он
  трогает. `registry.yaml`/`lexicon.yaml` запечатаны — правки новыми ревизиями.
- Никогда `git add -A`; файлы тимлида исполнитель коммитит, не правит;
  отчёты — файлами в `docs/reports/`, в чат — только путь. Впрыснутые треды
  проб (`injected: true`) не входят в продакшн-агрегаты и цену окна.
- Пре-регистрации не переоткрываются; провал закрывает вопрос (one attempt);
  stop-rule закрывает ЛИНИЮ. **Платный под не оставляют без опроса** — рунг
  живости в каждой регистрации (9 720 с холостых = $1.43, 20.08).
- **Ревью пятью линзами ДО первого `pod create`, по ЗАМОРОЖЕННОМУ дереву** —
  строка шаблона; тесты гоняют КОМАНДЫ (точки входа), не только функции (два
  фатальных дефекта pass2 жили в `main`); отчётное поле не может отказать.

## Урок недели (ретро 18.08)

Узкое место — тимлидский ярус (P1–P7); пилоты H1/H6, H5b, H4 СРАБОТАЛИ. H5a/H7 и
split-талли фазы — на закрытии фазы 6.
