# STATUS — market-pulse-llm (2026-07-31)

> Operator-facing document, kept in Russian by operator request (exception to
> the English-artifacts rule). Updated by the team lead at every phase close.

## Что это за проект (одним абзацем)

Ключевой инструмент маркетинговых исследований компании (ТМ «Гармонія»):
мониторим Telegram-каналы украинских ритейл-сетей (Сільпо, АТБ, Varus) и
агрегаторов, находим анонсы новинок и промо по молочке/мороженому, извлекаем
бренды, классифицируем реакции покупателей (тональность, сарказм, скрытые
интенции: вкус/цена/упаковка/качество/наличие). Ядро — одна открытая LLM,
дообученная QLoRA на арендованной GPU. MVP: только Telegram, $0 за данные.

## Карта фаз — ГДЕ МЫ

| Фаза | Что | Статус |
|---|---|---|
| 0 | Спека (сейчас rev. 3.7), утверждена | ✅ DONE |
| 1 | Скелет репо: pytest+ruff, scorer-заглушки, реестр | ✅ DONE |
| 2 | Данные: коллектор, корпус 11.3k, разметка, замороженные тест-сеты | ✅ DONE 2026-07-28 |
| 3 | **Бейзлайны + синтетика.** Полная таблица: TF-IDF, три LLM-кандидата + справочный Haiku (OpenRouter, $0.91 из $8), XLM-R supervised (CPU, 101 мин, $0); 600 синтетики (QA ✅); решения гейта в ADR; с 3c каждый прогон дампит попстрочные предсказания; ownership-слой файлов стоит | ✅ DONE 2026-07-31 |
| 4 | **QLoRA fine-tune.** Обе руки обучены, скорены по разу; правило отобрало **real-only**, синтетика отброшена (просадила G1c/G1d сверх допуска); **гейты: 2 из 5** (G1d, G1e — PASS с запасом; G1a, G1b, G1c — FAIL по марже при росте всех голов над базой). Артефакт: NF4-база + несмёрженный адаптер c0e462af… | ✅ DONE 2026-08-02 (приёмка + квиз 2/2) |
| **4.5** | **Программа качества (к измеренному потолку):** 4.5a аудит ошибок + потолок → по его итогам: доразметка, синтетика v2, dev-подбор гиперпараметров — каждый шаг со своим гейтом | **🔄 ИДЁТ** (4.5a: пакет → арбитраж оператора) |
| 5 | Продакшн-петля: поллинг + батч-инференс + агрегация | ⏸ после 4.5 — решение оператора 02.08 «сначала качество, потом петля» |
| 6 | Дашборд (числа только из result-файлов) | — |
| 7 | Валидация на ≥2 реальных запусках конкурентов + упаковка | — |

## Что уже ДОКАЗАНО (числа)

- Корпус: 11 338 комментов + 6 057 постов из 5 каналов, легально, $0
  (гейт был ≥5 000 комментов — пройден с запасом ×2.3).
- Разметка: 2 000 комментов + 1 000 постов (LLM + операторская калибровка,
  agreement 96.3% / 100% при пороге 90%).
- Заморожено с SHA256: тест комментов 400 строк (v2, 52 сарказма), тест
  постов 250, sarcasm-holdout 108. Трейн-источники: 2 946 реальных строк,
  из них **2 195 скорируемых** (T1: 906 из 1600 комментов + 540 из 746
  кандидатов; T2: 749 из 750 постов; unclear выпадает из обучения по
  контракту 4b) + 600 синтетики. Уточнено верификацией 4b — прежнее
  «~2 350 скорируемых» было устаревшим.
- Верификатор: make check, **275 тестов**, зелёный после каждого коммита.
- **Смоук тренера (4b, 01.08):** лосс 0.1799 → 0.0399 за 50 шагов (carve
  0.0585 → 0.0335), LoRA r16 на 410 модулях языковой части (vision исключён),
  чекпойнт сохранён/перезагружен/скорится, пик 30.47 из 48 ГБ, 45.23 с/шаг.
  Проекции: real-only 272 шага = 3.42 ч ($1.81), with-synthetic 348 шагов =
  4.37 ч ($2.32). Гиперпараметры заморожены в `config/qlora.yaml` до гейтов.
- Zero-shot бейзлайны 3b: 4 модели × 758 строк = 3 032 запроса, **$0.91 из
  потолка $8**, числа только через scorer в `results/baselines.json`.
- Supervised-бейзлайн XLM-R (seed 42, CPU 101.0 мин, провенанс сверен: код =
  ровно коммит 6d4deb3): G1a **0.7824** · G1c **0.6007** · G1d **0.7347** —
  выше TF-IDF (G1c вровень, +0.0007), ниже Gemma по всем сопоставимым клеткам,
  выбор базы Phase 4 не двигает. G1b `null`, G1e NOT COVERED («не пытались»,
  не «ноль»). 54/108 сарказмов холдаута — диагностика уклона в negative
  (107/108 строк негативные), не чтение иронии и не срез G1b.
- **Гейты Phase 4 (4c, 01–02.08): 2 из 5, выбрана real-only.** Обе руки
  758/758 без отказов, батч 1. Real-only против якоря: G1a 0.9107 (+1.89 pp,
  планка 0.9418 — FAIL; полы ua/ru пройдены) · G1b 21/44 при пороге 27 —
  FAIL (guard +0.0189 ✅; из 23 непочиненных: 15 только сарказм, 8 обе,
  0 только тональность; детекция сарказма 82/108 против 64 у базы) ·
  G1c 0.8212 (+2.76 pp, планка 0.8436 — FAIL) · G1d 0.9386 (+3.02 pp НАД
  якорем — PASS) · G1e 0.9333 (+3.59 pp — PASS). With-synthetic: G1b лучше
  (24/44), ru +4.44 pp, но G1c −1.39 / G1d −2.27 — правило отбросило.
  Всё пересчитано тимлидом из дампов бит-в-бит. Спенд фазы $6.88 из $25.
- **Own-pod строка Gemma (4a, 01.08): 758/758, ноль отказов, батч 1, 39 мин,
  $0.35.** G1a 0.8918 (ua 0.8918 · ru 0.8849) · G1c 0.7936 · G1d **0.9084** ·
  G1e **0.8974**. Это ЯКОРЬ всех гейтов (амендмент 3.5). Срез G1b = **44 id**
  (13 sentiment-ошибок ⊂ 44 sarcasm), файл с sha256. Расхождение с OpenRouter
  fp8 −0.024…+0.019 по головам — записано, не усреднено. Находка исполнителя:
  greedy НЕ батч-инвариантен на этом стеке (1 строка из 24 сменила метки
  между батчами 8 и 1) — все eval'ы гейтов идут батчом 1. Верификация
  приёмки: независимый пересчёт G1d из дампа — бит-в-бит.

## Ключевые решения (и почему)

1. Telegram-only MVP — решение оператора (легально, бесплатно; FB/IG/X
   исследованы и отложены в трек R с ценами).
2. Ритейл-сети вместо производителей — решение оператора (производители не
   ведут TG; у сетей живые каналы).
3. EN исключён из гейтов (поправка 3.1) — в корпусе 8 EN-строк из 2 000.
4. Sarcasm-holdout 108 строк вместо 150–200 (поправка 3.2) — корпус
   физически исчерпан; честно записан fallback на малое n.

## Решения, которые СОЗНАТЕЛЬНО ОТЛОЖЕНЫ (и когда принимаются)

- ~~Какую LLM дообучаем~~ РЕШЕНО на гейте вечером 31.07, по числам:
  **Gemma-4-31B** — все 4 гейтовые головы с отрывом, обошла референс Haiku,
  чистый прогон без потерь строк, Apache-2.0 (подтверждено независимо).
  Выбор защищён bound-анализом непарности (ADR phase4-base-model-gate,
  пишется в 3c). Якорь G1d/G1e — её перепрогон на нашем поде в смоуке
  Phase 4 (ADR `3b-infra-and-precision` §(d),(f)).
- ~~GPU-провайдер~~ РЕШЕНО 2026-07-28, УТОЧНЕНО 2026-07-31: **RunPod** остаётся
  провайдером для **обучения** (Phase 4 QLoRA) — A6000 48GB $0.53/ч, network
  volume $0.07/ГБ/мес, есть CLI `runpodctl` v2. Но **бейзлайны 3b на нём не
  считаются**: zero-shot ушёл на API OpenRouter (см. ревизию 31.07).
  Пополнение RunPod отложено до гейта Phase 4 — решение оператора 31.07.
- ~~Архитектура продакшн-петли~~ РЕШЕНО на архитектурной сессии 2026-07-28:
  инференс — RunPod serverless (батч 2–4×/день, ~$2–5/мес); петля сбора —
  Mac cron для MVP, VPS (~$5/мес) при переходе в бой (Phase 5); агрегаты —
  SQLite; дашборд — Streamlit. Каждый блок заменяем независимо.
- Бейзлайны Phase 3 расширены: + справочная строка zero-shot фронтирного API
  (Claude Haiku, ~$1–3 одноразово) — «самая дешёвая реалистичная
  альтернатива». Гейты G1a–e остаются привязаны к трём основным бейзлайнам.
- ~~Список моделей-кандидатов~~ РЕШЕНО 2026-07-31 живым поиском: три кандидата
  зафиксированы (см. ревизию инфраструктуры ниже), исполнитель их не меняет.
- ~~LoRA в bf16 или QLoRA в 4-bit~~ **РЕШЕНО на брифинге 01.08: ветка (2),
  QLoRA 4-bit (NF4) на A6000** — амендмент 3.4; расклад ниже сохранён как
  история решения. Поднято оператором 2026-07-31.
  Суть: «Q» в QLoRA — это только квантование замороженной базы; обычная LoRA
  на неквантованной bf16-базе технически возможна и упирается лишь в память.
  Расклад по кандидатам (база + адаптеры + активации):
  Gemma-4-31B — bf16 ~70 ГБ / 4-bit ~24 ГБ · Qwen3.6-27B — ~62 / ~21 ·
  **Qwen3.5-9B — ~22 / ~8, то есть bf16 влезает в A6000 48 ГБ.**
  Цена обучения аргументом НЕ является: датасет крошечный (2350 реальных +
  600 синтетических строк, короткие тексты), прогон 1–1.5 ч, разница
  QLoRA на A6000 ($0.53/ч) против LoRA bf16 на A100-80 ($1.39/ч) — единицы
  долларов. Решает согласованность: **учим в той точности, в которой отдаём
  в прод**. Адаптер, обученный на квантованной базе и влитый в bf16-веса,
  расходится с ними — и ровно так же расходится адаптер, обученный на bf16
  и запущенный на квантованной базе. Прод по решению 28.07 — RunPod
  serverless, батч 2–4×/день, где холодный старт (вес модели) важнее
  вычислений, что склоняет к квантованию для 27B/31B.
  Три ветки: (1) выигрывает 9B → чистая LoRA bf16 на A6000, квантования нет
  нигде; (2) выигрывает 27B/31B и прод квантованный → QLoRA; (3) выигрывает
  27B/31B и прод bf16 → LoRA bf16 на A100-80.
  **После гейта 31.07 (выиграла Gemma-4-31B; bf16 ~70 ГБ в A6000 не влезает;
  прод по решению 28.07 — serverless квантованный) рабочая гипотеза —
  ветка (2): QLoRA 4-bit на A6000. УТВЕРЖДЕНА на брифинге 01.08.**

## Ревизия инфраструктуры (2026-07-31)

Повод: у оператора пополнен **OpenRouter ($9.84)**, а на **RunPod $0.00**.
Это сломало предпосылку замороженного промта 3b («аренда A6000, bf16»)
и мы пересобрали шаг на совместном разборе.

**Что решено:**

1. **Zero-shot считаем через API OpenRouter, GPU в 3b не арендуем.**
   758 строк на кандидата (400 комментов + 250 постов + 108 холдаута)
   стоят ~$0.05–0.25 за модель. Весь шаг ≈ $2 против ~$12 аренды.
   Потолок бюджета: **$8** (pre-registered, ниже реального баланса).
2. **Правило точности (precision) — записано ДО первых цифр.**
   Исполнитель сначала опрашивает `/api/v1/models/<slug>/endpoints` по всем
   трём кандидатам. Если `bf16` доступен у всех троих — берём bf16 (исходное
   ограничение 28.07 соблюдено). Если хоть у одного нет — берём `fp8` у всех
   троих. **Смешивать точности между кандидатами запрещено** — иначе разница
   в числах будет от квантизации, а не от модели, и парное сравнение
   рассыплется. Маршрутизация пиннится жёстко (`allow_fallbacks: false`),
   провайдер и квантизация пишутся в provenance каждой строки.
3. **Честная оговорка и её компенсация.** Числа приходят со сторонних
   эндпоинтов, а не с нашего железа. Компенсация: модель, выбранная на гейте
   Phase 4, один раз перепрогоняется zero-shot на нашем поде в смоуке —
   сверка «OpenRouter vs своё железо». Записано в ADR.
4. **Три кандидата зафиксированы оператором** (живой поиск проведён 31.07):
   `google/gemma-4-31b-it` (~31B, **Apache-2.0** — проверено по карточке HF
   и по странице лицензии Google, Gemma 4 сменила лицензионное семейство;
   в промте 3b было «Gemma Terms of Use») ·
   `qwen/qwen3.6-27b` (~27B, Apache-2.0) ·
   `qwen/qwen3.5-9b` (~9B, Apache-2.0 — дешёвая нижняя граница, отвечает на
   вопрос «а хватит ли маленькой»). Обе крупные вышли в апреле 2026.
5. **Справочная строка Haiku идёт через тот же OpenRouter** — отдельный ключ
   Anthropic не нужен, из `.env` он выпадает. Но **не** `:batch`: OpenRouter
   отдаёт этот вариант только через асинхронный `/api/beta/batches` и на
   `/chat/completions` возвращает 404. Прогнано на синхронном
   `anthropic/claude-haiku-4.5` ($1.00/$5.00 за 1M) — та же модель, +$0.24.
6. **XLM-R (supervised бейзлайн) обучаем локально на Mac (MPS), $0.**
   Сначала таймленный смоук; если оценка полного прогона > 60 минут —
   исполнитель останавливается и докладывает, переносим на под.
   **Сработало именно так:** смоук намерил 3.503 с/шаг на CPU (→ 130.8 мин)
   и 48.117 с/шаг на MPS (→ 1764 мин), оба выше потолка, полный прогон не
   стартовал. MPS на этой машине не просто медленнее — он ещё и падает по
   памяти на 9.07 ГиБ. Скрипт готов и ждёт пода.
7. **RunPod не пополняем сейчас.** Сейчас — только поставить `runpodctl`
   и создать API-ключ (бесплатно). Деньги (~$30–40) — на гейте Phase 4,
   когда известна модель. Важно на будущее: при балансе $0 поды **без**
   network volume удаляются безвозвратно, поды **с** volume — сохраняются.

## Решения ревизии подхода (2026-07-28)

- Совместный протокол: ни один промт в Claude Code без обсуждения шага
  (брифинг → вопросы обеих сторон → решение). Вшито в скилл team-lead.
- Постоянное делегирование: чисто механические фиксы (опечатки, даты,
  формат) идут без обсуждения, но всегда видны в отчёте исполнителя.
- ADR — на английском в knowledge/decisions/; русские выжимки решений —
  здесь, в STATUS.md.
- Residual-лик холдаута (31 строка делит треды с обучающим пулом): принят
  с обоснованием (модель видит только текст коммента), компенсируется
  расширением сарказм-данных для ОБУЧЕНИЯ (источник обсуждается).

## Итог 3b (2026-07-31, исполнитель отчитался)

**Правило precision сработало в ветку `fp8`:** у `qwen/qwen3.6-27b` нет ни
одного bf16-эндпоинта ни у одного провайдера, значит по предрегистрированному
правилу fp8 у всех троих. Пины: gemma → `parasail/fp8`, qwen3.6 → `io-net/fp8`,
qwen3.5 → `venice/fp8`, `allow_fallbacks: false`, квантизация и провайдер — в
provenance каждой строки. Probe воспроизводится командой
`python3 scripts/eval_zero_shot.py --precision-probe` (бесплатно, только GET).

Числа читаются `python3 scripts/show_results.py --last`. **Выводы о выборе
модели тут не делаются** — это гейт Phase 4.

| строка | G1a overall (ua · ru) | G1c | **G1d** post_type (relevance) | G1e | ошибки на холдауте sent./sarc. |
|---|---|---|---|---|---|
| tfidf-logreg (3a) | 0.6834 (0.6982 · 0.6730) | 0.6000 | **0.6653** (0.8119) | 0.1964 | — |
| gemma-4-31b-it | 0.8944 (0.8957 · 0.8688) | 0.7981 | **0.8898** (0.9505) | 0.9211 | 10 / 40 |
| qwen3.6-27b ⚠️ | 0.8541 (0.8536 · 0.8667) | 0.7606 | **0.7605** (0.9778) | 0.8919 | 14 / 64 |
| qwen3.5-9b | 0.7745 (0.7957 · 0.8579) | 0.6252 | **0.5077** (0.9778) | 0.6476 | 26 / 70 |
| *справочно* claude-haiku-4.5 | 0.8708 (0.8692 · 0.8988) | 0.7739 | 0.7718 (0.9694) | 0.8537 | 21 / 47 |

Справочная строка в файле помечена так, что поиск по `G1d` её не найдёт: у неё
все поля `gate` переписаны в `ref`. Она не якорит гейты по решению 28.07.

**⚠️ qwen3.6-27b: два прогона, оба `gate_anchor_valid: false`.** Модель
возвращает JSON без поля `sentiment`. Первый прогон — 10 непригодных строк из
758, перезапуск 31.07 — 8. Строки в основном **те же самые** (3 из 4 на
комментах, 3 из 5 на холдауте совпали), то есть это свойство конкретных
текстов, а не сетевой шум; повторный прогон проблему не лечит. Порог 2%
превышен на холдауте (4 из 108 = 3.7%). Непригодные строки посчитаны отдельно
по причинам, названы по id и **исключены из счёта, а не подставлены дефолтной
меткой**.

Побочный результат перезапуска, который стоит держать в голове: **G1d у
qwen3.6 сдвинулся с 0.7308 на 0.7605 просто от другого подмножества строк.**
Это ровно то, о чём предупреждает оговорка про сторонние эндпоинты —
детерминизм на стороне провайдера не гарантирован даже при temperature 0.

**Три строки кандидатов не спарены по инстансам:** gemma и qwen3.5 посчитаны на
400/250/108, qwen3.6 — на 397/249/104. SPEC §5 требует «paired on identical
instances». **Поправка тимлида (приёмка 31.07):** заявленный исполнителем
«пересчёт по пересечению из файла, бесплатно» НЕ существует — в записях лежат
только хэши списков id (`scored_ids_sha256`) и счётчики ошибок, попстрочные
предсказания не сохранялись. Непарность закрывается bound-анализом в ADR
(решение гейта, см. ниже); с шага 3c каждый прогон обязан писать попстрочный
дамп предсказаний, чтобы такое не повторилось.

**Бюджет: $0.9124 из $8.00.** Провайдерская цифра, ledger — в
`results/spend_3b.json`. На ключе остаётся ~$9.09.

**Отклонения исполнителя от промта 3b** — девять штук, все в
`implementation-notes.md` § Deviations. Существенные: лицензия Gemma 4
оказалась Apache-2.0 (D3); `GET /api/v1/generation?id=` из промта не работает —
404 на валидный id при любой задержке, спенд считается из `usage.cost` в теле
ответа плюс сверка с `/credits` (D1); «errs on N of 108» отдано **тремя**
числами вместо одного, потому что поправка 3.2 говорит «misclassifies», а
холдаут несёт две метки — выбор чтения оставлен оператору (D2); дефолт XLM-R
переключён с MPS на CPU по измерениям (D7).

## Гейт 3b — четыре решения (вечер 31.07, совместный разбор)

1. **База Phase 4 = Gemma-4-31B.** По числам: все 4 гейтовые головы с
   отрывом, обошла референс Haiku, чистый прогон, Apache-2.0.
2. **Непарность строки 27B закрывается bound-анализом в ADR, $0.**
   Находка приёмки: попстрочные предсказания не сохранялись, «бесплатный
   пересчёт из файла» из отчёта исполнителя не существовал. В резерве
   свежий парный прогон ~$0.90 — если bound не защитит хоть одну голову,
   исполнитель обязан остановиться и доложить.
3. **Срез G1b = union ошибок (sentiment ∪ sarcasm).** Определяется
   перепрогоном выбранной модели на НАШЕМ поде в смоуке Phase 4 (полные
   108 строк, детерминированно, с дампом предсказаний). 40/108 с
   OpenRouter — превью, не срез.
4. **XLM-R — ночной прогон на CPU этого Mac**, потолок 60 мин снят
   оператором; бейзлайн не режется под часы (эпохи, MAX_LENGTH,
   архитектура — как предрегистрировано).

Плюс постоянный guard с шага 3c: каждый прогон пишет попстрочный дамп
предсказаний (id + метки, без голда и текстов) + sha256 в записи.

## Владение файлами — один писатель на файл (решение вечера 31.07)

Проблему поднял оператор: STATUS.md писали и тимлид, и исполнитель — два
конкурирующих писателя, нет одного источника правды. Кейс дня: перезапись
STATUS исполнителем на закрытии 3b (а) уронила правку тимлида по устаревшему
чтению и (б) занесла в операторский документ непроверенное (и ложное)
заявление про «бесплатный пересчёт». Решено:

- **Файлы тимлида** (исполнитель: читать и коммитить можно, править нельзя):
  `docs/STATUS.md`, `docs/SPEC.md`, `docs/PROMPT-*.md`. Поправки в спеку
  отныне вписывает тимлид напрямую, исполнитель коммитит.
- **Файлы исполнителя** (тимлид не правит): src, tests, scripts, results,
  knowledge/** (hot.md, daily logs, ADR), implementation-notes.md, runbook'и,
  остальной docs/.
- Механика, слоями: deny-правила на Edit/Write в `.claude/settings.json`
  (гарантия «каждый раз»); негативная строка в `/save` и `/close` (сами
  команды проверены — уже чистые, называют только файлы исполнителя);
  матрица в CLAUDE.md; стандартная DO NOT-строка в каждом промте; свип
  `git log -- docs/STATUS.md docs/SPEC.md docs/PROMPT-*` на каждой приёмке.
- Тимлид зеркально: STATUS правится только после приёмки отчёта и всегда
  перечитывается перед правкой.
- Скилл team-lead обновлён этим же вечером (мета-правило: процессный баг
  чинится в той же сессии).
- **Слой стоит и проверен боем (2b423c8, поздний вечер 31.07):** отказ без
  рестарта на всех трёх файлах, негативный контроль (WATCHLIST.md правится)
  пройден. Форма правил — находка F1: только `Edit(/docs/…)` — в CC 2.1.220
  `Edit(path)` покрывает все пишущие тулы, `Write(path)` не проверяется.
  brain-init обновлён в тот же вечер: новые проекты получают слой из коробки,
  после F1 выровнен на ту же Edit-форму.

## Phase 3 ЗАКРЫТА (поздний вечер 31.07) — как принималась

Приёмка тимлида, всё сверено на диске: XLM-R чистый (101.0 мин при прогнозе
128, seed 42, провенанс = ровно код 6d4deb3), строка в таблице — 87e3327;
второй мозг догнал факт — 8fc81ff; ownership-слой — 2b423c8, deny подхватился
без рестарта, боевой отказ на всех трёх файлах + негативный контроль пройден.
Квиз оператора 2/2. Deviations F1–F3 приняты. make check: 202 зелёных.

## Брифинг Phase 4 (2026-08-01) — четыре решения

Совместный разбор проведён, все развилки закрыты, вписаны в спеку
амендментом 3.4 (спека теперь rev. 3.4):

1. **Ветка (2) утверждена:** QLoRA 4-bit (NF4, compute bf16) на A6000 48 ГБ.
   Артефакт = 4-bit база + НЕмёрдженный адаптер; гейты скорятся ровно в этой
   конфигурации; мёрдж в bf16 запрещён до замера (Phase 5).
2. **Абляция синтетики:** обе руки (с файлом / без) скорятся на замороженных
   сетах; правило выбора зафиксировано ДО запуска — синтетика остаётся, если
   её G1b fix-rate строго выше И ни одна другая гейтовая голова не хуже
   >0.5 pp. Вердикт гейтов = выбранная рука; обе колонки публикуются; третьего
   прогона нет; перетренинга после просмотра чисел нет.
3. **RunPod: $35 + network volume ~100 ГБ** (веса bf16 ~62 ГБ нужны на диске
   для квантования при загрузке; volume страхует от безвозвратного удаления
   пода при балансе $0).
4. **Тайм-бокс:** хард-кап $25 GPU на всю Phase 4 (сверка биллинга перед
   каждым стартом) + потолок 4 ч/руку по смоук-проекции — выше: стоп и доклад.

## Приёмка 4a (вечер 01.08) — принят + три решения (амендмент 3.5)

Приёмка тимлида с независимой верификацией (субагент, 7/7 PASS): пересчёт
G1d из дампа через скорер — бит-в-бит; union-срез пересобран из дампа+голда
поэлементно; оба sha256 сошлись; prompt-SHA own-pod = 3b; свип владения
чистый; make check 239 зелёных лично у тимлида; спенд $0.62 из $25, под
остановлен. Deviations F1–F5 приняты (F2 — находка про батч-инвариантность,
предотвращена тихая порча меток). Write-probe отказал как ожидалось — тезис
F1 «Edit покрывает и Write» добит эмпирически.

Решения приёмки (все — ДО существования дообученных чисел):

1. **Own-pod строка = baseline (c) для ВСЕХ гейтов** (предложение оператора,
   согласие тимлида): цели G1a ≥ 0.9418 (floors ua 0.8718 / ru 0.8649),
   G1c ≥ 0.8436. Правило двигает планки в обе стороны — не «подбор мягкой».
2. **G1d/G1e: «+10 pp» заменён на «не хуже якоря − 1 pp»** (G1d ≥ 0.8984,
   G1e ≥ 0.8874; улучшение репортится, не гейтится). Причина: насыщение —
   0.9084 + 10 pp = 1.0084 > 1, гейт был невозможен математически. Это была
   видимая ещё 31.07 арифметика — недоработка тимлида на гейте 3b, записана
   в амендменте открытым текстом. Альтернатива «сокращение ошибки ≥25%»
   отклонена: у потолка упирается в шум разметки. Амбиция фазы — на
   G1a/G1b/G1c; T2-гейты теперь меряют реальный риск мультитаска: забывание.
3. **Механика G1b:** срез = файл `results/g1b_slice.json` (44 id); скорер
   по инструкции тимлида учится читать его снаружи; «починено» = строка
   выпала из union-ошибок дообученной модели (верно по ОБЕИМ меткам);
   порог ≥60% от 44 = **≥27 строк**, guard ≤2 pp; n=44 рядом с вердиктом.
   Все eval'ы гейтов — батч 1.

## Приёмка 4b (ночь 01.08) — принят; стоп-линия сработала штатно

Независимая верификация (субагент): сборка датасета воспроизведена **до
байта sha256** (2 171 = 906+540+749 скорируемых − 24 carve; все 230
сарказмов на месте — источники не терялись), проекции пересчитаны точно,
якорных литералов в коде ноль (grep пуст), baselines.json бит-в-бит не
тронут, 275 тестов зелёные лично у тимлида, свип владения чистый. Спенд
$1.12 из $25. Deviations D1–D4 приняты. Рука with-synthetic спроецировалась
в 4.37 ч > потолка 4 ч — исполнитель остановил линию по правилу 3.4 (4).

Решения оператора (амендмент 3.6, до старта полных прогонов):

1. **Потолок поднят до 5 ч/руку единообразно** — конфиг (2 эпохи и всё
   остальное) не тронут, парность абляции сохранена; защита — хард-кап $25
   (обучение ≈ $4.13). Отклонено: 1 эпоха (половинит сигнал G1b ради
   прокси) и снятие синтетической руки (решает абляцию без измерения).
2. **Рука = под-сессия, безтомно там, где есть сток A6000** (докачка весов:
   4 мин, $0.04); чекпойнты и артефакты синкаются на Mac по ходу — ничто не
   живёт только на поде. Том CA-MTL-3 — бонус при совпадении стока; второй
   том в EU-RO-1 отклонён (сток там был none всё окно).
3. **Resume-тест — обязательный первый шаг на поде 4c** (решение тимлида):
   реальное состояние оптимизатора, is_trainable=True на k-bit базу,
   продолжение траектории лосса.

## Приёмка 4c и закрытие Phase 4 (ночь 01–02.08)

Верификация тимлида (независимый субагент): каждое вердиктное число
пересчитано из попстрочных дампов **бит-в-бит** (обе руки, все головы, оба
fix-rate, guard-дельты); правило отбора перезапущено — вывод совпал
дословно; планки выведены из якоря программно (в коде ни одного числа);
дисциплина «одной попытки» доказана артефактами: resume-proof до руки A,
все артефакты руки B позже руки A, «0 rows already scored» в обоих eval,
третьих прогонов не существует, дифф кода между руками пуст. 313 тестов
зелёные лично у тимлида. Свип владения чистый. Deviations D1–D4 приняты.
Квиз оператора **2/2**. Находки верификации — уровня центов и формулировок
(заголовочные суммы точны: $6.8779 из $25, остаток $18.12).

Как читать итог — записано в ADR phase4-gate-verdict: три вопроса закрыты
отрицательно навсегда для этой конфигурации (перепопыток нет), два —
положительно с запасом (забывание T2 не материализовалось), артефакт при
этом лучше базы по каждой голове. Провалы — по марже, не по регрессу.

## Программа качества — решения обсуждения 02.08 (амендмент 3.7)

Цель оператора: реальные результаты + максимум опыта; названная планка
0.98. Честная поправка тимлида, принятая оператором: **0.98 выше потолка
измерительного прибора** — голд комментов калиброван на согласии 96.3%,
потолок теста ≈0.96–0.97 для идеальной модели. Программа гонится за
ИЗМЕРЕННЫМ потолком, не за абсолютным числом. Решено:

1. **Секвенирование: сначала качество, потом петля** — Phase 5 ждёт
   итогов Phase 4.5 (выбор оператора против рекомендации «параллельно»;
   зафиксировано).
2. **4.5a — аудит ошибок**: исполнитель собирает АНОНИМИЗИРОВАННЫЙ пакет
   (две метки в случайном порядке, без указания чья; ключ запечатан),
   оператор — единственный арбитр (1–2 вечера). Плюс контрольная выборка
   30–40 строк согласия — мера шума голда там, где спора нет.
3. **Аппетит доразметки — решается ПОСЛЕ аудита** (прицельно по таксономии
   ошибок, не «всё подряд»).
4. Дальнейшие шаги (доразметка, синтетика v2, dev-подбор) — каждый со
   своим предрегистрированным гейтом, только после чисел 4.5a.

## Решения, ОТЛОЖЕННЫЕ (видимые, с критерием развязки)

- **Артефакт Phase 5** (адаптер vs zero-shot) — решается после Phase 4.5,
  на её итоговых числах (лучший адаптер может смениться).
- **Правка голда (тест v3)** — только если аудит покажет грязь; отдельное
  решение на гейте 4.5a, с честным версионированием (старые прогоны
  перескориваются из дампов бесплатно).
- **Том CA-MTL-3 (100 ГБ, ~$7/мес)** — держим до брифинга Phase 5.
- Мёрдж адаптера в bf16 запрещён до замера (Phase 5, если вообще).

## Приёмка пакета 4.5a (02.08) — принят; формула одобрена

Верификация: ослепление доказано (140 пар сверены с ключом, A/B 68/72,
атрибуции нигде), все 40 контрольных строк — настоящие согласия, наборы
разногласий воспроизведены из дампа+голда поэлементно (35/67/23/11/4),
frozen нетронут, 336 тестов. «37 vs 35» — артефакт счёта строк (перенос
внутри кавычек), записей ровно 35.

Решения тимлида по вопросам исполнителя: (1) **заголовочный потолок ADR —
metric-unit** (пересчёт «идеальной модели» через скорер, сравним с
планками; верхняя граница — помечено), accuracy-unit рядом как
диагностика; (2) **G1e потолок не оценивается** — n=4, смысловых ≤3;
омоглиф-находка уходит в отложенный guard нормализации (чинить сейчас
нельзя — сместило бы будущие G1e против прошлых); (3) до инжеста харнесс
ужесточается: отказ на незнакомых колонках + подложенный негативный тест
ослепления становится постоянным.

Решение оператора: **контроль расширяется точечно** — sentiment и intents
с 8 до 40 строк каждый (+64; шаг полосы ~2.3 пп вместо ~11), T2-головы
остаются на 8. Итоговый пакет арбитража: **244 строки**.

## Арбитраж 4.5a — ЗАВЕРШЁН (02.08, 244/244)

Приключения сохранения (Numbers экспортировал в соседнюю папку с «;»,
сентимент побывал в Корзине пустым и был отсужен заново) закончились
благополучно: тимлид сверил все пять файлов построчно — id и метки
нетронуты, словарь чистый, 244/244 вердиктов. Авторитетный комплект
запинен по sha256 в `data/annotation/audit_45a_returned/`. Вердикты
(без ключа ничего не говорят о том, кто прав): sentiment A19/B16 ·
intents A32/B33/amb2 · slice A3/B20 · posts A7/B8 · control: correct 81 /
incorrect 23.

## Гейт 4.5 — итоги аудита (02.08, вечер): потолки измерены

Харнесс перезапущен тимлидом бит-в-бит, арифметика сверена вручную.
Слепой арбитраж вскрыл (gold-wrong = оператор вслепую выбрал ответ модели):

- **Тональность:** 15 из 35 «ошибок» модели — ошибки голда; контроль 0/40.
  Потолок 0.9613; рука A против исправленного голда ≈ 0.95 — почти у
  потолка. Честных ошибок — 20.
- **Сарказм-холдаут:** 15 из 23 «непочиненных» — ошибки голда; по правде
  рука починила 36/44 при планке ≥27. **Вердикт Phase 4 (2/5) против v2
  НЕ переоткрывается** — v3-числа пойдут отдельной колонкой.
- **Тип поста:** 5 из 11 — голд; потолок 0.9683, рука 0.9386.
- **Интенции — развилка закона:** 22/40 контрольных строк оператор счёл
  неверными, из них 19 — согласованные пустые наборы []. Гайдлайн Phase 2
  сознательно слал сервисные жалобы в [] — сегодняшнее прочтение оператора
  с ним расходится. Потолок интенций не определён до выбора закона.
- **Про цель 0.98:** измерено — идеальная модель на текущем голде даёт
  0.96–0.97. Реалистичная цель: тональность 0.95+ (почти есть), интенции —
  после закона.

Решения гейта: (1) **закон интенций — после очной сверки** (пакет: правила
[] из гайдлайна + 19 строк, готовит исполнитель); (2) **тест v3 одобрен** —
38 точечных исправлений по слепым вердиктам (15+15+5+3), интенции не
трогаются; v2 неизменен навсегда, все дампы перескориваются бесплатно.

## Тест v3 — заморожен и перескорен (02.08, принят тимлидом)

38 слепых присуждений применены (тональность 15 · сарказм-пара 15 · тип
поста 5 · бренды 3; интенции не тронуты — байт-в-байт v2). v2 неизменен,
вердикт Phase 4 против v2 не переоткрывается. Перескор из дампов, $0:

| голова | база | рука A | рука B |
|---|---|---|---|
| G1a | 0.8918→0.9067 | 0.9107→**0.9499** | 0.9172→0.9456 |
| G1d | 0.9084→0.9190 | 0.9386→**0.9705** | 0.9159→0.9357 |
| G1e | 0.8974→0.9383 | 0.9333→**0.9744** | 0.9577→0.9189 |
| G1b (исходный срез 44) | — | 21/44→**36/44=0.8182** | 24/44→29/44 |

(Потолки 0.9613/0.9683 мерились ОБ v2 — против починенного голда потолок
выше, потому 0.9705 > 0.9683 не парадокс.)

Два решения тимлида по вопросам исполнителя:

1. **Асимметрия v3:** правки выведены из зоны разногласий руки A, поэтому
   её v3-оценка полная, а у базы и руки B — нижние границы (их «ошибки»
   могут ещё содержать неаудированный кривой голд). Правило: v3-числа —
   абсолютные оценки каждого прогона; **сравнения рук по v3 запрещены**
   (знак-флип G1e — ровно этот артефакт). Выбор руки закрыт против v2.
2. **Версия голда:** Phase 4 и её планки — v2 навсегда. Программа
   качества с этого момента меряется по v3 (или v4, если закон интенций
   изменится); новые планки программы предрегистрируются на следующем
   гейте. Дефолты eval-скриптов не трогаются до него.

## Гейт закона интенций — РЕШЁН интервью (02.08, амендмент 3.8)

Вместо чтения пакета провели структурированное интервью (10 вопросов по
группам строк). Итоги:

1. **Старый закон устоял там, где его оспаривали:** 17 из 19 спорных []
   подтверждены оператором (эмодзи, благодарности, шутки, розыгрыши) —
   вердикты контроля списаны на артефакт дизайна пакета: интенции
   показывались БЕЗ тональности и сарказма, и «клиент доволен» / «это же
   сарказм» читались как потерянный сигнал, хотя живут в своих полях.
   Недоработка пакета — моя (тимлида), записана. «Кризис 22/40» распался.
2. **Таксономия v2: шестой интент `service`** (продуктовое решение
   оператора) — «взаимодействие с ритейлером как услугой»: обслуживание,
   процесс доставки, приложение/касса, поддержка, персонал, организация
   акций и розыгрышей. Граница ШИРОКАЯ (розыгрыши-недоверие → service,
   осознанный пересмотр ответа из интервью); availability остаётся про
   наличие/доставку ТОВАРА; совместимость (service + продуктовые) разрешена.
3. **Когда:** сейчас, одним заходом с доразметкой корпуса (один конвейер,
   одна калибровка, одно переобучение). Следствия предрегистрированы в
   3.8: гайдлайн v2 → переразметка интенций всей разметки → тест v4 →
   новый zero-shot якорь G1c (промпт растёт на класс) → ретрейн → новые
   планки ДО скоринга.

## ХЕНДОФФ между сессиями тимлида (02.08, вечер)

Сессия тимлида закрыта по длине. **Вся память проекта — на диске, в чате
не живёт ничего:** SPEC rev. 3.8 (все решения = амендменты 3.4–3.8), этот
STATUS (карта, числа, решения, «ты здесь»), knowledge/ (ADR + hot.md),
docs/PROMPT-*.md (все контракты).

Сделано за сессию 01–02.08: **Phase 4 целиком** (брифинг → 4a якорь и срез
→ 4b тренер и смоук → 4c две руки, гейты 2/5, приёмка, закрытие) →
**Phase 4.5**: аудит 244 строк (слепой, с приключениями Numbers) → потолки
→ тест v3 + перескор (рука A: G1a 0.9499 · G1d 0.9705 · G1e 0.9744 · срез
36/44) → закон интенций интервью (амендмент 3.8: 17/19 — артефакт пакета;
таксономия v2 + `service` широкий) → PROMPT-4.5d запущен исполнителем.

**В полёте:** 4.5d в Claude Code (гайдлайн v2, промпт v2 рядом с v1, проба
50 строк ≤$2, счёт кандидатов доразметки, план калибровки).

**Следующей сессии тимлида:** восстановиться из этого STATUS + шапки SPEC
+ hot.md (как делала эта); принять отчёт 4.5d по чек-листу PROMPT-4.5d —
верификация на диске + независимый пересчёт (дисциплина сессии: субагент
на числа, свип владения, make check лично); провести **гейт аппетита**
(три уровня из отчёта: ~+2k/+5k/+9k, решение оператора) → PROMPT-4.5e
(полная переразметка → калибровка оператора ≥90% → тест v4 → свежий
zero-shot якорь G1c промптом v2 → ретрейн → новые планки ДО скоринга).

Бюджеты: RunPod **$18.12** остатка (ретрейну 4.5e хватает с запасом);
OpenRouter **≈$8** (проба ≤$2 внутри). Правила, которые легко забыть:
батч 1 на всех eval; сравнения рук по v3 запрещены; v2-голд и Phase-4
вердикт неизменны; промпт v1 и его SHA неприкосновенны.

## Гейт 4.5d — принят, аппетит решён (02.08, вечер)

4.5d принят тимлидом после верификации на диске: `make check` 398 лично,
ledger $0.0187 до цента (якорь до первого запроса), свип владения чист,
арифметика воронки/тиров/дрейфа пересчитана, оба новых закон-примера
найдены дословно в сыром корпусе. 8 отклонений — ратифицированы.

Что проба показала: полная переразметка всех 3 771 строк = **$0.61**; на
значимых строках 64% меняют набор, 40% несут `service`, 24% — несогласие
модели со старым голдом (решит калибровка ≥90%). Корпус: пригодно
**1 912** строк, сарказм-кандидатов **0** — G1b данными из этого корпуса
больше не улучшить, только новый источник (вопрос брифинга Phase 5).

**Решения гейта (оператор):**

1. **Аппетит: взять все 1 912** — де-факто тир +2k без ожидания (train
   2 346 → ~4 258, +81%). Доразметка стартует ТОЛЬКО после того, как
   переразметка пройдёт калибровку ≥90% — не размечаем 2 часа по ещё не
   валидированному закону.
2. **Три пограничные правки закона v2 ратифицированы** как написаны:
   шашлык `@VARUS_channel:5951` → `price`+`service` (товар был, отказ на
   кассе — это сервис); оба розыгрыша → `service` (буква 3.8).

**Решения тимлида в рамках 3.8:** переразметка = ВСЕ 3 771, включая
`unclear` (+$0.16; иначе в одной колонке две таксономии); файлы не
мутируются — v2-копии рядом (как frozen v2/v3), `annotator` не
переписывается, провенанс миграции в записи прогона; замороженные
тест/холдаут — в шаге v4 (4.5f) со снятием кодового guard.

**Почему дрейф читается по колонке scoreable, n=25 (важно и для чтения
калибровки):** половина пробы — `unclear`-строки (ответы сети,
перепалки), ни один гейт их не скорит, поэтому их дрейф не влияет на
будущие метрики. n=25 НЕ «надёжнее» n=50 — наоборот, меньшая выборка даёт
БОЛЕЕ широкий интервал; просто только эти 25 строк вообще что-то меряют.
На калибровке то же чтение: гейтовая сотня берётся из scoreable-строк,
её точность ±5.9 pp вокруг планки 0.90.

## 4.5e — переразметка выполнена и принята (02.08, ночь)

Принято тимлидом после верификации: `make check` 413 лично, свип
владения чист, staged-подсчёты 1 594+740+915 = 3 249 сошлись, пакет
слепой (в gated.csv нет старой метки, 0 предзаполненных вердиктов,
пересечений со changed.csv — 0), ledger $0.5448 из $1.50 по якорю до
первого запроса, resume-арифметика 66→29→18→14 бьётся. 9 отклонений
ратифицированы (в т.ч. предрегистрация знаменателя — исполнитель закрыл
дыру промпта: пустая ячейка = несогласие).

Числа полного прохода (замена прикидке пробы, 0 новых запросов):
изменилось 45% (scoreable 49%), несут `service` 31% (scoreable 34%),
изменилось БЕЗ service 15%. Главный необъяснённый кусок оттока — 149
строк, бывших `[]`, которым модель дала `availability`; его читает
диагностическая полусотня. 14 строк остались на v1 (модель упорно не
отдаёт поле intents; 13 из 14 были `[]` — не приведены к `[]` намеренно).

**Вопрос 1 отчёта снят кодом:** `train_qlora.py` (стр. 126) отбрасывает
`unclear`-строки — 1 190 переразмеченных unclear не попадают ни в
метрики, ни в обучение; их переразметка — чистая консистентность колонки.

**Отложенные решения (видимые):** 54 замороженных id с двумя домами —
сводит 4.5f при выпуске холдаута v2; 14 нечитаемых строк — микро-пакет
оператору на следующей сессии вердиктов (дефолт тимлида, можно
возразить); скрипт-приёмник вердиктов — первый шаг следующего промпта.

## Вердикты калибровки сданы (02.08, поздний вечер)

Оператор заполнил оба файла (~43 мин по mtime). **changed.csv: 47/50 за
новую метку** — переразметка в основном улучшает; три не-`new` строки
разобраны с оператором, все три — семья «вкус»:

| id | текст | было → стало | вердикт закона |
|---|---|---|---|
| 11972 | «Картопля з печінкою» | taste → [] | **["taste"]** (old прав) |
| 11960 | «З вишнею» | taste → [] | **["taste"]** (old прав) |
| 11902 | «…зʼявився суп том ям» | [] → availability | **["taste"]** (продиктовано) |

**Корень 11972/11960 системный:** переразметчик (и весь конвейер T1) не
видит родительский пост — короткие ответы под постом-опросом теряют
интент. Это объяснимый срез «необъяснённых 15%»; 4.5f меряет его размер
из уже оплаченных строк ($0). Прецедент 11902 (пожелание блюда → taste,
не availability) записан здесь; в гайдлайн — только если класс
повторится (дисциплина закона: правила для повторяющихся случаев).

**gated.csv: 100/100 `correct`.** Чтение тимлида, зафиксированное как
дефолт (вето оператора — до вставки 4.5f): это честный результат более
мягкого инструмента — без старой метки рядом контраст не виден, и все
три реальные ошибки пойманы именно контрастным changed. Гейт официально
посчитает приёмник 4.5f; контр-сигнал (3/50 в changed) встаёт в запись
рядом с числом гейта, не под ним.

## 4.5f — гейт калибровки ПРОЙДЕН, принят (02.08, ночь)

**100/100 = 100% против планки 90%.** Приёмник пересобрал запечатанный
пакет с нуля (seed 42) и потребовал sha-совпадения с манифестом — гейт
посчитан, не прочитан. Контр-сигнал (3/50 в changed) записан рядом с
числом. Три вердикта оператора применены к staged-файлам (диф ровно 3
строки), негативный контроль приёмника работает. 437 тестов, свип чист,
8 отклонений ратифицированы. Переразметка v2 — принята, колонка intents
теперь закон для train.

**Находка 4.5f: 97 «опустошённых» строк** (модель сняла метку в ноль;
медиана длины 18 симв. против 55) = **31% необъяснённого дрейфа на
популяции гейта**. Корень: промпты не видят родительский пост.
Потерянные метки: taste 40, price 29, availability 12, packaging 9,
quality 6+1. Запись: `results/drop_45f.json`.

**Решения гейта 4.5f (оператор):**

1. **Родительский пост добавляется в v2-промпты СЕЙЧАС** — до якоря и
   ретрейна. T1v2 не имеет записанных прогонов, смена бесплатна по
   реестру; новые ревизии регистрируются РЯДОМ, старые SHA неизменны.
   Precheck 1 912, якорь G1c и ретрейн идут уже с постом.
2. **97 строк: модель-с-постом + выборка** — передоразметка (~$0.02),
   контроль: сеяные 40 из 97 контрастно, планка new/40 ≥ 90%
   (предрегистрирована в PROMPT-4.5g); ниже планки — все 97 руками.

Мелкие решения тимлида: hot.md протух → оператор говорит `/save` в
сессии Claude Code; конвенция пакетов остаётся (заполненные уходят через
add -f); в гайдлайн ничего не пишем (97 — дефект конвейера, не закона;
прецедент «том ям» n=1); **54 двойных id — явно в шаг теста v4 (4.5h)**.

## 4.5g принят; квиз по 97 строкам провёл тимлид в чате (03.08)

4.5g принят (482 теста лично, $0.5978 из $1.25, свип чист). Дальше
вместо CSV-разметки 97 строк оператор выбрал квиз в чате. Итог квиза
(20 сеяных строк, планка 18/20 предрегистрирована до первого блока):
**11/20 — планка не взята, автопочинка отклонена.**
`docs/quiz-45g-verdicts.md` — полная таблица.

Что узнали: «вареничная» семья (короткие ответы-начинки) держится —
10/11 подтверждены как taste; ценовые/наличие v1-метки на этом классе
рухнули (0/5 и 0/2) — часть ушла в service (широкая граница v2 дотянулась
до строк, которые модель не трогала), часть в []. **Оговорка: 55% — про
этот патологический класс, не про корпус** (корпусный гейт 100/100
стоит).

**Главное — оператор назвал корневой дефект инструмента:** все строки
класса висят под постами-КАРТИНКАМИ, которые в MVP не собирались. Судить
их вслепую — гадание, и для человека, и для модели (навсегда). Решение:
скачать картинки именно нужных постов (Telegram-сессия есть; уникальных
постов мало — под одним опросом 89 ответов) и дать оператору одну
сессию с картинками рядом. Слепые вердикты по 9 разошедшимся строкам НЕ
применены — они уйдут на пересмотр с картинками. 424 из 1 912
доразмечаемых строк — тот же класс; вопрос «собирать ли медиа вообще»
уходит в брифинг Phase 5.

**Решение оператора (03.08, шире по его инициативе):** пост должен
попадать в модель ВСЕГДА — для текстовых постов уже сделано (гейт 4.5f);
для постов-картинок добавляем **подписи**: vision-модель описывает
картинку (с транскрипцией видимого текста — опросы, цены, даты), подпись
идёт в промпт как `[image description]`. 424 медиа-строки доразметки
перепроверяются с подписями (~$0.1); пакет сессии пересобирается ДО
первого вердикта (пересборка честна и названа в новом манифесте).

## 4.5g2 — принят (03.08). Открытие: опросы всегда были текстом

Принят тимлидом: 520 тестов лично, свип чист, ledger $0.1791 из $0.75,
манифест пересборки несёт `verdicts_present: 0` и supersedes-блок.

**Открытие шага: 16 из 41 «немых» постов — опросы Telegram**, их вопрос
всё это время лежал в API (`message.poll`), а коллектор писал «текста
нет». Транскрипт взят из уже скачанных сообщений (тег `[poll]`, модель
не участвовала); 21 картинка подписана vision-моделью; 4 поста без
суррогата названы поимённо (розыгрыши 1835/1844, видео, голосовое).

**Суррогат работает, и это измерено честно:** из 424 медиа-строк
сдвинулось 153 (опросы 43%, картинки 17%) при **контрольном нуле** на
25 строках, чей промпт остался байт-в-байт прежним → сдвиг — инструмент,
не шум декодирования. Intents: +29 taste, +21 service. Квиз закрыл 22
строки из 97; 75 идут оператору руками — теперь с постом перед глазами.

**Отложено в брифинг Phase 5:** коллектор не читает `message.poll` —
сколько опросов по всему стору записано «без текста»; структура
розыгрышей в API (третий вид суррогата); сбор медиа вообще.
hot.md протухший — чинится Step 0 следующего промпта (`/save`-паттерн).

## Сессия вердиктов пошла КВИЗОМ в чате тимлида (03.08)

Оператор выбрал формат: без CSV, вся сессия — квиз кнопками в чате. По
третьей информированной просьбе оператора зарегистрирован **Amendment 2**
(`docs/quiz-sitting-45g-log.md`, до показа каких-либо счётов): вердикты
ставит LLM-тимлид, оператор судит спорные строки + слепую контрольную
двадцатку НА тимлида (планка 18/20; провал = вердикты тимлида
недействительны, откат к операторскому захвату). **Провенанс
переименован честно: это НЕ операторская калибровка SPEC §8**, а
«LLM-триаж тимлида с операторской адъюдикацией и контролем» — каждая
запись ниже по течению обязана называть её так.

Сделано: **гейтовая часть 300/300 вердиктов в файле** (16 оператора +
триаж тимлида + 11 спорных, решённых оператором паттерн-квизом; одна
самопоправка оператора по 12621). Счёт запечатан в логе до контрольной
двадцатки. **7 правил оператора записаны как закон для guideline v2.1**:
оффтоп-шутки → unclear; канцелярит поддержки без маркеров → unclear
(голос сети); похвала работы сети → ["service"]; мок-цитата → sarcasm
true; кімчі-семья → taste + sarcasm; шахраї-строка скорируема; мем-строка
как в файле.

## Контрольная двадцатка — ПРОЙДЕНА 20/20 (03.08)

Слепой спот-чек оператора НА тимлида: пак seed 42 из confident-correct
вердиктов; рамку и конвенцию сэмплинга запинили в чате ДО генерации;
тимлид до конца счёта не читал ни ключ (precheck300), ни хвост лога, ни
манифесты. **20/20 при планке 18/20 — вердикты тимлида в силе**,
void-фолбэк не сработал. Счёт посчитан парсером из файла захвата, не
глазами. Полный захват: `docs/quiz-sitting-45g-check20.md`; там же одно
отклонение тимлида (перестановка предъявления строк 16/17 в блоке 4,
повердиктный захват не задет). Вопрос оператора на строке 1 («почему не
определены бренды») отвечен: бренды вне четырёх судимых полей пака, их
черёд — тест v4 и якорь (шаг 4.5h).

## Разметка redo-75 + unreadable14 — ГОТОВА (03.08, вечер)

Тимлид разметил с постами и картинками (Amendment 2): **75/75**
intents_final в `emptied_redo.csv` (71 tl-llm + 4 вердикта оператора по
спорным) и **14/14** intents_v2 в `unreadable14.csv` (11×`[]` — голос
сети/cross-commenter/шум, 2×service, 1×packaging). Картинки открывались
для шести строк; два вердикта решены именно картинкой (мочі «4 шт» →
packaging; логотип Zernari → packaging). Все расхождения с v1/моделью
названы в notes построчно. Спорные (4) оператор решил паттерн-квизом:
все четыре → `[]` (cross-commenter, шутка про дом, невосстановимый
референт, рисунки маскота). Раскладка 75: taste 18 · price 18 · `[]` 11 ·
service 9 · packaging 8 · availability 7 · quality 2 · парные 2.

## ТЫ ЗДЕСЬ — промпт приёмника (03.08)

1. ~~Контрольная двадцатка~~ **ПРОЙДЕНА 20/20** (см. выше).
2. ~~redo-75 + unreadable14~~ **ГОТОВО** (см. выше).
3. **Промпт приёмника ГОТОВ: `docs/PROMPT-4.5g3.md`** — вставить в
   Claude Code, отчёт вернуть тимлиду на приёмку. Постратные гейты ≥90%
   (страта ниже планки возвращает весь пласт), guideline v2.1, мёрж по
   гейтам, `/save` в Step 0. Решения брифинга 03.08: (а) 42 incorrect в
   прошедших стратах чинятся построчно по вердиктам (неоднозначные —
   PENDING в отчёт); (б) вторая волна = модель с промптом T1v2.1 (новая
   ревизия РЯДОМ) + свежая запечатанная сотня-гейт той же планки; судит
   её следующая сессия, исполнителю судить запрещено. Арифметика лога:
   258/300 = 86% средняя ⇒ минимум одна страта провалится — вторая
   волна ожидаема, это предрегистрированное правило, не авария.
   Дальше — брифинг 4.5h: тест v4 (guard кодом, 54 двойных id) → якорь
   G1c промптом-с-постом → ретрейн → планки ДО скоринга.
4. Хвост Phase 2 (не блокер): dataset cards публичных датасетов аугментации +
   licence check.

Операторская шпаргалка по командам 3b: `scripts/runbook_3b.md`.
