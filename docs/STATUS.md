# STATUS — market-pulse-llm (05.09.2026) — карта этапа 1 «промо-пульс сетей»: что за чем, где мы, когда конец

> Операторский документ, по-русски; правит только тимлид, после приёмок. Хроника — git этого файла, `docs/reviews/`, ADR.
> Потолок — 60 строк карты (скилл v3 §8); машинный блок в конце не считается и не редактируется.

## Что это за проект (одним абзацем; переспека 27.08, рулинг (х))
Инструмент маркетинговых исследований ТМ «Гармонія» по украинскому Telegram (`docs/PRODUCT.md`). Замер 27.08: бренды
watchlist названы ≈ в 40 комментах за 18 мес., Гармонія — в 0; зато 2 102 промо-поста сетей с ценами и 1 092 коммента под ними.
Поэтому **этап 1 = «промо-пульс сетей»: что и почём сети промоутируют по молочке и мороженому, и как покупатели реагируют
под промо-постами — неделя за неделей**, непрерывной петлёй с расписанием из UI (`docs/SPEC-v2-promo-pulse.md`). GPU арендные.

## План этапа 1 — что за чем идёт (фаза `PHASE-promo-pulse-1` v7, все числа — из result-файлов)
| # | слой | что это для тебя | состояние | держит |
|---|---|---|---|---|
| S0 | источники | 17 каналов сетей собраны, все страницы листовок куплены (3 008 стр., $2.99) | ✅ C2 закрыт; Маркетопт (@marketopt_promo, Кременчук) внутри — 22 позиции; его приватный промо-канал собран 04.09 (33 поста, $0, `449cab1`); страницы, третье окно и строки на экране — сессия «c3» ПОСЛЕ холдаута (рулинг (q)4) | исполнитель (c3, ≤$0.50) |
| S1 | цены | таблица промо-позиций: бренд · товар · объём · промо-цена · −N% | ✅ **1 113 позиций, 12 сетей** в `pulse.db :: w2`; Гармонія — 0 позиций в листовках окна. **Бар не измерен**: нужен эталон тимлида по 46 страницам | **тимлид** (2 сессии, 06–07.09) |
| S2 | реакции | под каждым промо: о чём коммент (сеть / бренд / товар) и что говорит (жалоба · похвала · спрос · привычка · цена) | **Итерация 3 (04.09, $0.49, закон v1.2, транспорт починен): субъект 0.8714 ✅ (бар 0.80, 122/140), сигналы 0.9104 ✅ (бар 0.75)** — `grade_promo_dev40_iter3.json`; чтение полное (40/40, 0 parse failures), взято в 3 из ≤5 ранов; диф против итерации 2 — только 18 строк треда `VARUS:6216`. По стратам: currency 0.787 (48/61), decimal 0.937 (74/79). Промахи (18): «о чём» (post ↔ chain ↔ sku), смысл совпадает в 13 из 18, 3 — ирония. **Осталось: холдаут-40, один выстрел** — эталон тимлида ✅ 05.09 (`docs/labels-promo-holdout.jsonl`, 188 строк, sha `6fa80488…`) → покупка ≤$0.90 | исполнитель (prep $0 → выстрел) |
| S3 | тренды | цена SKU по неделям и сетям, глубина скидок по сети и бренду — SQL | ✅ 14 тестов зелёные, пересчёт дважды идентичен | — |
| S4 | петля | `make tick` по расписанию, экран из result-файлов | ✅ идемпотентна (второй прогон — 0 новых строк); экран рендерится | — |
| гейт | ты | смотришь 20 строк под сидом 42 (`draw_truth_20.py`) и говоришь, что верно/неверно | после S1-бара и холдаута S2 | **оператор** (30 мин, 08.09) |
| дальше | C6 | фронт: петля + расписание в UI; рост источников (`@ON_LINE_MO` после c3, оптовики) — параллельным треком, как только есть список | после гейта / параллельно | слово оператора |

**Где мы сейчас, одной строкой:** dev-бар S2 взят, эталон холдаута готов; критический путь — выстрел холдаута (исполнитель: prep $0 → покупка ≤$0.90) и эталон позиций тимлида (46 страниц, 06–07.09); всё остальное этапа 1 сделано или стоит $0.
**Честно:** закон v1.2 держит один near-quote dev-коммента (`@msuaaaa:3454:1217` — он же в промахах); заменяется ПОСЛЕ холдаута (рулинг (q)2), чтобы холдаут мерил байт-в-байт тот инструмент, что взял dev-бар.

## Ближайшие шаги — по порядку, кто что делает
1. ✅ 04.09: A-rest · Маркетопт собран · B ($0.22, субъект красный по транспорту) · транспорт починен (s16) · регистрация 3 (s17) · тест по записи (s18) · **итерация 3 куплена (s19, $0.49): оба бара держат** — принята (q).
2. Исполнитель, сессия «holdout-prep» ($0, одна): эмиттер и грейдер берут холдаут параметрами (`--part holdout`, `--gold docs/labels-promo-holdout.jsonl`, `--step promo-holdout --cap 0.90`),
   раннбук без `_iter3`, леджер dev-петли закрыт; чек — dry-run печатает 40 тредов, цену и «gold missing → no record», `make check` зелёный. Стандартный промт, без изменений.
3. ✅ Тимлид 05.09: эталон holdout-40 `docs/labels-promo-holdout.jsonl` — вслепую, 188 строк (все комменты с текстом 40 тредов), валидатор 0 ошибок, свежий аудитор по букве кодбука; 2 треда `@matusi_ukr` — вне домена, размечены как есть.
4. Исполнитель, сессия «holdout» (платно, ≤$0.90, ожидаемо $0.37–0.58): регистрация с пином эталона → смок → покупка → K8 → таблица ошибок; зелёный или красный — вопрос S2 закрыт одним
   полным чтением (неполный ран по §6.5 докупается под следующим номером, закон не движется). Потом «c3» (≤$0.50); после него том `mp-srv2` удаляется (слово оператора 05.09).
5. Тимлид (06–07.09): эталон позиций по 46 страницам → K6 → бары S1. 6. Оператор: гейт (08.09) + список оптовиков (или «найди») для параллельного трека.
**Оценка: гейт этапа 1 — 08–09.09.** 05.09 holdout-gold + prep · 06.09 выстрел + c3 + позиции (1/2) · 07.09 позиции (2/2) + K6 · 08.09 clean-clone e2e + `draw_truth_20` + твой гейт. Сдвиг: +1 день на каждый день без сессии тимлида; красный холдаут гейт не сдвигает — он закрывает вопрос S2 числом.

## Деньги (гард после итерации 3: `results/spend_cycle3.json`, `docs/plans/promo-pulse-1.PROGRESS.md`; биллинг постит с опозданием 30–40 мин)
Цикл-3: потрачено $5.03 из $7.00 по дельте баланса, остаток $1.97 (вечер 04.09); том `mp-srv2` капает ≈$0.24/день. Dev-петля ЗАКРЫТА: $1.24 из $2.16 (итерация 1 $0.53 + B $0.22 + итерация 3 $0.49 — этот под был в 1.5–2.3× медленнее пода B на тех же ответах).
Впереди: холдаут ≤$0.90 (кап = жёсткий стоп; прежний забор $0.30 был оценкой по заимствованной ставке) + c3 ≤$0.50 → остаток ≈$0.46 = двое суток капли → том удаляется после c3; C6 грузит модель холодно (+10–15 мин на старт).
Шаг S4 закрыт settlement $2.9867/$3.95. История: фаза 4 $32.47/$33; цикл-2 $17.68/$20; lora-c $1.12; D2 $5.84. `mp-lora-c` удаляется в `phase7-a1`.

## Что доказано и остаётся в продукте
Адаптер 4.5h2 (`results/verdict_45h2.json`): тональность macro-F1 0.9214, сарказм 23/38, аспекты 0.8478, тип поста 0.9586, бренды 0.9610.
Позиции 5c2 (`run_5c2_positions.json`): 145 из 159 стр.; vision 7.872 с/стр. (n=30). Сигналы в брендовой постановке закрыты (х): проход-1 68%,
thinking +0 (`think_zero_shot_table.json`; D1 (инструмент, $0) принят 26.08). Закрытые линии: однопроходный читатель, lora-b, lora-c,
think-zero-shot, харнес v1. Витрина A1–A6 — параллельно, $0, не гейт.

## Живые решения (последние; полные тексты — `docs/reviews/2026-08-30-plan-promo-pulse-1.md`, история — git этого файла)
- (q) 05.09 — итерация 3 ПРИНЯТА (оба бара на полном чтении, свой прогон грейдера + свежий проверяющий); закон заморожен байт-в-байт как куплен (4 пина); холдаут — один выстрел, кап $0.90 по измеренному
  темпу самого медленного пода, раньше c3; near-quote и синхронизация кодбука-документа — после холдаута; том — после последнего платного рана; исполнителю — «holdout-prep» ($0).
- (n)(o)(p) 04.09 — починка транспорта принята (s16); тест, пиннивший популяцию итерации 2, читает регистрацию в обе стороны (s18; PHASE §6.6 v6);
  заимствованная ставка ушла из рунга 0 — собственный измеренный максимум, при непопадании средний угол с капом как жёстким стопом.
- (m) 04.09 — итерация 2 стоит как измерена (субъект красный из-за ОДНОГО ответа-массива, оборванного стоп-правилом); починка = итерация 3; плато нет; нога B закрыта двумя `[]`.
- (l) 04.09 — Маркетопт собран (33 поста, $0); «c3» одной сессией (параметры у C2-продюсеров; свои `results/*_c3.json`; экран — все окна; один id `marketopt_promo`); кап c3 $0.50.
- (k) 04.09 — A-rest ПРИНЯТ целиком; near-quote «Шикарно…» и синхронизация кодбука-документа — (q)2 перенёс их ПОСЛЕ холдаута.
- (i)(j) 04.09 — приватный Маркетопт — третье окно; `@ON_LINE_MO` через боковой файл; оптовики — по списку оператора или «найди»; A(2): `config/chain_aliases.yaml`; 11 дословных dev-цитат ушли из закона.
- (f) 03.09 — итерация 1 прочитана: промахи — атрибуция, не смысл; кодбук v1.1; итерация 2 = метка + id сети + примеры. (e) 03.09 — процесс v3: без `/goal`, стандартный промт, `PROGRESS.md`, рулинг ≤12 строк.
- (я)(a)(b) 03.09 — раскол derived-стора (w1 побайтно, C2 в `data/derived_w2/`), `carrier` в ключе, план S9 принят. (х)(ц)(ч)(ш) 27–30.08 — переспека «промо-пульс», ценз 17 каналов, дро 20/20, бары S1/S2.

## Отложено сознательно (что · развязка)
Проектная цель одной `/goal` поверх `make done` — после гейта C5. Окно-2, мёрдж bf16, 5c3, G1a/G1c — по слову оператора.
`think-adapter` — при +0 ценность упала. Долги исполнителя — в `docs/plans/promo-pulse-1.PROGRESS.md` («named, not built»).

## Уроки (ретро `docs/reviews/2026-09-03-retro-process-v3.md`; разборы остановок `docs/reviews/2026-09-0{4,5}-stop-patterns.md`)
Восемь пауз фазы до 03.09 — восемь правильных отказов, $0 потеряно; корни у тимлида (закон рос и не убывал, деньги по интуиции, план без инструмента). Лекарство — v3: один файл фазы,
один файл прогресса, стандартный промт, рулинг ≤12 строк, диета закона. Сессии/день: 03.09 — 4; 04.09 — 8 (s12–s19), остановок 4: (l)(m)(o) предвидимые (сняты PHASE v5/v6), (q) плановая —
её два вопроса (забор будущего шага — оценка по измеренному темпу; инструмент на холдауте байт-в-байт) сняты PHASE v7 и скиллом team-lead v3.3. Закон исполнителя за два дня не вырос.

<!-- MACHINE-READ BLOCK — quoted verbatim by sealed producers (`scripts/write_lora_c_prereg.py :: quoted()`).
     The diet of 26.08 deleted these and reddened 5 tests. Bytes below are law: never reword, never re-flow.
     This block is NOT map prose and does not count toward the 120-line diet. -->
## Архив рулингов, которые читают запечатанные записи (не редактировать)

- (в) **Затем — (4) LoRA «рассуждение→метка» поверх v2 с (3) синтетикой как армом B** одной регистрации; цель обучения — из (б).
- (к) **РУЛИНГ 22.08 (вечер) — дизайн `lora-c`:** промпт v3 = v2 + поле `rationale` ПЕРЕД меткой, одним рендерингом для обучения и инференса; таблица парно в четыре колонки (база v2 · база v3 · адаптер A · адаптер B) на одном пуле соседей; рационали для обучающих строк пишет Claude Code по кодбуку и метке тимлида, тимлид ревьюит все «наши» + ~40 пограничных; синтетика ~160 (по 40 на класс ошибки) — арм B, тег в provenance, абляция армом A, никогда в холдаут/голд; из обучения и из пула соседей исключены холдаут-100 и ВСЕ строки 16 референсных тредов; **бар на арм (один выстрел): холдаут-100 ≥ 64 И сквозной бар 1 = 5/5 И бар 3 = 0 — все три или КРАСНЫЙ**; отчётно: ячейка «упоминание → категория» на холдауте (18 → ?), dev-200 (fit), 14 золотых (шестой взгляд). Два контракта: `lora-c-prep` ($0, с ревью-гейтом тимлида) → `lora-c-run` (один A6000, кап **$4.00**).
- (н) синтетический запрос рендерится ТЕМ ЖЕ правилом на ТОМ ЖЕ пуле 515 (закон уже запрещает синтетику только как СОСЕДА; запрет двойника действует)
- (н) проход-2 в ране — ТОЛЬКО для армов (2 ноги, не 4): база v2 сквозной уже измерена (r2: 4/5 · 2), база v3 бара не берёт — экономия ~$0.47 по заряду.
- (о) кап lora-c-run **остаётся $4.00** (оба существующих замера s/step влезают по фактической цене; смок решает, рунг проекции убивает честно)
- (о) карта — **RTX PRO 4500, 32 GB, $0.72/ч, EU-RO-1** (A6000 в датацентре тома none; 32 GB — запас против OOM при замороженном micro_batch; фолбэк 4090 преавторизован)
- (о) ложная строка топика в синтетических заголовках заменяется выделенной константой (продовая строка NO_POST_TEXT в синтетике запрещена); конфаунд маркера принят и именован; в ран добавлен отчётный маркер-ценз (~$0.05) — отделяет «не помогло» от «выучил маркер»
- (ф) **(ф) 26.08, вечер — ПРИОРИТЕТ: СЛОЙ СИГНАЛОВ.** Система бесполезна, пока не находит сигналы в комментах и тредах на уровне ручного разбора тимлида; **только локальная Gemma-4-31B, thinking ОБЯЗАТЕЛЬНО**; API-модели отклонены. Находка: `enable_thinking: false` стояло во ВСЕХ запусках проекта — читатель с thinking ON ни разу не измерен; регистрации сами пишут «thinking-ON reader = НОВАЯ регистрация». Решение о дальнейшем пути — на этой таблице. [цитируется `scripts/write_think_zero_shot_prereg.py :: quoted()`; рулинг закрыт (х) 27.08, байты — закон]
