# STATUS — market-pulse-llm (06.09.2026, 16:00) — карта этапа 1 «промо-пульс сетей»: что за чем, где мы, когда конец

> Операторский документ, по-русски; правит только тимлид, после приёмок. Хроника — git этого файла, `docs/reviews/`, ADR.
> Потолок — 60 строк карты (скилл v3 §8); машинный блок в конце не считается и не редактируется.

## Что это за проект (одним абзацем; переспека 27.08, рулинг (х))
Инструмент маркетинговых исследований ТМ «Гармонія» по украинскому Telegram (`docs/PRODUCT.md`). Замер 27.08: бренды
watchlist названы ≈ в 40 комментах за 18 мес., Гармонія — в 0; зато 2 102 промо-поста сетей с ценами и 1 092 коммента под ними.
Поэтому **этап 1 = «промо-пульс сетей»: что и почём сети промоутируют по молочке и мороженому, и как покупатели реагируют
под промо-постами — неделя за неделей**, непрерывной петлёй с расписанием из UI (`docs/SPEC-v2-promo-pulse.md`). GPU арендные.

## План этапа 1 — что за чем идёт (фаза `PHASE-promo-pulse-1` v15, все числа — из result-файлов)
| # | слой | что это для тебя | состояние | держит |
|---|---|---|---|---|
| S0 | источники | 17 каналов сетей собраны, все страницы листовок куплены (3 008 стр., $2.99) | ✅ C2 закрыт; Маркетопт (@marketopt_promo, Кременчук) внутри — 22 позиции; его приватный промо-канал собран 04.09 (33 поста, $0, `449cab1`); страницы, третье окно и строки на экране — сессия «c3» ПОСЛЕ холдаута (рулинг (q)4) | исполнитель (c3, ≤$0.50) |
| S1 | цены | таблица промо-позиций: бренд · товар · объём · промо-цена · −N% | ✅ **1 113 позиций, 12 сетей** в `pulse.db :: w2`; Гармонія — 0 позиций в листовках окна. **Бар не измерен**: нужен эталон тимлида по 46 страницам | **тимлид** (2 сессии, 06–07.09) |
| S2 | реакции | под каждым промо: о чём коммент (сеть / бренд / товар) и что говорит (жалоба · похвала · спрос · привычка · цена) | dev-40 (итерация 3, 04.09): субъект 0.8714 ✅ · сигналы 0.9104 ✅. **Холдаут-40 (05.09, $0.30, полное чтение 40/40): сигналы 0.7958 ✅ (бар 0.75) · субъект 0.7181 ❌ (бар 0.80)** — `grade_promo_holdout40.json`; вопрос закрыт числом: смысл переносится, атрибуция на 0.80 — нет. Из 53 промахов: 21 — внедоменный тред `@matusi_ukr` (канал приостановлен в r2, popул. v1 его не вычла; без него 0.804 — чтение, не бар), 11 — партнёры акций (izibank/OTP/Укрпошта: chain vs brand), 7 — формы слова, 14 — атрибуция в домене. **Итерация 5 (06.09, $0.94, полное чтение 80/80, закон v1.2): dev-40 субъект 0.8857 ✅ · сигналы 0.8667 ✅ — оба бара взяты на пятом, последнем ране; dev-2 (сожжённый холдаут, чтение) 0.8883 / 0.8296** — `grade_promo_dev40_iter5.json`, K8 v2 прогнан мной. Инструмент заморожен как куплен (4 пина `d598573`). Дальше — один выстрел holdout-2 из популяции продукта | **тимлид** (эталон holdout-2 вслепую, 112 комментов) ∥ исполнитель («holdout-2 prep» $0) → выстрел ≤$0.90 |
| S3 | тренды | цена SKU по неделям и сетям, глубина скидок по сети и бренду — SQL | ✅ 14 тестов зелёные, пересчёт дважды идентичен | — |
| S4 | петля | `make tick` по расписанию, экран из result-файлов | ✅ идемпотентна (второй прогон — 0 новых строк); экран рендерится | — |
| гейт | ты | смотришь 20 строк под сидом 42 (`draw_truth_20.py`) и говоришь, что верно/неверно | после S1-бара и холдаута S2 | **оператор** (30 мин, 08.09) |
| дальше | C6 | фронт: петля + расписание в UI; рост источников (`@ON_LINE_MO` после c3, оптовики) — параллельным треком, как только есть список | после гейта / параллельно | слово оператора |

**Где мы сейчас, одной строкой:** **итерация 5 ЗЕЛЁНАЯ** (s31, $0.94; рулинг (z)) — dev-петля закрыта на 5 из 5, закон v1.2 заморожен; критический путь теперь у тимлида: эталон holdout-2 вслепую (112 комментов) — параллельно исполнитель делает «holdout-2 prep» ($0: нога holdout-2 в эмиттере, две починки верификатора на стороне Мака, ранбук выстрела, dry-run ≈ $0.52 при капе $0.90 на темпе 53.6 с/тред) → свежий верификатор → выстрел (07–08.09).
**Честно:** holdout-40 сожжён как бар (закон тюнится на его промахах) — дальше он dev-2, чтение; holdout-2 берётся из популяции ПРОДУКТА (каналы r2), а не из «всех 678» — это сужение заявлено до дро, в файле фазы v9. Near-quote «Шикарно…» уходит из закона вместе с v1.2.

## Ближайшие шаги — по порядку, кто что делает
1. ✅ 05.09: holdout-prep (s20, $0) · рулинг (r) снял стоп s21 · **выстрел холдаута (s22, $0.30): сигналы держат, субъект красный** — принят (s) как измерено.
2. ✅ Тимлид 05.09 12:20: **кодбук v1.2** (`docs/CODEBOOK-promo-signals.md`, §9 — диф конвенций; эталон dev-2 `docs/labels-promo-dev2.jsonl`, 14 строк перечитаны) — партнёры акций → `chain` как в посте, условия совместной акции → сеть-хозяин; внедоменный тред → всё `post`; товар вне поста → украинский именительный типа; товар из поста → строка поста без «ТМ/від», объёма и %;
   пары «спрос+цена» и реплики в споре — синтетические примеры в шаблон; near-quote заменён, документ синхронизирован. Перечитка обоих эталонов по диффу конвенций (holdout-40 → dev-2).
3. ✅ Исполнитель s23 «v1.2-prep» ($0): потолок $9.00 в гарде; **holdout-2 дро** (популяция продукта 398 тредов; 40/40/40, без пересечений); K8 v2 (на замороженном эталоне holdout-40 даёт +1 строку из 188 — послабление известно и мало); закон из v1.2, leak-check чист на 440 комментах. Стоп на регистрации итерации 4: кап $0.60 я назвал без цены инструмента — рулинг (t): среднее ЦЕЛОГО рана самого медленного пода (47.7 с/тред), кап $1.20 (твоё слово 13:05).
4. ✅ s24 ($0): итерация 4 ЗАРЕГИСТРИРОВАНА — заимствование мертво на всех ногах, ставка по целым ранам (17.7 / 47.7 / 25.8 с/тред), mean-угол **$0.87** при капе $1.20, `make check` 4321/0 (рулинг (u)). ✅ s25 ($0): эталон dev-2 запиннен, `--score` по рукам. s26: линия `promo-iter4` (рулинг (v)) → под куплен → **CUDA OOM на самом длинном рендере в смоке** (закон v1.2 длиннее, 24 ГБ не хватило): чтение НЕПОЛНОЕ (§6.5), $0.69, из них $0.62 — ожидание после краха. Рулинг (w): инструмент не движется, меняется сервинг — карта ≥ 32 ГБ, аллокатор, раннер сообщает о падении сразу; **перекупка = итерация 5 (последняя из пяти) на линии `promo-iter5`, кап $1.40** (твоё слово 16:35). ✅ s27–s28 ($0, рулинг (x)): обе линии ЗАКРЫТЫ по run record — `promo-iter4` $0.6948 (2.73 % от пост-рановой ссылки $0.7143), `promo-holdout` $0.2982 (по одному walk, без позднего чтения); (w)3 сделан — раннер пишет ответ-ошибку упавшего юнита, Мак читает её как «смок не вернулся» и удаляет под сразу; отчёт этапа (k) переписан (21 строка). Свежий верификатор ДО покупки нашёл пять скрытых укусов (дефолтная линия эмиттера отказывает; карта — константа; ERROR-строка роняет `--close-segment`/`--score`; рваная строка в `--smoke`; бэкстоп 90 мин из константы) → ✅ s29 06.09 «iteration 5 prep» ($0, рулинг (y)): карта и бэкстоп — поля записи (RTX PRO 4500 32 ГБ, gpu-id из `runpodctl gpu list`, 116 мин из капа), ERROR-строка читается везде, ранбук на итерацию 5, +1 тест в обе стороны; стоп s29 верный и мой (пятый денежный): регистрация ЯКОРИТ линию, а старый якорь пьёт каплю тома в ссылку закрытия (полоса 5 % закрывается за ≈ 4 ч) → регистрация идёт В платной сессии, минуты до create. ✅ s30 ($0): ранбук §0a исправлен. ✅ **s31 06.09 «iteration 5» (платно, $0.9396 за 4698 с на RTX PRO 4500; рулинг (z)): 80/80 ответов, 0 нераспарсенных — субъект 0.8857 · сигналы 0.8667 на dev-40, ОБА БАРА ДЕРЖАТ; dev-2 0.8883 / 0.8296 (чтение); длинный рендер, уронивший итерацию 4, ответил за 314 с — причиной был сервинг, инструмент не двигался.** Линия `promo-iter5` закроется на старте следующей сессии (биллинг отстаёт). → тимлид — эталон holdout-2 (вслепую) ∥ исполнитель — «holdout-2 prep» ($0) → верификатор → выстрел holdout-2 (≤$0.90) → S2 закрыт вторым чтением, зелёным или красным.
5. Тимлид (параллельно, 06–07.09): эталон позиций по 46 страницам → K6 → бары S1. Потом c3 (≤$0.50, после holdout-2), удаление тома, clean-clone e2e + `draw_truth_20` → твой гейт.
**Оценка: гейт этапа 1 — 11–12.09** при эталоне holdout-2 07.09 (тимлид), выстреле 07–08.09, c3 и эталоне позиций 08–10.09. Выстрел holdout-2 — один; красный закрывает вопрос S2 числом, дальше — твоё слово. Риск сняли: темп RTX PRO 4500 измерен — 53.6 с/тред (n=80), в 1.13× медленнее худшего 4090-хоста; выстрел из 43 единиц укладывается в кап $0.90 с запасом 1.7×.

## Деньги (гард после итерации 3: `results/spend_cycle3.json`, `docs/plans/promo-pulse-1.PROGRESS.md`; биллинг постит с опозданием 30–40 мин)
Цикл-3 (гард 12:39Z 06.09, пост-рановое чтение): потрачено **$7.3933**, **потолок $10.00** (твоё слово 06.09 15:55; $9.00 — 05.09; якорь $14.48 не движется; на счёте $7.09; исполнитель ставит `CYCLE3_CAP_USD = 10.00` в гарде на старте следующей сессии) → **REMAINING $2.6067**; том `mp-srv2` капает ≈$0.24/день. Линии ЗАКРЫТЫ по run record: `promo-pulse-1` $2.9867 · `promo-holdout` $0.2982 · `promo-iter4` $0.6948; **`promo-iter5` — под $0.9396 (4698 с), чтение $0.9495 из $1.40, закрытие ждёт биллинг** (walk 1.07 из 4.70 млн мс; повтор на старте следующей сессии, ожидаемое расхождение ≈1 %); `promo-dev-loop` открыта по построению ((r)4), её гард отказывает — линия больше не называется. Впереди: выстрел holdout-2 ≤$0.90 (mean-угол ≈$0.52 на 53.6 с/тред при $0.72/ч, жёсткий стоп 75 мин) → c3 ≤$0.50 → капля тома ~4 дня — всё влезает в $10.00; снова спросят только при шестом чтении. Без пополнения — баланс выше якоря цикла отказывает гарду. Том остаётся до выстрела holdout-2, удаляется после.
Шаг S4 закрыт settlement $2.9867/$3.95. История: фаза 4 $32.47/$33; цикл-2 $17.68/$20; lora-c $1.12; D2 $5.84. `mp-lora-c` удаляется в `phase7-a1`.

## Что доказано и остаётся в продукте
Адаптер 4.5h2 (`results/verdict_45h2.json`): тональность macro-F1 0.9214, сарказм 23/38, аспекты 0.8478, тип поста 0.9586, бренды 0.9610.
Позиции 5c2 (`run_5c2_positions.json`): 145 из 159 стр.; vision 7.872 с/стр. (n=30). Сигналы в брендовой постановке закрыты (х): проход-1 68%,
thinking +0 (`think_zero_shot_table.json`; D1 (инструмент, $0) принят 26.08). Закрытые линии: однопроходный читатель, lora-b, lora-c,
think-zero-shot, харнес v1. Витрина A1–A6 — параллельно, $0, не гейт.

## Живые решения (последние; полные тексты — `docs/reviews/2026-08-30-plan-promo-pulse-1.md`, история — git этого файла)
- (z) 06.09 — **итерация 5 ПРИНЯТА на полном чтении (80/80, 0 нераспарсенных): dev-40 субъект 0.8857 · сигналы 0.8667 — оба бара держат, K8 v2 прогнан мной; dev-2 0.8883 / 0.8296 — чтение; (w) отвечен: причиной был сервинг.** Инструмент заморожен как куплен — 4 пина `d598573` (кодбук `86ed01a4…`, шаблон `32fa6c42…`, `promo_prompts.py da60348d…`, раннер `87072444…`); пин, сдвинувшийся до выстрела, — стоп. Деньги: под $0.9396, чтение $0.9495 из $1.40, цикл $7.3933 из $9.00; закрытие линии — на старте следующей сессии (биллинг). Дальше параллельно: тимлид — эталон holdout-2 вслепую; исполнитель — «holdout-2 prep» ($0: нога holdout-2 как part эмиттера, два риска верификатора починены на стороне Мака, ранбук выстрела, dry-run ≈ $0.52 при капе $0.90; заканчивается на dry-run — без регистрации) → верификатор → моё чтение → выстрел. **Потолок цикла-3 — $10.00 (твоё слово 15:55)**: выстрел + c3 + капля влезают. Класс: плановое чтение, предвидимого нет.
- (y) 06.09 — s29 «iteration 5 prep» принят ($0): карта/бэкстоп — поля записи, ERROR-строка читается везде, ранбук на итерацию 5; стоп верный и мой (пятый денежный): `--register` читает гард с `--step` и тем САМЫМ якорит линию, а старый якорь пьёт каплю тома в ссылку закрытия (полоса 5 % закрывается за ≈ 4 ч) → регистрация идёт в платной сессии, минуты до create ((x)2 отозван); моё чтение до покупки = dry-run + HEAD + листинг + свежий верификатор (1 укус: `--pack` до коммита в §0a ранбука; 2 риска названы, не построены). PHASE v14 §6.1, PROCESS «Деньги» v2.1: стоящий закон бьёт рулинг по механике — исполнитель идёт по PROCESS, называет противоречие, без остановки.
- (x) 05.09 — s28 принят ($0): обе линии закрыты, (w)3 сделан, отчёт (k) переписан; верификатор до покупки — пять скрытых укусов → «iteration 5 prep» одним $0-айтемом (карта и цена — параметры записи из `runpodctl gpu list`, RTX PRO 4500 → A6000/L40S; ERROR-строка читается везде; бэкстоп = минуты капа; ранбук на итерацию 5), покупка — следующей сессией после моего чтения записи; фит-проба за $0 — долг, не гейт (смок с (w)3 и есть проба ценой минут).
- (w)+ 05.09 — s27 принят ($0): (w)1 приказал закрытие `promo-iter4`, у которого не было ссылки — единственная запись линии была ДО пода ($0.0097 → 7000 % off, отказ навсегда); пост-рановое чтение $0.7143 принято как ссылка (дрейф ≈3.2 % < 5 %), закрытие — в ритуале старта, когда биллинг дойдёт. PHASE v13 §6.1: ссылка гейта = `--note` после пода и ДО любого следующего; линия без ссылки закрывается по одному walk и позднего чтения не берёт (`promo-holdout` — сегодня, с `--until`). Четвёртая денежная за день — моя.
- (w) 05.09 — итерация 4 неполная (OOM на 24 ГБ при законе v1.2): инструмент не движется, сервинг — да (карта ≥ 32 ГБ в EU-RO-1 до $0.90/ч, `expandable_segments`, ответ-ошибка юнита); итерация 5 — последняя из пяти, линия `promo-iter5`, кап $1.40 (оператор).
- (v) 05.09 — стоп s26 верный и мой (третий денежный за день): платный ран покупается под СВОЕЙ линией шага (`promo-iter<N>`, `promo-holdout2`, `promo-c3`), открытая линия `promo-dev-loop` нового рана не несёт; регистрация читает гард с `--step`; кап $1.20 не движется.
- (u) 05.09 — s24 принят ($0): итерация 4 зарегистрирована на mean-угле $0.87 (кап $1.20); перед подом — пин эталона dev-2 в регистрацию и `--score` по рукам (dev-40 бар / dev-2 чтение), потом покупка.
- (t) 05.09 — s23 принят; стоп на регистрации итерации 4 верный и мой (кап назван числом, заимствованная ставка жила на dev-ноге): на любой ноге — mean-угол по среднему целого рана самого медленного пода + жёсткий стоп, полосный гейт не запускается; кап $1.20 (оператор).
- (s) 05.09 — холдаут прочитан (сигналы 0.7958 ✅, субъект 0.7181 ❌, чтение полное) и принят как измерено; слово оператора: кодбук v1.2 сейчас, dev-2 = сожжённый холдаут, holdout-2 из популяции продукта, K8 v2 по идентичности товара; c3 после; пополнение до итерации 4.
- (r) 05.09 — s20 принят ($0); стоп s21 верный и предвидимый (q)5: гейт денег холдаута = FITS рунга 0 + жёсткий стоп, таблица решений ветвится по ноге; леджер dev-петли открыт по построению; кап $1.10 (оператор).
- (q) 05.09 — итерация 3 ПРИНЯТА (оба бара на полном чтении); закон заморожен байт-в-байт как куплен (4 пина); холдаут — один выстрел раньше c3; near-quote и синхронизация кодбука-документа — после холдаута; том — после последнего платного рана.
- (n)(o)(p) 04.09 — починка транспорта принята (s16); тест, пиннивший популяцию итерации 2, читает регистрацию в обе стороны (s18; PHASE §6.6 v6);
  заимствованная ставка ушла из рунга 0 — собственный измеренный максимум, при непопадании средний угол с капом как жёстким стопом.
- (f) 03.09 — итерация 1 прочитана: промахи — атрибуция, не смысл; кодбук v1.1; итерация 2 = метка + id сети + примеры. (e) 03.09 — процесс v3: без `/goal`, стандартный промт, `PROGRESS.md`, рулинг ≤12 строк.
- (я)(a)(b) 03.09 — раскол derived-стора (w1 побайтно, C2 в `data/derived_w2/`), `carrier` в ключе, план S9 принят. (х)(ц)(ч)(ш) 27–30.08 — переспека «промо-пульс», ценз 17 каналов, дро 20/20, бары S1/S2.

## Отложено сознательно (что · развязка)
Проектная цель одной `/goal` поверх `make done` — после гейта C5. Окно-2, мёрдж bf16, 5c3, G1a/G1c — по слову оператора.
`think-adapter` — при +0 ценность упала. Долги исполнителя — в `docs/plans/promo-pulse-1.PROGRESS.md` («named, not built»).

## Уроки (ретро `docs/reviews/2026-09-03-retro-process-v3.md`; разборы остановок `docs/reviews/2026-09-0{4,5,6}-stop-patterns.md`)
Восемь пауз фазы до 03.09 — восемь правильных отказов, $0 потеряно; корни у тимлида. Лекарство — v3: один файл фазы, один файл прогресса, стандартный промт, рулинг ≤12 строк, диета закона.
Сессии/день: 04.09 — 8, остановок 4 ((l)(m)(o) предвидимые → PHASE v5/v6; (q) плановая → PHASE v7); 05.09 — 8 сессий, 5 остановок (s28 — без остановки; верификатор до покупки — 5 укусов): 4 предвидимые тимлидом (все деньги), 1 сервинг (OOM) ((r) поля записи по ноге → v8; (t) кап без цены, заимствование → v10; (v) линия шага на ран → v11; (w)+ закрытие без ссылки → v13), 2 плановых чтения.
06.09 — 3 сессии (s29 prep, s30 ранбук, s31 платная итерация 5 — ЗЕЛЁНАЯ), 1 остановка (s29: предвидимая, тимлида, деньги/процесс — (y) регистрация якорит линию → PHASE v14, PROCESS v2.1), 1 плановое чтение ((z)); верификатор до покупки — 1 укус (снят), 2 риска (один выстрелил как предсказано, гейтов не трогает). Разбор: `docs/reviews/2026-09-06-stop-patterns.md`.
Диета: раздел «Деньги» в шаблоне файла фазы (brain-init) с СЕМЬЮ дефолтами (линия на ран · ставка целого рана · кап из dry-run · гард с `--step` · пост-рановое чтение как ссылка закрытия · карта/цена — поля записи · регистрация открывает линию в платной сессии), не новые правила по одному. Закон исполнителя: +1 тест (дефект §4), +0 гардов.

<!-- MACHINE-READ BLOCK — quoted verbatim by sealed producers (`scripts/write_lora_c_prereg.py :: quoted()`).
     The diet of 26.08 deleted these and reddened 5 tests. Bytes below are law: never reword, never re-flow.
     This block is NOT map prose and does not count toward the 120-line diet. -->
## Архив рулингов, которые читают запечатанные записи (не редактировать)

- (в) **Затем — (4) LoRA «рассуждение→метка» поверх v2 с (3) синтетикой как армом B** одной регистрации; цель обучения — из (б).
- (к) **РУЛИНГ 22.08 (вечер) — дизайн `lora-c`:** промпт v3 = v2 + поле `rationale` ПЕРЕД меткой, одним рендерингом для обучения и инференса; таблица парно в четыре колонки (база v2 · база v3 · адаптер A · адаптер B) на одном пуле соседей; рационали для обучающих строк пишет Claude Code по кодбуку и метке тимлида, тимлид ревьюит все «наши» + ~40 пограничных; синтетика ~160 (по 40 на класс ошибки) — арм B, тег в provenance, абляция армом A, никогда в холдаут/голд; из обучения и из пула соседей исключены холдаут-100 и ВСЕ строки 16 референсных тредов; **бар на арм (один выстрел): холдаут-100 ≥ 64 И сквозной бар 1 = 5/5 И бар 3 = 0 — все три или КРАСНЫЙ**; отчётно: ячейка «упоминание → категория» на холдауте (18 → ?), dev-200 (fit), 14 золотых (шестой взгляд). Два контракта: `lora-c-prep` ($0, с ревью-гейтом тимлида) → `lora-c-run` (один A6000, кап **$4.00**).
- (н) синтетический запрос рендерится ТЕМ ЖЕ правилом на ТОМ ЖЕ пуле 515 (закон уже запрещает синтетику только как СОСЕДА; запрет двойника действует)
- (н) проход-2 в ране — ТОЛЬКО для армов (2 ноги, не 4): база v2 сквозной уже измерена (r2: 4/5 · 2), база v3 бара не берёт — экономия ~$0.47 по заряду.
- (о) кап lora-c-run **остаётся $4.00** (оба существующих замера s/step влезают по фактической цене; смок решает, рунг проекции убивает честно)
- (о) карта — **RTX PRO 4500, 32 GB, $0.72/ч, EU-RO-1** (A6000 в датацентре тома none; 32 GB — запас против OOM при замороженном micro_batch; фолбэк 4090 преавторизован)
- (о) ложная строка топика в синтетических заголовках заменяется выделенной константой (продовая строка NO_POST_TEXT в синтетике запрещена); конфаунд маркера принят и именован; в ран добавлен отчётный маркер-ценз (~$0.05) — отделяет «не помогло» от «выучил маркер»
- (ф) **(ф) 26.08, вечер — ПРИОРИТЕТ: СЛОЙ СИГНАЛОВ.** Система бесполезна, пока не находит сигналы в комментах и тредах на уровне ручного разбора тимлида; **только локальная Gemma-4-31B, thinking ОБЯЗАТЕЛЬНО**; API-модели отклонены. Находка: `enable_thinking: false` стояло во ВСЕХ запусках проекта — читатель с thinking ON ни разу не измерен; регистрации сами пишут «thinking-ON reader = НОВАЯ регистрация». Решение о дальнейшем пути — на этой таблице. [цитируется `scripts/write_think_zero_shot_prereg.py :: quoted()`; рулинг закрыт (х) 27.08, байты — закон]
