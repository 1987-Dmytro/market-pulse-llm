# STATUS — market-pulse-llm (09.09.2026, 18:00) — карта этапа 1 «промо-пульс сетей»: что за чем, где мы, когда конец

> Операторский документ, по-русски; правит только тимлид, после приёмок. Хроника — git этого файла, `docs/reviews/`, ADR.
> Потолок — 60 строк карты (скилл v3 §8); машинный блок в конце не считается и не редактируется.

## Что это за проект (одним абзацем; переспека 27.08, рулинг (х))
Инструмент маркетинговых исследований ТМ «Гармонія» по украинскому Telegram (`docs/PRODUCT.md`). Замер 27.08: бренды
watchlist названы ≈ в 40 комментах за 18 мес., Гармонія — в 0; зато 2 102 промо-поста сетей с ценами и 1 092 коммента под ними.
Поэтому **этап 1 = «промо-пульс сетей»: что и почём сети промоутируют по молочке и мороженому, и как покупатели реагируют
под промо-постами — неделя за неделей**, непрерывной петлёй с расписанием из UI (`docs/SPEC-v2-promo-pulse.md`). GPU арендные.

## План этапа 1 — что за чем идёт (фаза `PHASE-promo-pulse-1` v26, все числа — из result-файлов)
| # | слой | что это для тебя | состояние | держит |
|---|---|---|---|---|
| S0 | источники | 17 каналов сетей собраны, все страницы листовок куплены (3 008 стр., $2.99) | ✅ C2 закрыт; Маркетопт (@marketopt_promo, Кременчук) внутри — 22 позиции; его приватный промо-канал собран 04.09 (33 поста, $0, `449cab1`); страницы, третье окно и строки на экране — сессия «c3» ПОСЛЕ холдаута (рулинг (q)4). ✅ **s38 «c3-prep» принят (gg, 09.09): нога c3 собрана и оценена за $0 — 30 страниц + 8 постов, dry run $0.1864 при капе $0.50 (−62.7 %), мой dry run — те же числа; ранбук c3 с гейтами §0.** ✅ **s39 «c3-prep-2» принят (hh, 09.09 15:30): поле режима доказано на свежем старте; allow-правила сняты; запись проекции честна. Верификатор до покупки: NOT SAFE при капе $0.50 (внутренний гейт пачки) → твоё слово: кап $0.80** | исполнитель («c3-prep-3» $0 → c3 ≤ $0.80) |
| S1 | цены | таблица промо-позиций: бренд · товар · объём · промо-цена · −N% | ✅ **1 113 позиций, 12 сетей** в `pulse.db :: w2`; Гармонія — 0 позиций в листовках окна. **Бар не измерен**: нужен эталон тимлида по 46 страницам | **тимлид** (2 сессии, 08–10.09) |
| S2 | реакции | под каждым промо: о чём коммент (сеть / бренд / товар) и что говорит (жалоба · похвала · спрос · привычка · цена) | **dev-40 — итерация 5 (06.09, $0.94, 80/80, закон v1.2, инструмент заморожен 4 пинами `d598573`): субъект 0.8857 ✅ · сигналы 0.8667 ✅** — `grade_promo_dev40_iter5.json`; dev-2 (сожжённый holdout-40, чтение) 0.8883 / 0.8296. Холдауты на эталонах тимлида вслепую: holdout-40 (05.09, $0.30) сигналы 0.7958 ✅ · субъект 0.7181 ❌ — `grade_promo_holdout40.json`; **holdout-2 (s33 06.09, $0.42, 40/40, популяция продукта, эталон 112 строк `5525ddf1…`): сигналы 0.8021 ✅ (бар 0.75) · субъект 0.7054 ❌ (бар 0.80, 79/112)** — `grade_promo_holdout2.json`; с P1 (детерминированный слой, 3 конвенции, s34 $0) субъект **0.7500** (84/112) — `grade_promo_p1_readings.json`; 16 из 28 промахов — ничьи эталона (`unsure`, любая ось; правило файла). Второй холдаут подряд ≈ 0.71: dev-бар не переносится. **Твоё слово (cc): S2 ЗАКРЫТ как измерено**; дро holdout-3 — в резерве. ✅ **s35 «p1-ship» принят (dd, 08.09): P1 в `make tick`, блок S2 на экране и в README из трёх result-файлов.** ✅ **s36 «s2-loop» принят (ee, 08.09 вечер): число ПРОДУКТА — его же функциями, `grade_promo_loop_readings.json`: holdout-2 субъект **0.7411** (83/112) ❌ · сигналы **0.7937** ✅ (dev-40 / dev-2 без изменений), печатается первым на экране и в README; замеры бара рядом, не переписаны** | закрыт; «chain-fold» ($0, после c3) — один Маркетопт на экране |
| S3 | тренды | цена SKU по неделям и сетям, глубина скидок по сети и бренду — SQL | ✅ 14 тестов зелёные, пересчёт дважды идентичен | — |
| S4 | петля | `make tick` по расписанию, экран из result-файлов | ✅ идемпотентна (второй прогон — 0 новых строк); экран рендерится | — |
| гейт | ты | смотришь 20 строк под сидом 42 (`draw_truth_20.py`) и говоришь, что верно/неверно | после S1-бара и холдаута S2 | **оператор** (30 мин, 12–13.09) |
| дальше | C6 | фронт: петля + расписание в UI; рост источников (`@ON_LINE_MO` после c3, оптовики) — параллельным треком, как только есть список | после гейта / параллельно | слово оператора |

**Где мы сейчас, одной строкой:** **S2 закрыт как измерено (cc); s36 «s2-loop» ПРИНЯТ (ee, 08.09 вечер) на моих прогонах: число продукта 0.7411 / 0.7937 — из файла, первым на экране и в README; замеры бара рядом.** Вечером — ревизия инструментария по докам Claude Code (`docs/reviews/2026-09-08-tooling-audit.md`): харнес исполнителя получает ПОЛЯ вместо ритуалов (строка запуска, усилие, два allow-правила, таймауты хуков в секундах, один хук старта) — ✅ **s37 «harness-fields» принят (ff, 09.09): живой `.claude/settings.json` = выпущенный байт в байт; поля effort/ultracode/allow живые.** Девиация s37 (сессия шла в auto-режиме, флаг не действовал) закрыта твоим словом: режим bypass — дефолт в твоём `~/.claude/settings.json`, флаг больше не нужен; проверка режима — штамп в `.claude/session_mode` + гейт §0 ранбука c3. ✅ **s38 «c3-prep» принят (gg, 09.09 14:00) на моих прогонах: нога c3 собрана и оценена за $0 (30 страниц + 8 постов, dry run $0.1864 при капе $0.50 — мой dry run те же числа; мой гард 13:32Z: REMAINING $1.4859). Стоп исполнителя (l)3 «один chain id» — верный: предпосылка моего рулинга (l) не была измерена (ни один канал Маркетопта сегодня не сворачивается в `marketopt_promo`), это правка измеренного слоя → отдельный $0-шаг «chain-fold» ПОСЛЕ c3, число продукта перемеряется. Два allow-правила сняты (в bypass не действуют — доки), третий гейт ранбука с ними; отказы `cp`/`sed` в s37/s38 — это deny-правила, доходящие до Bash-команд (доки), а не классификатор; харнес кладётся инструментом Write, `diff` — доказательство. Замечание: «c3-prep» шла ПРОДОЛЖЕНИЕМ процесса s37, не свежим запуском.** ✅ **s39 «c3-prep-2» принят (hh, 09.09 15:30): свежий запуск — в шапке «bypass permissions on», один хук старта, штамп `bypassPermissions` живым хуком — поле режима ДОКАЗАНО; allow-правила сняты; проекция называет то, что читала; мой dry run те же числа, мой `make check` 4326/2. Свежий верификатор до покупки (39 чтений, 89 тестов): NOT SAFE — при капе $0.50 внутренний гейт эмиттера (резерв на зависший джоб $0.28 + хвост + пачка, считанная по времени, а не по 30 страницам) закончил бы ногу, не купив ни страницы; плюс: запуск `| tee` в переднем плане (в C2 харнес убивал его дважды), у `--close` нет `--tolerance`, нет строки с ключом API, том удалялся до закрытия линии, регистрация называет файлы C2. Твоё слово 15:20: кап **$0.80** (расход тот же ≈ $0.19, худший случай ≈ $0.40).** ✅ **s40 «c3-prep-3» принят (ii, 09.09 16:50): §0 ранбука с моего прогона — `CAP 0.80`, пол $0.15 отказывает; регистрация называет файлы c3; dry run те же числа. Узкий верификатор — снова NOT SAFE, два детерминированных стопа в платной сессии, оба — строки ранбука: ключ API экспортирован в одном вызове Bash, а запуск — в другом (между вызовами среда не живёт); закрытие линии с `--expect-ms` по «wall» записи отказало бы по построению — serverless-биллинг считает время жизни воркера, C2 закрывалась без него. Плюс `--note` сразу после teardown (полоса 5 % закрывается через ≈ 1 ч), `python3.11` в строках гарда, `cap_from` из флага. → «c3-prep-4» ($0, ≈ 20 мин): ранбук репетируется КАЖДОЙ строкой в тех же границах вызовов, что и платная сессия.** **Сейчас: «c3-prep-4» ПРИНЯТ (jj, 18:00) на моих прогонах — `python3.11` везде, dry run $0.1864 FITS −76.7% при $0.80, `cap_from`/`go_no_go` из ноги, закрытие без `--expect-ms`, девиация `unauthorised-create` реверснута за $0 (мои листинги чисты, REMAINING $1.4470 = чтение до create), `make check` 4326/2; ТРЕТИЙ проход верификатора — SAFE TO BUY, только fail-safe ниты → c3 GO** (платно, кап $0.80, свежий процесс; том — после закрытия линии) → «chain-fold» + «launch-field» ($0) → e2e → твой гейт; тимлид — эталон позиций 10–11.09 → K6 → бары S1.
**Честно, по-простому:** на знакомых 40 тредах (dev-40, пять подкруток промта) субъект 0.8857 — это экзамен по выученным билетам; на незнакомых тредах система угадывает «о чём коммент» в ≈ 71 % случаев (два холдаута: 0.7181 и 0.7054), с исправителем P1 — 75 %; «что говорит» — 80 % (сигналы держат планку дважды). Половина оставшихся промахов — «монеты» самого кодбука (нет в наличии — сеть или товар; опечатка в посте своей сети — пост или сеть), где оба ответа приемлемы для маркетолога; поэтому правилами до 0.80 не дотянуть, а перекраивать кодбук и заново тюнить промт — новый денежный цикл. Уточнение 08.09: 75 % / 80 % — это ответы модели «как есть»; продукт перед показом выкидывает строки, где цитата не совпадает с комментом буква в букву (3 из ≈130 на holdout-2), и его собственные числа чуть ниже — ≈ 74 % / 79 %; вердикт тот же, на экране первым будет стоять число продукта. Отгружаем с честными числами.

## Ближайшие шаги — по порядку, кто что делает
1–3. ✅ 05.09: holdout-prep (s20) · **выстрел holdout-40 (s22, $0.30): сигналы держат, субъект красный** — принят (s) как измерено · тимлид: **кодбук v1.2** (`docs/CODEBOOK-promo-signals.md` §9 — диф конвенций; эталон dev-2 перечитан) · s23 «v1.2-prep» ($0): **holdout-2 дро** из популяции продукта (398 тредов; 40/40/40, без пересечений), K8 v2, leak-check чист; стоп на регистрации итерации 4 — рулинг (t): ставка = среднее ЦЕЛОГО рана самого медленного пода, кап $1.20 (твоё слово 13:05).
4. ✅ s24 ($0): итерация 4 ЗАРЕГИСТРИРОВАНА — заимствование мертво на всех ногах, ставка по целым ранам (17.7 / 47.7 / 25.8 с/тред), mean-угол **$0.87** при капе $1.20, `make check` 4321/0 (рулинг (u)). ✅ s25 ($0): эталон dev-2 запиннен, `--score` по рукам. s26: линия `promo-iter4` (рулинг (v)) → под куплен → **CUDA OOM на самом длинном рендере в смоке** (закон v1.2 длиннее, 24 ГБ не хватило): чтение НЕПОЛНОЕ (§6.5), $0.69, из них $0.62 — ожидание после краха. Рулинг (w): инструмент не движется, меняется сервинг — карта ≥ 32 ГБ, аллокатор, раннер сообщает о падении сразу; **перекупка = итерация 5 (последняя из пяти) на линии `promo-iter5`, кап $1.40** (твоё слово 16:35). ✅ s27–s28 ($0, рулинг (x)): обе линии ЗАКРЫТЫ по run record — `promo-iter4` $0.6948 (2.73 % от пост-рановой ссылки $0.7143), `promo-holdout` $0.2982 (по одному walk, без позднего чтения); (w)3 сделан — раннер пишет ответ-ошибку упавшего юнита, Мак читает её как «смок не вернулся» и удаляет под сразу; отчёт этапа (k) переписан (21 строка). Свежий верификатор ДО покупки нашёл пять скрытых укусов (дефолтная линия эмиттера отказывает; карта — константа; ERROR-строка роняет `--close-segment`/`--score`; рваная строка в `--smoke`; бэкстоп 90 мин из константы) → ✅ s29 06.09 «iteration 5 prep» ($0, рулинг (y)): карта и бэкстоп — поля записи (RTX PRO 4500 32 ГБ, gpu-id из `runpodctl gpu list`, 116 мин из капа), ERROR-строка читается везде, ранбук на итерацию 5, +1 тест в обе стороны; стоп s29 верный и мой (пятый денежный): регистрация ЯКОРИТ линию, а старый якорь пьёт каплю тома в ссылку закрытия (полоса 5 % закрывается за ≈ 4 ч) → регистрация идёт В платной сессии, минуты до create. ✅ s30 ($0): ранбук §0a исправлен. ✅ **s31 06.09 «iteration 5» (платно, $0.9396 за 4698 с на RTX PRO 4500; рулинг (z)): 80/80 ответов, 0 нераспарсенных — субъект 0.8857 · сигналы 0.8667 на dev-40, ОБА БАРА ДЕРЖАТ; dev-2 0.8883 / 0.8296 (чтение); длинный рендер, уронивший итерацию 4, ответил за 314 с — причиной был сервинг, инструмент не двигался.** Линия `promo-iter5` закроется на старте следующей сессии (биллинг отстаёт). ✅ **Тимлид 06.09 17:00: эталон holdout-2 вслепую — 112 строк, 40 тредов, sha `5525ddf1…`, валидатор 0 ошибок** ((z) addendum 2). ✅ **s32 06.09 «holdout-2 prep» ($0, рулинг (aa)): `promo-iter5` ЗАКРЫТА $0.9450 (0,47 % от пост-ранового чтения), потолок $10.00 в гарде, эталон закоммичен по пути, нога holdout-2 в эмиттере (каждое поле записи — по ноге, `--cap` обязателен), две починки (y)4 внутри одного теста в обе стороны, ранбук перенацелен, dry-run: 40 тредов, самый длинный рендер 10 529 знаков, mean $0.5170 / dear $2.81 при $0.72/ч → FITS на среднем, жёсткий стоп 75 мин; мой dry-run — байт-в-байт, мой rung 0 — те же числа; свежий верификатор (32 чтения, 20 тестов) — SAFE TO BUY: 1 риск (повторная регистрация на якорённой линии отказывает после первой копейки → новая линия `-r2`) + 2 нита (пин-чек печатает, не останавливает; на ровном капе `--note` отказывает) = ≤3 строки ранбука в начале платной сессии.** ✅ **s33 06.09 «holdout-2» (платно, $0.4232 за 2116 с на RTX PRO 4500 — 40.87 с/тред, n=40; рулинг (bb)): 40/40, 0 нераспарсенных — сигналы 0.8021 ✅ · субъект 0.7054 ❌; создание пода дважды отклонял классификатор харнеса — снято твоим «запускай» ($0). Твоё слово (bb): P1 + holdout-3.** ✅ **s34 «p1-prep» ($0, рулинг (cc)): `promo-holdout2` ЗАКРЫТА $0.4253; дро holdout-3 (358 → 20/20, вслепую, `76435461…`); P1 + один тест; таблица за $0: dev-40 0.8857 =, dev-2 0.8883 → 0.9043, dev-3 0.7054 → 0.7500 — гейт покупки не пройден, развилка вернулась к тебе за $0. Твоё слово (cc) 21:45: отгрузить как измерено.** ✅ **s35 06.09 «p1-ship» ($0, рулинг (dd) 08.09): P1 в `make tick` над каждой строкой ридера до id (K10 держит), блок S2 на экране и в README из трёх result-файлов с отказом по имени, счётчик ничьих — в записи (16/28); стоп-развилка «петля кормит P1 не тем, чем замер» — верная, решена (c): число продукта его же функциями за $0.** → «s2-loop» ($0) → «c3-prep» ($0) → c3 (≤ $0.50) → том → e2e → гейт.
5. Тимлид (параллельно, 10–11.09 10:00–12:00, в календаре — слот 09.09 ушёл на s37/s38): эталон позиций по 46 страницам → K6 → бары S1. Исполнитель: ✅ «s2-loop» (08.09) → ✅ «harness-fields» (s37, 09.09) → ✅ «c3-prep» (s38, 09.09) → ✅ «c3-prep-2» (s39, 09.09) → ✅ «c3-prep-3» (s40, 09.09) → «c3-prep-4» ($0, 09.09, ≈ 20 мин: ключ API в том же вызове, что и запуск; закрытие по образцу C2; репетиция каждой строки) → третий проход верификатора (§3/§5) → c3 (кап $0.80, 09.09 ≈ 17:30 или 10.09 10:00; том удаляется ПОСЛЕ закрытия линии) → «chain-fold» + «launch-field» ($0) → clean-clone e2e + `draw_truth_20` (11.09) → твой гейт.
**Оценка: гейт этапа 1 — 12–13.09** (в календаре 12.09 10:00) при «c3-prep-4» 09.09, c3 09–10.09, эталоне позиций 10–11.09 (тимлид), «chain-fold» + «launch-field» + e2e 11.09. Платных шагов впереди один — c3, кап $0.80 (ожидаемо ≈ $0.19); holdout-3 не покупается.

## Деньги (гард после итерации 3: `results/spend_cycle3.json`, `docs/plans/promo-pulse-1.PROGRESS.md`; биллинг постит с опозданием 30–40 мин)
Цикл-3 (гард 12:39Z 06.09, пост-рановое чтение): потрачено **$7.3933**, **потолок $10.00** (твоё слово 06.09 15:55; $9.00 — 05.09; якорь $14.48 не движется; на счёте $7.09; исполнитель ставит `CYCLE3_CAP_USD = 10.00` в гарде на старте следующей сессии) → **REMAINING $2.6067**; том `mp-srv2` капает ≈$0.24/день. Линии ЗАКРЫТЫ по run record: `promo-pulse-1` $2.9867 · `promo-holdout` $0.2982 · `promo-iter4` $0.6948; **`promo-iter5` ЗАКРЫТА 06.09 14:21Z: $0.9450 по биллингу (walk 4,70 из 4,70 млн мс, 0,47 % от пост-ранового чтения $0.9495)** — цикл-3 после закрытия $7.4277; **`promo-holdout2` — под $0.4232 (2116 с), пост-рановое чтение $0.4107, закрытие ждёт биллинг (walk 0 из 2.12 млн мс; повтор на старте следующей сессии); цикл-3 $7.8480 из $10.00, REMAINING $2.1520** (17:21Z); `promo-dev-loop` открыта по построению ((r)4), её гард отказывает — линия больше не называется. Линия `promo-holdout2` ЗАКРЫТА $0.4253 (3.6 % от пост-ранового чтения); цикл-3 **$7.8822 из $10.00, REMAINING $2.1178** (18:21Z). **09.09 13:32Z (мой гард): цикл-3 $8.5141 из $10.00, REMAINING $1.4859** — том `mp-srv2` (100 ГБ, EU-RO-1) выпил ≈ $0.63 с 06.09 (≈ $0.23/день). **15:05Z: $1.4762; 15:50Z (мой гард после c3-prep-4): REMAINING $1.4470 — c3 GO, кап $0.80.** Впереди: c3 — кап min($0.80, REMAINING − $0.30) = $0.80 (твоё слово 15:20; при $0.50 внутренний гейт пачки закончил бы ногу без единой страницы), dry run $0.1864 (dear corner), худший случай ≈ $0.40 → после c3 ≈ $1.2 минус капля тома до его удаления после закрытия линии; влезает в $10.00 без пополнения; holdout-3 не покупается. Без пополнения — баланс выше якоря цикла отказывает гарду.
Шаг S4 закрыт settlement $2.9867/$3.95. История: фаза 4 $32.47/$33; цикл-2 $17.68/$20; lora-c $1.12; D2 $5.84. `mp-lora-c` удаляется в `phase7-a1`.

## Что доказано и остаётся в продукте
Адаптер 4.5h2 (`results/verdict_45h2.json`): тональность macro-F1 0.9214, сарказм 23/38, аспекты 0.8478, тип поста 0.9586, бренды 0.9610.
Позиции 5c2 (`run_5c2_positions.json`): 145 из 159 стр.; vision 7.872 с/стр. (n=30). Сигналы в брендовой постановке закрыты (х): проход-1 68%,
thinking +0 (`think_zero_shot_table.json`; D1 (инструмент, $0) принят 26.08). Закрытые линии: однопроходный читатель, lora-b, lora-c,
think-zero-shot, харнес v1. Витрина A1–A6 — параллельно, $0, не гейт.

## Живые решения (последние; полные тексты — `docs/reviews/2026-08-30-plan-promo-pulse-1.md`; (q)–(x) 05–06.09 и раньше — в истории git этого файла, живое из них — в файле фазы)
- (jj) 09.09 18:00 — **s41 «c3-prep-4» ПРИНЯТ на моих прогонах ($0): `python3.11` везде (17×), dry run 30 стр. `289b9cc9…` + 8 постов `9363ff8d…`, `pagecount: match`, dear $0.1864 FITS −76.7% при `--cap 0.80`; `cap_from` из флага (0.80 → команда §0, 3.95 → рулинг (b)), `go_no_go` называет C3; набор флагов закрытия §5 парсится без `--expect-ms`, а с ним `complete(5,84млн, 9,82млн)=False` (риск B был реален); §3 один вызов export→braced `nohup` → один pid, `ps -o args=` называет; `make check` 4326/2 (11:55). Девиация `unauthorised-create` (§2 запущен по ошибке выбором блока по номеру фенса, шаблон `kpf574tmph` создан и удалён) реверснута за $0 — мои листинги: 2 доисторических шаблона, serverless/pod `[]`, том на месте, гард REMAINING $1.4470 = чтение ДО create (шаблон не биллит, endpoint не существовал). ТРЕТИЙ проход верификатора (§3/§5/`cap_from`) — SAFE TO BUY: ни одного денежного риска (любой построимый отказ — ДО загрузки воркера), два fail-safe нита (плейсхолдеры `--endpoint E`/`-T`; §3 `export` маскирует статус подстановки — гард `[ -n "$RUNPOD_API_KEY" ]`, симметрично §0, продрилен мной за $0) → фолд в «chain-fold», НЕ пятая prep. → **c3 GO** (свежий процесс, кап $0.80, ранбук §0→§5; том после закрытия линии).** v3.20 подтверждён третьим проходом — нового паттерна в скилл нет. Класс: плановое чтение верификатора (не стоп) + девиация исполнителя, закрыта им за $0.
- (hh) 09.09 15:30 — **s39 «c3-prep-2» принят на моих прогонах ($0): харнес = выпущенный файл (`fae771af…`), однострочник v2.6 OK, штамп живым хуком на СВЕЖЕМ старте (шапка «bypass permissions on») — поле режима доказано; проекция называет файл c3; мой dry run те же числа; `make check` 4326/2; гард REMAINING $1.4762. Моя ошибка в (gg)5 (контроль «байт в байт» был недостижим — запись C2 содержит живое чтение гарда) — исполнитель прогнал более строгое утверждение, верно. Свежий верификатор до покупки — NOT SAFE: (1) при капе $0.50 гейт пачки внутри эмиттера (резерв $0.28 на зависший джоб + хвост $0.02 + пачка по времени, а не по 30 страницам) закончил бы ногу до первой страницы; (2) запуск `| tee` в переднем плане — в C2 харнес убил его дважды; ниты: `--close` без `--tolerance`, нет строки с ключом API, том до закрытия линии, регистрация называет файлы C2, кап — арифметика руками. Твоё слово 15:20: кап $0.80 (min($0.80, REMAINING − $0.30)); код денег не трогаем до покупки — правка пачки «по оставшимся строкам» и dry run с внутренними гейтами названы на ретро. Исполнитель: «c3-prep-3» ($0): ранбук (ключ API, кап командой + гейт пола $0.15, запуск detached + короткие опросы, закрытие с `--expect-ms/--until/--tolerance 0.05`, том ПОСЛЕ закрытия) + `register()` называет то, что читает + dry run при $0.80 → узкий верификатор по диффу → c3.** PHASE v24, скилл v3.19. Класс: 3 находки верификатора = 3 непроизошедших стопа (тимлида).
- (gg) 09.09 14:00 — **s38 «c3-prep» принят на моих прогонах ($0): файл харнеса байт в байт (`c9d25657…`), штамп `bypassPermissions`, гейты §0 в обе стороны, четыре записи c3 прочитаны, мой dry run = 30 страниц + 8 постов, $0.1864 при капе $0.50 (−62.7 %), мой `make check` 4326/2, мой гард REMAINING $1.4859. Стоп (l)3 «один chain id» — верный, развилка моя: предпосылка (l) не была измерена — ни один канал Маркетопта не сворачивается в `marketopt_promo` (R2/R3 пишут имя реестра, алиасы его не знают), любая из трёх форм двигает измеренный слой P1 → форма (b) отдельным $0-шагом «chain-fold» ПОСЛЕ c3 и тома, число продукта перемеряется его же функциями (sha до/после). Allow-правила сняты (в bypass не действуют — доки), третий гейт ранбука с ними — файл `docs/reviews/2026-09-09-harness-allow/settings.json`, однострочник v2.6. Отказы `cp`/`sed` на `docs/reviews/**` (s37 auto, s38 bypass) — deny-правила, доходящие до Bash-команд `cat/head/tail/sed` и `cp` (доки; (ff)2 ошибочно винил классификатор): файлы тимлида читаются Read, харнес кладётся Write, `diff` — доказательство. Запись проекции называла файл C2, читая файл c3 — правится в «c3-prep-2». Твоё слово (в рулинге): том удаляется в сессии c3 после полной записи.** PROCESS v2.6, PHASE v23, скилл v3.18. Класс: развилка (тимлида, предвидимая) + 3 девиации записи (тимлида).
- (ff) 09.09 — **s37 «harness-fields» принят на моих проверках ($0): файл байт в байт (`0210144e…`), однострочник OK, тест хука зелёный. Вопрос исполнителя закрыт доками: deny-правило `Edit(…)` останавливает и Write. Девиация: s37 шла в auto-режиме (отказы `cp`/`sed` — см. (gg): deny-правила, не классификатор) — флаг в руках оператора оказался ритуалом с именем поля; твоё слово: `defaultMode = bypassPermissions` в `~/.claude/settings.json` (из пользовательского файла работает, из проектного — нет; применено, бэкап), плюс штамп режима `.claude/session_mode` и гейт §0 ранбука c3 — в «c3-prep».** PROCESS v2.5, скилл v3.17. Класс: процесс, тимлида.
- (ee) 08.09 (вечер) — **s36 «s2-loop» принят на моих прогонах (число продукта 0.7411 / 0.7937 — из файла, дважды байт в байт; экран и README воспроизводятся). Ревизия инструментария по докам Claude Code (`docs/reviews/2026-09-08-tooling-audit.md`): отказ на `pod create` (s33) — широкое правило `Bash(*)` в auto-режиме не действует, узкие действуют; таймауты хуков — секунды (у нас стояли как мс = часы, и в шаблонах тоже); хуки старта бегут параллельно; `/effort xhigh` руками — ритуал, а не поле. Твоё слово: поля вместо ритуалов — s37 «harness-fields» ($0, файл готов: `docs/reviews/2026-09-08-harness-fields/settings.json`) ПЕРЕД «c3-prep»; строка запуска `claude --dangerously-skip-permissions` — поле ранбука c3; проверка «`--help` прошёл» отменена (ничего не доказывала); ccc (Telegram-пульт) снят с Мака; шаблоны two-tier-dev (`250265f`) и brain-init починены; модель исполнителя до гейта — Opus 5.** PROCESS v2.4 «Harness fields», PHASE v21, скилл v3.16. Класс: процесс, тимлида (правило v2.3 писалось из симптома, не из доков).
- (dd) 08.09 — **s35 «p1-ship» принят ($0) на моих прогонах; открытый стоп — развилка тимлида (граница инструмента): замеры (bb)/(cc) грейдили СЫРОЙ ответ, петля продукта до P1 пропускает ответ через хуки S4 (цитата = точная подстрока) — моё чтение функциями петли: holdout-2 0.7500/0.8021 → 0.7411/0.7937, dev-40 и dev-2 без изменений. Решение (c): число ПРОДУКТА считается его же функциями за $0 (`grade_promo_loop_readings.json`) и печатается первым как отгруженное; замеры бара не переписываются; запись C3 не расширяется (P1 не переписывает субъект по строке с непроверенной цитатой). Ничьи — по правилу файла: 16 из 28. Исполнитель: «s2-loop» ($0) → «c3-prep» (+ контракт `@` в `channel` записи C3).** PHASE v20 §2/§6.7, разбор `docs/reviews/2026-09-08-stop-patterns.md`, скилл v3.15. Класс: развилка, предвидимая, тимлида.
- (cc) 06.09 21:45 — **s34 «p1-prep» принят ($0): линия закрыта, дро holdout-3 вслепую, P1 + тест, $0-таблица (dev-3 0.75 < 0.80) — гейт покупки не пройден, развилка вернулась за $0; ты попросил объяснить с нуля (красный гейт на карту — исправлено в «Честно»); твоё слово: (а) ОТГРУЗИТЬ КАК ИЗМЕРЕНО — S2 закрыт, P1 в продукте, числа и разбор ничьих на экране/в README, дро holdout-3 в резерве.** Дальше «p1-ship» ($0) → «c3-prep» → c3. Класс: плановое чтение.

## Отложено сознательно (что · развязка)
Проектная цель одной `/goal` поверх `make done` — после гейта C5. Окно-2, мёрдж bf16, 5c3, G1a/G1c — по слову оператора. `think-adapter` — при +0 ценность упала. Долги исполнителя — в `docs/plans/promo-pulse-1.PROGRESS.md` («named, not built»).

## Уроки (ретро `docs/reviews/2026-09-03-retro-process-v3.md`; разборы остановок `docs/reviews/2026-09-0{4,5,6}-stop-patterns.md`)
Восемь пауз фазы до 03.09 — восемь правильных отказов, $0 потеряно; корни у тимлида. Лекарство — v3: один файл фазы, один файл прогресса, стандартный промт, рулинг ≤12 строк, диета закона.
Сессии/день: 04.09 — 8 сессий, 4 остановки (3 предвидимые → PHASE v5/v6, 1 плановая); 05.09 — 8 сессий, 5 остановок (4 предвидимые тимлидом, все деньги → PHASE v8/v10/v11/v13; 1 сервинг OOM), 2 плановых чтения; верификатор до покупки — 5 укусов.
06.09 — 6 сессий (s29 prep, s30 ранбук, s31 платная итерация 5 — ЗЕЛЁНАЯ, s32 holdout-2 prep, s33 платный holdout-2 — КРАСНЫЙ по субъекту, s34 p1-prep — S2 закрыт как измерено), 2 остановки (s29: предвидимая, тимлида, деньги/процесс → PHASE v14, PROCESS v2.1; s33: классификатор харнеса отклонил `pod create` — харнес, предвидимая: разрешение на платную команду проверяется за $0 в prep-сессии → PROCESS «Executor conventions», скилл v3.13), 4 плановых чтения ((z), (aa), (bb), (cc)) + 1 красный гейт на карту (мои таблицы решений на языке тимлида → «Честно» переписана простым языком, скилл v3.14 §8); верификаторы до покупки — 1 укус + 1 риск + 2 нита ((aa) → PROCESS v2.2, PHASE v17). Тимлид: эталон holdout-2 вслепую (112 строк). Урок дня: dev-бар, подогнанный пятью итерациями на dev-40, дважды не перенёсся на свежие дро (≈ 0.71) — второй заход идёт другим классом инструмента (детерминированный слой), а не шестой итерацией того же закона. Разбор: `docs/reviews/2026-09-06-stop-patterns.md`.
08.09 — 1 сессия исполнителя (s35 «p1-ship», $0, принят), 1 остановка — развилка, предвидимая, тимлида: все замеры мерили МОДЕЛЬ (сырой ответ), а продукт фильтрует ответ хуками до P1; граница измеряемого инструмента не была названа файлом фазы. Урок: измеряемый инструмент = пайплайн продукта от модели до экрана, граница названа ДО первой покупки (PHASE v20 §2/§6.7, скилл v3.15 §2). Вечер: s36 принят; ревизия инструментария — 7 находок по докам (три латентных дефекта харнеса: мс вместо секунд, гонка хуков старта, режим запуска не записан; правило v2.3 писалось из симптома); лечение — поля вместо ритуалов (s37), скилл v3.16. Разбор: `docs/reviews/2026-09-08-stop-patterns.md` §1–§2. 09.09 — s37 принят ($0); девиация: флаг запуска в руках оператора — ритуал с именем поля (сессия шла в auto); урок: поле — там, где платформа читает его без рук (дефолт режима в пользовательских настройках + штамп режима и гейт ранбука); скилл v3.17. Днём: s38 «c3-prep» принят ($0), 1 стоп — верный, развилка тимлида (предпосылка рулинга (l)3 не была измерена прогоном), + 3 девиации записи тимлида (allow-правила — инертное поле = ритуал; (ff)2 винил классификатор без цитаты доков — отказы `cp`/`sed` были deny-правилами, доходящими до Bash; «c3-prep» шла продолжением процесса, не свежим запуском — поймано первой строкой PROGRESS и штампом). Уроки: предпосылка рулинга измеряется до записи; инертное поле снимается; отказ приписывается только механизму из доков; скилл v3.18. 15:30: s39 принят ($0) — поле режима доказано на свежем старте; верификатор до покупки нашёл 2 риска + 5 нитов = 3 непроизошедших стопа (гейт пачки, написанный под 3 008 страниц, душит ногу в 30; ранбук нёс форму запуска, которую C2 дважды убил и заменил в сессии; мой контроль (gg)5 был недостижим) — кап c3 $0.80 твоим словом, «c3-prep-3»; уроки: денежный гейт перечитывается на популяции новой ноги до покупки (dry run или верификатор функциями эмиттера); ранбук несёт то, что РАБОТАЛО в прошлом ране, а не то, что планировалось; контроль в рулинге прогоняется до его выпуска; скилл v3.19. 16:50: s40 принят ($0); узкий верификатор — ещё 2 непроизошедших стопа (ключ API не переживает границу вызовов Bash; закрытие по «wall» записи против биллинга uptime воркера — C2 закрывалась без `--expect-ms`); урок: ранбук платного рана репетируется каждой строкой в границах вызовов платной сессии, закрытие пишется по реальному закрытию прошлой ноги, а не по форме записи; скилл v3.20. Разбор: `docs/reviews/2026-09-09-stop-patterns.md` §1–§4.

<!-- MACHINE-READ BLOCK — quoted verbatim by sealed producers (`scripts/write_lora_c_prereg.py :: quoted()`).
     The diet of 26.08 deleted these and reddened 5 tests. Bytes below are law: never reword, never re-flow.
     This block is NOT map prose and does not count toward the 120-line diet. -->
## Архив рулингов, которые читают запечатанные записи (не редактировать)

- (в) **Затем — (4) LoRA «рассуждение→метка» поверх v2 с (3) синтетикой как армом B** одной регистрации; цель обучения — из (б).
- (к) **РУЛИНГ 22.08 (вечер) — дизайн `lora-c`:** промпт v3 = v2 + поле `rationale` ПЕРЕД меткой, одним рендерингом для обучения и инференса; таблица парно в четыре колонки (база v2 · база v3 · адаптер A · адаптер B) на одном пуле соседей; рационали для обучающих строк пишет Claude Code по кодбуку и метке тимлида, тимлид ревьюит все «наши» + ~40 пограничных; синтетика ~160 (по 40 на класс ошибки) — арм B, тег в provenance, абляция армом A, никогда в холдаут/голд; из обучения и из пула соседей исключены холдаут-100 и ВСЕ строки 16 референсных тредов; **бар на арм (один выстрел): холдаут-100 ≥ 64 И сквозной бар 1 = 5/5 И бар 3 = 0 — все три или КРАСНЫЙ**; отчётно: ячейка «упоминание → категория» на холдауте (18 → ?), dev-200 (fit), 14 золотых (шестой взгляд). Два контракта: `lora-c-prep` ($0, с ревью-гейтом тимлида) → `lora-c-run` (один A6000, кап **$4.00**).
- (н) синтетический запрос рендерится ТЕМ ЖЕ правилом на ТОМ ЖЕ пуле 515 (закон уже запрещает синтетику только как СОСЕДА; запрет двойника действует)
- (н) проход-2 в ране — ТОЛЬКО для армов (2 ноги, не 4): база v2 сквозной уже измерена (r2: 4/5 · 2), база v3 бара не берёт — экономия ~$0.47 по заряду.
- (о) кап lora-c-run **остаётся $4.00** (оба существующих замера s/step влезают по фактической цене; смок решает, рунг проекции убивает честно)
- (о) карта — **RTX PRO 4500, 32 GB, $0.72/ч, EU-RO-1** (A6000 в датацентре тома none; 32 GB — запас против OOM при замороженном micro_batch; фолбэк 4090 преавторизован)
- (о) ложная строка топика в синтетических заголовках заменяется выделенной константой (продовая строка NO_POST_TEXT в синтетике запрещена); конфаунд маркера принят и именован; в ран добавлен отчётный маркер-ценз (~$0.05) — отделяет «не помогло» от «выучил маркер»
- (ф) **(ф) 26.08, вечер — ПРИОРИТЕТ: СЛОЙ СИГНАЛОВ.** Система бесполезна, пока не находит сигналы в комментах и тредах на уровне ручного разбора тимлида; **только локальная Gemma-4-31B, thinking ОБЯЗАТЕЛЬНО**; API-модели отклонены. Находка: `enable_thinking: false` стояло во ВСЕХ запусках проекта — читатель с thinking ON ни разу не измерен; регистрации сами пишут «thinking-ON reader = НОВАЯ регистрация». Решение о дальнейшем пути — на этой таблице. [цитируется `scripts/write_think_zero_shot_prereg.py :: quoted()`; рулинг закрыт (х) 27.08, байты — закон]
