# STATUS — market-pulse-llm (2026-09-03, ночь; этап 1 «промо-пульс сетей» — ИТЕРАЦИЯ 1 ПРОШЛА: сигналы 0.879 (бар 0.75 ✅), субъект 0.793 (бар 0.80, минус один коммент); первая пауза ПО СУЩЕСТВУ — таблица ошибок; кодбук v1.1; итерация 2 назначена)

> Операторский документ, по-русски; правит только тимлид, после приёмок. Хроника — git этого файла,
> `docs/reports/`, `docs/reviews/`, ADR. Здесь — карта, DoD, деньги, живые решения. Потолок — 120 строк.

## Что это за проект (одним абзацем) — ПЕРЕФОРМУЛИРОВАНО 27.08 (рулинг (х))

Инструмент маркетинговых исследований ТМ «Гармонія» по украинскому Telegram (`docs/PRODUCT.md`).
**Замер 27.08 (27 662 коммента, 18 мес.): бренды watchlist названы ≈ в 40 комментах, Гармонія — в 0;
зато 2 102 промо-поста сетей с ценами и 1 092 коммента под ними.** Поэтому этап 1
(`docs/SPEC-v2-promo-pulse.md`) — **«промо-пульс сетей»**: максимум сетей, цены и глубина скидок по
молочке и мороженому, реакции покупателей под промо; бренд-тренды — трек роста источников. Непрерывная
петля с расписанием из UI. Витрина навыков (LinkedIn) — не гейт. GPU арендные.

## Карта — ГДЕ МЫ (этап 1, SPEC v2 §8)

| # | Контракт | $ | Статус |
|---|---|---|---|
| — | Фазы 0–6 (сбор · адаптер 4.5h2 · позиции 5c2 · центр v1) и 6t (thinking +0) | — | ✅ закрыты; их числа ниже |
| C1 | ценз r1+r2+r3 и харнес v2.1 | 0 | ✅ 27–30.08. **Голос = Varus + msuaaaa: 4 718 комментов в 678 ценовых тредах**; бренд-голос Полтавщины = 0 (дважды перепроверено); 36 сетей → 17 каналов A1 |
| C2–C5 | **`PHASE-promo-pulse-1`** — цены (17 строк A1 + группа АТБ; `@atb_market_official` возвращён — носитель листовок; `@znishkom` снят — Steam-скидки; PAUSED 39 и B-17 — строки с `collect: false`) → сигналы на 678 ценовых тредах (дро 20/20 по типу) → тренды → экран `make tick` | ≈$2.1–3.3 | **🔄 ТЫ ЗДЕСЬ — 03.09: S4 КУПЛЕН ЦЕЛИКОМ — 3 008 страниц + 385 постов, все 17 каналов, $3.03 из $3.95 (ставки по каналам 0.8–12.9 с/стр., АТБ 5.2 с); 1 199 позиций листовок + 54 текстовых на диске. Четвёртая пауза — развилка: сборка `pulse.db` читает реестр через печать 5c2 (r1) и отказывает 7 новым каналам r2 → рулинг (э): второе ОКНО в той же БД через собственную печать C2, тест леджера выводит живую линию из гарда. Цель в сессии активна — «go». **03.09, день: эталон dev-40 ГОТОВ — `docs/labels-promo-dev.jsonl` 140 строк (все 40 тредов; 68 комментов без текста — вне эталона, SPEC 3.19), кодбук `docs/CODEBOOK-promo-signals.md` v1; рулинг 03.09: промт `promo_prompts.CODEBOOK` перерендерить по кодбуку ДО первой платной итерации ($0, рендер показать). **03.09, вечер — ПЯТАЯ ПАУЗА (сессия 5 исполнителя, $0):** окно C2 = `w2` в `pulse.db` (17 каналов · 1 113 позиций; `w1` зеркало 902/902), промт перерендерен по кодбуку (`a97d3c71…`), грейдер K8 доказан в 4 направлениях на эталоне, передро-50 (sha `61b4fda7…`), 4 268 passed; две развилки → рулинг (я): раскол derived-стора (печати w1 восстанавливаются побайтно) и `carrier` в ключе `positions`; рендер прочитан — принят (+1 строка); K6: эталон ПО СТРАНИЦАМ (46 страниц); S9 итерация 1 — после смока по таблице. **03.09, ночь — ШЕСТАЯ ПАУЗА (сессия 6, $0): обе развилки ЗАКРЫТЫ — стор расколот (`data/derived_w2/`, 38/38 печатей w1 побайтно), `carrier` в ключе, шаг S4 ЗАКРЫТ ($2.9867 settled), кодбук в промте `a694d005…`, K6-запись страничная (46 страниц, 205 строк извлечения, якорь `fe3f5841…`); `make check` ЗЕЛЁНЫЙ — 4 297 passed, exit 0. Пауза: у S9 нет платного инструмента → план §9 rev 9 → рулинг (a): ПРИНЯТ с 5 поправками, строить и покупать смок + итерацию 1; §8 → v4. **СЕДЬМАЯ ПАУЗА (сессия 7, $0): $0-половина S9 ПОСТРОЕНА — регистрация FITS $1.3193 из $2.50 (единица пода: секунды × $/ч, EU-RO-1 RTX 4090 $0.74/ч), пак 56 юнитов (40 тредов + 16 постов, смок 6523/9006/6009), раннер, рунбук, 12 тестов, `make check` 4 313 зелёный; под НЕ создан — сессия кончилась раньше платного прогона (правильно: платный ран = целая сессия). Рулинг (b): принято; строка create — из рунбука сиблинга дословно (образ `runpod/pytorch:1.0.2-cu1281-torch280-ubuntu2404`, EU-RO-1, диск 30), `--terminate-after` выводится из капа, не печатается руками (90m < дорогой угол 107 мин); следующая сессия — только рунбук §0→§6. **ВОСЬМАЯ ПАУЗА (сессия 8, $0.09): два пода куплены и убиты СОБСТВЕННЫМ dead-man (180 с из записи ПРОБЫ; продакшен-сиблинг регистрирует 500 с) — рунг сработал, ничего не сгенерировано, третий recreate отвергнут исполнителем правильно. Рулинг (e): dead-man 500 с, рунги 1–2 для шагов ≤$3 сняты (кап = `--terminate-after`), `/goal` для фазы ОТМЕНЁН — стандартный промт + `PROGRESS.md`; ретро процесса `docs/reviews/2026-09-03-retro-process-v3.md`. **СЕССИЯ 9 (стандартный промт, без `/goal`): ИТЕРАЦИЯ 1 ПРОШЛА ЦЕЛИКОМ — под 2 120 с, $0.4358; 56/56 юнитов; `grade_promo_dev40_iter1.json`: сигналы 0.8792 (бар 0.75 ✅; страты 0.86/0.90), субъект 111/140 = 0.7929 (бар 0.80 — не хватило ОДНОГО коммента; страты 0.74/0.84). Таблица ошибок прочитана: 29 промахов — атрибуция, не смысл (сарказм читает): 4 — ответы админа (модель не видит автора), 5 — имя сети как функция/написание (`додаток`, `Варус`), 3 — правило «наличие → товар», 3 — спорные строки эталона, остальное — ирония/шутки. Рулинг (f): кодбук v1.1 (3 строки эталона → `post`; имя сети — не функция; id сети из реестра), итерация 2 = `[admin]`-метка + канонический id сети + примеры из кодбука в шаблоне ($0-сессия) → платный прогон ($1.97 осталось в шаге)** |
| ↳ | один `/goal` (§8 v3): S4 бэкфилл (405 текстов → 3 008 стр., кап $3.95) → передро-50 → грейдеры по меткам тимлида → dev-петля ≤5 → холдаут (1) → гейт 20 строк | ≤$7.00 | ожидаемые паузы: «жду метки» (dev-40, позиции-50) · плато · нотис холдаута |

## ФИНИШ — когда этап 1 закрыт (обновляется каждой приёмкой; прогноз 03.09)

Осталось до гейта C5 (ты смотришь 20 строк под сидом и говоришь, что верно/неверно): **итерация 2** (сессия A $0: `[admin]`-метка,
канонический id сети, примеры из кодбука в шаблоне, тесты; сессия B платная ≤$1.97: прогон + K8) → при субъекте ≥ 0.80 — прережка
холдаута (STOP-нотис) → **holdout-40 размечает тимлид** → один выстрел → **параллельно: разметка позиций
тимлидом ПО СТРАНИЦАМ — 46 страниц/постов дро, все молочные и мороженое; 2 сессии — критический путь S1** → dev-петля до плато
(≤5) → холдаут (1 выстрел) → гейт. **Оценка: гейт ~07–09.09** (итерация 1 прошла 03.09 за одну сессию по новому процессу; страничный эталон S1 — две сессии; dev-40 закрыт 03.09;
риск — бар субъекта 0.80: 53 из 140 строк эталона — `post` (админ, шум), модель берёт их только по правилу (2) рулинга 03.09). Твоего времени: один гейт (20 строк); слов о деньгах больше не нужно, пока не сработает рунг. После гейта C5: слово о C6 (фронт петли, UI расписания) и окне-2; витрина A1–A6 — параллельно, $0.
| C6 | фронт: петля + расписание в UI | слово | после C5 |

## Что доказано и остаётся в продукте — числа из result-файлов

Адаптер 4.5h2 (`results/verdict_45h2.json`): тональность macro-F1 **0.9214**, сарказм 23/38, аспекты 0.8478,
тип поста 0.9586, бренды 0.9610. Позиции 5c2 (`run_5c2_positions.json`): 145 позиций из 159 стр. + 44 текстов;
**vision 7.872 с/стр. (смок 30.08, n=30), маргинал 2.6–3.4** (`smoke_vision_c2.json`).
Сигналы в брендовой постановке — **закрыты (х)**: проход-1 68%, бары прохода-2 красные, thinking +0
(`think_zero_shot_table.json`; D1 (инструмент, $0) принят 26.08); перечитываются под S2. Закрытые линии: однопроходный читатель (17.08), lora-b,
lora-c, think-zero-shot, харнес v1. Витрина A1–A6 (рулинг (у)) — параллельно, $0, не гейт.

## Деньги (читаются ИЗ ГАРДА; чтение тимлида 26.08 после r3)

Фаза 4 закрыта $32.4708/$33. Цикл-2 закрыт на $17.68/$20. **Цикл-3: оператор внёс +$10 (баланс $14.37,
сверено гардом 01.09); потолок поднят словом оператора до $7.00; ≈$7.4 — НЕПРИКОСНОВЕННЫЙ резерв** (C6,
окно-2, непредвиденное). Внутри потолка: C2 все **3 008 страниц** (сосчитаны точно, `promo_pagecount_c2.json`)
≈ $3.16 по пессимистичному маргиналу · dev-петля кап $2.50 · холдаут $0.30 · дрип тома $0.2333/день. **Чтение 03.09 (гард,
мой прогон): цикл-3 потрачено $3.4437 (S4 $3.03 + дрип), остаток $3.5563. S4 закрыт по деньгам: весь ценз куплен, шаг-леджер
закрывается `--close` в ритуале; dev-петля = min($2.50, остаток − $0.30) = $2.50, пол $2.00; холдаут $0.30 неприкосновенен.**
**Закрытие шага S4 (сессия 6, гард `--close --tolerance 0.05`): `PROMO-PULSE-1 CLOSED — settled $2.9867 of $3.95` (поды $0.0521 +
serverless $2.9347; дрип тома $0.2236 — в цикле 3, вне шага); цикл-3: потрачено $3.5117 of $7.00, остаток $3.4883; dev-петля — полные $2.50.
Моя цифра «$3.1909 (delta)» из рулинга (я) — чтение баланса, не settlement; отозвана рулингом (a).**
Ставка vision: 7.872 с/стр. с бутом (n=30), маргинал 2.623–3.369 (`smoke_vision_c2.json`). Резов страниц и
под-раннера НЕТ — фаза финансирована целиком.
История: lora-c $1.123; D2 $5.84 по часам пода (полная программа D2 $20.87 — не куплена, недокупленные
стадии баров не несут). Тома: `mp-srv2` (EU-RO-1, сервинг, остаётся до дизайна C6) + `mp-lora-c` (CA-MTL-3,
**удаляется в `phase7-a1`**, доказательство — листинг). Долг: `money-anchors` ($0) — settled-закрытия, 19 без `anchored_at`.

## Живые рулинги (полные тексты прежних — git этого файла до 26.08)

- **(д) 04.09 — сессия A остановилась ДО первой правки: `admin_anon_ids` нельзя добавить в `config/registry.yaml` — файл пиннят 21 печать.**
  Рулинг (g): id админов — в НОВЫЙ файл `config/channel_admins.yaml` (реестр не трогать, ревизии r3 нет); бестекстовые комменты из рендера
  убрать; регистрация B переиздаётся под sha v1.1 (+ `template_sha256`); чтение v1.1 — в отдельный файл; `--out` по номеру итерации.
- **(г) 03.09, ночь — итерация 1 прочитана (рулинг (f)).** Принята: сигналы 0.8792 ✅, субъект 0.7929 (−1 коммент). Промахи — атрибуция:
  админ 4 (метка `[admin]` в рендере), имя сети 5 (канонический id из реестра, «функция → имя сети»), наличие → товар 3 (примеры в шаблон),
  спорный эталон 3 (кодбук v1.1: реплика без субъекта и сигнала → `post`; чтение после v1.1 — 0.814, не заявка на бар). Итерация 2 — две
  сессии: $0 (метка, id, примеры из кодбука — не из dev-тредов, тесты `--score/--project`) → платная. Деньги шага — по секундам пода:
  $0.5254/$2.50; settlement с допуском к таким шагам не применяется. Fence в ответах Gemma снят в `parse` — принято.
- **(в) 03.09, вечер — восьмая пауза и переоснование процесса.** Dead-man = 500 с из `prereg_pass2_signals_r2.json` (запись с измерениями),
  не 180 с из пробы. Для шагов ≤$3 рунги 1–2 сняты: кап → `--terminate-after`, одна строка леджера на сессию. `/goal` для фаз со
  стоп-точками отменён (оценщик не умеет паузу; 27 холостых ходов): стандартный промт ≤12 строк каждую сессию, одна задача за сессию,
  `docs/plans/promo-pulse-1.PROGRESS.md` вместо STOP+отчётов. Ретро: тесты+скрипты : продукт = 15:1, 45 % коммитов — процесс, 8 пауз
  за 4 дня, 6 из 8 — закон тимлида. Скилл team-lead v3 — на карточке оператора; brain-init M6 v2 — дельты в ретро §6. Рулинги ≤12 строк.
- **(b) 03.09, поздно — седьмая пауза.** $0-половина S9 принята (регистрация $1.3193/$2.50 в единице пода, пак 56, раннер, 12 тестов, 4 313
  зелёных). Платный ран = целая сессия (PROCESS §Money). Строка create — сиблинга дословно (`runbook_pass2_signals_r2.md:89`), `--image`
  `runpod/pytorch:1.0.2-cu1281-torch280-ubuntu2404`, EU-RO-1/SECURE/`--ssh`/диск 30; `--terminate-after` — секунды капа по цене дня из
  `--pre-create-check`, не «90m». Паттерн семи пауз — в §Уроки. §8 не меняется.
- **(a) 03.09, ночь — шестая пауза.** План §9 rev 9 (платный инструмент S9: шаг `promo-dev-loop`, свой леджер, кап $2.50/пол $2.00, 4 рунга,
  флаги `--register/--smoke/--run/--close`, смок n=3 по правилу «короткий/медианный/длинный») — ПРИНЯТ с 5 поправками: регистрация ценит
  смок + итерацию 1 (не пять); один под на итерацию, teardown перед каждым STOP; итерация 1 = базлайн (кодбук и шаблон как есть), 2–5 меняют
  только шаблон/рендер/декодинг (кодбук — только тимлид на плато); каждая итерация хранит свои predicted/grade/errors; `--close 0.05`.
  Деньги: линия шага — settlement $2.9867 (моя delta-цифра отозвана). Дедуп трендов — по НОГЕ (литеральный ключ снял бы 39 строк — 34 группы
  это два разных промо одного SKU). §8 → v4: хвост ветки PAUSE («after the ruling lands… re-enters…») удалён — оценщик читал его как
  предусловие (ADR исполнителя); протокол резюма — в PROCESS. Позиции-50 — против якоря `fe3f5841…`, 46 страниц, у тимлида.
- **(я) 03.09, вечер — пятая пауза.** Развилка 1: РАСКОЛ derived-стора (C2 — свой корень, `data/derived/` — байты 5c2; C2 только
  дописывал: 23 файла целы, 15 — точный префикс; копия → проверка → обрезка, бэкап-листинг до первой обрезки; чек — 38/38 печатей sha,
  экспорт w1 == закоммиченному включая provenance). Развилка 2: `carrier` в ключе `positions`, тест переписан по рулингу (id не двигаются,
  w1 побайтно). Закрытие шага `--tolerance 0.05` (3.15 % — дрип тома). 16 постов без evidence — доспросить на поде смока S9, отдельная
  строка леджера. Рендер промта прочитан, принят + 1 строка (сроки акции в целом → chain). **K6: эталон позиций — ПО СТРАНИЦАМ** (дро
  строк вслепую не размечается: страница несёт до 10 строк): все молочные/мороженое на 46 страницах, predicted = все строки страниц.
  S9 итерация 1 — после смока: ≤$0.80/итерация — идти; $0.80–1.20 — одна и STOP; >$1.20 — STOP до покупки. Пауза = `/goal clear` оператором.
- **(ю) 03.09 — эталон dev-40 и закон промта.** `docs/labels-promo-dev.jsonl` 140 строк по кодбуку v1 (`docs/CODEBOOK-promo-signals.md`,
  тимлид-файл; deny-строка `Edit(/docs/CODEBOOK-*.md)` — контракт исполнителю). Комменты без текста — вне эталона (SPEC 3.19, `n_wordless`).
  `promo_prompts.CODEBOOK` расходился с кодбуком в местах, которые грейдер считает (post/шум, форма субъекта, пары цена+жалоба) → перерендер
  по шести дельтам рулинга ДО первой платной итерации; K8 на dev-40 = итерация 1. Дальше у тимлида: позиции-50 после передро; холдаут — после нотиса.
- **(э) 03.09 — окно C2 в `pulse.db` (форма 1).** Печать C2 = `prereg_promo_c2.json` (+ пин реестра r2 `eff8ba5b…` писателем регистрации);
  сборка строит ВСЕ окна в одну БД, каждое через свою печать (5c2 → w1 через r1 без изменений — sha экспорта w1 пиннится тестом);
  живого реестра в сборке нет. Тест леджера выводит линии из констант гарда (Dv, cause: ruling). Ранкер OCR — измеренный минус, на полку.
- **(ы)(ь) 02.09 — кап S4 $3.95 и гейт по каналам — ИСПОЛНЕНЫ.** Оператор: «$3.95 — весь шаг», «АТБ → дешёвые → малые». Гейт (10)(a)
  по ногам и по каналам (первая пачка — замер, остаток канала целиком или следующий); итог — весь ценз за $3.03, все 17 каналов целиком.
- **(ъ) 02.09 — якорь цикла-3 и запуск /goal.** Оператор: «Да: якорь + $10.00» → `runpod_balance_at_cycle3_start` = 14.4799665639,
  `anchored_at` не меняется, потолок $7.00. Причина паузы — рулинг 01.09 (later) «якорь не менять» не был прогнан через `enforce()`.
  Запуск: `/effort xhigh` (не ultracode) → `/goal` ВРУЧНУЮ → тело §8 v3 (внутри — ни одной /команды; «Goal set» без хода → «go»).
- **(щ) 01.09 — цикл-3 и протокол.** «Цикл-3 = весь баланс» (потолок $4.8 → $7.00 вечером после +$10). Ретро принят: один `/goal`
  на фазу, стоп = пауза, каждая передача — для свежей сессии, стоп-точка несёт стол решений (PROCESS §Каденция 1·3; скилл v2.3).
- **(ш) 30.08, ночь — SP-4 и смок.** Рулинг (а): **v1 — архив, только чтение; живой корень `data/raw_r2/`;
  читатель v1 ∪ r2, дедуп (канал, msg_id)**. Докачка +1 340 постов, +1 190 комментов (вступления 7/8, v1 6/6,
  дро байт-стабильно). Факты: группа АТБ — только через `linked_chat`; приватный Маркетопт пока не собирается.
- **(ч) 30.08, вечер — ревью плана фазы.** Оператор: «Go на $0, деньги — на стопе S3»; «Все 678, дро 20/20 по типу» (цена в
  картинке — тоже ценовой). Тимлид: бар цены S1 — только промо-цена; старая цена хранится, НЕ печатается (SPEC 3.21/3.22); схема
  шести таблиц (ключ сигнала несёт субъект, дайджест — текст и `covers_up_to_msg_id`, `window_id` вне identity, `unsure`).
- **(ц) 30.08 — рулинги оператора в сессии r2/r3:** «комменты» = только треды под ЦЕНОВЫМИ постами ((ч):
  цена в картинке — тоже ценовой); языковой ценз снят; вступление в приватный Маркетопт разрешено; r3-добор
  авторизован (80/41). Ценз: A1 17 строк (АТБ/Маркетопт/Епіцентр — цены в КАРТИНКАХ → vision); голос —
  Varus + msuaaaa (+Копійочка, ФАНАТИК+группа, Rozetka); Novus/Ашан — каналов нет.
- **(х) 27.08 — ПЕРЕСПЕКА, путь В.** 0 комментов про Гармонію за 18 мес. → этап 1 — исключительно цены и
  комменты к промо сетей, перечень сетей максимальный; продукт строится на том, что данные дают, рост
  источников — внутри петли; петля непрерывная, расписание в UI; эталон размечает тимлид. Спека —
  `docs/SPEC-v2-promo-pulse.md`. Мердж адаптера/vLLM — вне этапа 1; `mp-lora-c` удаляется.
- **(ф) 26.08 — слой сигналов, брендовая постановка — ЗАКРЫТ (х), переписан под S2.** Полный текст —
  в MACHINE-READ BLOCK ниже (его цитируют запечатанные продюсеры) и в git до 27.08.
- **(у) 26.08 — ПЕРЕПЛАНИРОВАНИЕ** (`docs/reviews/2026-08-26-replan-and-process-diet.md`): lora-c закрыта
  stop-rule'ом (прережка НЕ потрачена; переоткрытие после ≥300 позитивов); DoD A1–A6 + B; остаток — не на
  обучение; **диета процесса** (контракт ≤50 / отчёт ≤80, четыре рунга, кап 2×, STATUS ≤120, хэндофф ≤30).
- **(0) 20.08:** база — Gemma-4-31B; разметка — тимлид. **(а) 21.08:** v2 — базовый промпт прохода-1.
  **(к) 22.08:** дизайн lora-c — исполнен до r3, закрыт (у). **(л)(м) 23.08:** SPEC 3.25/3.26 — потолок 3 072.
- Стоп-рулы, связывающие тимлида: однопроходный промпт-читатель (17.08), линия B (20.08), lora-c (26.08).

## Реестр измерений (пока здесь; переезжает в `results/measurements.jsonl` контрактом a1)

Обучение: A100 80 GB $1.39/ч — 89.961 с/шаг (3 072/mb2/ga8), пик 49.54 GiB; A6000 48 GB при 1 408 —
61–68 с/шаг; 32 GB — OOM на 3 072 (`results/train/*/provenance.json`). Инференс thinking OFF
(`results/measurements.jsonl`): проход-1 2.29–2.73 с/звонок; **проход-2 23.76 с/тред (max 135)**; сервинг
srv2d $1.43 / 1 000 строк, под в 2.383× дешевле (`results/srv2d_cost.json`). **Позиции 5c2: 10.408 с/строку
реальные** (`run_5c2_positions.json :: timing`), «1.7 с/стр.» — маргинал разогрева; vision с/стр. в реестре НЕТ —
пишет смок C2. Thinking ON (D2): проход-1 110 с/звонок, проход-2 396 с/тред, ≈$22 / 1 000 строк. Walk биллинга
отстаёт на 30–40 мин.

## Отложено СОЗНАТЕЛЬНО (что · критерий развязки)

- **Проектная цель (решение 02.09, оператор согласен):** одна `/goal` на ВЕСЬ проект поверх исполняемого предиката `make done`
  (таблица PASS/FAIL по result-файлам, леджеру гарда, файлам меток и `docs/GATES.md` — гейты оператора его рукой); предикат ≤5 строк;
  тип паузы «у фазы нет проверок». Развязка: после гейта C5 — дизайн `DONE.yaml`/`GATES.md` (тимлид), включение с C6; посреди фазы не менять.
- Окно-2, prep-b, serving-латенси, мёрдж bf16, история 11 143, батч-паритет, 5c3, G1a/G1c — по слову оператора.
- Долги исполнителя: `make preflight` перечисляет два r2-пути как «differ», а не «reached r1» (Dv2, условие тимлида);
  `promo_projection_c2.py` пишет отменённые $4.80 и игнорирует exit гарда — K4 не перезапускать до починки (заметки ревью 02.09).
  `/code-review` до первого платного шага — ВЫПОЛНЕН 02.09. Судьба тома `mp-srv2` ($7/мес) — вместе с дизайном сервинга петли (C6).
- `think-adapter` (4.5h2 с thinking ON) — при +0 на zero-shot и ×40 по цене ценность упала; слово оператора.
- Адъюдикация референса (E1) — если заказчик попросит. Мелочи step-0.5 (`t5_depth`, ₴, fix-b(2), Dv732/541/536/521,
  blind-40, формат-дрифт) — как прежде.

## Правила, которые легко забыть (механика — `docs/PROCESS.md`)

- Числа — только из result-файлов, каждое называет файл; деньги — из гарда; кап 2× оценки; перерасход
  ≤20% → ASK. Пре-регистрации не переоткрываются; провал закрывает вопрос; stop-rule закрывает линию.
- Файлы тимлида исполнитель коммитит по пути, не правит; никогда `git add -A` / `make fmt` репо-целиком;
  пины — `make preflight` до контракта; отчёт — файл, в чат только путь; приёмка — в тот же день.

## Уроки (ретро 26.08; D2 27.08; C1/харнес 27–28.08)

Три из шести остановок lora-c снимались чтением файлов, уже лежавших в репо — лекарство: реестр
измерений, не новый рунг. D2: ошибки прохода-1 — дыры кодбука и нехватка контекста, не рассуждения;
думать дольше над той же неполной информацией не помогает. C1: контракт описывал механику, не критерий
успеха → скилл v2.1. 30.08: ценз читал ТЕКСТ — и чуть не выкинул носитель листовок; регекс «цены»
ловил даты — две трети «ценовых» тредов были без цены в тексте. Урок: инструмент ценза называет,
что он НЕ видит (картинки), прежде чем выносить вердикт «без промо». Итерации/день:
26.08 — 2 · 27.08 — 2 · 28.08 — 1 · **30.08 — 4** (ценз r2+r3; план — пять столкновений ДО работы; $0-срез —
S2 стал перед необратимым; s2s3 — смок по прережке, /code-review нашёл дефект S1b: `collectable` не читает `collect`) ·
01.09 — 1 (вставка ушла текстом, цели не было; вечерняя — обрезана на вложенном `/goal`, 12 ч без хода) · **02.09 — 3** (77 мин —
весь $0-инструмент, пауза по якорю; 60 мин — якорь, 3 008 страниц на диске, драйвер S4, пауза по капу; 40 мин — ран 1, гейт отказал:
ставка — свойство канала, не населения; вечером — ран 2: весь ценз за $3.03, 4-я пауза — печать реестра). Уроки 02.09: рулинг о гарде —
прогоном гарда (скилл v2.4); проекция «весь шаг по одной ставке» лжёт в обе стороны — мерить по страте на платном транспорте; платный ран
запускать отсоединённым (`os.setsid`) — харнес убил процесс дважды (~$0.09). **03.09 — 4** (dev-40: 140 строк за сессию с привязанным Mac; сессия 5: окно w2, промт, грейдер, передро — $0; сессия 6: раскол стора, ключ,
шаг закрыт, check зелёный, план S9 — $0; сессия 7: инструмент S9 построен и зарегистрирован — $0).
**Паттерн семи пауз фазы (03.09, ответ оператору):** семь остановок — семь ПРАВИЛЬНЫХ отказов исполнителя, $0 потеряно, ни одной
печати сломано, ни одного теста ослаблено. Корни — на стороне тимлида, шесть из семи: (А) рост стора против печатей, которые спека не
опросила (паузы 4, 5: реестр через печать 5c2, хэши файлов w1, ключ positions) — нарушено правило скилла §7 «спроси, кто читает файл,
до контракта»; (Б) деньги, отруленные по интуиции, а не прогоном инструмента (1: якорь без `enforce()`, 3: «весь шаг по одной ставке»,
6: «$3.1909 delta» — гард такую цифру не производит); (В) строка плана «S9 dev loop» принята без инструмента (6) и дро строк для слепой
разметки (5); (Г) одна сессия на «построить И купить» (7). НЕ паттерн: ошибки исполнителя (0/7) и спешка оператора — паузы случаются
внутри сессий исполнителя независимо от скорости релея. Лекарство, уже в рулингах (c)/(d) и в PROCESS: рулинг называет команду, чей
вывод — число, а не число; развилка на печати перечисляет печати до рулинга; платный ран = целая сессия. Оставшиеся паузы — плановые:
«iteration 1 read», плато, нотис холдаута, «жду позиции». **Восьмая пауза (dead-man с пробы) подтвердила диагноз: каждый лишний гард —
новое место для ошибки; полный ретро и переоснование — `docs/reviews/2026-09-03-retro-process-v3.md`.**
Урок 03.09 (б): дро строк для слепой разметки — дефект дизайна K5/K6 (совпадение по содержимому + слепой разметчик = лотерея); эталон
извлечения — по единице, которую видит человек (страница), а не по единице, которую породил инструмент (строка). Evaluator `/goal` не
читает ветку PAUSE — 8 холостых ходов до капа; пауза закрывается `/goal clear`.
Урок 03.09: закон промта и закон эталона писались порознь (промт — до кодбука) — сверять рендер промта с кодбуком ДО первой платной
итерации, иначе первая из ≤5 уходит на расхождение законов, не на модель.

<!-- MACHINE-READ BLOCK — quoted verbatim by sealed producers (`scripts/write_lora_c_prereg.py :: quoted()`).
     The diet of 26.08 deleted these and reddened 5 tests. Bytes below are law: never reword, never re-flow.
     This block is NOT map prose and does not count toward the 120-line diet. -->
## Архив рулингов, которые читают запечатанные записи (не редактировать)

- (в) **Затем — (4) LoRA «рассуждение→метка» поверх v2 с (3) синтетикой как армом B** одной регистрации; цель обучения — из (б).
- (к) **РУЛИНГ 22.08 (вечер) — дизайн `lora-c`:** промпт v3 = v2 + поле `rationale` ПЕРЕД меткой, одним рендерингом для обучения и инференса; таблица парно в четыре колонки (база v2 · база v3 · адаптер A · адаптер B) на одном пуле соседей; рационали для обучающих строк пишет Claude Code по кодбуку и метке тимлида, тимлид ревьюит все «наши» + ~40 пограничных; синтетика ~160 (по 40 на класс ошибки) — арм B, тег в provenance, абляция армом A, никогда в холдаут/голд; из обучения и из пула соседей исключены холдаут-100 и ВСЕ строки 16 референсных тредов; **бар на арм (один выстрел): холдаут-100 ≥ 64 И сквозной бар 1 = 5/5 И бар 3 = 0 — все три или КРАСНЫЙ**; отчётно: ячейка «упоминание → категория» на холдауте (18 → ?), dev-200 (fit), 14 золотых (шестой взгляд). Два контракта: `lora-c-prep` ($0, с ревью-гейтом тимлида) → `lora-c-run` (один A6000, кап **$4.00**).
- (н) синтетический запрос рендерится ТЕМ ЖЕ правилом на ТОМ ЖЕ пуле 515 (закон уже запрещает синтетику только как СОСЕДА; запрет двойника действует)
- (н) проход-2 в ране — ТОЛЬКО для армов (2 ноги, не 4): база v2 сквозной уже измерена (r2: 4/5 · 2), база v3 бара не берёт — экономия ~$0.47 по заряду.
- (о) кап lora-c-run **остаётся $4.00** (оба существующих замера s/step влезают по фактической цене; смок решает, рунг проекции убивает честно)
- (о) карта — **RTX PRO 4500, 32 GB, $0.72/ч, EU-RO-1** (A6000 в датацентре тома none; 32 GB — запас против OOM при замороженном micro_batch; фолбэк 4090 преавторизован)
- (о) ложная строка топика в синтетических заголовках заменяется выделенной константой (продовая строка NO_POST_TEXT в синтетике запрещена); конфаунд маркера принят и именован; в ран добавлен отчётный маркер-ценз (~$0.05) — отделяет «не помогло» от «выучил маркер»
- (ф) **(ф) 26.08, вечер — ПРИОРИТЕТ: СЛОЙ СИГНАЛОВ.** Система бесполезна, пока не находит сигналы в комментах и тредах на уровне ручного разбора тимлида; **только локальная Gemma-4-31B, thinking ОБЯЗАТЕЛЬНО**; API-модели отклонены. Находка: `enable_thinking: false` стояло во ВСЕХ запусках проекта — читатель с thinking ON ни разу не измерен; регистрации сами пишут «thinking-ON reader = НОВАЯ регистрация». Решение о дальнейшем пути — на этой таблице. [цитируется `scripts/write_think_zero_shot_prereg.py :: quoted()`; рулинг закрыт (х) 27.08, байты — закон]
