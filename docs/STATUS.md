# STATUS — market-pulse-llm (2026-08-19; компактная карта)

> Операторский документ, по-русски по решению оператора. Правит только тимлид,
> после приёмок. Полная хроника фаз 0–5 и дней 15–17.08 — в git-истории этого
> файла и в docs/reports/; здесь — карта и живые решения.

## Что это за проект (одним абзацем)

Ключевой инструмент маркетинговых исследований компании (ТМ «Гармонія»):
мониторим Telegram-каналы украинских ритейл-сетей (Сільпо, АТБ, Varus,
Маркетопт) и агрегаторов, находим анонсы новинок и промо по молочке/мороженому,
извлекаем бренды и позиции с ценами, классифицируем реакции покупателей
(тональность, сарказм, скрытые интенции), и превращаем это в командный центр
директора по маркетингу. Ядро — открытая LLM (Gemma-4-31B), дообученная QLoRA;
GPU только арендные, между сессиями консоль пуста. Северная звезда — семь
вопросов директора в `docs/PRODUCT.md`.

## Карта фаз — ГДЕ МЫ

| Фаза | Что | Статус |
|---|---|---|
| 0–4.5 | Спека (rev 3.24) · скелет · данные · бейзлайны · QLoRA · программа качества | ✅ (хроника в git) |
| 5 | Продакшн-петля 5a…5c2: окно-1 куплено целиком, сидение ратифицировало обе половины | ✅ 14.08 |
| **6** | **Командный центр + слой сигналов из комментариев** | **🔄 ТЫ ЗДЕСЬ — v1 центра построен; однопроходный промпт-читатель ЗАКРЫТ stop-rule'ом 17.08; каркас — гибрид D (сидение 17.08); ПРОХОД-1 ИЗМЕРЕН 18.08: бар P1 9/14 при пороге 12 → оговорка вернула вопрос СИДЕНИЮ (B: разметка subject_type + LoRA / C: другая база) — первый рулинг оператора; эталон цели — `docs/REFERENCE-signals-w1.md`** |
| 7 | Валидация на ≥2 окнах + упаковка | — |

## Модель сейчас (деливерабл)

Классификация: Gemma-4-31B NF4 + несмёрженный QLoRA-адаптер 4.5h2, батч 1,
гейты v4: 3/5 (G1a 0.9214 · G1b 23/38 · G1c 0.8478 FAIL на 0.0005 · G1d 0.9586
· G1e 0.9610). Сигналы из комментариев — каркас «гибрид D»: v5-читатель треда
(сущности 4/4×2, шум, цитаты — на сданном), проход-1 — короткая per-comment
классификация с контекстом сущностей, проход-2 — сборка сигналов. Проход-1
ИЗМЕРЕН (pass1-probe-b): инструмент и транспорт зелёные, чтение subject_type
не взято → сидение B/C. Позиции листовок: инструмент v2, промо+бейдж — зелёные
ноги; зачёркнутая цена не печатается нигде (законы 3.17/3.18/3.22).

## День 18.08 — недельное ретро; pass1-probe-b ИЗМЕРЕН и ПРИНЯТ

**Недельное ретро + дистилляция скилла**
(`docs/reviews/2026-08-18-weekly-retro.md`, закоммичен): паттерны P1–P7,
гипотезы H1–H8, скилл team-lead 729→689 строк (эталон — в проекте Claude).
Живое для контрактов с этого дня: (а) **enum cause-тегов v2 ЗАКРЫТ** —
`contract-gap | spec-gap | verify-gap | env | tooling | model | process`, имя
урока — отдельным `[[wiki-именем]]`, приёмка грепает enum (пилот H4: ≥90%
каноничности, базовая линия ~30%); (б) **чеклист приёмки — 6 пунктов**:
добавлен size/health (скилл ≤700 · STATUS ≤300 · boot tax исполнителя ≤ цели);
(в) **каждая регистрируемая константа несёт формулу вывода**, исполнитель
пере-выводит все отказным гейтом шага 1 (H6); (г) **kill-правило на
биллинговом часе ДО milestone-гейтов**, дедлайны от худшего замера, разброс
регистрируется, не усредняется (H3); (д) пилоты — см. «Урок недели» внизу.

**`pass1-probe-b` исполнен и ПРИНЯТ 18.08 — проход-1 наконец ИЗМЕРЕН, и ответ
отрицательный** (отчёт `docs/reports/pass1-probe-b.md`, Dv490–499). Транспорт
сработал целиком: gate-0 GO за 14.5 с (третий GO подряд: 23.4 / 24 / 14.5 с при
дедмане 180 с), бут 237.2 с (три замера стека 192–293 с; разброс 1.53×
зарегистрирован), 64/64 юнита прочитаны, 0 отказов, потолок 256 токенов не
стрелял, под удалён с доказательством, **$0.135050 из капа $0.20** (внутри
worst case $0.18289). **А чтение — нет: бар P1 = 9/14 при пороге 12 → STOP,
оговорка возврата к сидению СРАБОТАЛА.** Все 5 потерь — subject_type: 2 «не
типизирую» (None) и 3 другое прочтение (в т.ч. 580124 бренд→сеть); stance 3/3
верно; на цензовых соседях инструмент воздерживается в половине строк (25/50
None — это ОТВЕТ модели, не сбой: парс 64/64). Куплено знанием: звонок
5.162 с/коммент (0.81× зарегистрированной границы; фит занижал в 1.76×);
проход-1 по окну (1 032 платных) = **$1.095 генерации — ПОЛ, без бута**.
Верификация тимлида: свой `make check` **2911/2** (база 2879 + 32 новых),
скорер воспроизведён байт-в-байт (git diff чист), независимый пересчёт бара P1
свежим контекст-субагентом — **AGREE построчно** (триггер: контракт двигал
BOOT_KILL_S — константу, гейтящую траты), облако перелистано с позитивным
контролем (пусто, том жив), ownership-свип чист, порядок коммитов
prereg→anchor→run сверен по git-часам, арифметика Dv490 пересчитана ($0.18289
честный worst case; go/no-go не зависел). Dv-теги отчёта — legacy-конвенция
(контракт выдан до ретро); enum v2 обязателен с «--until»; этот отчёт —
базовая линия H4.

**`guard-until` исполнен и ПРИНЯТ 18.08, поздний вечер** (отчёт
`docs/reports/guard-until.md`, Dv500–517; suite 2911→**2927/2** — свой прогон
тимлида). Отказный гейт H6 СРАБОТАЛ: толеранс «6–7%» из контрольной таблицы
pass1-probe НЕ пере-вывелся — таблица мерила баг ОКНА, не разброс биллинга
(старты окон = первая сессия, а не якорь; probe_a — окно нулевой ширины;
5c1_vis — два якоря в одном файле; и Dv504: записанное чтение леджера
ОТСТАЁТ от биллинга — баланс оседает позже истории). Контракт сам не закрыл
НИЧЕГО; `--tolerance` — обязательный параметр без дефолта. Поставлено: гард
`--until`/`--expect-ms` (трёхсостоянный walk Dv488 в коде, сим на argv-шве),
preflight-инструмент H1 (`make preflight`, реестр 1 466 пинов; дважды поймал
сам себя и нашёл устаревший пин самого гарда → MOVED-манёвр). **Аддендум по
рулингу оператора («закрой три сходящихся под 7%»): 45h2 / srv2b / srv2d
ЗАКРЫТЫ** ($8.2176 / $0.9400 / $1.1632 при 6.92 / 5.99 / 5.68% в полосе 7%);
каверза пред-оконных трат записана ВНУТРИ записей ($0.4204 / $0 / $0.0104);
append-only и пере-вывод цифр подтверждены свежим субагентом (AGREE); суита
ПОСЛЕ записей 2927/2. **Рулинг зарегистрирован этой записью STATUS.**
Пилоты: H4 — enum v2 **18/18** каноничных (база ~30%); H7 — split-талли:
contract-health 11 · paid lessons 7, все находки за $0; H1/H6 — сработали.

**Денежные долги после guard-until:** `pass1-probe` — walk ЗАВИС на 300 000 из
427 000 мс (>12 ч без движения; если биллинг не допостит — нужен рулинг);
`pass1-probe-b` — walk ПОЛНЫЙ (+0.104%), но закрытие отказано ЧЕСТНО: правило
сверяет с записанным чтением леджера ($0.0853), а оно отстающее (64.9% правды,
Dv504) → нужен рулинг о правой части закрытия; 19 старых леджеров отказаны по
именованным причинам (нет `anchored_at` / нет окна / ключ якоря не матчится).
Форма следующего денежного контракта: дисциплина `anchored_at` + правая часть.
Открытые чтения дрейфуют на томе — это капель, не перерасход.

**`pass1-data-prep` исполнен и ПРИНЯТ 18.08 (ночь)** (отчёт
`docs/reports/pass1-data-prep.md`, Dv518–523; suite 2927→**2959/2** — свой
прогон тимлида). Пак разметки: **500 целей** под seed 20260818 из 968
оставшихся платных (1 032 − 64; пробные юниты оказались РОВНО платным
множеством своих 7 тредов — вычитание, не оценка); кап на тред 14 (p90;
достижимость 572 ≥ 500 посчитана ДО регистрации — Dv520: гейт прошёл по трём
константам, а думать пришлось о четвёртой, которую никто не регистрировал);
контаминация доказана четырьмя пустыми списками + независимый греп тимлида
по страницам (0 пробных тредов, 0 голд-id); кодбук — закон v5 ТЕМИ ЖЕ
БАЙТАМИ из prompts.py + F2a + рулинги r2, с купюрой Dv519 (reading рулинга r2
называет голд-строку и цифры бара — вырезано на литеральном маркере, два
теста стерегут страницы); валидатор меток red-first (6 отказов, «ничего не
пишет» доказано по исходнику); слепые 40 отрендерены без ключа. Два ADR
(сидение B; guard-until) в INDEX. Enum: 6/6. **Разметка — за тимлидом:**
`docs/label-pack-pass1-r1.md` → `docs/labels-pass1-r1.jsonl` (файл тимлида,
исполнитель коммитит дословно).

**Разметка ВЫПОЛНЕНА тимлидом 18.08 (ночь); 19.08 ЗАПЛОМБИРОВАНА
(`8d2e1ba`).** Распределение: не_наш_рынок 251 · null 167 · сеть_ритейлер 44 ·
категория_личное 36 · молочный_бренд 2. СИГНАЛ для LoRA-брифинга: «наших»
классов мало (38 строк категория+бренд) — окно-1 доминируется маркетплейс/
рецептными тредами; варианты (взвешивание · доп-розыгрыш из «молочных» ·
синтетика с provenance+аблацией) НЕ решены — на регистрацию.

## День 19.08 — vault-dream и labels-boot-audit ПРИНЯТЫ; оба рулинга исполнены

**`vault-dream` ПРИНЯТ** (`docs/reports/vault-dream.md`, Dv524–531, $0;
верификация тимлида по диску). Потолок MEMORY.md — из констант самого
загрузчика (200 строк / 25 000 UTF-16-юнитов, «25KB» шапки было неправдой);
индекс 197→147 строк, обе оси <75%, 51 ссылка вытеснена с прочитанным домом,
186 файлов уроков целы; boot tax 14.5K→13.1K, ≤9K недостижим в scope. Талли:
contract-health 6 · paid 2, $0; спайк дал правило таймстемпов в шаблон
промпта — в следующем контракте класс исчез.

**Оба рулинга 19.08 ИСПОЛНЕНЫ контрактом labels-boot-audit; их ADR:**
`the-absence-test-is-a-clock-and-flips-with-its-artifact` (тест на отсутствие
планового артефакта — часы, не верификатор; переворот — в коммите артефакта)
и `boot-tax-target-suspended-and-the-census-axis` (≤9K приостановлен, hot.md
ради числа не режем, ось census = ось загрузчика; до пере-регистрации
«> 9.0K» census читается как «цель на пере-регистрации»).

**`labels-boot-audit` исполнен и ПРИНЯТ 19.08**
(`docs/reports/labels-boot-audit.md`, Dv532–536, $0; suite 2 959→**2 966/2 —
свой прогон тимлида**). Метки ЗАПЛОМБИРОВАНЫ атомарным коммитом `8d2e1ba`:
500 строк дословно, sha 776b204f… тремя чтениями (рабочее дерево =
results-копия = байты из git), валидатор зелёный на РЕАЛЬНОМ файле,
provenance-сайдкар = пин (реестр префлайта 1 469→1 472, по пути 5/5 digests);
негативный контроль: перевёрнутая метка ловится тремя гардами. Ось census
(Dv526): дельта −1 Б, ПРЕДСКАЗАННАЯ до прогона; 6 гардов пинуют 200/25 000 и
«байты, не юниты». hot.md — ровно две правки, sealed-блоки целы. Свежий
контекст-субагент (тронуты гарды — триггер): печать держит одиночный дрейф
любого из трёх файлов; два дешёвых усиления, НЕ блокирующих — шагом 0.5:
(а) тест сайдкара не проверяет сами `file`-пути блоков record'а, (б) strip()
Python ≠ trim() JS на BOM/NEL (экзотика для авто-файла). Талли: contract-
health 3 (c-gap 2 · v-gap 1) · paid 2 (process ×2), $0 — спайк закрыт.
Прозовый дрейф hot.md (деньги vs гард, счёт пинов — Dv536) — след. контракту.

**СОВМЕСТНАЯ СЕССИЯ СОСТОЯЛАСЬ 19.08, рулинг оператора: вытесняем ВСЁ, группы
A–E** (A дубли записей ~8.4K Б · B копии чисел ~2.2K · C день-сводка · D
CLAUDE.md → paths:-правила ~3.1K · E Dev Stack из ~/CLAUDE.md — парковка вне
загрузки, ни один проект его не грепает). Цель boot tax — ПРЕ-РЕГИСТРИРОВАННАЯ
ФОРМУЛА: TARGET_KTOK = измеренный пол де-блоата × 1.1 (проекция пола ≈9.6K →
цель ≈10.5K); число ложится на приёмке `boot-debloat`, НЕ раньше. Не трогаем:
блокеры, ТЫ-ЗДЕСЬ, ЦЕНА прохода-1, Ячейка ценза, sealed-блоки, AUTO-GEN.

## Очередь контрактов

1. ✅ ПРИНЯТЫ: `guard --until` · `pass1-data-prep` (18.08) · `vault-dream` ·
   `labels-boot-audit` (19.08) — см. дни выше.
2. **boot-debloat** ($0, СЛЕДУЮЩИЙ — контракт `docs/PROMPT-boot-debloat.md`
   НАПИСАН): шаг 0.5 — усиления гардов (пути сайдкара; strip/trim=BOM);
   D1 — вытеснение A+B+C из hot.md (дом ЧИТАЕТСЯ, не грепается;
   mapping-таблица; одна строка-указатель); D2 — CLAUDE.md → paths:-правила;
   D3 — парковка Dev Stack (снапшот, дословно, ничто не грузит); D4 —
   TARGET_KTOK = пол×1.1 ПОСЛЕДНИМ шагом + ADR рулинга сидения. После
   приёмки — LoRA-брифинг (решение §1 повестки).
3. **prep-b** — коллекция Сільпо/Varus/Маркетопт ($0, контракт согласован) —
   после читательского тракта (слово оператора 16.08).

**Сидение B/C СОСТОЯЛОСЬ 18.08, рулинг оператора: линия B, разметку делает
ТИМЛИД.** Рамка честности (судья/gold r2 не двигается · метки тимлида =
тренировочные, тегируются и аблируются · контаминация исключена с
доказательством · кодбук = закон v5 + F2a + рулинги r2 · объём ~500) —
исполнена data-prep'ом и печатью 8d2e1ba; полный текст — ADR
`sitting-b-line-b-and-the-team-lead-labels`. Слепые 40 (~30 мин твоего
времени) — опция за оператором, НЕ блокирует.

## Деньги (чтение гарда тимлидом на приёмке 18.08)

Фаза 4 ЗАКРЫТА $32.4708 из $33.00 (финальное чтение 16.08, не растёт).
**Цикл-2: $1.8243 из $20.00, остаток $18.1757; баланс $20.69** (чтение на
приёмке guard-until, поздний вечер 18.08). Внутри — все
пять попыток читателя/прохода-1 и капель тома (~$0.0116/ч, всегда отдельной
строкой). Влезает всё решённое (окно-2 ≈$4.8 · проход-1 окна $1.1 ПОЛ ·
батч-пробник $2.13 · том ≈$2/нед); история 11 143 — нет. Деньги читаются ИЗ
ГАРДА (`scripts/runpod_guard.py`), не из этого файла и не из отчётов.

## Открытые решения (повестка брифинга)

1. **LoRA-регистрация линии B** (после разметки): конфиг тренировки, кап и
   прогноз цены ОТ ЗАМЕРОВ фазы 4 (H6-формулы, не выдумки), бар — тот же
   запечатанный gold r2 ≥12/14; отдельный брифинг. Само сидение B/C РЕШЕНО
   18.08 — рулинг и рамка честности в «Очереди контрактов».
2. **Денежная гигиена (рулинги + контракт `money-anchors`, $0):** правая часть
   закрытия шага (записанное чтение отстаёт от биллинга — Dv504; чем сверять
   settled?), судьба зависшего walk'а pass1-probe (300k/427k мс), дисциплина
   `anchored_at` для 19 отказанных леджеров. После data-prep, словом оператора.
3. **Дизайн командного центра** — отвергнут оператором 15.08; Design-плагин не
   установлен; направления на выбор — после установки.
4. Мелочи step-0.5 (ждут контракта, трогающего их файлы): заголовок `t5_depth`
   (бейдж, не пара цен), валюта ₴ в шапке промо-таблицы, fix-b(2) «две
   колонки — одно число».

## Живые рулинги читательского тракта (сидения 15–17.08, сжато)

- Эталон цели: `docs/REFERENCE-signals-w1.md` (ручной разбор тимлида 15.08:
  51 кандидат → ~дюжина тредов с сигналом); «система должна выдавать
  аналогичный ответ».
- Популяция тракта читателя окна-1: **129 тредов / ~1 032 платных коммента**
  (varto-глушитель снят для ТРАКТА ЧИТАТЕЛЯ; гейт оплаты классификации и
  бренд-атрибуция r1 не тронуты).
- Голд: ревизия **r2** (референс не правится; рулинг «категория» =
  «категория_личное» — отдельной записью; пины не двигаются зря).
- Транспорт (выстрадан v3→v5b, живёт в коде и суите): один JSON-объект, `[]`
  для пустого; расхождение двух top-level объектов = отказ; раннер режет
  генерацию на первом сбалансированном объекте; эхо msg_id из запроса,
  недостача печатается рядом с барами; потолок вывода 4000 (один на обе ноги);
  gate-0 «ssh жив к 180 с или KILL», ≤2 пересозданий, третий мёртвый под =
  STOP; лог пода scp'ится вместе с jsonl перед каждым гейтом; gate-записи —
  только append; поды, не serverless (рулинг 16.08: холодный старт 20 мин
  съел кап v3).
- F2a-carve-out (рулинг 17.08): событие о доступности/статусе предмета поста —
  сигнал о предмете поста; мнения без имени не переносятся.
- Рулинг «пак по убыванию payable» ОТОЗВАН 17.08 до всякого пода (гейтова
  таблица показала STOP на юните 1; урок: рулинг-фикс прогоняется через гейт,
  которому служит, ДО регистрации).
- **Однопроходная промпт-линия ЗАКРЫТА stop-rule'ом** (пре-регистрирован в v5,
  сработал на завершённом v5b: бар-1 2/5, бар-4 0.4286); ADR закрытия
  связывает тимлида; проход-1-классификатор = «two passes» из самого правила,
  не «v6».

## Отложено СОЗНАТЕЛЬНО (что · критерий развязки)

- **Serving-оптимизация (латенси)** — разбор 16.08 (git-хроника): холодный
  старт serverless ~239 с + декод ~5 ток/с — принятая цена отладки; критерий
  развязки: слово оператора «готовим прод» (ожидаемо — вход Фазы 7). Пути
  лечения известны (пре-квант база → старт ~1 мин; merge+vLLM новой
  прережкой → строка <1 с).
- **История 11 143 строк** — ждёт батч-решения И денег; вотермарк-люк известен
  (очередь читает 0; контракт истории сам сбрасывает поле `inference`).
- **Батч-паритет** — прережка пишется вместе с решением гнать историю (там
  окупается: $4.35 при −30%); проекция в
  `results/batch_cycle2_projection.json`.
- **5c3: категорийный слой** (таксономия 13+2 утверждена, механика не
  строена), сайдкар v2 (реакции/просмотры).
- **G1a/G1c: новая фаза качества или потолок принят** — на свежих данных
  цикла; сцеплено с судьбой читательского тракта (стенс-слой может изменить,
  что нужно от классификационных голов).
- **Алерты (вопрос 6)** — дизайн порогов словом оператора + нужно окно-2 для
  базовой линии. **Тренды** — окно-2. **Продажи ТПК** — по слову собственника,
  архитектура (SQLite по окнам) готова принять.
- **Мёрдж адаптера в bf16** — только после замера в прод-конфигурации.
- **LLM-нарратив отчёта (5d)** — модель выбирается на живых агрегатах.

## Правила, которые легко забыть

- Все eval гейтов — батчом 1; v2/v4-голды и запечатанные вердикты неизменны.
- Числа — только из result-файлов через скорер; в презентационные артефакты
  руками не вписываются (закон 3.20, отравленный тест).
- **Деньги читаются из гарда, не из леджера и не из прозы** (Dv33); у шагового
  чтения пока нет верхней границы окна — чинится контрактом «--until».
- **Прережка замерзает при первом `pod create` попытки**; правка после —
  только новой регистрацией; кап живёт в регистрации, его поднятие — слово
  оператора ДО эндпоинта.
- **Пины — потребители:** `prompts.py`, `brands.py`, `local_llm.py`,
  `window_summary_5c2.py`, PLAN-файлы пинуются запечатанными записями; правка
  требует MOVED-манёвра, не перепина. Префлайт любого контракта: греп пинов
  на всё, что он трогает (с «--until» — через preflight-инструмент H1).
- `registry.yaml` и `lexicon.yaml` запечатаны — правила матчинга в
  `config/watchlist_rules.yaml`; правки только новыми ревизиями.
- Никогда `git add -A`; файлы тимлида исполнитель коммитит, не правит;
  отчёты — файлами в `docs/reports/`, в чат — только путь.
- Впрыснутые треды проб (`injected: true`) никогда не входят в
  продакшн-агрегаты и цену окна.
- Пре-регистрации не переоткрываются; провал закрывает вопрос (one attempt);
  сработавший stop-rule закрывает ЛИНИЮ, не попытку.

## Урок недели (ретро 18.08)

Узкое место — тимлидский ярус (P1–P7, ревью ретро). Пилоты СРАБОТАЛИ: H1/H6
(«--until»), H5b (vault-dream), H4 (enum 18/18 →6/6 →8/8 →5/5), таймстемпы
базлайнов (спайк 6→3). H5a/H7 и split-талли фазы — на закрытии фазы 6.
