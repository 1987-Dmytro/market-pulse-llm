# STATUS — market-pulse-llm (2026-08-26, вечер; карта после перепланирования)

> Операторский документ, по-русски. Правит только тимлид, после приёмок. Хроника фаз 0–6 и дней
> 15–26.08 — в git-истории этого файла, в `docs/reports/`, `docs/reviews/`, ADR. Здесь — карта,
> Definition of Done, деньги и живые решения. Потолок этого файла — 120 строк (диета 26.08).

## Что это за проект (одним абзацем)

Инструмент маркетинговых исследований ТМ «Гармонія» (`docs/PRODUCT.md` — семь вопросов директора):
мониторинг Telegram-каналов украинского ритейла, разметка реакций покупателей (тональность, сарказм,
скрытые интенции, бренды) дообученной открытой LLM (Gemma-4-31B + QLoRA-адаптер 4.5h2), агрегаты →
командный центр. Вторая цель — витрина навыков дообучения (LinkedIn): «база ошибается на сарказме,
дообученная — нет», с честными числами и хроникой. GPU только арендные; между сессиями консоль пуста.

## Карта фаз — ГДЕ МЫ

| Фаза | Что | Статус |
|---|---|---|
| 0–4.5 | Спека · скелет · данные · бейзлайны · QLoRA 4.5h2 · программа качества | ✅ (Фаза 4 закрыта 05.08, $32.47) |
| 5 | Продакшн-петля, окно-1 куплено целиком | ✅ 14.08 |
| 6 | Командный центр v1 (phase6b, 9 вкладок) + слой сигналов (проход-1 v2 + проход-2, измерен 22.08) | ✅ закрыта 26.08 рулингом (у): сигналы шипятся ПРОВИЗОРНО; **линии lora-b (20.08) и lora-c (26.08) ЗАКРЫТЫ stop-rule'ом** |
| **6t** | **Слой сигналов: замер Gemma-4 с thinking ON против купленных чтений (рулинг (ф))** | **🔄 ТЫ ЗДЕСЬ — контракт `think-zero-shot` выдан 26.08 вечером; решение о пути — на его таблице** |
| 7 | Упаковка (DoD ниже) + окно-2 по слову оператора | ждёт решения по 6t; `phase7-a1` ($0, закрытие lora-c + README) — по слову оператора |

## Модель сейчас (деливерабл) — числа из `results/verdict_45h2.json` / `results/baselines.json`

Gemma-4-31B NF4 + QLoRA 4.5h2 (арм without-plast): G1a тональность macro-F1 **0.9214** (TF-IDF 0.683 ·
XLM-R 0.782 · Qwen3.6-27B zero-shot 0.854 · Haiku-4.5 референс 0.871); G1b сарказм **23/38** выправлено
(бар 60% взят); G1c интенты 0.8478 (FAIL на 0.0005); G1d тип поста 0.9586; G1e бренды 0.9610.
Сигналы из комментов — «гибрид D»: проход-1 v2 (базовый промпт, рулинг (а)) + проход-2 под строгой
властью; три бара 22.08 красные по атрибуции прохода-1 — названо, не чинится обучением.

## Definition of Done фазы 7 (рулинг (у), 26.08) — галочки ставит тимлид на приёмке

| # | артефакт | готово |
|---|---|---|
| A1 | README с таблицей результатов из `results/` (`make readme`, падает при отсутствии источника) | ☐ контракт `phase7-a1` |
| A2 | Демо сарказма: строки слайса `g1b_slice_v4` (база ошиблась → адаптер выправил), под сидом; из СУЩЕСТВУЮЩИХ дампов (`predictions/…20260804T122638Z` = база; `srv2d-volume-rowdump--sarcasm_holdout` = адаптер) — $0 | ☐ |
| A3 | Дашборд: скриншоты 7 вопросов с окна-1 + walkthrough, EN-режим «как построено» | ☐ |
| A4 | Карточка датасета (источники, объёмы, разметка, замороженные тесты, лицензии) | ☐ |
| A5 | «Что мы узнали» — честная хроника, линии B/C как отрицательный результат, деньги, stop-rules | ☐ |
| A6 | Воспроизводимость: `make` от сырых данных до таблицы; версии, сиды; прогон на чистом клоне | ☐ |
| B | Командный центр v1 для заказчика — есть; окно-2 (тренды, базовая линия алертов, $4.8) — по слову оператора | ☐ |

## Деньги (читаются ИЗ ГАРДА; чтение тимлида 26.08 после r3)

Фаза 4 закрыта $32.4708 из $33.00. **Цикл-2: $10.4921 из $20.00, остаток $9.5079.** Линия lora-c
итого $1.123 (r2 $0.7606 · проба $0.0752 · миграция $0.287 · r3 $0.4448 — всё по часам подов).
Тома: `mp-srv2` (EU-RO-1, сервинг, ОСТАЁТСЯ) + `mp-lora-c` (CA-MTL-3, **удаляется в `phase7-a1`**,
доказательство — листинг). Витрина A1–A6 стоит $0 (все дампы есть); окно-2 $4.8 — отдельное слово.
Долги денег: `money-anchors` ($0) — settled-закрытия, зависший walk pass1-probe, 19 без `anchored_at`.

## Живые рулинги (полные тексты прежних — git этого файла до 26.08)

- **(ф) 26.08, вечер — ПРИОРИТЕТ: СЛОЙ СИГНАЛОВ.** Система бесполезна, пока не находит сигналы в
  комментах и тредах на уровне ручного разбора тимлида; **только локальная Gemma-4-31B, thinking
  ОБЯЗАТЕЛЬНО**; API-модели отклонены. Находка: `enable_thinking: false` стояло во ВСЕХ запусках
  проекта (v5b, проход-1 v1/v2, проход-2, пробы, обе LoRA-линии) — читатель с thinking ON ни разу
  не измерен; регистрации сами пишут «thinking-ON reader = НОВАЯ регистрация». **Первый контракт —
  `think-zero-shot`:** те же промпты, те же строки, thinking ON — парная таблица против купленных
  чтений (dev-200 v1 87 · v2 136 из 200; «наши» 31/38 из 49; холдаут 64/100; бары прохода-2
  4/5 · 3/4 · красный), стадии по ценности, кап $8.00, ASK-рунг. Решение о дальнейшем пути — на
  этой таблице. Дашборд признан пустым для директора: агрегаты без «что говорят» + поток окна-1 почти
  без брендовых мнений (2 из 500 размеченных) — перестройка центра и ценз источников после 6t.

- **(у) 26.08 — ПЕРЕПЛАНИРОВАНИЕ** (`docs/reviews/2026-08-26-replan-and-process-diet.md`, ретро
  `…lora-c-week-retro.md`): (1) lora-c закрыта stop-rule'ом — попытка прережки `prereg_lora_c.json`
  остаётся НЕПОТРАЧЕННОЙ, файл не правится, закрытие — сайдкар + ADR; переоткрытие только после
  разметки ≥300 позитивов; (2) DoD A1–A6 + B утверждён; (3) остаток цикла — не на обучение;
  демо A2 из дампов за $0; (4) **диета процесса**: единица работы — артефакт DoD, одна фича на
  сессию; контракт ≤50 строк, отчёт ≤80; без чекеров на отчёт, без ADR без решения; платный контракт
  — четыре рунга (цена · живость · проекция с ASK-рунгом · хард-стоп), кап 2× оценки; STATUS ≤120,
  хэндофф ≤30; `results/measurements.jsonl` — реестр физических констант; скилл — дистилляция ≤400.
- **(0) 20.08:** база не меняется — Gemma-4-31B; разметку делает тимлид. **(а) 21.08:** v2 — базовый
  промпт прохода-1; проход-2 — не «однопроходный читатель» (ADR). **(к) 22.08:** дизайн lora-c —
  исполнен до r3, закрыт (у). **(л)(м) 23.08:** SPEC 3.25/3.26 — потолок 3 072 (стоит как факт).
- Стоп-рулы, связывающие тимлида: однопроходный промпт-читатель (17.08), линия B (20.08), lora-c (26.08).

## Реестр измерений (пока здесь; переезжает в `results/measurements.jsonl` контрактом a1)

A100 PCIe 80 GB $1.39/ч CA-MTL-3: **89.961 с/шаг** при 3 072/mb2/ga8, пик **49.54 GiB**; загрузка
модели 40.22 / 138.49 / 13 с (тёплый/холодный/кэш); venv 132 с; закачка 62.58 GB за 184 с.
A6000 48 GB при 1 408: 61.047 / 68.442 с/шаг, пик 33–35 GB (`results/train/*/provenance.json`).
32 GB (PRO 4500) — OOM на 3 072 при mb1, дефицит структурный. Инференс: v3 9.20 с/звонок (4090),
v1/v2 6.14; проход-2 97 с/тред; сервинг srv2d $1.43 / 1 000 строк. EU-SE-1 (и 4 ДЦ пробы) — без
сетевых томов; walk биллинга отстаёт на 30–40 мин.

## Отложено СОЗНАТЕЛЬНО (что · критерий развязки)

- Окно-2 (тренды, алерты, базовая линия) — слово оператора по деньгам после A1–A2.
- prep-b (Сільпо/Varus/Маркетопт, $0) — после витрины. Serving-латенси, мёрдж в bf16, история
  11 143 строк, батч-паритет, 5c3 категории, G1a/G1c новая фаза — как прежде, по слову оператора.
- Дистилляция скилла team-lead (705 → ≤400) и компакция вольта — отдельная сессия ПОСЛЕ приёмки a1.
- Адъюдикация референса (E1 прохода-2; S1 в N1) — только если заказчик B попросит.
- Мелочи step-0.5: `t5_depth`-бейдж · ₴ в шапке промо · fix-b(2) · кириллица сетей в registry (Dv732)
  · Dv541/536/521 · index lines · blind-40 · формат-дрифт двух файлов при запиненном продюсере.

## Правила, которые легко забыть

- Числа — только из result-файлов через скорер/билд; в README и дашборд руками не вписываются
  (закон 3.20). Каждое число в отчёте называет файл, откуда взято.
- Деньги — из гарда (Dv33); часы пода — число для цитаты; walk отстаёт. Платный под не оставляют
  без опроса; кап — 2× оценки из реестра; перерасход ≤20% → ASK-рунг, не KILL.
- Пре-регистрации не переоткрываются; провал закрывает вопрос; stop-rule закрывает ЛИНИЮ.
- Файлы тимлида (STATUS/SPEC/PROMPT-*/`docs/labels-pass1-r1.jsonl`/`docs/reviews/`) исполнитель
  коммитит, не правит. Никогда `git add -A`. `make fmt` репо-целиком запрещён (пин продюсера).
- Пины — потребители: `prompts.py`, `scorer.py`, `brands.py`, `local_llm.py`, `window_summary_5c2.py`,
  PLAN-файлы; `registry.yaml`/`lexicon.yaml` запечатаны — правки новыми ревизиями. `make preflight`
  перед контрактом, трогающим пины.
- Новый гейт/тест/рунг называет потерю, которую предотвращает; контракт — пункт DoD; иначе не выдаётся.
- Отчёт — файл в `docs/reports/`, в чат — только путь; приёмка фичи — в тот же день.

## Урок недели (ретро 26.08)

Три из шести суточных остановок линии lora-c устранялись чтением файлов, уже лежавших в репо
(пик 35 GB при 1 408; прайор с/шаг на вдвое коротких строках; отсутствующий датасет) — все на ярусе
тимлида. Лекарство — реестр измерений и правило «физические потребители сдвинутой константы», а не
новый рунг. Талли линии: contract-health 72 · paid 44.
