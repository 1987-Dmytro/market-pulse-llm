# STATUS — market-pulse-llm (2026-08-27; карта после переспеки — этап 1 «промо-пульс сетей»)

> Операторский документ, по-русски. Правит только тимлид, после приёмок. Хроника фаз 0–6 и дней
> 15–26.08 — в git-истории этого файла, в `docs/reports/`, `docs/reviews/`, ADR. Здесь — карта,
> Definition of Done, деньги и живые решения. Потолок этого файла — 120 строк (диета 26.08).

## Что это за проект (одним абзацем) — ПЕРЕФОРМУЛИРОВАНО 27.08 (рулинг (х))

Инструмент маркетинговых исследований ТМ «Гармонія» по украинскому Telegram (`docs/PRODUCT.md`).
**Замер 27.08 по сырому стору (27 662 коммента, 18 мес.): потребители называют молочные бренды
watchlist ≈ в 40 комментах, Гармонію — в 0; зато 2 102 промо-поста сетей с брендами и ценами и
1 092 коммента под ними.** Поэтому этап 1 (`docs/SPEC-v2-promo-pulse.md`) — **«промо-пульс сетей»**:
максимум торговых сетей, цены и глубина скидок по молочке и мороженому, реакции покупателей под
промо-постами; тренды/алерты по брендам — трек роста источников. Работает непрерывной петлёй с
расписанием из UI. Вторая цель — витрина навыков (LinkedIn) — не гейт. GPU арендные.

## Карта — ГДЕ МЫ (этап 1, SPEC v2 §8)

| # | Контракт | $ | Статус |
|---|---|---|---|
| — | Фазы 0–6 (сбор · адаптер 4.5h2 · позиции 5c2 · центр v1) и 6t (thinking +0) | — | ✅ закрыты; их числа ниже |
| C1 | `retail-census` — ценз сетей и агрегаторов, entry-check, таблица → выбор оператора; шаг 0: удалить `mp-lora-c` | 0 | **🔄 ТЫ ЗДЕСЬ — промпт готов, ждёт «go»** |
| C2 | `positions-scale` — позиции по выбранным сетям за 4 недели, бар S1 | ≤1 | ждёт C1 |
| C3 | `promo-comments-prep/-dev` — атрибуция + сигналы под промо, dev-40, эталон тимлида | 0 → ≤5 | параллельно: разметка тимлидом |
| C4 | `trends-sql` + дайджест/инкремент, `make tick` | 0 | ждёт C2–C3 |
| C5 | `promo-screen` — экран из result-файлов, гейт оператора на 20 строках под сидом | 0 | закрывает этап 1 |
| C6 | фронт: петля + расписание в UI | слово | после C5 |

## Что доказано и остаётся в продукте — числа из result-файлов

Адаптер 4.5h2 (`results/verdict_45h2.json`): тональность macro-F1 **0.9214**, сарказм 23/38 выправлено,
аспекты 0.8478, тип поста 0.9586, бренды 0.9610 — разметка комментов, работает. Позиции 5c2
(`results/run_5c2_positions.json`): 145 позиций из 106 страниц листовок + 39 текстов, 1.7 с/стр.
Сигналы в брендовой постановке — **закрыты рулингом (х)**: проход-1 68%, бары прохода-2 красные,
thinking +0 (`results/think_zero_shot_table.json`); перечитываются под S2 (комменты под промо, контекст поста).
Закрытые линии: однопроходный читатель (17.08), lora-b (20.08), lora-c (26.08), think-zero-shot (27.08),
харнес v1 в брендовой постановке (27.08). Витрина A1–A6 (рулинг (у)) — параллельно, $0, не гейт.

## Деньги (читаются ИЗ ГАРДА; чтение тимлида 26.08 после r3)

Фаза 4 закрыта $32.4708 из $33.00. **Цикл-2: $16.5885 из $20.00, остаток $3.4115** (гард 26.08 22:07Z).
Линия lora-c итого $1.123 (r2 $0.7606 · проба $0.0752 · миграция $0.287 · r3 $0.4448). think-zero-shot D2:
**$5.84 по часам пода** (8 ч 06 м 35 с × $0.72), дельта гарда $6.02 (тома). Полная программа D2 стоила бы $20.87 —
не покупается в цикле-2; недокупленные стадии (v1, холдаут, 67 тредов) не несут баров.
Тома: `mp-srv2` (EU-RO-1, сервинг, ОСТАЁТСЯ) + `mp-lora-c` (CA-MTL-3, **удаляется в `phase7-a1`**,
доказательство — листинг). Витрина A1–A6 стоит $0 (все дампы есть); окно-2 $4.8 — отдельное слово.
Долги денег: `money-anchors` ($0) — settled-закрытия, зависший walk pass1-probe, 19 без `anchored_at`.

## Живые рулинги (полные тексты прежних — git этого файла до 26.08)

- **(х) 27.08 — ПЕРЕСПЕКА, путь В.** Оператор увидел числа источников (0 комментов про Гармонію за
  18 мес.) и постановил: этап 1 — исключительно цены и комменты к промо сетей, перечень сетей
  расширить максимально; продукт строится на том, что данные дают, рост источников — внутри петли;
  петля непрерывная, расписание в UI; эталон размечает тимлид. Спека — `docs/SPEC-v2-promo-pulse.md`.
  Мердж адаптера/vLLM — вне пути этапа 1 (скорость не блокирует: 31 тред/день), отдельная регистрация
  по деньгам; `mp-lora-c` удаляется (тома тянут ≈$0.53/день при балансе $5.71).
- **(ф) 26.08, вечер — слой сигналов, брендовая постановка — ЗАКРЫТ (х), переписан под S2.** Система бесполезна, пока не находит сигналы в
  комментах и тредах на уровне ручного разбора тимлида; **только локальная Gemma-4-31B, thinking
  ОБЯЗАТЕЛЬНО**; API-модели отклонены. До D2 `enable_thinking: false` стояло во ВСЕХ запусках проекта.
  **`think-zero-shot` куплен (D2, 26.08):** те же промпты и строки, thinking ON — парная таблица
  `results/think_zero_shot_table.json` / `docs/reports/think-zero-shot.md`: +0 на dev-200, бары без
  изменений, ×40/×17 по времени. **Решение о пути — на этой таблице после чтения оператором.**
  Дашборд признан пустым для директора: агрегаты без «что говорят» + поток окна-1 почти без брендовых
  мнений (2 из 500 размеченных) — перестройка центра и ценз источников после 6t.

- **(у) 26.08 — ПЕРЕПЛАНИРОВАНИЕ** (`docs/reviews/2026-08-26-replan-and-process-diet.md`, ретро
  `…lora-c-week-retro.md`): (1) lora-c закрыта stop-rule'ом — попытка прережки `prereg_lora_c.json`
  остаётся НЕПОТРАЧЕННОЙ, файл не правится, закрытие — сайдкар + ADR; переоткрытие только после
  разметки ≥300 позитивов; (2) DoD A1–A6 + B утверждён; (3) остаток цикла — не на обучение;
  демо A2 из дампов за $0; (4) **диета процесса**: единица работы — артефакт DoD, одна фича на
  сессию; контракт ≤50 строк, отчёт ≤80; без чекеров на отчёт, без ADR без решения; платный контракт
  — четыре рунга (цена · живость · проекция с ASK-рунгом · хард-стоп), кап 2× оценки; STATUS ≤120,
  хэндофф ≤30; `results/measurements.jsonl` — реестр физических констант; скилл — дистилляция ≤400.
- **(0) 20.08:** база не меняется — Gemma-4-31B; разметку делает тимлид. **(а) 21.08:** v2 — базовый
  промпт прохода-1; проход-2 — не «однопроходный читатель» (ADR). **(к) 22.08:** дизайн lora-c —
  исполнен до r3, закрыт (у). **(л)(м) 23.08:** SPEC 3.25/3.26 — потолок 3 072 (стоит как факт).
- Стоп-рулы, связывающие тимлида: однопроходный промпт-читатель (17.08), линия B (20.08), lora-c (26.08).

## Реестр измерений (пока здесь; переезжает в `results/measurements.jsonl` контрактом a1)

A100 PCIe 80 GB $1.39/ч CA-MTL-3: **89.961 с/шаг** при 3 072/mb2/ga8, пик **49.54 GiB**; загрузка
модели 40.22 / 138.49 / 13 с (тёплый/холодный/кэш); venv 132 с; закачка 62.58 GB за 184 с.
A6000 48 GB при 1 408: 61.047 / 68.442 с/шаг, пик 33–35 GB (`results/train/*/provenance.json`).
32 GB (PRO 4500) — OOM на 3 072 при mb1, дефицит структурный. Инференс (thinking OFF, из
`results/measurements.jsonl`, D1 26.08): проход-1 v1 2.293 с/звонок (max 2.97), v2 2.726 (max 4.07),
окно r2 2.694 на 901 строке; **проход-2 r2 23.76 с/тред в среднем, max 135.2** (прежние «97 с/тред»
были ошибкой памяти, ×4.1). Сервинг srv2d $1.43 / 1 000 строк. EU-SE-1 (и 4 ДЦ пробы) — без сетевых
томов; walk биллинга отстаёт на 30–40 мин. **Thinking ON (D2, PRO 4500 32 GB):** проход-1 **110.18 с/звонок**
(n=200, max 380), мысль 1 130 токенов в среднем (2 строки упёрлись в потолок 4 000); проход-2 **395.7 с/тред**
(n=11, max 732), мысль 3 511; загрузка модели 152.7 с; пик VRAM 31.17 из 32 GiB. ≈$22 / 1 000 строк.

## Отложено СОЗНАТЕЛЬНО (что · критерий развязки)

- Окно-2 (тренды, алерты, базовая линия) — слово оператора по деньгам после A1–A2.
- prep-b (Сільпо/Varus/Маркетопт, $0) — после витрины. Serving-латенси, мёрдж в bf16, история
  11 143 строк, батч-паритет, 5c3 категории, G1a/G1c новая фаза — как прежде, по слову оператора.
- Долг ($0, шаг 0 следующего контракта): `test_repair_phase4_ledger` — silence-check на
  `spend_cycle2.json` красный после r3 (`make check`: 1 failed, 4063 passed). `test_lora_c_prep` — зелёный после D2.
- `think-adapter` (адаптер 4.5h2 с thinking ON) — ПОСЛЕ гейта D2: при +0 на zero-shot и ×40 по цене
  ценность замера упала; решение оператора вместе с путём. Инструмент — $0, сервинг-конфиг.
- Адъюдикация референса (E1; S1 в N1) — если заказчик попросит. Мелочи step-0.5: `t5_depth`-бейдж · ₴ в промо
  · fix-b(2) · кириллица сетей (Dv732) · Dv541/536/521 · index lines · blind-40 · формат-дрифт двух файлов.

## Правила, которые легко забыть (механика — `docs/PROCESS.md`)

- Числа — только из result-файлов через скорер/билд, каждое называет файл (закон 3.20). Деньги — из
  гарда; час пода — число для цитаты, гард рядом; кап 2× оценки; перерасход ≤20% → ASK, не KILL.
- Пре-регистрации не переоткрываются; провал закрывает вопрос; stop-rule закрывает линию.
- Файлы тимлида исполнитель коммитит по пути, не правит. Никогда `git add -A`, никогда `make fmt` репо-целиком.
  Пины и запечатанные записи — `make preflight` до контракта, который их двигает.
- Новый гейт/тест/рунг называет потерю, которую предотвращает; контракт — пункт DoD; отчёт — файл в
  `docs/reports/`, в чат только путь; приёмка — в тот же день.

## Уроки (ретро 26.08 — `docs/reviews/2026-08-26-lora-c-week-retro.md`; D2 27.08)

Три из шести остановок lora-c снимались чтением файлов, уже лежавших в репо — лекарство: реестр
измерений, не новый рунг. D2: ошибки прохода-1 — не ошибки рассуждения, а дыры кодбука и нехватка
контекста (три строки прочитаны руками: `#21181` исправлена, `#21576` «страшные» → null по примеру из
промпта, `#21177` 4 000 токенов кругами между тремя классами). Думать дольше над той же неполной
информацией не помогает. Итерации/день: D1+D2 — 2 контракта за 26.08.

<!-- MACHINE-READ BLOCK — quoted verbatim by sealed producers (`scripts/write_lora_c_prereg.py :: quoted()`).
     The diet of 26.08 deleted these and reddened 5 tests. Bytes below are law: never reword, never re-flow.
     This block is NOT map prose and does not count toward the 120-line diet. -->
## Архив рулингов, которые читают запечатанные записи (не редактировать)

- (в) **Затем — (4) LoRA «рассуждение→метка» поверх v2 с (3) синтетикой как армом B** одной регистрации; цель обучения — из (б).
- (к) **РУЛИНГ 22.08 (вечер) — дизайн `lora-c`:** промпт v3 = v2 + поле `rationale` ПЕРЕД меткой, одним рендерингом для обучения и инференса; таблица парно в четыре колонки (база v2 · база v3 · адаптер A · адаптер B) на одном пуле соседей; рационали для обучающих строк пишет Claude Code по кодбуку и метке тимлида, тимлид ревьюит все «наши» + ~40 пограничных; синтетика ~160 (по 40 на класс ошибки) — арм B, тег в provenance, абляция армом A, никогда в холдаут/голд; из обучения и из пула соседей исключены холдаут-100 и ВСЕ строки 16 референсных тредов; **бар на арм (один выстрел): холдаут-100 ≥ 64 И сквозной бар 1 = 5/5 И бар 3 = 0 — все три или КРАСНЫЙ**; отчётно: ячейка «упоминание → категория» на холдауте (18 → ?), dev-200 (fit), 14 золотых (шестой взгляд). Два контракта: `lora-c-prep` ($0, с ревью-гейтом тимлида) → `lora-c-run` (один A6000, кап **$4.00**).
- (н) синтетический запрос рендерится ТЕМ ЖЕ правилом на ТОМ ЖЕ пуле 515 (закон уже запрещает синтетику только как СОСЕДА; запрет двойника действует)
- (н) проход-2 в ране — ТОЛЬКО для армов (2 ноги, не 4): база v2 сквозной уже измерена (r2: 4/5 · 2), база v3 бара не берёт — экономия ~$0.47 по заряду.
- (о) кап lora-c-run **остаётся $4.00** (оба существующих замера s/step влезают по фактической цене; смок решает, рунг проекции убивает честно)
- (о) карта — **RTX PRO 4500, 32 GB, $0.72/ч, EU-RO-1** (A6000 в датацентре тома none; 32 GB — запас против OOM при замороженном micro_batch; фолбэк 4090 преавторизован)
- (о) ложная строка топика в синтетических заголовках заменяется выделенной константой (продовая строка NO_POST_TEXT в синтетике запрещена); конфаунд маркера принят и именован; в ран добавлен отчётный маркер-ценз (~$0.05) — отделяет «не помогло» от «выучил маркер»
- (ф) **(ф) 26.08, вечер — ПРИОРИТЕТ: СЛОЙ СИГНАЛОВ.** Система бесполезна, пока не находит сигналы в комментах и тредах на уровне ручного разбора тимлида; **только локальная Gemma-4-31B, thinking ОБЯЗАТЕЛЬНО**; API-модели отклонены. Находка: `enable_thinking: false` стояло во ВСЕХ запусках проекта — читатель с thinking ON ни разу не измерен; регистрации сами пишут «thinking-ON reader = НОВАЯ регистрация». Решение о дальнейшем пути — на этой таблице. [цитируется `scripts/write_think_zero_shot_prereg.py :: quoted()`; рулинг закрыт (х) 27.08, байты — закон]
