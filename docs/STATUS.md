# STATUS — market-pulse-llm (10.09.2026, 20:15) — карта ФИНИША: что значит «готово», шаги по порядку, когда конец

> Операторский документ, по-русски; правит только тимлид, после приёмок. Хроника — git этого файла, `docs/reviews/`, ADR.
> Потолок — 60 строк карты (скилл v3 §8); машинный блок в конце не считается и не редактируется.

## Что это за проект (одним абзацем; переспека 27.08, рулинг (х))
Инструмент маркетинговых исследований ТМ «Гармонія» по украинскому Telegram (`docs/PRODUCT.md`): что говорят о марках и конкурентах (тональность · сарказм · аспекты · бренды — дообученный адаптер 4.5h2), что и почём промоутируют сети по молочке и мороженому и как покупатели реагируют под промо-постами — окно за окном, непрерывной петлёй. GPU арендные, модель локальная (Gemma-4-31B на RunPod).

## Финиш (переспека 10.09, рулинг (nn)) — что значит «готово» и КОГДА
**Готово = три вещи, которые ты запускаешь и показываешь:** (а) `make serve` на твоём Маке → `http://localhost:8000/` — React-командный центр (T0–T8 плана фазы 6 + промо-пульс: позиции по всем окнам, тренды, реакции, качество, петля с расписанием), каждое число — из result-файла и говорит, из какого; `make loop` держит петлю по расписанию; (б) `make front` → `dashboard/app/` — статический билд того же приложения, публичная витрина, выкладываешь сам одной командой `firebase deploy`; (в) README как главная страница продукта (скриншоты, как запустить, числа качества из файлов, «база vs файнтюн», dataset card) + отчёт `docs/reports/ship-1.md`.
**Когда: пятница 11.09 вечером — твой гейт (квиз «10 секунд» по приложению + 20 строк `truth_20.json`); резерв — суббота 12.09 12:00.** Деньги: $0 на всём пути (платное чтение петли подключено, но покупает только по твоему слову — endpoint + гард; не в этой фазе).

## Карта финиша — шаги по порядку (файл фазы `docs/PHASE-ship-1.md` v1 §2 — у каждого шага своя проверка; одна сессия исполнителя = один шаг)
| # | шаг | кто | что ты увидишь | когда | статус |
|---|---|---|---|---|---|
| 1 | **w3** — 57 позиций Маркетопта (купленные 09.09) в базе и на экране; экран по ВСЕМ окнам (1 301 строка); плохой id окна → ошибка, не пустой экран | исполнитель | `make tick` печатает три окна; на экране 57 строк Маркетопта рядом с 1 113 | Чт 10.09 12:10–12:44 | ✅ принят (oo) на моих прогонах: 145/1113/57 в базе, 1 301 строка на экране, 57 Маркетопта, плохой id → отказ, второй тик — 0 новых строк |
| 2 | **chain-fold** — один Маркетопт на экране (83 строки = 26 + 57 вместо двух каналов; моя вчерашняя «79» считала только w2); число продукта перемерено его же функциями | исполнитель | одна сеть «Маркетопт» в таблицах и графиках | Чт 10.09 14:09–14:34 | ✅ принят (pp) на моих прогонах: 83 под одним id, `marketopt_private` 0, числа качества не сдвинулись |
| 3 | **s2-promote** — 120 купленных тредов реакций попадают в базу продукта → лента реакций на экране (сегодня она ПУСТА, экран пишет «not bought») | исполнитель | вкладка Реакції с реальными цитатами и сигналами; «прочитано N из 678 · в очереди M» | Чт 10.09 15:55–16:29 | ✅ принят (qq): 118 тредов, 281 строка реакций (жалоба 149 · спрос 66 · похвала 38 · цена 25 · привычка 3), «118 из 678 прочитано · 548 в очереди · 12 в остановленных каналах», числа качества не сдвинулись |
| 4 | **serve-loop** — `make serve` (API: статус, расписание чтение/запись, кнопка «тик», экспорты) + `make loop` (демон по расписанию, платное чтение только по твоему слову) + шаблон launchd | исполнитель | `curl localhost:8000/api/status` показывает окна и очередь; плохое расписание отклоняется | Чт 10.09 17:06–17:40 | ✅ принят (ss) на моих прогонах: `/api/status` — три окна, «118 прочитано · 548 в очереди», $1.1602 из файла гарда; 30-минутное расписание → 422 дважды, файл байт в байт; кнопка «тик» → 0 новых строк ×6, экспорт не сдвинулся; демон пишет одну строку «queue 548 · reading unbought»; облако пусто (pods/serverless/volumes `[]`) |
| 4а | **s1-grade** — грейдер S1 читает СВОЙ файл предсказаний (сегодня он ищет поле `volume`, которого там нет → 0.0); один тест в обе стороны; эталон закоммичен по пути; файл `results/grade_positions_50.json` с числами тимлида | исполнитель | вкладка Якість покажет бар S1 с его файлом — честно КРАСНЫЙ | Чт 10.09 вечер / Пт утро | — (рулинг (rr)) |
| 5 | **front-1** — React-приложение (Vite + React + TS, Recharts, UA/EN, светлая/тёмная, мобильная) с промо-вкладками Позиції · Тренди · Реакції · Якість · Петля; статический билд | исполнитель | `make serve` → пять промо-вкладок на экране; скриншоты в `docs/reports/screens/` | Пт 11.09 утро | — |
| 6 | **front-2** — командный центр T0–T8 в приложении (окно-1: KPI, тональность, аспекты, сегменты, конкуренты, промо/цены, честные заглушки категорий и алертов, методология с провенансом) | исполнитель | девять вкладок; квиз «10 секунд» отвечен по вкладкам | Пт 11.09 день | — |
| 7 | **e2e-ship** — чистый клон: `make check && make front && make tick && make promo-screen`, `make serve`-дым, `make session`, `firebase.json` в репо, README как главная страница, отчёт ≤ 30 строк | исполнитель | README со скриншотами и инструкцией «как запустить / как выложить» | Пт 11.09 день | — |
| т1 | **design-brief** — `docs/DESIGN-ship-1.md`: токены, раскладка, компоненты, правила графиков, карта «вкладка ← файл», квиз | тимлид | исполнитель фронта не задаёт вопросов о дизайне | Чт 10.09 12:45 | ✅ |
| т2 | **positions-gold** — эталон по 46 страницам (180 строк-предложений на 44 страницах, вслепую, sha `44242b2a…`) → бар S1 (полнота ≥ 0.90 · цена ≥ 0.95); результат — на вкладке Якість | тимлид | честное число качества извлечения цен | Чт 10.09 16:45 | ✅ эталон записан; **бар S1 КРАСНЫЙ: полнота 0.23 (42 из 180 строк найдены по тройке бренд·продукт·объём), цена 0.95 на найденных (40 из 42)**; по бренду+объёму найдено 136 из 180 (0.76) — теряется поле «продукт» (модель пишет свободный текст, а не линейку/категорию), плюс мелкий шрифт читается с выдумкой; публикуем как есть (рулинг (rr)) |
| т3 | **linkedin-pack** — пост + набор скриншотов + легенда «как построено», только из README и result-файлов | тимлид | готовый пост | Пт 11.09 | — |
| г | **гейт** — `make serve`, квиз «10 секунд», 20 строк `truth_20.json`, твои слова «верно/неверно»; потом `firebase deploy`, если хочешь витрину | оператор | работающий, упакованный продукт | Пт 11.09 вечер (рез. Сб 12:00) | — |

**Где мы сейчас, одной строкой (10.09 20:15):** шаги 1–4 и т1–т2 СДЕЛАНЫ — три окна в базе, экран по всем окнам (1 301 позиция), один Маркетопт (83), лента реакций живая (118 тредов, 281 строка), **продукт ЗАПУСКАЕТСЯ: `make serve` → `localhost:8000` с API, `make loop` тикает по расписанию и ничего не покупает**, дизайн-бриф и эталон цен записаны; 34 + 25 + 34 + 34 минуты исполнителя, 0 стопов, $0; следующий шаг исполнителя — 4а «s1-grade» (тот же промт v2), затем front-1. Чего нет: React-фронта (на `/` пока заглушка с перечнем API), упаковки — шаги 5–7. **Честно про гейт:** прочитанные треды — это треды с комментариями (Varus и агрегатор), а 1 301 позиция — из листовок; пересечение — 1 позиция из 1 301, поэтому в 20 строках гейта у реакций будет «в очереди», а сами реакции ты смотришь на вкладке Реакції (281 строка с цитатами). Оставшиеся 548 тредов — очередь петли после гейта. Что уже есть и работает: сбор (Telethon, 17 сетей), модель (vision листовок + текст), позиции w1 145 · w2 1 113 · c3 57, 120 тредов реакций, SQLite-агрегаты, тренды SQL, `make tick` идемпотентный, статический командный центр T0–T8 (16.08) и промо-экран, README с числами из файлов, локальный сервер с API и демон петли, 4 339 тестов зелёные.
**Честно, по-простому:** реакции — на незнакомых тредах система угадывает «о чём коммент» в ≈ 74 % (продукт, с исправителем P1) и «что говорит» в ≈ 79 %; на выученных 40 тредах — 0.89 (экзамен по билетам); половина промахов — «монеты» самого кодбука. Цены из листовок — бар S1 измерен по моему эталону и он КРАСНЫЙ: из 180 предложений на 46 страницах система нашла по тройке «бренд · продукт · объём» только 42 (0.23; бар 0.90), зато цена на найденных верна в 40 из 42 (0.95); по «бренд · объём» найдено 136 из 180 — модель пишет продукт свободным текстом («Сир Кантрі» вместо линейки «Кантрі»), путает написание бренда (KOMO, ЗвениГора) и на мелком шрифте выдумывает размеры. Новой вычитки листовок в этой фазе нет (это работа с моделью, не $0), поэтому на вкладке Якість стоит красное число со своим файлом — и это правда, которую показываем. На гейте реакции будут стоять только у позиций из 120 прочитанных тредов, у остальных — честное «тред в очереди, не прочитан». Отгружаем с честными числами и говорим, чего не хватает, на каждой вкладке.

## Деньги (гард: `results/spend_cycle3.json`; биллинг постит с опозданием 30–60 мин)
Цикл-3: **$8.8398 из $10.00, REMAINING $1.1602** (18:57Z 09.09, мой гард); все линии ЗАКРЫТЫ по run record (`promo-pulse-1` $2.9867 · `promo-holdout` $0.2982 · `promo-iter4` $0.6948 · `promo-iter5` $0.9450 · `promo-holdout2` $0.4253 · `promo-c3` $0.2577 — пол $0.05 закрыл её); том `mp-srv2` УДАЛЁН 09.09 — капанья нет; облако пустое. **Впереди $0**: holdout-3 не покупается, 558 непрочитанных тредов — очередь петли после гейта (endpoint + гард, твоим словом). История: фаза 4 $32.47/$33; цикл-2 $17.68/$20; lora-c $1.12; D2 $5.84.

## Что доказано и остаётся в продукте
Адаптер 4.5h2 (`results/verdict_45h2.json`): тональность macro-F1 0.9214, сарказм 23/38, аспекты 0.8478, тип поста 0.9586, бренды 0.9610.
Позиции 5c2 (`run_5c2_positions.json`): 145 из 159 стр.; vision 7.872 с/стр. (n=30). Сигналы в брендовой постановке закрыты (х): проход-1 68%,
thinking +0 (`think_zero_shot_table.json`; D1 (инструмент, $0) принят 26.08). Закрытые линии: однопроходный читатель, lora-b, lora-c,
think-zero-shot, харнес v1. Витрина A1–A6 — параллельно, $0, не гейт.

## Живые решения (последние; полные тексты — `docs/reviews/2026-08-30-plan-promo-pulse-1.md`; (ll) и раньше — в истории git этого файла, живое из них — в файлах фаз)
- (ss) 10.09 20:15 — **s47 «serve-loop» ПРИНЯТ ($0, `8d85ec0`) на моих прогонах: `make serve` → `/api/status` (три окна, «118 прочитано · 548 в очереди», $1.1602 — последняя строка файла гарда, без запроса баланса), 30-минутное расписание → 422 трижды и файл байт в байт, кнопка «тик» → 0 новых ×6 и sha экспорта на месте, экспорт по имени 200 / `..` 404, только `127.0.0.1`; `make loop --once` → одна строка «queue 548 · reading unbought», в демоне нет платного пути; братья-читатели отказывают на `all` и складывают сеть (тест в обе стороны); эталон в HEAD с тем же sha; облако: pods/serverless/volumes `[]`. Промт v3 (правило «читать через Read tool» — первой фразой, после двух одинаковых девиаций), PROCESS v2.9 (набор дольками — механика исполнителя, один вызов — мой снимок).** Класс: зелёная приёмка + процесс. 34 мин, 0 стопов.
- (rr) 10.09 17:15 — **эталон позиций записан вслепую (`docs/labels-positions-50.jsonl`, 180 строк на 44 страницах из 46, sha `44242b2a…`) и бар S1 измерен на снимке: КРАСНЫЙ — полнота 0.2333 (42/180), цена 0.9524 на найденных (40/42), бейдж 40/42; по бренду+объёму 136/180. Находка-дефект: грейдер как есть даёт 0.0 — ищет поле `volume`, а файл предсказаний пишет `size_value`/`size_unit` (грейдер писали по синтетике и ни разу не прогнали по своему файлу) → шаг 4а «s1-grade» исполнителя (один тест в обе стороны, файл оценки по пути). Ничего не ослаблено: ни эталон, ни правило сопоставления, ни бар; красное число публикуется со своим файлом. Снимок s46: 4333 + 2 skip + то же средовое падение.** Класс: продукт (дефект исполнителя 03.09) + честное красное измерение. $0.
- (qq) 10.09 16:40 — **s46 «s2-promote» ПРИНЯТ ($0, `161a901`) на моих прогонах: 118 записей в `results/promo_signals/` (2 треда `@matusi_ukr` исключены — канал остановлен), таблицы 414/184/281/100/0/571, мой тик — 0 новых строк (sha `e45860c6…`), лента 281 строка, фраза «118 of 678 … 548 in the queue · 12 not collected» на экране, «not bought» исчезло, числа качества не сдвинулись. Мой порог «unsure > 0» был напечатан, а не измерен — на этих ответах 0; клауза читает «counted». Снимок chain-fold: 4332 + 1 средовое падение (`test_baselines` — путь репо), записано в PROCESS.** Класс: зелёная приёмка + девиация тимлида (порог вместо счётчика). 34 мин, 0 стопов.
- (pp) 10.09 15:40 — **s45 «chain-fold» ПРИНЯТ ($0, `b26da6d`) на моих прогонах: `marketopt_promo` 83 (26 + 57), `marketopt_private` 0, все 57 строк c3 под одним id; карта `chain_of_channel` в `config/chain_aliases.yaml` читается ДО написаний внутри `chain_key`; числа качества не сдвинулись (0.8857/0.8667 · 0.9043/0.8296 · 0.7411/0.7937 — только sha); мой тик — 0 новых строк; `truth_20` байт в байт. Моё чтение `make check` 14:05 — ПУСТОЕ (гонка с правками s45: старый загрузчик встретил новый yaml — 8 ложных падений); отныне мой набор бежит на снимке дерева (PROCESS v2.8). Долги названы: карта без гарда (не дефект), sha-файлы P1, братья-читатели → «serve-loop».** Класс: зелёная приёмка + девиация тимлида (чтение на живом дереве). 25 мин, 0 стопов.
- (oo) 10.09 14:10 — **s44 «w3» ПРИНЯТ ($0, `6fb8d1b`) на моих прогонах: позиции w1|145 · w2|1113 · w3|57; экспорт `all` — три окна с якорями, 1 301 строка (все уникальны), 57 строк c3 под `marketopt_private`, 9 строк паузного канала вне экрана; адденда c3 = w3 · пин реестра · дата · рулинг; мой `make tick` — 0 новых строк, sha экспорта не сдвинулся; `--window w9` → exit 1 с именами окон, файл байт в байт; `draw_truth_20` → 1 301; дро S1 не тронуто. Моя ошибка: порог chain-fold 79 считал только w2 — на объединении `marketopt_promo` = 26 → **83**; исправлено в файле фазы. Читатели `promo_by_chain`/`coverage` (пусто на `all`) и определение очереди — в «serve-loop».** Класс: зелёная приёмка + девиация тимлида (число проверки выведено не на той популяции). 34 мин, 0 стопов.
- (nn) 10.09 12:45 — **ПЕРЕСПЕКА твоим словом: «к пятнице проект полностью готов и рабочий, React-фронт, презентабельно, упаковано». Твои ответы: локально `make serve` + публичный статический билд (деплой — твоя команда); объём — командный центр T0–T8 + промо; «vision» = визуал UI. Один файл фазы `docs/PHASE-ship-1.md` v1 несёт весь остаток (хвост promo-pulse-1 внутри), `docs/DESIGN-ship-1.md` v1 закрывает развилки дизайна (UI-развилка — НЕ стоп), промт v2; приложение не считает ни одного числа — `results/front_data.json` пишется функциями существующих скриптов; демон петли ничего не создаёт в облаке.** Класс: процесс, тимлида — карта отвечала «когда гейт», а не «когда продукт работает и где показать»; скилл v3.22 (предложен): артефакт закрытия фазы — то, что заказчик ЗАПУСКАЕТ; блок FINISH называет команду запуска и адрес показа.

## Отложено сознательно (что · развязка)
Платное чтение оставшихся 558 тредов — петля после гейта (endpoint твоим словом). Слой категорий (T6) и логика алертов (T7) — честные заглушки до окна-2/5c3. Проектная цель одной `/goal` поверх `make done` — после финиша. holdout-3 — в резерве. Долги исполнителя — в PROGRESS («named, not built»).

## Уроки (ретро `docs/reviews/2026-09-03-retro-process-v3.md`; разборы остановок `docs/reviews/2026-09-*-stop-patterns.md`)
До 03.09 — восемь пауз, $0 потеряно, корни у тимлида → v3 (один файл фазы, один файл прогресса, стандартный промт, рулинг ≤ 12 строк). 04–06.09 — деньги и сервинг: регистрация в платной сессии, ставка по целому рану, пол рядом с полосой. 08–09.09 — граница инструмента = пайплайн продукта до экрана; поля вместо ритуалов в харнесе; верификатор до покупки (5 непроизошедших стопов); c3 куплена и закрыта. 10.09 — «куплено ≠ на экране» дважды (позиции c3, реакции); и главное: карта считала финишем ГЕЙТ на статичном экране, а заказчик — работающий продукт → переспека (nn), один файл фазы до финиша, дизайн-бриф, скилл v3.22. Разбор: `docs/reviews/2026-09-10-stop-patterns.md`.

<!-- MACHINE-READ BLOCK — quoted verbatim by sealed producers (`scripts/write_lora_c_prereg.py :: quoted()`).
     The diet of 26.08 deleted these and reddened 5 tests. Bytes below are law: never reword, never re-flow.
     This block is NOT map prose and does not count toward the 120-line diet. -->
## Архив рулингов, которые читают запечатанные записи (не редактировать)

- (в) **Затем — (4) LoRA «рассуждение→метка» поверх v2 с (3) синтетикой как армом B** одной регистрации; цель обучения — из (б).
- (к) **РУЛИНГ 22.08 (вечер) — дизайн `lora-c`:** промпт v3 = v2 + поле `rationale` ПЕРЕД меткой, одним рендерингом для обучения и инференса; таблица парно в четыре колонки (база v2 · база v3 · адаптер A · адаптер B) на одном пуле соседей; рационали для обучающих строк пишет Claude Code по кодбуку и метке тимлида, тимлид ревьюит все «наши» + ~40 пограничных; синтетика ~160 (по 40 на класс ошибки) — арм B, тег в provenance, абляция армом A, никогда в холдаут/голд; из обучения и из пула соседей исключены холдаут-100 и ВСЕ строки 16 референсных тредов; **бар на арм (один выстрел): холдаут-100 ≥ 64 И сквозной бар 1 = 5/5 И бар 3 = 0 — все три или КРАСНЫЙ**; отчётно: ячейка «упоминание → категория» на холдауте (18 → ?), dev-200 (fit), 14 золотых (шестой взгляд). Два контракта: `lora-c-prep` ($0, с ревью-гейтом тимлида) → `lora-c-run` (один A6000, кап **$4.00**).
- (н) синтетический запрос рендерится ТЕМ ЖЕ правилом на ТОМ ЖЕ пуле 515 (закон уже запрещает синтетику только как СОСЕДА; запрет двойника действует)
- (н) проход-2 в ране — ТОЛЬКО для армов (2 ноги, не 4): база v2 сквозной уже измерена (r2: 4/5 · 2), база v3 бара не берёт — экономия ~$0.47 по заряду.
- (о) кап lora-c-run **остаётся $4.00** (оба существующих замера s/step влезают по фактической цене; смок решает, рунг проекции убивает честно)
- (о) карта — **RTX PRO 4500, 32 GB, $0.72/ч, EU-RO-1** (A6000 в датацентре тома none; 32 GB — запас против OOM при замороженном micro_batch; фолбэк 4090 преавторизован)
- (о) ложная строка топика в синтетических заголовках заменяется выделенной константой (продовая строка NO_POST_TEXT в синтетике запрещена); конфаунд маркера принят и именован; в ран добавлен отчётный маркер-ценз (~$0.05) — отделяет «не помогло» от «выучил маркер»
- (ф) **(ф) 26.08, вечер — ПРИОРИТЕТ: СЛОЙ СИГНАЛОВ.** Система бесполезна, пока не находит сигналы в комментах и тредах на уровне ручного разбора тимлида; **только локальная Gemma-4-31B, thinking ОБЯЗАТЕЛЬНО**; API-модели отклонены. Находка: `enable_thinking: false` стояло во ВСЕХ запусках проекта — читатель с thinking ON ни разу не измерен; регистрации сами пишут «thinking-ON reader = НОВАЯ регистрация». Решение о дальнейшем пути — на этой таблице. [цитируется `scripts/write_think_zero_shot_prereg.py :: quoted()`; рулинг закрыт (х) 27.08, байты — закон]
