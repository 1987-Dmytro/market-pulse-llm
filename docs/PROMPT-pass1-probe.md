# PROMPT — pass1-probe: the decomposition's decisive micro-measurement

**The sitting of 17.08 closed the one-shot prompt reader by its own pre-registered
stop-rule and ruled architecture D — hybrid decomposition. This contract buys the
DECISIVE measurement of that ruling: can a SHORT per-comment classification call, fed
the thread's already-bought entity context, take bar 4's own gold? One $0 build, one
paid rung on a pod, cap $0.20, ONE attempt. If the bar is not taken, the finding goes
BACK TO THE SITTING (options B — labelled data + LoRA, C — another base); there is no
pass1-v2 prompt iteration without a sitting ruling. That clause is pre-registered.**

**What this is NOT:** a v6 of the closed reader. The stop-rule's own words name «two
passes» as the next step; pass 1 is that step. The ADR below records the reading so
the next session inherits it as law, not as memory.

**Baseline:** `make check` 2 828 / 2 skipped (team lead's own run, 17.08).

## Step 0 — tail

By live `git status`, by path; team-lead files verbatim in their own commit.

## Step 0.5 — debts of the accepted reader-v5b, and one debt that has been sliding

1. **Close the `reader-v5b` step** once the walk posts (it had not at acceptance:
   $0.3223 lower bound): `--step reader-v5b --step-cap 0.50 --close --note
   "reader-v5b settled"`. Then re-run the v5b scorer so bar 5 scores against the
   closed ledger; commit; state that ONLY `5_time_and_cost` moved (compare and show).
   If the walk still has not posted, the refusal is correct — carry the debt forward
   NAMED, do not wait on it.
2. **ADR `reader-programme-closed-and-the-architecture-sitting-17-08`** (+ INDEX):
   the stop-rule fired on a COMPLETED run (bars: 2/5 and 0.4286 vs ≥0.80; transport
   and mechanics all green — numbers from the verdict, not from memory); the
   withdrawn-order episode with its dates; the sitting's three rulings (architecture
   D; the micro-probe first; labelling planned only after the frame confirms); and
   the legal reading: pass-1 classification is «two passes», not «a v6». **This
   record binds the TEAM LEAD too: no new one-shot reader registration may be
   written; say so in the record.**
3. **The 22 old step ledgers, closed as a batch.** Named on 15.08 «at the first
   contract that touches the guard» — and it has slid through three guard-touching
   contracts since. This contract touches the guard; the batch rides here:
   `--close --since` with each ledger's window from its own report; a walk that
   answers «no rows» for an old window closes at its recorded reading. One commit,
   the list of 22 in the report. This is bookkeeping, not archaeology — timebox it;
   any ledger that resists gets NAMED and skipped, never hand-edited.

## D1 ($0) — the pass-1 instrument

1. **Prompt `pass1_comment_gm4_v1`** in `prompts.py` (the sha moves → the MOVED-tuple
   manoeuvre again, same drill as v5-prep §3.3; re-render tests for every frozen
   record must hold). SHORT, single-comment task: given ONE comment, the thread's
   topic (post_summary) and the thread's RESOLVED ENTITIES (name → subject_type →
   one-phrase reading, from bought verdicts), return ONE JSON object
   `{"msg_id", "subject_type", "subject_id", "stance"}` — nothing else. The
   attribution law travels from v5: the subject is read off THE COMMENT; the swap
   test decides категория_личное vs the chain vs the brand; «сеть_ритейлер» only
   where the shop AS a shop is discussed; stance null when the comment carries no
   attitude. Synthetic examples ONLY, and the v5-prep contamination gate runs over
   the new examples (corpus + normalised, with the positive control).
   **No gold VALUE may be encoded:** the prompt states rules, never a specific
   msg_id, thread, or gold answer. One attempt is the real guard and it is
   registered.
2. **Renderer:** `reader_messages_gm4` is NOT touched. A new small renderer (or a
   `pass1_messages_gm4` beside it) builds the per-comment request; ids as attributes,
   fenced, same injection hygiene. The ENTITY BLOCK per thread comes from the latest
   parsed verdict on disk — v5b's, and for `@VARUS_channel:10613` (refused under
   v5b) v4's — the source file and its sha recorded PER ROW in the evidence. No new
   reading is bought for context.
3. **Parser:** strict single-object, domain-checked (four subject_type readings,
   stance domain, msg_id must echo the request's); refuse on anything else. The
   balanced-object transport stop applies as in v5b.
4. **Registration `results/prereg_pass1_probe.json`** via a producer with
   byte-identical rebuild + a test file holding its claims:
   - **Population, enumerated not chosen:** the 14 gold rows of bar 4 (from gold r2,
     `scored_over`), PLUS every other payable comment of those same gold threads as
     the CENSUS population (report-only, no gold). Counts derived by the producer
     from the census/gold files, never typed.
   - **Bar P1 (gating): ≥12 of 14** gold rows agreed on their stated fields, the
     symmetric collapse applying to `subject_type` — the SAME comparison semantics
     as bar 4 (call the registered scorer's comparison, do not restate it).
   - Census (non-gating): per-thread distribution over the neighbor rows, refusal
     count, seconds and tokens per call — this prices the window's pass-1.
   - **One attempt; freeze at the FIRST pod create; the return-to-sitting clause**
     (a failed bar goes to the sitting, never to a prompt iteration).
   - Money: cap **$0.20** all-in, gate-0 (180 s ssh dead-man, ≤2 recreates, third
     dead pod = STOP), segment accounting, boot-kill and full-pass gates re-derived
     for ~44 short calls (projection from v5b's measured seconds-per-token at the
     real price; publish the table; the worked example may use $0.74/h and must say
     so). Gates APPEND. Per-row persistence exactly as v5b (request + sha, raw
     reply, parse outcome, seconds, finish_reason, cut_chars).
5. **Pack** at a registered path (`results/pass1_probe_pack.json`), order registered
   (thread-grouped, gold rows and census rows interleaved as enumerated — solve the
   full-pass gate backwards over the built pack and PUBLISH the table before create,
   Dv471's method; if any knife-edge appears, STOP and report before the pod).

## D2 — the run

Transport machinery inherited from v5b by thin callers (Dv451/Dv478 idiom — shipped
runners/drivers are called or subclassed, never edited): bundle staging to a NEW
directory, `--repo` explicit, gate-0 watch, detached launch with pid-based liveness
(Dv479), partial scp before every gate, resume-skip with the tolerant reader, delete
never stop, three listings with the volume as positive control, guard anchor before
create and a read after delete (expect a LOWER BOUND; close only when the walk
answers, else the named debt).

## D3 — scoring and the verdict

A scorer that CALLS the registered bar-4 comparison for P1 and prints: the 14-row
table (agreed / disagreed with field and both values / absent), the census
distribution, refusals by shape, seconds/tokens per call, and the window pass-1
re-price (per-comment cost × the census cell's 1 032 payable). Verdict record with
sha, byte-reproducible.

## Verify (paste outputs, `python3.11` throughout)

```
make check                                    # counts before and after
runpodctl pod list -a                         # [] after; plus the other two listings
python3.11 scripts/runpod_guard.py --step pass1-probe --step-cap 0.20
<registration digest + byte-identical rebuild + the backwards-solved gate table>
<contamination gate output with the positive control>
<the 22-ledger batch: list and outcome per ledger>
<verdict printed by the scorer: P1, the 14-row table, census, re-price>
```

## Report

`docs/reports/pass1-probe.md`, path-only in chat. Deviations from **Dv482** with
`[cause:]` tags; five-line Process signals. Read back first, one line each: what P1
gates and what the census does not; the return-to-sitting clause; where the entity
context comes from and for which thread it is v4's; the no-gold-in-prompt rule; when
the registration freezes.

## DO NOT

- No serverless. One pod at a time; ≤2 recreates; delete, never stop.
- No edits to: the v5/v5b registrations and packs, gold r2, `reader_messages_gm4`'s
  default render, the v3 parser's behaviour, `scorer.py`, `local_llm.py`, any sealed
  record. Frozen re-render tests must stay green.
- No gold values, gold msg_ids or gold thread texts in the prompt or its examples.
- No second attempt, no cap raise, no prompt iteration after a failed bar — the
  sitting owns the next move.
- Team-lead files: commit verbatim, never edit. Never `git add -A`.
- Probe rows never enter production aggregates or window prices.
