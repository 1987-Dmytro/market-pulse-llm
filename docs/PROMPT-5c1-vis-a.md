# PROMPT-5c1-vis-a — the GM4 caption path ($0) — ON HOLD

> **ON HOLD — do not execute.** Ruling 23 / SPEC amendment 3.14 (operator,
> 2026-08-08 late evening): the runtime target moved to serverless and all
> paid 5c1 steps wait for the srv-2 track. This contract will be RE-ISSUED
> against the proven runtime after srv-2b parity — the caption runner and
> its runbook must target the endpoint, not a pod session.
>
> What SURVIVES unchanged into the re-issue (amendment 3.13): the caption
> instrument is the NF4 BASE at the pinned revision, ADAPTER OFF, greedy,
> batch 1, its own registered prompt (new sha before any result), and
> `caption_source` provenance (`qwen-4.5g2` | `gm4-nf4-base`) — numbers
> from different caption instruments are never compared silently.
