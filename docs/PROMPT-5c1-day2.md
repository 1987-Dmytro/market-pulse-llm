# PROMPT-5c1-day2 — the approved day-2 run (exits · joins · gates · searches · harvest)

**Contract:** `docs/PROMPT-5c1.md` remains the phase brief; THIS file
consolidates and SUPERSEDES the queued addenda 3–10 as the single day-2
execution order. The operator approved the full composition on 08.08
evening (canon: `docs/CHANNELS-launch.md` — МАСТЕР-ЛИСТ, Рулинги волна
3 + добор, Дозаявки №3/№5/№8). $0; Telegram API only; the FloodWait
wall clears **2026-08-08 10:02 UTC** — nothing network-side before it.

## Order of execution
Resolve-budget aware; on any FloodWait: stop, record its expiry,
continue with non-resolve work only.

0. **EXITS (3):** leave @katyal55, @tretyakovaele, @kuksa2022;
   membership flag re-read after each (D28). The operator may have
   left some manually — record `not_a_member`, never an error.
1. **JOINS — List A (7):** @ya_Nenka @useful_healthy_fitness_menu
   @denisovapro @sashafitnesslife @olgaa_trainer @chifit_family
   @eftforhealth. Same pacing, every join logged.
2. **GATES — regions (16, Дозаявка №3):** posts-only entry, segment
   `regional`.
3. **GATES — retail (3):** @forainfo @ekomarket_shop @tadaua — segment
   `retail_official`; comments bucket per the group finding.
4. **GATES — harvest (6, Дозаявка №8):** @tvorcha_matusyua
   @pavlushaiyava @mamaiagolodniy (expected `baby_food`)
   @educationwithloven @lab_of_childhood (`mothers_kids`) @mandziak
   (`health_fitness`).
5. **WINDOW COLLECTION:** 28-day posts for every new pass; comments for
   all member groups; `shasum -c` 6/6 after; dry-run totals quoted.
6. **LANGUAGE CENSUS over the new sources' windows** (same
   pre-registered bars): RU_DOMINANT → REPORT ONLY, the operator rules
   fresh entries — no auto-exit.
7. **SEARCHES (TG-side):** consumer channels of Novus, Velmart,
   Fozzy C&C, Auchan, METRO; the Max Control note (5 queries,
   «фальсифікат» kept open for a human look); handle top-up for five
   titles: «Матусі України», «Мамо, не псіхуй!», «Дитяче харчування»,
   «Все про дітей | Виховання | Психологія», «Сучасні батьки |
   Виховання». Found → gate; private → privates ledger; not found →
   negative recorded.
8. **AUDIENCE fill** for every new entry per the canon segmentation
   table. Reserves and the privates ledger stay RECORDED, not actioned.
9. **HARVEST similar-channels** (only if resolve budget remains):
   recommendations from @ya_Nenka, @tarilka_malyuka, @blwbabies →
   `results/harvest_mothers_ua.json` for the operator's next sitting.

## Report — the phase-acceptance package
Registry diff (39 → up to ~72 by passes) · exits confirmed by flag ·
join log · collection totals + queue depth · gate rows for every
candidate · census verdicts for new sources · search outcomes ·
audience counts · ledger subscriber delta · complete Deviations.
Numbers by artifact path, never prose recall.

## DO NOT
Standing rules: team-lead files read-and-commit only; no pods, no
endpoints, no OpenRouter; no invite-link joins; no reserve entries
without an operator ruling; stage by path, never `git add -A`.
