# PROMPT-5c2-run — the backlog, bought: three legs under the sealed $8.00

> Authority: `results/prereg_5c2_run.json` (SEALED — the one file that
> authorizes this session), SPEC 3.18 (5)/(7), operator go 2026-08-13.
> ONE paid session, session cap **$8.00** (equality-pinned), remaining
> $9.1690 under the $33.00 phase cap. Projected $7.8546: 5 075 comments +
> 159 leaflet pages + 44 posts. Every stop rule is IN the registration —
> this contract adds sequence and hygiene, never new numbers.

## Read first (sections, not documents)

- `results/prereg_5c2_run.json` :: `legs`, `session_cap`, `stop_rules`,
  `validate_consequence` — the populations, the cap, the three gates, and
  the per-row evidence the run must leave behind.
- `docs/reports/skub2-run.md` §Gates 2–4 and 7 — staging, template, endpoint
  and PROVEN teardown for the POSITIONS path; repeat what worked.
- `scripts/runbook_srv2b.md` + `results/srv2d_cost.json :: inputs` — the
  serverless COMMENT path and the serving config srv-2d priced.
- Your own `post_pass` wiring (prep-c3a/b) — the leg that has never run paid.

**Read-back — FIRST lines of the report:** the cap and the file that pins
it; the two-endpoints rule in one line; the three stop rules one line each;
what a mid-session death costs on re-entry (only a boot — watermarks + the
fourth kind re-buy nothing); and the store your evidence rows land in.

## Sequence (ordered; each gate blocks the next)

1. **$0 gates.** `make check` green; the registration's own verifier runs
   clean (it refuses on any moved input — if it refuses, STOP, that is a
   team-lead matter, not a fix-in-session); balance re-read vs
   `spend_phase4.json`; `pod list -a` → `[]`, `serverless list` → `[]`
   (positive-control the listing on nothing — it must at least render its
   header); driver dry-runs show EXACTLY the registered populations:
   5 075 / 159 / 44, matched against the prereg's selection hashes.
   **Anchor `results/spend_5c2run.json` BEFORE anything bills** (the Dv149
   protocol), committed.
2. **Endpoint A — POSITIONS (the cheap rung carries the new path).** Stage
   the volume per the skub2 gates (explicit-sha reset, content check, pod
   deleted and proven). New template, `ADA_24 · workers-max 1 · idle 60 ·
   execution-timeout 900`, flags read back from the API answer. Warm-ups =
   REPRESENTATIVE inputs per the registration (a real leaflet page AND a
   real post row — the (11)(c) lesson is in the sealed go/no-go). Go/no-go
   vs the cap. Then: **leaflet leg (159), then post leg (44)** — the new
   integration debuts inside $0.37, not after $7.48 is spent. In-run cap
   gate between jobs. **Teardown, proven by listing** with a positive
   control, before Endpoint B exists.
3. **Endpoint B — the srv-2d serving config, comments.** Same staging
   discipline. Warm-up on a real comment row, go/no-go re-projection
   against what is LEFT of $8.00 after the positions legs. Then the
   comment leg: 5 075 across the 19 pinned channels, ~25 jobs, watermarks
   advancing leg-by-leg as rows land durable-first. Teardown, proven.
4. **Outcomes.** Completion (expected): every leg's rows in the derived
   store with the full REQUIRED evidence (rendering, reply, shas — the
   validate sitting of 3.18 (6) is built from these; a run that answered
   but did not persist is the LOST.md failure re-run); the summary record
   with per-leg counts and the three cost readings kept distinct (Dv33:
   the balance delta is a FLOOR). A go/no-go refusal: teardown, report,
   STOP — the attempt is intact, the registration unconsumed. A mid-run
   cap stop: teardown, report, STOP — a finding about the cap, team-lead
   ruling, never a silent re-run.
5. **Ledger + teardown, whichever exit runs:** balance re-read through
   `spend_or_note`, ledger entry via `log_run`, console proven EMPTY by
   listing (0 pods, 0 endpoints — the between-sessions norm).
6. **Commits:** team-lead docs verbatim first if any are queued; artifacts
   by path; the report; vault tail its own commit. Never `git add -A`.

## Re-entry (this run, unlike skub2, MAY resume)

A death mid-session is a boot, not a loss: watermarks + the `post_text`
marker mean re-entry re-asks NOTHING answered (measured, prep-c3b). On
re-entry: re-run step 1's $0 gates, re-project go/no-go over the REMAINDER
against what is left of the SAME $8.00 — the session cap survives the
session's restarts; it never resets.

## Recovery clause

After a PROVEN deletion, an endpoint/template MAY be re-created within this
session and cap for a staging/config fix. GOALS: never two billing
endpoints CONCURRENTLY; nothing left billing after any exit; no registered
row re-bought after its answer landed.

## Do NOT

- Do not edit prompts, the parser, `positions.py`, the pins, or any
  constant mid-session — a cap is not raised and an instrument is not
  edited to finish a run. Do not touch the sealed registration, the frozen
  family, `results/baselines.json`, the v1 raw stores (baseline sha holds),
  or team-lead files (commit-only).
- Do not ask one row outside the three pinned selections. Do not run both
  endpoints at once. Do not delete the volume.
- If ANY prereg refusal fires — inputs moved, suite red — STOP. Nothing is
  re-derived in-session to make a sealed number green.

## Report — evidence, not assertions

`docs/reports/5c2-run.md`, own commit; chat gets ONLY the path. Read-back
first; listings before/after verbatim; boot + warm-up timings and the
go/no-go arithmetic per endpoint; per-leg marginals vs the registered
prices; which outcome and its artifacts with shas; watermark states after;
`make check` tail; the per-commit checkout table (repaired instrument);
Deviations **Dv303+** with `[cause: …]` tags — explicitly "none" if none —
and the **Process signals** section (≤5 lines). English. State your
assumptions.
