# PROMPT-skub2-prep — instrument v2 and the B′ registration, before the re-measurement ($0)

> Authority: SPEC §3.17 (13), ratified 2026-08-12. Light contract. $0,
> local, no paid calls. The re-measurement is skub2-run, after this.

**On checkout, ONE test is red BY DESIGN** (the seventh marker); step
0.2 adds `sku-b-ratification-7` to the enumeration.

**Read-back — FIRST lines:** the five deliverables one line each, then
(13)(a)–(d) one line each, then "no paid calls".

## Step 0

1. Team-lead docs commit, unedited (SPEC -7, this brief, STATUS).
2. Enumeration gains `sku-b-ratification-7`.
3. Vault tail → its own final commit.

## Deliverables (five)

1. **The decomposition verdicts applied.** The team lead's 25 rulings
   (the a/b/c table relayed with this contract's chat message — the
   dictated-verdicts pattern) land in
   `results/sku_miss_decomposition.json` with per-class counts
   (a=11 · b=12 · c=2 · pending=4) and mechanisms; the 4 pending pairs
   (#4, #9, #28, #29) are marked `PENDING_TEAM_LEAD` and their pages
   listed for the acceptance read.
2. **Instrument v2 — the parser family ((13)(a)).** Asterisk-footnoted
   discounts (`-50%*` → 50.0 + `footnote: true`), multipack pack-counts
   (`6х100 г` → size 100 г + `pack_count: 6`), price ranges
   (`від 39,90` → 39.9 + `qualifier: from`) become WARNINGS recorded on
   the position — a page is never refused for them. The positions
   ceiling moves 800 → **1200** in a serving pin **v2** file registered
   BESIDE v1 (same producer pattern; v1 stays sealed). Preflight
   re-driven: the three ex-refusal inputs now ACCEPT with their
   warnings, a truncated tail still refuses, every existing control
   kept. Regression: the sealed v4 artifacts are untouched; the new
   parser is a NEW registration beside the old (the old parsed the paid
   replies and stays what it was).
3. **The aliases ((13)(b)).** `Three Bears → try-vedmedi`,
   `Rud → rud`, `LIMO → limo` into the watchlist alias table
   (provenance stamp, no row deletions); a NEGATIVE guard: Latin
   `Galicia` (a juice TM) must NOT resolve to `halychyna` — test both
   ways.
4. **Bar-1 gold re-scoped ((13)(c)) + pre-registration B′**
   `results/sku_pilot_prereg_b2.json` BESIDE v4: bars' verbatim texts
   and thresholds byte-equal; gold v2 = the 55 pairs minus class-b and
   class-c (finalised only after the 4 pending verdicts land at
   acceptance — the producer REFUSES to write while any pair is
   pending); instrument shas move to v2 (new prompt? NO — prompts are
   unchanged; parser+ceiling+aliases move, so the pin set names the
   parser module sha, serving pin v2 and the registry sha); population
   the same 138, cap $0.40, all (9)–(12) readings.
5. **Projection** from the v4-measured marginals (boot range, 4.02
   s/page, 2.82 s/row, tail) vs $0.40 — corners and headroom.

Tests + `make check` green; checkout table; Deviations **Dv189+**;
report `docs/reports/skub2-prep.md`, chat gets only the path.

## Do NOT

- No paid calls; no edits to sealed artifacts, v1–v4 registrations,
  registered prompt texts, team-lead files; never `git add -A`.
- The registry edit touches the ALIAS table only — no row deletions,
  no composition change (the 66 stand as signed).
