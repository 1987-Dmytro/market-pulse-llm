# REPORT — `promo-pulse-1`, session 5 (2026-09-03): the C2 window is IN; the phase stops on w1's seals

**Question (`docs/PHASE-promo-pulse-1.md` §1):** «Что и почём промоутируют сети по молочке и мороженому, и как покупатели на это реагируют — неделя за неделей?»

**Answer — the WHAT and the POČЁM are now in the database; the REACTION half is unbought.** `data/derived/pulse.db` carries the C2 window `w2`: **17 channels · 3 397 markers · 1 113 position rows** (`scripts/build_aggregates.py` stdout), and the screen renders from it — **401 rollup rows** (`make tick`; `results/promo_screen_data.json :: window_id = w2`). `attribution/signal/evidence/digest/unsure` are 0: S9's first paid call has not run, because ruling 03.09 puts the team lead's read of the re-rendered prompt before it. Ruling 02.09 (d) shape 1 is implemented and `w1` is intact where it can be — **902/902** numeric leaves of the sealed `results/window_summary_5c2.json` re-derived from SQL, 0 disagreed, 0 missing. The phase STOPS because shape 1 alone does not restore w1's sealed ARTIFACTS — measured leaf by leaf, today's `results/dashboard_data_w1.json` differs from the committed one in **55 leaves, every one under `provenance.*`** and **none** outside it, with all 4 474 data leaves identical: `docs/plans/promo-pulse-1.STOP.md`.

**Money — nothing created, nothing bought.** `PROMO-PULSE-1 SPENT $3.1909 of $3.95` · `CYCLE 3 SPENT $3.4826 of $7.00` · `REMAINING $3.5174` (`scripts/runpod_guard.py --step promo-pulse-1 --step-cap 3.95`). The step's two paid legs, named: **$0.0599** (run 1 — the (10)(a) gate refused the whole step at a 13.466 s warm-up, 0 pages bought) and **$3.0335** (run 2 — 3 008 pages + 385 posts), `results/spend_promo_pulse_1.json :: gpu_sessions`. The step is NOT closed: `--close --tolerance` takes a fraction the plan does not name (STOP item 3).

**Checks, each run in the transcript.** (a) `results/promo_pagecount_c2.json` — 17 channels, **3 008** pages exact from store metadata; `results/promo_projection_c2.json :: verdict.c2_priced_usd` **$3.1563**, ONE number at the marginal (`the_one_number_usd` $7.2621 is the boot-inclusive ceiling). (b) `tests/test_collect_5c1.py` **23 passed** — `collect: false` refused, `collect: true` collected. (c) positions per channel above; five channels read 0 and that is an ANSWER — `@epicentrk_sale`, `@fozzyshopua`, `@kop1chat`, `@kopiyochka1`, `@rrozetka` were fully answered (`unreadable` 0) and `n_positions` sums to 0. (d) `results/positions_draw_50.json` drawn twice, identical sha **`61b4fda7fdc83197…`**, 50 of 1 113 from 12 chains — `docs/labels-positions-50.jsonl` is owed against THAT sha. (e) `promo_prompts.CODEBOOK` re-rendered from `docs/CODEBOOK-promo-signals.md` v1; `codebook_version()` **`a97d3c71b8cf9b14…`**; the gold through all four hooks as a fixture — **140 about rows, 84 signal rows, 0 failures**, 140/140 comments with text covered, 0 lines on a wordless comment. (f) `tests/test_trends_sql.py` **10 passed**. (g) `make tick` twice on an unchanged store: **zero new rows in all six tables**, `unsure` included, rollup 401→401. (h) clean clone `make tick && make promo-screen` exit 0; the source removed → **exit 2**, «promo-screen REFUSED: missing source …». (i) `scripts/draw_truth_20.py` — 20 rows, seed 42. (l) `git status --porcelain src tests scripts config results docs/plans docs/reports` prints nothing.

**Deviations.** `[cause: ruling]` `tests/test_repair_phase4_ledger.py` derives its live lines from the guard's own constants (02.09 (d)) — **12 passed**, the three Dv903 reds are gone. `[cause: the data]` `positions` gains `carrier` in its PRIMARY KEY: 7 C2 posts were read by BOTH legs and `channel:msg_id:ordinal` cannot tell the image row from the text row; no stored id moves and w1 is untouched by it. `[cause: the run]` **16 of the 405 pinned C2 posts have no evidence row** (14 `@epicentrk_sale`, 1 `@ekomarket_shop`, 1 `@forainfo`) — all 16 sit in the store with real text, and `runs[1].post_text` is `[]`, lost to the two harness kills, so `runs[1].unbought` = 0/0 is a claim about the PAGE stages only.

**`make check`:** **13 failed · 4 263 passed · 2 skipped · 7 errors in 734.74 s**, measured at `82346cf` over a clean tree — 20 red against a 35-red baseline, and **+16 passed** (4 247 → 4 263). **Every remaining red is one of the two forks and nothing else:** 19 are fork 1 (`test_build_dashboard` 7 errors + 4 failed, `test_build_validate_pack` 4, `test_window_summary_5c2` 2, `test_export_dashboard_data` 2 — each one a w1 record whose `provenance` names the store as it was sealed, refusing on a C2 file: «`data/derived/position_rows/ATB_FANatik.jsonl` is not in the export's provenance.evidence»), 1 is fork 2 (`test_positions_is_untouched`). No test file was deleted, and the only test CHANGED to pass is the one ruling 02.09 (d) names; the 7 window-default reds were fixture wiring of my own making and are fixed — both helpers now NAME the window their fixture builds instead of riding a production default.
