# REPORT — `promo-pulse-1`, sessions 9–28 (2026-09-04 … 09-05): S2's instrument is MEASURED and its registered question is still open — the ONE holdout shot came back RED on subject, the law was reworked, and the re-read that follows it is INCOMPLETE on a card too small for it. Sessions 6–8 are the previous revision of this file (git history).

**Question (`docs/PHASE-promo-pulse-1.md` §1):** «Что и почём промоутируют сети по молочке и мороженому, и как покупатели на это реагируют — неделя за неделей?»

**Answer — the WHAT and the ПОЧЁМ stand as sessions 6–8 left them; the REACTION half is one paid run short of an answer, and that is a number, not a hope.** The dev loop closed GREEN at iteration 3: `results/grade_promo_dev40_iter3.json :: bars` — subject **0.8714** ≥ 0.80, signal **0.9104** ≥ 0.75, on a complete reading (iterations 1 and 2 read subject 0.7929 and 0.7500; signal held throughout at 0.8792 / 0.8854). §8 (e)'s ONE pre-registered holdout attempt was then SPENT and it did **not** confirm: `results/grade_promo_holdout40.json :: bars` — signal **0.7958** HOLDS, subject **0.7181** RED. That is the answer of record for the registration that bought it, and ruling 05.09 (s) closed the registered question with it. The line continued on the operator's word: codebook **v1.2**, the spent holdout relabelled `dev-2` (a reading, never a bar again), a NEW `holdout-2` draw. Iteration 4 — the first read under the new law — came back **INCOMPLETE** by PHASE §6.5: `torch.OutOfMemoryError` in `gemma4._norm` on the smoke's LONGEST render (`@VARUS_channel:8647`, 14 281 chars), 338 MiB refused at 23.19 of 23.52 GiB (`results/promo_dev40_iter4_pod.log`) — no GO written, none of the 80 units bought, nothing compared to the bars. **Iteration 5 on a ≥ 32 GB card is the LAST of the five registered dev runs; holdout-2 follows it.** Everything else below is $0.

**Money — every line but one is CLOSED, and the phase is inside its ceiling.** `results/spend_cycle3.json`: **$6.2494 of $9.00, REMAINING $2.7506** (anchor $14.4800, unmoved since 2026-09-01). Closed lines, each settled on its own run record: `promo-pulse-1` **$2.986741**/$3.95 · `promo-holdout` **$0.298209**/$1.10 · `promo-iter4` **$0.694815**/$1.20. The one line still OPEN is `promo-dev-loop` and it **REFUSES** — **$2.5799 of its own $2.50** — because its walk is unbounded by construction ((r) item 4) and has swallowed every pod since 04.09; it carries no new run, its cap cannot be raised and it cannot be closed, so it is never named again and iteration 5 spends under `promo-iter5`. `results/promo_dev_loop_run.json` prices the six pods it has seen at **$1.934483** in total, the last of them iteration 4's 3 364 s / **$0.691489**.

**Evidence — session 28, the one this report is handed in for ($0, no pod).** (1) `promo-iter4` CLOSED: the read-only walk went FIRST and came back COMPLETE — **3 363 287 ms of the run record's 3 364 000**, 713 ms short of a ±33 640 band, where session 27 read 57.7% and was right to refuse. Settled **$0.694815** (pods only; the volume's $0.019444 stays beside the run) against the post-run reference **$0.7143** — **2.73%**, inside the registered 0.05. (2) `promo-holdout` CLOSED on the WALK ALONE (PHASE v13 §6.1): its only reading is $0.0 and PRE-pod, so `recorded_reading()` returns 0 and the tolerance is skipped; **no late `--note` was taken**, one would have landed the $1.0708 balance delta as the reference and shut the line for ever. The bound was measured, not chosen: `walk_ms` saturates at **1 443 991** (of 1 443 000, ±14 430) at every instant from 08:20Z to 13:21Z with pods flat at $0.298209, and **unbounded reads 4 807 278 ms / $0.993024** — iteration 4's pod, refused by `complete()`. That negative control fired. Settled **$0.298209** of $1.10. Each close wrote TWO files in one commit: the closing entry and cycle 3's witness line.

**Evidence — ruling (w) item 3, the transport defect, fixed at $0 (`acfe360`, `54c0cc5`).** Iteration 4's runner died at 164 s and said so only in the pod log; the out-file stopped at two rows, which is byte-for-byte what a third unit still generating looks like, so the Mac waited out the GO deadline — **$0.62 of the run's $0.69 was that wait**. POD half (`scripts/promo_dev_pod_runner.py`, the pin moves with the fix): `run_or_report` catches a unit's exception, appends that unit's ERROR reply (`id`, `error`, `exception`, `unanswered`) and returns non-zero; the unit named is DERIVED — the reader walks `todo` in order and flushes each row before asking the next — and `SystemExit` is deliberately not caught, so a named refusal never comes back as a unit error. MAC half (`scripts/promo_dev_pass.py`): `smoke_state` + `--smoke` reads the out-file into the registration's OWN decision-table branch; an error reply IS «the smoke did not come back», exit 1, delete at once. WAITING stays a poll — no file distinguishes a slow unit from a dead one — so the runbook pairs it with `pgrep`. **Replayed on the real artifact** (`results/promo_dev40_iter4.jsonl`): unchanged it reads `WAITING`, the crash replayed reads `THE SMOKE DID NOT COME BACK` naming `@VARUS_channel:8647`, exit 1. ONE test, both directions, over both halves, on the registration's real three smoke units (§4's allowance for a caught product defect; nothing else added). `make preflight`: the runner is now `43646d52e12f…` and both preregs still pin `102fa524b742…` — EXPECTED, and named rather than repaired.

**Evidence — sessions 9–27 in one line each, all accepted by the team lead on the diff.** Codebook **v1.2** and both golds re-read by the convention diff (team lead); `holdout-2` drawn over the PRODUCT's population — `results/promo_threads_draw_2.json :: exclusions.eligible_after` **398** threads after the union of the two exclusions (90 removed, the overlap counted once), **20/20 by stratum**, disjoint from the 80 already drawn; `results/promo_law_leak_check.json` **CLEAN** over **440** comments with text (dev-40 140 · dev-2 188 · holdout-2 112); iteration 4 REGISTERED on the MEAN corner — `results/prereg_promo_dev_loop.json :: rung_0` **$0.8705 of $1.20** with the dear corner **$4.9984** priced and left visible, the borrow dead on every leg and the rate taken from WHOLE runs (`results/measurements.jsonl`, `sample: whole run`, n=40: **17.7118** and **47.6615** s/thread on dev-40, **25.8263** on holdout-40), the cap the operator's own $1.20 and the hard stop derived from it; `dev-2`'s gold pinned into the registration and `--score` taking the arm as parameters; one paid run = ONE step line ((v), PHASE v11) after iteration 4 was nearly bought on a closed line.

**§8, where each clause stands.** (e) is the open one and is answered above — the bars are taken on dev-40, the ONE holdout attempt is spent, and iteration 5 is the fifth and last of the ≤ 5 dev runs. (j) holds: **4322 passed, 2 skipped**, no test file deleted and none weakened — the one test added is §4's, for a caught product defect, in its fix's own commit. (k) is this file. (l) holds: `git status --porcelain src tests scripts config results docs/plans docs/reports` prints nothing at `c145936`. **(a)–(d) and (f)–(i) were last PROVEN in the sessions 6–8 revision of this file and are NOT re-read here** — nothing in sessions 9–28 touched `data/derived/pulse.db`, the C2 census, `results/positions_draw_50.json`, the trends SQL, the tick or the clean-clone path (the new draw of these sessions is S2's `holdout-2`, not (d)'s positions draw); the phase gate re-runs them, and this report does not claim a reading it did not take.

**Deviations.** `Dv1 [cause: process]` — ruling (w) item 1 ordered a close whose reference the line did not have: `recorded_reading()` takes the LAST open `step_spent_usd` and `promo-iter4`'s only entry was the PRE-pod $0.0097, ~7000% off a settled $0.6915 and refused for ever. Resolved by taking the post-run reading first, the path every line that ever closed under a tolerance used; the team lead carried it into PHASE **v13 §6.1**. `Dv2 [cause: env]` — law v1.2 renders longer than v1.1 and the longest registered render no longer fits a 24 GB card; the SERVING moves (≥ 32 GB, `expandable_segments`), the instrument does not (PHASE v12 §6.5). `Dv3 [cause: tooling]` — the runner reported a unit's death only in the pod log; fixed above. `Dv4 [cause: contract-gap]` — (w)3 does not say what to do about `already_answered` counting an ERROR row as ANSWERED, which would let a replacement pod skip the unit that killed the last one; fixing it in `reader_v5_pod_runner` would move a §4-PINNED shipped runner, so `refuse_to_resume_over_a_death` refuses in the file whose pin already moves. Named here as the fork I resolved rather than escalated. `Dv5 [cause: verify-gap]` — I first read `promo-dev-loop` at a cap I typed ($2.16) instead of its ledger's ($2.50), and started a `make check` that was overtaken by two commits; both re-read at HEAD, and the corrected numbers are the ones above.

**Debts, none of them blocking iteration 5.** §6.5's **fit proof is not built** — the longest registered render proven to fit BEFORE a pod, at $0; (w)3 fixed the REPORTING of a crash, not its prevention, and the proof belongs to iteration 5's $0 half where the card is chosen. `--open`/`--close-segment` write `results/promo_dev_loop_run.json` at BATCH scale, not LINE scale, so its `latest` verdict reads `OVER` while the line it describes was inside its cap; `--expect-ms` must be taken from the segment at or after the line's anchor, done twice today and agreed to 713 ms. `committed_registration()` still does not re-verify `pinned_inputs` (inherited, named in the record itself). No test for the arm selector or for the step-aware guard read. `prereg_promo_holdout.json` is SEALED over a run that happened under the old runner bytes and is NOT re-pinned — its stale pin is a fact of that record, not a defect to green. Owed by the team lead: `holdout-2`'s gold, blind, before that shot.

**`make check`:** `4322 passed, 2 skipped in 702.58s (0:11:42)`, exit 0, taken over a CLEAN tree at **`54c0cc5`** — the first run was killed at 43% because two commits had landed under it and a reading across a moving HEAD is void. HEAD at this report's writing is **`c145936`**; every commit between the two is documentation (`docs/plans/promo-pulse-1.PROGRESS.md` and this file), no code, no test and no result file.
