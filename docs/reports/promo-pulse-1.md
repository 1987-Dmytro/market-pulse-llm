# REPORT — `promo-pulse-1`, sessions 6–7 (2026-09-03): both forks closed; S9's paid instrument is BUILT and priced, and stops at the money boundary

**Question (`docs/PHASE-promo-pulse-1.md` §1):** «Что и почём промоутируют сети по молочке и мороженому, и как покупатели на это реагируют — неделя за неделей?»

**Answer — the WHAT and the ПОЧЁМ are in the database and their evidence is sound again; the REACTION half cannot be bought because its instrument is unwritten.** `data/derived/pulse.db` carries window `w2`: **17 channels · 3 397 markers · 1 113 position rows** (`scripts/build_aggregates.py` stdout), the per-channel table below. Ruling 03.09 (b)'s **fork 1 is closed**: the derived store is split, and all **38/38** sources `results/window_summary_5c2.json` seals hash to their sealed values again — `w1` re-derives **902/902** numeric leaves, and `results/dashboard_data_w1.json` re-exports with **5** differing leaves instead of 55, **none under `provenance.evidence`** and all 4 474 data leaves identical. **Fork 2 is closed**: `carrier` completes the `positions` key and S3's trends dedupe. The instrument that was missing is now WRITTEN and PRICED — the paragraph below — and the phase stops one step short of the pod: `docs/plans/promo-pulse-1.STOP.md`.

**Money — nothing created, nothing bought, no rung fired.** The step is CLOSED: `results/spend_promo_pulse_1.json :: gpu_sessions[2].settled_usd` **$2.986741** of $3.95 at `--tolerance 0.05`, checked against `recorded_reading_usd` $3.0335 (a 1.54 % gap). Its legs: pods $0.052079 + serverless $2.934662; the network volume's $0.223611 is excluded by the closing walk and stays in the cycle. `results/spend_cycle3.json` — `CYCLE 3 SPENT $3.5117 of $7.00 · REMAINING $3.4883`, so the dev loop keeps the full `min($2.50, $3.4883 − $0.30)` above its $2.00 floor. **The ruling named $3.1909 as the spend line; that is a balance delta, and the guard settles by billing lines — I did not type it in. Which figure is of record is a STOP question.**

**Fork 1, and how the split point was found.** Never guessed: for each sealed source, `scripts/split_derived_w2.py` walks the file for the k whose prefix sha256 IS the sealed digest — **38/38 placed**, 23 whole · 15 shorter (529 lines) · 35 files the seal never named moved entire (3 972 lines). Copy → verify → truncate, `data/derived_backup_2026-09-03` made before the first cut. `data/derived_w2/` is the live root; `pulse.db` stays put because the seal hashes files and none of them is the database. `scripts/build_aggregates.py` moved and is CLAIMED through `tests/moved_pins`' machinery (`MOVED_BY_THE_SECOND_WINDOW` at `58ff037`, witness `WINDOW_ID_C2`), never re-pinned. **All 19 fork-1 reds are green with no assertion of theirs changed.**

**Fork 2, and the ruling's conditional check fired.** `results/dashboard_data_w1.json`'s 145 `row_id`s are all `channel:msg_id:ordinal` and carry no carrier, so the key is `(window_id, row_id, carrier)`. The $0 reading owed: **7** row_ids stored under both carriers, **6** messages with position rows from both, and **exactly 1** (brand, product, volume) triple read by both legs — `@forainfo:6056`; w1 has **0**. 1 > 0, so `src/market_pulse/trends.py` dedupes. **Your key over-reaches and I narrowed it — measured, not judged.** Taking ONE row per (window, channel, msg_id, SKU) drops **39** rows over **35** groups in today's store; only **1** group spans carriers, 34 are two readings of one SKU by ONE leg, **24** of those hold more than one distinct promo price (`@atb_market_official:4359` prints Активіа Біфідойогурт 260 г at 23.9 AND 24.7) and **2** sit inside the SEALED w1 window. Those are two promos, not one counted twice, and «prefer `leaflet_page`» cannot be applied to a single-carrier group at all. So the CTE keeps the preferred LEG whole: **1 group, 1 row dropped, 0 in w1** — `@forainfo:6056:0`, the very collision fork 2 made storable — and `results/promo_screen_data.json` moves in exactly one leaf (`@forainfo` · Ласунка · 2026-W35 `priced_positions` 4.0 → 3.0). Plan revision 8 names it; **if the literal key is meant, say so and I widen it**.

**Checks, each run in the transcript.** (a) `results/promo_pagecount_c2.json` — 17 channels, **3 008** pages exact from store metadata; `results/promo_projection_c2.json :: verdict.c2_priced_usd` **$3.1563** as ONE number (`the_one_number_usd` $7.2621 is the boot-inclusive ceiling). (b) `collect: false` honoured, both directions named — `tests/test_collect_5c1.py::test_a_paused_source_is_not_collected_even_though_the_rulings_entered_it` and `::test_the_same_source_collecting_is_collected`, **23 passed** in the file. (c) positions per channel, w2 (`data/derived/pulse.db`): ATB_FANatik 95 · VARUS 289 · atb_aktsiyi 154 · atb_market_official 129 · blyzenkoua 209 · ekomarket_shop 16 · forainfo 7 · marketopt_promo 22 · msuaaaa 145 · silposilpo 1 · sim23_simi 42 · xochydeshevshe 4 = **1 113**; **five channels read 0 and that is an ANSWER** — `@epicentrk_sale`, `@fozzyshopua`, `@kop1chat`, `@kopiyochka1`, `@rrozetka` are DIY, coupon-aggregator and electronics channels and the parser found no dairy or ice cream to write. (d) `results/positions_draw_50.json` drawn twice identically; the draw did NOT move — `rows_sha256_as_drawn` **`fe3f5841175a3953…`** equals the sha over the committed record's rows and the 50 row_ids are identical, so the labels are owed against it. (e) $0 half only: `promo_prompts.codebook_version()` **`a694d005972d3a66…`** after the ruled line landed in rule 6, and the gold re-driven through all four hooks — **140 about · 84 signal · 0 failures**, 140/140 comments with text covered, 0 rows on a wordless comment. (f) `tests/test_trends_sql.py` **14 passed** (10 + the dedupe's four). (g) `make tick` twice on an unchanged store: **zero new rows in all six tables**, `unsure` included. (h) clean clone `make tick && make promo-screen` **exit 0**, rendered from `results/promo_screen_data.json` alone; that source removed → **exit 2**, «promo-screen REFUSED: missing source …». (i) `scripts/draw_truth_20.py` — 20 rows, seed 42. (l) `git status --porcelain src tests scripts config results docs/plans docs/reports` prints nothing.

**K6 is page-level as ruled.** The 50 rows come from **46** pages; `results/positions_50_predicted.jsonl` is re-emitted as every extracted row of those pages, both carriers — **205** rows, sha `781b02b473ed8104…`. Each drawn row and each page gained `image`; **45 of 46** resolve, and `@silposilpo:3822` is a `post_text` carrier whose link is the whole of it. The file's own sha necessarily moved from `61b4fda7…`; `anchor_note` in the record says so.

**Deviations.** `Dv [cause: ruling]` ×5, all from ruling 03.09 (b): S4's write target amended to a per-window root; `test_positions_is_untouched` re-scoped (not deleted); the trends dedupe and its `PREFERRED_CARRIER`, narrowed to carrier-spanning groups as measured above; `image` added to the labeller's closed field list; and `population_state` DERIVED per window instead of pinning the literal «PRE-C2» — S4 made that literal false, and the shipped record was telling the labeller not to label it. `Dv [cause: mine]` ×1: `tests/test_run_promo_c2.py::stage()` patched `DERIVED_ROOT` only, so a stub run appended **13 rows** into the real `data/derived_w2/`. The live root was rebuilt from the backup by re-running the split (3008/1093/15/385 exactly) and the w1 seal never moved; the fixture now derives the roots it must patch from `dir(run_loop)` and refused `STORE_ROOT` by name when first written too wide.

**S9's paid instrument, session 7, $0 and no pod.** Ruling 03.09 (c)'s five amendments are folded into plan §9a (`bf21167`), including one mechanic §9 left loose that a FACT settles: the transport is a POD, because `local_llm.batch` renders from `prompts.py` and that module is pinned — serving this prompt over an endpoint would move a pinned file. **Rung 0 FITS at the dear corner: `results/prereg_promo_dev_loop.json :: rung_0.dear_usd` $1.3193 of $2.5000** (cheap $0.2766 · priced $0.3340), priced in the POD's own unit and not in a serverless per-second rate from another transport. Every input is read: the cap from the guard's own `REMAINING $3.4591` less the holdout's $0.30; $0.74/h from `runpodctl gpu list` (the DEARER of the two clouds, EU-RO-1, RTX 4090, stock Medium); the 279 s of non-generation overhead from the sibling that SETTLED — `results/pass2_signals_r2_run.json :: pods[0]` billed 2 061 s for 75 threads at the borrowed 23.76 s; the transport gates from v5b's frozen record, its 180 s dead-man DERIVED from the two fields that bound it. `results/promo_dev40_pack.json` carries **56** units — 40 promo threads and the 16 leg-B posts enumerated from the store by S4's own subtraction — with the smoke's three first by the shortest/median/longest rule (`@msuaaaa:6523` 4 895 c · `@VARUS_channel:9006` 5 202 c · `@VARUS_channel:6009` 8 170 c). `scripts/promo_dev_pod_runner.py` swaps ONE function of `reader_v5_pod_runner` and dispatches on the item's own task, so both legs ride one boot with the GO between them; its whole preamble was DRIVEN on the Mac with no GPU over the real pack — the codebook handshake, `check_instrument` and all 56 per-item shas — and that dry contact **refused at `@ekomarket_shop:1457`**: the pack pinned the sha of the payload where the pod re-derives the rendered request. On a live pod that is a boot thrown away; it is fixed and it is now a test. **Nothing was created and nothing was bought: `pod list -a` and `serverless list` are `[]` in the transcript.** `scripts/runbook_promo_dev_1.md` is the pod session end to end.

**Debts.** `docs/labels-positions-50.jsonl` — 46 pages, every in-scope position, against `fe3f5841…`. `docs/labels-promo-dev.jsonl` is in hand but (e)'s bars need the paid loop. The **16** pinned C2 posts with no evidence row (14 `@epicentrk_sale`, 1 `@ekomarket_shop`, 1 `@forainfo`) are pinned as leg B of the pack and are bought by the same boot as iteration 1. The runbook's `--image-name` is the one field no record in the repo carries — it needs the operator's word before the create.

**`make check` @ `ae99c7a`:** `4313 passed, 2 skipped in 751.97s (0:12:31)`, exit 0 — **zero failed, zero errors**. (j) holds on both halves: **4 313 ≥ 4 266**, and green; session 7 added 12 and changed no assertion of anyone else's. (Session 6 read 4 297 at `bbc7483`.) Against the 20 red of the last report: 19 were fork 1 and went green with no assertion of theirs changed, 1 was fork 2 and changed by the ruling. No test file was deleted, and the three tests that changed (`test_positions_is_untouched`, the labeller's closed field list, `population_state`) each changed BY the ruling and are named above.
