# REPORT — `promo-pulse-1-s2s3`
> «Сколько страниц купит C2 и по какой измеренной ставке — и что готово к разметке»

**1 022…9 653 страницы. ИЗМЕРЕННОЙ ставки нет — смок купить нечем, и это стоп-поинт**
(`docs/plans/promo-pulse-1.STOP.md`): `scripts/run_5c2.py` идёт `--endpoint <id>`, а живого эндпоинта нет: `$RUNPOD_5C2_ENDPOINT` не задан, id живого эндпоинта нигде в `results/` не записан, и `runpodctl` серверлес-эндпоинты вообще не умеет перечислять (`get` знает `pod` и `cloud`). Ключ при этом ЕСТЬ — в `~/.runpod/config.toml`, просто не экспортирован под именем `$RUNPOD_API_KEY`; не преувеличиваю блокер. Поднять эндпоинт — это деплой воркера, шаг со своим счётом, а ревью авторизовало ОДИН смок. Поэтому в `results/measurements.jsonl` нет строки `vision_seconds_per_page`, и K4 отдаёт ВИЛКУ, как и был написан.
**К разметке готово:** `results/promo_threads_draw.json` — dev-40 и holdout-40, по 20 currency + 20 decimal-only, непересекающиеся, seed 42, над замороженными 678.

| | |
|---|---|
| **K3** `results/promo_census_c2.json` | окно 2026-08-03…**2026-08-31** (якорь = последний день корпуса + 1, прочитан ПОСЛЕ докачки), `ids_sha256` `a56dc6dace1bfb44` над 1 373 строками; **968** листовочных постов → **1 022…9 653** страницы, **405** текстово-ценовых |
| **K4** `results/promo_projection_c2.json` | остаток перечитан на месте: **$2.3908**. warm-up 1.729 → $0.54…5.12 · fitted 4.2421 (`measured: false`) → $1.33…12.56 · **realised 10.408 → $3.26…$30.81 — не влезает ни одним концом.** SP-1 — слово оператора: (a) цикл-3, (b) сузить C2, (c) под-раннер |
| **S2** `results/collect_r2.json` | **+1 340 постов** в `data/raw_r2/` на 17 из 19 каналов; шесть были пустыми (@kop1chat 576, @rrozetka 170, @blyzenkoua 58, @sim23_simi 18, @xochydeshevshe 10, @fozzyshopua 9). Самый свежий пост: 2026-08-08 → **2026-08-30** |
| **v1** | `shasum -c results/raw_v1_baseline.sha256` → **6 из 6 OK** до прогона и после: докачка целиком в живом корне |
| **K2** | `74 источника — collected 17, not collected 57`; @znishkom и 17 полтавских вышли из сбора, @marketopt_promo остался |
| **K7** | два прогона байт-в-байт `6f9fa245b9d70254`; 678 тредов (229 + 449) → 488 после правила очереди (182 / 306), 2 808 бессловесных из 4 718 |
| **K0** | `ruff` clean · **4 200 passed, 2 skipped** (база 4 195), ни один тест-файл не удалён |
| **`/code-review`** свежий сабагент, S1b+S6 `952faeb` | **FINDINGS: 1** — `scripts/collect_5c1.py:105-119` (`collectable`) отбирает по `verified` + составу `results/entry_gate_5c1.json` и НЕ читает `source.collect`: 62 строки, 56 из них `collect: false`. Пять названных проверок пройдены, остальные четыре чисто (шесть uuid5-ключей совпадают с SP-5/§5.7, `window_id` нет ни в одной идентичности кроме `rollup`; цепочка ревизий воспроизводит `58ff037` байт в байт и отказывает на недостижимом пине). **Смягчение, проверено:** `collect_5c1.STORE_ROOT` — это `ARCHIVE_ROOT`, и новый гард `RawStore.append` отказывает ему на записи целиком; флаг всё равно надо починить — слово тимлида, это дефект S1b, не этого слайса |

## Отклонения
- **Dv8 [env]** Смок не запущен — нет эндпоинта (ключ есть, см. выше). Из трёх предпаидных условий ревью тест в отказывающую сторону сделан (`466a08f`); `make preflight`, называющий достигнутую ревизию вместо «DIFFERS», остаётся долгом, как ревью и разрешило.
- **Dv9 [contract-gap]** `test_the_paused_rows_are_loaded_and_are_not_collected` подвинут 39 → 57: композиционный счёт, который двигает сам рулинг, и вместе с ним переписан `test_a_channel_..._refused_before_the_client` — он теперь гоняет гард с `STORE_ROOT`, выставленным в архив, потому что ретаргет увёл путь записи. Оба **не ослаблены**: у первого каждая правка считается своим `paused_by` (39 + 17 + 1), у второго добавлено третье направление — под живым корнем тот же пиненный канал собирается.
- **Dv10 [env]** Не резолвятся с этого аккаунта: группа @ATB_FANatik (`1925810730`) и приватный Маркетопт (`+Ejz6ubzm21IyMTQy`) — ждут фазы `--join`.
- **Dv11 [process]** Фаза `--join` (900 с между вступлениями) успела **1 из N** (`@VARUS_channel: joined`) и остановлена вместе со слайсом; `results/joins_r2.jsonl` — её курсор. Поэтому `comments_added_r2` везде 0.

- **Dv12 [process]** `git status` не пуст ровно на одном файле, и он не мой: `docs/reports/lora-c-run-r3.md` пришёл в сессию уже изменённым (одна строка — честная правка «CLOSING_COUNT» на «reading NOT TAKEN»). Красное, которого я не делал, называется, а не чинится молча. Всё остальное закоммичено по путям, `git add -A` не использовался.

- **(k) НЕ ВЫПОЛНЕН**, и это надо сказать прямо: `git status --porcelain` печатает одну строку — `docs/reports/lora-c-run-r3.md`. Всё, что этот слайс трогал, закоммичено по путям.
- **После-чтение дро:** прогон K7 ПОСЛЕ докачки даёт тот же `6f9fa245b9d70254`, `git status --porcelain results/` пуст — утечки объединения в замороженную популяцию нет.

**Коммиты:** `05cf96c` план · `134eaba` реестр · `80a9d10` стор · `258ba61` дро · `466a08f` отказ цепочки · `ea3cf47` докачка + перепись + проекция.
