# REPORT — `promo-pulse-1-s2s3`
> «Сколько страниц купит C2 и по какой измеренной ставке — и что готово к разметке»

**1 022…9 653 страницы по измеренной ставке 7.872 с/стр. (n=30).** Одно число: **$2.4674** против перечитанного остатка **$2.3299** — у пола не влезает. Но 7.872 включает холодный старт, а платный проход платит его ОДИН раз: с амортизированным бутом маргинал ограничен **2.623…3.369 с/стр.**, и C2 у пола стоит **$0.8704…$1.1043 — влезает**, у потолка $7.81…$10.02 — нет. **Ставка перестала быть неизвестной; неизвестной осталась ЧИСЛЕННОСТЬ СТРАНИЦ**, и SP-1 решается на этом, а не на вилке из трёх кандидатных ставок.
**К разметке готово:** `results/promo_threads_draw.json` — dev-40 и holdout-40, по 20 currency + 20 decimal-only, непересекающиеся, seed 42, над замороженными 678.

| | |
|---|---|
| **Смок (ЕДИНСТВЕННЫЙ платный прогон)** | эндпоинт `s3o601108zg3ef` из шаблона `i5iqf96ir6` на выжившем волюме `mp-srv2`. **Рунг 0:** прогноз $0.2442 ≤ cap $0.35 (два бута по измеренным 212.041 с + 30 стр. × 10.408 + хвост 60 с), закоммичен ДО создания — `results/prereg_smoke_vision_c2.json` @ `1a8b1d4`. **Рунг 1:** `info` отдал POSITIONS на пиненной ревизии, `assert_serving` против `sku_pilot_serving_v2.json` прошёл ДО единой страницы. 30 страниц → 30 ответов, 0 failed. Гард: $2.3908 → **$2.3299** ($0.0609 осело; серверлес-строки оседают поздно) |
| **Теардаун** | `serverless delete` + `template delete` сразу; `runpodctl serverless list` → `[]`. **Положительный контроль:** `mp-srv2` и `market-pulse-5b-a` в том же ответе на месте — листинг различает, а не молчит |
| **K4** `results/promo_projection_c2.json` | измеренный угол в таблице; `marginal_bound` выведен из полей самой записи смока (`worker_seconds`, `wall_seconds`, `wall_seconds_client`), ни одно число не набрано руками. Правило капа названо: cap = 2× оценки, «a cap is not raised to finish a run» |
| **K3** `results/promo_census_c2.json` | окно 2026-08-03…**2026-08-31** (якорь = последний день корпуса + 1, прочитан ПОСЛЕ докачки), `ids_sha256` `a56dc6dace1bfb44` над 1 373 строками; **968** листовочных постов, **405** текстово-ценовых |
| **S2** `results/collect_r2.json` | **+1 340 постов** в `data/raw_r2/` на 17 из 19 каналов; шесть были пустыми (@kop1chat 576, @rrozetka 170, @blyzenkoua 58, @sim23_simi 18, @xochydeshevshe 10, @fozzyshopua 9). Свежесть: 2026-08-08 → **2026-08-30** |
| **v1** | `shasum -c results/raw_v1_baseline.sha256` → **6 из 6 OK** до и после: докачка целиком в живом корне |
| **K2** | `74 источника — collected 17, not collected 57`; @znishkom и 17 полтавских вышли из сбора, @marketopt_promo остался |
| **K7** | два прогона байт-в-байт `6f9fa245b9d70254`, и третий ПОСЛЕ докачки — тот же: утечки объединения в замороженную популяцию нет. 678 → 488 после правила очереди (182 / 306) |
| **K0** | `ruff` clean · ****4 205 passed, 2 skipped**** (база 4 195), ни один тест-файл не удалён |
| **`/code-review`** свежий сабагент, S1b+S6 `952faeb` | **FINDINGS: 1** — `scripts/collect_5c1.py:105-119` (`collectable`) не читает `source.collect`: 62 строки, 56 из них `collect: false`. Остальные четыре проверки чисты. **Смягчение, проверено:** его `STORE_ROOT` — это `ARCHIVE_ROOT`, новый гард `RawStore.append` отказывает ему на записи целиком. Чинить флаг — слово тимлида, это дефект S1b |

## Отклонения
- **Dv8 [contract-gap]** Смок пришлось СОБРАТЬ, а не запустить: эндпоинт и шаблон 5c2 удалены ещё 08.08 (`srv2c_bootlog.json :: deletions_proven_by_listing`), а `run_5c2.py` привязан к запечатанной преригистрации 5c2 — гонять его значит перекупать замороженный экзамен. Отсюда `scripts/smoke_vision_c2.py`: транспорт импортирован (`EndpointClient`, `loop.render_page`, `PAGE_TASK`), страницы взяты из двух манифестов, а 159 страниц `post_media_5c1.json` исключены по имени.
- **Dv9 [contract-gap]** Два теста подвинуты рулингом, оба **не ослаблены**: композиционный счёт 39 → 57 разложен по трём `paused_by` (39 + 17 + 1), а гард ретаргета теперь гоняется с `STORE_ROOT` в архиве и получил третье направление — под живым корнем тот же пиненный канал собирается.
- **Dv10 [env]** Не резолвятся с этого аккаунта: группа @ATB_FANatik (`1925810730`) и приватный Маркетопт (`+Ejz6ubzm21IyMTQy`) — ждут фазы `--join`.
- **Dv11 [process]** Фаза `--join` (900 с между вступлениями) успела **1 из N** (`@VARUS_channel: joined`) и остановлена вместе со слайсом; `results/joins_r2.jsonl` — её курсор. Поэтому `comments_added_r2` везде 0.
- **Долг:** `make preflight`, называющий достигнутую ревизию вместо «DIFFERS» для двух путей r2 — ревью разрешило нести его долгом.
- **`git status` не пуст на одном файле, и он не мой:** `docs/reports/lora-c-run-r3.md` пришёл в сессию изменённым. Всё, что трогал этот слайс, закоммичено по путям; `git add -A` не использовался.

## Аддендум (30.08, вечер) — фаза `--join` + `--comments` догнана ПОСЛЕ приёмки на `ee23a13` · тело выше не тронуто ни на байт, ново только это
**Вступлений 7 из 8** (`@VARUS_channel`, `@msuaaaa`, `@kopiyochka1`, `@kop1chat`, `@ATB_FANatik`, `@sim23_simi`, `@xochydeshevshe`), пейсинг 900 с. **Комменты: +1 190** в `data/raw_r2/` по шести каналам — VARUS +452 (треды 835 → 899), msuaaaa +260 (703 → 737), kop1chat **+350 с нуля** (0 → 150), kopiyochka1 +93 (37 → 60), ATB_FANatik +18, sim23_simi +17, xochydeshevshe 0. **Dv11 закрыт**, `comments_added_r2` больше не нули. **Dv10 закрыт наполовину и не «не получилось», а факт про инструмент:** `1925810730` не резолвится ни после вступления в родительский канал — голый числовой id Telethon не разрешает, пока сущность не в диалогах сессии. Группа при этом достижима через `@ATB_FANatik` (`GetFullChannelRequest.linked_chat_id`), и её комменты пришли именно оттуда. То есть строка реестра с числовым id — второй хэндл той же группы, и **напрямую этот коллектор её собрать не может никогда**; `+Ejz6ubzm21IyMTQy` остаётся нерезолвимым по той же причине.
**Два контроля после докачки:** дро перегнано и осталось `6f9fa245b9d70254` — 1 190 комментов легли в живой корень, а v1-only ридер их не увидел, что и есть проверка §5.14 на утечку объединения под нагрузкой; перепись перегнана, `ids_sha256 a56dc6dace1bfb44` и окно не сдвинулись (фаза постов не добавляла). `shasum -c results/raw_v1_baseline.sha256` → 6 из 6 OK.
