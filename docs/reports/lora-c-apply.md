# lora-c-apply — the two verdicts, amendment 3.25, and the count that refuses the ceiling

**Everything below cost $0. No pod, no endpoint, no paid call of any kind.** Both review gates are
applied and closed; the registration is re-issued as a DRAFT. **One thing STOPS back to the
operator, and it is the reality check amendment 3.25 (1) ordered against exactly this hazard.**

## Read back — one line each

**Amendment 3.25, three clauses:**

1. **`training.max_seq_len` rises 1 408 → 2 816 by a NEW REVISION of `config/qlora.yaml`** — frozen
   law, precedent 1 024 → 1 408 (operator, 2026-08-04); if the model tokenizer is importable, every
   one of the 506 rows is tokenized for real at $0 and **a true count above 2 800 STOPS the line**;
   lora-b's pin on the old revision stays sealed and is never re-pinned.
2. **The neighbour-selection rule gains ONE refusal** — a candidate whose whitespace-collapsed,
   casefolded text EQUALS the query's own is refused; equality and nothing below it; all packs
   rebuilt at $0; nothing of it touches a sealed record.
3. **STOP 1 is accepted AS BUILT** — 506 rendered rows, zero real `молочный_бренд` positives in arm
   A, the holdout's reachable maximum 98 of 100 with the bar's integer unchanged, and the nine rows
   of `@VARUS_channel:10367` stay unrendered.

**Gate 1, five named rulings:** `20696` «власний формат мережі» → «формат мережі» · `21190` a habit
claimed on a contentless join → the join named as a join · `578032` the rationale must argue the
label it carries, and the LABEL STANDS · `20766` PASSES and was ABSENT from the sample, so the
sample gains it · `49685` label `null` → `не_наш_рынок` via a labels r3 delta. Plus two enumerable
patterns, P-NULL and P-MARKER, with borderlines defaulting to KEEP.

**Gate 2, six rulings:** R1 the 15 flagged rows are right and the fix is the TEXT · R2
`mention_vs_about` 5 becomes genuinely instrumental · R3 seven rationales into Ukrainian · R4 the
real handle `@olega13` out · R5 no skeleton over a third of the 32 `молочный_бренд` rows · R6 a
seeded newline pass into ~30 rows. Plus «власний формат» → «формат мережі» in `non_dairy_brand`
25–26, and **NO LABEL CHANGES anywhere**.

All eleven documents were on disk before step 1: `docs/SPEC.md` amendment 3.25 (lines 1016–1048),
`docs/reviews/lora-c-rationales-verdict.md`, `docs/reviews/lora-c-synthetic-verdict.md`.

## Step 0 — baselines, at the opening tree

```
## Baselines — instrument output, 2026-08-23T07:34:45+00:00

- head: 97548dfdc1d0 on main
- porcelain:
     M docs/SPEC.md
     M docs/STATUS.md
     M knowledge/daily_logs/2026-08-23.md
     M knowledge/hot.md
     M knowledge/index.md
    ?? docs/PROMPT-lora-c-apply.md
    ?? docs/reviews/lora-c-rationales-verdict.md
    ?? docs/reviews/lora-c-synthetic-verdict.md
- census: brain-census: 10.3Ktok boot tax
- suite: 35 passed — whole suite, stamped 2026-08-23T07:23:54+00:00 at 97548dfdc1d0
- boot files: … = 181 lines / 23454 UTF-16 units
- preflight: 1561 pinned paths · 2789 pins · 130 records — 1523 match every pin on them, 38 carry an older pin
```

**The suite stamp said 35 and the tree was RED.** `make check` run live at that tree:
**8 failed, 3 650 passed, 2 skipped**, and every one of the eight is caused by the team lead's files
arriving — four are the landing manoeuvre amendment 3.25 owes (`amendment-3.25` is a marked block no
producer had looked at), four are lora-c's own records still saying `verdict_present: false`. Both
are this contract's work, and the first commit (`f5eda67`) is the team-lead files verbatim plus the
previous session's vault tail, staged by path and never `-A`.

**Step-0 sha for the closing diff: `f5eda670efa4931c7945191a3a783729143e4c8c`.**

## D1 — the gate-1 verdict

### 1–3. The four named rewrites, the r3 delta, and the sample addendum

The four rewrite texts are the verdict's own words, copied and not paraphrased. The `cue` FIELD
follows the inline cue where the verdict moved it — `21190` «Я» → «Я🤝», `578032` «Морозиво» →
«Мені чудово допомагає» — because every shipped row satisfies «the cue field is a substring of the
rationale» and a rewrite must too.

**`results/labels_pass1_r3.jsonl` is a DELTA, and the distinction is load-bearing.** r2 composes
with r1 by being a second **disjoint draw** of 150 units; r3 draws nothing — it is one correction
laid over the same keys, last wins. So it has no pack, and `build_pass1_sft.labelled_units`'s
pack-equals-labels refusal never sees it. `build_lora_c_data.apply_r3` refuses a key r1+r2 never
answered, because a row arriving through a «delta» would be a unit nobody drew:

```
$ python3.11 -m pytest tests/test_lora_c_prep.py::test_the_r3_delta_is_a_layer_and_refuses_to_be_a_draw -q
1 passed
```

Its scope is registered as narrowly as it is real: **the lora-c pool only.**
`results/pass1_sft_arm_a.jsonl` and `_b.jsonl` are sealed at bytes taken before the correction
existed and are never re-derived to follow a later ruling — asserted in both directions, so the
sealed arm still carrying `null` on `#49685` is a test and not a hope.

The sample addendum is appended by hand and **only** by hand: the producer now refuses to rewrite
`docs/reviews/lora-c-rationales-sample.md` at all (below), so nothing can overwrite it.

### 4. P-NULL and P-MARKER — the rule, the lists, and what was NOT rewritten

**The verdict's rule underdetermines, and the enumeration is a judgement it delegated.** Two
readings of «function vs subject after the dash» are both faithful to the words and land 35 rows
apart: a narrow one at 13 and a broad one («any discourse act with a topic complement») at 48. The
narrow one is applied, and the reason is the contract's own DO NOT list — «rewrite any rationale not
named by a verdict ruling» is forbidden, so over-application is a scope violation that cannot be
undone and under-application is a gap the team lead can rule on. The verdict installs the same
asymmetry: «Borderline calls default to KEEP and are listed as kept.»

**The rule as applied, stated so a reader can re-run it:**

> After the dash the span must name what the comment DOES and may relate it only to the thread's own
> participants and moves. It is a DEFECT when **(a)** the span opens with «про …», so it names no act
> at all and the words contradict the label directly, or **(b)** the act's complement is a definite
> THING — a product, a good, a substance — which is the subject the `null` label denies. Everything
> else is borderline and KEPT.

Clause (a) is purely lexical and carries no judgement — **11 rows**. Clause (b) is the verdict's own
exemplar and one row of the same shape — **2 rows**. Total **13 rewrites**, and the eleven kept
borderlines are the ones whose act carries an abstract topic rather than a thing.

| row | clause | was | now |
|---|---|---|---|
| `@mandziak:3775#49094` | a | …— про апетит власної дитини | …— відповідь у треді власним прикладом |
| `@matusi_ukr:22092#576913` | a | …— про апетит власної дитини | …— приєднання до попередньої репліки власним прикладом |
| `@matusi_ukr:22092#576969` | a | …— про власну лактацію при завершенні ГВ | …— відповідь співрозмовниці власним прикладом |
| `@matusi_ukr:22092#576970` | a | …— про власну лактацію при завершенні ГВ | …— відповідь співрозмовниці власним прикладом |
| `@matusi_ukr:22263#579243` | a | …— про підготовку власної дитини до школи | …— розповідь у треді власним прикладом |
| `@matusi_ukr:22263#579245` | a | …— про власну дитину і цифри | …— відповідь у треді власним прикладом |
| `@matusi_ukr:22308#580149` | a | …— про умови для переселенців у Норвегії | …— репліка в дискусії з посиланням на власний досвід |
| `@matusi_ukr:22308#580159` | a | …— про умови для переселенців у Словенії | …— репліка в дискусії з посиланням на чужий досвід |
| `@tarilka_malyuka:715#49` | a | …— про походження рецепта | …— відповідь авторці, що джерела вже не пригадує |
| `@ya_Nenka:836#2577` | a | …— про міфи материнства | …— заперечення почутого раніше твердження |
| `@ya_Nenka:836#2581` | a | …— про міфи материнства | …— згода з попередньою реплікою |
| `@tarilka_malyuka:695#8` | b | …— сумнів у першому продукті прикорму | …— емоційний вигук у відповідь на пораду *(the verdict's own rewrite, verbatim)* |
| `@matusi_ukr:22158#578035` | b | …— застереження проти полоскання содою | …— застереження у відповідь на пораду в треді |

**Clause (a) is asserted EMPTY after the pass**, so the rule and the list cannot part.

**P-MARKER: «Expected small» does not reproduce, and the reason is a denominator.** The rule as
written — subject named in the rationale body, absent from the comment, no «допис» — matches
**143 of 515**. Narrowed to what the marker MEANS («the marker is what makes post-derived subjects
honest»), i.e. the subject also occurs in the thread's POST text, it is **72**. Of those, **40** are
kept: the comment predicates the subject in its own words, or the rationale already carries a
derivation marker of its own («з оголошення», «на гіфці», «з фото», «за посиланням», «з попередньої
репліки») — adding «з допису» to those would state something false. **One** more is exempt because
the verdict rewrote it by name and the team lead's text is final (`@klopotenkofood:6032#21190`).
**31 markers added.** The rewrite is additive and changes nothing else, exactly as ruled.

The gap to «expected small» has a cause and it is not a disagreement about the rule:
**lens 2 judged 113 of 515 rationales, not all 515** (`docs/reports/lora-c-prep.md`, «The content
lens»). Its «17» and «24» are counts over a 113-row read, so they are not population counts and the
verdict's expectation was calibrated on a sample. 17 of 113 scales to ≈77 of 515 — which is what the
post-derived enumeration finds.

### 5. The hybrid grep, with its negative control

```
rationale bodies (cue excluded) carrying RU-only letters [ыэъё]: 0 of 515
control — the same grep over the WHOLE rationale (cue included): 38 rows
```

The verdict predicted zero and the zero holds. The control is the point: a grep that returns 0 and a
grep that is broken produce the same output, so the same expression is run over the text the cue is
in and returns 38 ([[a_checker_whose_failure_is_silence]]).

### 6. `rationale_reviewed: true` on all 515, flipped only after 1–5 landed

Every hand-written string was checked against four invariants before the file was written — ≤ 160
characters, no ASCII quote, no raw newline (the seventh state, Dv763), and the cue still occurring
in its own comment under `norm()`. **Invariant failures: `[]`.**

## D2 — the gate-2 verdict

**NO LABEL CHANGES**, asserted rather than intended: the applier compares every row's
`(subject_type, subject_id)` before and after and prints what moved — `labels_moved: []`. The
8/8/8/8/8 balance and the ±2 rule therefore survive by construction, and the rebuilt record confirms
it for all four classes.

| ruling | what landed | check |
|---|---|---|
| **R1** | 15 texts rewritten into thing-quality claims with the chain named only as WHERE; rationales follow | `self_flagged` — the instrument that FOUND them — now matches **0 of 160**, against 15 at the gate-2 sample |
| **R2** | `mention_vs_about` 5 → «Мороженое в тесто для кекса вместо масла, тесто мягче.» — instrumental, no habitual-use reading; label unchanged | the row now sits with rows 1–4 and 6–8 by shape |
| **R3** | seven rationales into Ukrainian («лише ЗГАДУЄ …; насправді про …») | 0 of 160 rationale bodies carry RU-only letters |
| **R4** | `@olega13` → `@kavuncha_77` and `@lira_pobut`; texts otherwise unchanged | below |
| **R5** | 10 rows moved off two over-full skeletons | before/after below |
| **R6** | seeded newline pass, 30 rows | seed `20260823`, registered, idempotent |
| **consistency** | «власний формат» → «формат мережі» in `non_dairy_brand` 25–26; row 27's «власна торгова марка» STAYS | — |

**R4 — which check ran, and which could not.** One store record opened first, as the contract asks:

```
ONE COMMENT RECORD, keys: ['at', 'channel', 'model_revision', 'msg_id', 'parent_msg_id',
                           'post_state', 'prompt_sha256', 'record_type', 'rendering', 'reply',
                           'row_kind', 'served_by', 'task']
```

**The store carries no author field at all**, so the author check the contract conditions on does
not exist to run. What ran is the substring check against the window's raw comment TEXTS, plus a
both-directions containment check against the 19 handles that appear in them. `@olega13` occurs
nowhere in the synthetic file afterwards (`grep -c` → 0).

**R5 — the skeleton measure is an instrument now, and it reproduced the verdict's number untuned.**
`build_lora_c_synthetic.skeletons()` was written from the verdict's own description of the frames
and run once:

| skeleton | before | after |
|---|---|---|
| brand + product + property | **16** | 9 |
| brand + predicate | **12** | 9 |
| brand + preposition | 4 | 4 |
| not-brand-initial | 0 | 7 |
| question | 0 | 3 |
| **ceiling (a third of 32)** | 10 | 10 |
| **passes** | ❌ | ✅ |

The 16 is the verdict's 16, on the first run, with no tuning — and the instrument found something
the prose did not: **a SECOND skeleton over the ceiling.** «Rewrite enough of the 16» names one
frame; the rule «no single skeleton over a third of the 32» binds on two, because `brand +
predicate` carried 12. Both are down to 9.

**R6 — the pass is idempotent by construction.** It collapses every text to one line before the
draw, so it is a function of (seed, collapsed texts) and of nothing it wrote itself; re-running it on
its own output reproduces it, and the record computes that property at every build
(`newline_pass.reproduces: true`). No break is ever placed at the end of a text, because
`register`'s `no_terminal_punctuation_pct` reads `text[-1]` and a trailing newline would move an axis
the verdict deliberately left alone.

### Both instruments, re-run over the NEW texts

**Contamination — the four lists, with the 6-gram index rebuilt over the rewritten rows:**

| corpus | rows | collisions |
|---|---|---|
| labelled rows | 650 | `[]` |
| holdout rows | 100 | `[]` |
| reference-thread comments | 106 | `[]` |
| gold quotes | 67 | `[]` |

**The register table — the newline axis is present, and three gaps closed on their own:**

| axis | the window's 515 | these 160 | delta | matched at ±3 |
|---|---|---|---|---|
| carries an emoji % | 19 | 20 | +1 | ✅ |
| opens lowercase % | 6 | 5 | −1 | ✅ |
| **carries a newline %** | **19** | **19** | **0** | **✅ (new axis, R6)** |
| no terminal punctuation % | 59 | 58 | −1 | ✅ *(was −5)* |
| carries a question mark % | 13 | 10 | −3 | ✅ *(was −5)* |
| words, median | 11 | 8 | −3 | ✅ *(was −4)* |
| characters, median | 68 | 46 | −22 | ❌ — deliberate, named |

**Four of six axes missing became ONE of seven, and not by tuning an axis.** R1 and R2 replaced
fifteen stock-and-price fragments with thing-quality claims and R5 broke ten skeletons into
question, reply and anecdote forms — all longer and more punctuated than what they replaced. The
record now emits an exception only for an axis that actually misses, and names the three that
closed, because three standing «deliberate gap» notes would otherwise have outlived their gaps.

`reviewed: true` on all 160, flipped only after every rewrite landed.

## D3 — amendment 3.25 (1): `config/qlora.yaml` revision 2, and the count that STOPS the line

**Revision 2 is an in-place edit of `config/qlora.yaml`, and that is the project's own vocabulary,**
checked rather than assumed: `git log --follow` shows the file has exactly two commits, so the
precedent the amendment cites (1 024 → 1 408, 2026-08-04) was itself an in-place revision; and
amendment 3.21 names the two SEALED configs by hand — `config/registry.yaml` and
`config/lexicon.yaml` — and this is not one of them.

**Line B's three sealed artefacts rebuild BYTE-IDENTICAL under revision 2.** This is the control the
amendment's «stays sealed» rests on, and it is a measurement:

```
$ PYTHONPATH=src python3.11 scripts/build_pass1_sft.py --outdir <tmp>
IDENTICAL  pass1_sft_arm_a.jsonl
IDENTICAL  pass1_sft_arm_b.jsonl
IDENTICAL  lora_b_smoke_pack.json
```

It could not have been otherwise and the record says why: `dropped_for_length.n` is **0** at 1 408
and `longest_kept` is **1 222**, so raising the ceiling cannot return a row that was never dropped.
`results/pass1_sft.json` is NOT regenerated — two sealed records pin its bytes — and its
`config_sha256` and `length.max_seq_len` now describe revision 1, which is what a sealed record doing
its job looks like.

### The reality check RAN, and it STOPS the line

The tokenizer is obtainable at $0 — the model is already in this machine's Hugging Face cache at the
pinned revision. **The quantity measured is the pod's own**: `len(apply_chat_template(prompt)) +
len(target)`, which is `train_qlora.encode_pass1`'s refusal expression, by the same calls in the same
order.

**Why `AutoTokenizer` is not a substitute here, proven rather than assumed.** The pod loads the
PROCESSOR and `train_qlora.text_tokenizer` unwraps it, and that module's own docstring warns the two
chat templates are not guaranteed equal. `AutoProcessor` will not import on this machine —
`Gemma4ImageProcessor` needs `torchvision`, an image dependency a text-only count has no use for. So
the equality was established from the repository: the snapshot carries exactly one template file,
`chat_template.jinja`, and the processor-specific `chat_template.json` is recorded by the Hub as
**not existing**. One file, one template, both loaders. The producer re-derives that at every run and
refuses all three states that would make it unprovable.

```
$ PYTHONPATH=src python3.11 scripts/tokenize_lora_c_rows.py
wrote results/lora_c_tokens.json  506 rows
  pod count      min 1445  median 1633.0  max 2975
  + TEMPLATE_SLACK 16:  max 2991  against max_seq_len 2816 and the STOP at 2800
  over 2800:  2 by the TRUE count,  3 with slack  (@matusi_ukr:22327#580336)
  over max_seq_len 2816:  2 under BOTH readings
  STOP — a true count above 2 800 on 2 of 506 rows, and 2 of them are over max_seq_len 2816
  itself. The STOP fires under BOTH readings of the threshold. Back to the operator; the next
  rung is 3 072
```

**The threshold has two readings and the record publishes both, because one row sits between them.**

| row | the tokenizer's count | + `TEMPLATE_SLACK` | over 2 800? | over `max_seq_len` 2 816? |
|---|---|---|---|---|
| `@tarilka_malyuka:746#83` | **2 975** | 2 991 | yes / yes | **yes / yes** |
| `@klopotenkofood:6060#21366` | **2 963** | 2 979 | yes / yes | **yes / yes** |
| `@matusi_ukr:22327#580336` | 2 785 | 2 801 | **no** / yes | no / no |
| `@matusi_ukr:22111#577092` | 2 778 | 2 794 | no / no | no / no |

**2 of 506 by the true count, 3 with slack.** Amendment 3.25 (1) says «a **true count** above
2 800», and the true count is the tokenizer's own: `TEMPLATE_SLACK` = 16 existed to correct a
CHARACTER-RATIO estimate for the chat template, which `apply_chat_template` here already counts, so
adding it back double-counts the template — at exactly the row that decides 2 against 3.
`@matusi_ukr:22327#580336` clears the true threshold by 15 tokens and lands one token over it with
slack.

**The STOP is invariant to that question, and that is what makes it a finding rather than a wobble.**
Two rows exceed `max_seq_len` **2 816 itself** under BOTH readings — those are the rows
`train_qlora.encode_pass1` would refuse on the pod — and 2 975 and 2 963 are over 2 800 on the raw
count with no slack argument available.

**The three-ratio model re-printed against 2 816, beside the count:**

| tokens-per-character | model's max | over 2 816 | the TOKENIZER's max |
|---|---|---|---|
| 0.291741 — the registered worst | 2 763 | **0 of 506** | **2 991** |
| 0.282802 — the median | 2 679 | 0 of 506 | 2 991 |
| 0.269618 — the minimum | 2 555 | 0 of 506 | 2 991 |

**The model clears the new ceiling on every row and the count does not.** It under-predicts the
widest row by **228 tokens**, which is precisely the hazard 3.25 (1) ordered the check against
(Dv757: a ratio is a MODEL carried across a change of row length).

**It is not this contract's doing, and the two-reading split makes that sharper.** Run over the rows
as they stood at `97548df` — before the gate-1 rewrites and before the neighbour refusal — the same
tokenizer counts **2 974, with TWO rows over 2 800 under BOTH readings** and two over `max_seq_len`.
So **the true-count answer is 2 at both shas and this rebuild did not move it**; what moved is the
with-slack answer, 2 -> 3, because `@matusi_ukr:22327#580336` went 2 784 -> 2 785 and crossed the
slack boundary by ONE token. The finding predates the contract that reports it.

The amendment's own next rung fits: 2 991 sits under 3 072 with 81 tokens to spare on the
conservative reading and 97 on the true count, and 3.25 (1) already calls it «one word, not a
redesign». **This contract does not take it.** Raising frozen law is the operator's word.

### What the STOP stops, and why D4 and D5 ran anyway

D3's instruction is «STOP back to the team lead if any row exceeds 2 800», and this report then
presents a completed D4 and D5. The reading applied, stated so it can be overruled: **the STOP is on
the paid line, not on the $0 contract.** Amendment 3.25 (1) scopes it in its own words — «**before
`lora-c-run` buys anything**: … a true count above 2 800 STOPS the line back to the operator» — D5
of this contract requires «the tokenizer result from D3» inside the registration, which is only
writable if D5 runs, and the contract closes with «STOP after the report for team-lead acceptance».
So the whole $0 scope was completed and the STOP is carried into the registration, the ADR and the
report rather than into an unfinished tree.

**What was NOT done on the strength of it:** the 3 072 rung was not taken, no threshold was moved, no
row was dropped or shortened, and nothing about the run was registered as priced. If the intended
reading was «stop the contract at D3», the cost of this choice is that D4 and D5 must be re-accepted
rather than re-done — every artifact they produced is committed and re-derivable.

## D4 — amendment 3.25 (2): the equality refusal, and the full rebuild

**The refusal is a pre-filter on the candidate pool, not an edit to
`build_pass1_fewshot_packs.neighbours`, and that is forced by the amendment's own sentence.** Ten
sealed records pin that file's sha256 (`make preflight ARGS='build_pass1_fewshot_packs'`), and 3.25
(2) says «nothing of this touches a sealed record». Removing the equal-text candidates before the
call is the same function of the same inputs: the Jaccard rule, the total tie key and the own-thread
block are all still the sealed producer's, and there is exactly one selection rule rather than a
second copy of it ([[a_moved_guard_that_left_its_copy]]).

**The enumeration ran standalone BEFORE any renderer changed**, because `neighbours` raises
`SystemExit` on an empty class and a producer that exits writes nothing and reports nothing:

```
population: training / shared pool (515 as queries)     population: eval set E (200 pairs)
  unrenderable before the amendment       9               unrenderable before the amendment       2
  own text in examples BEFORE            13               own text in examples BEFORE            22
  own text in examples AFTER              0               own text in examples AFTER              0
  queries whose five moved               13               queries whose five moved               22
  queries that cannot fill five AFTER     0               queries that cannot fill five AFTER     0
                                                          of the 22, holdout rows                 5
```

**No query loses its k-th neighbour. There is no new instance of STOP 1.** The 13 and the 22 and the
5 are lens 3's own numbers, reproduced independently before anything was changed.

**From my own scan of the rebuilt packs:**

```
THE DISJOINTNESS BATTERY, by the (thread, msg_id) PAIR
  training rows                                   506
  E items per leg                                 198
  training ∩ E                                      0
  training ∩ holdout-100                            0
  training ∩ gold-14 (by msg_id)                    0
  training rows inside a reference thread           0
  example ids outside the shared pool (train)       0
  example ids outside the shared pool (E)           0
  training rows shown a neighbour from own thread   0
  E items shown a neighbour from own thread         0
  synthetic ids anywhere in pool / train / E        0
  training ∩ E by msg_id ALONE                      1   <- the id-space trap, still 1

EXAMPLES AT SIMILARITY EXACTLY 1.0 — the own-text leak
  training rows                                   0 of 506
  E items                                         0 of 198
  highest similarity anywhere now              0.9875
```

**0.9875 is the number that proves the threshold is equality and nothing below it.** It is the value
lens 3 named as the highest similarity strictly below 1.0 in this window, and it is now the maximum:
the refusal removed exactly the equality tier and left the continuum beneath it untouched.

`results/lora_c_eval_pack.json::own_text_in_examples` reads **0 of 198, 0 on the gating bar**, and
the block keeps measuring rather than asserting — a selection rule that ever stopped refusing would
show up there as a number and not as a missing guard.

**The pass-2 pack does not render example blocks at all** (it calls no neighbour rule), so it
rebuilds byte-identical, and `--reproduce` still reads what r2 measured:

```
$ PYTHONPATH=src python3.11 scripts/build_lora_c_pass2_pack.py --reproduce
threads selected: 11 of the 16 reference threads
  replies parsed 11 · refused []
  bar 1: 4 of 5  (r2 measured 4 of 5)
  bar 3: 2 signals  (r2 measured 2)
  AGREES: True
```

## D5 — registration DRAFT v2

New shas for every rebuilt input. The r3 delta is pinned as a written input with its kind and its
scope. The sampler was driven again over the r3-corrected classes — `не_наш_рынок` 268, `null` 164,
`категория_личное` 40, `сеть_ритейлер` 34 — and the measured draws are 125.9–127.6 per class per
epoch over 60 epochs. Both `steps` numbers stand: 62/82 for the optimizer, 64/84 `planned` for the
LR schedule. The three rates and their invalidation conditions are unchanged. The money block is
still **OPEN**, the cap is still $4.00, and `test_the_registration_carries_no_price` still passes.

`config_agrees_with_lora_b` is now **`false`**, and the field publishes the false value with its
reason rather than being deleted — a boolean whose honest value inverted is not a field to remove.

All three clauses of amendment 3.25 are quoted into the record and **grepped back into
`docs/SPEC.md` at every build**, whitespace-normalised, by the same machinery that greps the STATUS
rulings. A quotation nobody grepped is a paraphrase with quotation marks around it.

**The reachability blocks are still four**, and the second one is renamed to what it now says:
`three_rows_do_not_fit_max_seq_len`, carrying the count, the STOP, and — nested under
`superseded_by` — the ratio table that DERIVED 2 816, so a reader can see what was derived from
what.

### The samples are frozen, and the guard says so

Both `--sample` renderers now refuse to rewrite their file once its verdict exists. The file a team
lead read is the evidence their ruling rests on, and re-rendering it from data the ruling has since
moved would hand a later reader a document nobody reviewed under the name of one that was. The
rebuild-identity test still DRIVES `--sample` (so the renderer and the guard both execute) and no
longer compares those two files; `test_a_sample_is_closed_by_its_verdict` is the guard's control,
in both directions.

This is also why `docs/reviews/lora-c-synthetic.md` is unchanged: it is **outside this contract's
commit list**, and the gate-2 rewrites would otherwise have moved it.

## Verification, at the closing tree

```
## Baselines — instrument output, 2026-08-23T09:00:07+00:00

- head: 681b11ea9598 on main
- porcelain:
    (clean)
- census: brain-census: 10.3Ktok boot tax
- suite: 3668 passed / 2 skipped — whole suite, stamped 2026-08-23T08:58:08+00:00 at f24352e1a41e
- preflight: 1563 pinned paths · 2794 pins · 132 records — 1525 match every pin on them, 38 carry an older pin
```

`make check` on a STABLE tree: **3 668 passed, 2 skipped, exit 0.** Every one of the six producers
was then re-run and `git status --porcelain` is empty — every shipped artifact is what its producer
emits today. Re-run once more after Dv777's correction, at `1d0fe4f`: **3 668 passed, 2 skipped,
exit 0**, stamped `2026-08-23T09:44:37+00:00`.

**The older-pin delta is 38 → 38, and NOT because nothing moved.** `config/qlora.yaml`'s revision is
invisible to `preflight`: its pin lives at `results/prereg_lora_b.json::instruments.config_sha256`,
a sha field whose siblings are `parser`, `trainer`, `transport` — none of them a `PATH_KEYS` name —
so the walk cannot resolve it to a file and the pin is not in the registry at all. The amendment
says «verify with `make preflight`», and preflight would have returned green for the wrong reason.
What verifies it instead is the enumerated diff above, plus the byte-identical rebuild of all three
sealed artefacts. Of the three paths preflight DOES report as moved, two were already older pins at
step 0 (`scripts/build_pass1_sft.py`, `docs/SPEC.md`) and the third —
`scripts/write_prereg_5c2.py` — was too: at `f5eda67` it already hashed to
`4ddd937d…` against the pinned `1477512a…`.

## Deviations from Dv765

Each with its cause tag from the closed enum v2. **Every one was found at $0; there is no money on
this contract.**

| # | cause | what |
|---|---|---|
| **Dv765** | `[cause: model]` [[projected_rate_versus_measured_rate]] | **The ceiling the amendment derived does not hold, and the real tokenizer is what says so.** 2 816 comes from 2 759 — the widest row at the registered worst tokens-per-character. The count is **2 975 (2 991 with slack)**: **two rows** over the amendment's own 2 800 stop by the true count, three with slack, and **two over `max_seq_len` 2 816 itself under both readings**. The model under-predicts by **228 tokens** because it was measured on 2 778–3 923-character rows and applied to rows of 5 262–9 415. **It is not this rebuild's doing**: at `97548df` the same measurement returns 2 974 and TWO rows over under both readings — the true-count answer never moved. The line STOPS back to the operator; 3.25 (1)'s own next rung, 3 072, clears 2 991 by 81. |
| **Dv766** | `[cause: process]` [[a_count_in_prose_is_not_the_enumeration]] | **A reviewer's counts were read as population counts and they are sample counts.** The verdict says P-MARKER is «expected small» on the strength of «lens 2's 17 candidates», and lens 2 judged **113 of 515** rationales. 17 of 113 scales to ≈77 of 515, which is what the enumeration finds (72 post-derived). The same applies to «24 null rows». The expectation was never wrong about the shape; it was calibrated on a third of the corpus and quoted as if on all of it. |
| **Dv767** | `[cause: contract-gap]` [[a_borrowed_rule_carries_an_unstated_population]] | **P-NULL's rule underdetermines by 35 rows and the choice is mine.** «Function vs subject after the dash» reads narrow (13) or broad (48) with equal fidelity to the words; the verdict's four ACCEPTED examples all have thread-internal complements and its one FLAGGED example has a definite product, which is evidence for the broad reading, and the DO NOT list forbidding rewrites of unnamed rows is decisive against it. Narrow applied, rule written out verbatim, every borderline listed as kept, and the gap to lens 2's 24 reported instead of tuned away. |
| **Dv768** | `[cause: verify-gap]` [[a_moved_guard_that_left_its_copy]] | **«Nothing of this touches a sealed record» is only satisfiable one way.** `scripts/build_pass1_fewshot_packs.py` is pinned by **ten** sealed records, so editing `neighbours` in place would break every one of them. The refusal is therefore a pre-filter at lora-c's two call sites — the same function of the same inputs, one selection rule and not two copies of it. Found by running preflight on the file the contract named before editing it. |
| **Dv769** | `[cause: tooling]` [[a_checker_whose_failure_is_silence]] | **The instrument the amendment names for the verification cannot see the pin in question.** `preflight`'s registry is seeded from sha fields joined to a sibling naming a path; `instruments.config_sha256` has no such sibling, so `config/qlora.yaml` is not in the registry and its moved pin is invisible. Older pins read 38 → 38 across a revision of that file. Verified instead by the enumerated record diff and the byte-identical rebuild of all three sealed line-B artefacts. |
| **Dv770** | `[cause: verify-gap]` [[corrections_break_derivations]] | **Three records kept asserting the world they were written in.** `own_text_in_examples.remedy_named_not_taken` still said the remedy was the operator's to take, after the operator took it; `self_flagged.action_taken` still said «NONE — review gate 2 is a STOP», after R1 ruled; and the register block still carried named-deliberate excuses for three axes that now match at ±3. Each was rewritten to DERIVE its reading from the measurement, so a state change cannot leave prose behind again. |
| **Dv771** | `[cause: process]` [[the_prompt_must_carry_the_annotators_law]] | **A pattern pass edited a row the verdict had rewritten by name.** P-MARKER matched `@klopotenkofood:6032#21190` — whose text the team lead supplied verbatim — and inserted «з допису» into it. Caught by reading the applier's own log rather than its exit code. Named rows are now exempt from both patterns: where a named ruling and an enumerable pattern meet, the named ruling is the law. |
| **Dv772** | `[cause: model]` [[a_count_in_prose_is_not_the_enumeration]] | **The skeleton ceiling binds on two frames and R5's prose names one.** `skeletons()`, written from the verdict's own description and run once, reproduced its «16 of 32» exactly — and showed `brand + predicate` at **12**, also over the third-of-32 ceiling the same ruling sets. «Rewrite enough of the 16» would have left the class failing its own rule. Both are down to 9. |
| **Dv773** | `[cause: verify-gap]` [[unreadable_now_versus_never]] | **My own guard fired on my own bug, which is what a guard is for.** `try_to_load_from_cache` has THREE outcomes — a path, a sentinel meaning «the Hub says this file does not exist», and `None` meaning nobody ever asked — and the first version of the template check read two of them. It refused the measurement, correctly by its own lights, on a repository that satisfies the condition. Absent and unasked are different answers, and only one licenses a tokenizer stand-in. |
| **Dv774** | `[cause: contract-gap]` [[a_test_that_reads_a_shipped_artifact]] | **A review sample is byte-compared by the suite and is also the evidence a verdict rests on.** Rewriting rationales and synthetic texts moves both sample files, and `docs/reviews/lora-c-synthetic.md` is outside this contract's commit list entirely. Resolved by making the verdict CLOSE its own sample: the producer refuses to rewrite it, the rebuild test still drives `--sample` and no longer compares those two files, and the gate-1 addendum is appended by hand under a guard that guarantees nothing will overwrite it. |
| **Dv775** | `[cause: process]` [[the_fix_widened_the_denominator]] | **A metric improved without being targeted, and the record nearly took credit for tuning.** Four of six register axes missed at the gate-2 sample; one of seven misses now. Nothing aimed at those axes: R1/R2 replaced fifteen price-and-stock fragments with thing-quality claims and R5 rewrote ten skeletons into longer forms. Recorded with its cause, because «the register now matches» read as an achievement of the rewrite would be a claim about an instrument nobody moved. |
| **Dv777** | `[cause: verify-gap]` [[two_values_for_one_input_get_quoted_kindly]] | **The stop threshold has two readings and the record published one — the larger.** `rows_over_the_stop_threshold` tested `pod_count + TEMPLATE_SLACK > 2 800` and reported **3**; the amendment's words are «a **true count** above 2 800» and the true count is **2**. `TEMPLATE_SLACK` = 16 exists to correct a CHARACTER-RATIO estimate of the chat template, which `apply_chat_template` already counts here, so the +16 double-counts — at exactly the row that separates 2 from 3. `@matusi_ukr:22327#580336` reads 2 785 raw and 2 801 with slack: over by ONE token under a correction that should not have been applied. Both readings are now published with the row that separates them, and the STOP is shown invariant to the question — two rows are over `max_seq_len` 2 816 itself under either. **This is the seventh instance of one input with two values on this line**, and the first where the wrong one was the number an operator's ruling rests on. |
| **Dv778** | `[cause: contract-gap]` [[the_contracts_scope_is_narrower_than_the_rulings]] | **D3 says STOP and D4, D5 and the report ran after it.** The reading applied — the STOP is on the PAID line, not on the $0 contract — rests on three sentences: amendment 3.25 (1) scopes it «before `lora-c-run` buys anything», D5 of this contract requires «the tokenizer result from D3» inside the registration, and the contract closes with «STOP after the report for team-lead acceptance». Recorded as a deviation rather than left as prose, because a team lead reading a STOP instruction followed by two more phases is owed the reasoning and the option to overrule it. Nothing was taken on the strength of it: the 3 072 rung is untouched, no threshold moved, no row dropped. |
| **Dv779** | `[cause: process]` [[a_review_that_verifies_a_moving_tree]] | **The second skeptic was launched on a frozen sha and returned nothing — the second time on this line.** It was given both shas, the checklist and the DO NOT list; it ran 28 minutes and a full `make check` of its own, then answered three requests for its text — one of which offered a one-line `NOTHING TO REPORT` — with four empty idle notifications. `lora-c-prep` closed with the same shape (Dv748: five lenses, none answering for 95 minutes). **The construction is right and the apparatus does not answer**, and this row exists so «reviewed» is not read off a section that says nothing. What replaced it is the same checklist run by ME over the same range, recorded as the executor checking his own work; every DO NOT clause holds, and the one question an executor cannot answer about himself — whether the P-NULL rule I wrote is the rule the verdict meant — is left open in the report rather than closed by my own re-reading. |
| **Dv776** | `[cause: process]` [[a_review_that_verifies_a_moving_tree]] | **The opening tree was RED and the suite stamp said 35.** Eight of 3 658 failed, all of them caused by the team-lead files already on disk before the first commit: four the landing manoeuvre for `amendment-3.25`, four lora-c's records still saying `verdict_present: false`. Recorded at step 0 rather than discovered at D5 — the baseline instrument's stamp (35 passed) was a partial run and the live `make check` is what said so. |

**The tally, by the grep the template names:**

```python
import re, pathlib, collections
flat = " ".join(pathlib.Path("docs/reports/lora-c-apply.md").read_text(encoding="utf-8").split())
tag = {}
for chunk in re.split(r"(?=\*\*Dv\d+)", flat):
    if (m := re.match(r"\*\*Dv(\d+)", chunk)) and (t := re.findall(r"\[cause:\s*([a-z-]+)\]", chunk)):
        tag.setdefault(int(m.group(1)), t[0])
inr = {d: t for d, t in tag.items() if 765 <= d <= 779}
health = sum(1 for t in inr.values() if t in ("contract-gap", "spec-gap", "verify-gap"))
print(len(inr), dict(collections.Counter(inr.values()).most_common()))
print("contract health", health, "· paid", len(inr) - health, "· enum canonicity", len(inr), "of 15")
```

## Process signals

1. **The reality check earned its place on the first try.** Amendment 3.25 (1) could have raised the
   ceiling and moved on; instead it ordered a $0 count against the model that produced the number,
   and the count refused it. Three rows and 228 tokens is the whole finding, and it was unreachable
   from any amount of re-reading the ratio table.
2. **A reviewer's count is a reading of a sample until it says otherwise.** «17 candidates», «24 null
   rows», «16 of 32» — two of the three scaled badly and one reproduced exactly, and the difference
   was a denominator sitting in a section of my own previous report that I had not read. The lesson
   is not «distrust the lens» but «find the lens's population before quoting its integer».
3. **The rule the verdict delegated was the hardest thing here, and it is not a measurement.** P-NULL
   underdetermines by 35 rows. What settled it was not a sixth candidate rule but the contract's own
   asymmetry: an over-broad rewrite cannot be undone and an under-broad one is a list the team lead
   can rule on. Writing the applied rule out verbatim is the only thing that makes the choice
   reviewable.
4. **Prose in a record outlives the state it describes, three times in one contract.** Every one was
   a sentence that had been TRUE and became a standing excuse — a remedy «not taken» after it was
   taken, a concern «NONE — the gate decides» after the gate decided, three named-deliberate gaps
   after they closed. All three now derive their reading from the measurement beside them.
5. **The guards that fired were mine, on me.** The template check refused my own two-state reading of
   a three-state API; the applier's log caught a pattern pass overwriting the team lead's own words;
   the enumerated-diff test caught a config value moving where only a sha was expected. None of the
   three was found by reading the code that contained it.

## The second-skeptic debt — launched, and it did not return

Launched against `git diff f5eda670efa4931c7945191a3a783729143e4c8c..4c7947891197c5c0db099cf1cc21805bee49bdb3`
with the contract's checklist («does every change trace to a verdict ruling or to 3.25; was anything
else touched»), the two shas to quote back, and the DO NOT list. **It ran for 28 minutes, executed a
full `make check` of its own, and delivered no verdict.** Three requests for its text — including one
that offered `NOTHING TO REPORT` plus a single line naming what stopped it — returned four empty idle
notifications and nothing else. **There is no verdict to paste, and this section is not a clean bill.**

**Why the diff starts at `f5eda67` and not at `97548df`**, which the step-0 baseline names as head:
`f5eda67` is the team-lead files' OWN commit, so a diff from `97548df` would show `docs/SPEC.md` and
`docs/STATUS.md` as changes of this contract, which they are not.

**And its range ends before this contract's last correction.** The review was given
`..4c7947891197c5c0db099cf1cc21805bee49bdb3`, which is the closing commit the contract's final step
names — so Dv777 (the stop threshold's two readings) and Dv778 (why D4 and D5 ran past D3's STOP)
landed AFTER it and were never in its subject. Both came from the advisor rather than from this
review, and the correction they produced moved the headline integer from 3 to 2.

**What stands in its place is named as MINE, not as independent.** The same checklist, run by the
executor over the same range, is not a second skeptic — it is the first one checking its own work,
and it is recorded that way:

```
rationale rows changed in total : 48        =  31 markers + 4 named + 13 P-NULL
  of which a marker was ADDED   : 31
labels changed in rationales    : 1         <- the r3 row, and only it
rationale_reviewed all true     : True | n 515
synthetic labels changed        : 0         <- gate 2 moved no label
synthetic texts changed         : 51        =  15 R1 + 1 R2 + 2 R4 + 10 R5 + 30 R6, overlapping on 7
synthetic reviewed all true     : True | n 160
gate-2 sample touched at all    : NO        <- outside the commit list
team-lead files touched         : NONE      <- STATUS, SPEC, PROMPT-*, both verdicts
sealed records touched          : NONE      <- prereg_lora_b, lora_b_verdict, pass1_sft,
                                               prereg_5c2_run, both sealed arms, labels r1/r2,
                                               prompts.py, registry.yaml, lexicon.yaml
gate-1 sample diff              : +22 lines, one appended block, nothing above it moved
```

Every DO NOT clause the contract lists is checked above and holds. What NOBODY independent checked is
the thing an executor is worst placed to check: whether the P-NULL rule I wrote is the rule the
verdict meant, and whether all 48 rationale rewrites trace to a ruling rather than to my reading of
one. **That question is open and it is the first thing a reviewer should take.**
