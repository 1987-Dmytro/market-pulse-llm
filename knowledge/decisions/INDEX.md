---
type: index
tags: [decision]
---

# Decision records

One ADR per recorded decision, backfilled 2026-07-28 from docs/SPEC.md revisions, docs/STATUS.md
and the docs/frozen-testsets.md changelog. Russian summaries of the same decisions live in
docs/STATUS.md; these are the English long form with the numbers.

| date | decision | status |
|---|---|---|
| 2026-07-26 | [[telegram-only-mvp]] — Telegram only, $0 data budget | accepted |
| 2026-07-26 | [[retail-chains-pivot]] — observe retail chains, not producers | accepted |
| 2026-07-26 | [[category-and-watchlist]] — dairy + ice cream, 20-brand watchlist | accepted |
| 2026-07-27 | [[en-out-of-gates-3.1]] — EN out of the per-language gates | accepted |
| 2026-07-27 | [[znizhki-ua-removed]] — dead aggregator dropped from the registry | accepted |
| 2026-07-27 | [[testset-refreeze-v2]] — 11 operator-approved test-set corrections | accepted |
| 2026-07-27 | [[train-recalibration-v2]] — train sources recalibrated to the v2 reading | accepted |
| 2026-07-28 | [[hybrid-sarcasm-holdout-3.2]] — hybrid 108-row holdout for G1b | accepted |
| 2026-07-28 | [[holdout-residual-thread-leak-accepted]] — 31 rows share threads with training | accepted |
| 2026-07-28 | [[architecture-stack]] — serverless inference · Mac cron · SQLite · Streamlit | accepted |
| 2026-07-28 | [[gpu-provider-runpod]] — RunPod, Vast.ai as the experiment fallback | accepted |
| 2026-07-28 | [[frontier-api-reference-baseline]] — Claude Haiku as a reference row, not a gate | accepted |
| 2026-07-28 | [[g1d-gate-clarification-3-3]] — G1d gates the 3-class post type; relevance reported only | accepted |
| 2026-07-28 | [[synthetic-sarcasm-augmentation]] — 600 synthetic sarcastic rows, ablation-gated | accepted |
| 2026-07-31 | [[3b-infra-and-precision]] — 3b zero-shot on OpenRouter at pinned fp8, $8 cap | accepted |
| 2026-07-31 | [[phase4-base-model-gate]] — base model gemma-4-31b-it; the unpaired 27B row cannot flip it | accepted |
| 2026-08-01 | [[phase4-own-pod-anchor]] — the own-pod NF4 row anchors G1d/G1e; G1b slice = 44 ids; greedy is not batch-invariant | accepted |
| 2026-08-01 | [[4b-training-contract]] — frozen QLoRA hyperparameters; a training example is an eval prompt; the second arm projects over the 4 h ceiling | accepted |
| 2026-08-02 | [[phase4-gate-verdict]] — both arms trained and scored once; the rule drops synthetic; 2 of 5 Tier-1 gates pass | accepted |
| 2026-08-02 | [[phase45a-ceiling]] — 244 blind verdicts: the per-head ceiling in metric and accuracy units | proposed |
| 2026-08-02 | [[test-v3]] — 38 blind verdicts applied beside v2; intents pending the law review | proposed |
| 2026-08-02 | [[taxonomy-v2-relabel-and-appetite]] — all 1,912 rows up-labelled after the ≥90% gate; three boundary calls ratified; v2 copies beside originals | accepted |
| 2026-08-02 | [[45g-parent-context-and-uplabel]] — the parent post joins the v2 prompts as new revisions; the 97 emptied rows re-asked; the 1,912 up-label goes as a calibrated precheck | accepted |
| 2026-08-03 | [[45g2-captions-and-quiz-rulings]] — the 11/20 quiz applies 11 rows plus one validated pattern; media-only posts get an image caption or a poll transcript; the sitting is resealed | accepted |
| 2026-08-03 | [[45g3-sitting-gates]] — all three strata FAIL (88/89/81); guideline v2.1 from the sitting rulings; the whole 1,912 re-labelled under `precheck_v2.1_with_post` and a fresh blind hundred sealed | accepted |
| 2026-08-03 | [[45g4-v22-affirmative-rewrite]] — the v2.1 rulings restated affirmatively as v2.2 (FORM-ONLY); a 100-row in-sample probe pre-registered and gated before any full re-run | accepted |
| 2026-08-03 | [[45g5-features-over-prompts]] — the probe's KILL closes the prompt-form track; 35 adjudicated verdicts written into the batch; the collector learns `reply_to`; both families measured, and a naive reply rule would cost 62 of 258 judged-correct rows | accepted |
| 2026-08-03 | [[45g6-context-lines-probe]] — six dictated verdicts complete the adjudicated 40; the two features rendered as facts beside the post (RENDER-ONLY, no prompt text moves); KILL at 41/58 preserved with feature-fixed 9/17 — the fact behaved like the rule it was chosen instead of | accepted |
| 2026-08-04 | [[45h-v4-and-the-precheck]] — the confound caught before the money: both arms under the v2 law; test v4 = v3 → the 508-row intents pass → 31+1 operator rulings on top; the 54 single-homed at 917 | accepted |
| 2026-08-05 | [[45h2-ablation-verdict]] — both arms trained and scored once on test v4; the пласт loses G1c by 4.05 pp and is DROPPED; 3 of 5 Tier-1 gates pass, G1c short by 0.0005 and not retried | accepted |
| 2026-08-06 | [[5a-census-api-and-theme-expansion]] — the poll payload was never on disk, so the census fetched (16/16 byte-identical control, $0 held); three themes buy 6.8% of the coverage gap; four more themes and the seed handles authorised, the target itself deferred to the combined ledger | accepted |
| 2026-08-06 | [[5b-parity-abort-and-pod-runtime]] — the pair aborted: no serverless endpoint on this account consumes a job, RunPod's own hub worker included, while the same artifact answers on an A6000 pod; A ships and merging stays closed; production runtime becomes a stop-after pod on AMPERE_48, serverless only via a fresh §(2) measurement | accepted |
| 2026-08-06 | [[5b2-batch-measurement]] — the carve ladder finds every N byte-identical and picks 16; the paid run scores 666 of 758 rows and dies on GPU memory, so the adoption rule has no G1b and cannot run: serving is fixed at batch 1 permanently and the run-rate question returns to the operator | accepted |
| 2026-08-08 | [[5c1-day2-composition-and-search]] — three "city feeds" were chats; the search for replacements reached feeds the town-name scan never returned (137,221 against a 16,056 chat); five flags cleared, one FAIL moved to watch; registry 39 → 67, all of it provisional pending the yield screen | accepted |
| 2026-08-08 | [[5c1-relevance-floor-and-discovery]] — 66 channels had passed four gates and none of them ever asked whether the tracked category was in there; the yield screen refuses to report because АТБ's own 28 days carry 0 relevant posts (19 of 25 image-only), and «варто» — a private label and the ordinary word for "it is worth" — outfires every real brand | accepted |
| 2026-08-08 | [[srv2-serverless-runtime-target]] — the 5b wall did not reproduce: a two-job console probe consumed and completed both, so the production runtime target moves back to serverless (ruling 20 stays the measured fallback); four things the probe does not prove are pre-registered, and 3.11 (2) parity remains mandatory | accepted |
| 2026-08-09 | [[srv2-program-close]] — the programme closes: parity 758/758 with worst head movement 0.0000 earns serverless as the runtime, srv-2b's hang is charged to the platform by the operator's unwrapped control, and the price is a finding AGAINST the chosen path ($1.0825/pass vs the pod's $0.4611, ×2.35) that goes to the 5c2 briefing; the scale-to-zero saving is NOT measured | accepted |
| 2026-08-09 | [[5c1-vis-b-caption-instrument]] — the project's own Gemma 4 is the caption instrument: 19 of 19 silent ATB posts captioned and bar A taken 0 → 14 against a bar of 4 (qwen 13), so the pre-registered instrument-failure rule did not fire; the 8-of-18 bridge against qwen sits on inputs proven byte-identical, which makes it a finding about the task — a caption is a SAMPLE of a six-page leaflet — and a 5c2 design input rather than a captioner defect | accepted |
| 2026-08-10 | [[opus-review-programme-close]] — 25 of 25 packs and 498 of 498 rows close the second-instrument review: the matcher is acquitted (`fn_matcher` 0, every one of the 102 misses image-only, so `recall_candidate` 0.4769 measures the CAPTIONS), 163 captions judged 117 faithful / 43 partial / 3 wrong, 93 of the 104 false positives are «варто» and «Президент», and two misses expose a Latin-script alias gap the caption got right | accepted |
| 2026-08-10 | [[sitting-2026-08-10-composition-signed]] — the sitting rules «Варто» text-matching OFF (it stands in both columns: 69 false positives, 13 image-side misses) and «Селянське» anchored-only (TM, butter grade and Рудь's ice-cream line on the same pages), defers the 141 unlisted names into the position layer rather than the registry, and SIGNS the launch composition 66 = launch 59 + watch 7 unchanged — a stamp that moves the registry's sha and no row of it | accepted |
| 2026-08-10 | [[sku-b-pilot-readings-ratified]] — all five executor readings of sku-b's denominators are ratified (R1 recall per POST macro-averaged over 15, R2 the 108 sent pages, R3 four empty-gold posts as a precision probe, R4 n ≥ 10 pairs, R5 unreadable replies excluded and counted); the 30-row text gold comes back 11 position · 3 product_mention · 16 none with two triage rulings, and bar 3's denominator is ALL 30 rows — narrowing it to the 14 rung-carrying ones is refused as a post-hoc change that would delete the half of the sample pricing refusal | accepted |
| 2026-08-11 | [[sku-b-serving-and-cap-discipline]] — the record debt of sku-b-prep: the prep/run split, the POSITIONS pin (NF4 base with the adapter OFF, greedy, batch 1, 800-token ceiling) and the warm-up on non-gold inputs behind SPEC 3.17 (9); the three cap readings of (10) — go/no-go before the first gold call consumes NO attempt, a mid-leg stop is a finding about the CAP, and no single job may out-bill the cap; and the security pass that motivated (c), where a 1800 s job timeout could bill $0.5520 against a $0.35 cap | accepted |
| 2026-08-11 | [[sku-b-run-acceptance-and-resume]] — the single paid session is ACCEPTED at 17 of 138 gold calls ($0.1965, stopped by its own (10)(b) cap gate): the go/no-go's arithmetic was right and its PROBE was not — a generated 64×64 image answered in 1.436 s where a real leaflet page cost 5.0772 s (3.54×), so the gate passed the run it exists to refuse; no bar is scored, the team lead's 13-pair calibration read (6 correct, every miss the kopeck superscript) is NON-GATING, and SPEC 3.17 (11) resolves the stop as a RESUME — buy exactly the 121 unbought elements, instrument FROZEN, the warm-up becomes representative, cap $0.45 | accepted |
| 2026-08-11 | [[sku-b-v3-refusal-and-v4]] — the resumed session was refused by its own (10)(a) gate before the first gold call ($0.5964 projected against $0.45): the fixed (11)(c) probe priced a real unsent page at 14.808 s where the drawn population marginal is 5.0772 s (n=17, 10 of them empty), so THREE marginals exist and none of them is the population's rate; the attempt is intact, no element changed hands, and SPEC 3.17 (12) authorises one more session at a $0.65 cap sized to admit the pessimistic corner ($0.6143 with the 3% drift, $0.0357 of headroom, break-even 16.04 s/page), on a FRESH anchor — a refusal charges the phase and never the next attempt's cap (Dv167: the old three constants would have left $0.2974 and refused on the refused session's boot) | accepted |
| 2026-08-12 | [[sku-b-pilot-closed-by-measurement]] — two bars of three failed and the pilot closes as "instrument not ready" BY MEASUREMENT: brand recall 0.3603 vs 0.75, price-pair accuracy 0.3279 vs 0.80 on the team lead's read of all 61 pairs, tier accuracy 0.8621 vs 0.85 the one PASS. The failure is one glyph class — promo price 61/61 and printed % 61/61 correct against the crossed-out old price at 20/61 — and the instrument under-reads rather than invents (precision 0.9286, zero false positives on the four empty-gold posts, every bar-3 miss downward); depth is the exception that matters, where the badge (0.29 pp median) and the old price already extracted (0.18 pp) are BOTH inside 2 pp on all 61 pairs, so bar 2 fails on the printed number and not on the depth it feeds. $0.6032 over three sessions, and the registered probe reproduced to 1.2% while over-pricing the population 3.64× | accepted |
| 2026-08-12 | [[skub2-b-prime-closed]] — B′ closes as "instrument not ready" BY MEASUREMENT with two bars of three passing: brand recall 0.3603 → 0.9800 vs 0.75 (like-for-like 0.703 → 0.9800 on the re-scoped gold, and what that +0.277 is MADE of is not measured — Dv236), tier accuracy 0.8667, and price-pair accuracy 0.4125 vs 0.80 the one FAIL. (13) did what it was written to do — 5 unreadable replies → 0, the one 800-token truncation → 0, 79 positions → 101, 61 price pairs → 80 — and touched the failure not at all: of the 80 pairs, 61 are the v4 read with NOT ONE verdict moved (20/61 = 0.3279) and the 19 new ones sit on exactly the four pages v1 refused (13/19), so bar 2's whole gain is a widened denominator. Promo 80/80 and printed % 80/80 against the crossed-out old at 33/80: a resolution failure on a glyph class, which a parser amendment could never have reached. Depth stays adequate on the wider population under both instruments (badge 0.3212 pp median, extracted old price 0.1670 pp, both 80/80 within 2 pp), so question 7 is served by promo + the printed badge with the old price kept as a flagged depth input and never trusted to the kopiyka — a CANDIDATE for the 5c2 ruling, not a decision. $0.8796 for the position instrument end to end; this closure cost $0 | accepted |
| 2026-08-13 | [[the-law-grows-inside-marked-blocks]] — `docs/SPEC.md` is pinned WHOLE inside five sealed pre-registrations, so the law may only grow inside a marked block whose name `write_sku_prereg.RATIFICATION_NAME` knows; a name the expression does not know reddens the pin instead of passing quietly, and the fix is always to teach the strip, never to re-pin a sealed record. The `rev. 3.14` heading is FROZEN because those bytes are pinned — the live revision lives in the `amendment-index` block. A future amendment wears `<!-- amendment-3.N begin/end -->` and joins the literal enumeration in `tests/test_sku_prereg.py`, which grows loudly on purpose: (12), (13), (14) and 3.18 each arrived one name short and the list is where every one of them was caught | accepted |
| 2026-08-13 | [[5c2-stop-ruling-and-cap-33]] — the prep-c2 STOP is answered by raising the line, not by cutting the window: the anchor 2026-08-09 is RATIFIED with both alternatives seen and declined, the phase cap moves 30 → 33 (no money added, balance 11.1690), the session buys the WHOLE two-leg window so no ordering clause exists, the leaflet leg's population is the 159 pages on disk rather than the window's 78, and the post leg enters only through the relevance pre-filter — 349 of 9 158 posts, $0.3291, of which 29 carry a currency marker and four cooking channels are 71.6%. The clause that binds the TEAM LEAD: records written under the 30 cap are never regenerated to fit the new one, and their tests decouple by the CAP_IN_FORCE pattern — `fits: false` stayed green across the raise while its meaning inverted, which is the whole reason the rule is written down | accepted |
| 2026-08-13 | [[the-d-cut-and-the-fourth-kind]] — the post leg's registered population is the **D cut**: of the 349 posts the pre-filter passes, keep a row iff its channel's `source_type` is `official_retail`/`aggregator` OR its evidence carries a `currency` pattern. **44 rows, $0.0581** — and 44 rather than the expected 50–55 because the overlap is 16, not the ~5–10 that 31 + 29 implies: currency-bearing rows cluster in exactly the carriers the first half already keeps, which is the coherence the ruling rests on. Both readings of "its matched evidence" select the same rows (29 either way) and the producer exits non-zero where they would part; the ruling's "250 rows from four cooking channels" is the top four of the concentration table and the audience is really 270 across six, all of them removed. That is what makes `POST_PRICE_ORIGIN`'s inheritance of skub2's `retail_leaflet` defensible — recipe carriers no longer reach it. Beside it, the team lead's ruling on Dv285: `evidence.KINDS` grows a fourth member, `post_text`, an empty-`KIND_FIELDS` marker written LAST per post, so an interrupted pass re-asks nothing (the exhausted-queue smoke 2 transport calls → 0, with the old key planted as the negative control) | accepted |
| 2026-08-14 | [[5c2-closed-the-sitting-and-the-shelf-life-redesign]] — the 3.18 (6) sitting is HELD and both halves are RATIFIED (6 leaflet posts / 36 position rows and 5 of 5 comments, disputed 0, REVIEW-class orders 0, the operator's «распознавание SKU идеальное» kept as data); `results/validate_5c2_returns.json` is the returns record and the pack's skeleton is never written over. The sitting's one RULING is SPEC amendment **3.19** — text-less comments (1 361 of 5 075, 26.8%, all empty at the source store) leave the inference QUEUE from the next paid cycle, reporting denominators follow, and nothing already bought is re-scored; the block reddened six designed refusals and the fix taught the enumerations without growing the sealed pin's ten-block keep. Named once here so the next flip cites it: the **shelf-life redesign** — a green assertion fixed to a moment goes red when the world moves legitimately, and the fix is the weaker statement that is still a statement, with the old failure planted in the other direction. Four instances now (cap-in-force, derived-root snapshot, ten-keep strip, recover-instead-of-re-pin). Phase 4 closes at $31.4493 of $33.00 | accepted |
| 2026-08-15 | [[reader-probe-card-and-interface]] — two paid probes settle the serving card (`ADA_24` is 2.64× the L4 at the same $/s, `gpuIds` reads back FREE from the create call, and no rate travels between contracts without its card) and name the BINDING constraint: not the reading but the interface. v2 closed both measured defects and neither returned, yet ten of 23 replies still refused across six other shapes of the same class — container-only coercion takes replies 13 → 19 and entity cases 2 → 3 of 4, while the coerced-and-collapsed per-comment bar is 0.643 against 0.80, so NO bar clears under any reading. Four threads bought past SPEC 3.21 (1)'s marker rule answered 2 of the 4 entity cases, which is the measured argument that the hygiene rule is a payment gate for the reader. And the only bar that PASSED was a count of zero over four threads with no verdict at all. Four options priced ($0 / ~$0.08 / sitting / sitting); the money rules that fall out are blockers: a bill read at deletion is a lower bound, and `--step` has no corroborating reading | accepted |
| 2026-08-16 | [[serving-latency-deferred]] — the «cosmic» latency is decomposed from records already on disk and the cure is DEFERRED: a ~239 s serverless cold start is ~214 s of loading 62 GB of bf16 and NF4-compressing it on the fly, with `idleTimeout: 60` making every isolated request pay it again, plus a ~5 tok/s decode that is the research stack behaving as chosen (bnb-NF4 + unmerged LoRA + HF generate at batch 1, SPEC 3.11 (2)). Neither the runtime nor the card is the culprit — a warm row is 4.07 s on a pod against 4.10 s on serverless, and the 4090 is already the measured price/speed leader. Two cures are priced and not bought (a pre-quantised base → ~1 min start; merge + vLLM under a fresh registration → row < 1 s, thread ~5 s); the operator defers both until «готовим прод», expected at Phase 7 / packaging, and leaves runtime and cards unchanged. The accepted price is written down rather than rediscovered: ~4.1 s a row, 21–32 s a thread, ~4 min for an isolated call | accepted |
| 2026-08-16 | [[reader-sitting-16-08]] — probe-b is ACCEPTED after a from-scratch re-verification (2 562 / 2, the bar scorer reproduced byte for byte, the 23/134/4 population and `ccef35fa…` agreed, the cloud re-listed with a positive control) and `docs/REFERENCE-signals-w1.md` becomes the confirmed target. Four rulings: (1) the cycle-2 budget line is **$20**, its amendment lands by the house manoeuvre BEFORE the first paid step, Phase 4 closes at a final reading and the anchor is written only after the top-up; (2) the reader instrument is A+B as ONE new registration (v3) — container tolerance by explicit ruling with the domains left strict and two disagreeing top-level objects REFUSED rather than last-wins, a v3 prompt («one JSON object», «`[]` for empty») carrying F1b/F1c/F2a, re-read of the same 23 threads at ~$0.08 against gold r2; (3) the varto/marker silencer is OFF for the READER path only — classification's payment gate and r1 brand attribution untouched, the reader's window-1 population 129 threads with spam/scam kept, and the new census cell (narrow · varto off · plus-spam/scam on) MEASURED in reader-v3, never derived; (4) «категория» of the reference ≡ «категория_личное» of the ratified taxonomy — the gold is re-derived as a NAMED revision r2, the reference file itself is not edited, and the ruling lives as its own record | accepted |
| 2026-08-16 | [[reader-v3-serverless-close-and-the-pod-ruling]] — reader-v3's step CLOSES at **$0.393577** with **zero threads read**, over its own $0.35 cap: the ledger's decomposition is serverless $0.371765 (endpoint `77o1ing6cy0972`, **1 211 005 ms** that answered nothing), the staging pod $0.021812 and the always-on volume $0.009722, and every bar of the frozen v3 registration stays UNSCORED rather than failed. The defect the money bought is structural and not about serverless: v3's only go/no-go sat AFTER a warm-up a boot never reached, so the run had no branch that could stop it. The operator rules the reader onto **one rented pod** — the meter is a stopwatch from `pod create`, the boot is tailed live under `min(720 s, usable − elapsed − reading projection)` with both numbers printed, and the kill rule is a command returning WAIT/KILL/GO rather than an eyeball. Measured the next evening on the SAME instrument and the SAME 23 threads: **$0.236118 for 23 of 23 read**, boot 175.1 s — the measurement cost less than the boot that produced nothing. v3 stays FROZEN and is not withdrawn | accepted |
| 2026-08-17 | [[reader-v4-closed-and-sitting-17-08]] — reader-v4 is ACCEPTED as an honest NEGATIVE: **three bars of five pass** (2 entity cases 4/4, 3 noise 0 signals, 5 settled at **$0.236118** of $0.35, pods only) and bars **1 and 4 FAIL** (flagships 2 of 5, per-comment agreement **0.500** against 0.80). The containers are cured and the reading is not — 19 of 23 parse against probe-b's 13 with **zero of the nine container repairs fired**, so nothing recovered came through a repair; all four bar-4 disagreements are `subject_type` on already-collapsed gold (a comment about a category read as a comment about the chain), and one requested row in six is never written (**93 returned of 111**), which is where three of the fourteen gold rows go missing. The attempt is spent by the registration's own one-attempt clause. The sitting of 17.08 rules instrument **v5** in one registration: (a) an attribution block — the subject is read off THE COMMENT, and a complaint that survives swapping the chain or the brand is «категория_личное»; (b) the «two objects» class dies in the TRANSPORT, the runner cutting generation at the first balanced JSON object with the prompt line and refuse-on-conflict left as defence; (c) a NARROW F2a carve-out for events that change the availability or status of the post's subject, opinions without a name still never carried; (d) a msg_id ECHO duty with the shortfall printed beside the bars, and the chunking mechanism bought once on `@klopotenkofood:6040` (43 payable, chunks ≤16) under MECHANICAL bars that cannot touch the semantic ones; (e) cap **$0.45**; (f) the PROGRAMME STOP-RULE, pre-registered — if v5 completes and bars 1 and 4 are not both taken, the prompt-engineering line CLOSES and the next step is an architecture sitting, never a v6 of the same kind. Every prompt example is SYNTHETIC with a grep proof, and two instrument notes ride along: gate records must APPEND, and every verify command names `python3.11` | accepted |
| 2026-08-17 | [[reader-programme-closed-and-the-architecture-sitting-17-08]] — the prompt-engineering line CLOSES by its own pre-registered stop-rule, on a run that COMPLETED: `reader-v5b` read 26 of 26 units for **$0.311211** of a $0.50 cap with every gate GO, the msg_id echo duty 117/117 with zero absent, the chunking mechanism m1–m4 all PASS and the «two objects» class extinct (26 of 26 balanced) — and bars **1 (2 of 5)** and **4 (0.4286, and 0.600 over the rows actually answered)** both FAIL against v4's 2 of 5 and 0.500. Row by row, ONE gold row moved from disagreement to agreement on attribution (48283); three arrived out of absence through the echo duty and only one agreed; four went ABSENT when `@VARUS_channel:10613` was refused, two of them having AGREED under v4 — none of it is the reader learning to attribute. Beside it, a ruling WITHDRAWN before any pod: leg A ordered by descending payable was priced at $0 through the live gate's own function and STOPs at unit 1 by 23.2 s/unit (the cap raise alone had already cured the knife-edge), withdrawn 18:11 CEST, and the forecast was then confirmed by the run at 103.8 s against 105.3. The sitting rules architecture **D** — hybrid decomposition, per-comment attribution into its own pass 1 — with a ~$0.10–0.15 micro-measurement first (≥12 of 14, one attempt, entity context from ALREADY BOUGHT verdicts, a failed bar returning to the sitting) and subject_type labelling authorised only after the frame confirms. The clause that binds the TEAM LEAD: **no new one-shot reader registration may be written** — pass-1 classification is the stop-rule's own «two passes», not a v6 | accepted |
| 2026-08-18 | [[pass1-probe-b-measured-and-the-sitting-owns-it]] — pass 1 is MEASURED and bar **P1 FAILS at 9 of 14** against 12 (disagreed 5, absent 0, losses 5 of a budget of 2), so the one-attempt clause is spent and the STOP returns the question to the SITTING (B: labelled `subject_type` + LoRA / C: another base) — never to a prompt iteration. The failure is ONE field: all five losses are `subject_type` while `stance` is right on all three rows that score it, and two losses are rows the model declined to type at all — an abstention, not a parse failure, with **0 refusals of 64** and 25 of 50 census rows untyped behind it. The transport is settled and is not the question: gate 0 GO at 14.5 s with 165.5 s of its dead-man unused (third consecutive GO), boot 237.2 s inside the raised 420 s ceiling, the stack's three boots spanning a **1.53× spread that is registered rather than averaged**, one segment, `DONE` on its own, **$0.135050 of $0.20** over 657.0 billed seconds. The window's pass-1 FLOOR goes on the record at 1 032 payable comments × 5.162 s = **$1.0950** of generation with no boot in it. Both money closes stay NAMED and unclosed — `pass1-probe` still reads 300 000 of ~427 000 ms — and belong to the `--until` contract | accepted |
| 2026-08-18 | [[sitting-b-line-b-and-the-team-lead-labels]] — the sitting of 18.08 (evening) rules **line B** — labelled `subject_type` plus a LoRA over the same base — and the **TEAM LEAD does the labelling** («я доверяю разметку тебе»). What forced it is one field: all five of bar P1's losses are `subject_type` while `stance` is right on all three rows that score it, two of the five are rows the model declined to type at all with **0 refusals of 64** behind them, and 25 of 50 census rows are untyped the same way — an unwillingness to commit to a class, which is what supervision buys and what another base is not guaranteed to change. The honesty frame is registered in five points: the judge does NOT move (the sealed gold r2 ≥12/14, and the labeller never touches exam material); team-lead labels are TRAINING data — provenance-tagged and **ablated at training time**, which is the LoRA registration's law named here so it is not lost between contracts; contamination is excluded WITH PROOF by msg_id, the data-prep contract widening the sitting's «the 14 gold rows and their threads» to every thread carrying any of the **64 registered probe units**; the codebook is not an opinion — the v5 attribution law by the SAME BYTES, the F2a carve-out and the gold-r2 adjudication; volume ≈500 rows against the intents pass's 508, formula fixed by the contract. The ~40-row blind control is the operator's option and blocks nothing. Training config, cost and renders belong to the LoRA registration and nothing here may pre-empt it | accepted |
| 2026-08-18 | [[guard-until-the-tolerance-that-was-a-window-bug]] — the contract's third constant did NOT re-derive and the artifact it was to come from cannot yield one: «6–7%» holds for **3 of 7 rows**, max divergence **100.00%**, and every outlier decomposes into a defect in how the table's own window was built — the window START is each ledger's first SESSION rather than its anchor (pre-window spend 0.00–99.97% of the recorded reading, and the three rows inside 7% are exactly the three with the smallest), `probe_a`'s window is zero-width, `5c1_vis` holds two anchors in one file, and the right-hand side is a LAGGED reading of the same walk — `pass1-probe-b`'s $0.0853189556 is that walk truncated seven minutes earlier, so the ledger recorded **64.9%** of the truth. A bigger tolerance would license a known bug rather than absorb noise, so **`--tolerance` ships REQUIRED with no default** and the contract closed NOTHING. Delivered: the walk's end bound (`--until`, `--expect-ms`, a three-state walk at a 1% band, a live close that re-walks when a window is supplied), the preflight as an instrument (`make preflight`, 1 466 pins across three shapes — it caught itself twice and found the guard already stale against its own pin), and the written-down rule that a STEP's close rests on the billing walk ALONE because on an old ledger the balance delta is negative. **ADDENDUM, the operator's ruling «закрой три сходящихся леджера под 7%»:** `45h2` $8.217558 (6.92%), `srv2b` $0.939957 (5.99%), `srv2d` $1.163191 (5.68%) CLOSED, each entry carrying its window, walk_ms, recorded reading and the tolerance it was judged under, and the pre-window caveat ($0.4204 / $0 / $0.0104) written INSIDE the settled records because a closing entry is never re-derived; append-only proved on all four touched files; 19 of 22 refuse and stay refused | accepted |
| 2026-08-19 | [[the-absence-test-is-a-clock-and-flips-with-its-artifact]] — a test that asserts a planned artifact's ABSENCE is a clock, not a verifier: it stops verifying the hour it was set for and goes off exactly when the planned work succeeds, which is how `pass1-data-prep`'s two `assert not …exists()` lines turned the suite red at 2 957 / 2 the night the team lead wrote the 500 labels — no code involved. The operator rules the flip into the SAME commit that lands the artifact (`8d2e1ba`, staged by path): a fresh clone never sees `docs/labels-pass1-r1.jsonl` without the tests that seal it, and `git bisect` walks no red window that was never a real state of the work. What replaced the absence is a net gain and not a subtraction — the file exists · 500 rows · the registered distribution · the sha pinned in `results/labels_pass1_r1_provenance.json` and re-derived at test time · the validator's green path driven on the REAL file · the freeze proven byte-identical, the whole seal checked against a single flipped label that keeps 500 rows and still passes the validator, and caught three ways. Fifth instance of the shelf-life class after cap-in-force, the derived-root snapshot, the ten-block keep and recover-instead-of-re-pin | accepted |
| 2026-08-19 | [[boot-tax-target-suspended-and-the-census-axis]] — the ≤9K boot-tax target is SUSPENDED rather than missed: `vault-dream`'s pre-cut inequality shows 9 323 B of three `CLAUDE.md` plus 24 532 B of `hot.md` = 8.46K before `MEMORY.md` contributes a byte, so ≤9.0K needs `MEMORY.md` ≤ 2 145 B (a 91% eviction the no-home law forbids) or `hot.md` down 66.5%, and cutting two thirds of the live-state file is a decision about what the project stops keeping in front of itself, not a consolidation. Re-registration comes from the measured high-signal floor at a joint sitting, the three `CLAUDE.md` join the de-bloat scope, and until then the census warning reads «the target is on re-registration». Beside it Dv526 is fixed: the census counted `min(st_size, 25*1024)` UTF-8 bytes where the loader trims, keeps 200 LINES and 25 000 UTF-16 code units cut back to the last newline — two units and two numbers — so `loaded_memory()` models the loader and returns BYTES (measured move −1 B, the trailing newline, predicted before the run), `TARGET_KTOK` and the warning untouched, 200 / 25 000 / 9.0 pinned by a new guard. The sitting starts from a table, not an edit, and the mechanism it can spend is proven: `.claude/rules/*.md` holds 22 629 B that costs 0 at boot behind `paths:` | accepted |
| 2026-08-19 | [[boot-tax-re-registered-from-the-measured-floor]] — the joint sitting rules that ALL FIVE de-bloat groups go and fixes the target by a PRE-REGISTERED FORMULA rather than by opinion: `TARGET_KTOK := the measured post-debloat floor × 1.1`, one decimal, written at the END from the measurement. Groups A (11 record/lesson duplicates) + B (4 prose copies of numbers that have producers) + C (the day summary in `**Last update:**`) take `knowledge/hot.md` 24 662 → 13 457 B; group D moves `## Code map`, `## Harness plumbing`, the collector-scoped Pitfalls and `## graphify` out of `CLAUDE.md` (6 999 → 4 581 B, 115 → 80 lines) into three `paths:`-scoped rules and the tooling runbook; group E parks `## Dev Stack` verbatim at `~/dev-stack-parked.md`, a file nothing loads. Census **13.2 → 10.4 → 9.8 → 9.7K**, the floor identical across three consecutive runs, so 9.7 × 1.1 = 10.67 → **TARGET_KTOK = 10.7** and the warning stops firing; constant, its docstring and the test pin move in one commit. The no-home-no-eviction law held one block back — the idioms card, because «право исполнителя на отказ» is stated in no file and its only «home» was a roster line (Dv529) — and every other block evicted with `file:line` behind it. This record un-suspends what [[boot-tax-target-suspended-and-the-census-axis]] suspended | accepted |
| 2026-08-20 | [[pass1-fewshot-closes-at-the-ssh-dead-man]] — pass1-fewshot's whole D0 is committed and green and the PAID session never generated a token: two RTX 4090s in EU-RO-1 at the registered `costPerHr` 0.74, both KILLed on **rung 2, the 180 s ssh dead-man**, and the recovery clause's ONE re-creation used — so **the ONE attempt is NOT spent** and the question about the v2 prompt returns to the operator intact. Pod 1 was killed by my own instrument (the poll loop grepped `"host"`, a key `runpodctl ssh info` never emits, spun blind for 240 s, and the `--gate0` after it asserted an endpoint that WAS answering had not) and the KILL stands, because a gate records the reading it is GIVEN and a deadline that cannot be demonstrated has not been passed; pod 2 was killed by the pod on a clean bounded poll, still `pod not ready` at **231.9 s** against 180 — so 180 s is a reading of 18.08 and not a property of the stack, exactly as the boot on it never was (146.8 … 353 s). Delivered at $0 and committed BEFORE the money: the **holdout-100** (seed 20260820, largest remainder with a FLOOR OF ONE per class, because plain largest remainder drops `молочный_бренд` entirely — EVALUATION ONLY, enforced by `build_pass1_sft.py` with the exemption keyed on the two sealed arms' SHAS), **`pass1_comment_gm4_v2`** (v1's bytes + the codebook clause + the examples slot, v1's own sha unmoved), the **dev-200 in two paired legs** and probe-b's **64 under v2**, both with one labelled neighbour PER CLASS (nearest-k would have reproduced the 52% `не_наш_рынок` prior the line-B adapter learned), four empty contamination lists apiece, a gate whose **rung 5 is a blocking loop that deletes an idle pod**, and a registration whose H6 refuses at step 1. **573.0 s = $0.117783** of the $1.50 cap; cycle 2 **$5.8992 of $20.00**. Rung 2 needs the operator's word — wait, or RE-REGISTER the dead-man; this registration may not be edited and has no recovery left | accepted |
| 2026-08-21 | [[v2-is-the-base-and-pass-2-is-not-a-one-shot-reader]] — the operator's ruling of 21.08, п. 1 (а)–(д), quoted verbatim and recorded where a later contract cannot re-open it. **(а)** `pass1_comment_gm4_v2` becomes the BASE prompt of pass 1 with its «before» numbers fixed at **136/200 and 38/49 on dev-200** — the r2 registration closed the prompt ITERATION and not the acceptance of what it measured, so v2 ships as a PROVISIONAL filter for an end-to-end run whose bar is transport, and never as an accepted deliverable: its dev gate is RED at +7 of +10 and stays red, its gain is bought almost entirely on `не_наш_рынок` (12 → 46), the base's error was SILENCE (13 of the 14 «our» rows v2 gained were `None`), and the mention-vs-about cell is the one cell v2 did not move (22 → **24**). **The fourteen inside the window are a census row with its multiplicity named — the FOURTH look at the same rows (base 9/14 · arm A 9/14 · v2 registered and never fired · v2 inside a production pass) — NEVER a bar and never «v2 взял 12/14»**, and the same holds for the 650 labelled rows and the 450 outside dev-200; a bar on the fourteen needs a new registration, its own attempt and the operator's word. **(г)** the stop-rule of 17.08 does NOT cover pass 2: one call per THREAD over comments pass 1 has already attributed does no per-comment attribution and answers nothing from the raw thread, which is both clauses of the instrument [[reader-programme-closed-and-the-architecture-sitting-17-08]] closed — and the reading is symmetric, so a pass-2 design that starts attributing per comment from the raw thread IS that instrument and this record refuses it. **(д)** two contracts, one paid run each: `pass1-window` (transport and completeness, cap **$1.50**, a fresh step ledger — `pass1-fewshot`'s $0.397133 stays in its own closed step) → `pass2-signals` (F 5/5 · E 4/4 · N 0, cap **$1.50**), priced from this run's per-thread census. Binds the TEAM LEAD as well as the executor. **MEASURED 21.08: `pass1-window` closed RED at rung 7 — 131 of 1 032 answered, one pod, $0.173694 of $1.50, killed by rung 4 on the SECONDS while the cap sat at $1.3948 of $1.50 and the recovery clause then refused. No bar on v2 was taken and nothing of (а)/(б)/(г) is disturbed; the transport was clean on all 131 rows. What failed is the RATE: 4.498 s/call against the 2.726 the registration priced from — 1.65x — with request width, a cold leg, the prompt, the card model, the datacenter and the population each eliminated with numbers. `pass2-signals` therefore cannot be priced from this census (24 of 127 callable threads), and the 901 comments still owed need a registration whose rate is charged on the POD CLASS** | accepted |
| 2026-08-22 | [[pass-2-in-the-readers-schema-and-strict-authority]] — the second half of [[v2-is-the-base-and-pass-2-is-not-a-one-shot-reader]]: that record said what pass 2 is NOT, this one says what it IS and what it may not become. **STRICT authority**, enforced by `market_pulse.pass2.parse_pass2` and not asked for in prose — a `subject_type` in `per_comment` must equal the pass-1 label of that comment, a signal's must be the label of ONE of the rows it cites (one and not all: pass 1 labelled each comment on its own, and demanding all would refuse F1(б)'s two-row shape for a reason no ruling gave), and a signal citing NO comment is refused because `from_post` is the hole the relabelling refusal would be walked around through. A relabelling is `pass2.RelabelError`, a `ParseError` subclass, counted by its own cause and never folded into «other». **What pass 2 MAY do is DROP** — the DROP table by thread and by pass-1 label is the false-positive reading of the filter the whole end-to-end run was bought for — and DOUBT, report-only, changing nothing downstream. **The schema is the v5 reader's**, so `prompts._reader_object` + `prompts._reader` and `scorer.reader_*` apply unchanged and nothing is forked; `prompts.py` is pinned by 38 sealed records so the text and the renderer live in `src/market_pulse/pass2.py` (`pass2_thread_gm4_v1`). **`entities` is BOUGHT, not answered** — the v5b/v4/topup block placed into the payload between the parser's two halves, because it is 1 288 chars on the smoke's first unit and a copy slip would refuse the thread and take bar 1's hardest case with it — **so bar 2's value is knowable at $0 and the registration carries it: 3 of 4, RED, E1's thread carrying no filtered row and never being called.** Reachability is a property of the POPULATION and pass 2's differs from the reader probe's in BOTH directions: E1 falls out, E4a/E4b come in, **F2a is unreachable because pass 1 labelled msg 580124 `сеть_ритейлер` where the gold says `молочный_бренд`** — expected RED by construction, bar unmoved at 5 of 5. **No bar on the fourteen: `per_comment` is pass 1's pass-through and a threshold over it would be their SIXTH look.** And the money shape a later contract should copy: a prompt with NO measured rate prices the SMOKE at a named assumption (120 s/call, 3 000 s = $0.6667), makes `total_seconds` the smoke's worst case and not the run's, and authorises the remaining 74 by **rung S′** — `max(1.5 × smoke_mean, smoke_max)` against the 6 600 s stop, knife edge **48.6486 s/call**, verdict RECORDED before the token is written — with a STOP as a registered outcome. The smoke is NOT a sample: 6.0 filtered rows a thread against the population's 3.56. **MEASURED 22.08: rung S′ said STOP at 5 of 79.** One pod, **526.0 s = $0.108122** of $1.50; ssh 38 s, boot **139.5 s** (a new floor), pre-generation 135 s against 1 100 charged. The decode nobody had measured: 58.07 · 15.80 · 40.85 · 56.46 · 56.70 → **mean 45.582, max 58.07 — 0.53 of the one-shot reader on the SAME five threads**. `charged_full = max(1.5 × 45.582, 58.07) = 68.373`, projected 6 876.9 against the 6 600 s stop, over by 276.9 s = **3.74 s a call** — and three readings sit beside that: the LIVE knife edge was **64.63** and not the registered 48.6486 (the pod reached the decision at 517.3 s of elapsed, not the ~1 700 the worked arm assumes); the **max arm alone would have PASSED** with 6.5 s a call to spare, so it is the 1.5× multiplier on a mean drawn from the five richest threads that closed it; and the row-weighted arm predicted the 74 at **27.485 s/call** and **would have said GO**. Correct under the law as registered, and on this evidence a STOP about the SAMPLE. **Strict authority held: 0 relabellings.** Six of the seven gold flagship signals FOUND on the four threads that parsed — F1 took all three, the case v5b scored 0 of 3 on, and the two that turn on an aspect are exactly the clauses spliced in from `prompts` after the review. All three bars UNSCORED with a per-case reason and none reported as a number. DROP 0 of 28; `subject_doubt` 4 of 28, all on `категория_личное`, one of them the АТБ/творог row that IS the mention-vs-about cell v2 did not move. **One refusal, and it is the rule for next time:** F2 died on `per_comment.note is not a non-empty string` — the model wrote `"note": ""` — the THIRD report-only field found to carry whole-thread refusal power and the first to fire. A report-only field may not be able to refuse. **r2 EXECUTED 22.08:** the operator's ruling (з) bought the 75 still owed at `ceil(58.07 × 1.67) = 97 s/thread` under a $2.50 cap; the four parsed replies were CARRIED and never re-bought; the parser's refusal set is CLOSED at six causes with every report-only field read through the pinned validator that owns it. One pod, **2 061 s = $0.42365**, 75 bought at **23.760 s/thread — 0.245 of the charge**. **All 79 read, all three bars SCORED and all three RED, and the three REDs are three kinds:** bar 1 **4 of 5** (6 of 7 signals; the miss is F2a, UNREACHABLE by construction — F2 was ANSWERED and READ this time and still does not take it, so the RED is about AUTHORITY), bar 2 **3 of 4** (the value computed at $0 before the pod, agreeing exactly), bar 3 **2 signals against 0** — the only RED the layer produced: `@VARUS_channel:10366`, a giveaway thread, dropped none of its four `сеть_ритейлер` rows and read `похвала`/`жалоба` about the giveaway. DROP **21 of 281 = 7.47 %** and `сеть_ритейлер` drops at **11.86 %** against `категория_личное`'s 6.28 %; `subject_doubt` 15 of 260 with its highest rate at **`молочный_бренд` 21.4 %** — a café, a stationery brand and a throat spray, all labelled a dairy brand by pass 1. **What the carry proved:** `@matusi_ukr:22303` is the one thread both pods answered and its reply came back BYTE-IDENTICAL (815 chars, same sha, 304 completion tokens), 15.801 → 15.934 s = **1.008× — the only size-free pod-class reading this stack owns**, against the 1.67 the registration borrowed. **And what Dv702 was worth:** r1's parser over r2's out-file refuses **52 of 79 threads, 65.8 %**, every one on `per_comment.note` — the F2 refusal was the MODAL outcome, not a fluke | accepted |
| 2026-08-22 | [[lora-c-rationale-supervision-and-the-shared-pool]] — `lora-c-prep` executed at $0 under rulings **(в)** and **(к)** of 22.08, both quoted verbatim and GREPPED back into `docs/STATUS.md` on every build of the registration. **Prompt v3 is a SIBLING module and that is the contract's own question answered by driving it:** `build_pass1_fewshot_packs.rendered_item` cannot take v3 because `prompts.pass1_messages_gm4` raises on any task outside `prompts.PASS1` and `prompts.py` is pinned by 38 records — so `src/market_pulse/pass1_v3.py` restates the assembly and CALLS the v2 text, the examples validator and the ceiling, with the text a strict PREFIX extension of v2's asserted in both directions. `rationale` is REPORT-ONLY and **can never refuse a reply** — pass2-r2's rule applied BEFORE the money — and the four SCORED fields still go through `prompts.parse_pass1` unchanged, legal because `_require` collects only what is MISSING. **The pool is 515 and not the contract's ~430:** `650 − 100 − 41 + 6`, because the two exclusions OVERLAP on six holdout rows inside reference threads; 8 of the 16 reference threads carry labelled rows, and the same 515 are both the training set and the neighbour pool. **515 rationales written here, and the measurement that makes them worth writing: 501 distinct cues over 515 rows, 267 over the 272-row majority class**, every cue checked back into its own comment — a rationale derivable from the label alone would leave the target a deterministic function of the class, which is what line B's adapter learned. **160 synthetic rows, 8/8/8/8/8 inside every error class (spread 0 against ±2), four EMPTY contamination lists on word 6-grams**, and a register MEASURED on both sides: two of six axes clear ±3 and four do not, all named. **E = 200, renders 198**; all 14 gold rows inside it and taking **no bar**. **`build_lora_c_pass2_pack.py --reproduce` returns bar 1 = 4 of 5 and bar 3 = 2 signals** on the window's own out-files cut to the 11 reference threads that carry a filtered row — exactly r2's readings, so the selection is measured and not argued. **FOUR reachability blocks, each with its arithmetic and its remedies, none taken:** (1) the pool holds ONE `молочный_бренд` row (the holdout took the other), so `neighbours` cannot serve any query in its thread — 9 training rows and 2 eval rows refused and **the rendered training set has ZERO real positives of that class**; (2) **all 506 rows are over `config/qlora.yaml`'s frozen `max_seq_len` of 1 408 at every ratio this repo has measured** — at the MINIMUM observed tokens-per-character the SHORTEST row needs 1 419 of the pod's own count, because v3's request is 5 262–9 402 chars against lora-b's 2 950/4 132; (3) **arm A carries ZERO `молочный_бренд` targets** — `train_qlora.class_weights` returns four classes for it, not five, so `sampling_order` can never draw one — while arm B carries **32** from the synthetic rows; since **bar 1 = 5/5 turns on relabelling msg 580124 to `молочный_бренд`**, the bar is near-unreachable for arm A and reachable for arm B by the same arithmetic, and it is NOT lowered; (4) `train_qlora.py` is pinned live by lora-b's prereg AND verdict and **refuses a v3 dataset on two guards driven at $0**, with what PASSES named beside them. **Four neighbours dissolves (1) and MEASURABLY NOT (2)** — every drop-choice computed: four examples leaves 484–506 over at the most favourable ratio depending which neighbour goes, ONE example leaves 62–125, and ZERO is refused by the renderer, so the best case anywhere is 62 still over; two decisions, not one. The reason is narrower than «the text»: the prompt is 1 109–1 200 tokens of the 1 408 ceiling so the margin is thin, and the TAIL is what no legal count reaches — at one example the MEDIAN row fits. The registration is a **DRAFT with no price**: formulas, the $4.00 cap, and for each of the three borrowed rates the condition that invalidates it — 6.14 s/call was measured on v2 and three legs send v3; 61.047 / 68.442 / 121.0 s/step are all readings at ≤1 222 tokens. **THE REVIEW'S WORST FINDING, and it lands on the gating bar: 22 of 198 eval items are shown a BYTE-IDENTICAL copy of their own comment carrying a label, and FIVE are holdout rows** — five of the 98 the registered `holdout-100 ≥ 64 of 100` is scored on, each shown its own gold answer. The own-thread rule blocks the query's THREAD; `neighbours` maximises Jaccard over character 3-grams and an identical string scores 1.0, so the duplicate is chosen FIRST wherever this window reposts a comment verbatim. Neutral to arm-vs-base and to the A/B ablation (one `chosen` feeds every leg), decisive for the absolute reading; reported and NOT repaired because the contract mandates that selection rule. Beside it: **dev-200 IS in E (80 of 200)** and the record denied it in the same document that listed them, and **two registered numbers were refuted by the files the record cites** — `expected_draws` 101.2 against the sampler's measured 126.5 (`random.choices` normalises and the vector sums to 404.8 of 506, because the rule expects five classes and four are rendered), and `121.0 s/step` called «the worst case while every rung says GO» when lora-b's own example at 121.0 says KILL. **Of five review lenses launched on a frozen sha, ONE returned — 95 minutes late, with eight findings including that the correction above was itself two hand-typed literals; the other four delivered after the report had been written saying none had, and the second skeptic pass therefore did not happen.** **Both review gates are OPEN and D0 stops at them, and the executor FLAGS 15 of its own 160 synthetic rows** — they label a stock/price/assortment claim about a named chain `не_наш_рынок` where the codebook rules `сеть_ритейлер` and the team lead's own labels on three real rows of that shape agree with the codebook; nothing rewritten, the gate-2 file opens with the finding. And the boundary draw found the INSTRUMENT before the rows: the shipped registry-and-watchlist matchers find **zero** retailer or brand mentions in all 466 non-«our» rows, because `config/registry.yaml` spells the chain `Varus` in Latin and the comments write «варусі» — no file in this repo carries its Cyrillic spelling | accepted |
| 2026-08-23 | [[lora-c-rationale-supervision-and-the-shared-pool]] (update) — `lora-c-apply` executed the team lead's two review verdicts and **amendment 3.25** at $0. **Both gates CLOSED:** 515 rationales `rationale_reviewed: true`, 160 synthetic rows `reviewed: true`, and NO label moved in gate 2 — so the 8/8/8/8/8 balance survives by construction. Gate 1: four named rewrites in the team lead's own words, a one-row labels **r3 delta** (`@retsepty:7342#49685` `null` → `не_наш_рынок`) that composes as a LAYER over r1+r2 and draws no unit — so it has no pack and `labelled_units`'s pack-equals-labels refusal never sees it — 13 P-NULL rewrites and 31 P-MARKER markers, and the hybrid grep returns **0 of 515 against a control of 38**. Gate 2: fifteen texts rewritten so the claim is the THING and the chain only WHERE, after which `self_flagged` — the instrument that raised them — matches **0 of 160**; ten skeletons broken, and the coded measure reproduced the verdict's «16 of 32» on its first run untuned AND found a SECOND frame over the same ceiling (12) that the prose named only the first of; a seeded newline pass into 30 rows, idempotent by construction. The register table went from **four of six axes missing to one of seven** — not by tuning an axis but because the rewrites are longer and more punctuated. **Amendment 3.25 (2) closes the own-text leak and 0.9875 is what proves it:** E items shown their own comment 22 → 0, on the gating bar 5 → 0, training rows 13 → 0, and the highest similarity anywhere is now exactly the value lens 3 named as the highest strictly below 1.0 — so the refusal took the equality tier and left the continuum beneath it. **No query lost its fifth neighbour**, so there is no new instance of STOP 1. The refusal is a PRE-FILTER at lora-c's call sites because ten sealed records pin `build_pass1_fewshot_packs.py`. **`config/qlora.yaml` is at revision 2 (1 408 → 2 816) in place** — the file's own history shows the cited precedent was an in-place revision, and 3.21 names the two sealed configs and this is not one — and line B's three sealed artefacts rebuild **byte-identical**, because its longest kept row is 1 222 tokens. **AND THE REALITY CHECK 3.25 (1) ORDERED STOPS THE LINE:** the model's own tokenizer at the pinned revision counts **2 975 on the widest of the 506 rows, 2 991 with `TEMPLATE_SLACK`**, against the amendment's stop at 2 800 and against `max_seq_len` 2 816 itself; **two rows are over by the true count, three with slack**, and the ratio MODEL that derived 2 816 predicts 2 763 and clears every row — under-predicting by **228 tokens** on rows three times longer than the ones it was measured on. Not the rebuild's doing: at `97548df` the same count is 2 974 with two over. 3 072 clears 2 991 by 81 and taking that rung is the operator's word. Two measurement lessons: **a reviewer's integer is a reading of its own population** — lens 2 judged 113 of 515, so its «17» and «24» are sample counts and the verdict's «expected small» was calibrated on a third of the corpus — and **a record's prose outlives the state it describes**, three times here, each now deriving its reading from the measurement beside it. Registration re-issued as a DRAFT with no price, cap $4.00, all three clauses of 3.25 grepped back into `docs/SPEC.md` at every build | accepted |
| 2026-08-23 | [[lora-c-rationale-supervision-and-the-shared-pool]] (ruling) — **операторский рулинг 23.08: «поднимай max_seq_len до 3072»**, та самая ступень, которую поправка 3.25 (1) назвала заранее («the next rung is 3 072 and it is one word, not a redesign»), данная после того, как заказанная той же клаузой проверка реальности вернулась выше её собственного стопа 2 800. `config/qlora.yaml` — **ревизия 3**. Ни одна из 506 строк не за потолком ни по счёту токенизатора (широчайшая 2 975), ни со слэком (2 991); запас **97 токенов по истинному счёту и 81 со слэком** — обе читки опубликованы, потому что одно число в прозе здесь заново открывает Dv777. **Запас тонкий, меньше 4 % потолка, но это СЧЁТ, а не проекция** — в этом вся разница с тем, как ставили 2 816: модель отношения по-прежнему недооценивает широчайшую строку на 228 токенов, и рулинг этой находки не трогает. Константа `STOP_AT = 2800` остаётся в продюсере: удалить порог значило бы удалить причину, по которой существует 3 072, и `results/lora_c_tokens.json` теперь читается как история — счёт, который сработал, эскалация, ответ. **Линия B не тронута второй раз, и это измерено, а не выведено:** все три запечатанных артефакта пересобираются байт в байт при ревизии 3, её пины остаются на ревизии 1. **Чего в законе НЕТ:** `docs/SPEC.md` по-прежнему читается «1 408 → 2 816», размеченного блока под этот рулинг не существует — SPEC файл тимлида, исполнитель его не правит. Регистрация несёт поле рядом с дословной цитатой о том, что рулинг перекрыл число, чтобы грep по SPEC за действующим потолком возвращал 2 816 **с указателем**, а не молча. Написать блок — за тимлидом (Dv780) | accepted |
| 2026-08-24 | [[lora-c-the-card-cannot-train-at-3072]] — `lora-c-run r2` bought ONE pod under ruling (о) — RTX PRO 4500 32 GB, $0.72/h, EU-RO-1 — and **KILLED it on a pre-registered rung with the attempt intact**: 3 803 s = **$0.7606 of a $4.00 cap**. The smoke's six optimizer steps of arm A's config at 3 072 died `torch.OutOfMemoryError` with **zero steps logged**, and the order of the log lines is the finding: the shipped trainer's OWN registered fallback fired first (`micro_batch 2 → 1, grad_accum 8 → 16`, effective batch held — `config/qlora.yaml`'s own words) and it OOMed AGAIN at micro_batch 1, 29.70 GiB of 31.37 in use, trying to allocate 1.72 GiB in `backward()`. **Nothing was edited on the pod and nothing needed to be: the config's smallest permitted step does not fit on 32 GB.** The ruling's reasoning was that 32 GB is headroom AGAINST OOM at a frozen micro_batch; the measurement is the opposite, and it cost $0.76 before a training dollar. It does NOT say 3 072 is wrong — every s/step reading this stack holds was taken on 48 GB and the rows have not moved; the remedies (a 48 GB card, which reads `none` in the volume's datacenter in two readings a day apart; another datacenter = another volume, unpriced; RTX PRO 6000 96 GB at $2.09/h, which re-prices the plan; `expandable_segments`, a guess against 1.33 GiB of fragmentation) are named and NONE taken. **The same $0.76 kept both base legs** — 198 replies each, hashed on the pod and again on the Mac before the delete, **0 parse refusals** — and the CONTROL column did what it was registered for: base v3 is base v2 plus one clause asking for a sentence of reasoning before the label, and it agrees with the team lead on **66 of the 98 holdout rows inside E against base v2's 61** (85 → 91 of 133 overall), with the whole gain in `не_наш_рынок` (35 → 41 of 66) and `сеть_ритейлер` giving one back — the share of any arm's win that would have been credited to the adapter and belongs to the prompt. Report-only: base v3 takes no bar, the denominator is 98 not 100, and the bar is per ARM. The mention cell barely moves (20 → 19 of 66, on E's denominator, NOT holdout-100's 52). **The charged 6.14 s/call is a v1/v2 rate: v2 realised 5.32 and v3 realised 9.20 — 1.50× the charge** — measured by this line's own rung 3, which grades a base leg on the rate it is REALISING while it bills; without it the first spend decision would have been the projection four thousand charged seconds in. **Seven transport gaps were found at $0 before the create**, by driving the shipped chain against the frozen packs with a fake client: the pass-1 handshake refused this line's pack (v3 is not in `prompts.PASS1` and may not be added) — the v2 leg with it, since one pack carries both; the render sent a v3 item to the READER's renderer (`KeyError: 'post'`, the shape that killed pass1-probe at 427 billed seconds); the leg runner had no `--adapter`; and the pass-2 pack carried neither its runner's instrument block, nor `carried`, nor `serving`. The seventh has a reachability answer: that pack DECLARED `pass2_r2`'s 15 569-char ceiling and RENDERED at `pass2`'s 12 000, and the gap is reachable by an ADAPTER and not by the base leg that built it — bounded by rendering every reference thread with EVERY row filtered in, **one thread of 15 crosses 12 000 (`@matusi_ukr:22272`, 12 399) and none crosses 15 569**, and that thread carries a bar-1 flagship case so dropping it would make the bar unscoreable. **Step 0.75:** the false production string leaves the synthetic header (160/0 → 0/0) for a registered constant that now reads 160/0; prefix byte-identical, census still 666/0 with headroom 97 and the widest row still real — and the confound REMAINS, registered rather than repaired, with its $0-built 40-request census unspent. Registration FROZEN by the create; **attempt NOT SPENT**; no bar scored and none could be | accepted |
| 2026-08-24 | [[lora-c-arm-b-rendering-and-the-two-leg-pass-2]] — `lora-c-armb` at $0 closes the two questions `lora-c-run` STOPPED on, both by team-lead ruling (н) of 24.08, both grepped back into `docs/STATUS.md` on every build. **A synthetic row is a v3 QUERY under the rules a real one obeys** — same renderer, same pool of 515, five per-class neighbours, the own-thread block (trivially satisfied), amendment 3.25 (2)'s equality refusal, `target_for`'s +1 boundary with the rationale first — so `results/pass1_sft_v3_arm_b.jsonl` is now a FILE and not the arithmetic `506 + 160`: the 506 real rows BYTE-IDENTICAL as a prefix, re-rendered and compared against the SHIPPED bytes before a byte is written, then 160 rendered synthetic rows carrying `synthetic: true` and their error class where no real row carries anything. **666 of 666 encode** through `train_qlora.encode_pass1` with the real tokenizer at the pinned revision — 0 refused, min 1 445 / median 1 603 / max 2 975 against 3 072, headroom 97, and the widest row is still a real one; `class_weights` returns **five** classes with `молочный_бренд` = 666/(5·32) = **4.1625**; isolation is six readings, all zero where zero is the answer (800 examples all in the pool, 0 synthetic ids used as an example over all 666, 0 example texts equal to their query, synthetic ∩ E/holdout/gold = 0). **The fifth reading is a FINDING the contract did not ask for: five header lines are carried by synthetic rows and by NONE of the 506** — `NO_POST_TEXT` (0 of 506 real rows render it) and the four `<thread channel="synthetic" post_id="<error class>">` tags — marking all 160, and since those 160 carry ALL 32 `молочный_бренд` targets in this line (arm A has zero), an adapter can condition the class on a marker no eval request ever shows, which leaves «synthetic did not help» and «the model learned the marker» indistinguishable on the one comparison arm B exists to make. The ruling names the renderer, the pool and the target shape and does NOT name the four header fields; what was built takes the branch the existing rules already take, and the alternative — a synthetic row wearing a real channel — would fabricate provenance inside training data and break the `synthetic:` prefix the whole isolation rests on. The sixth reading is the control: the pool holds ONE `молочный_бренд` row, so that example slot is the same row for every query in this line, real or synthetic — **the examples block is not where the tell is**. **The second ruling fixes pass 2 at 2 legs, not 4, and it changes the answer:** 2 × 11 × 97 = 2 134 s = $0.4742 off the fixed part is the ONLY term that moved, and the break-even rises from lora-c-run's 48.47 / 58.61 to **63.29 s/step at $0.80/h and 73.43 at $0.74/h** — so lora-b's registered 61.047 now fits at both prices and its measured 68.442 at the billed one, where NEITHER fitted before. **Still no s/step is registered**: both readings were taken at ≤1 222 tokens against this line's 1 445–2 975 and the smoke buys the rate; the finding is published as a FIELD so a test checks it instead of parsing prose, and both price columns now charge the smoke identically and divide by 144 in each. **A read-only stock probe dates the card question:** EU-RO-1 (the volume's own datacenter) at 2026-08-24T08:24Z reads **RTX A6000 48 GB `none`** and A40 not catalogued there at all; 7 of 47 cards report stock, RTX PRO 4500 32 GB at $0.72 the only one at High. Listed, priced, NOT chosen — a listing is not a create, and the create is also this record's freeze. Registration still a **DRAFT**, attempt unspent, `bars` untouched, no price key | accepted |
| 2026-08-26 | [[lora-c-the-volume-moves-to-ca-mtl-3]] — `lora-c-migrate r2` built the training line's second network volume (`mp-lora-c`, 100 GB, CA-MTL-3) and **proved a pod there loads the model from it: 40.22 s at 17.046 GiB of 79.138 on an A100 PCIe 80 GB**, through the shipped loader at the pinned revision. $0.287267 of a $1.50 cap, 744 s, nothing trained and nothing evaluated. Two stage measurements are new to the project and every future plan budgets from them — **venv 132.0 s** and a **340 MB/s** download of 62.58 GB onto a network volume. **Rung 5 KILLed and the rule was NOT edited**: `du -sb` on the HF cache moved 40 bytes of the loader's own bookkeeping (`refs/main` plus four zero-byte absence markers) with 0 blobs written, so the verdict stands as the rung's own and the remedy — the invariant belongs on the blobs, not the cache directory — is carried into r3's step 0.5. The issue-time stock read 107 minutes after ruling (с) was written held **four cheaper volume-capable rows**, three of them in US-KS-2 which also carries the same A100; put to the operator with the priced table BEFORE the volume was created, answer **CA-MTL-3 as ruled** — the cheap rows are all 48 GB and 48 GB has never been measured at 3 072. The project now rents two volumes at ~$0.47/day, ~21 days of what cycle 2 has left | accepted |
| 2026-08-26 | [[lora-c-the-line-trains-at-90-seconds-a-step]] — `lora-c-run r3` bought ONE pod under ruling (т) — A100 PCIe 80 GB, $1.39/h, CA-MTL-3, cap $7.00 — and **KILLED it at the projection rung with the attempt intact**: 1 152 s = **$0.4448**. The reading it existed to buy is `provenance.json::run.seconds_per_step` = **89.961 s/step at `max_seq_len` 3 072**, the first measurement of this line's training rate that has ever existed, and it refuses the plan: 21 284.8 s against an 18 000 s stop and $8.1271 against a $7.00 cap. **It is not warm-up, and that was checkable because the decomposition was registered BEFORE the smoke landed** — the step-6 loss line covers ONE step and still divides by `log_every`, so multiplying it back gives that step's own wall, **87.207 s**, leaving 16.6 s of 539.8 (3.1%) as amortised start-up. Against the two priors this repo owned — 61.047 registered by lora-b and 68.442 measured on its arm A, both at ≤1 222 tokens on an A6000 — this line is **1.47× and 1.31×**; amendment 3.26 (3) retired the ratio model for CEILINGS and this retires it for RATES. **No charge explains the KILL away:** every corner of the two rates nobody had measured is still over, and even 6.14 s/call with the ten pass-2 threads read at $0 comes to $7.33; solved backwards the cap afforded 68.05 s/step and the stop 67.15. **The finding underneath it is bigger than the KILL: the shipped gate this session inherited would have said GO.** `gate_lora_b.measured_step` takes `max(mean, last)` of the loss lines, each of which divides by `log_every` regardless of its window, and on this session's own log it returns **53.690** — a 40.3% understatement landing under the 68.05 the cap afforded, so both arms would have trained into the platform's terminate flag for $6.95 with no adapter, no eval and no bar, and the frozen registration's one attempt with them. Both halves were already in this repo's ledger as **Dv813 and Dv814**, named by the vramprobe report on 25.08 and never fixed; they were fixed here because r3's gate was written by reading the trainer's own arithmetic instead of trusting the sibling. **$0.4448 also closed a registered blocker: 48 GB is refuted by measurement** — peak allocation **49.54 GiB**, `nvidia-smi` 57 297 MiB at 100% and 304.72 W, so every cheap alternative in the migration's stock table is smaller than this run's own peak. **A model load is not a constant: 40.22 · 138.49 · 13 s**, same weights, same volume, same loader, the last two inside ONE pod — a 10.7× cold/warm spread. **Rung 2 came within 152 s of KILLing a healthy run:** a training stage prints nothing until step `log_every`, so the smoke was silent 448 s against a 600 s deadline while the GPU sat at 100%, and the registered liveness signals cover neither; the rung was not bent and the remedy is the next contract's. Frozen registration unmoved, **ONE attempt UNSPENT**, base columns re-read and not re-bought, six optimizer steps bought and all of them the smoke's. Three prices for the operator, all three his: the plan as written **$8.2183**, **arm A alone $4.0339**, both arms at one epoch $5.7174 against `epochs: 2` being frozen law | proposed |
| 2026-08-28 | [[harness-guard-v2-behaviour-not-spelling]] — the `PreToolUse(Bash)` guard is rewritten from four literal regexes into a Python hook that splits on `&& \|\| ; \|`, strips env assignments and git globals, and judges the first tokens of each segment. Cause, measured against the shipped v1: **seven spellings of the same sweep exited 0** — bare `ruff format` (ruff's own `[FILES]... [default: .]`, a byte-identical file list to `ruff format .`, 475 files here), `ruff format src tests scripts config`, `ruff format --check .`, `ruff format --no-cache .`, `git add -u`, `git add :/`, `git stage -A`, `git -C . add -A` — while `git add -a`, which the regex DID refuse, is not a git switch at all (`error: unknown switch 'a'`): the alternation was written from the contract's four example strings rather than from git's flag set (Dv875). The same patterns matched anywhere in the command text, so a `git commit` whose MESSAGE quoted them was refused and every `harness-v2.1` commit went through `-F <file>` (Dv874). **`tests/test_hooks.py` asserted exactly the four strings the regex was written from, so the suite was green through both defects.** What it protects is a measured loss: repo-wide `ruff format` moves the sha of `scripts/write_lora_c_prereg.py` and `scripts/build_lora_c_marker_census.py`, both pinned inside sealed registrations, and `make check` runs `ruff check` and not `ruff format --check`. `permissions.deny` gains `Edit(/docs/archive/**)` in the same step — the 23 archived prompts had fallen outside `Edit(/docs/PROMPT-*.md)`, which is not recursive. Acceptance: `pytest tests/test_hooks.py -q`, 16 refused and 10 accepted, bare `ruff format` among the refused. Found by an adversarial re-derivation of the executor's own work, not by the suite | accepted; applies in `retail-census-r2` step 0 |
| 2026-08-30 | [[one-live-raw-root-and-an-opt-in-union]] — SP-4 stopped S2 before an irreversible append into the four PINNED files of `results/raw_v1_baseline.sha256`, and the team lead's ruling (a) widened the answer from the top-up to the whole loop: `data/raw/` is an ARCHIVE read-only forever, `data/raw_r2/` is the ONE live root taking the top-up for every collected channel, and a reader that wants the corpus reads **v1 ∪ r2, deduplicated on (channel, msg_id), r2 winning**. The load-bearing half is that the union is **opt-in and never the default**: nine consumers were counted with a *which-root* column, and two of them want opposite things — `promo_census_c2` must see r2 because it prices the topped-up corpus, while `draw_promo_threads` must NOT, because its population is the frozen 678 and a union there would move 678/488 with nothing in the file to say so. The draw's sha `6f9fa245b9d70254` was re-read after +1 340 posts and again after +1 190 comments and did not move — comments being exactly what it reads. The refusal moved out of `collect_r2.py::refuse_pinned` (a channel LIST, which passes trivially once the collector is retargeted and then guards nothing) into `RawStore.append`, the chokepoint every writer goes through; `refuse_pinned` stays as the refusal that fires before a Telegram session is opened. Two r1 writers, `collect_5c1.py` and `backfill.py`, are refused by the new guard and left in place rather than deleted — they are how the archive was built. `data/raw_fresh_45g5/` is a THIRD root the ruling does not mention: named as a debt, not folded in | implemented |
| 2026-09-01 | [[the-goal-loop-never-engaged]] — §8's resume protocol rests on a mechanism that did not engage | open |
