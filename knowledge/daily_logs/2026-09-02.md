---
type: daily
date: 2026-09-02
tags: [daily, promo-pulse-1, money, tick, screen, code-review]
---

# 2026-09-02 — the instrument finished; the money line still refuses

## Summary

**START RITUAL.** Four team-lead files committed by path (`620b6de`); the newest ruling read
(«01.09 (later)»: the operator topped up +$10, ceiling → $7.00, C2 runs whole, the even cut void);
the old STOP applied and deleted.

**The cap moved and the guard refused anyway.** `CYCLE3_CAP_USD` 4.80 → 7.00 with the ledger's own
`note` (`981202b`). The ruling's other clause — «anchor unchanged» — leaves the balance ($14.24)
above cycle 3's anchor (`4.4799665639`), and `enforce()` refuses on exactly that: **exit 1**, rung 0
(`--step promo-pulse-1 --step-cap 3.20`) writes no step ledger, S4 cannot start. The refusal is the
module's own documented invariant, so it was not weakened; the anchor is a write-once record and the
guard says re-anchoring is the operator's, so it was not moved. New STOP, and the operator corrected
its framing: **not a fork, one yes/no question** — may the anchor move to the balance at the moment
of his word with `anchored_at` left alone. Report and STOP rewritten to say that in one voice
(`6209166`, `11bdbf6`).

**S12–S14 and S5 landed, all $0** (`1db834d`, `4950856`). `make tick` (`scripts/tick.py`) promotes
what is on disk into the six promo tables — uuid5 ids and `INSERT OR IGNORE`, so a second tick
writes zero new rows and the clock-free export stays byte-identical. `make promo-screen`
(`scripts/build_promo_screen.py`) renders `results/promo_screen_data.json` and reads nothing else.
`scripts/draw_truth_20.py` (20 rows, seed 42) and `scripts/draw_positions_50.py` (50 of 145 over 8
chains, stratified) both reproduce byte for byte. The clean-clone end-to-end was proved by actually
cloning: same screen sha `28fffc93c723ab8d…`, removed source → `Error 1` naming the file.

**The `/code-review` before the first paid leg paid for itself: BLOCKING ×2**, both fixed in
`97e84e6`. (1) A refused `--close` still CLOSED the live money line — the entry was appended and
written hundreds of lines before `main` reached its refusals, so exit 1 said nothing happened over a
settled record on disk. (2) The ledger tripwire's watch list was a literal tuple policed by a test
built from the same tuple: a fourth ledger plus a real writer left the suite green. The list is
derived from the guard's own `Path` constants now, and the teardown walks `results/spend_*.json`, so
the 22 step ledgers are watched too.

`make check` **4 266 passed · 2 skipped · exit 0** @ `97e84e6` (4 230 + 36 new). **Bought: nothing.
Rungs fired: none.**

## Decisions

- [[the-schedule-reports-it-does-not-gate-the-tick]] — `data/schedule.json`'s minimum interval is a
  READING, not a refusal; a gate there would let «run twice → zero new rows» pass over a run that
  never happened.
- **Dv15** `[cause: process]`: the cooled-thread and digest helpers landed in `scripts/tick.py`, not
  in `src/market_pulse/loop.py` as plan §3 names. That module is pinned by TEN sealed records and
  moving it turned `make check` red in five files at once; restoring it cost nothing. Same manoeuvre
  and cause as Dv14 (S11 in `trends.py`). No pin moved.
- The live K10 is a saturated proxy — five of six tables are at zero on disk — so the discriminating
  reading is `tests/test_tick.py`, which ticks twice over a fixture with rows in all six and carries
  the negative control. Said out loud in the report rather than left for a reader to notice.

## Next

1. The operator's yes/no on cycle 3's anchor → `docs/plans/promo-pulse-1.STOP.md`. It is the only
   thing between here and S4.
2. Then **S4 (paid C2, all 3 008 pages, ≈$3.16)** → **re-draw the 50** (today's draw is a PRE-C2
   population and its record says so — it must not be labelled) → tick → screen.
3. The team lead's labels arrive in batches of 10; K6, K8, S9, S10 wait on them either way.

## Blockers

- **Cycle 3's anchor sits below the balance** — the guard refuses until the operator's word. The
  single blocker of the paid leg. → [[the-goal-loop-never-engaged]] is a separate, standing one.
- **K4 must not be re-run**: `scripts/promo_projection_c2.py` writes the repealed «$4.80» into its
  own record (`:366`), greps the renamed `CYCLE 2 SPENT` line (`:171`) so `remainder.spent_line` is
  empty, and ignores the guard's exit code (`:153`). Named debt from the same review, not fixed.
- The 5c2 endpoint and template were deleted 08.08 (the `mp-srv2` volume survives at $0.2333/day),
  so the next paid vision starts from a BUILD and rung 0 must be counted WITH the boot.
- 10:27: session ended (auto)
- 10:31: session ended (auto)

---

## Session 2 (10:31 →) — the anchor moved, S4 priced WHOLE, rung 0 fires

**START RITUAL.** Four team-lead files committed by path (`b68d14f`); ruling 02.09 read; the ONE
write it names done with the guard's own serialiser (`results/spend_cycle3.json`: anchor
`4.4799665639 → 14.4799665639`, `anchored_at` untouched, note appended verbatim, `bf1665d`); guard
→ **exit 0, anchor $14.48, CYCLE 3 SPENT $0.2528 of $7.00, REMAINING $6.7472**; STOP deleted
(`c9fe49e`).

**S4's $0 half, all of it.** The census's population had no pages on disk (three manifests cover
367 of 968 posts) → `scripts/fetch_promo_media_c2.py` ($0, Telegram, resumable, the pagecount's
own fold key, per-channel match against `pages_exact`). The driver `scripts/run_promo_c2.py`
imports `run_5c2`'s transport/gates/legs; the text leg is pinned by id off the live store; stages
in the spec §3 order; a re-projection after every channel; `--run` refuses when `rung_0.fits` is
false and runs the guard before the first call. 12 tests, the served half driven on a stub through
the real `loop.page_pass`/`post_pass` into a temp derived root — rows durable, a second run sends
nothing, a refusing gate makes no gold call. The fetch itself: 3 008 of 3 008 pages, 17 of 17
channels equal to the pagecount, 188 non-photo members recorded (146 video + 3 poll + 39 other, the
pagecount's own kinds); the harness killed the background run twice (its own limit, not Telegram),
the resume re-fetched the ONE truncated file the first kill left (`intact()`, tested) and the
last leg ran under `nohup`. Runbook `knowledge/runbooks/promo_c2_paid_leg.md`
(the 30.08 smoke's create was never recorded; the readback pattern of 5c2/vis-b is).

**Rung 0 on the WHOLE step does not fit 3.20.** The ruling's cap came from `c2_priced_usd`
$3.1563 — the vision leg alone, its note saying so. Pages + 405 posts + boots + tail at K4's own
corners: cheap $2.6041 (fits), priced $3.5241 (+10.1 %), dear $3.6059 (+12.7 %); 5c2's realised
rate beside as a reading ($9.97). The dear corner decides (K4's reason). → STOP, ONE question:
`--step-cap 3.95` (the cycle's room today after C3's two caps; dear + the cap gate's one-job
reserve $0.2843 = $3.8902) — yes/no, with what «no» buys (≈9 pages in 10, no text). Not weakened,
not re-priced kindly, not narrowed. → [[a-cap-set-from-one-legs-price]] (native memory).

**Bought: nothing. Rungs fired inside a run: none.** RunPod: `serverless list []`, 0 own
templates, `pod list -a []`, volume `mp-srv2` only.

### Next (session 3)
1. The cap ruling → apply to `run_promo_c2.STEP_CAP_USD`, `--register` again, runbook §1–§5.
2. After S4: `make tick` → re-draw the 50 → `make promo-screen` → report.
3. Labels (dev-40, positions-50) still owed by the team lead.

### Blockers
- **Rung 0 on S4: the step at the dear corner is $3.61 against a $3.20 cap** — the STOP's question.
- The cycle's room is a clock ($0.2333/day): a «yes» tomorrow leaves C3 ≈$0.23 short of its caps.
- K4 debt unchanged (`promo_projection_c2.py :366/:171/:153`); `/goal` engagement unverified
  ([[the-goal-loop-never-engaged]]).
- 11:27: session ended (auto)
- 11:27: session ended (auto)
- 11:27: session ended (auto)
- 11:28: session ended (auto)
- 11:28: session ended (auto)
- 11:28: session ended (auto)
- 11:33: session ended (auto)
- 11:38: session ended (auto)
- 11:38: session ended (auto)
- 11:38: session ended (auto)
- 11:38: session ended (auto)
- 11:38: session ended (auto)
- 11:39: session ended (auto)
- 11:40: session ended (auto)
- 11:42: session ended (auto)
- 11:42: session ended (auto)
- 11:42: session ended (auto)
- 11:42: session ended (auto)
- 11:43: session ended (auto)
- 11:43: session ended (auto)
- 11:43: session ended (auto)
- 11:43: session ended (auto)
- 11:43: session ended (auto)
- 11:43: session ended (auto)
- 11:44: session ended (auto)
- 11:44: session ended (auto)
- 11:45: session ended (auto)
- 11:47: session ended (auto)
- 11:57: session ended (auto)
- 11:59: session ended (auto)
- 11:59: session ended (auto)
- 12:00: session ended (auto)

## Checkpoint 12:23

Since the session-2 section above: the fetch finished — **3 008 of 3 008** pages, 17 of 17 channels
equal to the pagecount, 188 non-photo members recorded; the ONE file the harness's kill truncated
was re-fetched by `intact()` (`c6a876a`). `--register` wrote `results/prereg_promo_c2.json`
(pages sha `b6bc2f67…`, posts `590f91e4…`, `rung_0.fits: false`, dear $3.6059) and `--run` refused
at rung 0 before a key or client existed (`50ef7d9`). `make check` @ `50ef7d9`: **4 278 passed ·
2 skipped · exit 0**. Report (29 lines) + STOP committed (`86a5d3f`, `99e1a24`); porcelain on the
contract paths clean; `/goal` evaluated the transcript **as a PAUSE** (rung 0 firing) — the first
time the goal loop is seen to engage, which closes the doubt in [[the-goal-loop-never-engaged]] for
the launch protocol of ruling 02.09. (f) trends 10 passed and (i) `draw_truth_20` twice
`3d80c81a9c353130` re-shown. Guard at close: `CYCLE 3 SPENT $0.2625 of $7.00` — the drip only.
Lessons → native memory: [[a-cap-set-from-one-legs-price]], [[a-resume-trusts-the-file-the-kill-left]].
- 12:24: session ended (auto)

## Session 3 (14:50–15:45) — ruling (b) applied, the paid leg's own gate refused it

Ruling 02.09 (b) landed (cap $3.95, the whole step, text leg FIRST) and was applied at `f17d966`:
one constant, `run_the_legs` reordered (warm-ups → `post_leg` → re-projection → the leaflet loop),
`--dry-run` prints `post_text` as stage 0, one new test pins the order on the stub, the cap test now
reads the LAST `--step-cap` the rulings file names. `--register` → `rung_0.fits` true (dear $3.6059,
−8.7 %). The paid session ran the runbook top to bottom: guard `--step` anchored `results/spend_promo_pulse_1.json`
at $14.19 (`fba2d79`), template `ih2rh0mvox` + endpoint `1w2cn98hcikl7b` created, `--run` under `nohup`
with a Monitor. **The (10)(a) gate refused the whole step:** page warm-up 13.466 s on
`atb_market_official_4571.jpg`, text 0.794 s, projected $12.5928 against $3.95 — no gold call,
$0.0579 billed (boot 149 s + two warm-ups). Torn down, proven by listing. The refusal branch now
records `outcome.unbought` (`8b7b965`). STOP (`b61f6b8`) carries the ruling's table (17 channels ×
13.466 s = $12.42; text leg $0.09; room $3.514 → 851 pages at 13.5 s) and three questions: a per-leg
gate, which page marginal, the order under a partial buy. Why 13.5 s: the smoke's 2.6–3.4 s was ONE
30-page job on promo photos; 5c2 on the same channel warmed up at 1.729 s and realised 10.408 s/row.
Checks shown in the transcript: (a) (b) (d) (f) (g) (h) (i); `make check-stamped` @ `b61f6b8`
**4 279 passed · 2 skipped · HOLDS**. Lesson → native memory: [[a-gate-wider-than-the-order-it-guards]]
(a reorder under a whole-step gate is cosmetic). Not done: the re-draw of the 50 (S4 bought nothing).
- 15:32: session ended (auto)

## Checkpoint 15:46

Since the session-3 section: the billing walk was given 30 min and the step's reading taken with the
guard's `--note` — `PROMO-PULSE-1 SPENT $0.0599 of $3.95` by balance delta, billing rows still
UNAVAILABLE, so it is a lower bound holding ~34 min of the volume's drip; the step is left OPEN (no
`--close`). That one command maintains BOTH ledgers, so `results/spend_cycle3.json` also grew its
session row (`CYCLE 3 SPENT $0.3516 of $7.00`, `REMAINING $6.6484`) and turned up as a moved contract
path in the (l) check — read, then committed by path (`d854a63`, `9ce3c0a`), not «fixed». The report
was written from a draft outside the repo and trimmed 32 → 30 lines (`b12e072`), the STOP shown with
`cat`, and (l) now prints nothing. Endpoint `1w2cn98hcikl7b` and template `ih2rh0mvox` stay deleted;
`runpodctl serverless list` → `[]`, `pod list -a` → `[]`, `mp-srv2` alive. Waiting on the team lead's
answers to the STOP's three questions: a per-leg gate · which page marginal · the order under a
partial buy. MEMORY.md is at 24 995 of 25 000 UTF-16 units: the next lesson compresses a
line rather than adds one.
- 15:48: session ended (auto)
- 16:32: session ended (auto)
- 16:33: session ended (auto)
- 16:39: session ended (auto)
- 16:40: session ended (auto)
- 16:53: session ended (auto)
- 16:56: session ended (auto)
- 16:59: session ended (auto)
- 17:01: session ended (auto)
- 17:03: session ended (auto)
- 17:04: session ended (auto)
- 17:34: session ended (auto)
- 18:03: session ended (auto)
- 18:05: session ended (auto)
- 18:33: session ended (auto)
- 19:03: session ended (auto)
- 19:05: session ended (auto)
- 19:19: session ended (auto)
