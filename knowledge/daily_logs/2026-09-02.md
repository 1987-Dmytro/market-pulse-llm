---
type: daily
date: 2026-09-02
tags: [daily, promo-pulse-1, money, tick, screen]
---

# 2026-09-02 — the instrument finished, the money line refused

**START RITUAL.** Team-lead files committed by path (`620b6de`); newest ruling read («01.09
(later)» — top-up, ceiling $7.00, C2 whole); the STOP file applied and deleted.

**The cap moved and the guard still refuses.** `CYCLE3_CAP_USD` 4.80 → 7.00 with the ledger's
`note` (`981202b`). The ruling's other half — «anchor unchanged» — leaves the balance ($14.26)
above cycle 3's anchor ($4.48), and `enforce()` refuses on exactly that: exit 1, rung 0 writes no
step ledger. Not weakened, not re-anchored: both are the operator's. New STOP.

**S12–S14 and S5, all $0.** `make tick` (`scripts/tick.py`), `make promo-screen`
(`scripts/build_promo_screen.py`), `scripts/draw_truth_20.py`, `scripts/draw_positions_50.py`,
six promo-table writers and `upsert_digest` in `aggregates.py`. Clean-clone end to end proved by
cloning: same screen sha, removed source → `Error 1` with the file named.

**Two things worth remembering.**
1. `loop.py` is pinned by TEN sealed records. Moving it turned `make check` red in five files at
   once; restoring it and putting the helpers in `scripts/tick.py` cost nothing. Same shape as
   Dv14. Reach for a new module before a pinned one.
2. The `/code-review` before the paid leg was worth its cost: BLOCKING ×2. A refused `--close`
   still wrote a closing entry to the live money line — exit 1 saying nothing happened over a
   settled record on disk. And the tripwire's watch list was a literal tuple policed by a test
   built from the same tuple: a fourth ledger plus a writer left the suite green. Both fixed in
   `97e84e6`; the list is derived from the guard's own constants now.

`make check` 4 266 passed · 2 skipped · exit 0 @ `97e84e6`. Bought: nothing. Rungs fired: none.
