---
type: daily_log
date: 2026-09-03
tags: [daily]
---

# 2026-09-03

## Summary

Четыре сессии исполнителя. Дважды закрыты вилки рулинга (b), построена и куплена платная половина
S9, и **итерация 1 dev-петли прошла целиком**: 56 из 56 юнитов, signal-type agreement **0.8792**
держит бар 0.75, subject agreement **111/140 = 0.7929** — красный на ОДИН коммент против 0.80.
Деньги дня: $0.089622 на два пода, убитых собственным дед-маном с записи ПРОБЫ, и $0.435778 на
итерацию 1 → **$0.5254 из $2.50** по шагу, $4.0631 из $7.00 по циклу 3.

Три вещи, которых не было и без которых ранбук не исполним, построены за $0 до денег: соединение
ответов пода с гейтом K8 (`--score`), таблица ошибок и решающий гейт таблицы (`--project`).
Состязательный аудит платного пути подтвердил 16 дефектов до создания пода — восемь из них
испортили бы прогон. Решения дня: [[the-fence-is-not-the-answer]] и
[[terminate-after-is-the-cap-less-what-the-step-spent]].

Блокеры: subject ниже бара на один коммент — вопрос КОДБУКА, файла тимлида; леджер шага открыт
(billing walk не приземлился); забор ```json вокруг ответов ждёт рулинга.

## Sessions

## Сессия 6 (исполнитель, $0) — обе вилки рулинга 03.09 (b) закрыты, шестой STOP на несуществующем инструменте

- **Ритуал.** Файлы тимлида закоммичены по путям (`fb00e7f`). Шаг закрыт: `--close --tolerance 0.05`
  → `settled_usd` **$2.986741** (pods $0.052079 + serverless $2.934662; том $0.223611 исключён
  обходом и остаётся в цикле). Рулинг называл $3.1909 — это ДЕЛЬТА БАЛАНСА вчерашнего дня, а обход
  считает по строкам биллинга. Число тимлида в запись НЕ вписано; вопрос вынесен в STOP.
- **Вилка 1 (раскол стора) — закрыта.** Точка раздела найдена САМОЙ печатью: для каждого из 38
  источников — k, чей префиксный sha256 ЕСТЬ запечатанный дайджест. 38/38, 23 целых · 15 префиксов
  (529 строк) · 35 файлов целиком (3 972). Copy → verify → truncate, бэкап до первого реза.
  Экспорт w1: **55 расходящихся листьев → 5**, ноль под `provenance.evidence`, 4 474 листа данных
  идентичны. Пятый пин (`build_aggregates.py`) ЗАЯВЛЕН через moved-pins, не перепиннут.
- **Вилка 2 — закрыта, и её условная проверка СРАБОТАЛА.** 7 row_id под обоими носителями, 6
  сообщений, ровно **1** тройка (brand, product, volume) прочитана обеими ногами (`@forainfo:6056`),
  в w1 — 0. 1 > 0 → дедуп трендов по (window, channel, msg_id, SKU) с приоритетом `leaflet_page`.
- **Свой дефект, найденный и починенный.** Фикстура `test_run_promo_c2::stage()` патчила только
  `DERIVED_ROOT`, и заглушечный прогон дописал **13 строк** в РЕАЛЬНЫЙ `data/derived_w2`. Живой
  корень восстановлен повторным расколом из бэкапа (3008/1093/15/385), печать w1 не двигалась.
  Фикстура теперь проверяет ВСЕ derived-корни, выводя список из `dir(run_loop)`.
- **STOP.** Платной ноги S9 не существует: у `promo_dev_pass.py` нет ни `--smoke`, ни `--register`,
  ни `--run`; проверено по всему репозиторию. Рулинг санкционирует ЗАПУСК, а не СБОРКУ.
- **Сессия 7 ($0, седьмая пауза).** Рулинг 03.09 (c) сложен в план §9a одним коммитом ДО кода;
  шестой STOP удалён. Транспорт установлен ФАКТОМ: `local_llm.batch` рендерит из запиненного
  `prompts.py` → serverless не обслужит промт, значит ПОД. Построена $0-половина S9: `--register`
  (рунг 0 в единице пода, дорогой угол **$1.3193 из $2.50**), `--pack` (56 юнитов), `--open` /
  `--close-segment`, `promo_dev_pod_runner.py`, ранбук. 12 тестов, у каждого отрицательный контроль.
- **Урок дня: сухой контакт преамбулы пода на Маке ловит то, что иначе ловит платный бут.** Пак
  пинил sha пэйлоада, под передеривает отрендеренный запрос — разница в целой инструкции
  positions. Отказ был бы ПОСЛЕ бута. Прогон `check_law` + `check_instrument` + `check_requests`
  по настоящему паку стоит $0 и теперь это тест.
- Под не создавался. Ни один рунг не сработал. `pod list -a` и `serverless list` — `[]`.

## Session 8 — the first PAID attempt at S9, and the eighth STOP

Ruling 03.09 (d) executed §0→§2 of `scripts/runbook_promo_dev_1.md`. The three §1 corrections landed
first (`5d0386b`); `make check` green at that HEAD — 4 313 passed, exit 0 — before the bundle was cut.

Two pods, both killed by the registered ssh dead-man while reporting `RUNNING`:
`ocnsveqetsve8w` 215 s $0.044194 · `gxuil044h61fkx` 221 s $0.045428 → **$0.089622 of $2.50**
(`results/promo_dev_loop_run.json`). Rung 1 GO both times. Nothing generated, no leg staged,
listings `[]` after each delete.

**The gate is the defect, and it is mine.** `gates.ssh_deadman_seconds` 180 s came from the PROBE
record `prereg_reader_probe_v5b.json`; the production sibling `prereg_pass2_signals_r2.json` registers
500 s for the identical rung and records six readings of the span at 14.5 → 262.5 s. A gate below the
observed maximum KILLs before its measurement exists. I borrowed rung 0's overhead from the sibling
that settled and the gate from the probe — one transport, two authorities. Not moved by me: it is a
registered number in a pre-registration whose money has started. → [[one_transport_two_records]]

Side finding worth keeping: `runpodctl pod create --terminate-after` takes a **datetime**, not a
duration — the runbook's `90m` was never a value of that flag, quite apart from being too small.

## Session 9 — iteration 1 ran WHOLE, and eight defects caught before the money

Ruling 03.09 (e) executed as one item: the runbook end to end with the dead-man at 500 s.

**The ruled change.** `gates.ssh_deadman_seconds` 180 → **500**, read from
`prereg_pass2_signals_r2.json :: kill_clock[rung 2]` — the production sibling, six readings 14.5 →
262.5 s — with the source named in the record. The probe's record still supplies boot_kill,
delete_margin and max_recreates and nothing else. The test was re-pointed at the ruled source at
equal strength and now drives a second record to prove the number is READ, not typed. Registration
re-emitted (only that field plus the live money and price readings moved), pack rebuilt (only
`registration.sha256`), both committed before any pod existed. → [[one_transport_two_records]]

**The audit that paid for itself.** Six dimensions × adversarial verification, 44 agents, run before
the create: 16 defects confirmed, 22 refuted. Eight would have wasted the pod — the launch died at
import (`pass1_pod_runner.py` was not staged), `HF_HOME` was missing (59 GB of weights on the meter),
§7 overwrote the paid replies after teardown, leg B's answers were truncated to one offer,
`--terminate-after` bought the FULL cap on top of the $0.089622 already spent, the step ledger was
never anchored so the settle refused after the money, the decision number had no producer, and an
unreadable `DEAD` was a zero-second dead-man. All fixed at $0 before the create.

**The run.** Pod `0z95ve2n570f0t`, EU-RO-1 SECURE RTX 4090 at $0.74/h, 20:17:41Z → 20:53:01Z =
2 120 s = **$0.435778**. Port at **37 s** of create-elapsed. Boot 179.9 s off the warm volume. Smoke
17.6/47.7/178.3 s → `--project` measured **81.212 s/thread** (max 178.298) against the borrowed
23.76 — 3.4× — and read **GO at $0.7844** against its $0.80 band. GO cost ~14 s of idle. Then 53
units on the same loaded model, 1 717.7 s of generation, every reply balanced, every finish `stop`.

**The result.** Leg A 40/40, 151 gold-shaped rows, 0 parse failures: signal 0.8792 HOLDS, subject
0.7929 RED by one comment. Strata: currency 0.7377/0.8583, decimal_only 0.8354/0.9000. The 29 misses
are one shape — the model answers the CHAIN where the codebook wants the item. Leg B 16/16 empty
arrays, and correct: those posts are fish, pork, garden hoses and chandeliers.

**Two lessons.** The model fenced all 40 answers in ```json and the shipped parser failed every one
at char 0 — iteration 1 would have reported subject 0.0 for a formatting artifact; and the shipped
stop closes the first OBJECT, so leg B's array-shaped registered prompt would have been cut to one
offer. One shape: a parse or stop rule written for one leg silently destroys another.
→ [[the_wrapper_is_not_the_answer]] · [[the-fence-is-not-the-answer]]

**The settle refused, correctly.** `--close --expect-ms 2120000` met a walk that answered «no billing
rows yet» (0 ms) and refused rather than freeze a settled ~$0.00. The ledger stays OPEN and named;
the quotable figure is the pod-priced $0.435778. Without `--expect-ms`, `complete()` returns True on
any readable walk — the audit found that, and it would have cost the step's only money artifact.
→ [[unreadable_now_versus_never]]

`make check` 4 313 passed, 2 skipped, exit 0. Tree clean, both listings `[]`.
Open stop «iteration 1 read» with its three questions: `docs/plans/promo-pulse-1.PROGRESS.md`.

## Handover — read this BEFORE anything tomorrow

At close, four team-lead files are modified in the tree and **two of them are pinned inputs of the
registration whose money has already been spent**:

| pinned input | registered | on disk |
|---|---|---|
| `docs/CODEBOOK-promo-signals.md` | `6a74bc81cd2a922d` | **`2361034627ba8012` — MOVED** |
| `docs/labels-promo-dev.jsonl` | `302113b8ae3c9598` | **`2303ea4319dbf784` — MOVED** (6 lines) |
| `results/prereg_5c2_run.json` · `promo_dev40_prep.json` · `promo_threads_draw.json` | — | unmoved |

Three consequences, none of them acted on here (team-lead files; a ruling is presumably arriving):

1. **The gold moved, so iteration 1's grade is a reading against the OLD gold.** subject
   0.7929 = 111/140 and signal 0.8792 were computed before those six lines changed; correcting gold
   moves both the numerator and the denominator, and the bar comparison can flip in either
   direction. The raw replies are committed, so a re-score is $0 — do it before quoting the number.
   → [[correcting_gold_moves_the_denominator]]
2. **`promo_prompts.codebook_version()` is UNCHANGED at `a694d005972d3a66`** while the codebook
   DOCUMENT moved. The prompt and the doc have diverged again, which is exactly the state ruling
   03.09 required to be closed by re-rendering the prompt from the codebook BEFORE a paid run.
3. **Nothing checks this.** `committed_registration()` verifies only that the registration file
   itself is tracked and unmodified against HEAD; it never re-hashes the `pinned_inputs` it carries.
   A next paid run would proceed on moved pins in silence. Named, not built.
