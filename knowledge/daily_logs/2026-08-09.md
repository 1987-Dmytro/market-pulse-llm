---
type: daily_log
date: 2026-08-09
tags: [daily]
---

# 2026-08-09

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->

Сессия srv-2d шла через полночь по местному времени; её запись — в [[2026-08-08]] (UTC-день, в
котором она и прошла).

**День 09.08: `docs/PROMPT-5c1-vis-a.md` (переиздание) исполнен, $0.** Ничего платного не
создано и не вызвано: код, тесты, записи, ранбук. `make check` зелёный на каждом коммите,
**1301 → 1343 теста, 33–34 с**, `ruff format --check .` чист.

**Шаг 0 — четыре коммита долга.** Хвост vault за 08.08; тимлидские `docs/STATUS.md` и
переизданный контракт — закоммичены нередактированными; ADR [[srv2-program-close]], закрывающий
программу srv-2; зеркала `Write(...)` к трём правилам `Edit(...)` в `.claude/settings.json`.

**ADR: три решения и одно НЕ-измерение.** Serverless — продакшн-рантайм (рулинг 23 / амендмент
3.14), заработан паритетом 758/758 при `worst_drop 0.0000` против подового чтения той же
конфигурации. Зависание srv-2b — вина платформы, и решает это операторский контроль на
**необёрнутом** argv, а не наш boot-лог. Цена — находка ПРОТИВ выбранного пути ($1.0825/проход
против $0.4611, ×2.35) и уходит в брифинг 5c2. А экономия от scale-to-zero — весь экономический
аргумент за serverless — **не измерена**: srv-2d мерил один плотный проход, где `idle_share`
0.0091. Спенды по шагам НЕ суммированы: баланс-дельта — это пол, снятый в свой момент.

**Инструмент подписей построен целиком.** Третья конфигурация `CAPTION` (база NF4 на
запинованной ревизии, адаптер ВЫКЛ — и отказ на обе переменные адаптера, и отказ без
`MODEL_REVISION`, и `assert_no_adapter` на самом загруженном объекте); зарегистрированный промпт
`caption_post_gm4` (`41d33d0299fe…`) РЯДОМ с `caption_post`, выведенный `_swap`'ом по одной
клаузе; путь процессора (`load_captioner` + `CaptionClient`, 400 токенов, forward batch 1);
обязательное поле `caption_source` и отказ обоих читателей от молчаливой смеси инструментов;
драйвер `scripts/caption_gm4_5c1.py` без дефолтного endpoint id и со своим якорем траты; ранбук
`scripts/runbook_vis_b.md` с §A/§B/§C и лестницей аборта из восьми ступеней.

**Два числа, купленные бесплатно и нужные vis-b.** Один пост из шести картинок — **3.68 МБ** в
base64 против документированного потолка RunPod **10 МБ на `/run`**: картинки едут внутри джобы
(`data/annotation/**` в gitignore, на том их не положить), поэтому 19 постов АТБ — это **8 джоб,
крупнейшая 7.83 МБ**. И холодный старт на этом классе — **$0.0733** (239.022 с × $0.00030669/с),
7% капа vis-b до первой подписи; поэтому §C.1 ранбука меряет ставку на смоке из одного поста и
рвётся на проекции $0.50.

**Девиаций 13** — сильно выше нормы 0–4, и почти все из одной причины: пять дельверблов называют
интерфейсы инструмента, а не путь генерации под ними, так что работу пришлось искать в SPEC
3.13 (2)/(4), а не в контракте (Dv40). Dv50 — дефект брифа: sha `1aa89818…` принадлежит
`results/yield_bars_5c1.preregistration.json`, а не записи скрина, которая её несёт.

## Checkpoint 12:08

Контракт vis-a-r закрыт и закоммичен: **11 коммитов, дерево чистое, 1343 теста за 33 с**.
Коммиты 1–4 дельверблов проверены **по отдельности** (`git stash` → `make check` → `pop`):
1302 / 1323 / 1333 / 1342 — каждый зелёный сам по себе, а не только в конце.

**Правка после ревью, и она нашла настоящий баг.** §B ранбука — первая команда, которую оператор
набирает на платном эндпоинте, — ни разу не была прогнана в том виде, в котором документ её
печатает: write-path доказывался через `--smoke` с перенаправленными дефолтами и через фейкового
клиента с tmp-путями. Прогнал дословно (`--only` один пост, `--dump-prefix`, явные
`--record`/`--out`) — и выяснилось, что `--only` сужал `images` и `polls`, но **не** `blind`:
смок из одного поста записал бы в свою запись `no_surrogate_at_all` всего манифеста, то есть
счёт о постах, на которые прогон не смотрел. На манифесте АТБ `blind` пуст, поэтому ничего не
проявилось; у vis-c область другая. Починено, и §B теперь прогоняется тестом дословно.

**Судейское решение вынесено в шапку отчёта, а не оставлено на Dv40.** Путь генерации написан по
SPEC 3.13 (4), а не остановлен по клаузе «STOP, если дельвербл глубже брифа»; если тимлид читает
эту клаузу иначе — код написан, покрыт тестами и обозрим, но решение продолжать было моё.

Следующая сессия — **vis-b, и она платная (кап $1.00)**. Процедура целиком в
`scripts/runbook_vis_b.md`; предрегистрированный отказ инструмента (планка A падает там, где
qwen её держал) — не то, что эта сессия вправе смягчить.

## vis-b 13:55 — остановлено на ступени загрузки, $0.1581 из $1.00, ноль подписей

**Инструмент отказал сам себе.** `assert_no_adapter` — проверка, которую требует SPEC 3.13 (3), —
отвергла ту самую NF4-базу, ради которой существует: она читала
`getattr(model, "active_adapters")` на истинность, а transformers выдаёт это имя **каждой**
модели как связанный метод (`PeftAdapterMixin`), и связанный метод истинен всегда. Сообщение
самого воркера — диагноз: класс `Gemma4ForConditionalGeneration` (не `Peft*`), и `peft_config` в
списке сработавших меток **отсутствует**, то есть ничего не подключалось. Починено в `d408034`:
атрибут теперь **вызывается**, и только два его ответа «ничего не загружено» превращаются в
пустой список — всё остальное пробрасывается. Набор тестов vis-a был зелёным потому, что заглушки
вообще не несли этого атрибута: **заглушка, построенная на той же посылке, что и охранник, не
может ему возразить.** Новый негативный контроль падает против старого охранника **сообщением
самого воркера**.

**Единственное измерение, которое сессия всё-таки купила.** `results/visb_worker_boot.log`: семь
fitness-проверок за 3 983.97 мс, CUDA 12.8, затем **`1188/1188 [01:39]`** — NF4-база **вместе с
vision tower** грузится на 24-гигабайтную карту без OOM. У srv-2d сопоставимая загрузка была 127 с
*с адаптером* и без процессора. Прямой проход на шести картинках — другая аллокация и по-прежнему
**не измерен**.

**Факт, который сессию и закончил: живой воркер нельзя передеплоить.** Фикс уложен на том и
сверен побайтово — следующий handshake упал **идентично**. Boot-лог был **дописан**, а не обрезан
(ранняя копия — побайтовый префикс поздней): один `Starting Serverless Worker`, один worker id,
**две** полные загрузки весов по ~$0.031. Контейнер импортировал `serve_handler` на старте и держит
его; `--idle-timeout 60` не остановил воркер, уронивший джобу (`/health` показал
`workers.running: 1` через пятнадцать минут). Останавливает только `serverless delete` — а свежий
эндпоинт контракт запрещает поимённо. Отсюда и остановка: «a partial session reports what it
bought».

**Куплено и доказано:** том стадирован `ed9c0c9 → a8cc418 → d408034` с побайтовой сверкой шести
рантайм-файлов, адаптер `b3ca6308…` пережил merge, `start.sh` = `5b3bcbb2…`. Эндпоинт и шаблон
удалены и доказаны листингами — включая находку, что `runpodctl template list` показывает только
official+community, а `--type user` (3 → 2) единственный может доказать удаление шаблона.
Девиаций 7 (Dv54–Dv60), длинная форма — в `implementation-notes.md`.

**Следующее — vis-b-r, и ему нужен переиздание тимлида ровно по одной причине:** «No second
endpoint». Всё остальное готово: охранник починен, том уже на `d408034`, шаблон воссоздаётся
бесплатно, $0.8419 капа не потрачены. Процедура — `scripts/runbook_vis_b.md`, в который эта
сессия дописала порядок деплоя, ступень лестницы про НЕперезапуск воркера и
`--execution-timeout 1800`. Контекст программы — [[srv2-program-close]] и [[2026-08-08]].

## vis-b-r 14:40 — инструмент подписей работает, планка A взята, $0.4993 из $1.00

**19 из 19 постов АТБ подписаны собственной Gemma 4.** Восемь джоб (5.30–7.83 МБ, крупнейшая
ровно та, что vis-a предсказал по манифесту), 0 непригодных, у каждой строки
`caption_source: gm4-nf4-base`. `rematch` заново выводит ноль подписанного скрина и затем читает
**0 → 14 релевантных при планке 4**; у qwen его собственное купленное чтение было 0 → 13. Значит
**предрегистрированный отказ инструмента 3.13 (4) НЕ срабатывает** — развилку оператору отдавать
не надо. Прережка `1aa89818…` перехеширована ДО использования и не двигалась.

**R0 — префлайт на настоящем transformers, до всего платного.** Аддендум прямо говорит: под
проверкой сам миксин, поэтому заглушка субъектом быть не может. `scripts/preflight_serving_guards.py`
собирает настоящий `Gemma4ForConditionalGeneration` из крошечного конфига (без скачивания, CPU,
секунды) и гоняет гард в обе стороны: **ACCEPT** голой модели, **REFUSE** после
`PeftAdapterMixin.add_adapter`, и позитивный контроль — старый vis-a-гард обязан отвергнуть голую
модель. Три PASS, exit 0. Локальный стек — `transformers 5.14.1 · peft 0.20.0` — совпадает с
томом ровно по тем двум библиотекам, что под проверкой. Скрипт нашёл дефект в самом себе с
первого прогона: вердикт контроля считался с инвертированной полярностью.

**Boot-proof, и настоящая его половина — вторая.** `info` назвал `repo_commit d408034…` — но
`repo_commit()` дёргает `git rev-parse`, то есть читает ДИСК, а это ровно то, что в первой
попытке говорило «задеплоено», пока отвечал старый модуль. Достаточное доказательство
поведенческое: **`info` вообще ответил на `SERVING_CONFIG=CAPTION`**, а старый гард такого ответа
дать не мог. Стейджинг-под не понадобился совсем: `git diff d408034 HEAD` по всем файлам, которые
воркер импортирует, пуст.

**§C.1 прошла с трёхкратным запасом:** $0.005171/пост на тёплом воркере → 19 × + $0.0733 =
**$0.1716 против стопа $0.50**. Холодный старт измерен начисто — **244.074 с = $0.0749**, то есть
прережка $0.0733 точна до 2%. Это **опровергает** мою же вчерашнюю запись про «вдвое
консервативно»: та складывала `delayTime` и полосу загрузки весов, а `executionTime` покрывает
больше. Абзац переписан с названной ошибкой, а не удалён.

**Мост говорит: оба инструмента СЭМПЛИРУЮТ, а не описывают.** Совпадение терминов **8 из 18**
(четыре — обоюдно пустые). По постам выглядит тревожно — qwen описывает сьомгу там, где GM4
описывает майонез, — и бесплатная проверка закрывает вопрос: **все 18 постов ушли с побайтово
одинаковыми списками картинок по sha256**. Значит альбом АТБ — это шесть страниц листовки с
десятками товаров, а подпись в ~230 знаков это **выборка** из неё, и матчинг терминов ниже по
течению наследует эту выборку. Вход для 5c2, и выбором «лучшего» captioner'а не лечится.

**Одна обрезанная подпись** (`@atb_market_official:4391`, потолок 400 токенов, 847 знаков против
медианы 231) названа в записи и **не починена**: поднять бюджет значило бы сравнивать в мосте два
потолка. Все восемь дампов с тома выкачаны и сверены ДО удаления чего-либо — 19 строк против 19
записанных подписей, `sha8` совпадают по джобам.

**Уборка с позитивным контролем** (пункт 5 аддендума): сперва показано, что листинг ВИДИТ живой
объект нужного вида, потом — что его нет. Эндпоинт удалён при `jobs {completed: 12, failed: 0}`
и `workers {idle: 1, running: 0}` — сам ушёл в ноль, в отличие от попытки 1. Девиаций 7
(Dv61–Dv67), длинная форма — в `implementation-notes.md`.

**Дальше — vis-c** (232 поста + скрин v2, кап $1.50; фаза: $21.5004 из $25.00, остаток $3.4996).
Два вопроса, которые vis-c обязан решить, а не унаследовать: потолок 400 токенов обрезал 1 из 19
здесь, а посты vis-c — не листовки АТБ; и `blind` на широком манифесте непуст — баг `--only`,
который vis-a починил, был невидим именно потому, что здесь он пуст.

## Checkpoint 13:45

С чекпоинта 12:08 — **12 коммитов, дерево чистое, 1345 тестов за 34 с**, `ruff format --check`
чист, wikilinks чисты. Весь контракт vis-b пройден за две попытки внутри одного капа $1.00:
секции выше (13:55 и 14:40 по UTC+2) — это его записи, здесь только то, что появилось после них.

**Поправка, сделанная уже после отчёта, и она меняла вход в 5c2.** Стоимость этапа §C.2 я
опубликовал как $0.2211 — а это баланс-дельта между двумя чтениями, внутри окна которой работал
фетч-под за $0.24/ч. То есть под записался в форварды эндпоинта. Правильное число выводится из
измеренных секунд: 552.14 с × $0.00030669/с = **$0.1693**, и тогда проекция §C.1 ($0.1716) точна
до 1.4%. Заодно выяснилось обратное тому, что я написал про холодный старт: `worker_ids` во всех
трёх этапах один и тот же (`spotut2es3fgl2`), а `idle_share 0.1088` на 552 с не оставляет места
для 244-секундного буста — **вся сессия шла на одном воркере и заплатила один холодный старт**,
так что член `+$0.0733` в формуле был здесь консервативен. Секция трат теперь разделяет
rate-derived стоимости этапов и накопительные полы баланса и объясняет, почему их нельзя менять
местами — прецедент `cross_check_against_the_balance` из srv-2d.

**Ещё одна поправка, вчерашняя.** Запись «холодный старт вдвое консервативнее прережки» была
неверна: она складывала `delayTime` и полосу загрузки весов, тогда как `executionTime` покрывает
больше. Чистое измерение — 244.074 с против прережки 239.022 с, точность 2%. Абзац переписан с
названной ошибкой, а не удалён.

**Тест, который покраснел от собственного успеха.** `test_the_runbooks_smoke_invocation…`
утверждал, что `results/smoke/gm4_smoke1.jsonl` не существует — это было утверждение о записях
самого теста, одетое как утверждение о репозитории. Реальный прогон §B его создал, и набор
покраснел. Теперь тест пинит байты файла на время вызова; инъекция записи заставляет его падать
(проверено).

**В память ушли три урока:** живой процесс держит старый код (передеплой тома не доезжает);
докажи одинаковость входов, прежде чем винить модели; баланс-дельта — не постатейная стоимость.

**Состояние:** платного не запущено, `pod list -a` → `[]`, `serverless list` → `[]`,
`template list --type user` → два шаблона эпохи 5b (без начислений), том `qw4nwleanc` стоит.
Фаза 4 — **$21.5004 из $25.00, остаток $3.4996**. Дальше vis-c (кап $1.50), и он должен решить
сам два вопроса: потолок 400 токенов на не-листовках и непустой `blind` на широком манифесте.

## Sessions
- 00:37: session ended (auto)
- 10:26: session ended (auto)
- 11:51: session ended (auto)
- 12:11: session ended (auto)
- 12:51: session ended (auto)
- 13:43: session ended (auto)
- 13:47: session ended (auto)

## Закрытие 15:20 — vis-c: скрин отчитывается

**`docs/PROMPT-5c1-vis-c.md` выполнен целиком. $0.5505 из $1.50.** Длинная форма —
`implementation-notes.md`, раздел «5c1 vis-c», отклонения Dv68–Dv79. ADR закрытия vis-b:
[[5c1-vis-b-caption-instrument]].

**Главное: `verdicts_reportable` FALSE → TRUE.** Подписанный yield-скрин отказывался отчитываться,
потому что его пре-регистрированный позитивный контроль @atb_market_official мерил 0 релевантных
постов — а это ровно тот канал, у которого посты картинки. Скрин v2 — **тот же скрипт** (получил
`--captions`, не форк), тот же prereg `1aa89818…` перехеширован и не сдвинут, пять контролей
зелёные. **planka A взята 32 из 66** (было 29), ниже обеих 36 → 33. Три перехода FAIL → PASS:
@atb_market_official 0 → 14, @atb_aktsiyi 1 → 26, @useful_healthy_fitness_menu 3 → 6. Один сменил
**род**, а не вердикт: @gaid_skobioale был `TOO_FEW_TEXTED_POSTS` (1 текстовый пост из 18 при
планке 4 — недостижимо арифметически), его 17 подписей дают знаменатель, теперь `gradeable` и
по-прежнему FAIL — то есть измеренная находка о содержании вместо отказа судить.

**Популяция: 232 из брифа нет ни в одном артефакте.** Перепись именует 231 доступный id вне ATB и
отдельно считает 11, которые никогда не именует (медиа нет вообще) → 242. Свип: **175 fetchable,
56 blind, 0 owed**, 657 картинок, один клиент Telethon, `data/raw` не тронут. Каждый blind говорит
почему: **42 video/mp4**, 8 pdf, 2 ogg, 2 docx, 1 pptx, 1 giveaway. Два канала слепы целиком
(@polyakova_fitness 18 из 18 — видео). Из 175 **31 пост — опрос без картинки**: вопрос Telegram
несёт сам, транскрипт бесплатный, модели они не касаются. **Платная популяция — 144.**

**Ставка для 5c2: $0.002328/пост** (7.592 с/строку маржинально, старт 183.58 с оплачен один раз) —
**вдвое дешевле** ATB, и причина в манифесте, не в модели: 3.32 картинки на пост против 5.68.
Гейт §C.1 переоценивал перед каждым из 15 слайсов и ни разу не сработал ($0.35–$0.46 против
$1.4676 остатка). Реализован **внутри драйвера**: вторая инвокация заплатила бы второй холодный
старт за вопрос, на который у первой уже были цифры.

**Долг Dv67 окупился за час.** Спасённый boot-лог обрезается при каждом старте и содержал ОДНУ
загрузку весов и ровно ДЕВЯТЬ request id — это `timing.calls` re-pilot'а vis-b, а вызовов
handshake и smoke в нём нет. Значит между ними был старт: **re-pilot заплатил свой собственный
холодный старт** внутри `worker_seconds: 492.07`. «Один воркер, один холодный старт» из vis-b
**отозвано** в ADR с названной ошибкой — слот воркера сохраняет id через scale-to-zero.

**Смок потрачен на пост, который vis-b уже купил** — платить за смок надо было в любом случае, а
так он стал бесплатным контролем воспроизводимости между эндпоинтами. Те же веса, тот же prompt
sha, greedy, batch 1, списки картинок побайтово равны по sha256 — **подписи разные** (164 против
172 символов), другой воркер и другой драйвер. Матчер извлёк **ровно те же три термина**. n = 1:
доказательство существования, не частота.

**Уборка доказана позитивными контролями:** три пода, эндпоинт и шаблон — каждый показан живым в
листинге до удаления и отсутствующим после; `template list --type user` (находка vis-b) против
дефолтного листинга, который наши шаблоны не показывает вообще. Все 17 файлов сняты с тома и
сверены ДО удаления: 0 расхождений sha256, все 144 строки дампов равны написанным подписям. Том
`qw4nwleanc` остался.

**Подпись состава не поставлена — это слово оператора.** Пакет на столе:
`results/yield_screen_5c1_v2.json`. Фаза 4: **$22.0663 из $25.00, остаток $2.9337**.

Память: [[a_stable_worker_id_is_not_a_warm_worker]] (новая) и дополнение к
[[identical_seed_is_not_identical_run]] — воспроизводимость меряют на производной величине,
которую конвейер реально потребляет, а не на сыром тексте.
