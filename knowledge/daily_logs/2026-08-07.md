---
type: daily_log
date: 2026-08-07
tags: [daily]
---

# 2026-08-07

## Summary

**Ничего не исполнялось: ноль коммитов, ноль изменённых файлов, ноль трат.** День открылся ровно в
том состоянии, в котором его оставил `/close` от 06.08 (20:02), — программа паритета закрыта,
следующий шаг это **брифинг контракта 5c**, а брифа в очереди ещё нет. Единственное событие после
закрытия — уборка пяти фоновых ждунов сессий 5b.1 / 5b.2 (локальные `sleep` / `ssh`-мониторы). Они
ждали под `5pjikuu7rgitor`, удалённый 06.08 в 17:25 и доказанный листингом, поэтому снятие ждуна
ничего не осиротило и ничем не биллится.

Весь фактический материал дня 06.08 — пять подфаз, числа, вердикты, отклонения — лежит в
[[2026-08-06]]; здесь он сознательно не дублируется: один факт — один дом.

## Decisions

Нет. Ни одного решения не принималось и ни одна запись не менялась.

## Next

1. **Брифинг контракта 5c** — предложенное тимлидом деление (5c1 входные гейты + коллектор +
   $0-расчёт тома · 5c2 ядро петли + окно бэклога ~$2–2.5 · 5c3 категорийный слой + сайдкар v2 +
   алерты v0) утверждается там же. Детали и числа — в `docs/STATUS.md` «ТЫ ЗДЕСЬ» и в `hot.md`.
2. **Сверить счёт каналов на брифинге:** `docs/CHANNELS-launch.md` — «запуск **51** · watch 14»,
   набросок 5c1 в STATUS — **52**. Документ авторитетен; файл тимлида не правится.
3. **Шесть некоммиченных путей:** `docs/SPEC.md`, `docs/STATUS.md` (хвост тимлида) и
   `knowledge/hot.md`, `knowledge/daily_logs/2026-08-06.md`, этот файл (untracked),
   `knowledge/index.md` — вывод двух `/close`. Шаг 0 следующего брифа их и заберёт; если его список
   короче — коммитить по путям, а не останавливаться, и никогда `git add -A`.
4. **Два действия оператора, оба за $0:** тикет в RunPod (наблюдение по hub-воркеру — готовый
   текст) и таксономия молочки/мороженого к 5c3.

## Blockers

**Ни одного.** Ждать нечего: измерение serving сделано, батч закрыт правилом, экономика цикла-1
решена оператором 06.08 (решение 22, SPEC §3.11 (6)). Перед 5c стоит брифинг, а не препятствие.

## Деньги

**$0 за день.** GPU по-прежнему **$1.7069 из $4.00** стопа паритета (**$6.29 из $8 капа Phase 5 не
потрачено**, OpenRouter $0). Подов, эндпоинтов и шаблонов нет; капает только том CA-MTL-3
(~$0.24/день), судьба которого — $0-расчёт шага 0 у 5c.

Связано: [[2026-08-06]], [[5b2-batch-measurement]], [[5b-parity-abort-and-pod-runtime]],
[[5a-census-api-and-theme-expansion]].

## Checkpoint 16:00

Утренняя сводка выше устарела в тот же час: день из «ничего не исполнялось» превратился в **самый
плотный по правкам состава за проект**. Ниже — что изменилось с открытия.

### 5c1 исполнен целиком, кроме хвоста joins

**Гейт track-R** прошёл по 62 кандидатам (`results/entry_gate_5c1.json`): PASS 54 · FAIL 1 ·
FLAG 7, ноль FloodWait, ноль ERROR. Два правила зафиксированы **до** прогона: «мёртв» — это
конъюнкция самого канона (0 постов за 28 дней **и** нет группы, проверено 6/6 на исключённых и
14/14 на watch), иначе тест на ставку убил бы восемь операторских каналов на шуме; язык ключуется
на присутствии кириллицы, а не на `langid.detect`, чей класс `other` смешивает «букв нет» и
«кириллица вничью».

**FAIL оказался не мёртвым каналом, а не тем хендлом:** @marketopt_official ведёт на
«Маркетопт - Інформаційна», 132 подписчика против 41 518 в каноне. Дозаявка снята, заменена
@marketopt_promo (PASS, без группы → posts-only).

**Реестр** вырос 4 → 62 аддитивно (428 вставок, 0 удалений), затем ужался до **57** после вечерних
исключений. `SOURCE_TYPES` расширен `government` (@dpssgovua), `Source` — полем `watch`, оба под
тестом; `loop.plan_channel` теперь честит `watch` и не планирует треды, которые некому забрать.

**Сбор:** 2 132 поста за окно `since 2026-07-10`, 58 каналов, 0 повреждённых строк, `shasum -c`
**6/6** — шесть пинов v1 защищены на уровне канала, до постройки записи. Комменты: 106 строк по
@tretyakovaele — путь записи доказан на реальном треде (`reply_to_msg_id`, `parent_msg_id`,
64-символьный HMAC отправителя).

**D3 закрыт и исполнен оператором:** том CA-MTL-3 **удалён**, доказано листингом (было одно
хранилище → стало `[]`), перед удалением адаптер проверен на наличие второй копии — и первая
проверка не сошлась, потому что я взял хэш файла против хэша **каталога**. Пересчёт правильным
инструментом дал совпадение `b3ca6308…`.

### Состав пересобран трижды за день

51 → 48 (рулинги 07.08) → 43 (тематический скрин). Оператор увидел «Чат Аліекспрес» у себя в
клиенте — и это вскрыло **дыру в гейте**: он мерил способность, а не тему. `theme_screen_5c1.py`
померил её по уже собранному окну ($0, офлайн, лексиконом самого проекта): **из 44 измеримых
каналов у 23 ноль постов про молочку, у 15 — ноль про еду вообще**. Две болезни: не та тема
(@znishkom — 191 пост скидок на **игры**; @whitecode_zny — обувь; @offspringrus — российский
магазин) и правильная тема без текста (@discountua1 и @ATB_FANatik — листовки, товары внутри
картинок).

### Стена

**FloodWait 72 312 с (20 ч) на `ResolveUsernameRequest`**, снимается **08.08 10:02 UTC**. Лимит
аккаунтный, не по каналу: с резолва начинается каждый join, каждый фетч комментов и каждая
проверка гейта, а за день их ушло несколько сотен (63 строки гейта + 119 кандидатов скана + поиски
+ 13 joins). Joins **остановлены**, не «повторим» — повтор внутри окна удлиняет срок. Срок записан
в `results/joins_5c1.jsonl`, и коллектор теперь отказывается стартовать до его истечения.

### Дозаявка №2

@akcii_skidki_plt — **исключён** (10 постов между 2024-04-19 и 2024-06-06, 26 месяцев молчания).
«Хвилинка» — **закрыто NEGATIVE**: слово нарицательное, 12 совпадений — вантажна фірма, курсы
английского, деменция; единственная правдоподобная пара оказалась доской частных объявлений.
Скан городов Полтавщины: **119 кандидатов, 47 с группой, 85 живых, 777 367 подписчиков**
(`results/discovery_5c1_poltava.json`) — леджер для выбора, ничего никуда не вошло.

### Найдено собственными уликами

- Перезапуск скрипта рулингов **пачкал реестр** заголовком секции на прогоне, который не должен
  был менять ничего. Поймано на `git diff`, откачено, под тестом на побайтовое равенство.
- Гейт одного позднего кандидата **тихо остановил живой сбор**: `--gate-5c1` переписывал
  `registry_written` в `false`. Нашлось только потому, что я смотрел на процесс, а не на лог —
  лог последней строкой сообщал, что joins идут.
- Черновик выемки из реестра вёл блок «до следующего `- id:`» и **съел бы taxonomy и watchlist**,
  как только удаляемый источник окажется последним. Поймано до запуска.

`make check`: 1028 → **1111 passed**. Коммитов за день: 16 (`82ddfe1` → `13b04e6`). Траты: **$0**;
том удалён, значит ~$0.24/день прекратились.

Связано: [[2026-08-06]], [[5b2-batch-measurement]], [[5b-parity-abort-and-pod-runtime]].

## Checkpoint 16:52

Три задачи от тимлида, все офлайн и за $0 — стена FloodWait стоит весь отрезок, в Telegram не
ушло ни одного запроса. Два коммита: `9cb2eb8`, `2fc784a`. `make check` 1111 → **1137 passed**.

**Волна 2.** @uasaler исключён целиком — рулинг 07.08 назвал ЧАТ, рулинг 08.08 называет КАНАЛ.
Реестр 57 → 56, постовые 18 → 17, запуск 43 → **42**, что совпало с ожиданием оператора до
цифры. Снятие оставило строку по соглашению znizhki-ua: молча укоротившийся файл неотличим от
того, где канала не было. Снятие источника **не отзывает строки** — его 30 постов остались в
сторе, поэтому итоги сбора теперь несут базис: как собрано 2 132 / из них 1 830 на 38
гейтованных каналах запуска, 302 на 12 исключённых, 0 на watch.

**`replaced` у реверсированного рулинга.** Утренний KEPT для @discountua1 был затёрт на месте —
запись читалась так, будто реверса не было, и сказать обратное мог только `git show`. Старый
текст восстановлен из истории самой записи (`2970b71`) и **сверен равенством строк**, не глазами.
Прогон N и N+1 совпадают до `applied_at`; сид имеет собственную идемпотентность, потому что живая
ветка на уже случившемся реверсе не срабатывает.

Попутно: строка плана печатала `launch 43` на прогоне, давший 42 — снятый источник считался
«оригиналом»; числа канона были правы, врал скрипт. И у `remove_sources` **не было ни одного
теста** — у функции, чей черновик срезал бы taxonomy; закрыто двумя, один снимает последний
источник.

**Дозаявка №3 подключена.** 11 из 16 городских лент несут группу, поэтому бакет `posts` дал бы
11 FLAG на «привязана группа» — это ожидаемость спорит с рулингом, который их и разместил
(posts-only потому, что **вступление** отложено до фильтра тредов 5c2, а не потому, что групп
нет). Введён бакет `city`: группа измеряется и записывается, но не превращается в вердикт;
негативный контроль — та же запись в `posts` обязана флаг поднять. Список 16 не набран руками:
тест разбирает прозаический перечень канона. Добавлен `--only` — `get_entity` вызывается до
пофазных проверок, поэтому проход резолвил все 68 каналов; сужает **цикл, не запись**. Вся
последовательность после стены стоит ≈**63 резолва** против ~162.

**`audience` — третье измерение реестра.** Восемь значений, закрытый список, заполнено для всех
**56**: retail_official 5 · supermarket_deals 4 · cooking_recipes 13 · mothers_kids 9 ·
baby_food 7 · health_fitness 17 · food_quality_gov 1 · regional 0 (до прохождения гейта).
Ключ — **хендл, не id**: `@VARUS_channel` → `varus`, `@silposilpo` → `silpo`; таблица по id
выглядела бы полной и выпустила бы три источника с `null`. `update_sources` умел только
переписывать существующую строку — теперь вставляет после последней отступной строки блока, а не
в конец, где `_blocks` держит комментарии об удалении. По слову оператора 16 региональных
выписаны построчно с названиями из скана: тест сверяет каждое с
`results/discovery_5c1_poltava.json`, проверен подменой одного названия.

Предупреждено заранее: @LHVC_info (0.2 поста/нед., последний 20.07) почти наверняка даст FLAG по
существу, и `make check` покраснеет между шагами 2 и 3 — это работающий STOP, а не поломка.

Связано: [[2026-08-06]], [[5b2-batch-measurement]].

- 10:37: session ended (auto)
- 12:04: session ended (auto)
- 12:10: session ended (auto)
- 12:46: session ended (auto)
- 12:55: session ended (auto)
- 13:19: session ended (auto)
- 13:33: session ended (auto)
- 13:46: session ended (auto)
- 13:48: session ended (auto)
- 14:03: session ended (auto)
- 14:18: session ended (auto)
- 15:49: session ended (auto)
- 15:59: session ended (auto)
- 16:04: session ended (auto)
- 16:25: session ended (auto)
- 16:36: session ended (auto)
- 16:47: session ended (auto)
- 16:47: session ended (auto)
- 16:50: session ended (auto)
- 16:51: session ended (auto)
- 16:55: session ended (auto)
- 18:55: session ended (auto)
- 19:11: session ended (auto)
- 19:14: session ended (auto)
- 19:36: session ended (auto)
- 19:51: session ended (auto)
- 20:23: session ended (auto)
