---
type: daily_log
date: 2026-09-13
tags: [daily]
---

# 2026-09-13

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->
**s55–s58** (one unbroken run, the defect the team lead named in ruling (ccc)): «insight-2» ·
«insight-1» · «data-shape» · «front-2» delivered with no acceptance between them; the last died
mid-ritual with PROGRESS uncommitted. Three accepted, «front-2» left open.

**s59 «front-2-close»** ($0). Closed the standing item by its own checks rather than by its own
report. Every figure the dead session typed into PROGRESS was re-read out of
`results/front_data.json` before it was allowed to stand — twelve of them, all true; one cited the
wrong field (`sov.by_sample.payable` for what is `sov.sample.rows/of` = 3 of 5 075) and is corrected.

A read-only adversarial pass over `1a3e0e5` (five lenses, ten findings, each put to a skeptic; four
refuted) found **two figures on the screen that no file holds**: T5's «73.1 %» was 106 ÷ 145 divided
in the browser under a provenance line naming a block with no share in it, and T8's «рішення
[object Object]» stringified a ten-key block. Both fixed with one test each, both shown RED by their
own mutation. The third survivor — no error boundary, so a `must()` inside a tab's render unmounts
the root instead of drawing §5's red panel — is PRE-EXISTING architecture and is named, not built.

Carried: `make check` on the final tree, vitest 30/30, producer byte-identical (`dcef7ec5…`), the
sealed `promo_screen_data.json` unmoved (`e45860c6…`), $0. Next: «weekly-home».

### s60 «weekly-home» (20:0x–21:3x, $0) — both clauses, seven commits
The weekly dataset moved into `results/weekly/`, which git carries: ten weeks W27–W36, 1 301 rows,
one commit by path, `cmp`-identical to the accepted set. The lesson of the day came out of that
move — **the accepted combined digest `72d9d46e…` was `shasum -a 256 <paths> | shasum -a 256`, so it
hashes the FILENAMES and a lawful relocation breaks it by construction** (`b636e842…`), while the
content-only `cat positions_*.jsonl | shasum -a 256` read `d4fb6a44…` out of both homes.

Clause 2 moved T4's «Δ до свого» out of the browser into `command_center_derived`. Two halves of
that line were the app's own — the subtraction AND the reference, since the record lists two own
brands ordered by `brand_id`. The trap: our own brand's share is 0.0000, so **the Δ and the share
are the same number**, and every ordinary mutation (sign, operator, copy-through) prints identical
digits. What gave the test teeth was mutating the REFERENCE (`max(share)` → «expected -0.4 to be
close to +0») plus a grep for the deleted arithmetic in the tab.

Then five read-only lenses over the item's own diff, ten findings, three skeptics each: 30 votes,
all refuting, ZERO survivors — and re-reading the refuted list by hand still found three true ones
about my own text (a «reconstructible on a clean clone» claim that is 15 of 28 columns, a docstring
citing `aggregates.sov_block` which does not exist, an assertion comparing the block to itself).
A unanimous refutation is a reading like any other.

Checks: vitest 31/31, tsc clean, `make front` + `make tick` green, sealed `e45860c6…` unmoved,
`front_data.json` `dcef7ec5…` → `742013c1…` disclosed, `make check` 4344/2 on `d262d02`, $0.
Next: «region-collect» (the one authorized registry move).

## Sessions
- 10:39: session ended (auto)
- 11:41: session ended (auto)
- 13:15: session ended (auto)
- 14:38: session ended (auto)
- 14:43: session ended (auto)
- 17:00: session ended (auto)
- 17:40: session ended (auto)
- 17:43: session ended (auto)
- 18:06: session ended (auto)
- 19:08: session ended (auto)
- 21:30: session ended (auto)
