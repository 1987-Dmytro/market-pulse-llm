---
type: daily_log
date: 2026-08-17
tags: [daily]
---

# 2026-08-17

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->

## Sessions
- 11:17: session ended (auto)
- 11:42: session ended (auto)
- 14:19: session ended (auto)

## Checkpoint 14:41

**Контракт `reader-v5-prep` ИСПОЛНЕН за $0.00.** Отчёт `docs/reports/reader-v5-prep.md`, девиации
**Dv456–Dv468**, десять коммитов `974fe89…819960b`, **ни одного красного** (проверено чекаутом по
одному в throwaway-worktree с примонтированным стором). `make check` **2 685 → 2 777 / 2 skipped**.
Ничего платного не создано: `pod list -a` `[]`, `serverless list` `[]`, том — позитивный контроль.

**Шаг 0.5 — долги принятой фазы закрыты.** Шаг `reader-v4` **ЗАКРЫТ на $0.236118** settled (поды;
serverless $0.0000; $0.391674 того же обхода — капёж всегда-включённого тома, не принадлежит шагу).
Вердикт перескорен `7f5c96c8…` → `79c6c796…`, и сравнение по всем top-level ключам показало: двинулся
ТОЛЬКО `5_time_and_cost`, бары 1–4 байт-в-байт. **reader-v4 закрывается на 3 бара из 5.** Два ADR —
[[reader-v3-serverless-close-and-the-pod-ruling]] и [[reader-v4-closed-and-sitting-17-08]] — плюс
строки INDEX; гигиена runbook'а (восемь команд с `python3.11` + датированный блок правок), ни один
файл в `docs/reports/` не тронут.

**Инструмент v5 в дереве.** Промпт `reader_thread_gm4_v5` (`c529d279…`) — семь `_swap` над v3,
v4-текста нет и не было (номер идёт за контрактом, что его регистрирует). Все примеры синтетические:
доказано на 129 файлах / 43.9 млн символов с НЕГАТИВНЫМ контролем. Транспорт — `market_pulse.reader_v5`
(стоп на первом сбалансированном объекте, перепись эха, склейка чанков) + подовый раннер + Mac-драйвер
с APPEND-записью ворот + скорер v5. Регистрация `results/prereg_reader_probe_v5.json`
(`48b6cbde119ec3b1…`), сборка байт-в-байт, 25 тестов держат её утверждения, STOP-RULE программы
пре-зарегистрирован.

**Три вещи, которых контракт знать не мог:**

1. **Dv460 — «каждая шестая строка не написана» это НЕВЕРНОЕ ЧТЕНИЕ отчёта v4.** 18 «пропавших» id
   отвечены в `noise`, ещё 3 — в обоих списках; `per_comment ∪ noise` покрывает **111 из 111**,
   absent = 0, extra = 0. Пара 6 поэтому написана над ОБОИМИ списками (иначе бар отказал бы читателю,
   который слушается собственного правила промпта), а перепись считает три состояния.
   Это же перецифровывает достижимость бара 4: 7 согласны + 4 расхождения + 3 absent = 14, порог 0.80
   требует 12 — **v5 обязан взять минимум 5 из 7 несогласных строк**. Из трёх absent две (47899, 47902)
   лечит ТРАНСПОРТ (их тред отказал парсом), одну (578951) — блок атрибуции.
2. **Dv461 — потолок вывода поднят 2 000 → 4 000 токенов, обе ноги.** Бар m2 при чанке ≤16 недостижим
   арифметически: пессимистичный угол 16-строчного чанка — 2 491 токен, 7 из 26 юнитов прогона выше
   2 000, а самый длинный ответ v4 — 1 946 токенов, **97.3% старого потолка**. Модель вывода снята МНК
   по 14 тредам v4: `383.9 + 90.83 × строк`, макс. остаток +653.8.
3. **Dv457 — гард на поде отказывал пакету, которого никто не трогал.** `check_instrument` сравнивал
   ВЕСЬ набор промпт-sha пода с зарегистрированным, а собственный `prompt_rule` записи v4 говорит
   обратное. Сужен до задач, которые пинит пак; sha модуля оставлен свидетелем всего чекаута.

**Ревью готового контракта нашло ещё две** (обе починены до заморозки записи): **Dv467** — у m1 был
тот же дефект достижимости, что у m2 (id в обоих списках гасил бар, который меряет чанкинг);
**Dv468** — `--gate` теперь читает пак, это новая зависимость на пути kill-правила, названа отказом и
прогнана через `main()`.

**Деньги.** Линия цикла-2: потрачено **$0.8533 из $20.00**, свободно **$19.1467** (чтение 14:0x UTC).
Кап v5 — **$0.45**: 2 189.2 с при $0.74/ч (пример, цена читается В ДЕНЬ), usable 2 129.2 с, проекция
чтения 1 454.0 с (A 1 134.0 + B 320.0), affordability-дедлайн **675.2 с** create-elapsed связывает
РАНЬШЕ двенадцати минут.

**Дальше:** приёмка `reader-v5-prep` тимлидом → контракт `reader-v5-run` (под, кап $0.45).
Вчерашний день — [[2026-08-16]]; рулинги сидения — [[reader-v4-closed-and-sitting-17-08]].
- 14:45: session ended (auto)

## Checkpoint 15:35 — `reader-v5-run`: под прожил 727.9 с и не ответил ни разу

**Контракт `docs/PROMPT-reader-v5-run.md` исполнен. Прогон УБИТ на бут-воротах, ноль строк.**
Отчёт — `docs/reports/reader-v5-run.md`, девиации **Dv469–Dv473**.

**Шаг 0.** Хвост двумя коммитами по путям (`b9eeeca` vault, `9b32475` team-lead дословно). Дальше —
**baseline оказался НЕ зелёным (Dv469)**: 9 красных в `tests/test_volume_calc_5c1.py`, все от
`knowledge/hot.md: '~$0.24/day' is not in the file` — вчерашнее курирование hot.md выронило два
литерала, которые `scripts/volume_calc_5c1.py` грепает как ЦЕНОВЫЕ ВХОДЫ, вместе с заметкой,
которая про это предупреждала. Восстановлено (`b47e138`). Затем единственная правка прозы внутри
ещё не замороженной регистрации: `bars.5_time_and_cost.reported_not_gating.finish_reason` называл
потолок 2 000 внутри инструмента с потолком 4 000. Красный-до-зелёного показан, запись ПЕРЕСОБРАНА
(`48b6cbde…` → `8294673c…`), гард поставлен на ВСЮ запись (`66dc418`).

**До пода, за $0.** Recovery-clause контракта не имел механизма (**Dv470**) — раннер переспросил бы
все 26 юнитов и дописал дубли, которые считает проекция; починено и проверено тремя тестами, каждый
падает на прежнем раннере (`4cf51c6`). Гейт решён НАЗАД по собранному паку (**Dv471**): 43 платных
комментария ноги B попали в знаменатель второй ноги, второй юнит несёт 2 комментария → порог
**46.3 с** среднего по двум тредам, а ожидание 42.60 с — **запас 9%**. Записано, не исправлено:
регистрация замёрзла на шаге 0.

**Под.** `ivw5z93im4hfnz`, RTX 4090, RO, `costPerHr` **0.74** — ровно пример реестра. `ssh info`
отвечал `pod not ready` **все ~150 опросов за 12 минут**, при `pod list -a` = RUNNING. Ни scp, ни
клона, ни рукопожатия, ни загрузки модели. Ворота: WAIT 4.7 с → WAIT 346.6 с → WAIT 559.3 с →
**KILL 721.5 с** (`seconds_left −46.3`), все четыре снимка ДОПИСАНЫ в `results/reader_v5_run.json` —
именно тот прогон, ради которого правило APPEND и заводили. `pod delete` → `{"deleted": true}`,
три листинга сошлись, том на месте как положительный контроль. **Dv472.**

**Деньги.** Шаг **$0.1406** (дельта баланса, нижняя граница) из $0.45; метр пода 727.9 с × $0.74/ч =
**$0.1496**. `--close` ОТКАЗАН: «no billing rows yet» — названный долг с командой. Цикл-2:
**$1.0133 из $20.00**, свободно **$18.9867**.

**Recovery-clause посчитан и ОТКАЗЫВАЕТ**: остатка капа хватает на 1 445.2 с usable по самому
мягкому чтению (по метру — 1 401.4) против проекции чтения 1 454.0 с. Не хватает 8.8 с и 52.6 с
соответственно — и это ДО единой секунды провижена и бута. STOP, и это говорит гейт.

**Clause об ОДНОЙ попытке сработал; STOP-RULE ПРОГРАММЫ — НЕТ**: его условие «run COMPLETES», а
прогон не дошёл и ни один бар не посчитан. Вопрос «закрывать ли промпт-инженерию» ровно так же
открыт, как был до пода. Инструмент v5 целиком в дереве, оплачен $0 и на GPU не проверен ни секунды.

Предыдущий чекпойнт — [[2026-08-17]] выше; вчера — [[2026-08-16]].
