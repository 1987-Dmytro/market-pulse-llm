---
type: daily_log
date: 2026-08-17
tags: [daily]
---

# 2026-08-17

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->

## Sessions
- 11:17: session ended (auto)
- 11:42: session ended (auto)
- 14:19: session ended (auto)

## Checkpoint 14:41

**Контракт `reader-v5-prep` ИСПОЛНЕН за $0.00.** Отчёт `docs/reports/reader-v5-prep.md`, девиации
**Dv456–Dv468**, десять коммитов `974fe89…819960b`, **ни одного красного** (проверено чекаутом по
одному в throwaway-worktree с примонтированным стором). `make check` **2 685 → 2 777 / 2 skipped**.
Ничего платного не создано: `pod list -a` `[]`, `serverless list` `[]`, том — позитивный контроль.

**Шаг 0.5 — долги принятой фазы закрыты.** Шаг `reader-v4` **ЗАКРЫТ на $0.236118** settled (поды;
serverless $0.0000; $0.391674 того же обхода — капёж всегда-включённого тома, не принадлежит шагу).
Вердикт перескорен `7f5c96c8…` → `79c6c796…`, и сравнение по всем top-level ключам показало: двинулся
ТОЛЬКО `5_time_and_cost`, бары 1–4 байт-в-байт. **reader-v4 закрывается на 3 бара из 5.** Два ADR —
[[reader-v3-serverless-close-and-the-pod-ruling]] и [[reader-v4-closed-and-sitting-17-08]] — плюс
строки INDEX; гигиена runbook'а (восемь команд с `python3.11` + датированный блок правок), ни один
файл в `docs/reports/` не тронут.

**Инструмент v5 в дереве.** Промпт `reader_thread_gm4_v5` (`c529d279…`) — семь `_swap` над v3,
v4-текста нет и не было (номер идёт за контрактом, что его регистрирует). Все примеры синтетические:
доказано на 129 файлах / 43.9 млн символов с НЕГАТИВНЫМ контролем. Транспорт — `market_pulse.reader_v5`
(стоп на первом сбалансированном объекте, перепись эха, склейка чанков) + подовый раннер + Mac-драйвер
с APPEND-записью ворот + скорер v5. Регистрация `results/prereg_reader_probe_v5.json`
(`48b6cbde119ec3b1…`), сборка байт-в-байт, 25 тестов держат её утверждения, STOP-RULE программы
пре-зарегистрирован.

**Три вещи, которых контракт знать не мог:**

1. **Dv460 — «каждая шестая строка не написана» это НЕВЕРНОЕ ЧТЕНИЕ отчёта v4.** 18 «пропавших» id
   отвечены в `noise`, ещё 3 — в обоих списках; `per_comment ∪ noise` покрывает **111 из 111**,
   absent = 0, extra = 0. Пара 6 поэтому написана над ОБОИМИ списками (иначе бар отказал бы читателю,
   который слушается собственного правила промпта), а перепись считает три состояния.
   Это же перецифровывает достижимость бара 4: 7 согласны + 4 расхождения + 3 absent = 14, порог 0.80
   требует 12 — **v5 обязан взять минимум 5 из 7 несогласных строк**. Из трёх absent две (47899, 47902)
   лечит ТРАНСПОРТ (их тред отказал парсом), одну (578951) — блок атрибуции.
2. **Dv461 — потолок вывода поднят 2 000 → 4 000 токенов, обе ноги.** Бар m2 при чанке ≤16 недостижим
   арифметически: пессимистичный угол 16-строчного чанка — 2 491 токен, 7 из 26 юнитов прогона выше
   2 000, а самый длинный ответ v4 — 1 946 токенов, **97.3% старого потолка**. Модель вывода снята МНК
   по 14 тредам v4: `383.9 + 90.83 × строк`, макс. остаток +653.8.
3. **Dv457 — гард на поде отказывал пакету, которого никто не трогал.** `check_instrument` сравнивал
   ВЕСЬ набор промпт-sha пода с зарегистрированным, а собственный `prompt_rule` записи v4 говорит
   обратное. Сужен до задач, которые пинит пак; sha модуля оставлен свидетелем всего чекаута.

**Ревью готового контракта нашло ещё две** (обе починены до заморозки записи): **Dv467** — у m1 был
тот же дефект достижимости, что у m2 (id в обоих списках гасил бар, который меряет чанкинг);
**Dv468** — `--gate` теперь читает пак, это новая зависимость на пути kill-правила, названа отказом и
прогнана через `main()`.

**Деньги.** Линия цикла-2: потрачено **$0.8533 из $20.00**, свободно **$19.1467** (чтение 14:0x UTC).
Кап v5 — **$0.45**: 2 189.2 с при $0.74/ч (пример, цена читается В ДЕНЬ), usable 2 129.2 с, проекция
чтения 1 454.0 с (A 1 134.0 + B 320.0), affordability-дедлайн **675.2 с** create-elapsed связывает
РАНЬШЕ двенадцати минут.

**Дальше:** приёмка `reader-v5-prep` тимлидом → контракт `reader-v5-run` (под, кап $0.45).
Вчерашний день — [[2026-08-16]]; рулинги сидения — [[reader-v4-closed-and-sitting-17-08]].
- 14:45: session ended (auto)

## Checkpoint 15:35 — `reader-v5-run`: под прожил 727.9 с и не ответил ни разу

**Контракт `docs/PROMPT-reader-v5-run.md` исполнен. Прогон УБИТ на бут-воротах, ноль строк.**
Отчёт — `docs/reports/reader-v5-run.md`, девиации **Dv469–Dv473**.

**Шаг 0.** Хвост двумя коммитами по путям (`b9eeeca` vault, `9b32475` team-lead дословно). Дальше —
**baseline оказался НЕ зелёным (Dv469)**: 9 красных в `tests/test_volume_calc_5c1.py`, все от
`knowledge/hot.md: '~$0.24/day' is not in the file` — вчерашнее курирование hot.md выронило два
литерала, которые `scripts/volume_calc_5c1.py` грепает как ЦЕНОВЫЕ ВХОДЫ, вместе с заметкой,
которая про это предупреждала. Восстановлено (`b47e138`). Затем единственная правка прозы внутри
ещё не замороженной регистрации: `bars.5_time_and_cost.reported_not_gating.finish_reason` называл
потолок 2 000 внутри инструмента с потолком 4 000. Красный-до-зелёного показан, запись ПЕРЕСОБРАНА
(`48b6cbde…` → `8294673c…`), гард поставлен на ВСЮ запись (`66dc418`).

**До пода, за $0.** Recovery-clause контракта не имел механизма (**Dv470**) — раннер переспросил бы
все 26 юнитов и дописал дубли, которые считает проекция; починено и проверено тремя тестами, каждый
падает на прежнем раннере (`4cf51c6`). Гейт решён НАЗАД по собранному паку (**Dv471**): 43 платных
комментария ноги B попали в знаменатель второй ноги, второй юнит несёт 2 комментария → порог
**46.3 с** среднего по двум тредам, а ожидание 42.60 с — **запас 9%**. Записано, не исправлено:
регистрация замёрзла на шаге 0.

**Под.** `ivw5z93im4hfnz`, RTX 4090, RO, `costPerHr` **0.74** — ровно пример реестра. `ssh info`
отвечал `pod not ready` **все ~150 опросов за 12 минут**, при `pod list -a` = RUNNING. Ни scp, ни
клона, ни рукопожатия, ни загрузки модели. Ворота: WAIT 4.7 с → WAIT 346.6 с → WAIT 559.3 с →
**KILL 721.5 с** (`seconds_left −46.3`), все четыре снимка ДОПИСАНЫ в `results/reader_v5_run.json` —
именно тот прогон, ради которого правило APPEND и заводили. `pod delete` → `{"deleted": true}`,
три листинга сошлись, том на месте как положительный контроль. **Dv472.**

**Деньги.** Шаг **$0.1406** (дельта баланса, нижняя граница) из $0.45; метр пода 727.9 с × $0.74/ч =
**$0.1496**. `--close` ОТКАЗАН: «no billing rows yet» — названный долг с командой. Цикл-2:
**$1.0133 из $20.00**, свободно **$18.9867**.

**Recovery-clause посчитан и ОТКАЗЫВАЕТ**: остатка капа хватает на 1 445.2 с usable по самому
мягкому чтению (по метру — 1 401.4) против проекции чтения 1 454.0 с. Не хватает 8.8 с и 52.6 с
соответственно — и это ДО единой секунды провижена и бута. STOP, и это говорит гейт.

**Clause об ОДНОЙ попытке сработал; STOP-RULE ПРОГРАММЫ — НЕТ**: его условие «run COMPLETES», а
прогон не дошёл и ни один бар не посчитан. Вопрос «закрывать ли промпт-инженерию» ровно так же
открыт, как был до пода. Инструмент v5 целиком в дереве, оплачен $0 и на GPU не проверен ни секунды.

Предыдущий чекпойнт — [[2026-08-17]] выше; вчера — [[2026-08-16]].
- 16:13: session ended (auto)

## Checkpoint 17:04 — прогон принят как инфраструктурный минус, и два зазора в моей же починке

**Со времени чекпойнта 15:35.** Восемь коммитов, `b9eeeca` → `4f14b60`, дерево чистое,
`make check` **2 782 passed / 2 skipped**, ruff чист.

**Dv474 — я сам сломал шиппнутую запись и поймал это суитой.** Проверяя починку Dv469, я ЗАПУСТИЛ
`scripts/volume_calc_5c1.py`, а его `main()` пишет `results/volume_calc_5c1.json`: сдвинул
`generated_at` на десять дней вперёд (окно обхода баланса расширилось, внутрь попало пополнение,
граница ушла в −11.14/день) и **стёр блок `executed`** — рулинг оператора от 2026-08-07 с id,
размером и датацентром удалённого тома, 8 вставок против 29 удалений. Послепрогонный `make check`
дал одну красную (`test_the_idle_rate_is_reported_as_a_bound_not_as_an_estimate`,
`-11.1423 > 0.24`), откат `git checkout --`, 10 тестов зелены, `executed` на месте. Правильная
проверка — `calc.quoted(...)` / `calc.inputs()`, читатель, а не `main()`.
Урок в память: [[the-producer-is-not-the-verifier]].

**Доказательство вместо утверждения по clause «никаких правок замороженного, пока под жив».** Под
жил 15:18:13–15:30:19 CEST; последний коммит по любому файлу из `frozen_when_the_pod_exists` —
`66dc418` в **15:05:41**, последний коммит вообще перед create — `0ec2da6` в **15:17:38** (за 35 с),
следующий — `e23c851` в **15:42:45**. Семь `git log -1` в §6 отчёта.

**Память.** Три урока: [[a-negative-pre-generation-budget-is-a-forecast]] (запас на провижен надо
ПОКУПАТЬ: −44.8 с было предсказанием, а не сноской), [[a-new-leg-joins-the-gates-denominator]]
(правило гейта не двигалось — двигался его нож; решать гейт НАЗАД по собранному паку),
[[the-producer-is-not-the-verifier]].

**Тимлид ПРИНЯЛ прогон** как инфраструктурный отрицательный результат и написал
`docs/PROMPT-reader-v5b.md` (его файлы — читаю и коммичу дословно, не правлю). Из его верификации:
свой `make check` 2 782/2, **биллинг-обход ПОСТНУЛСЯ — $0.1501 pods-only, `--close` готов** (мой
названный долг закрываем), облако пусто, том жив, четыре гейт-снапшота append'ом, Dv474-реверт
держится. Рулинги сидения: кап v5b **$0.50**, транспортный гейт-0 **«ssh жив к 180 с или KILL»**,
до двух пересозданий, третий мёртвый под = STOP, и **пак по УБЫВАНИЮ payable** — от этого нож юнита 2
исчезает, а закон гейта не тронут.

**Два зазора в моей же починке, названные свежим субагентом тимлида и проверенные мной по коду:**
(1) `reader_v5_pod_runner.already_answered` парсит `json.loads(line)` НАГОЛО — я закрыл рваную
строку в `raw_rows` и оставил её в родном сиблинге того же файла, причём на пути, который срабатывает
только при восстановлении; (2) `projection()` считает `len(rows)`, а не уникальные id — мой
собственный докстринг говорит «the Mac's projection counts rows». Оба — red-first в шаге 0 v5b.

Предыдущий чекпойнт — 15:35 выше; вчера — [[2026-08-16]].
- 17:09: session ended (auto)
- 19:01: session ended (auto)

## Checkpoint 19:03

**`reader-v5b` ИСПОЛНЕН ЦЕЛИКОМ. Прогон СОСТОЯЛСЯ — и ответ отрицательный: бары 1 и 4 оба
провалены, программное stop-правило СРАБОТАЛО.** Отчёт — `docs/reports/reader-v5b.md`, девиации
**Dv475–Dv481**. Контракт — `docs/PROMPT-reader-v5b.md` (тимлидовский, закоммичен дословно).

### Шаг 0 и шаг 0.5 ($0)

- Хвост закоммичен по путям: `c8a2271` (`knowledge/**`), `4f30f57` (`docs/STATUS.md` +
  `docs/PROMPT-reader-v5b.md` дословно). Baseline `make check` **2 782 / 2 skipped** — зелёный,
  класс Dv469 не повторился (оба ценовых литерала в hot.md на месте, проверял ЧИТАТЕЛЕМ).
- **Шаг `reader-v5` ЗАКРЫТ на $0.150095** (только поды), `424bab0`. Обход постнулся.
- **Оба red-first фикса, `c7e2395`:** `already_answered` получил терпимый ридер (рваная последняя
  строка → юнит НЕ отвечен и переспрашивается; фрагмент удаляется из ФАЙЛА байтовым префиксом,
  иначе `--out` в append-режиме склеит следующий ответ с обрывком) и `unit_ids` считает
  УНИКАЛЬНЫЕ id, отказывая по имени на дубликате и на чужом id. Семь тестов, все семь красные
  против предыдущего коммита.

### D1 — регистрация, и измерение, которое отозвало рулинг

- `results/prereg_reader_probe_v5b.json`: продюсер **ВЫЗЫВАЕТ** продюсер v5 и отказывается, если тот
  не пересобирает свою замороженную запись байт в байт. `instruments`, `attempt`, `completeness`,
  `programme_stop_rule`, `scoring_rules` — объекты замороженной записи; бары 1–4 и четыре
  механических бара плеча B object-equal; весь диф зафиксирован тестом.
- **Открытие:** решённый назад его же функцией, зарегистрированный full-pass гейт под ПРАВИЛОМ
  ОПЕРАТОРА (пак по убыванию payable) даёт **STOP после ПЕРВОГО юнита**, −23.2 с/юнит, при любом
  бусте включая нулевой. Четыре порядка через тот же гейт: порядок v5 и «payable в секунду» проходят
  все 26, убывающий STOP на юните 1, возрастающий на юните 2. Подъём капа сам по себе превратил
  9% запаса Dv471 в 23%.
- **Оператор отозвал правило порядка** (кап $0.50 и гейт-0 остаются). Пересборка за $0:
  `f7e2263` + `1942a74`. Таблица отозванного порядка ОСТАЛАСЬ в регистрации как доказательство
  отзыва — `population.leg_a.order_withdrawn` с датами и причинами.

### D2 — прогон

Под `r7702vbsrxhz0j`, RTX 4090, EU-RO-1 SECURE, `costPerHr` 0.74 = зарегистрированный пример.
Создан 16:22:10Z, удалён 16:47:24Z, **1514.0 с = $0.311211** из капа $0.50. Один сегмент.

- **Гейт-0 ответил на 24-й секунде** (v5: ни разу за 727.9 с). Дед-ман не понадобился.
- 23 снимка гейтов, все дописаны: `open` WAIT, `gate0` GO, **21 `full_pass` — все GO**. Самый узкий
  момент **+3.39 с/юнит на юните 2** — ровно там, где регистрация его назвала. Финал: запас 880.7 с.
- **Контрфактика ИЗМЕРЕНА:** `@matusi_ukr:22272`, который отозванный порядок ставил первым и который
  таблица оценила в 105.3 с против 82.1 разрешённых, прочитался юнитом 17 за **103.8 с** — прогноз
  завышен на 1.4%. Плечо A: 1035.4 с против 907.6 у v4, рост **1.141** при зарегистрированном 1.127.

### D3 — вердикт

`results/reader_v5b_verdict.json` (`8c975558b8da31cf…`):

| бар | итог | число |
|---|---|---|
| `1_flagships` | **FAIL** | 2 из 5 |
| `2_entity_cases` | PASS | 4 из 4 |
| `3_noise` | PASS | — |
| `4_per_comment_agreement` | **FAIL** | **0.4286** (6/14; по отвеченным строкам 6/10 = 0.600) |
| `5_time_and_cost` | **OPEN** | обход не постнулся |
| плечо B m1–m4 | **ВСЕ PASS** | чанкинг заработал с первого раза |

**Бары 1 и 4 оба не взяты на ЗАВЕРШЁННОМ прогоне → prompt-engineering закрывается, дальше
архитектурное сидение, не v6.** Правило предзарегистрировано в v5 и переригистрировано дословно.

Помимо баров: эхо-обязанность выполнена **117 из 117, ноль absent**; 26 из 26 ответов сбалансированы,
стоп срезал 62 символа на 19 юнитах; потолок 4000 токенов НИ РАЗУ не сработал (максимум 2056 = 51%);
**4 отказа из 23 — ровно счёт v4**, но на других тредах. Плечо B: 175.1 с против прогноза 320.0.

**Чего сидению стоит знать:** три из четырёх отказов — транспортный стоп взял сбалансированный
объект, который не был ответом; четвёртый (`malformed JSON`) унёс 4 строки из знаменателя бара 4.
По отвеченным строкам бар 4 = 0.600 против 0.636 у v4 — инструмент не сдвинул бар 4 ни в одном
чтении, но часть провала оплачена стопом, который купило сидение, а не промптом.

### Долг и состояние

- **Бар 5 OPEN:** гвард отказал в закрытии дословно («no billing rows yet»), реестр не тронут.
  Счётчик $0.311211, дельта баланса $0.3126 — оба под капом. Закрытие — названный долг.
- `make check` **2828 / 2 skipped**, ruff чист (337 файлов), дерево чистое.
- Заморозка доказана временем коммитов: под жил 18:22:10–18:47:24 CEST, **в этом окне не было ни
  одного коммита**; последний до — `a25ab76` за 29 с до create.

Уроки в native memory: [[a-ruling-aimed-at-one-leg-lands-on-the-other]] (обновлён измерением),
[[a-self-pinning-producer-cannot-grow-a-parameter]], [[a-structural-stop-accepts-a-structural-non-answer]].
Предыдущий чекпойнт — 17:04 выше; вчера — [[2026-08-16]].
- 19:07: session ended (auto)
