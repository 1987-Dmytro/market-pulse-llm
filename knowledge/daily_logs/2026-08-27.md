---
type: daily_log
date: 2026-08-27
tags: [daily]
---

# 2026-08-27

## Summary

**Контракт C1 `retail-census` (SPEC v2 §3, рулинг (х)) — исполнен, $0.**

- **Шаг 0.** Файлы тимлида закоммичены по пути (`370f016`). Том `mp-lora-c` (`soymlju8q0`,
  CA-MTL-3) удалён (`d48fa96`); `mp-srv2` в обоих листингах как положительный контроль. Перед
  удалением проверил по рунбуку, а не по прозе: `runbook_think_zero_shot_d2.md` монтирует
  `qw4nwleanc` и читает `/workspace/hf`. Гард: остаток $3.1686 из $20.00, баланс $5.6783611613.
- **Ценз.** Две темы в `discover_channels.py` (`retail_chains` 140 запросов, `poltava_chats` 96);
  410 нерегистрированных хэндлов найдено, 200 измерено; реестровые 66 — по стору, без API.
  Отчёты: `docs/reports/retail-census.md`, `docs/reports/registry-revision-proposal.md`.
- **Стена.** FloodWait 85 352 с (23.7 ч) на кандидате 191 из 315; собранное сохранено, стена
  записана в `results/joins_5c1.jsonl`, повторный старт отказывает (негативный контроль пройден).
  Снимется 2026-08-28 10:21 UTC — досчёт одной командой, запись накопительная.

**Долг тимлиду:** переспека STATUS.md вымыла цитату рулинга (ф), которую
`write_think_zero_shot_prereg.py :: quoted()` грепает дословно → `test_think_zero_shot` красный с
шага 0. Цитируемым рулингам место в MACHINE-READ BLOCK. `make check`: 2 failed, 4099 passed.

**Числа, которые нашлись сами:** ветка `grn` из брифа сработала 0 раз по всему стору («грн» — 648,
`₴` — 50, `decimal` — 449); `has_media` как прокси листовки насыщен (медиана 0.99, 28 из 58 строк
= 1.00) и не различает; `@kop1chat` — открытый чат «Копійочки» на 22 сообщ./день, которого нет в
сборе. Отклонения Dv861–Dv871 — в `implementation-notes.md`.

## Sessions
- 00:12: session ended (auto)
- 12:26: session ended (auto)
- 12:56: session ended (auto)

## Checkpoint 15:15

Со времени записи выше — закрытие C1 и его проверка.

- **Стена и её протокол.** FloodWait 85 352 с пойман на 191/315. Дефект: ценз записал стену только
  в свою запись; `results/joins_5c1.jsonl` — то место, откуда все фазы читают «заперт ли аккаунт»
  (`collect_5c1.refuse_inside_flood_wait` — читатель, `entry_check.run_gate` — писатель того же
  ряда). Исправлено в обе стороны, негативный контроль прогнан: повторный старт отказывает и
  называет час. Урок дописан в [[rate_limit_on_the_shared_first_step]] — «свой курсор» это не тот
  файл, лимит принадлежит АККАУНТУ.
- **Три поправки чтения таблицы**, каждая числом: ветка `grn` = 0 попаданий по стору; `has_media`
  как прокси листовки насыщен (медиана 0.99); 23 строки — нижние границы, помечены `≥`.
- **Смок окупился до платы за остальное:** `@alesya_teperikova` пришёл `enter` без единого поста за
  28 дней — `build_verdict` грейдит СПОСОБНОСТЬ, а ценз спрашивает про ВХОД. Правило добавлено,
  `--regrade` перегрейдил уже измеренное без единого запроса к API.
- **Резолвы посчитаны, а не выведены:** telethon debug по смоку — ровно 10 `ResolveUsernameRequest`
  на 10 кандидатов, кэш сессии второй `get_entity` действительно поглощает.
- **Отчёт сверен с записью после последней правки** (`--report` → пустой `git diff`), по
  [[rehash_the_deliverable_after_the_suite]].
- `make check`: **2 failed, 4099 passed, 2 skipped** — те же два красных, новых нет.
- Потерян и переплачен один проход поиска: правил файл, пока фоновый процесс держал старый код;
  упало на сохранении после всех 236 запросов. Урок дописан в
  [[a_running_process_holds_its_old_code]] — у локального фонового джоба исходник тоже заморожен.

Коммиты: `370f016` `d48fa96` `d1e5423` `0318243` `acb70dd` `3c7904f` `9fa2ff4` `e11c9a9`.
- 15:20: session ended (auto)

## Checkpoint 16:40 — `harness-v2.1`, и блок `hot.md` ужат 148 → 40 строк

Гарды v2.1 поставлены файлами, не памятью: `scripts/hooks/refuse-sweeping-commands.sh`
(PreToolUse(Bash), отказ виден живьём), `permissions.deny` на все пути тимлида,
`.claude/commands/plan-phase.md` и `report.md`, `.claude/rules/reports-and-plans.md`.
Отчёт: `docs/reports/harness-v2.1.md`.

### Перенесено из курируемого блока `hot.md` (у каждого пункта есть дом; здесь — архив переноса)

**Долг тимлиду, как он был записан 27.08 днём (ФОРМУЛИРОВКА УСТАРЕЛА — см. отчёт
`harness-v2.1`):** «переспека STATUS.md вымыла цитату рулинга (ф), которую
`scripts/write_think_zero_shot_prereg.py :: quoted()` грепает дословно, — с шага 0
`tests/test_think_zero_shot.py::test_the_registration_rebuilds_except_where_step_0_moved_a_pin`
красный». Цитаты (ф) тимлид вернул в MACHINE-READ BLOCK, и `producer.build()` теперь проходит их
все три; тест всё равно красный — по ВТОРОЙ строке, вымытой тем же коммитом `370f016`: свидетель
`MOVED_BY_D2_STEP_0` `D1 (инструмент, $0)`. Он был в STATUS ровно на `09954df`.

**📏 Три инструментальные поправки, которые меняют чтение любой будущей таблицы источников.**
(1) Ветка `grn` из паттерна цены брифа сработала **0 раз** по всему стору («грн» 648 · `₴` 50 ·
`decimal` 449) — считать оба паттерна и ветки врозь. (2) `has_media` как прокси листовки
**насыщен**: медиана 0.99, у 28 из 58 каналов ровно 1.00 — различает не он, а доля цены (медиана
0.04). (3) Ставка канала, чьё окно упёрлось в потолок 1 200 сообщений, — **нижняя граница**
(~42.86/день), не замер; в отчёте помечена `≥`.

**🧭 Единица работы — пункт DoD, не регистрация с рунгами** (рулинг (у), полный текст в
`docs/reviews/2026-08-26-replan-and-process-diet.md`): одна фича на сессию, контракт **≤50 строк**,
отчёт **≤80**, без чекера «своих чисел», платный контракт — **четыре** рунга с ASK при перерасходе
≤20%, кап **2× оценки из реестра**.

**📐 Результат измерен 05.08, а README этого не знает.** `results/verdict_45h2.json`: тональность
**0.921 против 0.683**, сарказм **23 из 38**; README пишет «No measured numbers are published yet» —
это и есть фича A1.

**📊 Реестр физических констант** — `docs/STATUS.md` §«Реестр измерений» +
`results/measurements.jsonl` (создан D1, четыре BEFORE-ставки с источником, n и максимумом); полная
миграция — шаг 1d контракта `a1`. Ставка — свойство пода ([[a_rate_is_a_property_of_the_pod]]).

**⚠️ Две ставки реестра STATUS расходятся с замером по файлам ответов.** STATUS: «проход-2
**97 с/тред**», «v1/v2 **6.14**». Файлы: **23.760 / 135.232 макс** (n = 75) и **2.293 / 2.726**
(n = 200 каждая) — 97 переоценивает проход-2 в **4.1×**. Правка STATUS — за тимлидом.

**🧠 Thinking меняет форму ответа, а не только промпт.** `<|channel>`/`<channel|>` — special-токены,
`skip_special_tokens=True` СТИРАЕТ границу «работа/ответ», и её теряют трое читателей. Починено в
`9c29ae0` — [[the_marker_the_decoder_deletes]].

**🧭 У сдвинутой константы есть потребители, которых греп не находит.** VRAM, с/шаг и доллары
символ не читают — они им ДВИГАЮТСЯ. Цена молчания на `max_seq_len` 1 408 → 3 072: OOM за $0.76 и
KILL за $0.44. Перед платным шагом писать пять чисел из ближайшего ЗАМЕРА: пик памяти · с/шаг ·
секунды · $ ×1.0 и ×1.5 ([[a_shifted_constant_has_physical_consumers]]).

**✅ Закрыто, живёт в отчётах/ADR:** вся линия `lora-c` (итого **$1.123**) —
[[lora-c-the-line-trains-at-90-seconds-a-step]], `docs/reports/lora-c-run-r3.md`,
[[lora-c-the-volume-moves-to-ca-mtl-3]]; там же Dv813/Dv814 (гейт `gate_lora_b.measured_step`,
53.690 против 89.961) и бар 3 с атрибуцией прохода-1, которые обучением не лечатся.

**⛔ Было шесть красных (архив).** Диета STATUS 410 → 118 вымыла абзацы, которые пять запечатанных
продюсеров грепали дословно. Разбор — в git-истории `knowledge/hot.md` и в
`docs/reports/think-zero-shot-d1.md`; механизм тот же, что у свидетеля выше.

**⛔ Отчёт r3: чтение теперь есть, и оно опровергает предсказание.** «suite, closing» стояло
`NOT TAKEN` с предсказанием **3 999 / 2**. Step 0 контракта `think-zero-shot` снял то же чтение на
`8d40782`: **3 993 passed, 6 failed, 2 skipped**. Плейсхолдеры в отчёте r3 на месте, файл
модифицирован в дереве не исполнителем; вписывать — решение тимлида
([[gate_verdicts_need_an_artifact]]).

**⛔ `make fmt` обнуляет пины продюсеров — оба расформатированных файла запинены.**
`scripts/write_lora_c_prereg.py` → `results/prereg_lora_c.json::producer`;
`scripts/build_lora_c_marker_census.py` → `results/lora_c_marker_census_pack.json`. `make check`
гоняет `ruff check`, а не `ruff format --check`, и не видит ни того, ни другого. С 27.08 обе формы
(`make fmt`, `ruff format .`) отказывает хук — форматировать ТОЛЬКО по путям.

**⛔ Два долга инструмента, оба с домом.** Допуск гарда 7% недостижим на дешёвом шаге (n = 2, долг
в `money-anchors`; третья точка от r3 — 0.29%). И `preflight` слеп к `config_sha256`: у поля нет
соседа-пути, поэтому `config/qlora.yaml` не попадает в реестр пинов — проверять перечисленным
диффом записи, не preflight'ом.

**⚠️ `_to_delete/` в корне репо** — внутри один `git-index.lock` на 0 байт, появился вместе с
тимлидовой правкой. Не исполнительский файл; не тронут, назван.

**⛔ Инвариант рунга 5 — на блобах, не на каталоге кэша.** `du -sb /workspace/hf` сдвигается за
обычную загрузку (`refs/main` + нулевые маркеры `.no_exist`), блобов при этом пишется 0. Ремеди
исполнен в step 0.5 r3; на поде прочитано **12 блобов, 0 выросших**. Разбор —
`results/lora_c_migrate_r2_load_bytes.json` ([[an_invariant_on_the_container_not_the_payload]]).
- 16:34: session ended (auto)

### `MEMORY.md` — решение оператора, у которого не было дома (перенесено из `hot.md`, `9e7ae6e~1`:140-147)

> **🧹 `MEMORY.md` УПЁРСЯ В ОБА ПОТОЛКА, ВТОРОЙ НЕ ЗАКРЫТ.** Загрузчик
> (`context-census.py::loaded_memory`): trim → первые **200 строк** → отрез по **25 000 UTF-16 units**.
> После чекпоинта 14:40: **204 строки**, окно **186 / 24 933 units**, **18 указателей не доезжают**
> (файлы уроков целы). Четвёртая запись дня — [[the_marker_the_decoder_deletes]] — вставлена В НАЧАЛО
> `## Feedback`; торг: первым за окном теперь [[a_gate_command_is_a_write]]. Рычаг один —
> убрать ~15 СТРОК, это консолидация и отдельное решение оператора. Выживание проверять
> `loaded_memory`, **НЕ `head -c`** ([[the_wrong_unit_points_at_the_wrong_lever]]). Ценз буста
> **11.7 Ktok** при цели 10.7 — из них `MEMORY.md` 25 274 байта, больше половины.

Ценз буста после `harness-v2.1` — **9.3 Ktok** (цель 10.7), но потолки `MEMORY.md` этим не сдвинуты:
рычаг тот же, и он операторский. Курируемый блок `hot.md` держит короткую форму (два потолка +
инструмент), числа выше — здесь.

### Поправка к переносу 16:40

Перенос выше — не «verbatim»: часть блоков при переезде уплотнена. Полные редакции — в
`git show 9e7ae6e~1:knowledge/hot.md`, и у каждого блока есть свой дом (отчёты, ADR, файлы уроков),
названный в самом блоке.
- 16:50: session ended (auto)
- 16:55: session ended (auto)
- 17:02: session ended (auto)
