---
type: daily_log
date: 2026-07-26
tags: [daily]
---

# 2026-07-26

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->

## Checkpoint 18:24

`/init` on the existing `CLAUDE.md` — additive fold-in only, `Rules` / `Pitfalls` / `Second brain`
untouched, the `<!-- /init fold-in -->` placeholder consumed and removed. Added: **Stack & commands**
(`make check` / `make fmt` / single-test / registry CLI), **Code map** (`docs/SPEC.md` as the spine;
the reflective discovery in `tests/test_scorer.py` that forces every new public scorer function to
raise `NotImplementedError`; why `registry.py` is strict and `competitors.yaml` stays placeholder),
**Harness plumbing** (SessionStart/Stop hooks, generated regions, `/save` + `/close` are
operator-only). 36 → 74 lines, boot tax 1.5K → 2.0Ktok against the 9K target.

Deliberate: the live phase number stays out of `CLAUDE.md` (one home per fact → [[hot]]); the file
points at SPEC §7 instead, and the pipeline line points at SPEC §5 + `src/` rather than claiming
"Phase 2 has no code", which would rot. Kept the `/init`-mandated header prefix verbatim — scope is
not ours to trim.

Verified: `make check` green (6 passed), plus the two documented commands actually run
(`pytest tests/test_registry.py::test_duplicate_competitor_id_rejected`, registry CLI). Not
committed — operator's call.

## Sessions
- 18:16: session ended (auto)
- 18:23: session ended (auto)
- 18:25: session ended (auto)
