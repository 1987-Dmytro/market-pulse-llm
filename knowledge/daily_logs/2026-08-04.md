---
type: daily_log
date: 2026-08-04
tags: [daily]
---

# 2026-08-04

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->

## Sessions

## Checkpoint 12:47 — 4.5h precheck сдан: потолок пробит, и руки различаются не только объёмом

Прекчек-половина `PROMPT-4.5h` исполнена и отчитана; **$0**, ни одного запроса к модели, ни одного
пода, ни один freeze-файл не тронут. Ночная часть работы (шесть вопросов, четыре замера, числа и
пути) подробно записана в [[2026-08-03]] — сюда только итог и то, что нашлось **после** того, как
заметки фазы были написаны.

**Итог, одной строкой:** рука B проектируется в **5.44 ч** против потолка 5 ч амендмента 3.6 (и
5.21 ч на самом быстром наблюдённом шаге) — прогон не ужат, решение за тимлидом. Причина в том, что
пласт **действительно аддитивен**: 0 из 1 286 scoreable-строк несёт уже существующий id. «2 346» из
STATUS — другая величина (1600 + 746 до фильтра `unclear`); у тренера 2 195 scoreable / 2 171 после
carve, ровно как в `4c-arm-a/provenance.json`. Все 508 голд-строк требуют свежего прохода ($0.0869),
двудомность 54 растворилась (второй дом — файл, который тренеру запрещено открывать), утечки нет
(0 id / 0 тредов / 0 дословных), сравнений в корпусе нет (a∩b = 0 на 11 338 комментах).

**Найдено после написания заметок — два пункта, оба ушли в отдельные коммиты:**

1. **Руки различались бы не только объёмом.** `train_qlora.py:50` читает **v1**-файлы: в источниках
   руки A **0** строк с `service`, в пласте — **652**, а G1c судится тестом v4, который и есть
   таксономия v2. Правило отбора решалось бы экспозицией к таксономии и записалось бы как объём
   данных. `_tax2`-близнецы содержат те же 906 и 540 scoreable-строк — перенаправление `SOURCES` не
   двигает ни одного часа. Это четвёртое место, где `intents` сидит на v1, и единственное **не
   guard**: при ошибке ничего не откажет. Плюс: пласт-как-источник ломает парность carve — руки
   удержат разные 24 строки. Оба пункта — код 4.5h2, здесь ничего не менялось.
2. **`$18.0097` — это не баланс.** В `spend_phase4.json` два числа, и оба читаются как «сколько
   осталось»: баланс аккаунта `$28.0097` и остаток под кэпом $25 амендмента 3.4 (4) — `$18.0097`.
   Заметки назвали вторым первое; исправлено, оба поля теперь названы явно.

**Верификация:** `make check` 734 passed (на входе 689), `ruff format --check` чист, оба артефакта
прогнаны дважды и сверены `cmp`, шесть замороженных файлов на своих sha, пласт на `ea7fa2ab…`,
леджеры не тронуты. 7 коммитов, дерево чистое. 14 отклонений в `implementation-notes.md`.

**Принят в тот же день — блокеров не осталось.** Пока шёл чекпойнт, дерево вернулось с
**амендментом 3.9** и приёмкой: потолок руки 5 → **6.5 ч** (только для абляции 4.5h2, запас на
crash-resume); **обе руки читают `_tax2`-близнецов** — находка исполнителя принята дословно; **carve
жеребится ДО присоединения пласта**, парность как в Phase 4. 54 двудомных — опция (i), пул 971 → 917
новой версией рядом с pristine. Тест v4 = v3 → проход по 508 строкам (только `intents`) → 31 + 1
вердикта оператора **поверх**. Амендмент про (бренд+позицию) **отложен по числам**: замер №4 встаёт
бесплатным счётчиком в петлю Phase 5. Категория и позиция — **пост-слой, не дообучение**
(`docs/PRODUCT.md`, утверждён оператором). Конфаунд пойман до денег — ровно то, ради чего прекчек и
существует.

В очереди: `docs/PROMPT-4.5h2.md` (заморозка v4 → якорь → планки и правило кодом → ретрейн A/B) и
новый `docs/PRODUCT.md`. Не коммичу — коммиты по команде оператора. Связано:
[[45g6-context-lines-probe]], [[taxonomy-v2-relabel-and-appetite]], [[test-v3]],
[[4b-training-contract]].
- 12:53: session ended (auto)
