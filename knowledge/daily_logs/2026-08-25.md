---
type: daily_log
date: 2026-08-25
tags: [daily, lora, money, acceptance]
---

# 2026-08-25

## Summary

`lora-c-run r2` ПРИНЯТ тимлидом как KILL по рунгу 4. Зарегистрирован рулинг (п): проба
`expandable_segments` за $0.30, заряд v3 = 9.20 с/звонок навсегда, и ПРЯМОЙ ОТВЕТ на вопрос моего
отчёта — адаптер линии НУЖЕН. Подробности — в чекпойнте ниже.

## Sessions

## Checkpoint 15:50

**Смена контекста: моя сессия ничего не исполняла сегодня — это приёмка тимлида, прилетевшая в
рабочее дерево.** В нём: изменённый `docs/STATUS.md`, вывод Stop-хука за 24.08 и **новый
непрослеженный контракт `docs/PROMPT-lora-c-vramprobe.md`**. Ни один из них я не трогал: тимлидовы
файлы коммитятся дословно, контракт исполняется по слову оператора.

### Что приняли

**`lora-c-run r2` — ПРИНЯТ 25.08 как KILL по рунгу 4** (день [[2026-08-24]], отчёт
`docs/reports/lora-c-run-r2.md`, ADR [[lora-c-the-card-cannot-train-at-3072]]). Тимлид проверил
приёмку своими инструментами: согласия и поклассовые — своим парсером из сырых out-файлов
(совпали построчно с моими), деньги — гардом и листингом сам. Его чтение цикла-2: **$9.3512 из
$20.00**, и **$0.7606 по часам пода r2 — единственный платёж всей линии lora-c**.

### Рулинг (п), 25.08 — три вещи, и одна из них меняет мою рамку

1. **Проба `expandable_segments` на том же PRO 4500, кап $0.30** — `docs/PROMPT-lora-c-vramprobe.md`.
   Замороженную регистрацию не трогает, свой микро-рекорд. Оба исхода — ответы: выжил → есть и
   s/step на 3 072; нет → карта закрыта и вопрос датацентра идёт к оператору.
2. **Во всех будущих выводах v3 заряжается ИЗМЕРЕННЫМИ 9.20 с/звонок, не 6.14.** Это ровно то
   число, которое купил рунг 3, и оно стало правилом. `PRO 6000` $2.09/ч признан невлезающим
   (~$11 за сессию).
3. **Ответ на мой вопрос «нужен ли линии адаптер» — ДА,** и он получен дозамером тимлида за $0:
   **v3 не чинит ни 580124 (F2a: по-прежнему «сеть»), ни бар 3 (те же 4 ложных «сеть» в 10366);
   весь прирост v3 (+5 холдаут) сидит в `не_наш_рынок` — мимо красных целей.**

**Это моя ошибка рамки, и она стоила строки в сданном отчёте.** Я вынес 66/98 против 61/98 в
заголовок и написал, что «нужен ли линии адаптер — теперь живой вопрос». Прирост я РАЗБИЛ по
классам и сам же напечатал, что все +6 в `не_наш_рынок`, — но не сверил это с теми ячейками, на
которых стоят КРАСНЫЕ бары. Проверка стоила $0 и была у меня под рукой: бар 1 висит на 580124,
бар 3 — на 10366. Урок записан в нативную память под именем `a_gain_that_misses_the_gated_cells` — вики-ссылкой его не даю: валидатор разрешает `[[…]]` внутри волта, а память живёт снаружи.

### Состояние

HEAD `9d29453`, тринадцать коммитов от `e2d28b4`. Подов нет, регистрация ЗАМОРОЖЕНА созданием r2,
**попытка цела**. Верификатор на закрытии сессии 24.08: **3 778/2, «reading HOLDS» на `b9f0536`**.
Открытый долг с той сессии: `--close` у гарда — walk опаздывает часами, $0.0602 прочитано против
$0.7606 по часам пода; тимлид записал этот долг на следующую сессию.

**Некоммитнутым оставляю ровно то, что пришло не от меня:** `docs/STATUS.md` (тимлидов файл — читаю
и коммичу дословно, никогда не правлю), `knowledge/index.md` и хвост `2026-08-24.md` (вывод
Stop-хука) и `docs/PROMPT-lora-c-vramprobe.md`. Коммиты — отдельно, по слову оператора.
- 15:53: session ended (auto)
- 17:10: session ended (auto)

## Checkpoint 19:28

**Исполнен `docs/PROMPT-lora-c-vramprobe.md` — по слову оператора, полностью, STOP после отчёта.**
Отчёт `docs/reports/lora-c-vramprobe.md`, восемь коммитов от `eae308e` до `57ceb90`.

### Ответ: OOM снова — но настройка сработала

Под `cs8vla37upmy42`, RTX PRO 4500 32 GB, $0.72/ч, EU-RO-1. Создан 14:40:13Z, удалён 14:46:29Z:
**376 с = $0.0752 из капа $0.30**. Рунги: 0 GO (цена и карта, плюс жёсткий стоп перерешён на
наблюдённой цене — 1 500.0 с, дрейф 0.0), 1 WAIT → GO на 29.4 с, 3 WAIT, 3 **KILL**, close-pod GO.

**Переменная доказанно дошла до ПРОЦЕССА обучения, а не до шелла** — `/proc/215/environ`, снятый
пока pid 215 крутил собственную командную строку тренера, лежит артефактом
(`results/lora_c_vramprobe_environ.txt`). И сделала ровно то, что обещает: reserved-but-unallocated
**1.33 GiB → 544.22 MiB**. Аллокатор потратил отвоёванные ~0.8 GiB (allocated 28.07 → 29.17 GiB),
зашёл в тот же `backward()` дальше — и упал на **бо́льшем** запросе: **1.99 GiB против 1.35 свободных**.

| | r2 | проба |
|---|---|---|
| просил / свободно | 1.72 / 1.66 GiB | 1.99 / 1.35 GiB |
| **не хватило** | **0.06 GiB** | **0.64 GiB** |

Разрыв вырос вдесятеро — «ещё бы чуть-чуть VRAM» отпадает. И проба ограничивает дефицит **только
снизу**: шагов ноль на обоих подах, широчайшая строка (2 975) не бралась ни разу, **размер нужной
карты отсюда не следует**. Обе пробы ПАРНЫЕ, доказано за $0 до создания: `sampling_order` — чистая
функция от (n, весов, seed), веса пересчитаны на Маке и совпали с логом парного пода, первый
микро-батч — строки [231, 4] на обоих.

### Два бесплатных чтения, которые теперь у оператора

**В EU-RO-1 сегодня нет ни одной 48 GB карты ни за какие деньги** — A6000, PRO 6000 и
преавторизованный фолбэк 4090 все пустые. A6000 48 GB за $0.53/ч есть `Low` в EU-SE-1 и US-TX-1;
MIG-срез PRO 6000 2g.48gb за $1.09 — в EUR-IS-2 и US-PA-1. Другой ДЦ = другой том: 59 GB весов
качать заново плюс вторая аренда ~$0.24/сутки, и ни то ни другое не оценено ни одной регистрацией.

**И второе ограничение, которому всё равно, какая карта.** Остаток капа r2 = $3.2394 = 16 197 с.
r3 влезает только при **56.5–64.1 с/шаг** — диапазон, потому что маркер-ценз (40 звонков × 9.20 =
368 с) и проход-2 по его же зарегистрированной границе 15 тредов вместо заряженных 11 (+776 с)
лежат ВНЕ фикс-части 6 577.2 с. Против единственного замера репы **61.047** (на строках ≤1 222
токенов) это **+3.1 … −4.5**. Остатка, скорее всего, не хватит и на подходящей карте.

### Что было сделано до денег ($0)

Регистрация `results/prereg_lora_c_vramprobe.json` — от руки, ~40 констант и две формулы, без
продюсера; от дрейфа её держит `tests/test_lora_c_vramprobe.py` (28 тестов), который гоняет каждый
рунг КОМАНДОЙ и грепает каждый порог обратно в контракт. Гейт
`scripts/gate_lora_c_vramprobe.py` — сиблинг `gate_lora_c.py`, который сам сиблинг lora-b: рунги
0/1/2, кумулятивные часы, бэкстоп и close-pod ВЫЗЫВАЮТСЯ с переприсвоенными константами, ноль новых
копий. Своими написаны две вещи: перерешение жёсткого стопа на цене из ответа create, и рунг 3.

### Восемь Dv (812–819) — две находки, которые стоит помнить

- **Dv812** [contract-gap] контракт регистрирует ДВА предела на один прогон (6×181.5+300 от старта
  смока и 1 500 с от create) и не называет главный. Перерешены друг против друга ДО денег:
  пересекаются при старте смока на 21.0 с после create, значит кап связывает на любом достижимом
  поде. У этого — 170.17 с/шаг.
- **Dv813** [verify-gap] `log_every: 5` делает из `--max-steps 6` **две** loss-строки, не шесть.
  Рунг r2 ждёт шести и ждал бы вечно на выжившем смоке — не всплыло только потому, что флаг `--oom`
  замыкал счёт.
- **Dv815** [tooling] `$!` после `VAR=x nohup cmd &` внутри `bash -c` и `pgrep -f` **оба** отдали pid
  обёртки, чей environ читается **точно как «переменная не задана»**. Тот самый ложный отрицательный,
  который обнулил бы всю пробу. Лечится ходьбой по `ps -eo pid,ppid` до строки запуска тренера.

Талли `contract-gap 1 · verify-gap 2 · tooling 2 · process 3` — восемь против восьми строк.

### Процессный сигнал, который дороже пробы

**`make fmt` обнуляет пин продюсера ЗАМОРОЖЕННОЙ записи.** `scripts/write_lora_c_prereg.py`
запинен `a4eb2893…` в `results/prereg_lora_c.json::producer`; `ruff format` двигает его в
`6807d264…`. Проверил и вернул байт-в-байт; два файла сегодня расформатированы и оставлены как
есть. `make check` гоняет `ruff check`, а не `ruff format --check`, и не видит ни того, ни другого.

### Состояние

HEAD `57ceb90`, дерево чистое, подов нет, `--pre-create-check` читает «1 closed, 376 с = $0.0752».
Верификатор: **3 805/2 «reading HOLDS» на `c9b0e71`** до создания и **3 806/2 на `e2a2abd`** в
закрытие. Все **61 число отчёта** переводятся из файла-владельца скриптом
`scripts/check_lora_c_vramprobe_report.py`, который гоняет суита.

**По контракту НЕ делал** (там сказано STOP после отчёта): ADR, `hot.md`, лог дня — этот чекпойнт
пишется по отдельному слову оператора. Долги: `--close` у гарда по обоим шагам (walk отстаёт:
$0.0495 против $0.0752 по часам пода). Уроки в нативной памяти:
`a_remedy_that_worked_and_was_not_enough` и `an_inline_env_var_is_not_in_the_shells_environ` —
вики-ссылками не даю, память живёт снаружи волта.
- 19:32: session ended (auto)

## `lora-c-migrate` 20:15 — STOP на $0.0000: ДЦ рулинга не держит сетевой том

Контракт `docs/PROMPT-lora-c-migrate.md` (кап $1.00) не исполнен по D2, и это правильный исход:
**первое же действие D2 — «create the volume in EU-SE-1» — платформа отказывает.** Ни тома, ни
пода не создано; отчёт `docs/reports/lora-c-migrate.md`, запись `results/prereg_lora_c_migrate.json` помечена
`REFUSED BEFORE THE CREATE — EVIDENCE, not law`.

### Чем это доказано — отказанным create, бесплатно

`runpodctl network-volume create --data-center-id NOPE` перечисляет **21** ДЦ, где сетевой том
возможен. **EU-SE-1 среди них нет.** Как нет US-TX-1, EUR-IS-2 и US-PA-1 — всех четырёх, что
назвал §5 вчерашнего отчёта: в списке стоят US-TX-**3** и EUR-IS-1/3/4, другие ДЦ. Список приходит
из самого create-эндпоинта, не из каталога рядом с ним. Create в EU-SE-1 не пробовал: эндпоинт уже
сказал, какие ДЦ принимает, — предел назван в записи, а не оставлен допущением.

Сама A6000 `Low` **только** в EU-SE-1; в CA-MTL-3, EU-RO-1 и US-KS-2 — тех, что том держат, —
`none`. Обе половины падают, и каждой хватило бы поодиночке.

### Моя же вчерашняя таблица и была тем, на чём стоял рулинг

Dv821, и это самая полезная строка леджера: §5 отвечал на «где карта», а решение требовало «где
может жить том». Списки пересекаются по CA-MTL-3, EU-RO-1 и US-KS-2 — потому подмена и пережила
отчёт, рулинг и контракт (урок памяти `a_capability_gate_is_not_a_theme_gate`).

### Что куплено вместо переезда ($0)

- **Пересечение целиком:** {≥48 GB} × {в наличии} × {том возможен} = **19 карт**, дешевейшая
  PRO 6000 MIG 48GB $1.09/ч US-NE-1. Два независимых листинга сверены попарно: 137 пар, 0 расхождений.
- **Переезд не нужен, чтобы достать подходящую карту:** в EU-RO-1 сегодня есть RTX PRO 6000 96 GB
  $2.09/ч — ровно та, что рулинг (п) уже отклонил как невлезающую.
- **Остаток капа r2 не берёт 61.047 с/шаг ни при одной доступной цене**, и вывод держится, если
  вдвое ускорить карту (больше замеренного разброса 1.65×).

### Четыре находки в числах самого плана — до денег

Гард потолка из пробы требует **равенства** `cap/price×3600 == hard_stop`; там 1500.0 совпало
случайно, здесь $1.00/$0.53 даёт 6792.45 против 3600 — тот же хелпер бросил бы `SystemExit` на
`--open`, то есть **после** старта счётчика. Дедлайны 500+2400+600 = 3500 против стопа 3600
оставляют 100 с на маунт, бандл, **venv**, паки и teardown — а стадию venv не ограничивает ни одна
ступень и в репе её длительность не измерена нигде. Ливнесс закачки, прочитанный «по последней
строке лога», убьёт живой под: `hf download` рисует CR-прогрессбар — читать надо байты. И
`first_number` на правиле, начатом с карты, возвращает **6000.0** из «RTX A6000», делая потолок
цены бессмысленным при зелёной суите.

### Step 0.5 — один close прошёл, второй отказал, и отказ информативнее

`lora-c` закрыт: settled **$0.764952**, 4.2% от своей записи, окно 3 803 927 мс против 3 803 000.
`lora-c-vramprobe` **ОТКАЗАН** и остаётся открытым — но неверна не settled-величина ($0.075729
против $0.0752 по часам пода, 0.7%), а записанное **$0.0495**: дельта баланса, снятая через 21 с
после удаления. Обе записи отстают примерно на одну и ту же **абсолютную** величину ($0.031152 и
$0.026229) — 4.2% и 53.0%. При допуске 0.07 close недостижим для шага дешевле ~$0.37–0.45, как бы
он ни был верен. Полосу не трогал: не наша область, долг несётся названным.

### Состояние

$0.0000 из $1.00. Подов нет, том один — `mp-srv2`, не тронут. Замороженная регистрация побайтово
та же (`4d5a8f1d…`), попытка ЦЕЛА: 0 шагов, 0 ответов эвала, 0 адаптеров. Гейт-скрипт НЕ написан
намеренно (Dv826): у него все числа меняются со словом оператора. Суита 3 806/2 на входе, **3 843/2 на выходе** (`295815f`); 37 новых тестов; чекер отчёта 64/64. Линия ждёт одного: **поднять кап, урезать план или ждать A6000** —
таблица в `docs/reports/lora-c-migrate.md` §5, ни один вариант здесь не выбран.
- 20:19: session ended (auto)
- 20:20: session ended (auto)
- 20:35: session ended (auto)
- 20:37: session ended (auto)
- 20:40: session ended (auto)

### Хвост: закрывающее чтение поймало межконтрактную связь (Dv828)

Первые две попытки закрывающего `make check-stamped` были **убиты** харнессом, не упали — и
докладываю их как убитые. Вторая успела показать **3 842 passed / 1 failed**, и упавший тест был
чужой: `check_lora_c_vramprobe_report.py`. Мой же `--close` из Step 0.5 дописал более позднее
чтение в `results/spend_cycle2.json`, а две строки того чекера выводили трату цикла-2 из
**последней** строки леджера — верно ровно до тех пор, пока никто больше не пишет. То есть
запечатанный отчёт стали сверять с числом чужой сессии.

Починил **селектор, а не число**: строка выбирается по штампу чтения, о котором отчёт
(`2026-08-25T14:46:50+00:00`), и `the_row_at` падает на «нет строки» и на «строк больше одной»,
а не берёт соседа. Числа отчёта о пробе не тронуты, 61/61 по-прежнему сходятся. Его sha-таблица
получила **пометку**, что sha чекера сдвинулся `29abf704…` → `42a6efd7…`, вместо тихой перезаписи:
переписать sha значило бы стереть само свидетельство, что что-то менялось. Урок записан в память
как `select_one_row_refuse_ambiguity` в паре с `rewriting_a_record_resets_state_you_do_not_own`.

Третье чтение пошло **отцеплённым** от харнесса (ppid 1) и дошло: **3 843 passed / 2 skipped**,
«reading HOLDS» на `295815f`, exit 0. Талли деклараций — **девять** строк:
`contract-gap 3 · verify-gap 4 · tooling 1 · process 1`.
- 20:54: session ended (auto)

## Checkpoint 21:02

Контракт `lora-c-migrate` закрыт STOP-отчётом — разбор целиком в секции 20:15 выше, здесь только
состояние и то, что осталось открытым.

**Итог, одной строкой:** переезд, назначенный рулингом (р), неисполним — EU-SE-1 не держит сетевой
том, — и это выяснено бесплатно, до создания чего бы то ни было. **$0.0000 из $1.00.**

### Состояние на чекпойнте

- **Дерево чистое**, 10 коммитов (`42a0ccb` … `171ae1d`). Суита **3 843/2**, «reading HOLDS» на
  `295815f`; после него — только два докуметных коммита, как у пробы (`e2a2abd` при `57ceb90`).
- Чекеры отчётов: **64/64** (migrate) и **61/61** (vramprobe, после починки селектора).
- Подов нет, серверлесса нет, том один — `mp-srv2` (`qw4nwleanc`, 100 GB, EU-RO-1), не тронут.
- Замороженная регистрация `4d5a8f1d…` побайтово та же; **попытка ЦЕЛА** — 0 шагов, 0 ответов
  эвала, 0 адаптеров.
- Талли деклараций: `contract-gap 3 · verify-gap 4 · tooling 1 · process 1` — девять строк.

### Что остаётся открытым

1. **Слово оператора: поднять кап, урезать план или ждать A6000.** Переезд как таковой из вопроса
   выпал: подходящая карта есть в ДЦ существующего тома (RTX PRO 6000 96 GB $2.09/ч), просто это та,
   что рулинг (п) отклонил. Таблица — `docs/reports/lora-c-migrate.md` §5.
2. **`lora-c-vramprobe` в леджере ОТКАЗАН и остаётся открытым.** Долг несётся названным: полосу
   допуска не трогал, потому что она не наша область, а settled-величина у него верна.
3. **Гейт-скрипт миграции не написан намеренно** (Dv826) — все его числа меняются с ответом на п. 1.
4. **`make fmt` по-прежнему запрещён:** оба расформатированных файла запинены записями.
5. **Boot tax 10.8K против цели 10.7K.** Рычаг — не `hot.md`, а `MEMORY.md`: 24.4 KB, ~57% сбора,
   при сырых 26.2 записи индекса уже теряются. Консолидация — отдельное решение.

### Уроки этой сессии, ушедшие в память

`a_capability_gate_is_not_a_theme_gate` — «где ресурс» и «где может жить его зависимость» суть два
списка, и они пересекаются достаточно, чтобы подмена прошла три документа.
`a_relative_band_cannot_grade_a_cheap_step` — одна и та же абсолютная задержка расчёта дала 4.2% и
53.0%, и полоса отказала меньшей ошибке.
- 21:04: session ended (auto)

## `lora-c-migrate r2` 22:30 — переезд состоялся, $0.287267 из $1.50

**Деливерабл куплен.** Том **`mp-lora-c` (`soymlju8q0`, 100 GB, CA-MTL-3)** создан, веса на нём
(62 580 183 517 B = 58.28 GiB, 0 неполных блобов, снапшот = запиненная ревизия `842da379…`), и
**модель загружена с него на A100 PCIe 80 GB за 40.22 с, 17.046 GiB из 79.138** штатным
`local_llm.load`. Под `khixo68oxnls38` удалён на 744 с. Ноль шагов, ноль эвалов, ноль адаптеров,
попытка r2 цела, `mp-srv2` не тронут. Отчёт — `docs/reports/lora-c-migrate-r2.md`, 79/79.

**Первые замеры стадий этой линии.** ssh 35.3 с из 500 · **venv 132.0 с из 900** — первый замер
этой стадии в репе вообще (внутри: `python -m venv`, 27 пакетов, импорт torch — не разложено) ·
закачка 184 с из 1200 (340.1 MB/s; часы `hf` — 02:32) · загрузка 40.22 с из 600. Четыре стадии
съели 12.2% отпущенного, под — 20.7% хард-стопа.

**Рунг 5 дал KILL, и он записан как свой.** Правило требовало, чтобы `du -sb /workspace/hf` не
сдвинулся за загрузку; сдвинулся на **40 байт** — `refs/main` плюс четыре ПУСТЫХ маркера
`.no_exist`. Блобов записано 0. Перечислено на поде до удаления
(`results/lora_c_migrate_r2_load_bytes.json`), правило после чтения не правилось. Урок —
`an_invariant_on_the_container_not_the_payload`.

**Развилка отдана оператору до денег.** Свежее чтение в 19:48Z показало четыре более дешёвых
томопригодных строки ≥48 GB, три из них в US-KS-2, который несёт и ту же A100: A6000 **$0.53**,
L40 $0.82, RTX 6000 Ada $0.84. Только $0.53 (102.84 с/шаг) проходит единственный замер репы 61.047.
Показано с числами — выбран **CA-MTL-3, как в рулинге (с)**. Сток мерцает поминутно: три чтения
A100 за 17 минут дали три разные строки.

**Восемь Dv (829–836): contract-gap 2 · verify-gap 3 · spec-gap 1 · tooling 1 · process 1.**
Крупнейшие: чекер запечатанного отчёта читал ЖИВОЙ леджер по `[-1]` (Dv829, починен селектор —
и он выдержал две новые строки в этой же сессии, 61/61); порядок стадий в клаузе 4 неисполним, потому
что venv и загрузчик оба требуют чекаута (Dv830); рунг 5 (Dv831).

**Суита: базовая 3 843/2 на `8d3727e`, закрывающая 3 905/2 на `0b18366`, exit 0.** Число было
**предсказано до чтения** (3 843 + 1 + 61) и совпало. Второй урок сессии —
`a_number_typed_into_its_own_checker`: 184 с стояли и на странице, и в арифметике чекера, так что
проверка держалась ни над чем; теперь выводится из файла поллов.

**Открыто:** контракт r3 пишет тимлид (том тёплый, повторной закачки не нужно; кап словом оператора,
вилка $5–6.5 на A100) · `lora-c-vramprobe` close отказан структурно и остаётся открытым · два тома
капают ~$0.4667/сутки против остатка цикла $10.2694 (~22 дня).

**Обе новые записи памяти вставлены В НАЧАЛО `## Feedback`, а не в конец.** `MEMORY.md` — 26.9 KB
против окна загрузчика 24.4 KB, и 14 строк уже не доезжают; запись в хвост была бы записью, которую
следующая сессия не увидит. Файлы вытесненных уроков на диске целы — невидим только указатель.
Консолидация индекса остаётся открытым решением, она названа в блокерах `hot.md`.
