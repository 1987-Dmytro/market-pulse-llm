---
type: daily_log
date: 2026-08-11
tags: [daily]
---

# 2026-08-11

## Summary

**arch-a: карта построена, ничего не сдвинуто.** Девять коммитов, `make check` **1611 passed /
2 skipped на каждом из девяти** (каждый выкачен отдельно и прогнан на своём дереве), формат чист на
211 файлах, check-wikilinks зелёный. $0 — ни одного платного вызова, `graphify extract` (LLM-путь)
не запускался сознательно, граф собран `graphify update .` (AST-only).

**Step 0.** Грязный набор разложен по путям на четыре коммита (`866da4d` голд text30 · `daa2832`
файлы тимлида verbatim · `e1a46a5` Tooling-слой · `5c94f40` vault-хвост); `git add -A` не
применялся — он бы смёл `docs/PROMPT-arch-a.md` и `knowledge/runbooks/` в один коммит.

**SPEC 3.17 (7) — `d3bf781`, и это находка дня (Dv112).** Блок вставлен байт-в-байт (извлечён из
брифа программно, не перепечатан), чистая вставка 16 строк. Бриф велит эволюционировать ОДИН тест —
и этого мало: `prereg.main()` пересчитывает `pinned_inputs` с живого файла, а
`test_the_record_rebuilds_identically_apart_from_its_timestamp` сравнивает ребилд поле за полем.
Измерено: без strip'а в продюсере `fields that moved: ['pinned_inputs']`, `781f611a…` против
закоммиченного `973c8789…`, `a == b` False. Поэтому strip живёт в ОДНОЙ функции
`write_sku_prereg.registered_law`, которую зовут и продюсер, и тест. Пин проверяется в обе стороны;
прережка не тронута и не перепинована.

**ADR ратификации — `5add452`.** [[sku-b-pilot-readings-ratified]]: R1–R5 из записи прережки
(каждая цитата прогреплена обратно), состав голда через зарегистрированный валидатор (11 · 3 · 16,
из 16 — 12 с галочками без бренда и 4 пустых), два триаж-рулинга словами оператора из колонки notes,
и знаменатель бара 3 = ВСЕ 30 строк (сужение до 14 отклонено тимлидом 11.08).

**Курация hot.md — `483081a`.** 47 981 → 22 668 байт, **12.0K → 5.7K токенов** при планке 6.0K;
бут-налог 18.3K → 12.0K. Маркеры AUTO-GEN не тронуты (блок вклеен ПОСЛЕ END), `**Last update:**`
парсится stale-check'ом, оба литерала, которые греплет `volume_calc_5c1.py`, на месте — доказано
негативным контролем: подмена `~$0.24/day` роняет 9 из 10 тестов, файл восстановлен побайтово.

**Граф — `0278522`.** `graphify claude install` (CLAUDE.md 103 → 113 строк, потолок 200) +
PreToolUse-хук; deny-слой пережил запись байт-в-байт (`permissions` sha `6fdfb753…` до и после).
15 420 узлов / 18 318 рёбер / 1 362 сообщества за 13.6 с. `graphify-out/` в gitignore (28 МБ
производного AST, который хук пересобирает после каждого коммита). Пост-коммит хук показан живым: на
`b34fbfa` он написал `Rebuilt: 15441 nodes, 18338 edges` — граф действительно сдвинулся.

**`docs/ARCHITECTURE.md` — `b34fbfa`.** Два потока, 19 узлов сверены: **14 CONFIRMED, 3 DEVIATION,
2 NOT_BUILT**. Главное для sku-b: **дампа цен по позициям, из которого СКОРИТСЯ бар 2, не существует
— его должен построить сам sku-b** (Dv115); сегодняшний воркер отвечает только `info|batch|caption`
и отказал бы джобу `positions_post_gm4`, а прережка не пиннует serving-конфиг вообще (Dv114);
потолок 10 МБ — код только на подписной ноге (Dv113). Инвентарь 149 строк без выборки: 16 промптов
(RENDER_ONLY-близнец — один ИЗ 16), 111 файлов в `scripts/` (не 112 — `ls` считает `__pycache__`),
21 модуль. Покрытие доказано диффом готового документа против трёх листингов. **candidate-dead —
пусто**; два файла без входящих ссылок отданы оператору на рулинг.

Деталь и все девиации Dv112–Dv120 — `implementation-notes.md`, раздел arch-a. Вчерашний день —
[[2026-08-10]]. ADR дня: [[sku-b-pilot-readings-ratified]].

## Sessions
- 11:44: session ended (auto)
- 11:52: session ended (auto)
- 13:17: session ended (auto)
