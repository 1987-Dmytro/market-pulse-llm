---
type: daily_log
date: 2026-08-11
tags: [daily]
---

# 2026-08-11

## Summary

**arch-a исполнена: карта построена, ничего не сдвинуто.** Одиннадцать коммитов `866da4d..43b5886`,
`make check` **1611 passed / 2 skipped на каждом из одиннадцати** — каждый выкачен отдельно
(`git checkout --detach`) и прогнан на своём дереве, не на рабочем. Формат чист на 211 файлах,
`check-wikilinks` зелёный. **$0**: `graphify extract` (путь через LLM) не запускался сознательно,
граф собран `graphify update .` — AST-only, без ключа.

**Step 0 (1) — грязный набор по путям, четыре коммита.** `866da4d` адъюдицированный голд text30 ·
`daa2832` файлы тимлида verbatim (STATUS, бриф arch-a, deny-строка PRODUCT.md) · `e1a46a5`
Tooling-слой от `/brain-init augment` · `5c94f40` vault-хвост. `git add -A` не применялся — он бы
смёл `docs/PROMPT-arch-a.md` и `knowledge/runbooks/` в один коммит. Голд оператор сохранил с CRLF:
диффа 31 строка при 30 сдвинутых ячейках, оставлено как сохранено (исполнитель не нормализует голд),
валидатор читает чисто.

**Step 0 (2) — SPEC 3.17 (7), `d3bf781`, и это находка дня (Dv112).** Блок извлечён из брифа
программно и вставлен байт-в-байт: чистая вставка 16 строк, ноль удалений. Бриф велит
эволюционировать ОДИН тест — и этого недостаточно: `prereg.main()` пересчитывает `pinned_inputs` с
живого файла, а `test_the_record_rebuilds_identically_apart_from_its_timestamp` сравнивает ребилд
поле за полем. **Измерено, а не выведено:** без strip'а в продюсере `fields that moved:
['pinned_inputs']`, `781f611a…` против закоммиченного `973c8789…`, `a == b` **False**. Поэтому strip
живёт в ОДНОЙ функции `write_sku_prereg.registered_law`, которую зовут и продюсер, и тест. Пин
проверяется в обе стороны (блок присутствует · живой sha ≠ пин · stripped sha == пин), и strip
доказан точным против копии SPEC, снятой до записи. Прережка не тронута и не перепинована.

**Step 0 (3) — ADR, `5add452`.** [[sku-b-pilot-readings-ratified]]: R1–R5 читаны из записи прережки,
каждая цитата прогреплена обратно в источник; состав голда через зарегистрированный валидатор
(11 position · 3 product_mention · 0 brand_mention · 16 none, из 16 — 12 с галочками без бренда и
4 полностью пустых, все четыре названы); два триаж-рулинга словами оператора из колонки `notes`;
знаменатель бара 3 = ВСЕ 30 строк. Квиз подписей сверен с артефактом:
`caption_candidates.by_channel["@atb_market_official"]` = 19 judged / 9 faithful / 10 partial /
0 wrong, и `brands_visible_missed` называет четыре из пяти постов квиза (у `:4401` — восемь брендов
разом).

**Step 0 (4) — курация hot.md, `483081a` (+ хвост в `2fde2d5`).** 47 981 → 23 576 байт, **12.0K →
5.89K токенов** при планке 6.0K. Маркеры AUTO-GEN не тронуты (блок вклеен ПОСЛЕ END-маркера, а не
переписан вокруг него), `refresh-hot-cache.py` перезапущен чисто, `**Last update:**` парсится
stale-check'ом. Оба литерала, которые греплет `volume_calc_5c1.py`, на месте — доказано негативным
контролем: подмена `~$0.24/day` роняет 9 из 10 тестов, файл восстановлен побайтово (`cmp`).

**Deliverable A — граф, `0278522`.** `graphify claude install`: секция в CLAUDE.md (103 → 113 строк
при потолке 200) + PreToolUse-хук. Deny-слой пережил запись байт-в-байт — `permissions` sha
`6fdfb753…` до и после, 4 записи. Граф 15 420 узлов / 18 318 рёбер / 1 362 сообщества за 13.6 с.
`graphify-out/` в gitignore. Пост-коммит хук показан живым на `b34fbfa`: `Rebuilt: 15441 nodes,
18338 edges` — граф действительно сдвинулся, а не просто «хук запустился».

**Deliverables B и C — `b34fbfa`.** `docs/ARCHITECTURE.md`, 398 строк. Два потока, **19 узлов:
14 CONFIRMED · 3 DEVIATION · 2 NOT_BUILT**; вердикт закодирован в штрихе рамки, так что читатель
видит, какие части Flow 1 платная попытка обязана построить. Инвентарь 149 строк без выборки, у
каждой — одна проверенная улика. Сбор улик фанаутом из 13 read-only агентов; **покрытие доказано
механически**: возвращённые пути продиффены против `find`-листингов и `prompts.PROMPTS`, а потом
готовый документ продиффен против тех же трёх листингов ещё раз — 0 пропусков, 0 лишних. Классы —
мои, не агентов; все пять находок DEVIATION/NOT_BUILT перевыведены руками.

**arch-a ПРИНЯТА тимлидом в тот же день** (`docs/STATUS.md`, блок 11.08; сверено ими на диске:
suite перепрогнан 1611/2, пин SPEC проверен ими в обе стороны). Dv112 — аддитивная правка продюсера —
**принята с флагом, коллизия с DO-NOT признана дефектом брифа**, а не нарушением. Вставлена
новая фаза **uni-a** (аудит универсальности, $0), бриф `docs/PROMPT-uni-a.md` лежит в дереве
неотслеженным.

**uni-a исполнена тем же днём: схема следует реестру, словарь — нет.** Пять коммитов
`7d5d564..8c2ba25`, `make check` 1611/2 на пяти точках, формат чист на 212 файлах, `$0`. Отчёт —
ФАЙЛОМ (первое применение протокола 11.08): `docs/reports/uni-a.md`.

**Свип (deliverable A).** Паттерны сгенерированы ИЗ реестра и лексикона (27 категорийных имён,
39 display_names, 66 хендлов + широкий свип хендлов вне реестра), 848 уникальных строк-хитов
разделены на прозу и КОД через `tokenize` + `ast` — **158 prose / 690 code**, потому что молча
пережить смену реестра может только живой литерал. Код-хиты: `tests/` 373 в 50 файлах ·
`scripts/` 299 в 37 · `src/` 18 в ОДНОМ (`prompts.py`) + 3 хендла в `entry_check.py`. **20 из 22
модулей `src/` не содержат доменного литерала в коде вообще** — это и делает уровень (а)
дешёвым. Шесть утечек перечислены с `file:line` и НЕ починены.

**Проба (deliverable B), `results/uni_probe.json`.** Игрушечный реестр «кава» в tempdir + живой
блок `sources`: **1 follows · 2 hard-coded · 3 follows · 4 follows · 5 needs-new-registration.**
Главное число: на детерминированной выборке 2 000 строк корпуса под игрушечным кофейным
лексиконом проходит **11** строк, под ЖИВЫМ лексиконом рядом с кофейным реестром — **90**, и все
90 по молочным стемам («сир» 26, «масл» 17, «молок» 11, «сметан» 11, «морозив» 11). Шаги 1 и 5
читаются ТОЛЬКО в паре: парсер принимает пять кофейных категорий, которых не видел, а
зарегистрированный промпт велит модели отказывать кофе ПО ИМЕНИ («chocolate, sausage, coffee,
nappies and cheese-flavoured snacks are not dairy» — прогреплено из импортированного модуля).
Генератора промптов нет: измерено обходом сигнатур всех публичных callable'ов — список пуст.

**`docs/PORTING.md` (deliverable C), 269 строк.** Уровень (а) — процедура, которая уже
существует, плюс три цены, которых не было в рулинге 08.08: у лексикона нет брендовой секции
(Dv6, перемерено); `entry_check.py::CANDIDATES` — 96 хендлов руками рядом с правкой реестра; из
восьми файлов `results/`, пиннующих sha реестра, **три держат живой `920c7f20…`**, и два из них —
входы неотстрелянной платной попытки sku-b, так что правка watchlist ВО ВРЕМЯ B — коллизия, не
рутина. Уровень (б) упорядочен по блокирующим зависимостям. Вопрос `fat` ФЛАГНУТ, не решён: он в
`PRESENCE_FIELDS` → `ladder_table()` → `ladder_sha256()` = `b497c072…`, пиннован в живой прережке,
поэтому обобщение возможно до старта sku-b или после закрытия B — и никогда между.

## Decisions

- **[[sku-b-pilot-readings-ratified]]** (ADR записан сегодня, решение оператора 10.08) — пять чтений
  ратифицированы; **знаменатель бара 3 = все 30 адъюдицированных строк**, сужение до 14 отклонено
  тимлидом 11.08 как post-hoc смена зарегистрированного бара.
- **Strip вместо ре-пина** (исполнитель, Dv112) — амендмент 3.17 (7) носит собственные маркеры, а
  зарегистрированный закон = текст без блока. Одна функция на продюсер и тест. Альтернатива
  (перепиновать прережку) отвергнута: пин, который следует за файлом, — не пин.
- **`graphify-out/` в gitignore** (исполнитель) — 28 МБ производного AST, который хук пересобирает
  после каждого коммита; закоммиченная копия была бы диффом на 11 МБ и всегда на одну асинхронную
  пересборку отстающей.
- **candidate-dead оставлен пустым** (исполнитель) — класс требует конъюнкции из пяти отрицаний,
  недостижимой там, где артефакт каждого one-shot закоммичен. Два файла без входящих ссылок отданы
  оператору на рулинг, а не переклассифицированы явочным порядком.

**Рулинги оператора того же дня (из нового блока `docs/STATUS.md`):**

- **(а) candidate-dead — рулинг вынесен:** `make_annotation_batch.py` и `runbook_5b1.md` **остаются
  one-shot, не мёртвые** — их артефакты несущие. Вопрос закрыт.
- **(б) `git_state()` ×5 копий → fix-on-touch:** первая фаза, трогающая любой из пяти носителей или
  пишущая шестой штамп провенанса, сводит их на одну функцию в `src/` с тестом равенства штампов. До
  того — не трогать.
- **(в) НОВЫЙ ПРОТОКОЛ ОТЧЁТОВ, с uni-a:** отчёт фазы — ФАЙЛ `docs/reports/<фаза>.md`, свой коммит
  `docs(report): <фаза>`, в чат уходит **только путь**. Мотив — два покалеченных чат-релея 11.08;
  тимлид читает с диска. Ушло в память как `reports-go-to-files-not-chat`.

## Next

1. **Приёмка uni-a оператором** (~20–30 мин по брифу): рулинг по шести утечкам, `docs/PORTING.md`
   как стоячая процедура, вопрос `fat` — SPEC-вопрос с ограничением по порядку.
2. **Джойнт-ревью брифа sku-b** — Dv113–Dv115 уже вписаны тимлидом в список того, что sku-b строит.
3. **sku-b**: две ноги, кап $0.35, ОДНА попытка, красный бар закрывает B по измерению.
4. Затем **брифинг 5c2**.

## Blockers

- **Коллизия нумерации ЗАКРЫТА:** бриф uni-a говорил «continue at Dv119», Dv119/Dv120 были заняты
  arch-a; uni-a подала **Dv121–Dv124** и сказала об этом в отчёте. Dv живут в
  `docs/reports/uni-a.md`, в `implementation-notes.md` — только указатель.
- **Условие sku-b, не блокер сегодня:** дампа цен по позициям нет (Dv115) — артефакт, из которого
  СКОРИТСЯ бар 2. Воркер отказал бы `positions_post_gm4`-джобе (Dv114), serving-конфиг не
  пре-регистрирован (Dv114), гард ≤10 MB — только на подписной ноге (Dv113). Все четыре тимлид уже
  вписал в то, что sku-b строит.
- **Бут-налог 13.0K при цели 9.0K; hot.md 6.19K при планке arch-a 6.0K — планка пробита на 0.19K
  и это сказано, а не спрятано.** uni-a добавила два факта, которые обязаны быть на виду (лексикон
  вне досягаемости реестра + пин `fat`), и в уплату сжаты четыре блока (рулинг candidate-dead,
  deny-правила, разбивка фрейма префильтра, superseded-строка про паритет). Дальше резать — это
  уже сигнал, нужный sku-b. Следующий рычаг прежний: MEMORY.md (4.2K) и rules с `paths:`.
- **Бюджет:** Phase 4 стоит на $22.0663 из $25.00, $2.9337 осталось (чтение 09.08, всё ещё
  доседает — Dv33). Сегодня потрачено $0.

Вчерашний день — [[2026-08-10]]. Полная форма и девиации Dv112–Dv120 — `implementation-notes.md`,
раздел arch-a; **девиации uni-a (Dv121–Dv124) — в `docs/reports/uni-a.md`**, в
`implementation-notes.md` только указатель. ADR дня — [[sku-b-pilot-readings-ratified]]; связанные:
[[sitting-2026-08-10-composition-signed]], [[opus-review-programme-close]].

## Checkpoint 14:57

Первый чекпоинт дня; всё, что ниже, — сессия uni-a целиком (её содержание — в Summary выше).

**Шесть коммитов, `7d5d564..f331bee`, дерево чистое.**

| коммит | что |
|---|---|
| `7d5d564` | step 0 (1) — vault-хвост arch-a, который прошлая сессия дописала и не закоммитила |
| `3f51757` | файлы тимлида verbatim: блок приёмки в `docs/STATUS.md` + `docs/PROMPT-uni-a.md` |
| `a9ac92d` | step 0 (2) — `CLAUDE.md`: `docs/reports/` в карту владения, фраза про scorer исправлена (Dv118). 115 строк при потолке 200 |
| `eb8758e` | deliverable B — `scripts/uni_probe.py` + `results/uni_probe.json` |
| `341d7ff` | deliverable C — `docs/PORTING.md`, 269 строк |
| `8c2ba25` | **отчёт фазы — `docs/reports/uni-a.md`**, первое применение файлового протокола |
| `f331bee` | vault-хвост uni-a: этот лог, hot.md, указатель на Dv121–Dv124 |

**Верификация (не пересказ, а прогоны).** `make check` — 1611 passed / 2 skipped на пяти точках
(старт сессии, после правки CLAUDE.md, после пробы, после PORTING.md, после отчёта);
`ruff format --check .` — 212 файлов; `check-wikilinks` — OK; sha живого `config/registry.yaml`
`920c7f20…` не сдвинут (совпал с литералом брифа — это позитивный контроль на сам бриф);
`prompts.PROMPTS` — 16 записей, 15 различных sha, обе позиционные совпадают с пинами прережки;
единственный новый файл в `results/` — `uni_probe.json`; **0 расхождений** по 267 файлам `results/`,
4 конфигам и 110 файлам `data/raw`+`data/frozen` — база хешей снята ДО первого write-действия и
сверена ПОСЛЕ прогона сьюта. Проба воспроизводима: два прогона байт-в-байт, кроме `generated_at`
и блока `git`.

**Открыто и ждёт оператора:** приёмка uni-a — шесть утечек (L1 — словарь категорий в файле, куда
реестр не дотягивается; блокирует НЕ sku-b), `docs/PORTING.md` как стоячая процедура, вопрос `fat`
с ограничением по порядку. Потрачено сегодня: **$0**.

**В память ушло:** дополнение к [[run-the-instrument-on-the-named-example]] — «not found» от
навигационного инструмента стоит ровно столько же, сколько ноль от матчера: `graphify path`
провалил собственный позитивный контроль, поэтому нигде в отчёте не цитируется как улика.

## uni-b — закон словаря, поле attribute, пере-регистрация ($0)

Исполнен `docs/PROMPT-uni-b.md` — три хода универсальности одной церемонией, до пилота.
Отчёт-файл: **`docs/reports/uni-b.md`** (протокол uni-a, коммит `6f1e00a`).

| коммит | что |
|---|---|
| `258590a` | vault-хвост чекпоинта 14:57 (step 0) |
| `bb7378f` | файлы тимлида — STATUS-блок и бриф, вербатим |
| `d8827c8` | **A** — `config/lexicon.yaml` (закон), `src/market_pulse/lexicon.py`, `units` из `positions` |
| `1aa431f` | **D(1)** — SPEC 3.17 (8) + strip, снимающий ВСЕ помеченные блоки |
| `fc662e0` | **B + D(2) + D(3)** — `fat` → `attribute`, пересборка манифеста, прережка v2 рядом с запечатанной v1 |
| `b1a2a63` | **C** — L2–L5, и `docs/PORTING.md` приведён к сегодняшнему состоянию |
| `e7ea9d8` | **D(4)** — повтор пробы, `results/uni_probe_v2.json` |
| `6f1e00a` | отчёт фазы |

**Верификация — прогоны, не пересказ.** `make check` после КАЖДОГО деливерабла: 1623 · 1623 ·
1627 · 1633 · **1635 passed / 2 skipped** (база до фазы 1611/2). `ruff` чист, 215 файлов
отформатированы, `check-wikilinks` OK. Байт-идентичность по sha: `prompts.py` `de767900…`
(16 записей / 15 различных sha, обе позиционные не сдвинулись), `config/registry.yaml`
`920c7f20…`, драфт-лексикон `1225ad75…`, размеченный `text30.csv` `ba77b381…`, прережка v1
`b1bfa40d…`. По `results/` + `config/` + `data/` + `prompts.py` **сдвинулись ровно** пересобранный
манифест и три новых файла.

**Что сделано по существу.** Словарь категорий стал законом — `config/lexicon.yaml`, мигрирован из
драфта по содержимому (тест сверяет с файлом на диске), с `units` внутри (порядок значим и
проверяется как последовательность). Лестница переименована: `b497c072…` → `6a257e04…`, и это
БИЕКЦИЯ — ни одна ступень не сдвинулась; провод сохранил доменное слово (`WIRE_KEYS`), поэтому
голд `text30.csv` читается как читался: 11 position · 3 product_mention · 16 none. Прережка v2
зарегистрирована РЯДОМ: все планки, пороги, кап $0.35, одна попытка и R1–R5 байт-равны v1,
различий ровно два — sha лестницы и sha манифеста, и это утверждается тестом полистно.

**Находка фазы.** Закон отказал трём из четырёх кофейных стемов пробы («еспресо», «лате»,
«капучино» не префиксы ни одного «Кава …») — это цена закона и она записана, а не обойдена
(Dv130). Проба теперь делит стемы по правилу закона и печатает обе половины.

**Девиации Dv125–Dv132** — в `docs/reports/uni-b.md`, кратко: B и D(2)/D(3) неделимы (иначе
красный пин в истории); `lsp_find_references` недоступен → pyright по всему дереву (403 = база,
0 упоминаний переименованных символов); SPEC правился скриптом, т.к. `Edit(/docs/SPEC.md)` в deny
(прецедент `d3bf781`); `--manifest-only` — новый флаг, иначе пересборка манифеста уничтожила бы 26
размеченных строк; 5c1-скрины сознательно не мигрированы; тест census эволюционировал; счёт пинов
реестра в PORTING перемерен (8→9, 3→4).

**Ревью после сдачи (advisor) добавило четыре вещи, все проверены измерением и закрыты.**
(1) Пин манифеста внутри v1 устарел — `2b941243…` против `80e4e12c…` на диске; **пере-пиннивать
запрещено**, цепочка к сегодняшним байтам — блок `supersedes` в v2; сказано в отчёте и в hot.md.
(2) `--manifest-only` обходит гард заполненного пакета — свойство «ничего не пишет» теперь
закреплено тестом. (3) Семья инструментов без строки в `WIRE_KEYS` роняла атрибут ТИХО (замерено:
кофейный ответ с `"fat"` парсился в `attribute_pct=None`) — теперь громкий отказ, и PORTING.md
говорит, что регистрация семьи это ДВЕ правки, а не одна. (4) Свип «в прережке нет вердиктов»
переиспользует список прережек, чтобы будущий `_v3` не выпал молча. Итог: **1637 passed / 2
skipped**.

**Открыто и ждёт оператора:** приёмка uni-b (~15 мин: подпись прережки v2 + отчёт). Потрачено: **$0**.

## Checkpoint 16:27

Фаза **uni-b** исполнена и сдана целиком; дерево чистое, ветка `main`, **$0**. С предыдущего
чекпоинта (14:57) — девять коммитов `258590a..94573c6`, каждый прогнан на собственном чекауте.

| коммит | что |
|---|---|
| `258590a` · `bb7378f` | step 0: vault-хвост чекпоинта, затем файлы тимлида вербатим |
| `d8827c8` | **A** — `config/lexicon.yaml`, закон словаря, `src/market_pulse/lexicon.py` |
| `1aa431f` | **D(1)** — SPEC 3.17 (8), strip снимает ВСЕ помеченные блоки |
| `fc662e0` | **B + D(2) + D(3)** — `fat` → `attribute`, манифест, прережка v2 рядом с запечатанной v1 |
| `b1a2a63` | **C** — L2–L5 + `docs/PORTING.md` |
| `e7ea9d8` | **D(4)** — `results/uni_probe_v2.json`, шаг 2 = follows-registry(law) |
| `6f1e00a` · `e17ab43` | отчёт `docs/reports/uni-b.md` + улика «каждый коммит зелёный на своём чекауте» |
| `94573c6` | ревью-проход: тихая семья инструментов, незакреплённый no-write, устаревший пин v1 |

**Числа.** `make check` — 1623 · 1623 · 1627 · 1633 · 1635 · **1637 passed / 2 skipped** (база
1611/2); `ruff` чист, 215 файлов; `check-wikilinks` OK; по `results/` + `config/` + `data/` +
`prompts.py` сдвинулись ровно четыре файла: пересобранный манифест и три новых.

**Ключевое для следующей сессии, уже в [[hot]]:** пин манифеста внутри прережки v1 устарел —
`2b941243…` против `80e4e12c…` — и **пере-пиннивать его запрещено**; цепочка к сегодняшним байтам
это блок `supersedes` в v2. Инструмент без строки в `WIRE_KEYS` теперь отказывается громко, а не
роняет атрибут молча. Проба v2 померила цену закона: три из четырёх кофейных стемов закон
отвергает, потому что они не префиксы ни одного «Кава …».

**В дереве появился** изменённый `docs/STATUS.md` — файл тимлида, не открывал и не редактировал.

**В память ушло:** [[a-rename-the-data-cannot-follow]] — переименование, за которым данные не могут
последовать (заголовок CSV, ключ провода), превращает поиск в легальное «отсутствует», а не в
ошибку; и дополнение к [[a-commit-must-run-its-own-suite]] про то, почему worktree здесь не
инструмент проверки.

## sku-b-prep — серв-путь позиций, всё до платного пилота ($0)

Исполнен `docs/PROMPT-sku-b-prep.md`, шесть деливераблов. Отчёт: `docs/reports/sku-b-prep.md`
(читать С ДИСКА). Указатели Dv133–Dv140 — в `implementation-notes.md`.

| коммит | что |
|---|---|
| `1131d78` | тимлид-доки без правок: SPEC 3.17 (9), бриф, STATUS.md |
| `e657db7` | перечисление маркеров стрипа выучило `sku-b-ratification-3` |
| `68c84bb` | D1 — конфиг POSITIONS и ЗАКРЫТАЯ таблица op-роутинга |
| `7bc0bad` | D3 — один `git_state`, и названный дрейф, который он скрывал |
| `0577bef` | D4 — `results/sku_pilot_serving.json`, пин до запуска |
| `c0b0ecc` | проверка порядка пина должна пережить запуск, который она ограничивает |
| `b6cf872` | D2 — драйвер, доказан end-to-end на `--smoke`, без сети |
| `a2b66bc` | D5 — префлайт выучил гарды POSITIONS, в обе стороны |
| `de9a637` | D6 — `results/sku_projection.json`, верхний угол НЕ влезает |
| `8530f97` | счётчик импортёров пересчитан: 51, а не «около сорока» |
| `9157118` | **стоп по капу внутри прогона ПАДАЛ на первом вызове и не исполнялся ни разу** |
| `2beff78`, `25f921b` | отчёт |

Числа: suite 1637 → **1734 passed / 2 skipped**, ruff чист, 97 новых тестов. Каждый коммит прогнал
СВОЙ suite на СВОЁМ чекауте (1636+1F → 1637 → 1675 → 1684 → 1694 → 1694 → 1718 → 1718 → 1728 →
1728 → 1728 → 1734 → 1734). Байт-идентичность DO-NOT-списка: 16 из 16 не сдвинулись.

**Три находки, которые стоят больше кода:**

1. **Роутер op'ов был XOR** — верен для трёх конфигов и двух op'ов, ТИХО разрешающий для четырёх и
   трёх. Измерено: старое выражение под новой матрицей пропускает 3 из 12 ячеек, которые обязано
   отклонить. `batch` на POSITIONS был бы посчитан на голой базе с потолком 800 в записи, которая
   называет POSITIONS.
2. **Стоп по капу внутри прогона не исполнялся никогда** и упал `KeyError` на первом вызове.
   `--smoke` не создаёт леджер → `budget is None` → гейт вообще не передавался в цикл; все 24 теста
   драйвера гоняли `stop=None`. Плюс счётчик был по-ножный при по-прогонных секундах, плюс формула
   заново добавляла уже оплаченный холодный старт. Найдено ревьюером, исправлено, измерено.
3. **Проекция сидит НА капе**: $0.1752 в нижнем углу, **$0.3566 в заявленном верхнем — это выше
   $0.35**. Не подкручено. Ранний стоп ломает знаменатель планки 1 (R2: ровно 108 страниц).

**Ловушка процесса:** первая попытка проверить последние четыре коммита была НЕДЕЙСТВИТЕЛЬНА —
`git checkout` падает на грязном отслеживаемом файле (правился отчёт), stderr был проглочен, дерево
не двигалось, и два разных коммита отчитались одинаковым числом. Перепрогнано с печатью HEAD и
`grep -c '^def test_'` в каждой строке. Уроки в память: [[a-guard-wired-only-when-it-can-fire]],
[[a-checkout-that-never-happened]].

hot.md: 25647 байт ≈ 6.41K ток при планке 6.0K — сказано, не спрятано (uni-b оставил 25453).

## Checkpoint 18:44

Сделано с прошлого чекпоинта (16:27): исполнена целиком фаза **sku-b-prep** — секция выше, отчёт
`docs/reports/sku-b-prep.md`, шестнадцать коммитов `1131d78..a7bf178`, suite 1637 → **1734 / 2
skipped**, ruff чист, DO-NOT-список 16/16 не сдвинулся.

После раунда с ревьюером доделано три вещи, каждая — исправление собственной работы, а не новая:

1. **`9157118`** — стоп по капу внутри прогона падал `KeyError` на первом вызове и не исполнялся
   ни разу (`--smoke` не создаёт леджер → гейт не передавался в цикл; все 24 теста драйвера гоняли
   `stop=None`). Плюс счётчик по-ножный при по-прогонных секундах, плюс формула заново добавляла
   уже оплаченный холодный старт. Исправлено, у фейка теперь синтетические часы (иначе маргинал
   ноль и гейт не может сработать в принципе), шесть новых тестов.
2. **`31fb3ff`** — в докстринге `run_leg` сказано, что два аккумулятора принадлежат ПРОГОНУ и что
   их порядок значим: перепутать их — это пустой дамп рядом с полными outcomes, и все счётчики
   ниже по течению всё равно сойдутся.
3. **`a7bf178`** — вся таблица почекоммитной проверки перепрогнана заново. Первая версия части
   строк была НЕДЕЙСТВИТЕЛЬНА: `git checkout` падает на грязном отслеживаемом файле, stderr был
   проглочен, дерево не двигалось, и два коммита с разницей в шесть тестовых функций отчитались
   одинаковым числом. Теперь каждая строка печатает HEAD и `grep -c '^def test_'` — 16 строк,
   `(нет файла)` → 24 → 30 ровно там, где коммиты это обещают.

**ВНИМАНИЕ СЛЕДУЮЩЕЙ СЕССИИ: в рабочем дереве лежит НОВЫЙ блок тимлида — SPEC 3.17 (10),
`sku-b-ratification-4`, «Cap discipline for the paid attempt».** Он отвечает ровно на то, что
отчёт вынес на решение: (a) проекция после warm-up выше остатка капа = отказ ДО первого голд-вызова,
попытка НЕ израсходована, пилот возвращается за v3-регистрацией по измеренной цене; (b) стоп
посреди ноги — находка про КАП, а не про инструмент: планку не закрывает, тихий пере-прогон
запрещён; (c) ни один джоб не должен уметь выбить остаток капа в одиночку.

Из-за него **suite сейчас КРАСНЫЙ ровно на одном тесте** — `test_sku_prereg.py`'s перечисление
маркеров знает три имени, а в файле их четыре. **Пин прережки НЕ пострадал**: стрип
name-agnostic, `973c8789…` воспроизводится, (10) вне закона — проверено. Лечится ровно так же, как
`-3` в step 0.2 этой фазы: одна строка в перечислении. Это работа step 0 следующего брифа, не
чекпоинта; `docs/SPEC.md` не трогал и не коммитил.

Урок в память: [[a-law-that-grows-loudly]] — стрип обобщён нарочно, перечисление нарочно НЕ
обобщено. Плюс два из фазы: [[a-guard-wired-only-when-it-can-fire]],
[[a-checkout-that-never-happened]].

**И приёмка пришла в ту же сессию: sku-b-prep ПРИНЯТ, в дереве `docs/PROMPT-sku-b-prep-fix.md`** —
шесть фиксов money-path (авторитет 3.17 (10) + security-ревью приёмки: 1 блокер + 4 нита).
F1 `JOB_TIMEOUT_S` 900 и TIMED_OUT кончает прогон; F2 go/no-go после warm-up; F3 падение
`spend_now` не теряет запись; F4 раздельный smoke-редирект; F5 `--project-stop-usd` только
ужесточает; F6 idle-хвост ~$0.018 во все проекции — **больше, чем запас $0.0104 верхнего угла**,
так что «не влезает» станет хуже, и это надо сказать прямо. Плюс ADR (RECORD-долг фазы) и одна
прозаическая правка: Verify-таблица отчёта говорит 1728, факт HEAD — 1734. Исполнение — следующий
ход, не чекпоинт.

hot.md: 26676 байт ≈ 6.67K ток при планке 6.0K — сказано, не спрятано (uni-b оставил 25453).

## Sessions
- 11:44: session ended (auto)
- 11:52: session ended (auto)
- 13:17: session ended (auto)
- 13:27: session ended (auto)
- 13:58: session ended (auto)
- 14:42: session ended (auto)
- 14:58: session ended (auto)
- 16:11: session ended (auto)
- 16:28: session ended (auto)
- 18:20: session ended (auto)
- 18:49: session ended (auto)

## Checkpoint 19:26 — фикс-пасс sku-b-prep исполнен

`docs/PROMPT-sku-b-prep-fix.md` (пришёл в 18:44 вместе с приёмкой) исполнен целиком. Шесть коммитов
`77c0820..aafbb43` + отчёт `4adfd91`; suite **1637 → 1750 passed, 2 skipped**; каждый коммит прогнан
на своём чекауте (дерево предварительно застэшено — `git checkout` падает на грязном трекнутом файле,
[[a-checkout-that-never-happened]]).

**Step 0.** `77c0820` — три файла тимлида без единой правки (SPEC 3.17 (10) `sku-b-ratification-4`,
бриф фикс-пасса, вечерний STATUS). Красный на своём чекауте ПО КОНСТРУКЦИИ, как `1131d78`; `c6ab33f`
добавил одно имя в перечисление маркеров и одну `not in law`. Пин прережки `973c8789…` пере-выведен
ДО коммита. `f2d1506` — ADR [[sku-b-serving-and-cap-discipline]] + строка в INDEX: RECORD-долг фазы
(три ратификации за (9), три чтения капа (10), security-блокер за (10)(c)).

**Шесть фиксов.** F1 (блокер): `JOB_TIMEOUT_S` 1800 → 900 — при гейте только МЕЖДУ джобами один
зависший джоб мог отбиллить $0.5520 = 1.58× капа; и `serving.JobExpired` (подкласс `ApiError`,
только `TIMED_OUT` + дедлайн клиента) кончает ВЕСЬ прогон, а не батч. F2: go/no-go после двух
warm-up вызовов, каждая нога по своей мерке, отказ ДО первого голда — дамп на диск не пишется вообще
(`assert not out.exists()`), попытка не расходуется. F3: `spend_or_note` — падение чтения баланса
после платных ног больше не теряет единственную запись прогона. F4: `--smoke` редиректит `--out` и
`--record` по отдельности. F5: `--project-stop-usd` = `min(явное, остаток капа)`. F6: idle-хвост
60 с = $0.0184 вошёл в `projection()`, в go/no-go и в `write_sku_projection.py`.

**Главное число: углов над капом стало ДВА из четырёх** — $0.3580 и $0.3750 против $0.35, оба на
заявленном декод-аплифте. Значит go/no-go SPEC 3.17 (10)(a) — вероятное ПЕРВОЕ реальное событие
sku-b-run. Не подкручено; сказано в отчёте и в самой записи проекции.

**Два места, где тест едва не оказался бесполезным.** (1) На плоских часах go/no-go и внутри-ранный
гейт считают ОДНО И ТО ЖЕ число — значит любой бюджет, срабатывающий на втором, отказывает на первом,
и mid-leg путь через `main` недостижим; фейк получил `gold_seconds_per_call`. (2) `--smoke` не
создаёт леджер, поэтому F3 и F5 гоняются по ЛЕДЖЕРНОЙ ветке с подменой только `EndpointClient` и
`guard.balance` — [[a-guard-wired-only-when-it-can-fire]] второй раз за день.

Незакоммичено на момент чекпоинта: хвост vault (этот лог, `hot.md`, `index.md`) — идёт отдельным
финальным коммитом, как велит бриф.
- 19:30: session ended (auto)

## Checkpoint 19:38 — фикс-пасс ПРИНЯТ, sku-b-run АВТОРИЗОВАН

Со времени прошлого чекпоинта: коммит хвоста vault `3877543`, финальная проверка (`make check` 1750
passed / 2 skipped, `ruff format --check` чист, дерево чистое), отчёт отдан оператору путём.

**Приёмка пришла.** `docs/STATUS.md` (незакоммичено, файл тимлида): фикс-пасс принят, **sku-b-prep
ЗАКРЫТ**, Dv141–Dv147 обоснованы, сверено на диске — `make check` 1750, ownership чист, ADR
[[sku-b-serving-and-cap-discipline]] на месте, 900 с/job = $0.276 < капа, `min()`-затяжка,
idle-хвост в обеих проекциях. Тимлид принял и честное чтение исполнителя: **вероятный первый исход
платного запуска — отказ go/no-go** ($0.3580/$0.3750 против $0.35, idle-хвост $0.0184 съел запас).
Dv146 (супersеднутая строка Verify) зафиксирован тимлидом отдельно.

**Пришёл платный контракт: `docs/PROMPT-sku-b-run.md`** (untracked, не тронут — файл тимлида,
коммит-онли; его же step 8 велит закоммитить его первым). ОДНА платная сессия, кап **$0.35**, ОДНА
попытка, восемь последовательных гейтов: $0-гейты → стейдж тома `qw4nwleanc` ДО создания эндпойнта
→ НОВЫЙ темплейт ровно с тремя переменными → эндпойнт `ADA_24 / workers-max 1 / idle-timeout 60 /
execution-timeout 900` → один вызов драйвера в ФОРЕГРАУНДЕ → исход A (отказ go/no-go) или B
(вердикты через R1–R5, планка 2 — только дамп и его n) → teardown с позитивным контролем листинга
→ коммиты. Есть recovery-клауза (пере-создание после ДОКАЗАННОГО удаления) с тремя целями:
никогда два биллящих эндпойнта; никогда вторая попытка любой планки/ноги/жеребьёвки; ничего не
биллится после teardown. Отчёт → `docs/reports/sku-b-run.md`, Dv148+.

**Поправка к прошлому чекпоинту:** он был записан как «20:10», реальное время — 19:26 (по
`refresh-hot-cache`). Заголовок секции и `Last update` исправлены; ничего кроме меток не менялось.

Урок в память после коммита хвоста: [[a-new-gate-can-subsume-the-old-one]] — гейт, добавленный
ВЫШЕ существующего, на однородной фикстуре считает то же число и делает старый недостижимым; лечится
одной степенью свободы у фейка, а не подкруткой бюджета в тесте.
- 19:41: session ended (auto)

## 20:30 — sku-b-run: единственная платная попытка проведена, стоп по капу на 17/138

Отчёт: `docs/reports/sku-b-run.md` (коммит `b0232fc`). Восемь гейтов пройдены по порядку;
teardown доказан позитивным контролем; том `qw4nwleanc` остался, ничего не биллится.

**Исход — ни A, ни B.** go/no-go SPEC 3.17 **(10)(a)** ПРОПУСТИЛ ран (проекция $0.1936 против
$0.3403 остатка), а ин-ран гейт **(10)(b)** остановил его после первой страничной джобы — 17 из 138
голд-вызовов. Ни одна планка не оценена: планка 1 — 17 из зарегистрированных 108 страниц (R2 держит
знаменатель), планка 3 — 0 из ≥20 строк, планка 2 — дамп и `n = 13`, но с 17-страничного префикса,
и «SCORED» по R4 решает тимлид. Мяч у тимлида.

**Главная находка — про ПРОБУ, а не про кап и не про инструмент.** Warm-up мерил синтетическую
картинку 64×64 (1.436 с) и строку (0.756 с); реальная страница листовки стоит **5.0772 с/вызов,
×3.54**. Арифметика гейта была верна (бут 391 с = $0.12 лёг на `info`, допущение F2 подтвердилось),
врал вход. Урок: [[the-probe-must-cost-what-the-run-costs]].

**Деньги:** сессия $0.1965 (floor, перечитан в 18:23:45Z), фаза 4 — $22.7780 из $25.00.
Три показания цены (баланс-дельта $0.0943 на момент записи, billed-seconds $0.1472 — то, чем
оперировал гейт, и якорь-дельта $0.1965) названы в отчёте порознь. **$0.3540 > $0.35 и без вычета
стейдж-пода**, поэтому якорь Dv149 остановку НЕ вызвал.

**Девиации Dv148–153:** запуск через `nohup` (одобрен оператором; ex post не понадобился — ран уложился
в 538 с и влез бы в один foreground-вызов); якорь капа до стейдж-пода (одобрен); стейдж-под RTX 2000
Ada $0.24/ч вместо ~$0.53/ч; ложный побочный якорь `spend_sku-b.json` от `--step sku-b` (дефис против
подчёркивания) — прочитан и удалён неотслеженным; **overwrite-гард стоял ВЫШЕ `$0` dry-run** и покраснил
сюиту ровно в момент появления платных артефактов (`dffd8d3`, оба контроля в отчёте); `cost.jobs: 8` в
записи — это ПЛАН (7+1), а исполнено 4 джобы — артефакт НЕ правился.

**Не измерено и это важнее всего для v3:** текстовый голд-маргинал. Текстовая нога не сделала ни
одного вызова.
