---
type: daily_log
date: 2026-08-08
tags: [daily]
---

# 2026-08-08

## Summary

Самый длинный день фазы: **шесть контрактов, $2.3939 потрачено, и оба открытых вопроса рантайма
закрыты измерением.** Продолжение [[2026-08-07]]. Утро и день — Telegram за $0; вечер и ночь —
serverless-трек, четыре сессии подряд.

**5c1 day-2 (`docs/PROMPT-5c1-day2.md`, $0).** Стена FloodWait открылась в 10:02:05 UTC, десять
шагов плюс две дозаявки от оператора. Реестр **39 → 67**, после приёмки (@dikankaa вон, ru 1.00 +
РФ-география) — **66 = launch 59 + watch 7**. Три «городские ленты» оказались чатами, и починка
вышла крупнее дыры: @h_kremenchug на 137 221 подписчика против чата на 16 056, причём **три из
четырёх взятых не попадали в 119-кандидатный скан по названиям городов вообще**. Окно закрыто:
**9 393 поста и 4 880 комментариев** по 63 каналам, 0 битых строк, `shasum -c` 6/6; очередь для
5c2 — **16 218 строк**. Два канала, которых утром не существовало даже как хендлов, дают **81%
собранного**. `mothers_kids` перестал быть пустым сегментом (3 против 0).

**Yield-скрин (`docs/PROMPT-5c1-yield.md`, $0).** **Скрин отказался отчитываться, и отказ
честный:** позитивный контроль упал на @atb_market_official — в его 28 днях 25 постов, 19 только
картинки, ни молочки, ни бренда. `verdicts_reportable: false`, exit 1, запись при этом полная.
66 источников: бар A взяли 29, бар B — 8, ниже обоих 36, **но оценить можно только 24** — у
девяти 0 постов в окне, у трёх постов меньше самого бара, и семь из этих двенадцати это весь
`watch` целиком. «варто» (частная марка АТБ и обычное слово) перебивает все настоящие бренды:
89 постов в 24 каналах. Ничего не патчил — лексикон `draft-not-law`, watchlist оператора.

**Caption-пилот ($0.0180 из $0.10).** Провалившийся контроль был про модальность, и чтение
картинок отвечает: АТБ идёт **0 → 13**. Слепота корпуса — 2.8%, распределена неравномерно. Пилот
заплатил **вдвое против проектной ставки** ($0.000948/пост против $0.000483 у 4.5g2) — обе цифры
оператору.

**srv-2a ($0), srv-2b ($0.9999 из $4.00, ABORT).** srv-2a — конфиг воркера и раннбук, принят с
десятью отклонениями против нормы 0–4 (перегруженный контракт). srv-2b поставил том `qw4nwleanc`
(100 ГБ, EU-RO-1), доказал **24 ГБ хватает** (19 874 из 24 564 MiB, 8/8 строк карва на поде за
2.198 с/строку) и **упёрся в handshake-таймаут**: джоба висела `IN_QUEUE` все 1800 с, консоль
пуста. Вывод сессии — «виноват наш контейнер» — был **неверным**, и это выяснилось за два часа.

**srv-2c + оплаченный оператором контроль ($0.1377 из $0.75).** Обёртка старт-команды в редирект
на том дала бут-лог: контейнер стартует, джоба возвращает **COMPLETED** за 158 с. А контроль —
**необёрнутая, ровно srv-2b'шная argv, двадцать минут спустя, тот же том, регион и класс** —
тоже COMPLETED. Значит обёртка не была починкой, а зависание srv-2b было **платформой, а не нами**.
Одна сессия диагноза отменена за $0.03.

**srv-2d — паритет ($1.2383 из $2.00). Единственная попытка 3.11 (2) потрачена и прошла.**
758/758 строк, ноль отказов любого рода. Все гейты, проходившие на 4.5h2 (G1b, G1d, G1e),
проходят; **худшее движение head'а против подового чтения той же конфигурации — +0.0000**, никто
не упал, G1c +0.0018 и G1e +0.0133. Построчное согласие с подом **751/758 = 99.08%**. G1a валит
бар ровно как на поде и на 4.5h2, до тех же шестнадцати знаков.

**Находка дня — цена, а не гейты.** `$1.4281/1000 строк` против подового `$0.5993` — **2.38×**;
`$1.0825` за проход против `$0.4611`. Причина измерена: **4.262 с/строку против 4.071** (медленнее
на 4.7%) при **$1.1041/ч эквивалента против $0.53/ч**. Дело в цене железа, не в скорости модели.
Два независимых чтения сходятся в пределах 1%.

## Decisions

Длинная форма по 5c1 — [[5c1-day2-composition-and-search]] и [[5c1-relevance-floor-and-discovery]];
по рантайму — [[srv2-serverless-runtime-target]].

- **Рулинги гейта дня 2** (канон `docs/CHANNELS-launch.md`): три супергруппы вон · четыре флага
  «группа закрыта» сняты как неотносящиеся к бакету · @tadaua вон · @lab_of_childhood в watch ·
  @pavlushaiyava и @mandziak входят. Плюс дозаявки №9 (@KarlivkaLive) и №10 (шесть хендлов).
- **@dikankaa исключён на приёмке**, три RF_FLAG оставлены с построчным рулингом
  «war-news-explained» — реестр **66**.
- **Отказ перезаписи** для переписи, market-скрина и yield-скрина: их записи — улики рулингов, и
  повторный прогон физически пишет таблицу без строк, на которые ссылается рулинг.
- **`below_both` разделён на gradeable (24) и not_gradeable (12)** — строка, у которой окно
  меньше бара, валит бар арифметикой, а не содержанием; бар при этом не двигался.
- **Паритет 3.11 (2) исполнен и НЕ добавлен в `results/baselines.json`** — измерение паритета не
  якорь гейта; подового чтения от 06.08 там тоже нет. Вердикт вшит в `results/parity_srv2.json ::
  parity` механически, включая пункт «0.005» (`parity_verdict_5b.py --vs-pod`).
- **Транспорт паритета — одна джоба на слайс, форвард-батч 1.** Куплен контроль до попытки: те же
  8 строк карва через оба транспорта, **8/8 побайтово одинаковых ответов**.

## Next

**Рантайм решён — serverless.** Тимлид принял srv-2d той же ночью (`docs/STATUS.md`: «ТЫ ЗДЕСЬ —
serverless ДОКАЗАН (паритет Δ=0); 5c1 снят с HOLD»): паритет сверен по артефактам, рулинг 23
исполнен, **5c1 снимается с HOLD**, vis-контракты пере-издаются под эндпоинт. Находку про цену
приняли **как находку против выбранного пути** — она уходит в бриф 5c2, а не спорится: их чтение
тех же артефактов ($1.0825 за проход против $0.4611, ×2.35) совпадает с `results/srv2d_cost.json`
(отношение на 1000 строк — 2.38, другой знаменатель, оба верны). Экономия scale-to-zero между
проходами реальна и по-прежнему **не измерялась**.

**Следующая сессия: vis-a / vis-b на эндпоинте** (подписи листовок) → скрин v2 → подпись состава.
Долг тимлида на утро, отложен сознательно: ADR закрытия программы srv-2 и квиз оператора.

Ниже по треку 5c1: launch-подпись всё ещё за оператором, на числах v2; дискавери-сессия —
отдельный будущий контракт (3.12 (2)); кандидатский запас не задействован
(`results/harvest_mothers_ua.json`, пять непринятых городских аналогов, резерв №4).

## Blockers

**Ничего технического.** Telegram-стена не возвращалась (~100 резолвов, 0 FloodWait — потому что
каждый прогон нёс `--only`). Аккаунт RunPod чист: `pod list -a` → `[]`, `serverless list` → `[]`;
стоит только том `qw4nwleanc` как run-rate строка. Phase 4 — **$20.8844 из $25.00, остаток
$4.1156** (чтение 22:34:28Z; итог ещё оседает — Dv33).

**Открыто и записано, не разгадано:** 4090-**pod**-строка на $0.5098/2470 с, которую никто не
создавал (Dv38), и 30-секундная A4500-строка от srv-2b. Ни та ни другая не двигают ни одного
числа — гвардия берёт максимум из дельты баланса и реестра, и связывает дельта.

## Вечер — yield-скрин (`docs/PROMPT-5c1-yield.md`)

**Скрин отказался отчитываться, и отказ честный.** Пре-регистрированный позитивный контроль —
четыре исходных канала реестра — упал на **@atb_market_official**: в его собственных 28 днях
25 постов, **19 из них только картинки**, а шесть текстовых — Fairy, скидки по карте, товары
собственных марок. Ни молочки, ни мороженого, ни бренда из watchlist. Матчер тут ни при чём: по
всей истории канала (777 постов) он находит трек-категорию **34 раза**. `verdicts_reportable:
false`, exit 1, запись при этом полная — отказ забирает право ЧИТАТЬ колонку вердиктов, а не
числа.

**66 источников: бар A взяли 29, бар B — 8, ниже обоих — 36, но находкой из них являются 24.**
Двенадцать строк окно не может оценить вообще: у девяти **0 постов в окне**, у трёх читаемых
постов меньше, чем сам бар, — они валят бар A арифметикой, а не содержанием. Семь из двенадцати —
это **весь bucket `watch` целиком**, уже посаженный оператором в лист ожидания ровно за это
молчание; перевалить их сюда значило бы переголосовать решение уликой, которая ничего не меряет.
`below_both` оставлен как пре-регистрированный флаг, а запись делит его: `below_both_gradeable`
(24) и `below_both_not_gradeable` (12, с причиной у каждого). Перепись эту границу проводит давно
(`NO_POSTS_IN_WINDOW`), скрин пришлось ей научить.

Пусто по существу, по сегментам: health_fitness **7 из 17** · regional 8 из 17 · baby_food 4 из 8
· supermarket_deals 3 из 4 · mothers_kids 1 из 5 · retail_official 1 из 7 · cooking_recipes **0 из
7**. Находка выживает и уменьшается: сегменты, на набор которых ушла вся фаза, всё ещё там, где
таксономии меньше всего, но не «14 из 17», а семь.

**«варто» перебивает все настоящие бренды.** Это частная марка АТБ и обычное украинское слово:
89 постов в 24 каналах, больше любого реального бренда списка. «Президент» — 33 поста, почти всё
Зеленский; «масл» + окончание «ов» — министр юстиции Маслов. Ничего не патчил: лексикон
`draft-not-law`, watchlist — оператора. Вместо этого каждая цифра раскладывается до термина с
цитатой, а строка несёт `bar_A_sole_carriers` — вычеркни термин, останется ли пропуск.
@polyakova_fitness держится на «варто» одном, @myrhorodtown — на «Президенте».

**Шаг 5 был исполнен ещё днём.** @KarlivkaLive прошёл гейт 08.08 в 10:48 («Дозаявка №9»), лежит
в реестре, окно собрано. Резолв не потрачен: **Telegram сегодня не трогали ни разу**, 0 из одного
разрешённого, $0. Арифметика брифа «66 → 67» не сходится — реестр был 67 вместе с ним, минус
@dikankaa = **66**, и 67-го нет. Подробности и остальные семь отклонений — в
`implementation-notes.md`, раздел «Deviations — PROMPT-5c1-yield».

## Checkpoint 17:13

`docs/PROMPT-5c1-yield.md` исполнен целиком, семь шагов, **восемь коммитов** `e6926ca` → `76cf382`,
`make check` 1201 → **1231 passed**, `$0`, **ноль обращений к Telegram**. Долгая форма —
[[5c1-relevance-floor-and-discovery]]; отклонения (девять) — `implementation-notes.md`, раздел
«Deviations — PROMPT-5c1-yield».

- **Шаг 0** — хвост дня 2 закоммичен как есть (ADR дня 2 был untracked, на него уже ссылались оба
  индекса); D73 пере-выведен, а не принят на веру.
- **Шаги 1–2** — @dikankaa вон (реестр 67 → **66**), три RF_FLAG с построчным рулингом через
  `--close`, класс «закрытых групп» записан отдельно от четырёх поимённых текстов; watchlist +3 в
  реестре и в `docs/WATCHLIST.md`, вложенность «Яготинське для дітей» ⊃ «Яготинське» разрешена и
  закреплена тестом.
- **Шаг 3** — бары A=4 / B=10 в отдельном коммите `0259939`, **до** появления скрина.
- **Шаг 4** — скрин по 66. `verdicts_reportable: false`: контроль упал на @atb_market_official
  (25 постов в своём окне, 19 из них картинки, 0 релевантных — при 34 попаданиях по всей истории
  из 777 постов).
- **Шаг 5** — не потребовался: @KarlivkaLive прошёл гейт ещё днём 2 в 10:48.
- **Шаг 6** — ADR + строка в INDEX; все числа в нём пере-выведены скриптом из записи до коммита.
- **Поздняя правка** — 12 из 36 строк `below_both` окно не может оценить (весь `watch` среди них);
  `bar_A_reach` и раздельные списки. Заголовок по сегментам исправлен во всех трёх файлах, где он
  был опубликован: health_fitness 7 из 17, не 14.

**Принято тимлидом тем же вечером** (STATUS, «Yield-скрин v1 ИСПОЛНЕН И ПРИНЯТ»). Рулинги: провал
контроля — находка про АТБ, а не про экзамен, text-only скрин системно слеп к image-first
сегментам, и по числам v1 их не режем; два канала, державшие планку на шуме, считаются НИЖЕ планки,
ограждения для «Варто»/«Президент» — в 5c3. **Подпись launch-состава и судьба 22 текстовых пустых
заморожены до скрина v2.** Очередь: `docs/PROMPT-5c1-captions-pilot.md` — ценз картинок с проекцией
цены, 19 постов АТБ через vision, кап $0.10.

## Checkpoint 18:05

`docs/PROMPT-5c1-captions-pilot.md` исполнен целиком — пять шагов, четыре коммита
(`f2af877` → `6b15ebf`), `make check` 1 241 → **1 257**, потрачено **$0.0180 из капа $0.10**.
Длинная форма шага 0 и планок — [[5c1-relevance-floor-and-discovery]].

- **Шаг 0** — рулинги оператора записаны в `results/yield_screen_5c1.json` через `--close`:
  @polyakova_fitness и @myrhorodtown **считаются ниже планки A**. `bar_A` при этом остаётся
  `PASS` — это то, что нашёл инструмент; рулинг лежит рядом, а не поверх, и несёт собственный
  `git`-блок, чтобы не подменить провенанс подписанного измерения.
- **Шаг 1** — ценз картинок по `parents.context`: 66 каналов, 9 343 поста окна, **261 нечитаемых
  (2.8%)**, 250 с медиа. Слепота неравномерна: supermarket_deals 22.6% · health_fitness 16.2% ·
  retail_official 14.9% против regional 0.5%. Сверка с подписанным скрином — построчный отказ,
  а не сноска.
- **Шаг 2** — 19 бестекстовых постов АТБ, **159 картинок, ни одного FloodWait**; популяция
  прочитана из ценза, а не выведена заново. raw v1 сверен `shasum -c` до и после — 6/6.
- **Шаг 3** — **18 из 19** подписано инструментом 4.5g2 (та же модель, тот же пин, тот же
  `caption_post`). 4350 вернул `HTTP 400: Download multimodal file timed out` и **не
  переспрашивался**: одна попытка — то, что куплено брифом.
- **Шаг 4** — матчер по text+captions: **0 → 13 релевантных из 25, планка A FAIL → PASS**.
  Строгое чтение (вычеркнуть все бренд-алиасы, включая «Своя Лінія», 12 хитов из 13) — **10
  постов** на чистой категории; `bar_A_sole_carriers` пуст. Пять промахов — акции «7 ДНІВ»
  с сумками и игрушками.
- **Поздняя правка** — Dv10: пилот заплатил **$0.000948/пост** против спроектированных
  $0.000483, вдвое, и размер альбома этого не объясняет (5.37 картинки на пост против 5.33).
  Полный прогон переоценён: **$0.22 / $0.11 за оставшиеся 232 поста**. Обе цифры в отчёте —
  проекция старше результата, переоценка им подтверждена.

**Не сделано намеренно:** полный прогон и скрин v2 — решение оператора; `verdicts_reportable:
false` на yield-скрине **не снят**, подпись launch-состава и судьба 22 текстовых пустых
по-прежнему заморожены.

**Принято тимлидом тем же вечером** (STATUS, «Пилот ИСПОЛНЕН И ПРИНЯТ: модальность, не матчер»).
Два рулинга закрытия, и один из них **правит мою рамку**:

1. **Подписи остаются на дешёвом API-пути.** Чат-сессия исполнителя как источник данных запрещена
   (числа без пина модели и цены), своя Gemma на поде дороже ($0.5+ из GPU-капа) и несравнима с
   купленными подписями. $0.22 за пачку — самый дешёвый честный путь.
2. **Private label — полноценный бренд-хит, а не «промах второго рода».** Моя формулировка про
   подгузники под «Своя Лінія» неверна как рамка: СТМ в канале собственной сети — ценнейший класс
   хита, а не шум. К приёмке v2: бренд-хиты несут флаг `private_label` из реестра и считаются
   полностью; абляция брендов остаётся побочной метрикой и ничего не понижает.

**Полный прогон АВТОРИЗОВАН:** `docs/PROMPT-5c1-captions-full.md` — 232 оставшихся поста, кап
**$0.35**, затем скрин v2 по всему реестру с тем же prereg. Подпись состава — после v2, следующей
сессией. Не начат: /save это чекпоинт.

## Checkpoint 19:16 — srv-2a: рантайм развернулся на serverless

**Вечером тимлид перевернул две вещи сразу.** SPEC 3.12 → 3.14: поправка 3.13 снимает
платный API-путь подписей в пользу собственной Gemma-4 (поэтому `PROMPT-5c1-captions-full.md`
теперь **VOID**, кап $0.35 не тронут, а `PROMPT-5c1-vis-a.md` — **ON HOLD** до парити srv-2b);
поправка 3.14 возвращает целевой рантайм на **serverless**. Основание — зонд того же вечера
(`docs/probe-serverless-20260808.md`): эндпоинт `8mkl2lbxho3bfa` на родном mock-воркере RunPod,
две задачи съедены и COMPLETED, удалён сразу после ответа. Стена 5b (`parity_verdict_5b.json`,
06.08) **не воспроизвелась** — и файл 5b остаётся нетронутым как честная запись того дня.

**Исполнен `docs/PROMPT-srv-2a.md`** — $0-сессия, ни одного вызова `runpodctl`, ничего не создано.

- **Хвост закоммичен неотредактированным:** шесть тимлидских доков (`74a5adb`) + `knowledge/*`
  (`2d2a8e8`). `git diff` тимлидских файлов показан до `git add` — стейджил по путям.
- **Воркер 5b уже serverless-конфиг.** `/workspace` не вшит ни в `serve_handler.py`, ни в
  `start_5b_worker.sh`; env шаблона и `settings()` сходятся поле в поле; путь
  `runpod.serverless.start` доказан на поде 06.08, а не предположен.
- **Одна правка, которой проверка потребовала:** конфиг A — это `peft`, накладывающий LoRA на
  NF4-базу, и **ни одна запись не называла peft, который обслуживал**. `RUNTIME_LIBRARIES` расти
  не может (якорь 4.5h2 несёт три библиотеки, четвёртая была бы guard'ом, который не срабатывает
  никогда), поэтому `library_versions()` **сообщает** peft/accelerate/runpod внутри блока
  `runtime` — там, где расширение не двигает схему 5b. Пин 0.20.0 взят из `adapter_config.json`
  самого адаптера, то есть **изнутри директории, чей sha guard и так проверяет**; строка
  `peft==0.18.0` в `runbook_5b2.md` исправлена открыто и сам рунбук не тронут.
- **`scripts/runbook_srv2b.md`** — env/config-контракт, план тома (~80 из 100 ГБ; цена **читается**
  в консоли, датацентр выбирает D7-перезамер) и процедура с лестницей аборта. Кап — плейсхолдер.
- **13 новых тестов** парсят `--env` JSON прямо из рунбука в `settings()` и пере-выводят его пины
  из записей, на которые он ссылается: документ и воркер не могут разойтись молча.
  `make check` **1257 → 1270 passed**, `ruff format --check` чист.

**Три улова ревью, все до отчёта** (Dv6, Dv9, Dv10 в `implementation-notes.md`): формула цены
прогона вписывала 3085.4 с константой — это 758 × 4.071 с/строку **на A6000**, тогда как D7
обещает 4090; при 3.0 с/строку тот же тариф делает serverless дешевле, так что «на 17% дороже»
было выводом про железо, на котором прогон не пойдёт. Скачивание 59 ГБ печатало
`snapshot_download()`, который в проекте никогда не выполнялся, — заменено записанной командой 4a.
`smoke_5b.deployment()` (ветка, ни разу не исполнявшаяся, работает **после** оплаченных строк)
проверена и чиста.

Коммиты `74a5adb` → `6388229`. Связанное: [[5b-parity-abort-and-pod-runtime]],
[[5b2-batch-measurement]].

**Принято тимлидом тем же вечером** («srv-2a ИСПОЛНЕН И ПРИНЯТ, $0, сверено»): 1270 тестов
перепрогнаны на его стороне за 32.8 с, четыре коммита, хвост подтверждён нередактированным.
Отмечено отдельно:

- **Перестановка шагов зачтена как улучшение исполнителя:** D7-чтение стоит **ДО** создания тома
  (§C.1 перед §C.2), поэтому том ляжет в тот ДЦ, где GPU реально есть. В 5b было наоборот, и
  именно поэтому том пинил CA-MTL-3, где serverless не выдавал 48 ГБ ни одного класса.
- **Десять девиаций против нормы 0–4** — списано на перегруженность контракта, а не на
  самодеятельность.
- **Dv3 поймала ошибку тимлида:** контракт цитировал смоук из прозы (`implementation-notes`), а не
  из артефакта, — те три T2-строки не сохранены нигде, и воспроизвести их нельзя.

Дальше — брифинг srv-2b и его кап. Ни тома, ни эндпоинта до него.

## Checkpoint 21:17 — srv-2b: стена пала и с томом, но наш контейнер не стартует

Исполнен `docs/PROMPT-srv-2b.md` — первая платная сессия serverless-трека, кап $4.00.
**Итог: аборт на ступени handshake-timeout, потрачено $0.9999.** Пять коммитов `066c282` → `86eb0b7`.

**Шаг 0.** Хвост тимлида закоммичен нередактированным двумя коммитами; ADR рулинга 23 / амендмента
3.14 написан — [[srv2-serverless-runtime-target]] — и внесён в INDEX. Кап $4.00 подставлен в пять
гард-вызовов ранбука, `results/spend_srv2b.json` заякорен на балансе **$16.5012 до первого платного
действия** и закоммичен: свидетель порядка — история git, а не слово сессии. Тест-плейсхолдер из
srv-2a переписан: ранбук больше не обязан быть пустым от капа, но обязан не нести кап, которого не
ставил контракт.

**§C.1 — ДЦ выбран бесплатными чтениями.** GraphQL `dataCenters{storageSupport}` вернул **ровно 18
из 49** — цифра самого ранбука, пере-выведенная, а не принятая на веру. Скрещено с
`gpu list :: dataCenterAvailability`. Взят **EU-RO-1**: единственный ДЦ с томами, у которого
*запасной* класс имеет реальный сток (единственная `Medium` 4090 из восемнадцати). У US-NC-1 и
US-TX-3 был L40S `Low`, но 4090 `none` — отказ 48-ГБ класса там оставил бы том в регионе, где
позади ничего, ровно как стена 5b в CA-MTL-3.

**§C.3 — под на классе сервинга, и это окупилось.** RTX 4090, $0.74/ч. 59 ГБ легли за ~2 минуты,
пины сошлись с якорем 4.5h2, peft вышел **0.20.0** — ровно как srv-2a предсказала из
`adapter_config.json`. Смоук по петле: **8/8 распарсились, 19 874 из 24 564 MiB, холодный старт
175.791 с с сетевого тома, 2.198 с/строка против 4.071 у A6000**. Ступень OOM ЗАКРЫТА: GM4 NF4
конфиг A влезает в 24 ГБ с запасом ~4.6 ГБ и идёт в 1.85 раза быстрее. Пиковой VRAM для batch-1 не
было ни в одной записи репозитория до этой.

**§C.5 — ступень.** Джоба простояла `IN_QUEUE` весь 1800-с хендшейк при одном *running*-воркере и
нуле джоб в работе; в консоли **ноль строк лога за 35 минут**; воркер перезапустился через 26 минут.
Контроль вендорским `runpod/mock-worker:dev` **на том же томе, ДЦ и классе** довёл джобу за
delay 6 477 мс + exec 144 мс. Значит стена 5b **не вернулась** — и это новое сверх пробы тимлида,
которая шла без тома и сама пре-регистрировала эту дыру ([[srv2-serverless-runtime-target]]).
Виноват наш контейнер, и отказ — **до первого print**: serverless-путь job-loop не исполнялся в
проекте ни разу, под-пруф srv-2a гонял HTTP-режим того же файла.

**Паритет НЕ бежал.** `results/parity_srv2.json` не существует, тест v4 не открывался, единственная
попытка 3.11 (2) **не израсходована** — упал смоук на carve арма. Запись D7 —
`results/d7_reread_srv2b.json`, с манифестом тома и явным «48-ГБ половина сегодня не спрашивалась».

**Побочно найдено и починено:** `smoke_5b.py` падал ПОСЛЕ записи рекорда на относительном
`--record`; `volume_calc_5c1.idle_rate_bound` ходил по живому леджеру и от моих же гард-записей
получил «самый тихий» интервал, в котором тома не было вовсе ($0.0947/день) — теперь останавливается
на `generated_at` записи и пере-выводит замороженные $0.2528. Гард слеп на serverless: недосчёт
$0.86, кап держала только баланс-дельта.

Том `qw4nwleanc` оставлен как run-rate строка; цена **не прочитана** — ни у API, ни у биллинга её
нет, консоль не отрендерилась. Ретрая не делал, ни одна ступень его не авторизует.

**Принято тем же вечером.** Тимлид сверил аборт по артефактам и листингам: ступень признана,
диагноз (отказ наш, до первого print; job-loop-режим в проекте не исполнялся ни разу) принят,
попытка паритета зачтена как **непотраченная**. Отмечено купленным: закрытый OOM, пере-прочитанный
D7, пины и хеш адаптера против якоря, `workersMin 0` доказанный листингом. Слепота гарда на
serverless-биллинг (−$0.86) уходит в починку следующим шагом; консоль, к слову, **не отрендерилась
и у тимлида тоже** — пере-проверка листингов бесплатно на старте следующего шага.

**Дальше — srv-2c, кап $0.75** (`docs/PROMPT-srv-2c.md`, го оператора): ровно предложенная
boot-лог-диагностика — старт-команда воркера льёт вывод на том, одна джоба, ≤10 минут, лог
дословно вычитывается подом в артефакт. Дизъюнкция чистая: **файла нет** = команда не исполняется
под стоковым образом (тогда свой образ — отдельным брифингом); **файл есть** = в нём написано, где
умерло.

## Sessions
- 09:01: session ended (auto)
- 09:09: session ended (auto)
- 15:13: session ended (auto)
- 16:33: session ended (auto)
- 17:15: session ended (auto)
- 17:46: session ended (auto)
- 18:05: session ended (auto)
- 19:09: session ended (auto)
- 19:19: session ended (auto)
- 21:08: session ended (auto)
- 21:21: session ended (auto)

## srv-2c — бут-лог: контейнер стартует, и джоба возвращает COMPLETED

`docs/PROMPT-srv-2c.md`, кап $0.75, потрачено **$0.0654**. Запись — `results/srv2c_bootlog.json`,
лог внутри непорезанный.

**Ответ обратный тому, к чему вёл аборт srv-2b.** Обёрнутая старт-команда
`bash -c 'exec bash /runpod-volume/start.sh > /runpod-volume/worker-boot.log 2>&1'` оставила на
томе 20 827 байт (sha `4a48f32a…`): `Starting Serverless Worker | Version 1.11.0`, семь
fitness-проверок за 3 993 мс, `Jobs in queue: 1`, `Started.`, 1 188 шардов весов за 93 с,
`Finished.` Джоба `48a3c742-…-e1` — **COMPLETED**, `delay 15.9 с · exec 158.4 с`. Её `info` прогнан
через `assert_serving` и `assert_runtime_matches` с негативным контролем — обе прошли. Ветка
job-loop из `scripts/start_5b_worker.sh`, которая в проекте не исполнялась ни разу, работает.

**Чего сессия НЕ установила — почему висел srv-2b.** Различий два, а не одно: обёртка и сутки
платформенного времени. Верхняя гипотеза — блокирующая запись в недренируемый stdout (`python -u`
пишет прямо в fd 1, а консоль тем вечером не рендерилась ни у кого). Разделяющий эксперимент —
один эндпоинт с **необёрнутой** командой и одна джоба, **≈$0.06**; не куплен, это решение
оператора.

**Умерло бесплатно на том же поде:** `/runpod-volume/start.sh` побайтно равен репозиторному
(`5b3bcbb2…`, mode `-rwxrwxrwx`, 0 CRLF), и `Entrypoint nvidia_entrypoint.sh` образа действительно
исполняет свой CMD. Том обновлён `48948d7a` → `cf4cf71`, дерево чистое.

**Побочно закрыт Dv13:** цена тома осела в биллинге — `$0.009722222574` за 100 GB = ровно
`$7.00/720 ч` → **$0.07/GB/мес, $0.2333/день**. Прежние `$7.20/мес` были прайором и завышали на 3%.
Замороженный `results/volume_calc_5c1.json` намеренно не тронут. Dv18 поправлен в открытую: ставка
подтверждена дважды ($0.000307/с), а длительность в ledger'е читается как ≥$0.35 и ещё оседает.

Гард научен видеть serverless-биллинг (`37720fc`, негативный контроль прогнан). Все три листинга
пусты, кроме тома. См. [[srv2-serverless-runtime-target]].
- 21:54: session ended (auto)

### Контроль по слову оператора: необёрнутая команда тоже отвечает

Оператор купил разделяющий эксперимент прямо на отчёте. Эндпоинт `0nfuzwkyd9tp39`, шаблон
`pzay2h6m5j`, argv **ровно srv-2b'шный** `["bash","/runpod-volume/start.sh"]`, одна джоба, через
двадцать минут после обёрнутого прогона на том же томе, DC и классе: **COMPLETED**,
`delay 26.9 с · exec 212.0 с`, тот же `info`.

**Вывод по предзарегистрированному правилу, без интерпретации: висел srv-2b из-за платформы, не
из-за нашего контейнера и не из-за старт-команды.** Гипотеза «блокирующая запись в stdout» понижена
— она может описывать 08.08, если тогда лежал log-plane, но постоянным свойством запуска без
редиректа не является. Контроль чист по коду: чекаут тома сдвинулся `48948d7a` → `cf4cf71`, но диф
по `serve_handler.py`, `start_5b_worker.sh` и `src/market_pulse/` **пуст**.

Записано, но findings'ом не названо: необёрнутый исполнялся на 34% дольше (212.0 против 158.4 с) —
n=1 против n=1 на разных воркерах. Редирект всё равно оставить: канала логов воркера вне консоли у
RunPod нет.

srv-2c закрылась на **$0.1006 из $0.75**. Всё удалено, доказано листингом.
- 22:22: session ended (auto)

## Checkpoint 22:31 — srv-2c исполнена, контроль куплен, srv-2d выписан

Со времени чекпоинта 21:17 закрыты обе платные вещи вечера и принят вердикт тимлида.

**srv-2c ($0.1377 из $0.75, [[srv2-serverless-runtime-target]]).** Ответ на единственный вопрос —
обратный тому, к чему вёл аборт: `bash /runpod-volume/start.sh` исполняется как главный процесс,
бут-лог на томе (20 827 байт, sha `4a48f32a…`) держит полный старт SDK, джоба вернула **COMPLETED**
(`delay 15.9 с · exec 158.4 с`), и её `info` прошёл `assert_serving` + `assert_runtime_matches` с
негативным контролем. Две гипотезы умерли бесплатно на поде: `start.sh` побайтно равен
репозиторному, а `Entrypoint nvidia_entrypoint.sh` образа исполняет свой CMD. Том обновлён до
`cf4cf71`, дерево чистое.

**Контроль, купленный оператором ($0.03).** Необёрнутая команда — точный argv srv-2b — тоже
**COMPLETED** (`delay 26.9 с · exec 212.0 с`). Значит обёртка никогда не была причиной, и **вина
srv-2b — транзиент платформы, не наш контейнер**. Чистота по коду проверена: диф
`48948d7a..cf4cf71` по `serve_handler.py`, `start_5b_worker.sh` и `src/market_pulse/` пуст.
`d7_reread_srv2b.json` задним числом не переписан — поправка живёт в `srv2c_bootlog.json`.

**Побочно:** цена тома осела ровно в **$7.00/мес** ($0.07/GB/мес), что закрыло Dv13; гард научен
видеть serverless-строки биллинга; Dv18 поправлен в открытую; Dv33 — баланс-дельта отстаёт от
ресурса ($0.1006 → $0.1377 при пустых листингах), так что цифра конца прогона это нижняя граница.
Записи выше в этом файле называют $0.1006 — это чтение того момента, итог **$0.1377**.

**Принято тимлидом, и следующий шаг выписан: `docs/PROMPT-srv-2d.md`, паритет 758 строк батчом 1 на
живом эндпоинте, кап $2.00 из ~$5.35 GPU-остатка.** Это тратит единственную попытку 3.11 (2):
ретрая нет ни при каком исходе, провал гейта — это finding с обоими чтениями. Два дока RunPod
объявлены нормативными и читаются до траты; из них важное — TTL считается **от submission**,
результаты `/run` **удаляются через 30 минут** (потому построчный дамп обязан лечь на том), баг
job-tracking в SDK 1.7.11–1.10.0 (у нас 1.11.0, то есть вне диапазона — srv-2b он не объясняет), и
«логи появляются только для успешно инициализированных воркеров».

В память легли два урока: [[the-fix-that-was-not-a-fix]] и [[unreadable-now-versus-never]].
- 22:34: session ended (auto)

## srv-2d — паритет исполнен, попытка потрачена, гейты держатся (ночь на 09.08, $1.2383 из $2.00)

`docs/PROMPT-srv-2d.md`. Единственная попытка SPEC 3.11 (2) **потрачена и прошла**: 758/758 строк,
ноль отказов любого рода. Все гейты, которые проходили на 4.5h2 (G1b, G1d, G1e), проходят;
худшее движение head'а против подового чтения той же конфигурации — **+0.0000**, то есть не упал
никто, а G1c поднялся на 0.0018 и G1e на 0.0133. Построчное согласие с подом — **751/758 = 99.08%**,
это описание, а не гейт. Записи: `results/parity_srv2.json` (вердикт вшит в блок `parity`),
`results/srv2d_cost.json`, `results/serving_srv2d_smoke.json`, `results/srv2d_worker_boot.log`.

**Находка сессии — цена.** `$1.4281 / 1000 строк` против подового `$0.5993` — **2.38×**, и причина
не в скорости модели, а в цене железа: **4.262 с/строку против 4.071** (медленнее на 4.7%) при
**$1.1041/ч против $0.53/ч**. Два независимых чтения сходятся в пределах 1%: осевшая посекундная
ставка самого эндпоинта на измеренных секундах, и дельта баланса. Dv33 делает обе цифры полом.
Это противоречит экономической части SPEC 3.14 — и сессия по этому поводу **ничего не решает**:
уходит оператору так же, как ушёл бы упавший head, с обоими чтениями и без полномочий.

**Что куплено до попытки, чтобы попытка не была слепой.** Контроль «одна джоба»: те же 8 строк
карва через оба транспорта, **8/8 побайтово одинаковых ответов** — на тренировочных данных, без
экспозиции теста. Дампы с тома проверены, а не приняты на веру: 400/250/108 строк, индексы по
порядку, и хеш текста каждой строки равен хешу строки тест-сета на том же индексе. Первая строка
бут-лога — `runpod SDK 1.11.0`: новый boot-ассерт отработал в проде.

**Шесть отклонений (Dv34–Dv39)**, из них два стоят внимания. Policy в брифинге написана в секундах,
а нормативная страница RunPod специфицирует **миллисекунды** — литерал `3600` означал бы 3.6 с, ниже
их же минимума в 5 с. И стейджинг: `git fetch` по несуществующему рефу **не очищает FETCH_HEAD**,
поэтому следующий `merge --ff-only` слил прошлогодний указатель и напечатал `Already up to date` —
том остался на старом коммите за минуты до платного прогона, поймал только хеш-контроль. Урок лёг
в native memory: [[a-failed-fetch-leaves-the-old-ref]].

Всё созданное удалено и подтверждено листингом; стоит только том `qw4nwleanc`. Phase 4 —
**$20.8793 из $25.00**, остаток $4.1207.
