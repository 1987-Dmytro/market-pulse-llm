---
type: daily_log
date: 2026-08-15
tags: [daily]
---

# 2026-08-15

Начался цикл-2. Первый из двух $0-контрактов подготовки исполнен: правило SPEC 3.19 вшито в очередь
инференса, проекция батч-паритета написана из записей. Потрачено **$0.00**.

## Summary

### Сеанс 1 — `cycle2-prep-a` ($0): скип-правило 3.19 и проекция батч-паритета

Отчёт — `docs/reports/cycle2-prep-a.md`, отклонения **Dv330–Dv334**. `make check`
**2 347 / 2 skipped**, дерево чистое, `data/loop_cursor.json` не двигался
(`9b59aa5f…` до и после).

**Deliverable 1 — правило очереди.** Единственный текстовый предикат поднят из инлайна
`scripts/window_summary_5c2.py:198` в **`market_pulse.loop.has_text`**; тот же файл теперь его и
вызывает, второго определения нет. `strip()` vs `== ""` закрыт эмпирически: оба предиката дают
ровно **1 361** и на отправленном тексте, и на исходном сторе, расхождений 0 — амендментное «zero
lost in the pipeline» воспроизвелось независимо. Скип живёт в двух местах, где строится
оплачиваемая очередь (`plan_channel`, `queued`); `loop.queue_depth` не тронут, потому что им
считает перепись и это записано в `results/census_5c2.json`.

**Три красных теста, которых контракт не предусмотрел (Dv330).** Правка `loop.py` двигает sha,
который держат ДВА запечатанных рекорда (`window_summary_5c2.json`, `validate_5c2_pack.json`), и
клаузула «один предикат» тащит за собой `window_summary_5c2.py` — тоже запиненный. Рекорды НЕ
перепинены: гварды восстанавливают байты через `git show d69c812…:` и проверяются в обе стороны —
домашний паттерн из `tests/test_prereg_5c2.py`, ему один контракт от роду. Второе появление класса
[[a-comment-only-edit-moves-the-files-hash]].

**Deliverable 2 — проекция.** `results/batch_cycle2_projection.json`, продюсер
`scripts/projection_batch_cycle2.py`. Из записей выводится НЕРАВЕНСТВО: шаг с batch 16 требовал
23.99 ГиБ сверх весов, у 4090 24 ГБ их 3.99 — короче в 6.02×; 16/8/4/3 исключены, кандидат один —
**batch 2**, и влезет ли он — **НЕ ИЗМЕРИМО** без живого пробника. Главное открытие не про память:
при batch 16 разошлись **6 строк из 666**, тогда как на 24-строчном карве все размеры были
байт-в-байт — значит новая регистрация обязана мерить ИДЕНТИЧНОСТЬ на популяции. Пробник: фикс
$0.1520 + $0.0013025/строка; арм 758 с контролем $2.1266. Окупаемость −30%: $4.3536 на истории
11 143, $1.4511 на одном окне после 3.19. **Само правило 3.19 уже сэкономило $1.7727 на окне и
бесплатно.** Долю 26.8% на историю НЕ переносил — другая выборка.

**Найдено при самопроверке уже после коммитов:** `run_5c2.restrict` — просто фильтр, и между ним и
деньгами никто не сравнивает длину очереди с длиной запечатанного населения. Регистрация на 5 075
будет встречена очередью в 3 714 и упрётся в `assert_populations` ПОСЛЕ трат. Не патчил — денежный
путь не этого контракта; поставил красную линию тестом и вынес в `hot.md` и §1.5 отчёта.
Урок — [[a-narrowing-rule-shrinks-the-sealed-population]].

**Прочее:** Next #7 (приёмка `5c2-close` / Dv327) закрыт — тимлид ратифицировал маршрутизацию
`[model]` в `.claude/rules/`. Мидсешн прилетел файл тимлида `docs/PLAN-phase6-command-center.md`
(командный центр Фазы 6, черновик на утверждение оператора) — закоммичен по пути, не правлен
(Dv334).

## Sessions

- **`cycle2-prep-a`** — $0, 9 коммитов: `2b74cdf` (хвост 14.08 + контракт), `8d47fb3` (D1),
  `574915b` (D2), `59618b2` и `7cef9c4` (две самопроверочные находки), `2bf4014` + `27fb09a`
  (отчёт), `5427481` (план тимлида), `dfe8a8a` (hot.md).

## Next

- Приёмка `cycle2-prep-a` тимлидом; следом `cycle2-prep-b` — докачка корпуса + листовки
  Сільпо/Varus.
- Денежная линия батч-паритета — открытое решение оператора (см. проекцию).
- Свободный подсчёт: сколько из 11 143 строк истории бестекстовые. Ни один якорь не отвечает.
- 11:20: session ended (auto)

## Checkpoint 12:18 — `phase6a`: слой агрегатов, экспорт и словарь метрик ($0)

Отчёт — `docs/reports/phase6a.md`, отклонения **Dv335–Dv340**. `make check` **2 369 / 2 skipped**
(база 2 347), `data/loop_cursor.json` не двигался, потрачено **$0.00**. Семь коммитов:
`97add7e` (хвост) · `8513191` (амендмент 3.20) · `63676db` (D1) · `6043d95` (D2+D3) ·
`981c5ad` + `a7fd072` (две самопроверочные находки) · `3a16b55` (отчёт).

**Step 0 — 3.20 приземлён, но не так, как написано в контракте (Dv335).** Блок
`amendment-index`, который контракт велел обновить, ЛЕЖИТ ВНУТРИ запечатанного пина: он один из
десяти имён `write_prereg_5c2.KEEP_BLOCKS`, и правка выводит закон в `a39c05d5…` против
запиненного `3dd43923…`. Замерено на копии ДО первой правки — обе ветки. Пошёл маршрутом 3.19:
индексная запись живёт внутри самого блока 3.20 и вслух говорит, что указатель отстал уже на два
амендмента. Четвёртая подвижная деталь, которую контракт не назвал, — `BLOCKS_TODAY` (Dv336); её
собственный докстринг это предсказал. Все четыре пина SPEC деривируются, ни один рекорд не
перепинен. Класс — [[a-comment-only-edit-moves-the-files-hash]], третье появление.

**D1 — `data/derived/pulse.db` (gitignored).** Шов взят ИМПОРТОМ, не подъёмом (Dv337): подъём
ридеров в `src/` добавил бы КЛЮЧ в `producer.borrowed` двух запечатанных рекордов, а подстановка
sha спасает сдвинутый дайджест и не спасает отсутствующий ключ, пока `results/` заморожен. Урок —
[[a-lift-adds-a-key-a-move-only-changes-one]]. Гейт сделан исчерпывающим: `aggregates.mirror()`
пересобирает все пять блоков якоря из SQL, **902 из 902 числовых листьев сходятся**, 0 лишних, 0
пропущенных, и это оказалось МЕНЬШЕ кода, чем ручная карта пар —
[[mirror-the-whole-anchor-not-the-overlap]].

**D2/D3 — экспорт `results/dashboard_data_w1.json` (108 КБ) и `config/metrics.yaml`.** Две пары
экспорта байт-в-байт, закоммиченный файл равен свежесгенерированному. 30 объявленных общих чисел +
902 листа. У каждой метрики назван её сэмпл: SoV меряется на **11 строках из 5 075**, и это
написано рядом со значением. Восемь метрик UA/EN, биекция со словарём в обе стороны, семь
`NOT_COMPUTABLE`-заглушек с условиями разблокировки. Маркетопт (3.20 (6)) на промо-поверхности с
4 позициями, и тест грепает `@marketopt_promo` из самого блока 3.20.

**Что слой сказал про окно-1 (раньше никто не мерил).** Все **1 361** бестекстовых строк размечены
`neutral` — все до одной, ни одного позитива, ни одного негатива. Значит «neutral 4 144» в купленной
выборке содержит 1 361 ответ ни о чём, а NSR двигается **0.0770 → 0.1053 (+37%)** при переходе на
платимую выборку. Это самый сильный аргумент за 3.19 (2), какой у нас есть. Второе: **`retail_official`
— единственный сегмент с отрицательным NSR** (−0.1111 на 234 строках), тогда как все community-сегменты
тёплые: жалуются там, где сеть слушает. Третье: поправка на сарказм двигает **3 строки** из 5 075
(64 из 67 саркастичных и так были негативом). Четвёртое: сегмент `regional` — 3 канала, 13 маркеров,
9 позиций и **ноль комментариев**; экран обязан отрисовать пустой разговор, а не отсутствующий.

**Самопроверка после коммитов дала два исправления:** склеенный в SQL фильтр (`f"segment = '{seg}'"`
из значения, только что пришедшего из БД) и обращение к приватному `aggregates._where` через границу
модуля — обе в `981c5ad`; и таблица `watchlist`, которую перепись писала, но не считала в выводе
(`a7fd072`, найдено при перевыводе чисел отчёта из артефактов).

### Догон 12:35 — восьмая карточка сегмента (Dv341)

Самопроверка после отчёта нашла блокер: `cuts.comment_by_segment` строился от таблицы `channels`,
которую наполняют СОБРАННЫЕ строки, поэтому `food_quality` (1 канал в реестре, ноль строк любого
типа в окне) в срез не попадал — **семь карточек там, где реестр держит восемь**, а план §3 T3 —
одна карточка на аудиторию. UI фазы 6b на старом экспорте отрисовал бы семь и не узнал бы, что есть
восьмая.

Починено таблицей-измерением `segments` из реестра, а НЕ расширением `channels`:
`coverage.channels.with_a_row` считает строки в `channels`, и вставка всех 66 хендлов превратила бы
28/66 в 66/66 — метрика убила бы себя собственным знаменателем. Три состояния теперь различимы и
запинены тестом: сегмент говорил · сегмент нёс промо-улики и ноль разговора (`regional`) · сегмент
молчал (`food_quality` — нули, `registry_channels: 1`, пустой `channels_with_a_row` и **null**, а не
`0.0`, в rate). Коммиты `bc6d2bd` + `dc6a88e`, `make check` **2 370 / 2 skipped**.

Острое в находке: тот же аргумент был выигран в этом же контракте ДВАЖДЫ — таблица `watchlist`,
чтобы неупомянутый бренд был нулём, и `PROMO_CHAINS`, чтобы сеть из закона стояла ключом независимо
от строк, — и не был перенесён на сегменты. Урок — [[a-dimension-belongs-to-the-registry]].
- 12:32: session ended (auto)

## Checkpoint 13:38 — командный центр собран (`phase6b`, $0)

Исполнен контракт `docs/PROMPT-phase6b.md`. `scripts/build_dashboard.py` → **`dashboard/index.html`**
(502 765 байт, закоммичен): девять вкладок T0–T8, UA/EN и светлая/тёмная переключателями, всё
инлайном, **ноль внешних запросов** (единственные внешние ссылки — t.me у строк drill-down), экспорт
вложен в страницу байт в байт. Новый файл строк — `config/ui_strings.yaml` (D4). `make check`
**2 392 / 2 skipped** (база 2 370, +22 теста), потрачено $0.00, под `results/` не сдвинут ни байт.
Отчёт — `docs/reports/phase6b.md`, отклонения Dv342–Dv352.

**Шов честности:** число попадает на страницу только из экспорта или из арифметики над его же
значениями, посчитанной на билде. Там, где поля нет, — заглушка, и заглушки ДВУХ классов, намеренно
не слитых: семь `NOT_COMPUTABLE` самого экспорта («это ещё нельзя посчитать») и четыре `GAPS`
(«6a такого поля не экспортировал») — второй список и есть бэклог 6a, §4 отчёта. JS переключает,
сортирует и позиционирует; к вложенной записи он не притрагивается — оба языка каждой подписи и
каждого тултипа лежат в документе до его загрузки.

**Drill-down как гарант:** 27 раскрытий, 196 строк, 196 ссылок. Каждое объявляет ПОЛЕ ЭКСПОРТА со
своей популяцией, и билд отказывается, если найденные строки не сходятся с тем, что говорит экспорт;
жребий сидируется на стратум (`f"42:{key}"`), тест меряет ранги, а не id. Строки читаются
ЕДИНСТВЕННЫМ ридером, и каждый из 38 файлов улик сверяется со своим sha из `provenance` до разбора.

**Браузер нашёл то, чего не поймала бы ни одна текстовая проверка** (§5 отчёта): первая диаграмма
тональности рисовалась ПУСТОЙ — `metrics.nsr.by_sample` держит три счётчика на верхнем уровне блока,
а срез — под ключом `sentiment`; общий помощник угадывал форму и получал нули, без ошибки и без
красного теста. И полоса со 100% негатива проходила сквозь собственную колонку подписей. Урок —
[[the-helper-that-guesses-its-inputs-shape]].

**Самопроверка после коммита (Dv352):** вывод T0 утверждал «Єдиний сегмент із від'ємною чистою
тональністю» — правда для окна-1 и не гарантировано ничем: argmin считает код, слово «єдиний»
пришло из файла строк. Теперь правило считает, сколько сегментов отрицательны, и говорит именно это.
Урок — [[the-template-claims-more-than-the-rule-counted]].

**Dv343 — флага приватных марок нет нигде в репозитории.** План §2 называет домом флагов
`config/lexicon.yaml`; этот файл — словарь КАТЕГОРИЙ и брендов не держит вовсе, а вотчлист в
`config/registry.yaml` несёт только `own`, и четыре марки сетей отделены YAML-комментарием. Значок
на T4 — заглушка, чинится полем реестра и словом оператора. Второе попадание в
[[brief-can-outrun-the-document]].

### Догон 13:52 — второй проход самопроверки (Dv353)

Две находки, ни одна не двигает число. **Все вкладки были скрыты до запуска скрипта:**
`section.tab{display:none}`, а класс `.active` вешал только `show()` в конце инлайнового JS —
документ КАК НАПИСАН отрисовывал шапку, навигацию и пустоту. Проверяли в браузере, где скрипт
отработал, поэтому дыра не показалась; приёмка открывает файл холодным, и одна ошибка в скрипте
дала бы пустой командный центр. Первая вкладка теперь несёт `active` и `aria-selected="true"` в
разметке — проверено вырезанием ВСЕХ `<script>` из копии: T0 рисуется целиком.

И `PROMO_CHAINS` в дашборде не читал никто, кроме собственного теста: экспорт уже несёт
`named_by_amendment_3_20` у каждой сети (6a ставит флаг по тексту амендмента), так что константа
была вторым домом факта с хозяином — [[a-monkeypatch-is-not-a-reader]] в чистом виде. Удалена;
тест теперь проверяет, что экспорт помечает ровно четыре и что страница рисует каждую сеть из
экспорта. Коммит `526fc3e`, `make check` **2 392 / 2 skipped**, дерево чистое.
- 13:52: session ended (auto)

## Checkpoint 16:47 — контракт `fix-a`: правила вотчлиста стали законом, ценз гейта посчитан

Исполнен `docs/PROMPT-fix-a.md`, $0.00. Стартовый HEAD `ce97b2c`, `make check` 2 392 → **2 432 /
2 skipped**. Отчёт — `docs/reports/fix-a.md`, отклонения Dv354–Dv366.

**Амендмент 3.21** приземлён домашним манёвром (`0e0fbc0`): блок в `docs/SPEC.md` (index-note
внутри), перечисление в `tests/test_sku_prereg.py`, `write_prereg_5c2.BLOCKS_TODAY`. Контракт назвал
четыре подвижные части, в дереве оказалась **пятая** — `tests/test_prereg_5c2.py` перечисляет хвост
после печати ЛИТЕРАЛЬНО, так что блок не мог прийти сдвигом числа. Урезанный (stripped) закон не
сдвинулся: 50 135 байт с обеих сторон, все запечатанные пины выводятся.

**r1: 11 рядов с брендом → 3.** `config/watchlist_rules.yaml` — новый файл, которого не держит ни
один пин; `config/registry.yaml` и `config/lexicon.yaml` не тронуты ни байтом. Ревизия **opt-in по
принуждению**: `scripts/window_summary_5c2.py` запечатан по sha и не может быть отредактирован,
значит старое поведение обязано быть дефолтом. Убрано ровно восемь попаданий — семь «варто»
(обычное наречие) и один детский центр «Гармонія». Згадки 13 → 5, таблица брендов 6 → 4.

**Раскол якоря — две ТАБЛИЦЫ, не колонка.** 35 из 902 числовых листьев якоря выведены матчером,
**12 из них реально двигаются** под r1 — измерено негативным контролем (подменяем таблицу зеркала,
гейт краснеет ровно на `brand_attribution`). SQL пути конвергенции не изменился ни на символ.
`src/market_pulse/brands.py` сам запинен в двух запечатанных записях — вошёл в семью sha-подстановки
СВОИМ кортежем и своим свидетелем ([[an-invariant-the-new-member-cannot-satisfy]]).

**Ценз гейта** `results/gate_census_w1.json`: 514 тредов (джойн 514/514 к `data/raw/posts`),
narrow|on **111 тредов · 912 платных комментариев · $0.14–$1.19**. Цена — ИНТЕРВАЛ, потому что
единственная замеренная цена — за КОММЕНТАРИЙ (4.247 с/ряд), а план считает вызов за ТРЕД. Четвёртый
глушитель (гивевей-треды) **не реализуем**: в окне-1 нет ни одной метки типа поста, и «убрано 0»
прочиталось бы как «их не было». План обещал «~12–15 тредов» — это ручное чтение тимлида, механика
пропускает 111.

**Вопросы сидению** (в §5 отчёта): у `selianske` два чтения одного пункта, и они расходятся на
собственном примере сидения; `varus-pl` ловит СЕТЬ «Varus» как приватную марку — r1 это не правит;
SoV нашего бренда теперь честные **0.00 %**.

Дальше по очереди — `fix-b` (таблица промо-позиций T5, отдельный контракт, идёт ПОСЛЕ приёмки
fix-a).
- 16:56: session ended (auto)
- 17:57: session ended (auto)

## Checkpoint 17:57 — контракт `fix-b`: промо-ответ стал таблицей ($0)

Исполнен `docs/PROMPT-fix-b.md` — амендмент **3.21 (4)**: «какие позиции, по каким ценам, у каких
брендов в промо» отвечает ТАБЛИЦА из всех **145** позиционных рядов окна, не выборка. Отчёт —
`docs/reports/fix-b.md`, девиации **Dv367–Dv373**. `make check` **2 437 / 2 skipped**
(было 2 432), потрачено $0.00. Коммиты: `a25291a` (хвост) · `42494e1` (таблица + страница + два
артефакта) · `ad38b19` (отчёт) · `5ba95a0` (сужение claim'а в самой записи).

**Решение, которое и было контрактом — колонка `depth`.** Два пункта закона сходятся на ней:
3.18 (1) называет инструментом глубины промо-цену + НАПЕЧАТАННЫЙ бейдж −N%, а 3.17 (3), прочитанный
как правило о ПОРЯДКЕ строки, требует арифметику `(old − promo) / old` на 95 рядах, где есть обе
цены. Развязка — что арифметическое чтение отдаёт читателю рядом с промо-ценой:
`price_old = promo / (1 − depth)`, точно до копейки. Таблица несёт **бейдж**; арифметика остаётся
там, где она законна — оконный агрегат `metrics.promo_depth.readings.from_price_pair`. Это не
формальность: **88 из 95** рядов расходятся между чтениями, **8** — больше допуска 1.0 pp, максимум
**7.1707 pp**. Чтение записано в само поле `promo.positions_table.law`, а не осталось в голове
продюсера ([[two-readings-of-one-clause]]).

**Что окно на самом деле говорит:** 80 рядов с распознанным брендом, **65** с напечатанной маркой,
которую реестр не разрешает (третий класс, не «конкурент»), и **0** наших — фильтр «наші» отдаёт
пустое состояние словами, а не пустой tbody. 106 листовочных (только АТБ) против 39 текстов постов,
8 сетей, 39 различных отображаемых имён.

**Слой выучил сам товар:** `positions` получил шесть колонок (brand_raw, line, size, pack, fat),
`watchlist` — `display`, так что бренд-колонка это JOIN, а не вторая карта рядом со страницей.
Экспорт ОТКАЗЫВАЕТСЯ, если длина таблицы ≠ `window.populations.position_rows` — негативный контроль
удаляет канал из `channels` и ловит 141 против 145 (ряд, потерянный на джойне, выглядел бы полным
ответом). Конвергенция не тронута: 902/902.

**Находка, которую я НЕ чинил** (Dv372, §4 отчёта): drill-down того же экрана T5 печатает
АРИФМЕТИЧЕСКУЮ глубину рядом с промо-ценой на 15 из 20 вытянутых рядов — `promo / (1 − depth)` даёт
19.99, 42.50, 55.90. Это ровно та экспозиция, ради которой новая колонка сделана бейджем, но на
поверхности, которую контракт не называет, а 3.18 (1) можно прочитать как разрешающий (там печатается
глубина, а не цена). Правка — одна строка, и это решение оператора.

**Что я всё-таки поправил после ревью** (Dv373): поле `law` в экспорте утверждало «that number
reaches no surface» — безоговорочно, — и находка (1) это опровергает. Запись бы уехала с ложной
фразой, а страница вшивает экспорт байт в байт. Сужено до «ни одна КОЛОНКА этой таблицы», с указателем
на находку. Скрипт пере-вывода этого поймать не мог: он проверяет числа, а это проза
([[a-claim-no-number-can-check]]).

Дальше по очереди — сидение по дизайну комментных сигналов, затем `cycle2-prep-b` и `6c`.
- 18:01: session ended (auto)
- 18:33: session ended (auto)

## Checkpoint 18:35 — контракт fix-c: строка печатает бейдж сети, а не нашу арифметику ($0)

Исполнен `docs/PROMPT-fix-c.md` — микро-контракт нулевой стоимости, закрывающий находку (1) из
[[2026-08-15]]/fix-b решением оператора «бейдж везде». Отчёт — `docs/reports/fix-c.md`
(Dv374–Dv377). Коммиты `bfd801f` · `22d49f7` · `152004b` · `fdf9503`. `make check` **2 438 / 2
skipped** (было 2 437), потрачено $0.00.

**Поправка 3.22 в SPEC.** Домашний манёвр: блок пишется скриптом через Bash (`Edit(/docs/SPEC.md)`
запрещён, Bash под запрет не попадает), три печати выведены собственной `registered_law()`
продюсеров и не сдвинулись — `973c8789…` / `6818926d…` / `3dd43923…`, зарегистрированный закон те же
50 135 байт, блоков стало 14. Контракт назвал ЧЕТЫРЕ подвижные части, сдвинулось ШЕСТЬ: пятая —
литеральный хвост `BLOCKS_TODAY[10:]` (урок Dv336, он же покраснел на фоновом `make check`, который
пересёкся с записью SPEC), шестая — имя нарушителя в негативном контроле.

**Шестая часть — контроль, который перестал контролировать (Dv374).**
`test_a_twelfth_marked_block_is_refused_rather_than_stripped` подсаживал `amendment-3.21` как имя,
которого продюсер не знает. В fix-a 3.21 стало законом, имя не сдвинули — и тест целый контракт
проверял обнаружение ДУБЛИКАТА, а не приход неизвестного имени. Обе ветки отказывают одним и тем же
сообщением, поэтому ничего не покраснело. Имя переведено на `amendment-3.23` плюс
`assert intruder not in writer.BLOCKS_TODAY`, чтобы следующий пропуск был красным.

**Шов — одна строка.** `build_dashboard.position_row`: `depth` = `discount_pct_printed / 100`,
отсутствует без бейджа (`is not None`, а не истинность — напечатанный 0 % это факт). На странице
сдвинулось 18 из 1 117 узлов с цифрами, все — `depth` в drill-строках: 14 сменили значение, **4
получили глубину впервые** (бейдж есть, пары цен нет — правка не является чистым сужением, Dv375),
0 потеряли. Экспорт **побайтово тот же** (`939d6a9b…`) — проверено пересборкой и хешем, а не
рассуждением про `provenance.producers`; под `results/` и `data/` ноль путей.

**Тест красный на том рендерере, который заменяет** — падает на строке из контракта
(`promo 12.9`, 35.47 % → 35.00 %) — и отказывается быть пустым: хотя бы одна вытянутая строка должна
быть той, где два чтения расходятся, иначе удачная выборка прошла бы на обеих кодовых базах (Dv376).
До правки 15 из 20 вытянутых рядов обращались в старую цену, 15 из 15 — копейка в копейку.

**Пять находок оператору.** (1) Экспандер `t5_depth` теперь озаглавлен «чтение по паре цен», а его
строки показывают бейдж; все 95 его строк несут бейдж, поэтому пустых нет — чинится строкой в
`config/ui_strings.yaml`, а контракт запрещает двигать другие поверхности. (2) `build_validate_pack`
печатает оба чтения и саму старую цену — это инструмент, которым оператор судил извлечение, а не
промо-поверхность; решение вынесено по АРТЕФАКТУ, продюсер живой и это отдельный вопрос. (3) Бейдж
тоже обратим — но к цифре самой сети: медиана ошибки 0.49 ₴, максимум 8.91 ₴, и на 1 из 15 строк он
всё же попадает в `price_old` точно. (4) Поле `law` в экспорте всё ещё указывает на находку fix-b,
которую 3.22 закрыла. (5) Контроль из Dv374 как класс.

**Проверка по коммитам записана правилом, а не списком** — перечисление не может назвать коммит, в
котором само лежит; шрам репозитория на это — `b2ff741`. Два коммита с одной прозой отличаются от
проверенного дерева только текстом, и ни один тест не открывает их путей (проверено, а не
предположено). См. [[the-control-whose-premise-stopped-being-true]].

Дальше по очереди — дизайн-сидение по комментным сигналам, затем `cycle2-prep-b` и `6c`.
## Checkpoint 20:08 — контракт probe-a: читатель зарегистрирован, платный проход не открыт ($0.00)

Исполнены **D1–D3 и $0-половина D4** — всё закоммичено ДО того, как появился хоть один серверный
ресурс. **Платный проход не открывался**, и причина — деление, а не оценка: по измеренной ставке
$0.00030669/с кап $0.20 покупает **652.1 с** оплачиваемого времени воркера, а самое оптимистичное
чтение зарегистрированного прогона в самом же цензе требует **471.4 с** на 111 тредов + **99 с** на
одну измеренную загрузку весов + стейджинг-под, без которого тому не обойтись, — **$0.2069 ещё до
того, как посчитан варм-ап**. И оптимистичный конец недостижим: у читателя вход медианой 1 717
токенов против 809 у комментного вызова, потолок вывода 2 000 против 256. Создано ноль ресурсов
(`runpodctl serverless list` / `pod list -a` → `[]` до и после). Отчёт —
`docs/reports/probe-a.md` (Dv378–**Dv391**). Коммиты `7eaa2cb` · `4cbbd24` · `8a014e0` · `aa78c14` ·
`7bb4776` · `36b2cdf` · `1ffb262`. `make check` **2 508 / 2 skipped**.

**Что зарегистрировано.** Промпт `reader_thread_gm4` (`b272115637f784ad…`) — рамка из семи вопросов
PRODUCT.md, порядок обязанностей 15.08 (тред → КАЖДОЕ имя из контекста → и только потом сигналы),
ратифицированная таксономия дословно; четыре кейса разрешения сущностей в текст НЕ попали — они бар.
Голд `results/reader_gold_w1.json` из референса без переинтерпретации, каждая цитата грепается
обратно в файл тимлида. Прережка `results/prereg_reader_probe.json` пиннит популяцию **списком**
(дайджест `45cc560b…`), инструменты — по sha, и правила матчинга словами: четыре публичные функции
скорера, у каждой ручной тест.

**Главная находка, и она $0.** Маркер-правило гейта — платёжные ворота для читателя. Три из четырёх
обязательных кейсов (E1 «Гармонія», E4a/E4b «чи варто») и один из шести шумовых тредов (N3) **не
входят** в популяцию: каждый проходит гейт без глушителей и убирается правилом SPEC 3.21 (1), которое
снимает единственное лексиконное попадание треда. Бар 2 зарегистрирован 2/2, бар 3 — по четырём
тредам, каждое исключение с измеренной причиной. План §2 C сам говорит, что r1 — «временная гигиена…
не закон смысла; финальную атрибуцию делает читатель», — а в цензованном гейте она решает, что
читатель вообще увидит. Вопрос сидения.

**Ещё два расхождения референса со стором** (ни один файл не тронут): msg 20916 лежит под постом
#10375, а не #10366; а #10529, названный плюс-спамом, несёт ровно два коммента — и один из них сам
референс читает как сигнал в списке S. **Шесть из одиннадцати цитат референса** не грепаются в стор
как написаны — четыре из них на флагманах.

**Оператору — три варианта с ценой** (§9 отчёта): поднять кап до ~$0.45 (сетап + полный проход) или
до ~$1.35 (верхняя граница ценза) при остатке **$1.0923** по гварду; перерегистрировать меньшую
популяцию (~12 тредов референса, влезает в нынешний кап); отложить. Не рекомендуется покупать сетап
и варм-ап под нынешним капом: ~$0.09–0.16 за наблюдение предопределённого STOP.
См. [[the-setup-is-inside-the-cap]] и [[an-absolute-bar-needs-a-reachability-state]].

## Checkpoint 20:45 — probe-a: кап поднят, проба исполнена, гейт сказал STOP ($0.0750)

**Оператор поднял кап $0.20 → $0.45** после арифметики выше; прережка исправлена и перекоммичена
(`5790543`) ДО того, как появился эндпойнт, — двинулся только потолок, дайджест популяции,
инструменты и бары те же. Затем платный проход:

- **стейджинг** — под `owunnm26f4bu2u` (RTX 2000 Ada, EU-RO-1), 2 мин 26 с, **$0.0097**;
  `repo/` на томе 0793a3c → a4fb51e через fetch + hard reset (не `rm -rf`: внутри лежит
  gitignored адаптер 489 840 816 байт, он цел). Контент-чек + **venv тома рендерит промпт в
  `b272115637f784ad…` — байт в байт как Mac**;
- **эндпойнт** `g8ql4i3tunk6gr` (шаблон `3ge2s1p1aw`, класс AMPERE_24 → карта **NVIDIA L4**,
  23 034 MiB), хендшейк за 6 мин 13 с, `repo_commit a4fb51e`, `assert_serving` прошёл целиком;
- **варм-ап** — три зарегистрированных треда: 22.1 + 66.1 + 70.0 с воркера, **164.4 с на 3 треда =
  54.806 с/тред**, 14.947 с на платный коммент. Против 4.247 с ценза — **12.9×**;
- **гейт: STOP.** Проекции окна $1.8657 (по тредам) и $4.1807 (по платным комментам), связывает
  пессимистичная; при остатке $0.3996 и бюджете 30 мин (111 тредов = 101 минута);
- всё удалено, листинги пустые до и после; **гвард: $0.0750 из $0.45**, фаза — $1.0826.

**Два дефекта интерфейса, купленные этими деньгами и НЕ починенные** (инструмент заморожен с
момента появления эндпойнта): (1) `entities` приходит объектом по имени, а не списком — все три
ответа, парсер отказывает целиком; после офлайн-коэрса два из трёх парсятся полностью; (2) один
сигнал несёт `evidence: [null]` — читатель прочёл его в ПОСТЕ и применил правило самого промпта
«у поста id = null» к полю, где схема требует msg_id.

**И читатель читает.** `@tarilka_malyuka:715` — рецептный тред, которого ручной разбор не касался:
разрешены ТРИ молочные ТМ («агуня», «мілупа», «філадельфія»), у каждой фраза-чтение и цитата, плюс
сигнал `похвала` о густоте одной против другой. Обязанность (2) ровно так, как её задал оператор.

Отчёт — `docs/reports/probe-a.md` (§5 переписан, Dv392–**Dv396**, четыре варианта с ценой в §9).
Коммиты `5790543` · `3ab5963` · `a4fb51e` · `c387693` · `85a911d`. `make check` **2 521 / 2 skipped**.
См. [[the-setup-is-inside-the-cap]].

## Checkpoint 20:56 — probe-a закрыт: правило по коммитам, футган гварда, пятый сигнал

Дозакрытие после прогона (кода не двигал, деньги не тратил — **$0.00** с 20:45):

- **правило проверки по коммитам переписано под реальный диапазон** — не «два документных коммита»,
  а пять, плюс два коммита, двигающих только записи трат. И оно теперь ДОКАЗАНО, а не заявлено:
  `grep -rn "docs/reports" tests` даёт одно упоминание в докстринге и ни одного открытия файла,
  `implementation-notes.md` не читает никто, а два теста, которые действительно читают живой
  `results/spend_phase4.json` (`test_prereg_5c2`, `test_projection_5c2`), выбирают свою строку **по
  таймстемпу, а не по позиции** — с комментарием, объясняющим почему, — так что дописанная гвардом
  сессия их сдвинуть не может. Перечисление не может назвать коммит, в котором лежит; шрам —
  `b2ff741`;
- **футган Dv392 в `hot.md`**: `runpod_guard.py --step <имя-с-дефисом>` ЧИТАЕТ
  `spend_probe_a.json` и ПИШЕТ `spend_probe-a.json` — половина фикса Dv151. Оба файла с одним и тем
  же якорем закоммичены, money-path в полёте не чинил;
- **пятый process-signal** в отчёт: предрасчёт был прав в выводе и неправ в составе — сетап вышел
  ~$0.025 вместо забюджетированных $0.16, а взорвал бюджет единственный неизмеренный член, само
  чтение (12.9× от границы ценза). Урок — называть доминирующий член оценки и покупать наименьшее
  измерение, которое закрывает именно его.

Итог контракта: `docs/reports/probe-a.md` (Dv378–Dv396, пять находок, пять process-сигналов, четыре
варианта с ценой в §9). Коммиты `7eaa2cb` · `4cbbd24` · `8a014e0` · `aa78c14` · `7bb4776` ·
`36b2cdf` · `1ffb262` · `5790543` · `3ab5963` · `a4fb51e` · `c387693` · `85a911d` · `65eb87a` ·
`a3b2e2f` · `8c68107`. `make check` **2 521 / 2 skipped**, потрачено **$0.0750 из $0.45**, на RunPod
пусто. См. [[the-setup-is-inside-the-cap]] и [[one-object-per-name-reads-as-a-map]].

- 18:39: session ended (auto)
- 20:53: session ended (auto)
- 20:59: session ended (auto)
