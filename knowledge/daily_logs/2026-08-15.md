---
type: daily_log
date: 2026-08-15
tags: [daily]
---

# 2026-08-15

**Восемь контрактов за день.** Начался цикл-2, собрался командный центр Фазы 6, приземлились три
амендмента SPEC (3.20, 3.21, 3.22), и слой комментных сигналов прошёл две платные пробы подряд.
Потрачено за день по БИЛЛИНГУ (не по дельте баланса — см. Dv412): **probe-a ≈ $0.075** + **probe-b
$0.31316** = **≈ $0.388** GPU; всё остальное — $0.00. Плюс том 100 ГБ, который тянет ~$0.0092/ч
независимо ни от чего и принадлежит не шагу, а счёту. `make check` 2 347 → **2 562 / 2 skipped**.

---

## Summary

### 1. `cycle2-prep-a` ($0) — скип-правило 3.19 в очереди и проекция батч-паритета

Отчёт `docs/reports/cycle2-prep-a.md`, Dv330–Dv334. Единственный текстовый предикат поднят в
**`market_pulse.loop.has_text`**; `strip()` vs `== ""` закрыт эмпирически — оба дают ровно **1 361**
и на отправленном тексте, и на исходном сторе. Скип живёт в двух местах, где строится оплачиваемая
очередь; `loop.queue_depth` не тронут, им считает перепись.

Проекция `results/batch_cycle2_projection.json` выводит НЕРАВЕНСТВО: batch 16 требовал 23.99 ГиБ
сверх весов при 3.99 доступных — короче в 6.02×; 16/8/4/3 исключены, кандидат один — **batch 2**, и
влезет ли он, **НЕ ИЗМЕРИМО** без живого пробника. Главное не про память: при batch 16 разошлись
**6 строк из 666**, а на 24-строчном карве всё было байт-в-байт — новая регистрация обязана мерить
идентичность на популяции.

Три красных теста, которых контракт не предусмотрел (Dv330): правка `loop.py` двигает sha двух
запечатанных рекордов. Не перепинены — байты достаются через `git show`, проверка в обе стороны.
Класс — [[a-comment-only-edit-moves-the-files-hash]].

Найдено самопроверкой и НЕ починено: `run_5c2.restrict` — просто фильтр, регистрация на 5 075
встретится с очередью в 3 714 и упрётся в `assert_populations` ПОСЛЕ трат.
[[a-narrowing-rule-shrinks-the-sealed-population]].

### 2. `phase6a` ($0) — слой агрегатов, экспорт и словарь метрик

Отчёт `docs/reports/phase6a.md`, Dv335–Dv341. Амендмент **3.20** приземлён не так, как написано в
контракте (Dv335): блок `amendment-index` лежит ВНУТРИ запечатанного пина. Четвёртая подвижная
деталь — `BLOCKS_TODAY` (Dv336). Все четыре пина деривируются, ни один рекорд не перепинен.

`data/derived/pulse.db` взят **импортом, а не подъёмом** (Dv337): подъём добавил бы КЛЮЧ в
`producer.borrowed` двух запечатанных рекордов, а подстановка sha спасает сдвинутый дайджест и не
спасает отсутствующий ключ ([[a-lift-adds-a-key-a-move-only-changes-one]]). Гейт исчерпывающий:
`aggregates.mirror()` пересобирает **902 из 902** числовых листьев якоря из SQL, и это оказалось
МЕНЬШЕ кода, чем ручная карта пар ([[mirror-the-whole-anchor-not-the-overlap]]).

**Что слой сказал про окно-1, чего раньше никто не мерил:** все **1 361** бестекстовых строк
размечены `neutral` — ни одного позитива, ни одного негатива, значит NSR двигается
**0.0770 → 0.1053 (+37%)** при переходе на платимую выборку (самый сильный аргумент за 3.19 (2));
`retail_official` — **единственный** сегмент с отрицательным NSR (−0.1111 на 234 строках); поправка
на сарказм двигает **3 строки** из 5 075; сегмент `regional` — 9 позиций и **ноль** комментариев.

Догон 12:35 (Dv341): срез сегментов строился от таблицы `channels`, которую наполняют СОБРАННЫЕ
строки — **семь карточек там, где реестр держит восемь**. Починено таблицей-измерением `segments` из
реестра, а не расширением `channels` (иначе 28/66 стало бы 66/66 и метрика убила бы себя
знаменателем). Тот же аргумент был выигран в этом же контракте дважды и не перенесён —
[[a-dimension-belongs-to-the-registry]].

### 3. `phase6b` ($0) — командный центр

Отчёт `docs/reports/phase6b.md`, Dv342–Dv353. **`dashboard/index.html`** (502 765 байт): девять
вкладок T0–T8, UA/EN и тема переключателями, всё инлайном, **ноль внешних запросов**, экспорт вложен
байт в байт. Шов честности: число попадает на страницу только из экспорта или из арифметики над его
же значениями. Заглушки ДВУХ классов, намеренно не слитых: семь `NOT_COMPUTABLE` («ещё нельзя
посчитать») и четыре `GAPS` («6a такого поля не экспортировал» — это и есть бэклог 6a).

Drill-down: 27 раскрытий, 196 строк, 196 ссылок; каждое объявляет ПОЛЕ экспорта со своей популяцией,
и билд отказывается при расхождении. Жребий сидируется на стратум, тест меряет ранги.

**Браузер нашёл то, чего не поймала бы ни одна текстовая проверка:** первая диаграмма тональности
рисовалась ПУСТОЙ — общий помощник угадывал форму данных и получал нули, без ошибки и без красного
теста ([[the-helper-that-guesses-its-inputs-shape]]). Догон 13:52: **все вкладки были скрыты до
запуска скрипта** — документ как написан рисовал шапку и пустоту; приёмка открывает файл холодным.
И `PROMO_CHAINS` в дашборде не читал никто, кроме собственного теста
([[a-monkeypatch-is-not-a-reader]]). Вывод T0 утверждал «єдиний сегмент», а код брал argmin
([[the-template-claims-more-than-the-rule-counted]]).

### 4. `fix-a` ($0) — правила вотчлиста стали законом, ценз гейта посчитан

Отчёт `docs/reports/fix-a.md`, Dv354–Dv366. Амендмент **3.21**; контракт назвал четыре подвижные
части, в дереве оказалась **пятая**. `config/watchlist_rules.yaml` — новый файл, которого не держит
ни один пин; ревизия **opt-in по принуждению**, потому что `window_summary_5c2.py` запечатан по sha.
Убрано ровно восемь попаданий — семь «варто» (наречие) и один детский центр «Гармонія»; згадки
13 → 5, таблица брендов 6 → 4. Раскол якоря — две ТАБЛИЦЫ, не колонка: 12 из 35 выведенных матчером
листьев реально двигаются, измерено негативным контролем.

**Ценз гейта** `results/gate_census_w1.json`: 514 тредов, `narrow|silencers_on` — **111 тредов ·
912 платных комментариев · $0.14–$1.19**. Цена — ИНТЕРВАЛ, потому что единственная замеренная цена
за КОММЕНТАРИЙ (4.247 с), а план считает вызов за ТРЕД. Четвёртый глушитель нереализуем: в окне нет
меток типа поста. План обещал «~12–15 тредов» — это ручное чтение тимлида, механика пропускает 111.

### 5. `fix-b` ($0) — промо-ответ стал таблицей

Отчёт `docs/reports/fix-b.md`, Dv367–Dv373. Амендмент **3.21 (4)**: на вопрос «какие позиции, по
каким ценам, у каких брендов» отвечает таблица из всех **145** рядов окна. Развязка двух чтений
колонки `depth`: таблица несёт **бейдж**, арифметика остаётся оконным агрегатом. Это не
формальность — **88 из 95** рядов расходятся между чтениями, 8 больше допуска 1.0 pp, максимум
**7.1707 pp**. Чтение записано в само поле `promo.positions_table.law`
([[two-readings-of-one-clause]]).

Окно: 80 рядов с распознанным брендом, **65** с напечатанной маркой, которой реестр не разрешает, и
**0** наших. Экспорт ОТКАЗЫВАЕТСЯ при длине таблицы ≠ `window.populations.position_rows`.

### 6. `fix-c` ($0) — строка печатает бейдж сети, а не нашу арифметику

Отчёт `docs/reports/fix-c.md`, Dv374–Dv377. Амендмент **3.22** закрывает находку Dv372 решением
оператора «бейдж везде»: drill-строка T5 печатает НАПЕЧАТАННЫЙ бейдж (`printed_pct / 100`),
отсутствует без бейджа и никогда не арифметическое чтение. На странице сдвинулись 18 из 1 117 узлов
с цифрами: 14 сменили значение, **4 получили глубину впервые** (правка НЕ чистое сужение), 0
потеряли. Манёвр приземления амендмента оказался из **шести** частей, а не четырёх: шестая — имя
нарушителя в тесте, которое надо двигать при КАЖДОМ приземлении и которое целый контракт проверяло
дубликат вместо неизвестного имени.

### 7. `probe-a` ($0.075 GPU) — читатель зарегистрирован, гейт сказал STOP

Отчёт `docs/reports/probe-a.md`, Dv378–Dv396. $0-часть легла до всякого серверинга: промпт
`reader_thread_gm4` (`b272115637f784ad…`), голд `results/reader_gold_w1.json`, прережка с популяцией,
запиннутой **списком**, конфиг READER, драйвер и преflight 57/57.

Самое ценное измерение стоило $0: пересечение переписи с референсом показало, что **E1, E4a, E4b и
N3 вне популяции** — маркер-правило SPEC 3.21 (1) снимает единственное лексиконное попадание треда
ДО оплаты, так что бар 4/4 недостижим арифметически
([[an-absolute-bar-needs-a-reachability-state]]). Плюс: msg 20916 лежит под #10375; #10529 назван и
шумом (N), и сигналом (S); **6 из 11 цитат референса** не грепаются в сторе.

Затем арифметика показала, что $0.20 не купят прогон вообще ($0.2069 до варм-апа), оператор поднял
кап до $0.45, прережка исправлена ДО эндпойнта — и проба купила ровно то число, ради которого
делалась: **54.806 с/тред** против нижней границы ценза 4.247 (**12.9×**), окно $1.87–$4.18,
**STOP** за $0.0750 — и по биллингу это и есть цена (эндпойнт $0.02486 за 127.889 с + под L4 $0.04083 + доля стейджинга). Инструмент заморозился с двумя дефектами: `entities` приходит
объектом по имени ([[one-object-per-name-reads-as-a-map]]) и один сигнал несёт `evidence: [null]`.

### 8. `probe-b` ($0.31316 GPU, внутри капа $0.35) — карта заменена, интерфейс починен, бары посчитаны впервые

Отчёт `docs/reports/probe-b.md`, Dv397–Dv411. Три вещи изменены и больше ничего: инструмент, карта,
популяция.

**Бейзлайн контракта оказался невоспроизводим.** Тимлид отредактировал `docs/PLAN-comment-signals.md`
§3 (`сеть` → `сеть_ритейлер`, `категория` → `категория_личное`), и sha плана сдвинулся под двумя
замороженными записями probe-a — 4 красных теста до первой строки контракта. Записи НЕ перепиннены:
тупл `MOVED` со своим свидетелем, подстановка обязана СРАБОТАТЬ, негативный контроль показан. Голд
пересобирается байт-в-байт.

**Карта была дешёвым рычагом и сработала ровно как посчитано.** `gpuIds: ADA_24` прочитан
БЕСПЛАТНО из ответа на `serverless create` — создание не биллится, биллятся только запросы. Воркер:
**RTX 4090, 24 564 MiB**. Варм-ап **20.759 с/тред против 54.806 на L4 = 2.64×** при
зарегистрированной точке безубыточности **2.007×**, при той же цене за секунду. Гейт GO, 23 треда
прочитаны, 727.7 с воркера. Окно из 111 тредов на этой карте — **$0.71 / $1.58** вместо $1.87/$4.18.
[[the-class-you-asked-for-is-free-to-verify]]

**Бары посчитаны впервые в программе** — 2/5 · 2/4 · 0 сигналов (проходит) · 0.357 — и главный
результат не в них. Оба дефекта, ради которых писался v2, не вернулись; 10 из 23 ответов отказаны
**шестью другими формами того же класса** ([[a-container-defect-moves-to-the-next-field]]). Коэрс
только контейнеров: **13 → 19 из 23**, entity 2 → 3. А **единственный пройденный бар — ноль по
четырём тредам без вердикта вообще** ([[a-bar-that-counts-passes-on-an-unreadable-reply]]).

**Инъекция окупилась:** E1 «Гармонія» и E4 на обоих тредах отвечены — 2 из 4 entity-кейсов
существуют только потому, что прогон купил треды, которые продакшн-гейт снимает.

---

## Decisions

- **Новая запись:** [[reader-probe-card-and-interface]] — две пробы читателя закрывают вопрос карты
  (ADA_24, 2.64×, всякая проекция обязана называть карту) и называют связывающим ограничением не
  чтение, а интерфейс. Четыре приценённых варианта ждут оператора.
- **Ратифицировано оператором в течение дня** (в `decisions/` отдельными записями НЕ оформлено — это
  решение оператора, оформлять ли): амендменты **3.20** (слой агрегатов), **3.21** (правила
  вотчлиста как закон) и **3.21 (4)** (таблица промо-позиций), **3.22** (бейдж на всех построчных
  поверхностях), маршрутизация `[model]` в `.claude/rules/`, подъём капа probe-a $0.20 → $0.45 и кап
  probe-b $0.35 с покупкой тредов мимо продакшн-гейта.

## Next

1. **Выбрать из четырёх вариантов §9 отчёта probe-b.** A) контейнерно-терпимый парсер — $0 GPU, но
   не бесплатен: правило на случай расхождения двух объектов, перерегистрация значения слова
   «отказан» и снова сдвиг sha `prompts.py` в три запиннутые записи; B) v3-промпт («один JSON-объект»,
   «`[]` если пусто») ~$0.08 + новая прережка; C) разрыв чтения — сидение, $0; D) два стоящих вопроса
   сидения.
2. **Сидение по дизайну комментных сигналов** — маркер-правило как платёжные ворота читателя и
   `категория` vs `категория_личное` (8 из 14 голд-строк держатся на слове, которое план перестал
   демонстрировать сегодня).
3. `cycle2-prep-b` — докачка корпуса + листовки Сільпо/Varus; затем `6c`.
4. Денежная линия батч-паритета — открытое решение оператора (см. проекцию `cycle2-prep-a`).
5. Свободный подсчёт: сколько из 11 143 строк истории бестекстовые. Ни один якорь не отвечает.

## Blockers

- **Шаговое число гварда — одно чтение (Dv411) И оно измеряет СЧЁТ, а не шаг (Dv412). НЕ починено,
  это денежный путь.** Утром 16.08 гвард сказал `REFUSED: probe-b's $0.35 cap is reached ($0.4493
  spent)` и вышел с кодом 1 — при том, что по биллингу probe-b стоил **$0.31316** и уложился в кап.
  Разница — том. Починка: взять обход биллинга, ограниченный окном шага, исключить всегда-включённые
  виды ПО ВИДУ, и связывать пессимистичным из двух — то есть перенести на шаг правило фазы.
- **Шаговый счётчик построен на дельте баланса и НЕ ОСТАНАВЛИВАЕТСЯ** (Dv412, найдено утром 16.08).
  Том 100 ГБ тянет ~$0.0092/ч независимо ни от чего, и это попадает в `якорь − баланс` закрытого
  шага навсегда: probe-b читался $0.2907 при удалении, $0.3132 через два часа, **$0.4493 через
  тринадцать** — и гвард ОТКАЗЫВАЕТ. По биллингу probe-b стоил **$0.31316 GPU, внутри капа**
  (serverless $0.28447 / 927.592 с = $0.0003067/с — ровно зарегистрированная ставка). Вчерашняя
  формулировка «счёт оседает» ОТОЗВАНА: probe-a тоже стоил ровно свои $0.075, а $0.019 сверху были
  два часа тома. [[a-step-meter-on-a-balance-delta-never-stops]]
- **`run_5c2.restrict` не сравнивает длину очереди с запечатанным населением** — регистрация на
  5 075 упрётся в `assert_populations` ПОСЛЕ трат (найдено в `cycle2-prep-a`, красная линия
  поставлена тестом, чинить некому до следующего денежного контракта).
- **Флага приватных марок нет нигде в репозитории** (Dv343): план называет домом
  `config/lexicon.yaml`, который брендов не держит вовсе. Значок на T4 — заглушка, чинится полем
  реестра и словом оператора.
- `MEMORY.md` — 172 строки / 23 550 байт при лимите чтения 24.4 КБ. Влезает, но запаса почти нет;
  сжатие индекса — отдельная работа.

## Sessions

| контракт | отчёт | девиации | `make check` | траты |
|---|---|---|---|---|
| `cycle2-prep-a` | `docs/reports/cycle2-prep-a.md` | Dv330–Dv334 | 2 347 | $0 |
| `phase6a` | `docs/reports/phase6a.md` | Dv335–Dv341 | 2 370 | $0 |
| `phase6b` | `docs/reports/phase6b.md` | Dv342–Dv353 | 2 392 | $0 |
| `fix-a` | `docs/reports/fix-a.md` | Dv354–Dv366 | 2 432 | $0 |
| `fix-b` | `docs/reports/fix-b.md` | Dv367–Dv373 | 2 437 | $0 |
| `fix-c` | `docs/reports/fix-c.md` | Dv374–Dv377 | 2 438 | $0 |
| `probe-a` | `docs/reports/probe-a.md` | Dv378–Dv396 | 2 521 | $0.075 |
| `probe-b` | `docs/reports/probe-b.md` | Dv397–**Dv412** | **2 562** | $0.31316 |

Инфраструктура probe-b: `results/prereg_reader_probe_v2.json` (23 треда, 4 инъецированных, дайджест
`ccef35fa…`), `scripts/probe_b_population.py`, `scripts/read_threads_probe_b.py`,
`scripts/score_reader_probe_b.py`, и два пост-ран разбора —
`results/reader_probe_b_refusals.json` (какие кейсы упали на нечитаемых ответах) и
`results/reader_probe_b_coerced.json` (что стоил интерфейс). Преflight **63/63**.

На RunPod пусто: `serverless list` и `pod list -a` пустые до и после, том `qw4nwleanc` на месте.

## Session stamps (hook)

- 11:20 · 12:32 · 13:52 · 16:56 · 17:57 · 18:01 · 18:33 · 18:39 · 20:53 · 20:59: session ended (auto)
