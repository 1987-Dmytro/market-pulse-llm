---
type: daily_log
date: 2026-08-18
tags: [daily]
---

# 2026-08-18

## Summary

**Контракт `pass1-probe-b` ИСПОЛНЕН. Проход-1 ИЗМЕРЕН впервые — и бар P1 ПРОВАЛЕН: 9 из 14 при
пороге 12. Вердикт STOP, оговорка возврата к сидению взведена.** Отчёт
`docs/reports/pass1-probe-b.md`, девиации **Dv490–Dv499**, шесть коммитов
`a68699e…a401cff`, `make check` **2 879 → 2 911 / 2 skipped**. Прогон стоил **$0.135050 из капа
$0.20**, внутри зарегистрированного худшего случая $0.18289. Цикл-2: **$1.6769 из $20, остаток
$18.3231**.

День начался с хвоста предыдущей сессии (`/save` за 17.08) и с тимлидовских файлов, поставленных в
очередь, пока я работал: `docs/STATUS.md` принял `pass1-probe` как транспортный отрицательный,
`docs/PROMPT-pass1-probe-b.md` авторизовал вторую попытку. Оба закоммичены **дословно**, своим
коммитом, стейджингом по путям.

### Шаг 0 — закрытие, которого не случилось

`pass1-probe` закрыть **нельзя**: pod-scoped обход по `rx89zok35e5ca6` даёт **300 000 мс против
427 000 мс** в рекорде прогона. `closing_record()` селится на округлённых строках обхода за вычетом
всегда-включённых видов, так что close заморозил бы **$0.0617** вместо **$0.087772** — 70% правды.
Долг несётся именованным с обеими цифрами.

**Побочная находка, которая ухудшается со временем:** открытый леджер `pass1-probe` читает уже
**$0.2075 из своих $0.20** и отказывает. Это не под — это сетевой том, который капает на плечо
balance-delta, пока шаг открыт. Пробег пода $0.0617 прочитан / $0.087772 записан. Отказ верен и не
блокирует отдельный леджер `pass1-probe-b`, но цена незакрываемого шага в том, что она растёт.

### D1 ($0) — регистрация, прогнанная до заморозки

`results/prereg_pass1_probe_b.json` (`a29e85ad…`) + `results/pass1_probe_b_pack.json` (`bbbe6420…`).
Идиома v5b на поколение ниже: `scripts/write_pass1_prereg_b.py` **вызывает**
`write_pass1_prereg.build()` без подмен и отказывается, пока тот не пересоберёт замороженную запись
байт в байт; только после этого строит второй раз под подменой. Значит всё, на чём меряется P1 —
текст `pass1_comment_gm4_v1` и его sha `5a4a3cb6…`, парсер, четыре типа субъекта, голд r2,
популяция из 64 юнитов и её per-item sha, бар ≥12/14 с бюджетом потерь, правила ценза, класс одной
попытки и оговорка сидения — **объектно равно ПО ПОСТРОЕНИЮ**.

Весь дифф — **28 перечисленных путей, утверждённых в ОБЕ стороны** (сдвинулся неперечисленный →
отказ; перечисленный не сдвинулся → тоже отказ). Три негативных контроля стреляют: сдвинутый бар,
переименованная константа, лишний путь. Таблица гейта пропущена как ОДИН непрозрачный диапазон, и
эта дыра закрыта отдельно — порядок юнитов, каждый из 64 вердиктов, `first_stop_after_units` и
угол без бута сравниваются напрямую, с контролем, который переворачивает один вердикт.

Двинулась **одна константа в двух ячейках**: бут ЗАРЯЖЕН 192.13 → **300.0 с** (максимум двух
замеров стека), потолок `BOOT_KILL_S` 300 → **420 с**. Таблица пересчитана: тот же порядок, те же
64 GO, STOP по-прежнему нет, узкое место 4.938 → **3.226 с/юнит**, запас на юните 1 311.1 → 203.2 с.

Четыре из пяти чисел тимлида воспроизводятся точно (913.0 · 409.7 · 503.3 · +83.2). Пятое — нет:
**$0.175 = `(usable − delete_margin) × rate`**, а `usable` уже содержит вычет маржи, то есть 60 с
списаны дважды. Достижимый худший случай — **889.728 с = $0.18289**, зарегистрирован как
`money.arithmetic.worst_case` со своей арифметикой и с цифрой контракта рядом. Оба внутри капа.

**Правило Dv486 из урока стало поставкой:** регистрация прогнана через все команды, которые её
читают, за $0. Добавлено `money.arithmetic.boot_deadline_rule` — ключ, который
`read_threads_reader_v4.deadlines` читает ПО ИМЕНИ и которого не было в записи pass1-probe, отчего
`--deadlines` упал `KeyError` при работающем метре. Регрессия закреплена с двух сторон: на старой
записи функция обязана бросить, на новой — вернуть правило.

**Главная находка D1: `--gate` — это две команды.** Он маршрутизируется по СОСТОЯНИЮ, а не по
флагу: пусто на диске → ветка бут-дедлайна, есть строки → ветка проекции полного прохода, и вторая
читает платные счётчики пака, остаток капа на сегмент и usable-секунды, которых первая не касается.
До сегодня она ни разу не прогонялась до пода. Прогнана дважды, обе в суите
(`tests/test_pass1_transport_b.py`, на `tmp_path`). Урок в память: [[one-flag-two-branches]].

**Правило Dv487 исполнено:** freeze-коммит `9b9e7a5` прогнал свою суиту — **2 911 passed, 2
skipped**, `producer.sha256` пинит байты, лежащие В нём (проверено `git show` в throwaway-worktree).

### D2 — прогон

Под `zn3chzmo1xricf`, RTX 4090, RO, `costPerHr` **0.74** — ровно зарегистрированный worked example,
ни один дедлайн не перецифрован. Якорь `results/spend_pass1_probe_b.json` закоммичен в `dfdf636` ДО
создания. Три листинга до и после, том — позитивный контроль.

| create-elapsed | что |
|---|---|
| 10 с | эндпоинт ответил с ПЕРВОГО опроса |
| 14.5 с | гейт-0 **GO** против дед-мана 180 с — **третий подряд** независимый create |
| 54 с | клон бандла в **новый** каталог `repo-pass1b`, `HEAD = dfdf636` = HEAD Mac'а, чисто |
| 66 с | отцепленный запуск, `--repo` явно |
| 303.2 с | `READY · boot 237.2s` |
| 308.2 с | первый ответ |
| 392.7 с | jsonl **и лог** одной командой, затем `--gate` → **GO**, запас 282.1 с, $0.1297 |
| 633.7 с | `DONE · 64 generated here` |
| 657.0 с | `pod delete` → **657.0 с = $0.135050** |

**Бут измерен третий раз: 237.2 с.** Три замера — 192.1 / 237.2 / [267, 293] — расходятся в
**1.53×** на той же карте, том же томе и тех же весах, и ничто в прогонах этого не объясняет.
Заряд 300 с оказался консервативен на 62.8 с; потолок 420 с не был затронут.

**Честное чтение: подъём потолка сегодня ничего не спас.** При СТАРОМ потолке (66 + 300 = 366 с)
первый ответ в 308.2 с прошёл бы с запасом **57.8 с**. pass1-probe подошёл к тому же потолку на
0.6–26.6 с потому, что его бут был на 30–56 с медленнее, а не потому, что 300 с — неверное число.
Рулинг всё равно правильный (дедлайн покупают против худшего случая), но приписывать ему результат
нельзя: [[the-fix-that-was-not-a-fix]].

`results/pass1_probe_b_pod.log` закоммичен рядом с jsonl — **артефакт, который pass1-probe потерял**
(его единственный трейсбек выжил чтением с экрана и умер с подом).

### D3 — что именно провалилось

**Бар P1: 9 из 14, порог 12, ноль absent, пять потерь при бюджете два. STOP.**

- **Все пять — `subject_type`.** `stance` считается на трёх строках и прав на всех трёх.
- **Две из пяти — модель вообще отказалась типизировать** (`subject_type: null` там, где голд
  говорит `категория_личное`): легальный ответ по зарегистрированной схеме, распарсен без отказа.
- Три остальные — другое прочтение; одна из них (`молочный_бренд` → `сеть_ритейлер`) путаница
  субъекта, а не гранулярности.
- **Симметричный коллапс — no-op.** Голд r2 пишет `категория_личное`, текст прохода-1 предлагает
  ровно это слово, пять строк на нём и сошлись. Словарь никогда не был препятствием.
- Разрез: `категория_личное` **5 из 9**, прочие типы **3 из 4**, `stance` **3 из 3**.

**Ценз (не гейтит): 50 строк, 50 распарсено, ноль отказов на всех 64 вызовах** — каждый ответ один
сбалансированный объект, `finish_reason: stop`, потолок 256 токенов не сработал ни разу. И при этом
**25 из 50 соседних строк без `subject_type`, 24 без `stance`**. Воздержание — это ОТВЕТ, а не сбой
парсера, и это разные болезни с разными лекарствами: [[an-abstention-is-an-answer]].

**Цена вызова: 5.162 с** — **0.81×** зарегистрированной границы (граница выдержала) и **1.76×**
подгонки (подгонка недооценила). Регистрация публиковала оба чтения и говорила, что разницу измерит
прогон; измерила. Окно: 1 032 платных комментария × 5.162 с = **5 327 с = $1.0950** генерации как
**ПОЛ** (без бута, без стейджинга, без второго пода).

Строки пробы никуда не утекли: `results/gate_census_w1_reader.json` только читался, ни один
production-агрегат и ни один window-price файл не записан.

## Decisions

- **Бар P1 провален → сидение, не итерация промпта.** Зарегистрированная оговорка
  `return_to_sitting` названа явно: варианты B (размеченные данные по `subject_type` + LoRA) и C
  (другая база). Ничего в промпте, парсере, популяции или баре не тронуто. ADR не пишу — рулинг за
  оператором/тимлидом, факты в отчёте.
- **Худший случай зарегистрирован по СВОЕЙ арифметике**, цифра контракта названа рядом с причиной
  расхождения, а не тихо исправлена (Dv490).
- **`money.arithmetic.boot_deadline_rule` добавлен** — зарегистрированное поле, которое поставляемый
  транспорт читает по имени (Dv486 закрыт кодом, не описанием).
- **`bars.…scored_fields` в записи пуст, и это НЕ чинится здесь** (Dv494): он объектно равен
  замороженной записи, а реальное правило живёт в `scored_fields` каждой строки голда. Названо, не
  исправлено — контракт запрещает двигать.

## Next

1. **Сидение по проходу-1.** Цифры, которых у него не было: 5.162 с на вызов (0.81× границы, 1.76×
   подгонки) · $1.0950 как пол на окно · воздержание на половине соседних строк при нуле отказов ·
   провал целиком в `subject_type` на строках вида `категория_личное` (5 из 9), при `stance` 3 из 3.
2. **Гард `--until` + перезакрытие 22 старых леджеров ($0)** — уже в очереди у оператора, и теперь у
   него есть два новых клиента (ниже).
3. **prep-b** — после гарда.

## Blockers

- **Два незакрытых леджера, и оба дорожают.** `pass1-probe` — обход **300 000 из 427 000 мс**;
  `pass1-probe-b` — обход отвечает `[]` при **~657 000 мс** в рекорде, balance-delta $0.0853 против
  записанных $0.135050. Закрытие на любом из этих чтений заморозит цифру НИЖЕ правды. Пока открыты,
  оба копят каплю сетевого тома на delta-плече: `pass1-probe` уже читает $0.2075 из своих $0.20.
  Разблокирует контракт гарда `--until`.
- **Проход-1 не проходит бар в текущем виде** — и по зарегистрированному правилу это вопрос сидения,
  а не следующего промпта.
- **boot tax 19.1K токенов при цели 9.0K** (`hot.md` 41 КБ, `MEMORY.md` 26.5 КБ при лимите
  загрузчика 24.4 КБ — индекс грузится частично). Де-bloat — реструктуризация, нужен запрос
  оператора.

## Sessions
- 10:57: session ended (auto)
- 12:15: session ended (auto)
- 13:13: session ended (auto)
