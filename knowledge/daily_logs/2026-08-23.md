---
type: daily_log
date: 2026-08-23
tags: [daily]
---

# 2026-08-23

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->

**`lora-c-apply` ИСПОЛНЕН за $0.** Применены оба вердикта тимлида и поправка 3.25 целиком; отчёт
`docs/reports/lora-c-apply.md`, ADR [[lora-c-rationale-supervision-and-the-shared-pool]] обновлён.
Оба ревью-гейта **ЗАКРЫТЫ**: 515 рационалей и 160 синтетических строк несут `reviewed: true`.

**Главный результат — проверка реальности из 3.25 (1) ОСТАНАВЛИВАЕТ линию.** Настоящий токенизатор
Gemma-4 на запиненной ревизии, все 506 строк, за $0: широчайшая строка **2 975 токенов (2 991 со
слэком)** против порога 2 800 и против самого `max_seq_len` 2 816. **Две строки за порогом по истинному счёту, три со слэком** — слэк правил оценку по символам, которую `apply_chat_template` уже считает, и одна строка легла ровно между читками. **Две строки за самим `max_seq_len` 2 816 при обеих читках**, поэтому STOP инвариантен к вопросу. Модель
отношения, из которой выведено 2 816, предсказывает 2 763 и пропускает всё — недооценка **228
токенов**. На `97548df` тот же счёт даёт 2 974 и две строки за порогом при ОБЕИХ читках, то есть ответ по
истинному счёту не двигался вовсе — сдвинулась только читка со слэком, на один токен. Следующая ступень 3 072 покрывает 2 991 с запасом 81 — слово оператора.

**Гейт 1.** Четыре именованные правки словами тимлида · **r3-дельта** одной строкой
(`@retsepty:7342#49685` `null` → `не_наш_рынок`) слоем поверх r1+r2, без выборки единицы · 13 правок
P-NULL по выписанному узкому правилу (широкое дало бы 48 и нарушило запрет контракта) · 31 маркер
P-MARKER · гибридный грep **0 из 515 при контроле 38**.

**Гейт 2.** Пятнадцать текстов переписаны так, что утверждение — о ВЕЩИ, а сеть только МЕСТО; после
этого `self_flagged`, инструмент, который их нашёл, совпадает с **0 из 160**. Метки не двигались
нигде. Скелеты: закодированный измеритель воспроизвёл «16 из 32» вердикта с первого запуска без
подгонки **и нашёл второй скелет над тем же потолком (12)**, которого проза не называла. Посев
переводов строки идемпотентен по построению. Регистровая таблица: **четыре промаха из шести → один
из семи**, и не подгонкой оси.

**3.25 (2) — протечка закрыта, и доказывает это 0.9875.** E: 22 → 0, на платном баре 5 → 0,
обучающие 13 → 0; максимальная похожесть теперь ровно наибольшее значение строго ниже 1.0. Ни один
запрос не потерял пятого соседа.

**Два урока измерения.** Целое число ревьюера — чтение его собственной популяции: линза 2 судила
**113 из 515**, поэтому её «17» и «24» выборочные. И проза записи переживает состояние, которое
описывает, — три раза за контракт.

**РУЛИНГ ОПЕРАТОРА, вечер: «поднимай max_seq_len до 3072».** Та ступень, которую поправка 3.25 (1)
назвала заранее. `config/qlora.yaml` — ревизия 3. Ни одна из 506 строк не за потолком ни по счёту
токенизатора (широчайшая 2 975), ни со слэком (2 991); запас 97 / 81 — обе читки опубликованы.
Запас тонкий, меньше 4 %, **но это счёт, а не проекция** — модель по-прежнему недооценивает
широчайшую строку на 228 токенов, и рулинг этой находки не трогает. `STOP_AT = 2800` остаётся в
продюсере: удалить порог значило бы удалить причину, по которой существует 3 072. Линия B не тронута
второй раз — все три запечатанных артефакта байт в байт при ревизии 3. **Блока в `docs/SPEC.md` под
этот рулинг нет** и написать его — за тимлидом (Dv780).

Верификатор **3 668 passed / 2 skipped**, дерево чистое, все шесть продюсеров пересобирают свои
байты. Четырнадцать отклонений Dv765–Dv778.

## Sessions
- 08:58: session ended (auto)
- 08:59: session ended (auto)
- 11:34: session ended (auto)
- 11:35: session ended (auto)
- 11:36: session ended (auto)
- 11:38: session ended (auto)
- 11:45: session ended (auto)
- 12:08: session ended (auto)
