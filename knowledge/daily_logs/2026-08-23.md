---
type: daily_log
date: 2026-08-23
tags: [daily]
---

# 2026-08-23

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->

**`lora-c-apply` ИСПОЛНЕН за $0.** Применены оба вердикта тимлида и поправка 3.25 целиком; отчёт
`docs/reports/lora-c-apply.md`, ADR [[lora-c-rationale-supervision-and-the-shared-pool]] обновлён.
Оба ревью-гейта **ЗАКРЫТЫ**: 515 рационалей и 160 синтетических строк несут `reviewed: true`.

**Главный результат — проверка реальности из 3.25 (1) ОСТАНАВЛИВАЕТ линию.** Настоящий токенизатор
Gemma-4 на запиненной ревизии, все 506 строк, за $0: широчайшая строка **2 975 токенов (2 991 со
слэком)** против порога 2 800 и против самого `max_seq_len` 2 816. **Две строки за порогом по истинному счёту, три со слэком** — слэк правил оценку по символам, которую `apply_chat_template` уже считает, и одна строка легла ровно между читками. **Две строки за самим `max_seq_len` 2 816 при обеих читках**, поэтому STOP инвариантен к вопросу. Модель
отношения, из которой выведено 2 816, предсказывает 2 763 и пропускает всё — недооценка **228
токенов**. На `97548df` тот же счёт даёт 2 974 и две строки за порогом при ОБЕИХ читках, то есть ответ по
истинному счёту не двигался вовсе — сдвинулась только читка со слэком, на один токен. Следующая ступень 3 072 покрывает 2 991 с запасом 81 — слово оператора.

**Гейт 1.** Четыре именованные правки словами тимлида · **r3-дельта** одной строкой
(`@retsepty:7342#49685` `null` → `не_наш_рынок`) слоем поверх r1+r2, без выборки единицы · 13 правок
P-NULL по выписанному узкому правилу (широкое дало бы 48 и нарушило запрет контракта) · 31 маркер
P-MARKER · гибридный грep **0 из 515 при контроле 38**.

**Гейт 2.** Пятнадцать текстов переписаны так, что утверждение — о ВЕЩИ, а сеть только МЕСТО; после
этого `self_flagged`, инструмент, который их нашёл, совпадает с **0 из 160**. Метки не двигались
нигде. Скелеты: закодированный измеритель воспроизвёл «16 из 32» вердикта с первого запуска без
подгонки **и нашёл второй скелет над тем же потолком (12)**, которого проза не называла. Посев
переводов строки идемпотентен по построению. Регистровая таблица: **четыре промаха из шести → один
из семи**, и не подгонкой оси.

**3.25 (2) — протечка закрыта, и доказывает это 0.9875.** E: 22 → 0, на платном баре 5 → 0,
обучающие 13 → 0; максимальная похожесть теперь ровно наибольшее значение строго ниже 1.0. Ни один
запрос не потерял пятого соседа.

**Два урока измерения.** Целое число ревьюера — чтение его собственной популяции: линза 2 судила
**113 из 515**, поэтому её «17» и «24» выборочные. И проза записи переживает состояние, которое
описывает, — три раза за контракт.

**РУЛИНГ ОПЕРАТОРА, вечер: «поднимай max_seq_len до 3072».** Та ступень, которую поправка 3.25 (1)
назвала заранее. `config/qlora.yaml` — ревизия 3. Ни одна из 506 строк не за потолком ни по счёту
токенизатора (широчайшая 2 975), ни со слэком (2 991); запас 97 / 81 — обе читки опубликованы.
Запас тонкий, меньше 4 %, **но это счёт, а не проекция** — модель по-прежнему недооценивает
широчайшую строку на 228 токенов, и рулинг этой находки не трогает. `STOP_AT = 2800` остаётся в
продюсере: удалить порог значило бы удалить причину, по которой существует 3 072. Линия B не тронута
второй раз — все три запечатанных артефакта байт в байт при ревизии 3. **Блока в `docs/SPEC.md` под
этот рулинг нет** и написать его — за тимлидом (Dv780).

Верификатор **3 668 passed / 2 skipped**, дерево чистое, все шесть продюсеров пересобирают свои
байты. Четырнадцать отклонений Dv765–Dv778.

## Checkpoint 12:18

Первый чекпойнт дня — контракт `lora-c-apply` исполнен целиком за $0 **и операторский рулинг по
потолку получен и применён**. Десять коммитов от `f5eda67` до `c15b502`, дерево чистое.

**Обе половины сессии:**

1. **`lora-c-apply` (Dv765–Dv779).** Оба вердикта тимлида применены, оба ревью-гейта закрыты, вся
   поправка 3.25 исполнена, регистрация перевыпущена DRAFT'ом без цены. Детали в Summary выше и в
   `docs/reports/lora-c-apply.md`; решение — [[lora-c-rationale-supervision-and-the-shared-pool]].
2. **Рулинг оператора 23.08: `max_seq_len` 3 072 (Dv780).** Ступень, которую поправка 3.25 (1)
   назвала заранее. Ревизия 3 конфига; за потолком **0 из 506 при обеих читках**, запас 97 / 81.
   Линия B прогнана заново и байт в байт. `STOP_AT = 2800` оставлен как история сработавшего
   порога.

**Что закрылось за день:** гейт 1 · гейт 2 · протечка своего текста (22→0, на баре 5→0, максимальная
похожесть ровно 0.9875) · потолок последовательности.

**Что осталось открытым и названо открытым:**

- **Блок в `docs/SPEC.md` под рулинг 23.08 не написан** — закон читается «1 408 → 2 816», конфиг стоит
  на 3 072. За тимлидом (Dv780).
- **Второй скептический проход вердикта не дал** — 28 минут, собственный `make check`, три запроса,
  четыре пустых idle. Второй раз на этой линии после Dv748. Его чеклист прогнан мной и помечен как
  проверка исполнителем своей работы.
- **Непроверено независимо:** то ли правило P-NULL написано, которое имел в виду вердикт. Оно
  недоопределено на 35 строк; выбрано узкое прочтение (13 правок вместо 48) и выписано дословно.

**Три durable-урока ушли в память:** счёт ревьюера — чтение его собственной популяции ·
проза записи переживает состояние, которое описывает · рулинг может обогнать закон, который он
меняет.

Верификатор на закрытии: **3 668 passed / 2 skipped**, exit 0, все шесть продюсеров пересобирают
свои байты.

## lora-c-close 12:54 — поправка 3.26 села в реестр срезки; и один пин, который хеширует СЫРОЙ SPEC

Контракт `docs/PROMPT-lora-c-close.md` — микро, всё $0, Dv781–785. Отчёт:
`docs/reports/lora-c-close.md`. Тимлид написал блок под рулинг 23.08 — тот, что вчера вечером
стоял здесь как «чего нет в законе» (Dv780). Закон догнал рулинг, [[a_ruling_can_outrun_the_law_it_amends]]
закрыт с этой стороны.

**Манёвр посадки — четыре касания, ровно как у 3.25.** `write_prereg_5c2.BLOCKS_TODAY`,
перечисление в `test_sku_prereg`, литеральный хвост `BLOCKS_TODAY[10:]` в `test_prereg_5c2`, и
подсаженный самозванец `-3.26` → `-3.27`. Четвёртое касание не форсируется НИЧЕМ: дубликат имени
всё равно отказывает, поэтому забытый самозванец оставляет сьют зелёным, а следующая поправка
приезжает тихо. Четыре посадки подряд уже вошли в подсаженное имя.

**Закон не сдвинулся, и это утверждение проверено, а не заявлено.** Сырой файл ушёл
`ff300bc7 → b5aa39af`; все три чтения `registered_law` байт-в-байт одинаковы через приход блока —
`973c8789` (пины v1–v4), `3dd43923` (запечатанная десятка 5c2), `6818926d` (B′). Три модуля,
владеющие этими пинами: **88 passed**.

**Главная находка — посылка контракта неверна для одного класса записей.** «Nothing else changes:
old pins predate 3.26, stripping removes it» — верно для всего, что идёт через `registered_law()`.
`write_lora_c_prereg.py:647` зовёт `sha(SPEC)` по СЫРОМУ файлу; это единственный такой call-site в
`scripts/` и `src/`. Поэтому красных тестов на входе было два. Радиус взрыва перечислен от
ЗНАЧЕНИЯ, а не от падений: грep старого sha по `results/ docs/ config/` даёт ровно один файл, грep
старого sha STATUS.md — ноль. Урок ушёл в память: [[the_one_pin_that_skips_the_normaliser]].

**И поле, которое отрицало собственный файл.** `1_max_seq_len_SUPERSEDED` жирным утверждало «**No
marked block in docs/SPEC.md records that ruling yet**» — правда, когда я это писал утром, ложь с
часа, когда тимлид написал 3.26. Пересобрать один sha и оставить фразу — выпустить запись, которая
пинит файл и рядом отрицает его содержимое. Утверждение заменено ЧТЕНИЕМ: три цитаты 3.26 через
`quoted_spec`. Второй виток [[a_reading_that_outlived_its_state]] — и на сам отказ добавлен
негативный контроль, потому что запись, опирающаяся на гард, который никто не видел отказывающим,
это снова утверждение ([[guard_selftest_negative_control]]).

**Открыто и названо открытым:** у STATUS-близнеца `quoted()` негативного контроля по-прежнему нет ·
порог 2 800 из 3.26 (2) живёт законом в одном файле и константой `STOP_AT` в другом · вопрос про
правило P-NULL из `lora-c-apply` всё ещё за тимлидом.

Верификатор на закрытии: **3 669 passed / 2 skipped**, exit 0 (3 668 + один негативный контроль);
`ruff format --check` — 433 файла уже форматированы.

## Checkpoint 17:21

Со времени секции `lora-c-close` 12:54 — закрытие контракта и два последних коммита. Работы после
13:06 не было, сессия стояла.

**Гейт перемерян на ЗАКРЫВАЮЩЕМ дереве.** Показание 3 669/2 в §5 отчёта бралось по кодовому
дереву — до коммитов отчёта, ADR и vault, а `knowledge/hot.md` это ценовой вход для
`scripts/volume_calc_5c1.py`. Гейт, померянный до последних коммитов, померян по дереву, которого
больше нет ([[the_gates_evidence_outlived_its_artifact]]). Переснято на **`4a95be4`** с чистым
рабочим деревом: ruff чист · 433 файла форматированы · **3 669 passed / 2 skipped**, exit 0.
Показание то же.

**Свип, которого контракт не просил.** Дифф тимлида по дню 23.08 не только добавлял — он **удалил**
куски прозы дня 18–19 (`5.162 с/коммент`, `(метки 8d2e1ba)`, `reader-topup ($1.2259)`, строки
долгов). Продюсер, который грепает дословную строку из STATUS и НЕ приводится в движение
rebuild-тестом, стоял бы сегодня зелёным и упал бы `SystemExit` на следующем контракте. Ответ:
`docs/STATUS.md` с диска читает **ровно один** продюсер — `write_lora_c_prereg.py:145` — и он в
списке `PRODUCERS`, так что зелёный сьют это уже доказал. Открытого пункта не добавилось.

**Четвёртый открытый пункт — про сам отчёт.** Он длиннее, чем «short» в контракте, и Process
signals не в одну строку. Раскрыто в отчёте, а не подрезано: длина — это та самая евиденция,
которую требует шаг 4. Исполнитель, доложивший о правке одного слова в порядковом числительном,
должен доложить и об этом.

**Коммиты закрытия:** `4d948d4` (`docs(report)` — переснятый гейт + свип) · `6fc6773`
(`chore(vault)` — хвост Stop-хука). Дерево чистое, HEAD `6fc6773`, восемь коммитов от `c15b502`.
**Ждём приёмки `lora-c-close`.**

## lora-c-run 18:09 — STOP до создания пода, $0

Контракт `docs/PROMPT-lora-c-run.md` покупал измерение lora-c: четыре леги на одном платном поде,
кап $4.00. **Под не создан, потрачено $0.0000, попытка не израсходована, регистрация всё ещё
DRAFT.** Отчёт — `docs/reports/lora-c-run.md` (STOP-отчёт, прецедент `docs/reports/lora-c-prep.md`).

**Ридбэк-гейт прошёл целиком.** Все десять чисел переведены из файлов, которые ими владеют: шаги
62/82 и `planned` 64/84 из `config/qlora.yaml` (micro 2 · accum 8 · epochs 2), 506 строк с диска,
198 вызовов на легу из пака, 11 тредов / 46 строк из пака прохода-2, потолок 3 072 и порог 2 800
из закона, ставки 6.14 и 97 из регистрации, кап 4.00 из рулинга (к). Плюс два подтверждающих
чтения: 12 из 12 запиненных инструментов хешируются во что записано, и все восемь цитат (`quoted`
× 2, `quoted_spec` × 6) грепаются обратно после того, как тимлид переписал день 23 в `STATUS.md`.
Ровно это `docs/reports/lora-c-close.md` §«Open» 1 называл «проверено мной, один раз, руками».

**Первая находка — цена.** Вывод цены, который поправка 3.25 (1) сделала условием капа («the cap
stays $4.00 until that derivation reports»), готов и записан в
`results/prereg_lora_c.json::money.pre_pod_arithmetic`. **Ни одной s/step не зарегистрировано** —
её покупает смок. Зарегистрировано **неравенство, решённое наоборот**: фиксированная часть
11 019.88 с, при $0.80/ч это $2.4489 и на 144 шага остаётся **48.47 с/шаг** (58.61 при $0.74/ч).
Единственные два ЧТЕНИЯ — 61.047 и 68.442 — выше обоих порогов и оба сняты на ≤1 222 токенах против
здешних 1 445–2 975. По измеренным сиблинг-ставкам порог поднимается до 93.47–103.20: сессия
коротка, но не безнадёжна, и решает её смок, а не арифметика. **Поэтому решение — оператора, а не
исполнителя.**

**Вторая находка — арм B.** 666 строк зарегистрированы как `legs.arm_b.train_rows`, файла нет,
продюсера нет, и синтетические 160 — сырые комментарии без `prompt`/`target`/`learn_chars`. Не
хватает не только продюсера: **нигде не сказано, как синтетическая строка рендерится как
v3-запрос**. И порядок вынужден: `pod create` — это заморозка, значит create до появления файла
запечатает регистрацию, которая не может назвать половину эксперимента. **Поднятие капа это не
разблокирует.** Тест `test_the_registration_names_no_arm_b_dataset` фиксирует разрыв.

**Что сдано за $0 и зелено.** Два долга close: негативный контроль `quoted()` против `STATUS.md`
(обе цитаты, обе стороны) и привязка `STOP_AT` к 3.26 (2) через цитату в записи — число **парсится
из цитаты**, второго литерала в тесте нет. Sibling-тренер `scripts/train_qlora_v3.py`: ровно два
гарда перенесены, остальное ВЫЗЫВАЕТСЯ; ценз кодировки 506/506 через настоящий `encode_pass1`, 0
отказов, максимум 2 975, запас 97; три отказа по имени; и контроль, что сам `train_qlora`
по-прежнему отказывает v3-строке.

**Проверка, что запись сдвинулась только там, где хотели:** оба состояния сплющены в пути ключей —
37 добавленных полей, все под `.money.pre_pod_arithmetic`, 0 удалённых, 2 изменённых
(`.money.state`, `.producer.sha256`). Ни одного дрейфа в записи, по которой оператора просят
принять решение.

**РУЛИНГ ОПЕРАТОРА, тем же вечером, по этому отчёту.** Оба вопроса заданы с арифметикой перед
глазами и оба отвечены: **(1) сначала $0-контракт на датасет арма B** (тимлид правит рендеринг
синтетической строки как v3-запроса, продюсер рендерит 666, регистрация пинит файл), потом ОДИН
`lora-c-run` с обоими армами; **(2) кап остаётся $4.00**, пересчёт — в контракте, который знает
свой объём. Это опция 3 отчёта, единственная, при которой абляция A-vs-B вообще случается.
**Рулинг ждёт регистрации тимлидом в `STATUS.md` п. 1** — исполнитель туда не пишет. Датасет арма
B этим контрактом НЕ строится: это новый объём и решение по рендерингу — тимлида.

**Коммиты:** `ed1c341` (файлы тимлида) · `251afcd` (долги close) · `7c7035e` (sibling-тренер) ·
`1c84944` (вывод цены). Верификатор — базлайн **3 669/2** до единой правки, закрывающий прогон в
отчёте.

## Sessions
- 08:58: session ended (auto)
- 08:59: session ended (auto)
- 11:34: session ended (auto)
- 11:35: session ended (auto)
- 11:36: session ended (auto)
- 11:38: session ended (auto)
- 11:45: session ended (auto)
- 12:08: session ended (auto)
- 12:17: session ended (auto)
- 12:19: session ended (auto)
- 12:47: session ended (auto)
- 12:58: session ended (auto)
- 13:06: session ended (auto)
- 17:22: session ended (auto)
