---
type: daily_log
date: 2026-08-02
tags: [daily]
---

# 2026-08-02

## Summary

**Step 4c executed end to end — the one attempt is spent, and the report is with the team lead.**
The run crossed the date boundary: it began 2026-08-01 13:33 UTC (see [[2026-08-01]], which closed
with 4c "not started") and finished 2026-08-02 00:03 UTC. Both ablation arms trained in full on the
frozen config, each was scored on the frozen sets **exactly once**, the pre-registered rule was
applied mechanically, and the five Tier-1 gates were decided. **Nothing was retrained, re-scored or
reconfigured after a gate number was seen.** Record: [[phase4-gate-verdict]] (status `proposed`).

**The verdict — 2 of 5 Tier-1 gates pass, on the selected `real-only` arm:**

| gate | value | bar | |
|---|---|---|---|
| G1a overall | 0.9107 | ≥ 0.9418 | **FAIL** |
| G1a `ua` / `ru` floors | 0.9146 / 0.8919 | ≥ 0.8718 / 0.8649 | pass |
| G1b | **21 / n=44** | ≥ 27 | **FAIL** (guard +0.0189 passes) |
| G1c | 0.8212 | ≥ 0.8436 | **FAIL** |
| G1d | 0.9386 | ≥ 0.8984 | **PASS** |
| G1e | 0.9333 | ≥ 0.8874 | **PASS** |

The two that pass are amendment 3.5 (2)'s no-regression gates, and they pass by **+3.02** and
**+3.59 pp above the anchor** rather than by holding a line — the multi-task forgetting risk they
were rewritten to measure did not appear at all. The three that fail are margin gates and fail **by
margin, never by regression**: every gated head of both arms sits above the zero-shot anchor, and
both arms' ≤2 pp G1b guard is positive. A failed gate closes its question (SPEC §5).

**The synthetic source is dropped, on the half of the rule it was written for.** `with-synthetic`
won G1b outright — **24/44 against 21/44**, the largest single-head move either arm made — and lost
the second clause: G1c −1.39 pp and G1d −2.27 pp, both past the 0.5 pp tolerance. Exactly the trade
amendment 3.4 (3) pre-refused before any of these numbers existed. Both columns are published; there
is no third run. [[synthetic-sarcasm-augmentation]] stands as the record of how the rows were made
and QA'd — the ablation has now measured them.

**Recorded beside the verdict, not inside it:** `with-synthetic`'s G1a `ru` is **+4.44 pp** over
`real-only` (0.9364 vs 0.8919), the biggest per-language gap in the table. The per-language numbers
are floors of the G1a gate, not heads the rule reads; both arms clear both floors; the rule was
fixed first. It is the strongest sign in this ablation that the generated Russian rows did something
real, and it changed nothing.

**What G1b's failure is made of**, read off the committed dumps and not re-scored: of the 23 slice
rows `real-only` misses, **15 are wrong on sarcasm only, 8 on both, 0 on sentiment only**. Across
the whole 108-row holdout — every row gold `sarcasm: true` — `real-only` detects **82** and
`with-synthetic` **83**, against the base model's **64**. Both arms moved that head ~19 rows and
neither cleared a bar defined as 60% of the base model's own errors ([[hybrid-sarcasm-holdout-3.2]]).

**Both arms ran clean and inside every box.** Arm A **270 steps / 3.40 h**, arm B **346 steps /
4.17 h** — both under 4b's own projections (3.42 / 4.37 h) and well under amendment 3.6's 5 h
ceiling. Peak GPU 30.86 / 30.84 GB of 48; micro-batch 2 × accum 8 held throughout, the OOM branch
never fired. **758/758 rows scored on both evals with zero failures of any kind.** Adapter hashes
were computed on the pod and on the Mac and matched before either record was appended.

**Resume is proved against the real stack** — [[4b-training-contract]] §(h)'s question, closed in
the first ten minutes of the first pod: ten steps, reload with `is_trainable=True` onto the k-bit
base, `torch.load` of a real 238 MiB paged-AdamW state, five more steps. The optimizer step counter
continued (10 → 15, no reset) and the loss stayed on trajectory (0.10974 → **0.05680**, carve
0.11247 → 0.05915). `assert_resumable`, added for this, stayed silent.

**Protocol compliance is checkable, not asserted.** Arm A's record is stamped `18:37:47Z`; arm B's
training process was running at `18:49:37Z`. Arm B launched on the frozen config eleven minutes
after three of arm A's gates were known to have failed, and nothing was changed between them. Both
eval checkpoints show `0 rows already scored` — neither eval ever resumed, so nothing was re-scored.

**One defect caught before the pod could charge for it.** `eval_zero_shot.main` ends by printing
what it wrote, and the G1b-slice line was guarded by `anchor_valid` alone while the value it
dereferences is `None` on a fine-tuned arm. It raised `TypeError` **after** the record was already
written — a traceback and a non-zero exit at the end of a 45-minute eval following a 3.4 h training
run. Found by the test that now drives `main` through the whole `--record-out` path with stubbed
weights; `--smoke` returns before the record is built and `--probe` before it is written, so that
path had never run. Fixed in `34a27d7`.

**The deliverable is `results/train/4c-arm-a/adapter`** — the `real-only` LoRA adapter, sha256
`c0e462af81aad9f1…`, served **unmerged** on the same NF4 4-bit base every gate was scored through.
The `with-synthetic` adapter (`0566900e3f42451e…`) is kept beside it: the ablation's second column
is evidence, not waste.

**Money: $6.8779 of the $25 Phase 4 cap** (4c itself $5.74 against ~$5.2 projected), remaining
$18.12. Both 4c pods deleted — volume-less, so a stopped one bills 80 GB by the month.

**Commits (10):** `42ac2ea` team-lead SPEC rev. 3.6 / STATUS / PROMPT-4c verbatim · `b690f02` the
arm eval path with its crash-resume checkpoint · `01aa6c9` the selection rule and verdict script ·
`16e3af1` the 4c runbook and `assert_resumable` · `411bd84` gitignore for the optimizer state ·
`34a27d7` the eval's last-line crash · `8314dfb` arm A · `d5c6a24` arm B, the rule and the verdicts.
**313 tests, `make check` green after every one.**

## Decisions

- **[[phase4-gate-verdict]]** (`proposed`, awaiting the team lead) — both arms' full columns, the
  rule's arithmetic, the five verdicts, the resume proof, the cost, and six findings.
- The rule of amendment 3.4 (3) is **applied, not re-argued**: `scripts/gate_verdict.py` prints its
  inputs and its output, and the ADR quotes that output rather than paraphrasing it.
- **Nothing was fixed mid-phase.** Six findings were recorded and left alone, on one rule: 4c is one
  attempt and the arms must run identical code, so a fix landing between them would be a confound.
- Bars and head deltas now round to ten decimals in the scorer — `0.90 + 0.05` is 0.9500000000000001
  and the gates are "≥", so a model exactly on its bar must not fail on the last bit of an addition.

## Findings for Phase 5 (none gate-relevant)

1. `planned` over-counts steps by one per epoch — 272 planned, **270 run** — because the epoch's
   micro-batches do not divide by the accumulation.
2. Those leftover micro-batches' gradients are never zeroed, so epoch 1's first step folds in 6 of
   epoch 0's. One step in 270 with a 1.75× effective batch.
3. The post-loop `save()` records the loop variable rather than the stop position after an early
   `--max-steps` (`epoch 1 row 0` instead of `epoch 0 row ~160`). Unreachable by a full arm; the
   in-loop `save_every` write is the same `save()` from a correct call site.
4. `seconds_per_step` in a **resumed** provenance is understated — this process's clock over the
   cumulative counter (15.8 s against a real ~43 s).
5. **Training is not bit-reproducible on this stack**: 0.18010 vs 0.18051 at step 5 on identical
   data, seed and config on the same pod. Cause not isolated (NF4 reduction order, or the LoRA
   dropout mask sequence). Say "paired on data and config", never "identical".
6. The eval's last-line crash, above.

## Next

1. **The 4c report is with the team lead. STOP — there is no self-acceptance.** Phase acceptance
   (team-lead verification, operator quiz, RECORD sweep, STATUS) happens on that report. Until it
   lands, nothing in Phase 4 is retrained, re-scored or reconfigured — the one attempt is spent.
2. **Open questions, all the operator's:** what three margin-failed Tier-1 gates mean for the phase
   when every head is above its anchor and G1d/G1e clear by ~3 pp; the stale 4a pod
   `gxkdecf3g7k3y7` (`EXITED`, on the CA-MTL-3 volume) still billing; and Phase 5's already-named
   first measurement — **do not merge the adapter into bf16 without scoring the merged artefact in
   the configuration production serves**.
3. Still open from Phase 2 (not a blocker): dataset cards for the public augmentation datasets +
   licence check.

## Blockers

**None open.** 4c raised no escalation: the one pre-registered stop it could have hit — a projection
crossing the 5 h per-arm ceiling — did not fire (3.40 h and 4.17 h). The three items 4b escalated
were closed by SPEC rev. 3.6 before this step began, and the third of them (prove resume first on
the 4c pod) was executed and **passed**.

## Sessions
- 02:07: session ended (auto)
- 10:16: session ended (auto)

## Checkpoint 12:42

**Phase 4 is CLOSED at 2 of 5** — `docs/PROMPT-4-close.md` rev. 1 executed, five mechanical items,
no judgement calls and nothing measured.

- **Acceptance landed.** The team lead recomputed every verdict number bit-exact from the per-row
  dumps, re-ran the selection rule (output matched word for word), confirmed the one-attempt
  protocol from artefacts, and the operator's quiz came back **2/2**.
  [[phase4-gate-verdict]] front-matter flipped `proposed` → `accepted`, mirrored in
  [[INDEX]]. That flip *is* the acceptance instruction the `proposed` status was waiting on.
- **The stale 4a pod is gone.** `runpodctl pod delete gxkdecf3g7k3y7` → `{"deleted": true}`;
  `pod list -a` is now `[]`. The 100 GB CA-MTL-3 network volume `gfwa2an8fn` **survives on
  purpose** — checked after the delete, its fate is a Phase 5 briefing decision.
- **Ledger appended** (`results/spend_phase4.json`, via `scripts/runpod_guard.py --note`):
  balance $28.01, **$6.9903 of the $25 cap, remaining $18.01**. The $0.1124 above the 00:00 reading
  of $6.8779 is the network volume plus the stale pod's container disk billing through the morning
  — the first direct measurement of what "the volume bills with no pod attached" costs per half-day.
- **Not committed here, deliberately:** `docs/SPEC.md` (rev. 3.7) and `docs/PROMPT-4.5a.md`.
  `PROMPT-4.5a.md` §0 claims both for its own commit with its own message, so this close-out left
  them in the tree rather than absorbing them. hot.md's Next says so, so the next session cannot
  read them as work already begun.
- **Deviation (one, procedural):** the prompt lists the commit as item 1, and it was made last
  instead. Items 2–4 all touch tracked files (the ADR, the INDEX, the ledger, hot.md), so
  committing first would have forced a second commit and broken the item's own "one commit".
  Nothing else in the order changed.

`make check` green. Phase 5 stays PAUSED; the next thing is the operator's own discussion.
- 12:44: session ended (auto)

## Checkpoint 13:00

**Phase 4.5 is open and 4.5a's adjudication pack is built** — `docs/PROMPT-4.5a.md` rev. 1, steps
0–2. Nothing was judged, re-scored, trained or rented. $0.

- **Step 0 was a no-op check plus one commit.** `PROMPT-4-close` had already run
  ([[phase4-gate-verdict]] is `accepted`, `pod list -a` is empty), so the two held-back team-lead
  files went in verbatim as `b2772ce` — `docs/SPEC.md` rev. 3.7 and `docs/PROMPT-4.5a.md`.
  `docs/STATUS.md` was named by the prompt but was already committed clean in `378f467`, so it is
  not in that commit; nothing in it had changed.
- **The pack is 180 rows** (`40923da`): sentiment 35 · intents 67 · slice pair 23 · posts 15
  (post_type 11 + brands 4) · control 40. Source of truth is arm A's persisted dump, selected by
  `records.arm_record(history, "real-only")` and checked against the `predictions_sha256` the
  record stored — the audit reads the dump the gates were scored from or it audits something else.
- **Blinding, and what was actually checked.** Per-row seeded coin flip, both candidates through
  one renderer so a set's iteration order cannot say which side built it; key split 68 A / 72 B
  over 140 rows. The structural cells (every column but `text`/`notes`, plus headers and file
  names) carry no attribution vocabulary — and the check was proved to fire on a planted
  `label_gold` before being believed. **Distributional blinding was measured, not assumed:** on the
  67 intent-mismatch rows the gold and model set-size distributions are 0/1/2 → 15/35/17 against
  18/36/13, and both label distributions overlap heavily, so there is no tell to learn.
- **Two ceilings, deliberately.** `scripts/audit_ceiling.py` prints a metric-unit ceiling by
  re-scoring a simulated perfect model through `scorer.py` (an upper bound: it sees only the
  disagreement stratum) **and** an accuracy-unit ceiling over both strata, with the sentence that
  they are not comparable. The reason is concrete: the gates are macro/micro-F1 and a gold error on
  a rare class costs F1 far more than it costs accuracy, so an accuracy share read against a 0.98
  target stated in F1 would be a category error.
- **The number to distrust first when it lands:** the agreement stratum is ~90% of every head and
  is measured at n=8, so one extra `incorrect` moves that head's ceiling by ~12 pp. The harness
  prints the sensitivity band beside the point estimate rather than a lone number.
- **The brands stratum is n=4** — it cannot support a ceiling estimate at all, and at least one of
  the four is a Unicode-homoglyph normalisation difference rather than a semantic disagreement, so
  the effective semantic n is ≤3. Flagged to the team lead; the operator still rules the rows.

`make check`: **330 passed** (was 313), ruff clean, `ruff format --check` clean. Rebuild is
byte-identical for all five CSVs and the key. `data/frozen` is untouched and its five shas still
match `docs/frozen-testsets.md`.
- 13:04: session ended (auto)

## Checkpoint 13:28

**The 4.5a pack is accepted, the ceiling formula approved, and the pack is now 244 rows** —
`docs/PROMPT-4.5a-add.md` rev. 1, three steps. Still $0, still no pod, still nothing judged.

- **Control widened 40 → 104** (sentiment 40 · intents 40 · sarcasm_pair / post_type / brands 8
  each). The two expanded heads carry the largest agreement strata and the ceilings a phase
  decision is read off. Measured effect: one extra `incorrect` now moves sentiment **2.28 pp** and
  intents **2.08 pp**, against **11.41** and **10.41 pp** at n=8.
- **The expansion did not resample the accepted pack, and that was verified rather than asserted.**
  All four disagreement CSVs and the key are **byte-identical** (`cmp`) to the accepted build, and
  the accepted 40 control rows are present **verbatim** inside the 104 (set comparison of whole
  rows, columns unchanged, no duplicate ids). The mechanism: every head draws its `BASE_CONTROL` 8
  before any head draws a top-up, so the seeded stream up to that point is the stream that produced
  the accepted rows. Rebuilding was legal only because every verdict cell was still empty — checked
  first, and `has_verdicts` refuses without `--force` from here on.
- **Two hardenings.** `read_csv` refuses any column that is not the sealed shape, naming it: a
  spreadsheet round-trip is how a column actually appears, and reading by name would not notice an
  extra one that could hold an attribution or a second opinion. And the in-session blinding grep is
  now `build_audit_pack.blinding_sweep`, run by the builder itself, with **the negative control
  committed as a test** — it plants a `label_gold` column into a generated pack and asserts the
  sweep fails it. A sweep that has never failed proves nothing about what it would catch.
- **The homoglyph finding is FILED, not fixed** (`implementation-notes.md` → Phase 4.5a, and a
  hot.md footgun). `normalise_brand` does not fold Unicode homoglyphs, so a Cyrillic `о` inside a
  Latin-script brand misses the alias table and two identical-looking strings score as two
  entities. Fixing it now would silently redefine every future G1e number against Phase 3, the
  own-pod anchor and both arms — a re-scoring decision, not a bug fix.

`make check`: **341 passed** (was 336), ruff and formatter clean. Double build byte-identical.
`data/frozen` clean and its five shas still in `docs/frozen-testsets.md`. Manifest updated; the
previous CSV shas stay readable in git history.
- 13:30: session ended (auto)

## Checkpoint 15:49

Closing out `docs/PROMPT-4.5a-add.md`: one more commit after the 13:28 checkpoint, and the pack is
handed over.

- **`492502c` — one blinding check, one word list.** `test_the_pack_never_names_a_side` carried its
  own five-word copy of the attribution vocabulary, written before `audit.ATTRIBUTION` existed.
  Add a word to the real list and that test keeps passing while claiming to check blinding. Deleted
  — `test_blinding_sweep_passes_a_clean_pack` covers the same ground through the function the
  builder actually refuses on. **340 passed** (a net +4 over 336: five new, one removed).
- **Reported to the team lead:** 244 rows; control 104 (40/40/8/8/8); the four disagreement CSVs and
  the key byte-identical to the accepted build; harness hardened and still unrun. Three deviations,
  all additive — the builder now runs the blinding sweep and refuses on it (the prompt asked only
  for the test), `read_csv` also refuses a *missing* column (the prompt said unknown), and this log.
- **One open question left with the team lead:** `--force` is now the only rebuild path once the
  operator starts filling, and it **destroys partially-filled CSVs with no snapshot**. Those CSVs
  are the one artifact of this phase no generator can reproduce. Nothing in the current flow needs
  `--force`; the question is whether a copy-aside should be mandatory before it proceeds.

State: five commits today after the phase-4 close (`f1a1221` team-lead files · `bc88a9f` expansion
and hardening · `64ac276` the homoglyph finding filed · `492502c` the test dedup, plus `08732ee`
from the earlier step). Tree clean, `make check` green, nothing judged and no verdict pre-filled.
- 15:50: session ended (auto)

## Checkpoint 16:50 — `PROMPT-4.5b`: the returns ingested, the harness run once

The operator's arbitration came back complete (244/244) and team-lead-verified, sha-pinned in
`data/annotation/audit_45a_returned/`. Executed the prompt end to end; **$0, no pods.**

- **`62784c8` — team-lead files.** `docs/STATUS.md` (arbitration closed, 4.5b opened) and
  `docs/PROMPT-4.5b.md`, staged by path. The three `knowledge/` files left over from the 15:49
  `/save` stayed dirty for the record commit.
- **`471cc64` — `scripts/normalize_audit_returns.py` + 8 tests.** Semicolons and long-form verdicts
  (`B — правильная метка label_B`) → the sealed pack's own format. Every output row is the **sealed**
  row with one cell replaced; blanking the verdicts again must reproduce the sealed bytes, which
  covers quoting, column order and line endings at once. Verdicts derive twice (table + leading
  token). Refusals: an unpinned return set, a cell changed outside `verdict`, an unknown form, a
  form whose words and token disagree, and a pack that is neither sealed nor already-normalized —
  the last makes a rerun a no-op. `git_state` gained the record-path parameter so it could be reused.
- **Verified outside the script:** 244 verdict cells filled, **0** non-verdict cells changed, CRLF
  and comma preserved. Tallies match the team lead's independent count exactly (A19/B16 ·
  A32/B33/amb2 · A3/B20 · A7/B8 · 81/23). Frozen sets, predictions dump, G1b slice and the key all
  still match `results/audit_45a_manifest.json`; the filled pack was re-hashed after `make check` to
  prove the suite never touched it.
- **The harness ran, once, and every refusal passed on the way.** Ceilings in
  [[phase45a-ceiling]] (`proposed`, numbers only, linked in [[INDEX]]): sentiment 0.9613 macro-F1 /
  0.9625 accuracy · intents 0.9148 micro-F1 / 0.4596..0.4646 · sarcasm_pair 0.6591 both units ·
  post_type 0.9683 / 0.9800. G1e excluded from the arithmetic (team-lead decision), verdict counts
  only. **22 of the 23 control `incorrect` are in `intents`.**
- **Deviations, all in `implementation-notes.md` § Phase 4.5b:** there are no git-HEAD copies of the
  empty originals (`data/annotation/*` is gitignored) — the authority used was the committed
  manifest's per-CSV sha256, which all five matched; the record is the sibling
  `results/audit_45b_returns.json` rather than an extension of the sealed manifest; G1e is printed
  by the unmodified harness but excluded from the ADR's arithmetic.

State: `make check` **348 passed** (+8). Nothing was judged, re-scored, or written back to a frozen
file; the 4.5a gate review decides everything downstream.
- 16:55: session ended (auto)

## Checkpoint 18:10 — `PROMPT-4.5c`: the law pack, test v3, and every dump re-scored

Gate 4.5 decided two things (operator): the intents law goes to a face-to-face review, and test v3
is approved as 38 point fixes with intents untouched. Both executed; **$0, Mac-only, no model
calls.**

- **`29d63e1` — team-lead files** (`docs/STATUS.md`, `docs/PROMPT-4.5c.md`), staged by path.
- **`0906de6` — the intents law-review pack.** `data/annotation/audit_45a/intents-law-review.md`
  (Russian, gitignored; builder committed): the guideline's `[]` rules **sliced by anchor** out of
  `docs/annotation/comments.md` with line citations, then 19 + 6 + 3 row blocks. Nothing retyped,
  nothing recommended; the end-to-end test runs against the real guideline so a vanished anchor
  fails the suite, and the page is swept for attribution words outside the quoted texts.
- **`5054a90` — test v3.** Three new files (400/108/250 rows; 15/15/7 rows changed), v2 untouched.
  `scripts/freeze_testsets_v3.py` refuses any count but the gate's 15/15/5/3, takes each fix from
  the arm's own dump, and re-serialises every unruled row to prove it reproduces its v2 line byte
  for byte. `docs/frozen-testsets.md` quotes the generated changelog and a test asserts it still
  does. **`intents` byte-identical to v2** — 31 rulings derived, 0 applied.
- **`c75504e` — every dumped run re-scored.** `results/rescores_v3.json` (three runs; the eight
  runs without a dump are named on every run and skipped). Arm A: G1a 0.9107 → **0.9499**, G1d
  0.9386 → **0.9705**, G1e 0.9333 → **0.9744**, G1b **21/44 → 36/44** on the pre-registered slice.
  Arm B G1e falls 0.9577 → 0.9189. G1c and relevance unchanged to the last decimal.
  `show_results.py --gold v3` renders it beside v2.
- **Two G1b readings**, neither called *the* fix-rate: the pre-registered 44 ids vs v3, and the base
  model's error union recomputed vs v3 — **29 ids**, because 15 of its 44 "errors" were gold's.
- **Recorded:** [[test-v3]] (`proposed`, in [[INDEX]]), implementation-notes § Phase 4.5c with five
  deviations, hot.md.

State: `make check` **369 passed** (+21). Phase 4's verdict stays a v2 result and is not reopened;
nothing touches `intents` until the law review.
- 17:26: session ended (auto)

## Checkpoint 18:45 — 4.5c closed out: the verify-gate re-derived, one stale sentence fixed

- **`d103a34` — the version block.** `docs/frozen-testsets.md` had the title changed to name v3
  while the paragraph under it still read "after v2 the sets are immutable again"; the file is the
  authority on versions, so a reader stopping there learned the wrong thing. The "Sarcasm holdout
  (G1b)" section now also says that 15 of its rows had gold wrong, all inside the pre-registered
  44-id slice — that is where the G1b denominator moves and where someone reading the definition
  is actually looking.
- **The verify-gate was re-derived, not read back.** The 38 rulings / 37 rows / 39 field changes
  were recomputed here from the pack CSVs + the sealed key, independently of
  `results/frozen_v3.json`, and the id sets matched. Added afterwards: **all 15 fixed holdout rows
  are inside the 44-id slice and all 15 were shown in `slice_unfixed.csv`** — a fix in that head
  that the operator never saw would otherwise have been invisible. `intents` byte-identical to v2
  in all three files; only ruled rows moved (15/7/15).
- **Durable lesson → native memory:** [[test-v3]]'s G1b problem generalises — correcting gold moves
  any denominator *derived* from gold, so "the model's errors" silently becomes a different
  question. Report both readings, name which is which, persist the recomputed ids.
- **Queued, not touched:** `docs/PROMPT-4.5d.md` is untracked and `docs/SPEC.md` / `docs/STATUS.md`
  are modified — team-lead files, waiting for that prompt's own step 0. Nothing of mine is staged.

State: `make check` **369 passed**, working tree clean of executor files. The intents law review is
the operator's clock; nothing touches `intents` until it happens.
- 18:45: session ended (auto)
- 19:30: session ended (auto)

## Checkpoint 19:31 — 4.5d: taxonomy v2 prepared, probed and priced

`docs/PROMPT-4.5d.md` executed end to end, 7 atomic commits (`838da4e` step 0 → `199225d`),
`make check` green after each, `ruff format --check` clean. **398 passed** (+29).

- **The trap worth remembering.** `records.assert_prompt_sha` builds its map out of
  `prompts.TASKS` and compares whole maps, so registering `T1v2` *there* would have made every
  recorded run fail to verify. `TASKS` stays `("T1","T2")`; the v2 prompts live in `PROMPTS`, and
  a test runs the real records through the real check with a negative control that widens `TASKS`
  and requires the failure. Same reasoning kept `scorer.INTENTS` at five members —
  `run_baseline.py` and `train_xlmr_baseline.py` build one classifier per member.
- **Guideline v2** (`f26ae7c`): the `service` class, the `availability` boundary, the promo-mechanic
  rule that used to send rows to `[]`, a giveaway rule, and two sarcasm examples relabelled. It
  broke `build_intents_law_pack.py` (anchors), so that script now reads the guideline at the pinned
  revision the operator ruled on — the pack rebuilds byte-identically (`c6642920…`).
- **The probe** (`ba0df91`, `199225d`): 50 train rows, `qwen3.6-27b`, $0.0081. Headline drift was
  computed over a draw that is **half `unclear`**; split, the scoreable half (n=25) says 64%
  changed, **40% carry `service`**, 24% changed without gaining it. The split was re-derived from
  the rows the paid run wrote (`--from-rows`), not by re-running — greedy is not deterministic
  across a provider's batches.
- **The corpus is nearly out** (`960809b`): 1,912 labelable rows after the two exclusions that
  decide the count (3,771 already labelled, 1,239 in evaluation threads). **Zero** sarcasm
  candidates left — the highest irony score in the whole remaining pool is 1. All three appetite
  tiers are unreachable; +2k is ~2 weeks of collection away, +9k is 16 months or a new source.
- **Open for the gate:** three boundary rulings in the guideline are mine, not the operator's;
  "all labelled data" is 3,771 rows, not the 2,746 the prompt names; a re-labelled row cannot say
  who moved its intents. All five are listed at the end of `docs/taxonomy-v2-prep.md`.

State: ledger `results/spend_45d.json` **$0.0187 of $2.00**, 4 entries, anchored before the first
request. Nothing Phase-5-shaped touched; 4.5e waits for the appetite decision at the 4.5d gate.
- 19:34: session ended (auto)
- 20:22: session ended (auto)

## Checkpoint 21:24 — 4.5e: the re-label ran, and the pack is with the operator

`docs/PROMPT-4.5e.md` executed end to end, seven atomic commits `6f87384`→`946a60b`. The 4.5d gate
came back decided: all 1,912 labelable rows get up-labelled, but **only after** this re-label
passes ≥90%; the three boundary calls in guideline v2 are ratified as written. That decision is now
an ADR — [[taxonomy-v2-relabel-and-appetite]] — beside [[test-v3]], whose staging convention it
follows (v2 copies beside originals, `annotator` never rewritten, migration provenance in the run
record).

- **The runner grew a full pass** (`497a2f9`). `--phase` is required and has no default, because a
  forgotten flag would have charged 4.5e's rows to 4.5d's anchor and 4.5d's $2.00 cap. Beside it:
  `--skip-frozen` (drops the 54 frozen holdout ids and counts them; the post-draw guard still runs),
  `--resume` (re-checks every line already on disk against its source before adopting it), four
  workers behind a locked budget, and a per-file identity — `source = staged + skipped + unusable` —
  asserted rather than reported.
- **The run** (`05e2320`): 3,317 rows in three files, 54 skipped, **3,249 staged**, 14 with no
  readable answer. 3,263 labelable + 508 frozen = 3,771, which is how the file list proves itself
  complete. **$0.5448 of $1.50** against the anchor. First pass 10.5 minutes; three `--resume`
  passes took the unreadable rows 66 → 29 → 18 → 14 for $0.017, and the ids repeat between passes,
  so that residual is the model and not the transport.
- **The sharpest finding: 13 of the 14 carried `[]` under v1.** The hole is concentrated in exactly
  the class the taxonomy question is about, which is why coercing them to `[]` — free, and the
  majority answer — would have been the wrong kind of tidy.
- **Drift over the staged rows**, re-derived with no requests: 45% of intent sets changed, 31%
  carry `service`. On the 2,059 rows a gate scores: **49% / 34% / 15%** (changed / `service` /
  changed without gaining it). The probe's n=25 said 64 / 40 / 24 — a scoping figure now replaced by
  a measurement. The churn matrix says where it came from: `price` loses 184 rows, 166 of them now
  carry `service`; `availability` gains 166, and 149 of those were `[]` — drift no rule explains.
- **The pack** (`296bb88`, `cf208a1`): `data/annotation/calib_45e/`, 100 gated + 50 diagnostic,
  disjoint, manifest `results/calib_45e_manifest.json` with every sha. Two things fixed before any
  verdict exists: the denominator (`correct` / 100, a blank cell counting against the bar), in the
  README and the manifest in the same words; and blinding — no column and no ordering says which
  rows moved.

State: `make check` **413 passed**, `ruff format --check` clean, working tree clean, `TASKS` and
every v1 prompt SHA untouched. **The ≥90% gate is undecided** and nothing here reads it. Open for
the gate: the 1,190 `unclear` staged rows the calibration never samples (37% of the file); the 54
frozen ids that will have two homes after 4.5f; the 14 rows still on v1; and that no script reads
the returned CSVs back yet — the returns processor is the first thing the next step builds.
- 21:27: session ended (auto)
- 21:51: session ended (auto)

## Checkpoint 21:56 — 4.5f: the gate is decided, and it is PASS

`docs/PROMPT-4.5f.md` executed end to end, six atomic commits `d81bb0a`→`91952ca`, **$0, no API
call**. The calibration verdicts came back and the re-label is **accepted: 100 of 100 gated rows
`correct`, 100% against a bar of 0.90**. Everything downstream that was waiting on this gate is now
unblocked; nothing downstream was started.

- **The gate is computed, never eyeballed** (`826d08e`). `scripts/read_calibration_returns.py`
  rebuilds the sealed pack first — `load_pairs` → `strata` → the two row builders, seed 42, sample
  sizes read out of the manifest rather than the builder's constants — serializes it in the
  builder's own dialect and requires the result to hash to the pinned sha256. It does (`f26fb86c…`,
  `6192643a…`), and only then are the returned cells compared one at a time, because a spreadsheet
  round-trip changes bytes and no rows. Verdict forms live in a named case-folded table (`Old` and
  `old` are one verdict); an unregistered form stops the run; a blank gated cell counts as
  disagreement, which is what the manifest registered before the pack shipped. Record:
  `results/calib_45e_verdict.json` — the rule verbatim, 150 per-row verdicts, both sets of shas.
- **The counter-signal stands beside the number, not under it.** `gate` 100/100 = 1.00 PASS;
  `diagnostic` 47 `new` / 2 `old` / 1 `neither`, labelled as unable to move the verdict. STATUS.md
  pre-committed to that placement and the record has its own test for it.
- **Three rulings applied** (`dce2f26`): `:11972` and `:11960` `[] → ["taste"]`, `:11902`
  `["availability"] → ["taste"]`, all three in `comments_train_tax2.jsonl`, diff exactly three
  lines. `old` is never retyped into the table — a row ruled `old` reads its v1 label out of the
  **source** and stops on disagreement; the dictated one is carried by its `neither` cell plus the
  table, because a notes cell is free text. **The drift block was not recomputed and the diff proves
  it: 56 insertions, 0 deletions** — all 13,661 lines of `runs` byte-identical, `fixes` beside it.
- **The emptied rows measured** (`c4e545e`), $0, from rows already paid for: **97** went non-empty →
  `[]`, **20.5% of the 473** unexplained drift overall and **30.9% of the 311** on the population a
  gate scores. Decomposition: 97 + 376 = 473 and 96 + 215 = 311. They are short — median **18**
  characters against 55 for the rest, p75 30 against 109 — and the label they lost most is `taste`
  (40 of 97). The three rulings are reversed before counting, so the number describes the model;
  `changed` 1474 and `changed_without_service` 473 come out equal to the 4.5e record, which is the
  proof the reversal restored exactly the model's output. Record: `results/drop_45f.json`.
- **The premise is measured, not cited.** The rendered prompt is checked against `parent_msg_id`,
  `msg_id`, `channel` and `date` on a row that carries all four: none reach the model. **All 3,249
  re-labelled rows are replies**, so the reply share separates nothing — the parent is missing for
  the whole corpus and only bites where the comment alone carries no intent.
- **The micro-pack** (`25b14d2`): `data/annotation/calib_45e/unreadable14.csv` + its README,
  `;`-delimited, `intents_v2` **blank** — nothing proposed, because 13 of these 14 carried `[]` and
  pre-filling them is the bias that left them unlabelled. The set is derived twice (source minus
  staged minus frozen, against the runs' own unusable ids) and a disagreement stops the build. Pinned
  in a **new** `results/calib_45e_micro_manifest.json`; the sealed manifest is untouched.

**One consequence to know:** the reader now refuses to run — `comments_train_tax2.jsonl` hashes to
`e5a52a08…` where the sealed manifest pins `d2132c1e…`. That is the intended end state, and the
refusal names `results/calib_45e_verdict.json` as where the gate was computed.

State: `make check` **437 passed**, `ruff format --check` clean, working tree clean, `TASKS` and
every v1 prompt SHA untouched. Notes: `implementation-notes.md` § *Phase 4.5f*, eight deviations,
every number re-derived from the records by script. Durable lesson in native memory:
`hash-the-rebuild-not-the-returns`. Open and unowned: the 54 frozen ids with two homes (this
prompt did not scope them), whether `unreadable14.csv` should be committed blank, and whether the
97-row class counts as the "repeating class" that would put 11902's precedent into the guideline.
- 21:59: session ended (auto)
