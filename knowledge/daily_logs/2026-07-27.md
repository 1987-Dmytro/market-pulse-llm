---
type: daily_log
date: 2026-07-26
tags: [daily]
---

# 2026-07-26

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->

## Checkpoint 11:08

SPEC Phase 2 step 2a — registry migration. 5 commits (`d36e211`, `146aa5c`, `3bd01e4`, `0e02ae2`,
`3abbc8c`), `make check` green after each; `ruff format --check .` clean.

`config/competitors.yaml` → `config/registry.yaml` with the three rev. 3 entities (`docs/SPEC.md`
§3): 4 sources (silpo / atb / varus `official_retail`, znizhki-ua `aggregator`, all
`verified: false` until the 2b entry check), taxonomy (dairy + 9 subcategories, ice-cream;
kebab-case keys, UA display names), watchlist. `registry.py` rewritten to `Source` / `Taxonomy` /
`WatchlistBrand` / `Registry`, validating unique ids, non-empty channel lists, `@handle` shape,
`source_type` enum and non-empty `tracked_groups`. Tests 4 → 9.

Scope conflict the operator resolved: the task said "watchlist empty, operator fills it at 2d",
while `docs/WATCHLIST.md` was already CONFIRMED and said "folded at 2a". Operator chose the
fold-in → 20 brands (2 own, 9 dairy, 5 ice-cream, 4 chain private labels) shipped in 2a, and the
duplicate-`brand_id` check came with them (a duplicate key silently merges two brands into one
rollup).

Then, on operator request: stale competitor references dropped (README's phase line removed — the
one home for that is [[hot]]; package description follows rev. 3), `CLAUDE.md` + `docs/SPEC.md`
committed together (the new §3 / §5 / §8 refs only resolve against rev. 3), `docs/WATCHLIST.md`
tracked — it is the provenance for 20 committed entries.

Found, not fixed: `knowledge/templates/daily-log.md` hard-codes `2026-07-26` instead of `{{DATE}}`,
so every stub the Stop hook writes carries the wrong date — including this file.

## Checkpoint 14:19

Phase 2 steps **2b, 2c and 2d delivered** in one run. 13 commits, `make check` green
after each, `ruff format --check .` clean.

**2b — entry check.** Telethon client factory from `.env`, QR login helper, per-channel
entry check writing `data/entry_check_report.json`. Operator ran it: `@silpoua` and
`@varus_ua` dead, silpo/atb have comments disabled, varus and znizhki-ua carry them.
Then `--discover` mode over global search; the lead added `msuaaaa` from its output and
later dropped `znizhki-ua` — that channel's whole history is 7 posts from March 2024,
which the printed table hid because it showed posts/day without the window.
`comments_enabled` moved from YAML comments into the schema.

**2c — backfill.** Append-only JSONL raw store, posts and comments in separate files,
dedupe on (channel, msg_id), HMAC `sender_anon_id`, resumable cursor. Ran it myself:
first attempt was killed mid-thread, the resume worked and added nothing twice.
**Gate PASSED: 11 338 comments against the pre-registered 5 000.** 17 395 records, 11 MB.
Not accepted — the verdict line is machinery, acceptance is the operator's.

**2d — annotation.** Guidelines for T1 and T2 with examples from the corpus, UA/RU/EN
language heuristic, stratified export: 2 000 comments + 1 000 posts, seed 42,
byte-reproducible. ~1 800 of the 2 000 comments are expected to survive the `unclear`
rules.

Two bugs I shipped and caught before the operator hit them: `scripts/entry_check.py`
called Telethon synchronously, so every call returned a coroutine and the report would
have been garbage — fixed by making the scripts async. And the comment guideline quoted
a corpus string that did not exist, in a document whose first rule is "verbatim".
Both are in native memory now.

Open for the operator: Phase 1 closure, 2a-2d acceptance, SPEC rev. 3 approval, and the
[[hot]] note about the daily-log template date.

## Checkpoint 19:02

Step **2d-2 delivered** — LLM pre-annotation plus the review flow. 4 commits (`e845840`
SPEC amendment 3.1, `6da48e3` validator, `e3441dc` review sample, `19c2385` a fix to my
own `e3441dc`), `make check` green after each, `ruff format --check .` clean, 91 tests.

**Tooling.** `scripts/validate_annotations.py` + a checker core in `annotation.py`:
legal values from the guidelines, `brand_id` checked against the watchlist, half-filled
rows caught, record fields diffed against the pristine copy, coverage as the progress
meter. `scripts/make_review_sample.py`: 300 comments by (language, sentiment, sarcasm)
with the sarcastic cells 3x, 150 posts by post_type with `launch` 3x, seed 42.

**Labelling.** All 3 000 rows by hand, inline, in 30 chunks of 100 with the validator on
the whole file after every chunk — never red. Chose inline over a subagent fan-out
because §8 gates the set on *self*-agreement: 30 agents would have measured
inter-annotator agreement wearing one `annotator` value. Comments: neutral 1112 /
negative 637 / positive 251, sarcasm 74 (all negative), unclear 694 (34.7%) — 1306
usable. Posts: relevant 115, promo 753 / other 186 / launch 61, 143 brand mentions.

Two things I flagged rather than decided. The unclear rate is 34.7% on comments and 0.1%
on posts, because on posts I quietly extrapolated four rules the guideline never states
(service ≠ product, fast-food chain = venue not brand, syrnyky = prepared dish, and an
incidental mention still counts as relevant — 7 posts hang on that last one). And EN
survived labelling but fell out of the review sample: 8 rows against a 3x sarcasm weight
round below one whole row. Both are the lead's call; the affected ids are listed.

Corpus facts worth planning against: sarcasm yields 74 rows where G1b wants a curated
150–200, and `packaging` has 38 rows in 2 000 — single digits after the 2e split.

## Sessions
- 10:56: session ended (auto)
- 11:00: session ended (auto)
- 11:01: session ended (auto)
- 11:02: session ended (auto)
- 11:11: session ended (auto)
- 11:25: session ended (auto)
- 11:52: session ended (auto)
- 12:05: session ended (auto)
- 12:32: session ended (auto)
- 12:35: session ended (auto)
- 13:32: session ended (auto)
- 13:44: session ended (auto)
- 14:14: session ended (auto)
- 14:20: session ended (auto)
- 15:28: session ended (auto)
- 19:04: session ended (auto)
- 19:51: session ended (auto)
- 20:18: session ended (auto)
