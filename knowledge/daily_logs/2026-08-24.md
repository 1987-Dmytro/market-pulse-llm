---
type: daily_log
date: 2026-08-24
tags: [daily, lora, money, stop-report]
---

# 2026-08-24

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->

День открыт на **ожидании приёмки STOP-отчёта `docs/reports/lora-c-run.md`** и на рулинге
оператора, который ждёт регистрации тимлидом в `STATUS.md` п. 1: **сначала $0-контракт на датасет арма B**, потом
ОДИН `lora-c-run` с обоими армами; **кап остаётся $4.00** и пересчитывается в контракте, который
знает свой объём. Ничего не куплено, подов нет.

## Checkpoint 09:46

Работа сессии целиком лежит в логе за 23.08 (секция `## lora-c-run 18:09` и абзац с рулингом) —
сюда переносится только то, что закрылось после неё, и текущее состояние.

**Закрывающий верификатор переснят и зелёный.** `make check` → **3 682 passed / 2 skipped, exit 0**,
`ruff format --check .` → 435 файлов, на `7c0719b`, дерево чистое на старте. Прогон штампует `HEAD`
и `git status --porcelain` с обеих сторон себя — чтение носит доказательство собственного дерева.
Первая попытка была УБИТА, а не процитирована (Dv792): я правил `hot.md` под работающим прогоном,
тот же промах, что Dv785 контрактом раньше. Дерево двинулось один раз и это был Stop-хук — строка
`- 18:17: session ended (auto)` плюс индекс; ни один pytest не читает `knowledge/daily_logs/`, а
`hot.md`, который вход суиты, побайтно неизменен через прогон.

**Две дыры в доказательстве закрытия — закрыты грепом, а не вторым десятиминутным прогоном.**
(1) Чтение снято на коммит раньше, чем изменился сам отчёт: тридцать семь мест в `tests/`
открывают файл под `docs/`, и **ни один путь не под `docs/reports/`** — одиннадцать упоминаний
`docs/reports` в тестах это проза в докстрингах. (2) Тринадцать новых тестов теперь считаются
`pytest --collect-only -q`, а не вычитанием 3 682 − 3 669; перечисление сверено с коллекцией.

**Состояние.** HEAD `a8ad7de`, девять коммитов от `6fc6773`, дерево чистое, подов нет, цикл-2
остаток **$11.8609** из $20.00. `results/prereg_lora_c.json` — по-прежнему **DRAFT**, попытка не
израсходована. Все инструменты для арма A готовы: `scripts/train_qlora_v3.py::arm_of(666)` уже
читает `arm_b` из регистрации, и гард 2 примет файл в ту же секунду, как `population` его назовёт.

**Чего я НЕ делаю и почему.** Датасет арма B не строится и продюсер не набрасывается: это новый
объём, и он упирается в решение, которое может принять только тимлид — как синтетическая строка
рендерится как v3-запрос (изоляция запрещает синтетику как СОСЕДА и молчит о синтетике как
ЗАПРОСЕ). Готовый продюсер в дереве был бы давлением на непринятое решение.
- 09:48: session ended (auto)

## lora-c-armb — арм B стал файлом, цена сошлась, $0

Контракт `docs/PROMPT-lora-c-armb.md` закрывает обе находки, на которых остановился `lora-c-run`.
**Подов нет, потрачено $0.0000, попытка цела, регистрация всё ещё DRAFT.** Отчёт —
`docs/reports/lora-c-armb.md`, ADR — [[lora-c-arm-b-rendering-and-the-two-leg-pass-2]].

**Ридбэк-гейт прошёл целиком.** Шесть чисел из владеющих ими файлов: клаузы (н) из `STATUS.md`,
правило рендеринга из контракта, проход-2 = 2 ноги из рулинга, 666 = 506 + 160 с диска, вес
4.1625 через `train_qlora.class_weights`, форма break-even из записи. Ридбэк добавил два факта,
которых в нём не спрашивали: 160 строк лежат в ЧЕТЫРЁХ тредах по 40, и 32 таргета
`молочный_бренд` — по 8 на класс ошибки. Отсюда вышла главная находка.

**Step 0.5 — верификатор стал инструментом.** `make check-stamped`: HEAD и `git status --porcelain`
с обеих сторон суиты, exit 2 «reading VOID» при движении дерева вне вайтлиста. Прогнан ТРИЖДЫ по
настоящему git: `true` → HOLDS; команда, оставившая `results/.void_probe` → **VOID при ЗЕЛЁНОЙ
суите**; дописка в лог дня → HOLDS. Вайтлист (`knowledge/daily_logs/`) лицензирован тестом, который
парсит каждый тестовый модуль и отказывает ВЫЗОВУ с этим именем, — грепом нельзя, файл сам его
называет.

**D1 — арм B.** `results/pass1_sft_v3_arm_b.jsonl`: 506 реальных строк БАЙТ В БАЙТ префиксом
(перерендерены и сверены с ОТГРУЖЕННЫМ файлом ДО записи) + 160 синтетических через ТОТ ЖЕ рендерер
на ТОМ ЖЕ пуле 515. Ценз кодировки **666/666, 0 отказов**, макс 2 975 при 3 072, запас 97;
`class_weights` — **пять** классов, `молочный_бренд` 666/(5·32) = **4.1625**; изоляция — шесть
чтений, все нули там, где ноль ответ.

**Находка, которой в контракте не было (Dv795).** Рулинг называет рендерер, пул и форму таргета и
НЕ называет четыре поля хедера. Построено по существующим правилам — и **пять строк хедера несут
синтетические запросы и НИ ОДНА из 506**: `NO_POST_TEXT` (0 из 506) и четыре тега
`<thread channel="synthetic" post_id="…">`. Помечены все 160, а на них ВСЕ 32 таргета
`молочный_бренд`. Значит «синтетика не помогла» и «модель выучила маркер» станут неразличимы.
Измерено, не сказано; репортится, не чинится — рендеринг решает тимлид.

**D2 — цена сошлась.** Проход-2 = 2 ноги: минус **2 134 с = $0.4742**, единственный сдвинувшийся
член. Фикс-часть 11 019.88 → **8 885.88 с**, break-even 48.47/58.61 → **63.29/73.43 с/шаг**. Замер
lora-b 61.047 теперь влезает при обеих ценах, 68.442 — при $0.74. **s/step по-прежнему не
зарегистрирована**, её покупает смок. Стоковая проба (read-only, ничего не создано): A6000 в
EU-RO-1 — `none`, A40 там не каталогизирован; со стоком 7 карт из 47, RTX PRO 4500 32 GB $0.72 —
единственная High. Перечислено и не выбрано.

**Дырка в списке коммитов (Dv793).** (c) и (d) неразделимы как написаны: `lora_c_data.json` несёт
`produced_by.sha256`, регистрация пинит его ша, и суита гоняет обоих продюсеров — правка продюсера
тянет пересборку записи в ТОТ ЖЕ коммит. Измерено, не предположено. Разведено пересборкой записи
в (c) СТАРЫМ продюсером; оба коммита зелены по отдельности.

**Коммиты:** `b646361` (хвост прошлого) · `34cc939` (файлы тимлида) · `ba14ca4` (step 0.5) ·
`fe0cc87` (продюсер и данные) · `b2ed1b3` (регистрация и деньги) · `a51f39e` (отчёт и ADR).
Базлайн **3 682/2 на `34cc939`**, дерево не двинулось под ним.
- 10:52: session ended (auto)
