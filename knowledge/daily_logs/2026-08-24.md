---
type: daily_log
date: 2026-08-24
tags: [daily, lora, money, stop-report]
---

# 2026-08-24

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->

День открыт на **ожидании приёмки STOP-отчёта `docs/reports/lora-c-run.md`** и на рулинге
оператора, который ждёт регистрации тимлидом в `STATUS.md` п. 1: **сначала $0-контракт на датасет арма B**, потом
ОДИН `lora-c-run` с обоими армами; **кап остаётся $4.00** и пересчитывается в контракте, который
знает свой объём. Ничего не куплено, подов нет.

## Checkpoint 09:46

Работа сессии целиком лежит в логе за 23.08 (секция `## lora-c-run 18:09` и абзац с рулингом) —
сюда переносится только то, что закрылось после неё, и текущее состояние.

**Закрывающий верификатор переснят и зелёный.** `make check` → **3 682 passed / 2 skipped, exit 0**,
`ruff format --check .` → 435 файлов, на `7c0719b`, дерево чистое на старте. Прогон штампует `HEAD`
и `git status --porcelain` с обеих сторон себя — чтение носит доказательство собственного дерева.
Первая попытка была УБИТА, а не процитирована (Dv792): я правил `hot.md` под работающим прогоном,
тот же промах, что Dv785 контрактом раньше. Дерево двинулось один раз и это был Stop-хук — строка
`- 18:17: session ended (auto)` плюс индекс; ни один pytest не читает `knowledge/daily_logs/`, а
`hot.md`, который вход суиты, побайтно неизменен через прогон.

**Две дыры в доказательстве закрытия — закрыты грепом, а не вторым десятиминутным прогоном.**
(1) Чтение снято на коммит раньше, чем изменился сам отчёт: тридцать семь мест в `tests/`
открывают файл под `docs/`, и **ни один путь не под `docs/reports/`** — одиннадцать упоминаний
`docs/reports` в тестах это проза в докстрингах. (2) Тринадцать новых тестов теперь считаются
`pytest --collect-only -q`, а не вычитанием 3 682 − 3 669; перечисление сверено с коллекцией.

**Состояние.** HEAD `a8ad7de`, девять коммитов от `6fc6773`, дерево чистое, подов нет, цикл-2
остаток **$11.8609** из $20.00. `results/prereg_lora_c.json` — по-прежнему **DRAFT**, попытка не
израсходована. Все инструменты для арма A готовы: `scripts/train_qlora_v3.py::arm_of(666)` уже
читает `arm_b` из регистрации, и гард 2 примет файл в ту же секунду, как `population` его назовёт.

**Чего я НЕ делаю и почему.** Датасет арма B не строится и продюсер не набрасывается: это новый
объём, и он упирается в решение, которое может принять только тимлид — как синтетическая строка
рендерится как v3-запрос (изоляция запрещает синтетику как СОСЕДА и молчит о синтетике как
ЗАПРОСЕ). Готовый продюсер в дереве был бы давлением на непринятое решение.
- 09:48: session ended (auto)

## lora-c-armb — арм B стал файлом, цена сошлась, $0

Контракт `docs/PROMPT-lora-c-armb.md` закрывает обе находки, на которых остановился `lora-c-run`.
**Подов нет, потрачено $0.0000, попытка цела, регистрация всё ещё DRAFT.** Отчёт —
`docs/reports/lora-c-armb.md`, ADR — [[lora-c-arm-b-rendering-and-the-two-leg-pass-2]].

**Ридбэк-гейт прошёл целиком.** Шесть чисел из владеющих ими файлов: клаузы (н) из `STATUS.md`,
правило рендеринга из контракта, проход-2 = 2 ноги из рулинга, 666 = 506 + 160 с диска, вес
4.1625 через `train_qlora.class_weights`, форма break-even из записи. Ридбэк добавил два факта,
которых в нём не спрашивали: 160 строк лежат в ЧЕТЫРЁХ тредах по 40, и 32 таргета
`молочный_бренд` — по 8 на класс ошибки. Отсюда вышла главная находка.

**Step 0.5 — верификатор стал инструментом.** `make check-stamped`: HEAD и `git status --porcelain`
с обеих сторон суиты, exit 2 «reading VOID» при движении дерева вне вайтлиста. Прогнан ТРИЖДЫ по
настоящему git: `true` → HOLDS; команда, оставившая `results/.void_probe` → **VOID при ЗЕЛЁНОЙ
суите**; дописка в лог дня → HOLDS. Вайтлист (`knowledge/daily_logs/`) лицензирован тестом, который
парсит каждый тестовый модуль и отказывает ВЫЗОВУ с этим именем, — грепом нельзя, файл сам его
называет.

**D1 — арм B.** `results/pass1_sft_v3_arm_b.jsonl`: 506 реальных строк БАЙТ В БАЙТ префиксом
(перерендерены и сверены с ОТГРУЖЕННЫМ файлом ДО записи) + 160 синтетических через ТОТ ЖЕ рендерер
на ТОМ ЖЕ пуле 515. Ценз кодировки **666/666, 0 отказов**, макс 2 975 при 3 072, запас 97;
`class_weights` — **пять** классов, `молочный_бренд` 666/(5·32) = **4.1625**; изоляция — шесть
чтений, все нули там, где ноль ответ.

**Находка, которой в контракте не было (Dv795).** Рулинг называет рендерер, пул и форму таргета и
НЕ называет четыре поля хедера. Построено по существующим правилам — и **пять строк хедера несут
синтетические запросы и НИ ОДНА из 506**: `NO_POST_TEXT` (0 из 506) и четыре тега
`<thread channel="synthetic" post_id="…">`. Помечены все 160, а на них ВСЕ 32 таргета
`молочный_бренд`. Значит «синтетика не помогла» и «модель выучила маркер» станут неразличимы.
Измерено, не сказано; репортится, не чинится — рендеринг решает тимлид.

**D2 — цена сошлась.** Проход-2 = 2 ноги: минус **2 134 с = $0.4742**, единственный сдвинувшийся
член. Фикс-часть 11 019.88 → **8 885.88 с**, break-even 48.47/58.61 → **63.29/73.43 с/шаг**. Замер
lora-b 61.047 теперь влезает при обеих ценах, 68.442 — при $0.74. **s/step по-прежнему не
зарегистрирована**, её покупает смок. Стоковая проба (read-only, ничего не создано): A6000 в
EU-RO-1 — `none`, A40 там не каталогизирован; со стоком 7 карт из 47, RTX PRO 4500 32 GB $0.72 —
единственная High. Перечислено и не выбрано.

**Дырка в списке коммитов (Dv793).** (c) и (d) неразделимы как написаны: `lora_c_data.json` несёт
`produced_by.sha256`, регистрация пинит его ша, и суита гоняет обоих продюсеров — правка продюсера
тянет пересборку записи в ТОТ ЖЕ коммит. Измерено, не предположено. Разведено пересборкой записи
в (c) СТАРЫМ продюсером; оба коммита зелены по отдельности.

**Коммиты:** `b646361` (хвост прошлого) · `34cc939` (файлы тимлида) · `ba14ca4` (step 0.5) ·
`fe0cc87` (продюсер и данные) · `b2ed1b3` (регистрация и деньги) · `a51f39e` (отчёт и ADR).
Базлайн **3 682/2 на `34cc939`**, дерево не двинулось под ним.
- 10:52: session ended (auto)

**Закрывающее чтение и то, как инструмент отказал сам себе.** Первый закрывающий прогон вернул
**VOID при ЗЕЛЁНОЙ суите** (3 701/2): Stop-хук сработал посреди прогона и написал ДВА пути — лог дня
(в вайтлисте) и `knowledge/index.md`, который он перегенерирует, потому что ADR этого контракта
поднял счётчик decisions 68 → 69. Вайтлист расширять не стал: контракт фиксирует его на
`knowledge/daily_logs/`, а список исключений верификатора — гейт безопасности, и его безопасное
направление узкое. Слово за тимлидом (Dv802). До этого пришлось убить ещё один прогон: штампы
печатались буферизованным stdout, а суита писала прямо в fd, поэтому BEFORE всплывал ПОСЛЕ прогона
(Dv801) — починено `flush=True`, чтение переснято. Итог на чистом дереве: **`make check-stamped` →
3 701 passed / 2 skipped, exit 0, «reading HOLDS», HEAD `8ef994e`**, `ruff format --check` 438
файлов. Прирост **+19 тестов** посчитан коллекцией, а разница 3 682 → 3 701 — перекрёстная проверка,
а не счёт. Все четырнадцать ша артефактов отчёта перехешированы по закрывающему дереву и сходятся,
включая запиненный `train_qlora.py` — он не двигался.

**Коммиты (десять от `a8ad7de`):** `b646361` · `34cc939` · `ba14ca4` · `fe0cc87` · `b2ed1b3` ·
`a51f39e` · `cafd446` · `b659dea` · `8ef994e` · `1cd3e95`.
- 11:15: session ended (auto)

## Checkpoint 20:34

Контракт `lora-c-armb` целиком лежит в секции выше — сюда только то, что закрылось после неё, и
текущее состояние.

**Отчёт дополнен закрывающим чтением и двумя правками формулировок, которых потребовали числа.**
§2 помечен: три прогона `check-stamped` сняты на `34cc939`, дереве step 0.5, а не на закрывающем —
иначе читатель решит, что это один и тот же прогон. «Одиннадцать упоминаний» `docs/reports` в тестах
стало тринадцать: свой же новый тестовый файл добавил два. Все четырнадцать ша артефактов §5
перехешированы по закрывающему дереву — сходятся, включая запиненный `train_qlora.py`.

**Dv802 переформулирован по тому, что сессия за него заплатила.** Было «вайтлист покрывает один из
двух путей хука». Стало: **хук срабатывает на ПРОСТОЕ и пишет путь вне вайтлиста, значит сессия,
которая тихо ждёт собственный десятиминутный верификатор, не может получить цитируемое чтение**.
Третий прогон удержался только потому, что сессия держалась занятой все десять минут. Это и есть
аргумент за расширение — и он тимлида, не мой.

**`hot.md` дописан после чтения, и это раскрыто, а не сделано молча.** Строка `**Last update:**`
писалась до того, как верификатор существовал, поэтому в файле, который грузится каждым
SessionStart, для этого контракта не было ни одного числа. Теперь несёт `3 701/2 на 8ef994e`.
Побочный эффект назван: байты входа суиты разошлись с теми, что суита читала, — перепроверено
ровно то, что от этого зависит (`~$0.24/day` на месте дважды, `test_volume_calc_5c1` 10/10).
Ценз буста померен с обеих сторон: 11.5K → **11.6K** при цели 10.7K, литерал в Blockers исправлен
на измерение.

**Два durable-урока записаны** (третий — обновление существующего):
[[a_default_is_a_marker_when_nothing_takes_it]] — рулинг называет инструмент, а не поля, которые
инструменту нужны, и дефолт, которого не берёт ни одна старая строка, становится идеальным
разделителем; [[an_intersection_hides_a_per_group_signal]] — пересечение находит маркер, только если
он метит всю партию, а партия была четырьмя группами по 40. Обновлён
[[rewriting_a_record_resets_state_you_do_not_own]]: вторая форма — блок, который пишет ФЛАГ, и
пересборка без флага удаляет его молча.

**Состояние.** HEAD `aec3b11`, двенадцать коммитов от `a8ad7de`, подов нет, попытка цела,
регистрация DRAFT, цикл-2 **$8.3043 из $20.00**. Ждём три решения по отчёту: кап, карта, маркер в
хедере.
- 20:35: session ended (auto)

## lora-c-run r2 — один под, $0.7606, убит рунгом 4; попытка цела

**Контракт `docs/PROMPT-lora-c-run-r2.md` исполнен, отчёт `docs/reports/lora-c-run-r2.md`, ЖДЁТ
ПРИЁМКИ.** Десять коммитов от `e2d28b4`. Под `ob1svfs3y23teh`, RTX PRO 4500 32 GB, $0.72/ч, EU-RO-1:
создан 20:29:20Z, удалён 21:32:43Z — **3 803 с = $0.7606 из капа $4.00**, удаление доказано тремя
листингами с томом как положительным контролем.

**Главное: 32 GB не тянут эту линию при 3 072.** Смок умер `OutOfMemoryError` при НУЛЕ шагов — и это
ВТОРОЙ отказ: за две строки до трейсбека сам трейлер напечатал свой зарегистрированный откат
`micro_batch 2 → 1, grad_accum 8 → 16` (правило из `config/qlora.yaml`) и упал снова при
micro_batch 1. Замороженный закон на поде не правили и не пришлось. Средства названы, ни одно не
взято — это слово тимлида. См. [[lora-c-the-card-cannot-train-at-3072]].

**Те же $0.76 купили и СОХРАНИЛИ обе базовые ноги** (по 198, 0 отказов парсера, хэши сняты на поде и
на Маке ДО удаления). Контрольная колонка сработала: **база v3 — это база v2 плюс одна фраза — берёт
66 из 98 холдаут-строк против 61** (85 → 91 из 133), весь прирост в `не_наш_рынок` (35 → 41 из 66).
Это ровно та доля выигрыша любого арма, которую списали бы на адаптер. Отчётно: у базы v3 бара нет,
знаменатель 98, а не 100.

**Ставки, померенные на карте, которую репозиторий никогда не оплачивал:** v2 5.32 с/вызов против
заряженных 6.14, **v3 9.20 — ×1.50**. Померил рунг 3 — своё добавление этой линии, которое судит
ногу по ставке, которую она РЕАЛИЗУЕТ, пока платит. Без него первое денежное решение случилось бы
после смока, четыре тысячи заряженных секунд спустя.

**Семь разрывов транспорта, все найдены за $0 до создания пода** — прогоном отгруженной цепочки по
замороженным пакам с фейковым клиентом. Четыре из семи выстрелили бы с загруженной 31-гигабайтной
моделью. Седьмой с ответом о досягаемости: пак ОБЪЯВЛЯЛ потолок 15 569 и РЕНДЕРИЛ при 12 000, и
разрыв достижим адаптером, а не базовой ногой, которая пак построила — один тред из 15
(`@matusi_ukr:22272`, 12 399) переходит 12 000 и несёт кейс бара 1.

**Свои ошибки, найденные тестом, а не подом.** Первая версия рунга 3 заряжала оставшиеся шаги по
потолку кил-клока смока — 150 × 181.5 с больше, чем покупает весь кап, то есть ветка GO была
недостижима для любой карты. `header_tell` сортировал таблицу из `set` и выдавал разный файл на
каждом прогоне.

**Талли отклонений (Dv803–811, свой грепом):** contract-gap 3 · verify-gap 2 · spec-gap 1 ·
process 1 · model 1 · tooling 1 — девять.

**Состояние.** Регистрация ЗАМОРОЖЕНА созданием пода, **попытка НЕ ПОТРАЧЕНА** (рунг 8 тратит её на
первом ответе АДАПТЕРНОЙ ноги против E — адаптера нет). Баров не считали и не могли: они
зарегистрированы ПО АРМАМ. Долг: `--close` у гарда — биллинг постится часами позже, $0.0602 против
$0.7606 по часам пода.
- 23:54: session ended (auto)
