---
type: daily_log
date: 2026-09-05
tags: [daily]
---

# 2026-09-05

## Summary

**День, в котором холдаут был выстрелен и вопрос S2 закрыт числом.** Три сессии, одна покупка.

**s20 — «holdout-prep», $0, пода не было.** Файлы тимлида закоммичены по путям (`7378326`): рулинг
(q), PHASE v7, STATUS, эталон холдаута (188 строк, sha `6fa804880d5d14db…`). Эмиттер и грейдер взяли
холдаут ПАРАМЕТРАМИ (`f879bf9`), раннбук ушёл с `_iter3` на `_holdout` (`31f9461`), борроу снят на
собственные 88.772 / 201.967 s (`7a7d90c`). Сухие контакты за $0 поймали три дефекта на платном пути,
последний — `use_part("dev")` как тихий no-op (`858ead0`): перевязывал константы только при
`part != "dev"`, из-за чего проба пака отрапортовала dev-40 и holdout-40 ПЕРЕСЕКАЮЩИМИСЯ, хотя они
дизъюнктны 40 + 40.

**s21 — один пункт «next» взят и оказался ЗАБЛОКИРОВАН на команду раньше, чем считал s20.** $0,
пода не было. Стоп не на §5, а на **§0a**: `register()` писал `decision_table` без ветки по `PART`,
так что `--register --part holdout` — ПЕРВАЯ команда платной сессии — вкоммитил бы полосы dev-петли
в предрегистрацию холдаута, а `--pack` отказывает на грязной регистрации, то есть запись
запечатывалась бы тем самым коммитом, которого §0a и требует. Второй стоп: `--close` dev-петли
отказал на 33.7%.

**s22 — рулинг (r) снял ОБА стопа, и выстрел сделан.** Стоп (a) признан верным и предвидимым
(промах тимлида), решён вариантом (ii): денежный гейт холдаута = FITS рунга 0 плюс кап как жёсткий
стоп, полосный гейт §5 не запускается, `project()` не трогается. Стоп (b): леджер dev-петли открыт
**по построению**, гард не двигается. Кап поднят оператором до $1.10.
Ветка записи по ноге сделана (`0726665`): рулинг называл ЧЕТЫРЕ поля с решениями — аудит собранной
записи нашёл **СЕМЬ**. §0a закоммичен до существования пода (`50959b5` регистрация, `582a655` пак,
`26d4373` леджер на $0.0000). Под `pcu2fqc7ebwfcw` жил 07:52:12Z → 08:16:15Z, **1443 s**, и шёл в
**3.4× быстрее** зарегистрированной ставки. §5 отработал как проверка живости и формы, а не как
гейт: три ответа смоука (8.8 / 5.3 / 149.1 s, все `balanced`, все `finish=stop`) И БЫЛИ тем GO.

**Чтение (`6c6b37f`): сигнал 0.7958 ✅ (бар 0.75) · субъект 0.7181 ❌ (бар 0.80).** ПОЛНОЕ по §6.5 —
188 строк из **40 из 40** юнитов, **0 unparsed**, все `finish=stop`, id-множество равно порядку
записи без дублей и лишних (проверено МНОЖЕСТВОМ, потому что `wc -l == 40` проходит и на «дубль
плюс промах»). Против dev-бара (0.8714 / 0.9104), взятого побайтно тем же инструментом, субъект упал
на **0.1533**. Видимый кластер из 53 промахов — chain↔brand на банковском треде `@VARUS_channel:4422`
(`izibank`, `OTP банк`). По (r)5 это ЗАКАНЧИВАЕТ выстрел: красный бар не двигает гейт, он отвечает.

**Деньги: $0.296617 из капа $1.10.** Цикл 3 — $5.4962 из $7.00, остаток $1.5038.

## Decisions

- [[the-holdout-gate-is-rung-0-fits-plus-the-hard-stop]] — рулинг (r), вариант (ii), и почему
  переиспользованный продюсер обязан ветвить не только пути, но и поля записи с решениями.

## Next

1. **Слово тимлида по КРАСНОМУ бару субъекта** — вопрос S2 отвечен числом, что делать дальше, решает
   он: чтение полное, закон заморожен, перевыстрела по §8 (e) нет.
2. «c3» по (l)2–4, ≤ min($0.50, остаток); после него удаляется том `mp-srv2`.
3. near-quote «Шикарно…» и синхронизация кодбука-документа РАЗБЛОКИРОВАНЫ — (q)2 ставила их за
   выстрелом, выстрел сделан.

## Blockers

- **Леджер `promo-holdout` ОТКРЫТ.** `--close` отказал ДВАЖДЫ и не был форсирован: в 08:1x биллинг
  не сел вовсе (0 мс против 1443000), в 09:05 прошёл частично ($0.2130 против $0.296617). Оба отказа
  не записали НИЧЕГО — в `gpu_sessions` ровно одна запись, §0. Цитируемое число —
  `results/promo_holdout_run.json :: gates[1].spent_all_segments_usd` = $0.296617. Так же открыт и
  `promo-dev-loop` ((r)4, $1.153372); правая часть гейта закрытия — долг харнеса для ретро.
- **Ставка холдаута никогда не попадёт в `measurements.jsonl`:** `write_measurement()` зовётся только
  из ветки `--project`, которую (r)2 не запускает. Следующая нога по `own_rate()` прочтёт DEV-строку
  88.772 s там, где этот под намерил 25.826.
- **`make check` зелёный, но НЕ одним вызовом:** сюита идёт ~12 мин, больше потолка одного вызова, и
  два фоновых прогона были убиты. Прошло как `ruff check .` плюс три непересекающихся среза всех 229
  файлов — 999 + 2028 + 1293 = **4320 passed, 2 skipped**, те же итоги, что до пода.
- Остальные долги — «named, not built» в `docs/plans/promo-pulse-1.PROGRESS.md`.

Вчера: [[2026-09-04]]. Записи дня: [[the-holdout-gate-is-rung-0-fits-plus-the-hard-stop]].
- 11:18: session ended (auto)

# s23 — «v1.2-prep»: закон переехал на кодбук v1.2, holdout-2 вытянут, и деньги итерации 4 встали

Рулинг (s) принял выстрел как измеренный и дал слово оператора: кодбук v1.2 сейчас, dev-2 = сожжённый
холдаут, holdout-2 из популяции ПРОДУКТА, K8 v2. Сессия — одна позиция (b), целиком за **$0**.

## Сделано (4 коммита)
- **Ритуал денег** (`a6868e5`): потолок цикла-3 $7.00 → **$9.00** (доб. 8), якорь и строки сессий не
  двигались; `read_cycle3` больше не носит число в прозе. Гард: SPENT $5.4962 из $9.00.
- **holdout-2** (`4a03307`) — ПАРАМЕТРАМИ на том же продюсере, не форком. Исключения ПЕРЕСЕКАЮТСЯ (два
  треда `@matusi_ukr` и в паузе, и в дро-1), поэтому популяция = `eligible − union`: 90 убрано, 398
  осталось. Дро 1 переигрывается ПОБАЙТНО (`cmp` чист) — замороженная запись не двинулась.
- **K8 v2** (`0224167`) — одна парная `subjects_agree()`, её зовут и грейд, и таблица ошибок.
  **Измерено: алиас «Сильпо» = 0 строк, K8 v2 = 1 строка из 188** (0.7181 → 0.7234). Остальные промахи
  формы — однотокенные пары, где Жаккару нечего пересекать. Правило стоит как велено, выигрыш меньше
  мотивации — назвал тимлиду. [[measure_what_the_normalisation_would_fix]] ещё раз.
- **Закон v1.2** (`4be406e`), `--leak-check` ЧИСТ по трём наборам (440 комментов). Каждый литерал —
  ПЕРЕФРАЗ: кодбук-документ имеет право цитировать эталон, закон — нет.

## Стоп
Итерация 4 не регистрируется под кап $0.60: `--register --part dev --cap 0.60` отказал (dear $1.3100
против $0.6000), у dev-ноги нет отката на средний угол, а `own_rate()` отвечает 88.772, где не влезает
НИ ОДИН угол и жёсткий стоп 2919 с РЕЖЕТ ран (нужно 7647 с). Темп, которым инструмент реально шёл
(25.826), так и не попал в `measurements.jsonl`. Вопрос тимлиду — в PROGRESS, три части.
- 12:24: session ended (auto)

## Checkpoint 12:27 — стоп заострён числом из самого судьи, и второй красный назван заранее

С прошлой записи (после четырёх коммитов s23) советник указал на три вещи, все три закрыты:
1. **«Ни один угол не влезает» было арифметикой в транскрипте, а не прогоном.** Перезапустил
   `rung_0` НА НАСТОЯЩЕЙ ноге итерации 4 (3 смока + 80 тредов, RTX 4090 $0.74/h, цена взята из записи
   пробы, не из сети): `own_rate()` 88.772/201.967 → 1.5719 · 1.6292 · 3.5605, `issued_on=mean`,
   **FITS=False**; borrowed 23.760/135.232 → 0.4627 · 0.5201 · 2.4219, `issued_on=dear`, FITS=False.
   Судья тот же, что вынес бы вердикт на регистрации, — совпал с ручной таблицей до цента.
2. **Вопрос (2) — это вопрос к КОДУ, не только к данным.** `rate_for("dev")` возвращает ЗАИМСТВОВАННУЮ
   ставку и `own_rate()` на этой ноге не спрашивает вообще; проба выше считалась по ней. Ответить
   «берите темп холдаута» = записать строку в `measurements.jsonl` И поменять `rate_for`. Сказано вслух.
3. **Второй красный назван до того, как в него упрутся:** `test_promo_dev_pass.py:61` (`== 40`) и
   `:202` (`SMOKE_N + 40`) — литералы СТАРОЙ популяции, покраснеют, как только `dev_threads()` вернёт
   80. Это две переписки по §6.6, а не второй стоп; следующая сессия встретит их уже названными.

PROGRESS — 60 строк, открытый стоп — 15. Дерево чистое, шесть коммитов, $0 потрачено, пода нет.
Урок в память: [[an-ordered-instrument-change-reds-an-artifact-test]] — заказанная смена инструмента
красит тест, пиннящий отгруженный артефакт, а починка (пересборка) может быть заперта ДРУГИМ допуском.
- 12:29: session ended (auto)
- 13:25: session ended (auto)

## Checkpoint 13:26 — стоп s23 закрыт рулингом (t), итерация 4 зарегистрирована и запакована ($0)

Сессия s24. Ритуал старта: закоммичены файлы тимлида по путям (`47b750e` — PHASE v10, STATUS,
рулинг (t), разбор четвёртой остановки) и хвосты s23 из хуков (`8538266`). Дальше — ровно ОДИН
пункт «next» из PROGRESS: регистрация итерации 4 и её пак. Четыре коммита кода, все за $0.

1. **Ставка (`1ebc321`), рулинг (t) 3–4.** Обе половины стопа оказались КОДОМ. `rate_for("dev")`
   возвращала заимствованную `pass2_r2` и `own_rate()` не спрашивала вовсе; сама `own_rate()`
   читала три СМОКА, собранных под самый длинный тред, — 88.772 с/тред, втрое больше всего, чем
   инструмент когда-либо шёл. Теперь выбор идёт по `contract` + `sample == "whole run"`: mean-угол —
   среднее ЦЕЛОГО рана самого медленного пода, dear-угол — наибольший max (две строки НАРОЧНО, хотя
   сегодня обе даёт итерация 3). Условие `part != "dev"` ушло из `rung_0`. Новый `--measure-run`,
   и `--close-segment --replies` пишет строку на закрытии. Задним числом за $0 — ровно три, что
   назвал (t)4: итерация 2 **17.7118**/88.237, итерация 3 **47.6615**/286.248, холдаут
   **25.8263**/149.054, n=40 каждая. Заодно умерли два ложных поля записи: `cap_rule` приписывал
   $1.20 оператора рулингу (r)3, который говорил $1.10, а `floor_usd` рисовал $2.00 рядом с капом
   $1.20. [[the-hardening-did-not-reach-the-sibling-reader]], [[a-smoke-drawn-from-the-exam-is-not-a-rate-sample]].
2. **Популяция (`426a7d7`).** Нога — больше не одна рука: `ARMS` привязывается в `use_part`, нога
   dev = `("dev", "holdout")` первого дро, dev-40 на [0:40], dev-2 на [40:80]. `ITERATION = 4`,
   вся проза dev-ветки записи переписана под итерацию 4. Дро-рун: 80 тредов, 80 разных рендеров.
3. **Регистрация (`1ee15f4`).** cheap $0.8705 · priced $0.9279 · dear $4.9984 НЕ влезает →
   **FITS на MEAN-угле**, жёсткий стоп 5838 с ≈ 97 мин против капа $1.20. Совпало с прогнозом
   рулинга ((t)4 «≈ $0.87», (t)5 «≈ 97 мин») с двух сторон. Гард прочитан до записи.
4. **Пак (`a6a5b1c`).** 80 юнитов; названный красный тест позеленел ПО ИМЕНИ — двигался пак, а не
   тест. `make check` срезами: 1000 + 2028 + 1293 = **4321 passed, 0 failed, 2 skipped**; s23
   собирала те же 4321 как 4320 + 1 failed. [[an-ordered-instrument-change-reds-an-artifact-test]].

**Три вещи, до которых проверки не дотянулись** (советник указал, все три закрыты в `0a18c92`):
`--close-segment --replies` — ветка, которую платная сессия запускает ПОСЛЕ удаления пода и до
которой не доходит ни один тест: прогнал в песочнице оба направления плюс случай, когда строка
отказывает, а закрытие уже на диске (лечится `--measure-run`, не вторым закрытием). Порядок пака
НЕ строго dev-40→dev-2: смок — правило по корпусу ноги, две его единицы из dev-2, значит dev-40
закрывается на юните **42 из 80**. И единицы dev-2 — это единицы holdout-40, поэтому dev-порядок
примет чужой out-file. `hot.md` вёл следующую сессию в ЗАКРЫТЫЙ стоп (фаза v9, «4320 passed,
1 failed») — переписан, оба грепаемых литерала побайтно целы.

Открытого стопа НЕТ. Следующее — ПЛАТНАЯ итерация 4, ничто не блокирует. Главный названный долг:
`--score` не умеет ногу из 80 — он ложится ровно в тот момент, когда под уже оплачен.
Урок в память: [[a-downstream-promotion-undoes-the-ordering-you-were-asked-for]] — правило,
поднимающее подмножество в начало (смок, прогрев), молча отменяет порядок, который заказали выше;
проверять надо ВЫДАННЫЙ порядок, а не исходный список.
- 13:27: session ended (auto)
- 13:46: session ended (auto)
- 13:59: session ended (auto)

## Checkpoint 14:14 — рука стала параметром, эталон чтения запинен; платная итерация 4 разблокирована
Сессия s25, рулинг **(u) item 2**, ОДИН пункт, $0, под не создавался. Стартовый ритуал: `docs/STATUS.md`
и рулинг (u) закоммичены по путям (`ee15c55`).

**Порядок был не тот, что в PROGRESS.** «Next» в PROGRESS звал в платную итерацию 4 со словами
«ничто не блокирует», но новейший датированный раздел ревью — рулинг (u) — вставил ПЕРЕД подом
$0-предусловие. Новейший рулинг старше собственного файла прогресса
([[a-ruling-can-outrun-the-law-it-amends]]); сделал предусловие.

**1. Рука как параметр, ключ чтения запинен (`705e2d1`).** `--score` читал ОДИН эталон по ВСЕЙ ноге:
ключ dev-40 (140 строк) против 80 отвеченных юнитов, собственный ключ dev-2 (188 строк) не
открывался — ровно [[measure-on-the-rows-the-gate-scores]]. `DEV_ARMS` называет обе руки по МЕТКЕ
(`dev40`, `dev2`), каждая несёт свою часть ПЕРВОГО дро, свой стем и свой эталон; `DEV_FILES["arms"]`
ВЫВОДИТСЯ из неё — одна популяция не перечислена дважды. Метки и части дро сталкиваются на слове
«holdout» (dev-2 = сожжённый holdout-40) — два id-пространства, похожих на одно
([[id-spaces-that-look-comparable]]), поэтому `use_arm` отказывает любой руке вне `--part dev` и
связывает ТОЛЬКО скорер: связать эталон на `--register` значило бы запинить ключ читающей руки как
барный. `leg_golds()` — единственный список, который пинит регистрация.

**2. Прогон за $0 на уже существующих ответах** (`--suffix _armdrive`, выходы удалены): `--arm dev40`
на `promo_dev40_iter3.jsonl` → 40 из 40, эталон 140 строк, subject **0.8714** / signal **0.9104**
(числа итерации 3, перевыведены); `--arm dev2` на `promo_holdout40.jsonl` → 40 из 40, эталон 188
строк, subject **0.7394** / signal **0.7833**, по-стратумно 20+20. Наборы тредов ДИЗЪЮНКТНЫ, оба
отказа прогнаны. Это проводка и НЕ производительность: оба файла старше кодбука v1.2, а 0.7394 —
старые ответы против ПЕРЕЧИТАННОГО ключа (было 0.7181/0.7958 на старом эталоне) —
[[correcting-gold-moves-the-denominator]].

**3. Рунг 0 переоценён на день и не сдвинулся** (`--register` читает гард и живое предложение):
cheap $0.8705 · priced $0.9279 · dear $4.9984 → FITS на MEAN-угле, кап $1.2000, жёсткий стоп 5838 с.
REMAINING $3.4843. **Пак (`0bb62fb`)** — сдвинулся только `registration.sha256` 037267dc… → 9ded3a79…,
80 юнитов побайтно те же. Порядок вынужденный: prep → prereg → pack, потому что prep запинен в
`pinned_inputs`, а `committed_registration()` отказывает несозвучному с HEAD файлу.

**Срез тестов был НЕЗАКРЫТЫМ списком.** Раскроил `sed -n '1,40p' / '41,90p' / '91,200p'`, а
`ls tests/test_*.py` — уже **229** файлов: последние 29 не запускались, три зелёных среза дали 3900
вместо 4321. Ничего не падало — хвоста просто не было, и три строки «passed» читались как зелёный
прогон. Поймал по несходству с полом 4266, догнал 29 файлов отдельным срезом (421 passed). Итог:
656 + 1092 + 2152 + 421 = **4321 passed, 0 failed, 2 skipped** — как в s24, тестов не добавлено.
Урок дописан в [[make-check-exceeds-one-call]]; родня — [[a-patch-list-closed-by-enumeration]].

Открытого стопа НЕТ, дерево чисто. Тест на селектор руки НЕ добавлен (§4 и прецедент s24) — назван
в PROGRESS как то, что стоит заказать, вместе с негативным контролем (пустая карта стратумов на
ЧУЖОЙ части дро). Следующее — ПЛАТНАЯ итерация 4; (u)2 прямо разрешает правке и разблокированному
ею прогону быть в одной сессии.

**14:20 — аддендум (u)5: s25 ПРИНЯТ ($0),** все 8 пинов перевыведены тимлидом. Покупка — СЛЕДУЮЩАЯ
сессия, (u)4 без изменений; тест на селектор руки остаётся названным, не заказанным. `docs/STATUS.md`
и файл рулингов снова изменены в дереве — коммит по путям за оператором.
- 14:19: session ended (auto)
- 14:27: session ended (auto)
- 14:56: session ended (auto)
- 14:58: session ended (auto)
- 14:59: session ended (auto)
- 15:03: session ended (auto)
