---
type: daily_log
date: 2026-09-05
tags: [daily]
---

# 2026-09-05

## Summary
**s20 — «holdout-prep» по рулингу (q)5, весь день за $0, пода не было.** Файлы тимлида
закоммичены по путям (`7378326`): рулинг (q), PHASE v7, STATUS 05.09, разбор остановки и
**эталон холдаута** (188 строк, sha `6fa804880d5d14db…` — ровно та, что называет (q)7).
Эмиттер и грейдер взяли холдаут ПАРАМЕТРАМИ (`f879bf9`), раннбук ушёл с `_iter3` на
`_holdout` (`31f9461`). Борроу снят на собственные 88.772 / 201.967 s. Dev-нога побайтно
не сдвинулась — `promo_dev40_prep.json` = `5510dd2b3098cdd3…`, пин цел.

**Два открытых стопа** (детали — `docs/plans/promo-pulse-1.PROGRESS.md`):
`--project` убил бы холдаут по построению (max-угол $1.8999 против капа $0.90, что (q)3 уже
принял на рунге 0), и `--close` dev-петли отказал на 33.7% — правая часть допуска это
последний `--note`, всегда снятый ДО последнего пода. Ни одной строки в леджер не записано.

**Найдено сухим контактом за $0:** `None`-floor ронял печать ПОСЛЕ записи регистрации;
и смок холдаута писал бы свою ставку под ИМЕНЕМ dev-инструмента — обе починены.

## Sessions
- 07:01: session ended (auto)
- 08:30: session ended (auto)
- 08:32: session ended (auto)
- 08:46: session ended (auto)

## Checkpoint 08:53
**s20 закрыт по существу: (q)5 сделан, `make check` 4320 passed / 2 skipped / exit 0 на `858ead0`**
(счёт s19, то есть ни один тест не добавлен и ни один не изменён; пол §8 (j) — 4266). Дерево чистое,
§8 (l) держит. Коммиты дня: `7378326` файлы тимлида по путям · `f879bf9` параметры холдаута ·
`31f9461` раннбук · `7a7d90c` имя ставки по части · `858ead0` `use_part` вяжет ОБЕ половины ·
`032b69d` + хвост — `knowledge/` · `9d82b48` PROGRESS.

**Сухие контакты за $0 (в скретчпад, под `results/` ничего не двигалось) поймали ТРИ дефекта:**
1. `None`-floor ронял печать ПОСЛЕ записи регистрации холдаута;
2. смок холдаута писал бы ставку под именем dev-инструмента — популяция залезла бы в чужую;
3. `use_part("dev")` был тихим no-op: перевязывал константы только при `part != "dev"`, так что
   вернуться к dev было нельзя. Проба пака из-за него отрапортовала dev-40 и holdout-40 как
   ПЕРЕСЕКАЮЩИЕСЯ, хотя они дизъюнктны 40 + 40.
Все три — на платном пути, куда $0-контакты и делались.

**Числа, которые понадобятся платной сессии** (рунг 0 при $0.74/h, REMAINING $1.8312):
cheap $0.8420 (−6.4%) · priced $0.8993 (**−0.1%**) · dear $1.8999 (+111%), жёсткий стоп 4378 s.
Смок холдаута: `@msuaaaa:8768` · `@VARUS_channel:2353` · `@VARUS_channel:8647`.

**Открытыми остаются два стопа** — детали в `docs/plans/promo-pulse-1.PROGRESS.md`: (a) полосы
`--project` и его KILL против капа $0.90; (b) допуск `--close`, чья правая часть снята ДО последнего
пода. Ни одной строки в леджеры не записано, оба байт-в-байт прежние.
- 08:56: session ended (auto)
- 08:57: session ended (auto)
- 09:05: session ended (auto)

## Checkpoint 09:06
**s21: один пункт «next» взят и оказался ЗАБЛОКИРОВАН на команду раньше, чем считал s20.** Куплено
ничего, пода не было, строки в леджер не писал, под `results/` ничего не двигалось. Коммит дня
один: `1b6aa9f` (PROGRESS + `knowledge/`), дерево чистое, §8 (l) держит.

**Находка — стоп не на §5, а на §0a.** `register()` пишет `decision_table` **без ветки по `PART`**
(`promo_dev_pass.py:860`) — при том что соседний `out_of_scope` ветку имеет. Значит
`--register --part holdout`, ПЕРВАЯ команда платной сессии, вкоммитила бы полосы dev-петли в
предрегистрацию холдаута вместе со строкой «ruling 03.09 (b), quoted and not moved» и формулировкой
«run iteration 1 now»; `--pack` затем отказывает на грязной регистрации, так что запись
запечатывается тем самым коммитом, которого §0a и требует. Урок записан в native memory:
«missed the deciding field».

**Две проверки сверх того (обе в памяти, `register()` собран и не записан):**
1. `project()` полосы из записи **не читает**: `bands` — поле для показа, вердикт — литералы
   `0.80`/`1.20` (`:1275`), KILL при `not worst["fits"]`. Новая ruling, вписанная только в запись,
   гейт не сдвинет — поэтому ответу тимлида нужна ФОРМА, а не только числа.
2. Решил уравнение вместо «по построению»: KILL срабатывает, как только медленнейший из трёх
   смоука превысит **95.340 s/thread**, а этот юнит — самый длинный тред популяции по §5 рунбука,
   при собственном среднем инструмента 88.772 s и максимуме 201.967 s ($1.8425).

**Стоп (b) проверен и выстрел НЕ блокирует:** леджеры по-шаговые (`runpod_guard.py:307`), проверки
«сосед закрыт» нет, `promo_dev_pass.py` дев-леджер не читает. Посылка рунбука «`promo-dev-loop` is
closed and a closed step cannot carry a new run's spend» сегодня ЛОЖНА, а вывод (свой леджер)
держится — по причине, которую его проза называет неверно. Леджер остаётся ОТКРЫТ и назван.

**Вопрос оператору → тимлиду, нужна одна из двух форм:** (i) назвать полосы холдаута И разрешить
править арифметику `project()` (сегодня она НЕ ТРОНУТА, порог не из плана = смена скоупа), либо
(ii) постановить, что денежный гейт — это FITS рунга 0 плюс кап как жёсткий стоп
(`--terminate-after`), и гейт §5 не запускать. `issued_rule` от (q)3 клонится к (ii), не решая.
Ни одна не выбрана, вперёд ничего не написано.
- 09:12: session ended (auto)
- 09:42: session ended (auto)
