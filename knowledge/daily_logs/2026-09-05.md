---
type: daily_log
date: 2026-09-05
tags: [daily]
---

# 2026-09-05

## Summary

**День, в котором вопрос S2 был закрыт числом, закон переехал на кодбук v1.2, а вторая покупка
умерла на собственном смоке.** Семь сессий (s20–s26), две покупки, три остановки — все три про
деньги, все три предвидимые тимлидом.

**s20–s21 ($0, пода не было).** Холдаут подготовлен: эмиттер и грейдер взяли холдаут ПАРАМЕТРАМИ,
борроу снят. Сухие контакты за $0 поймали четыре дефекта на платном пути, включая `use_part("dev")`
как тихий no-op и `register()` без ветки по `PART` — последний означал бы, что ПЕРВАЯ команда платной
сессии вкоммитит полосы dev-петли в предрегистрацию холдаута.

**s22 — рулинг (r) снял оба стопа, выстрел сделан.** Денежный гейт холдаута = FITS рунга 0 плюс кап
как жёсткий стоп; полосный гейт §5 не запускается. Аудит собранной записи нашёл **СЕМЬ** полей с
решениями там, где рулинг называл четыре. Под `pcu2fqc7ebwfcw` прожил **1443 с** и шёл в **3.4×
быстрее** зарегистрированной ставки. **Чтение: сигнал 0.7958 ✅ · субъект 0.7181 ❌**, полное по §6.5
(40 из 40, 0 unparsed). Кластер из 53 промахов — chain↔brand на банковском треде. **$0.296617 из
капа $1.10.**

**s23 — «v1.2-prep» ($0).** Потолок цикла-3 $7.00 → **$9.00**. holdout-2 вытянут ПАРАМЕТРАМИ на том
же продюсере: исключения ПЕРЕСЕКАЮТСЯ, поэтому популяция = `eligible − union` (398 тредов); дро-1
переиграно побайтно. K8 v2: **измерено, что алиас «Сильпо» даёт 0 строк, а всё правило — 1 строку из
188** — выигрыш меньше мотивации, сказано вслух. Закон из v1.2, `--leak-check` чист по 440 комментам.
Стоп: итерация 4 не регистрируется под кап $0.60.

**s24 — рулинг (t), итерация 4 зарегистрирована ($0).** Обе половины стопа оказались КОДОМ:
`rate_for("dev")` возвращала заимствованную ставку, а `own_rate()` читала три СМОКА (88.772 с/тред —
втрое больше всего, чем инструмент когда-либо шёл). Теперь выбор по `contract` + `sample == "whole
run"`. Задним числом за $0 три строки целых ранов: 17.7118 · **47.6615** · 25.8263, n=40 каждая.
Нога dev стала ДВУМЯ руками (dev-40 + dev-2, 80 тредов). Рунг 0: cheap **$0.8705** FITS · dear
$4.9984 NO → на MEAN-угле, жёсткий стоп 5838 с при капе $1.20.

**s25 — рука стала параметром ($0).** `--score` читал ОДИН эталон по ВСЕЙ ноге: ключ dev-40 против
80 отвеченных юнитов, ключ dev-2 не открывался. `DEV_ARMS` называет обе руки по МЕТКЕ; `use_arm`
отказывает руке вне `--part dev` и связывает ТОЛЬКО скорер. Прогон за $0 на старых ответах
воспроизвёл итерацию 3 (0.8714 / 0.9104) и прочёл holdout-40 против перечитанного ключа (0.7394 /
0.7833) — проводка, не производительность. Отдельно: **срез `make check` был НЕЗАКРЫТЫМ списком** —
`ls tests/test_*.py` уже 229 файлов, три зелёных среза дали 3900 вместо 4321.

**s26 — рулинг (v), покупка, и она вернулась НЕПОЛНОЙ.** Стоп начала сессии: гейт, который раннбук
берёт ДО create, отказал — `--step promo-dev-loop --step-cap 1.20` → **$1.8462 из $1.20**; при старом
$2.50 остаётся $0.6538 < mean-угла $0.8705. Ни одно названное число ран не финансировало. Корень в
коде: `guard_reading()` звала гард БЕЗ `--step`, поэтому регистрация читала FITS против остатка
ЦИКЛА. Рулинг (v) + PHASE v11: **один платный ран = одна линия шага**. Сделано за $0:
`guard_reading(step, step_cap)`, `cap_rule` на обеих ветках = min(число оператора, REMAINING ЭТОЙ
линии, REMAINING цикла); негативный контроль прогнан.

Затем **под `kzcnhe01mgdwvk`** (13:21:09Z → 14:17:13Z, 3364 с, **$0.691489**). Рунг 1 GO, порт на
42-й секунде при дед-мане 500 с, том тёплый. И **смок не вернулся целиком**: два юнита легли
сбалансированными (6.4 с, 9.5 с), третий — @VARUS_channel:8647 на **14281 символ, самый длинный
рендер** — умер на `torch.OutOfMemoryError`, **338 MiB отказано при 23.19 из 23.52 GiB занятых**.
Решающая таблица записи взята как написано: **GO не записан**, ни один из 80 юнитов не куплен, под
удалён, сегмент закрыт, листинги показаны. Это неполное чтение **PHASE §6.5**: записывается под
своим номером, с барами НЕ сравнивается.

## Decisions

- [[the-holdout-gate-is-rung-0-fits-plus-the-hard-stop]] — рулинг (r), вариант (ii): переиспользованный
  продюсер обязан ветвить не только пути, но и поля записи с решениями.
- Рулинг (v) → PHASE **v11**: платный ран покупается под СВОЕЙ линией шага (`promo-iter<N>` ·
  `promo-holdout2` · `promo-c3`), открытая многорановая линия нового рана не несёт, регистрация
  читает гард с `--step`. Полный текст — `docs/reviews/2026-08-30-plan-promo-pulse-1.md`.
- Рулинг (w) → PHASE **v12**: сбой ОБСЛУЖИВАНИЯ (OOM, краш, мёртвый процесс) — транспортный
  дефект §6.5: инструмент не движется никогда, движется среда исполнения, и она всплывает СРАЗУ.

## Next

**Стоп по OOM закрыт рулингом (w) тем же вечером — завтра ПОКУПКА, итерация 5, и ничего кроме неё.**

1. За $0 сначала: **`--close` линии `promo-iter4`** (`--expect-ms 3364000 --tolerance 0.05`) — одна
   линия, один ран, ран состоялся. Затем **транспортный дефект по §4**: раннер ловит исключение
   юнита, пишет ERROR-ответ (`error`, имя исключения, юнит) и выходит ненулём; Мак считает ERROR
   среди трёх смок-ответов за «смок не вернулся» и удаляет СРАЗУ. ОДИН тест в обе стороны, тем же
   коммитом; пин раннера двигается вместе с фиксом. Крах стоил **$0.62 ожидания, а не счёта**:
   раннер умер на 164-й секунде, а Мак ждал третий ответ до дедлайна.
2. **Инструмент НЕ движется** — закон v1.2, шаблон, рендер, потолок 4000, NF4/bf16, greedy, раннер и
   парсер побайтно. Движется **ОБСЛУЖИВАНИЕ**, которое регистрация ценит, но не пинит как инструмент:
   карта **≥32 GB** в EU-RO-1 (RTX PRO 4500 32 GB ~$0.72/ч, иначе A6000 / L40S 48 GB) по дорогому
   предложению дня **≤$0.90/ч** — 4090 под этот промпт больше не покупается; плюс
   `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`. Оба раскрыты в `re_emission`,
   `extractor_version` не меняется: хосты расходились и раньше, а выходы оставались побайтно теми же.
3. **Итерация 5 — линия `promo-iter5`, кап $1.40** (оператор 16:35), FITS на mean-угле по цене дня,
   жёсткий стоп из капа, бэкстоп поднимается до минут капа, если кусает первым. Это ПЯТАЯ и
   последняя из зарегистрированных: RED на dev-40 → вопрос закрывается красным, решает оператор;
   GREEN → эталон holdout-2 и выстрел на `promo-holdout2` ≤$0.90, потом c3.
4. Деньги после: REMAINING $2.81 → ≥$1.41 → holdout-2 ≤$0.90 → на c3 нужен поднятый потолок (на
   счету $8.29) — слово оператора тогда.

## Blockers

- **`promo-iter4` ОТКРЫТ и его `--close` ПРИДЕРЖАН** — §6.5 перекупает неполное чтение под следующим
  номером, а закрывающая запись односторонняя. Линия: $0.6690 из $1.20 по дельте гарда, под-цена
  $0.691489. Так же открыты `promo-holdout` ($0.296617) и `promo-dev-loop` ($1.8462, нового рана не
  несёт).
- **`--open`/`--close-segment` пишут `results/promo_dev_loop_run.json` в масштабе ПАРТИИ, не ЛИНИИ:**
  его `spent_all_segments_usd` читает $1.9345 там, где линия читает $0.6690, и вердикт сегмента
  вышел «OVER» на здоровом закрытии. `--expect-ms` обязан фильтровать по `created_at >=` якоря линии
  — проверено, ровно ОДИН сегмент (3364000 мс). Это тот же перекос, что (v) починил для гарда.
- **Ставка холдаута так и не попала в `measurements.jsonl`** (`write_measurement()` зовётся только из
  ветки `--project`, которую (r)2 не запускает). У пода итерации 4 строки целого рана тоже НЕТ —
  `--close-segment` намеренно прошёл без `--replies`: два юнита не целый ран.
- **`make check` ~12 мин, больше потолка одного вызова**, и срез обязан быть ЗАКРЫТЫМ списком:
  `ls tests/test_*.py` = **229**. Последний полный прогон — 4320 passed, 1 failed (порядок
  «коммит → пак»), позеленел по имени; тестов за день не добавлено.
- Остальные долги — «named, not built» в `docs/plans/promo-pulse-1.PROGRESS.md`.

Вчера: [[2026-09-04]]. Уроки дня: [[a-record-must-read-the-gate-its-run-will-face]],
[[a-smoke-of-extremes-finds-what-an-average-hides]],
[[an-ordered-instrument-change-reds-an-artifact-test]],
[[a-downstream-promotion-undoes-the-ordering-you-were-asked-for]],
[[a-ruling-can-outrun-the-law-it-amends]], [[make-check-exceeds-one-call]].
- 16:31: session ended (auto)
- 16:42: session ended (auto)
- 16:58: session ended (auto)
- 17:26: session ended (auto)
