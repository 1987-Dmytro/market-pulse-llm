---
type: daily_log
date: 2026-09-05
tags: [daily]
---

# 2026-09-05

## Summary

**День, в котором вопрос S2 был закрыт числом, закон переехал на кодбук v1.2, вторая покупка умерла
на собственном смоке — а вечер ушёл на то, чтобы закрыть за ней деньги и починить транспорт.** Девять
сессий (s20–s28), две покупки, **четыре** остановки исполнителя, все четыре про деньги и все четыре
предвидимые тимлидом. Итог: **обе линии закрыты, транспортный дефект починен, красных тестов нет**.

**s20–s21 ($0, пода не было).** Холдаут подготовлен: эмиттер и грейдер взяли холдаут ПАРАМЕТРАМИ,
борроу снят. Сухие контакты за $0 поймали четыре дефекта на платном пути, включая `use_part("dev")`
как тихий no-op и `register()` без ветки по `PART` — последний означал бы, что ПЕРВАЯ команда платной
сессии вкоммитит полосы dev-петли в предрегистрацию холдаута.

**s22 — рулинг (r) снял оба стопа, выстрел сделан.** Денежный гейт холдаута = FITS рунга 0 плюс кап
как жёсткий стоп; полосный гейт §5 не запускается. Аудит собранной записи нашёл **СЕМЬ** полей с
решениями там, где рулинг называл четыре. Под `pcu2fqc7ebwfcw` прожил **1443 с** и шёл в **3.4×
быстрее** зарегистрированной ставки. **Чтение: сигнал 0.7958 ✅ · субъект 0.7181 ❌**, полное по §6.5
(40 из 40, 0 unparsed). Кластер из 53 промахов — chain↔brand на банковском треде. **$0.296617 из
капа $1.10.**

**s23 — «v1.2-prep» ($0).** Потолок цикла-3 $7.00 → **$9.00**. holdout-2 вытянут ПАРАМЕТРАМИ на том
же продюсере: исключения ПЕРЕСЕКАЮТСЯ, поэтому популяция = `eligible − union` (398 тредов, 20/20 по
стратам); дро-1 переиграно побайтно. K8 v2: измерено, что алиас «Сильпо» даёт 0 строк, а всё правило
— 1 строку из 188; выигрыш меньше мотивации, сказано вслух. Закон из v1.2, `--leak-check` чист по
440 комментам. Стоп: итерация 4 не регистрируется под кап $0.60.

**s24 — рулинг (t), итерация 4 зарегистрирована ($0).** Обе половины стопа оказались КОДОМ:
`rate_for("dev")` возвращала заимствованную ставку, а `own_rate()` читала три СМОКА (88.772 с/тред —
втрое больше всего, чем инструмент когда-либо шёл). Теперь выбор по `contract` + `sample == "whole
run"`. Задним числом за $0 три строки целых ранов: 17.7118 · **47.6615** · 25.8263, n=40 каждая.
Нога dev стала ДВУМЯ руками (dev-40 + dev-2, 80 тредов). Рунг 0: mean **$0.8705** FITS · dear
$4.9984 NO → на MEAN-угле, жёсткий стоп 5838 с при капе $1.20.

**s25 — рука стала параметром ($0).** `--score` читал ОДИН эталон по ВСЕЙ ноге: ключ dev-40 против
80 отвеченных юнитов, ключ dev-2 не открывался. `DEV_ARMS` называет обе руки по МЕТКЕ; `use_arm`
отказывает руке вне `--part dev` и связывает ТОЛЬКО скорер. Прогон за $0 на старых ответах
воспроизвёл итерацию 3 (0.8714 / 0.9104) и прочёл holdout-40 против перечитанного ключа (0.7394 /
0.7833) — проводка, не производительность. Отдельно: **срез `make check` был НЕЗАКРЫТЫМ списком** —
`ls tests/test_*.py` уже 229 файлов, три зелёных среза дали 3900 вместо 4321.

**s26 — рулинг (v), покупка, и она вернулась НЕПОЛНОЙ.** Стоп начала сессии: гейт, который раннбук
берёт ДО create, отказал — `--step promo-dev-loop --step-cap 1.20` → **$1.8462 из $1.20**. Корень в
коде: `guard_reading()` звала гард БЕЗ `--step`, поэтому регистрация читала FITS против остатка
ЦИКЛА. Рулинг (v) + PHASE v11: **один платный ран = одна линия шага**. Затем **под `kzcnhe01mgdwvk`**
(13:21:09Z → 14:17:13Z, 3364 с, **$0.691489**). Рунг 1 GO, порт на 42-й секунде при дед-мане 500 с,
том тёплый. И **смок не вернулся целиком**: два юнита легли сбалансированными (6.4 с, 9.5 с),
третий — `@VARUS_channel:8647` на **14281 символ, самый длинный рендер** — умер на
`torch.OutOfMemoryError`, **338 MiB отказано при 23.19 из 23.52 GiB занятых**. Таблица решений взята
как написано: **GO не записан**, ни один из 80 юнитов не куплен, под удалён. Неполное чтение
**PHASE §6.5** — под своим номером, с барами НЕ сравнивается.

**s27 — четвёртая денежная остановка дня, и она была тимлида.** Рулинг (w)1 приказал закрыть
`promo-iter4` по run record — а линия НЕ МОГЛА пройти гейт ни в тот вечер, ни потом:
`recorded_reading()` берёт ПОСЛЕДНЮЮ открытую запись линии как правую часть допуска, а у
`promo-iter4` она была одна и **ДО пода** ($0.0097) — settle ~$0.6915 против неё = **~7000% off**,
отказ навсегда, а не однажды. Перечитаны все линии, что когда-либо закрывались под допуском
(`lora-c` · `promo-pulse-1` · `srv2b` · `srv2d` · `45h2`) — каждая settle'илась против ПОСТ-РАНОВОГО
чтения. Снято чтение **$0.7143 в 14:40:49Z** (баланс 8.2403244051), единственная линия леджера за
сессию. Само закрытие уперлось в биллинг: walk покрывал **57.7%** в 14:57Z, а `complete()` считает
ЧАСТИЧНЫЙ walk третьим состоянием и на нём не settle'ит. Тимлид ответил аддендумом (w)+ и **PHASE
v13 §6.1**: ссылка гейта = пост-рановое `--note` после удаления пода и ДО любого следующего.

**s28 — обе линии закрыты, транспорт починен, отчёт перезаписан ($0, пода не было).**
`promo-iter4`: read-only walk СНАЧАЛА — вернулся ПОЛНЫМ, **3 363 287 мс из 3 364 000** (713 мс при
полосе ±33 640), то есть блокером были часы и они дотикали. Settle **$0.694815** против ссылки
$0.7143 — **2.73%**, внутри 0.05. `promo-holdout` закрыт **ПО ОДНОМУ WALK** (v13 §6.1): его
единственная запись 0.0 и до пода, поэтому допуск ПРОПУЩЕН, а позднего `--note` брать нельзя — он
лёг бы **$1.0708** и закрыл бы гейт навсегда. Границу выбрало измерение, а не вкус: `walk_ms`
насыщается на **1 443 991** в любой точке с 08:20Z до 13:21Z, а **безграничный читает 4 807 278 мс /
$0.993024** — под итерации 4, отказ `complete()`. Негативный контроль сработал. Settle **$0.298209**.
Затем **(w)3**: раннер ловит исключение юнита, дописывает ERROR-ответ и выходит ненулём; Мак читает
его как «смок не вернулся» и удаляет СРАЗУ. Проиграно на настоящем артефакте: старый файл читается
`WAITING`, крах — `THE SMOKE DID NOT COME BACK` с именем `@VARUS_channel:8647`. `make check`
**4322 / 2 skipped**, exit 0, на чистом дереве.

## Decisions

- [[the-holdout-gate-is-rung-0-fits-plus-the-hard-stop]] — рулинг (r), вариант (ii): переиспользованный
  продюсер обязан ветвить не только пути, но и поля записи с решениями.
- Рулинг (v) → PHASE **v11**: платный ран покупается под СВОЕЙ линией шага (`promo-iter<N>` ·
  `promo-holdout2` · `promo-c3`), открытая многорановая линия нового рана не несёт, регистрация
  читает гард с `--step`.
- Рулинг (w) → PHASE **v12**: сбой ОБСЛУЖИВАНИЯ (OOM, краш, мёртвый процесс) — транспортный
  дефект §6.5: инструмент не движется никогда, движется среда исполнения, и она всплывает СРАЗУ.
- [[a-lines-close-settles-against-its-post-run-reading]] — аддендум (w)+ → PHASE **v13 §6.1**: ссылка
  гейта у линии = её пост-рановое `--note`; линия без такого чтения закрывается по одному walk и
  позднего чтения НЕ БЕРЁТ. Полные тексты — `docs/reviews/2026-08-30-plan-promo-pulse-1.md`.
- Развилка, которую (w)3 не решает, закрыта внутри файла, чей пин движется: ERROR-строка с `id`
  считается `already_answered` ОТВЕЧЕННОЙ, поэтому resume поверх неё пропустил бы убивший под юнит;
  чинить это в `reader_v5_pod_runner` значило бы двигать ПРИПИНЕННЫЙ раннер (§4).

## Next

**Завтра — ПОКУПКА, итерация 5, и ничего кроме неё. Ни одна линия больше не мешает.**

1. **$0-половина первой:** ре-эмиссия регистрации (`--register` пересчитывает `pinned_inputs` с
   диска, поэтому новая sha раннера ложится туда сама) и **фит-пруф §6.5**, который так и не
   построен: самый длинный зарегистрированный рендер обязан быть доказан помещающимся ДО пода. (w)3
   починил СООБЩЕНИЕ о крахе, не его предотвращение.
2. **Инструмент НЕ движется** — закон v1.2, шаблон, рендер, потолок 4000, NF4/bf16, greedy, парсер
   побайтно. Движется **ОБСЛУЖИВАНИЕ**: карта **≥32 GB** в EU-RO-1 (RTX PRO 4500 32 GB ~$0.72/ч,
   иначе A6000 / L40S 48 GB) по дорогому предложению дня **≤$0.90/ч** — 4090 под этот промпт больше
   не покупается; плюс `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`. Оба раскрыты в
   `re_emission`, `extractor_version` не меняется. Раннер тоже движется — и это ТОЖЕ обслуживание.
3. **Итерация 5 — линия `promo-iter5`, кап $1.40** (оператор 16:35), FITS на mean-угле по цене дня,
   жёсткий стоп из капа. ПЯТАЯ и последняя: RED на dev-40 → вопрос закрывается красным, решает
   оператор; GREEN → эталон holdout-2 (тимлид, вслепую) и выстрел на `promo-holdout2` ≤$0.90.
4. **Её закрытие уже расписано v13 §6.1:** пост-рановое `--note` после удаления пода и ДО любого
   следующего, а `--close` несёт `--until` — окно iter4 теперь ловушка для безграничного walk.
5. Деньги: REMAINING **$2.7506** → ≥$1.35 → holdout-2 ≤$0.90 → на c3 нужен поднятый потолок.

## Blockers

- **Закрыто сегодня, больше не блокер:** `promo-iter4` ($0.694815 / $1.20) и `promo-holdout`
  ($0.298209 / $1.10) — обе линии settle'нуты и записаны, каждая двумя файлами в одном коммите.
- **`promo-dev-loop` — единственная открытая линия, и она ОТКАЗЫВАЕТ:** **$2.5799 из своих $2.50**,
  exit 1. Кап поднять нельзя, закрыть нельзя (её walk безграничен по построению, (r)4), поэтому её
  просто **больше никогда не называют** — итерация 5 читает FITS через `--step promo-iter5`. Ловушка,
  названная как ловушка. (Сегодня я сам прочёл её при капе $2.16, который НАПЕЧАТАЛ, а не взял из
  леджера; перечитано.)
- **Фит-пруф §6.5 не построен** — см. Next 1. Единственный настоящий долг перед покупкой.
- **`--open`/`--close-segment` пишут `results/promo_dev_loop_run.json` в масштабе ПАРТИИ, не ЛИНИИ:**
  `latest` читает `OVER`, суммируя итерации 1–3 с четвёртой. `--expect-ms` ОБЯЗАН браться из
  сегмента с `created_at >=` якоря линии — проверено дважды сегодня, walk сошёлся до 713 мс.
- **Ставки целого рана нет ни у пода итерации 4, ни у холдаута** (`write_measurement()` зовётся
  только из ветки `--project`, которую (r)2 не запускает).
- **`prereg_promo_holdout.json` ЗАПЕЧАТАН над раном под старыми байтами раннера** и НЕ
  перепинивается: его устаревший пин — факт той записи, а не дефект, который надо зеленить.
  `committed_registration()` по-прежнему не переверяет `pinned_inputs` — унаследованный долг.
- **`make check` ~12 мин, больше потолка одного вызова**, срез обязан быть ЗАКРЫТЫМ списком
  (`ls tests/test_*.py` = 229). Последний прогон: **4322 passed, 2 skipped**, exit 0, на чистом
  дереве в `54c0cc5`. **Красных тестов нет.**
- Остальные долги — «named, not built» в `docs/plans/promo-pulse-1.PROGRESS.md` и в
  `docs/reports/promo-pulse-1.md`.

Вчера: [[2026-09-04]]. Уроки дня: [[a-lines-gate-reference-is-its-post-run-reading]],
[[a-crash-must-write-into-the-file-its-reader-opens]],
[[a-record-must-read-the-gate-its-run-will-face]],
[[a-smoke-of-extremes-finds-what-an-average-hides]],
[[an-ordered-instrument-change-reds-an-artifact-test]],
[[a-downstream-promotion-undoes-the-ordering-you-were-asked-for]],
[[a-ruling-can-outrun-the-law-it-amends]], [[make-check-exceeds-one-call]],
[[a-number-typed-into-its-own-checker]], [[a-settlement-is-a-reading-too]].
- 18:37: session ended (auto)
