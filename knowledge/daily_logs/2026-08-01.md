---
type: daily_log
date: 2026-08-01
tags: [daily]
---

# 2026-08-01

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->
- **Step 4a executed, awaiting team-lead review** (`docs/PROMPT-4a.md` rev. 1). Phase 3's close and
  the Phase 4 briefing committed as one commit; Write-tool probe on `docs/STATUS.md` refused as
  designed and the file was not touched.
- Local NF4 inference path added to `scripts/eval_zero_shot.py` (`--backend local`), reusing 3b's
  prompts, parser, failure taxonomy, record and scorer unchanged. New `gpu` extra, 239 tests green.
- **The own-pod zero-shot row** — `google/gemma-4-31b-it`, NF4 on a rented A6000, 758/758 rows,
  zero failures — is the G1d/G1e anchor and defines the **44-id G1b slice**
  (`results/g1b_slice.json`). Cross-check against the OpenRouter fp8 row in
  [[phase4-own-pod-anchor]]; the own-pod number anchors, nothing averaged.
- **Greedy decoding is not batch-invariant on this stack** — measured, so the run went at batch
  size 1 (39 min, $0.35). Phase-4 GPU spend **$0.6456 of the $25 cap**; pod EXITED, volume kept
  for 4b.
- Open question for 4b: `scorer.sarcasm_slice_fix_rate` cannot consume the slice file 4a was asked
  to write. Operator decision, not worked around.

## Sessions
- 10:01: session ended (auto)
- 10:02: session ended (auto)
- 12:01: session ended (auto)
- 12:18: session ended (auto)

## Checkpoint 12:21

First checkpoint of the day; the Summary above covers step 4a end to end. Since the report was
delivered:

- **Operator closed open question 2:** the own-pod row is baseline **(c) everywhere**, not only the
  G1d/G1e anchor — pre-registered before 4b trains and before any fine-tuned number exists.
  Recorded in [[phase4-own-pod-anchor]] §(f) with the bar table it implies: G1a ≥ 0.9418 (floors
  `ua` 0.8718 · `ru` 0.8649), G1c ≥ 0.8436, G1e ≥ 0.9974. No code changed — nothing in `src/` or
  `scripts/` selects a baseline row, and building that now would be building 4b.
- **The decision surfaced a blocker and it went back to the operator, not into a workaround:**
  **G1d's bar is 0.9084 + 0.10 = 1.0084**, above the 1.0 ceiling of a macro-F1, so G1d as written
  cannot be passed by any model. Older than 4a — on the OpenRouter anchor it was G1e that was
  impossible (1.0211) and G1d that was merely brutal (0.9898). **No anchor makes both +10 pp gates
  satisfiable**, because `gemma-4-31b-it` zero-shot is already above 0.90 on both heads. Flagged;
  `docs/SPEC.md` §5 untouched.
- Two open items now stand before 4b is scoped: this one, and
  `scorer.sarcasm_slice_fix_rate` being unable to consume the 44 ids of `results/g1b_slice.json`.
- Commit `35c44c1`; `make check` green, 239 tests; tree clean.
- 12:22: session ended (auto)
- 13:59: session ended (auto)

## Checkpoint 15:00

**Step 4b executed end to end and accepted the same evening** (`docs/PROMPT-4b.md` rev. 1). Since
the 12:21 checkpoint:

- **Both of 4a's escalations came back decided** in SPEC amendment 3.5 and were executed, not
  worked around: G1d/G1e became `anchor − 1 pp` no-regression gates, and the scorer's fix-rate
  function now takes the persisted 44 ids explicitly, FIXED meaning correct on **both** labels.
- **The bars derive themselves.** `src/market_pulse/records.py` reads `results/baselines.json` the
  way a gate must — the anchor selector refuses ambiguity rather than picking (4c's fine-tuned rows
  are `backend: local` too), and the slice loader checks the file's sha256 before its ids are used.
  `scripts/gate_bars.py` prints the whole table; **no anchor number is typed anywhere in code**.
- **Two defects caught before any money was spent**, both in [[4b-training-contract]]: the record
  holds *two* rows under the gate id `G1d` and a dict keyed by id returned the ungated relevance
  number (bar 0.9315 instead of 0.8984); and Gemma 4's chat template drops the thinking channel
  when a full assistant turn is rendered, so the obvious way to build a training example would have
  trained on a context no gate row carries. A CPU stub run of the training loop found two more.
- **The smoke was clean** (pod `ouhimpvjem3spe`, A6000, US-TX-1, volume-less): loss 0.1799 → 0.0399
  over 50 steps with the held-out carve tracking it, 410 LoRA modules all in the language model,
  adapter saved and reloaded onto a freshly loaded NF4 base, 24 carved **training** rows scored
  with zero parse failures, peak 30.47 GB of 48. Artifacts in `results/train/4b-smoke/`.
- **The pre-registered stop fired**: 45.23 s/step projects 3.42 h / 4.37 h per arm against a 4 h
  ceiling. Reported with three levers and none taken.
- **Team-lead acceptance the same night — all three open items closed** (SPEC rev. 3.6):
  the ceiling rises **4 h → 5 h uniformly** (config untouched, ablation stays paired, the $25 cap
  stays the binding protection); **an arm is a volume-less pod session** wherever A6000 stock is,
  with the CA-MTL-3 volume a bonus and a second volume in EU-RO-1 rejected; and the **resume test
  is a mandatory first step** on the 4c pod — the gap this session disclosed rather than papered
  over.
- Phase-4 GPU spend **$1.1203 of $25.00**; 4b pod deleted, 4a pod EXITED, the 100 GB volume kept.
- Commits: `ff471e3 · 8c2c774 · 975a731 · 221d2a1 · fde75d2 · bbe535c · d4cc56d · 43d9a51`;
  `make check` green after each, 275 tests.
- **Next is 4c** (`docs/PROMPT-4c.md`, untracked in the tree): both arms trained in full, each
  scored once, the selection rule applied. Not started.
- 15:03: session ended (auto)
- 16:18: session ended (auto)
- 17:31: session ended (auto)
