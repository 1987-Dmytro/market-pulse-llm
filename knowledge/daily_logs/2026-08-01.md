---
type: daily_log
date: 2026-08-01
tags: [daily]
---

# 2026-08-01

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->
- **Step 4a executed, awaiting team-lead review** (`docs/PROMPT-4a.md` rev. 1). Phase 3's close and
  the Phase 4 briefing committed as one commit; Write-tool probe on `docs/STATUS.md` refused as
  designed and the file was not touched.
- Local NF4 inference path added to `scripts/eval_zero_shot.py` (`--backend local`), reusing 3b's
  prompts, parser, failure taxonomy, record and scorer unchanged. New `gpu` extra, 239 tests green.
- **The own-pod zero-shot row** — `google/gemma-4-31b-it`, NF4 on a rented A6000, 758/758 rows,
  zero failures — is the G1d/G1e anchor and defines the **44-id G1b slice**
  (`results/g1b_slice.json`). Cross-check against the OpenRouter fp8 row in
  [[phase4-own-pod-anchor]]; the own-pod number anchors, nothing averaged.
- **Greedy decoding is not batch-invariant on this stack** — measured, so the run went at batch
  size 1 (39 min, $0.35). Phase-4 GPU spend **$0.6456 of the $25 cap**; pod EXITED, volume kept
  for 4b.
- Open question for 4b: `scorer.sarcasm_slice_fix_rate` cannot consume the slice file 4a was asked
  to write. Operator decision, not worked around.

## Sessions
- 10:01: session ended (auto)
- 10:02: session ended (auto)
- 12:01: session ended (auto)
- 12:18: session ended (auto)

## Checkpoint 12:21

First checkpoint of the day; the Summary above covers step 4a end to end. Since the report was
delivered:

- **Operator closed open question 2:** the own-pod row is baseline **(c) everywhere**, not only the
  G1d/G1e anchor — pre-registered before 4b trains and before any fine-tuned number exists.
  Recorded in [[phase4-own-pod-anchor]] §(f) with the bar table it implies: G1a ≥ 0.9418 (floors
  `ua` 0.8718 · `ru` 0.8649), G1c ≥ 0.8436, G1e ≥ 0.9974. No code changed — nothing in `src/` or
  `scripts/` selects a baseline row, and building that now would be building 4b.
- **The decision surfaced a blocker and it went back to the operator, not into a workaround:**
  **G1d's bar is 0.9084 + 0.10 = 1.0084**, above the 1.0 ceiling of a macro-F1, so G1d as written
  cannot be passed by any model. Older than 4a — on the OpenRouter anchor it was G1e that was
  impossible (1.0211) and G1d that was merely brutal (0.9898). **No anchor makes both +10 pp gates
  satisfiable**, because `gemma-4-31b-it` zero-shot is already above 0.90 on both heads. Flagged;
  `docs/SPEC.md` §5 untouched.
- Two open items now stand before 4b is scoped: this one, and
  `scorer.sarcasm_slice_fix_rate` being unable to consume the 44 ids of `results/g1b_slice.json`.
- Commit `35c44c1`; `make check` green, 239 tests; tree clean.
- 12:22: session ended (auto)
