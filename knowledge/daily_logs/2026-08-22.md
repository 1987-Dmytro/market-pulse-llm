---
type: daily_log
date: 2026-08-22
tags: [daily, checkpoint, pass1, pass2, acceptance]
---

# 2026-08-22

## Checkpoint 06:19

**Сессия исполнителя `pass1-window r2` закрыта вчера и полностью записана в [[2026-08-21]]
(сессия 4).** Здесь — только то, что появилось ПОСЛЕ неё, и это чужая работа: её надо знать,
но не присваивать.

**Тимлид принял `pass1-window r2` ЗЕЛЁНЫМ** (правка `docs/STATUS.md`, некоммичена на момент
чекпойнта). Ключевое из его приёмки, чего у меня в отчёте нет:

- своя проверка независимым парсером: out-file, ценз объединения, 14 золотых и таблица фильтра
  воспроизведены им заново; **опровергатель 12/12**, enum 21/21, свип по отменённым числам чист;
  r1-артефакты байт-в-байт;
- **талли r2: contract-health 11 (c-gap 4 · s-gap 1 · v-gap 6) · paid 10 (process 6 · env 4)** —
  против авторского спайка r1 (29 · 7);
- его кросс-проверка таблицы прохода-2 против референса: **все пять флагманских F-тредов внутри
  фильтра**, а шумовой тред `10366` несёт 4 строки `сеть_ритейлер` — проход-2 обязан их выбросить.
  Этого чтения в моём цензе не было: я дал таблицу, он свёл её с эталоном;
- его замечание по процессу: **git-часы 19:39:58Z < create 19:40:45Z — 47 секунд, «второй раз
  впритык»**. Доказательство «план предшествовал деньгам» держится, но запас тонкий, и это
  повторяется. → [[a-git-clock-proof-needs-more-than-a-minute]]

**Зарегистрирован следующий контракт: `docs/PROMPT-pass2-signals.md`** (161 строка, кап $1.50,
D0 $0 → D1 платно: смок на пяти F-тредах ПЕРВЫМ, затем полный прогон → D2 $0). Он — для СВЕЖЕЙ
сессии исполнителя, не для этой. Что в нём задано: популяция — 79 тредов с ≥1 отфильтрованной
строкой (281 строка, 32 756 знаков), выводимые ВЫЗОВОМ фильтра ценза по объединению out-file'ов,
ключ — пара; один юнит = один тред; вход — пост + entity-блок + отфильтрованные комменты со своими
полями прохода-1, сырые комменты НЕ рендерятся; **STRICT-полномочия**: проход-2 никогда не
переразмечает субъект, может ВЫБРОСИТЬ коммент как шум, а расхождение пишет отчётным
`subject_doubt`; счёт — скорером читателя против золота r2; Deviations от **Dv680**.

**Состояние дерева на чекпойнте:** мои 16 коммитов вчерашней сессии на месте, `HEAD 70db5e3`,
suite **3 424/2**, облако пусто, том на месте. Некоммичены только файлы тимлида —
`docs/STATUS.md`, `docs/PROMPT-pass2-signals.md`, хвост `knowledge/index.md` и одна строка
авто-закрытия в [[2026-08-21]]. Коммитить их — отдельным словом оператора, и по путям.
- 08:32: session ended (auto)
- 09:52: session ended (auto)
- 10:05: session ended (auto)
- 10:06: session ended (auto)
- 10:08: session ended (auto)
- 10:08: session ended (auto)
- 10:09: session ended (auto)

## pass2-signals — D0 → D1 → D2, и рунг S′ сказал STOP

**Исполнено `docs/PROMPT-pass2-signals.md` целиком.** Отчёт — `docs/reports/pass2-signals.md`,
ADR [[pass-2-in-the-readers-schema-and-strict-authority]], прережка
`results/prereg_pass2_signals.json` (44 строки H6), вердикт `results/pass2_signals_verdict.json`.

### D0, $0, всё до пода

79 юнитов / 281 строка / 32 756 знаков — выведены **вызовом** цензового фильтра над объединением
двух out-file'ов прохода-1, ключ — пара `(тред, msg_id)`. Пять F-тредов первыми как смок-плечо.
Промпт и рендерер — в НОВОМ `src/market_pulse/pass2.py`, потому что `prompts.py` запинен 38
записями; парсер — обе половины ридерского (`_reader_object` → `_reader`).

**Три числа известны до пода и зарегистрированы как таковые:** бар 2 = 3 из 4 (E1 не вызывается),
F2a недостижим по построению, достижимость прохода-2 отличается от пробы ридера в ОБЕ стороны.

**Ревью пяти линз: 15 заявок, 13 различных дефектов, два фатальных** — все команды гейта падали на
`KeyError`, рунги 2 и 3 не работали вовсе. Оба в КОМАНДАХ, а 71 тест гонял функции за ними
[[the-entry-points-preamble-is-untested-code]]. Плюс три моих. Все 16 исправлены, каждый прогнан
руками ДО правки. Скептики ревью читали дерево, которое я правил под ними — их «12 опровергнуто»
значит «уже починено на HEAD», и это записано в отчёте честно.

### D1, платно: 526.0 с = $0.108122

Под `9rquj8p0lelct3`, 4090 EU-RO-1 $0.74/ч. **ssh 38 с, бут 139.5 с — новый пол стека.**
Коммит `07:52:05Z` и create `08:01:39Z` напечатаны рядом в UTC, как теперь требует ранбук.

**Декод прохода-2 измерен впервые:** 58.07 · 15.80 · 40.85 · 56.46 · 56.70 → **среднее 45.582,
максимум 58.07 — 0.53 от однопроходного читателя на тех же пяти тредах.**

**Рунг S′ — STOP**, перебор 276.9 с = 3.74 с/звонок. Три чтения рядом: живой нож 64.63 (а не
зарегистрированные 48.6486), плечо max в одиночку прошло бы, строково-взвешенное сказало бы GO при
27.485 с/звонок. STOP верен по закону и, по уликам, про ВЫБОРКУ
[[a-smoke-drawn-from-the-exam-is-not-a-rate-sample]].

### D2

Рунг 7 на STOP-плече **GO**: 5 из 5, 0 sha, 0 переразметок, **1 нечитаемый при бюджете ровно 1** —
по доле прохода-1 было бы 0 и чистый транспорт читался бы RED. Отказ — F2, `"note": ""`, третье
отчётное поле с правом убить тред и первое выстрелившее
[[a-report-only-field-can-refuse-the-whole-row]].

**Шесть из семи золотых сигналов найдены**, F1 взял все три (v5b — 0 из 3). Все три бара UNSCORED
с причиной. DROP 0 из 28; `subject_doubt` 4 из 28, все на `категория_личное`, одна — ячейка
mention-vs-about, которую v2 не сдвинул.

**Оператору возвращается:** измеренная ставка и решение по 74 юнитам — кап и ставка вместе,
$0.41–$1.03 по измеренному. Плечо выбирает оператор.

- 10:20: session ended (auto)
- 10:28: session ended (auto)
- 10:34: session ended (auto)
- 10:34: session ended (auto)

## Checkpoint 11:05

Закрытие `pass2-signals` и то, что всплыло уже после вердикта.

**Три уточнения в отчёт, которых иначе потребовала бы приёмка.** (1) `parse_refusals` и
`unreadable_replies` в этом прогоне оба равны 1 — но ТОЛЬКО потому, что переразметок ноль; бюджет
висит на втором, и совпадение чисел — ровно то, что процитируют неверно в следующий раз.
(2) `@VARUS_channel:10613#21601` стоит одновременно в скоркарде и в таблице сомнений, и это
непротиворечиво: сигнал F1b прочитан с **21599**, а сомнение — про происхождение **21601**, ответа
администрации сети со списком SKU. Один ответ, два чтения. (3) Ран-рекорд стартован ЧИСТЫМ на
create: три гейта `pre-create-check` эпохи D0 произведены кодом, который ревью потом заменило, — файл
короче r2-шного по замыслу, а не по потере.

**Деньги: три чтения, и они разошлись сильнее обычного.** Часы гейта **$0.108122**. Дельта баланса
$0.1057 на удалении и **$0.1180** через ~25 минут — ВЫШЕ часов, потому что без биллинговых строк
дельта тянет за собой всегда-включённый том, который иначе исключается ПО ВИДУ. Walk по-прежнему
`UNAVAILABLE`. Шаг оставлен ОТКРЫТЫМ, третьим подряд, за `money-anchors`.

**Процессное, для следующего запуска ревью.** Скептики пяти линз читали дерево, которое я правил под
ними: их «3 подтверждено, 12 опровергнуто» означает «12 уже починено на HEAD», а не суждение о
находках. Каждую я прогнал руками ДО правки, и это записано в отчёте — но сам аппарат так строить
нельзя [[a-review-that-verifies-a-moving-tree]].

**Верификатор:** 3 518 passed, 2 skipped. Подов нет, оба листинга пусты, том — позитивный контроль.

- 11:06: session ended (auto)

## pass2-signals-r2 — 75 докуплены, 79 прочитаны, слой окна-1 собран

`docs/PROMPT-pass2-signals-r2.md` исполнен целиком: шаг 0.5 → D0′ на замороженном дереве → D1
платный → D2. Отчёт `docs/reports/pass2-signals-r2.md`, ADR
[[pass-2-in-the-readers-schema-and-strict-authority]] получил секцию исхода r2.

**Шаг 0.5** — аддендум к отчёту r1: 23 отклонения перетегированы на закрытый энум (здоровье
контракта 16, оплачено 7, канон 23 из 23); хедрум пака **144, а не 603** — секция D0 цитировала
ПРЕ-ревьюшный пак, сдвинул `4d60d33` (+459 символов промпта за две клаузы); «среднее 43.09 дало бы
GO» ложно на 0.29 с — безубыток **43.087**, и его 1.5× это и есть живой нож 64.63. Четвёртый случай
одного входа с двумя значениями на этой линии, и он оказался в предложении, объяснявшем третий.

**D0′ — четыре сиблинга, не правки.** `pass2.py` запинен паком r1, пак запинен запечатанной
записью, и собственный тест r1 пересобирает пак: один символ — и `make check` краснеет на артефактах
закрытой платной сессии. Правило контракта сформулировано для ГЕЙТА («если байты r1 не двигаются —
`--revision`, иначе сиблинг, скажи какой»), и оно же решает модуль, раннер и скорер.

**Потолок выведен, а не назван.** Контекста модели нет НИ В ОДНОЙ записи стека (грепал
`context_window`/`context_length`/`max_position`/`n_ctx`). Якорь — самый широкий промпт, который
эта serving-конфигурация ДОКАЗАННО обслужила: `@matusi_ukr:22272` у читателя v5b, 4 510
prompt-токенов, `finish_reason: stop`, под той же резервацией `output_tokens: 4000`. Через
худшую измеренную плотность прохода-2 (3.4523 симв./токен) → **15 569 ≥ 11 856**, STOP не срабатывает.

**H6 отказал собственной цифре контракта.** Он аргументирует MAX через «самый широкий тред смока —
10 строк»; пак говорит **ВОСЕМЬ** (`@mandziak:3703` и `@matusi_ukr:22272` в ничью), и ни одно чтение
пятёрки не даёт 10. Зарегистрировано по выведенному, и аргумент стал сильнее: экстраполяция, которой
MAX избегает, — **26/8 = 3.25×**, а не 2.6×.

**Ревью на ЗАМОРОЖЕННОМ дереве — и цифра наконец что-то значит.** D0′ закоммичен первым (`d71a93d`),
каждой линзе выдана sha, все пять её процитировали: **12 подано, 10 подтверждено, 1 опровергнуто**
(в r1 было 3 из 15 — та цифра мерила тайминг моих коммитов [[a-review-that-verifies-a-moving-tree]]).
Фатальный: `parse_pass2` писал `None` в поля, которые `prompts._reader` гарантирует непустыми
СТРОКАМИ, а два потребителя по ним `Counter`-сортируют — один в ЗАПЕЧАТАННОМ файле. Одно нечитаемое
report-only поле в любом из 79 тредов роняло D2 **после того, как прогон оплачен и рунг 7 сказал GO**
[[a-repair-must-satisfy-the-consumers-contract]].

**Второй проход скептиков по коммиту ФИКСОВ (`7843d50`): 8 из 8 закрыто, 19 новых дефектов ВНУТРИ
фиксов.** Перепись перезаписей отделена от переписи нечитаемых (иначе измерение Dv702 раздувалось
полем, которое ридер прочитал прекрасно); `on_carried_rows` — последняя перепись, ещё спрашивавшая
список пака; пятая причина RED рунга 7 не была зарегистрирована нигде; и в дереве лежала
неотслеживаемая `results/pass2_signals_r2_run.json` с ТЕСТОВЫМ подом `p1` без `deleted_at` — рунг 0
вернул бы KILL до первого легитимного create [[a-gate-command-is-a-write]].

**И одно, что не было ничьей находкой:** тест го-токена в r1 был ЧАСАМИ. `run_go_no_go` проецирует
от `datetime.now()`, и с фиксированным `created_at` его GO-половина перестала проходить в
**10:32:50Z сегодня** — покраснела без единого коммита. Изолировано стэшем всего r2 и прогоном на
закоммиченном дереве.

**D1.** Под `dusd807citw8d7`, RTX 4090 EU-RO-1 $0.74/ч, create `12:07:23Z` → delete `12:41:44Z` =
**2 061 с = $0.42365** из $2.50. Последний коммит D0′ — `12:02:00Z`, за пять минут двадцать две
секунды до create. ssh **23 с**, бут **140.6 с**, первый ответ через 156.6 с после собственного
launch. Сид доехал с совпавшим дайджестом (`888f65bb…`, 4 строки), раннер первой строкой написал
«4 carried rows verified against the pack's renderings». Рунг 4 в пике $1.8266 (нестартовавшее
плечо по 97), потом $0.73–0.85. Рунг 5 не сработал, пиковый idle 115 с из 600.

**Ставка: 23.760 с/тред** (медиана 18.003, макс **135.232** на `@klopotenkofood:6040`, 26 строк) —
**0.245 от заряда**. Тяжёлый хвост реален и смок его не содержал: 2.33× максимума смока.

**Перенос доказан прямо.** `@matusi_ukr:22303` — единственный тред, отвеченный обоими подами: ответ
**байт в байт**, 815 символов, тот же sha, 304 токена, 15.801 → 15.934 с = **1.008×**. n = 1 и самый
короткий из пяти — цитировать вместе с 200/200 прохода-1.

**Чего стоил Dv702:** парсер r1 по этому файлу отказывает **52 треда из 79 = 65.8 %**, все на
`per_comment.note`. Модальный исход, а не случайность.

**D2 — все три бара посчитаны и все три КРАСНЫЕ, разного рода.** Бар 1 **4 из 5** (6 из 7 сигналов;
F2a UNREACHABLE по построению, и F2 в этот раз ПРОЧИТАН и всё равно не берётся — красное про
власть). Бар 2 **3 из 4**, ровно как вычислено за $0. Бар 3 **2 сигнала против 0** — единственное
красное, произведённое слоем: `@VARUS_channel:10366` не выбросил ни одной из четырёх строк
`сеть_ритейлер` и прочёл `похвала`/`жалоба` про сам розыгрыш.

DROP **21 из 281 = 7.47 %**, `сеть_ритейлер` — **11.86 %** против 6.28 % у `категория_личное`.
`subject_doubt` 15 из 260, максимум у **`молочный_бренд` — 21.4 %**: кафе, канцелярский бренд и
Тантум Верде, все помечены проходом 1 как молочный бренд. Учёт бьётся точно: 260 оставлено,
21 выброшено, 0 в обоих списках, 0 ни в одном.

**Отклонения Dv704–Dv727**, 24 из 24 с тегами: здоровье контракта 12, оплачено 12. У r1 было 16/7 в
другую сторону — r1 правил свой контракт, r2 правил ИНСТРУМЕНТЫ.

**Dv727 — находка последнего коммита.** Семнадцать тестов покраснели на коммите, добавившем платные
улики, и ни один потому, что что-то из проверяемого сдвинулось: фикстуры копировали
`results/pass2_signals_r2_v1.jsonl` как «сид», а это четыре строки до пода и 79 после. Урок r1 — тест,
читающий ещё не существующий артефакт; это он же после того, как артефакт ВЫРОС. Теперь каждая
фикстура СТРОИТ сид, прогоняя `--seed` во временный каталог.

**Верификатор:** 3 608 passed, 2 skipped. `make preflight` на закрывающем дереве: 11 из 11 и 6 из 6
запиненных путей совпадают, ни один не несёт старый пин. Подов нет, оба листинга пусты, том —
позитивный контроль.
