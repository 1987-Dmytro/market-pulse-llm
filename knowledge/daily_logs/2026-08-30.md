---
type: daily_log
date: 2026-08-30
tags: [daily]
---

# 2026-08-30

## Summary

**Три периода.** Утром — чтение: сессия, что вела `harness-v2.1` 27.08, проснулась через трое суток и
прочитала ответ тимлида. Дальше — исполнение `retail-census-r2` целиком, плюс три расширения по
слову оператора. Коммитов за день: **15**, от `cd7df0e` до `f99e6a3`. Отклонения **Dv880–Dv901**.
`make check` прошёл путь **2 failed, 4101 passed** → **1 failed, 4129 passed**: шаг 0 закрыл
`test_think_zero_shot`, оставшийся красный — старый долг r3 `test_repair_phase4_ledger`.

**Что принял тимлид до начала работы:** `harness-v2.1` принят 28.08 со всеми тремя долгами; C1 r1
принят частично — категория B да, категория A нет, инструмент не тот для вопроса (Dv871). Стена
FloodWait снята 28.08.

**Что принял тимлид ПО ИТОГУ дня (прочитано в дереве в конце сессии, `docs/STATUS.md` не
закоммичен):** строка `C1r2+r3` — **✅ 30.08 принят**, ценз завершён. Новый рулинг **(ц) 30.08**
записал все четыре решения оператора этой сессии. И вынесено то, что я выносил на решение:
**оставшийся 81 кандидат НЕ сканируем.** Дальше — слово оператора по ревизии registry
(A1 цены · A2 голос · B), затем `PHASE-promo-pulse-1` целиком (C2–C5). Ретро-строка тимлида:
«30.08 — 1 итерация (r2+r3, две нулевые гипотезы закрыты честно: бренд-голос Полтавщини и
гео-дискавери)».

**Третий период — вечер и ночь: слайс `promo-pulse-1-s2s3`, принят тимлидом GREEN на `ee23a13`.**
SP-4 разрешён рулингом (a) и реализован; докачка легла в новый живой корень; **куплен единственный
платный шаг дня — смок vision-ноги**, и он снял главную неизвестную: ставка измерена. Коммитов в
этом периоде **13**, от `05cf96c` до `8ae33e4`; `make check` **4 195 → 4 205 passed**. Деньги:
$2.3908 → **$2.3202** при капе $0.35. Детали — в чекпоинте 22:30 ниже.

### Блок 1 — розница: 17 из 36 строк имеют канал

Хэндлы взяты **с собственных свойств сетей**, не поиском по имени. Понадобилось четыре инструмента,
и это измерено, а не предположено: позитивный контроль показал, что **curl по HTML находит 1 из 6**
хэндлов, которые r1 уже знал. Отсюда `curl` · `curl+bundle` (футер клиентского рендера лежит в его
же JS — так нашёлся `@foraINFO`) · `browser` (мимо Cloudflare) · web-discovery для мёртвых доменов.
Урок → [[a_new_instrument_needs_the_old_ones_answers]].

**АТБ найден:** `@atb_market_official` (41 858) через `atbmarket.com` → официальный Instagram (202K)
→ Telegram. Но канал **корпоративный**: цена 0.000, комменты закрыты. **Маркетопт** — сайта нет
вообще, канал `+Ejz6ubzm21IyMTQy` (42 378) взят с его Instagram; **вступили по слову оператора**,
это первое не-read-only действие ценза. **Novus и Ашан канала не имеют** — проверено дважды каждый.

**Рулинг оператора переопределил метрику:** «нам нужны только комментарии ценовых промо». Пересчёт
по сырому стору со сшивкой через `parent_msg_id`: **16 324 коммента · 4 718 под ценой в 678 тредах ·
71% — шум**. Треды под ценой живут в двух местах: `msuaaaa` 3 407 и `@VARUS_channel` 1 210.
**Varus — единственная сеть с молочкой под живыми комментами.** Для остальных 16 — только цены,
и у Маркетопта с Епіцентром цены **внутри листовок** (`media_share` 1.000, ноль `грн`), то есть это
работа vision 5c2, а не текстового матчера.

### Блок 2 — премиса про бренды в райцентрах НЕ подтвердилась, проверено тремя способами

Покрытие выросло: **61 чат, 21 райцентр из 24** после снятия языковой планки. Но:

1. **Прямое чтение.** 4 507 сообщений из четырёх самых живых чатов — **ноль реальных упоминаний
   молочных брендов**. Все 4 срабатывания watchlist ложные: «третій **президент** України» и трижды
   «**Ферма** клубники» из объявлений о работе (Dv898).
2. **Другой жанр запроса.** Оператор поймал дыру: `CHAT_TERMS` = ('чат','спільнота','оголошення',
   'барахолка') — только барахолки, «підслухано» в репо не встречалось. Свип по 24 центрам:
   «типове» **0 везде**, «підслухано» — один настоящий городской `@svitlo5s` (1 865) с **0.107
   комм./день и молочкой 0.000**. По Чутовому 0 и здесь (Dv900).
3. **Геопозиция.** `contacts.getLocated` — **0 чатов по Полтаве и Кременчугу**, и структурно он ищет
   только ГЕОЧАТЫ (админ явно привязал локацию), а «Полтава чат» — обычная супергруппа (Dv901).

Потолок посчитан до широкого скана: 61 чат = **≈78 молочных сообщений в месяц** на область, и это
КАТЕГОРИЯ, до бренда. Независимо, по 16 324 собранным комментам: watchlist назван 228 раз, из них
**198 — `varus-pl` внутри собственного канала Varus**, **Гармонія 0** — воспроизводит замер тимлида
из SPEC v2 §0 с другой стороны (Dv899).

## Decisions

- [[poltava-chats-carry-category-voice-not-brand-voice]] — блок 2 как источник брендового сигнала
  закрыт тремя независимыми проверками; решение о покупке оставшегося 81 кандидата вынесено тимлиду.
- **Рулинг оператора 30.08:** «комментарии» для блока 1 значат **только треды под ценовым промо**;
  всё остальное — шум. Метрика канала переопределена, `results/promo_comment_yield.json`.
- **Рулинг оператора 30.08:** языковая планка `ua >= 0.5` **снята**, язык становится колонкой.
  Категория B выросла с 47 чатов / 18 центров до **61 / 21**.
- **Рулинг оператора 30.08:** вступить в приватный канал Маркетопта. Ценз перестал быть read-only
  ровно на одну строку; вступление в `results/joins_5c1.jsonl`, обратимо `--leave`.
- [[harness-guard-v2-behaviour-not-spelling]] (тимлид, 28.08) — применён шагом 0: 16 отказов,
  10 принятых, голый `ruff format` среди отказанных.
- **Рулинг тимлида 30.08 (ц), по итогам этой сессии:** ценз принят; **81 оставшийся кандидат не
  сканируем**; дальше ревизия registry по слову оператора → `PHASE-promo-pulse-1` (C2–C5).
- [[one-live-raw-root-and-an-opt-in-union]] — **SP-4, рулинг (a)**: v1 стал архивом навсегда,
  `data/raw_r2/` — единственный живой корень, union опционален. Реализовано в `80a9d10`, контроль
  прогнан дважды.
- **Рулинг оператора 30.08: «Да, смок авторизован»** — исполнен вечером: ОДИН платный прогон,
  30 страниц, кап $0.35, потрачено $0.0706. Четыре рунга по `PROCESS.md`, теардаун доказан
  листингом с положительным контролем.
- **Рулинг тимлида по итогу слайса (`ee23a13`, ~21:00):** GREEN; находка `/code-review` по
  `collect_5c1 :: collectable` заведена в новый контракт `docs/PROMPT-c2-pagecount.md` (пункт 2);
  разметка dev-40 разблокирована; SP-1 задаётся оператору ОДИН раз — на численности страниц.

## Next

1. **`docs/PROMPT-c2-pagecount.md` ($0)** — контракт тимлида, открыт по итогу слайса: разрешить
   **численность страниц** из стора и метаданных. Это ровно то, на чём теперь стоит SP-1: ставка
   измерена, вилка $0.87…$10.02 держится только шириной 1 022…9 653.
2. **Пункт 2 того же контракта** — починить `collect_5c1 :: collectable`, который не читает
   `source.collect` (находка `/code-review`; сейчас его спасает только гард `RawStore.append`).
3. **Разметка dev-40 разблокирована** — `results/promo_threads_draw.json`, сессии по 10 тредов.
   50 позиций — после S4.
4. **Слово оператора по SP-1** задаётся ОДИН раз, на числе из п.1: (a) цикл-3, (b) сузить C2,
   (c) под-раннер позиций.
5. **Нужен рулинг по строке реестра с числовым id** `1925810730` — см. блокеры.

## Blockers

- **SP-1 больше не про ставку — про численность страниц.** Смок дал 7.872 с/стр. (n=30,
  бут внутри) и маргинал 2.623…3.369 с/стр. с бутом, оплаченным один раз. C2 у пола страниц
  стоит $0.87…$1.10 (влезает), у потолка $7.81…$10.02 (нет). Остаток **$2.3202**, том ест
  $0.2333/день.
- **Числовой id и инвайт-хэш нельзя адресовать этим коллектором.** `1925810730` не резолвится даже
  после вступления в родительский канал: Telethon не разрешает голый id, пока сущность не в
  диалогах сессии. Группа достижима ТОЛЬКО через `@ATB_FANatik` (`linked_chat_id`) — её 18 комментов
  пришли оттуда. `+Ejz6ubzm21IyMTQy` нерезолвим по той же причине. Строку реестра надо либо убрать,
  либо пометить «достижима через родителя» — **слово тимлида**.
- **Эндпоинт 5c2 и его шаблон удалены ещё 08.08**, выжил только том `mp-srv2`. Любой следующий
  платный проход по vision-ноге начинается со сборки шаблона и эндпоинта — это не «запустить»,
  а «собрать», и рунг 0 надо считать с бутом.
- **`MEMORY.md` за обоими потолками загрузчика.** Убирать строки — решение оператора; мерить
  `context-census.py::loaded_memory`, не `head -c` → [[the_wrong_unit_points_at_the_wrong_lever]].
- **`docs/PROCESS.md:35` называет удалённый `.sh`-гард** — файл тимлида, правит он (Dv886).
- **В дереве чужое, не тронуто:** `docs/reports/lora-c-run-r3.md`, `docs/STATUS.md`,
  `docs/PROCESS.md`, `docs/reviews/**`, `docs/PROMPT-c2-pagecount.md`.

## Checkpoint 14:53 — план `promo-pulse-1` написан, два блокера найдены замером

`/plan-phase promo-pulse-1` → **`docs/plans/promo-pulse-1.md`** (344 строки, коммиты `0ffc55e` →
`ef8f4a0`). Ничего не реализовано, ждём «go». План открывается блоком §1a «что требует ответа, а что
нет»: семь шагов ($0-леса — S0, S1a, S1b, S2, S3, S6, S8) стартуют без единого ответа.

**Блокер 1 — фаза не влезает в свои деньги.** Гард прочитан вживую (`runpod_guard.py`, read-only,
`results/` и `config/` чисты после): anchor $22.51, баланс $4.95, **REMAINING $2.4394**. Не $3.17 —
это `spend_cycle2.json :: sessions[-1]` от 27.08, а $3.41 из SPEC v2 §9 — это `sessions[-2]`; обе
устарели на капле тома. Смета фазы по капам $0.4 + $2.5 + $0.3 = **$3.20**, уже на $0.76 больше.
И остаток — это **часы**: `mp-srv2` ест $0.2333/день (`srv2c_bootlog.json :: price_now_read`,
$7.00/мес ровно), то есть $2.4394 уходят сами за 10.4 дня, а разметка тимлида сидит внутри окна.
§6.1 велит спрашивать, когда проекция превышает остаток — она уже превышает, поэтому SP-1 переехал
на ревизию плана. Три варианта оператору: (а) цикл-3, (б) сузить C2, (в) собрать под-раннер позиций
(под дешевле serverless в 2.383×, `srv2d_cost.json`).

**Блокер 2 — ревизия registry ломает сборку агрегатов за $0.** Прогнал живую функцию:
`build_aggregates.py :: segment_for` кидает `SystemExit` на канале с evidence-строками и без строки
в registry. Из 28 каналов с evidence она **отказала на 21** — включая `@atb_market_official`
(**106 из 145 позиций, 73%**, и вся листовочная нога), `@matusi_ukr` (2 717 комментов) и
`@mandziak` (1 026). SPEC v2 §3 говорит «выводится из **сбора**… данные остаются» — я сперва прочёл
как «удалить строку». r2 получает флаг `collect: false`, строки остаются. Смена механизма, не скоупа.

**Что ещё замерено и попало в план.** Ставка 5c2 — **10.408 с/строку** (`run_5c2_positions.json ::
timing.seconds_per_row`, 205 строк), а «1.7 с/стр.» из STATUS — это `go_no_go.page_marginal_seconds`
1.729, прогревочный маргинал двух звонков → [[projected_rate_versus_measured_rate]]. Из 678 «ценовых
тредов» **449 (66%) ловятся веткой `decimal`**, и 10 из 10 под сидом 42 — это ДАТЫ, не цены
(«з 14.08 по 20.08»); 229 несут реальный маркер валюты. Популяция S2 = вопрос оператору. Жеребьёвка
вырождается в две страты: 665 из 678 (98.1%) — msuaaaa (447) + VARUS (218). Схемы пяти таблиц
(`attribution/signal/evidence/digest/rollup`) **нет ни в репо, ни в истории git** — предложена в §5.7
под ревизию. `unknown-brand` в коде нет: состояние уже есть — `brand_id IS NULL` + `brand_raw`
(65 из 145). Две строки A1 не выражаются под `_HANDLE` (инвайт Маркетопта, группа ФАНАТИКа с
`username: null`) — `registry.py` не пинится ничем, регекс расширяется в union.

**Проверено на живом коде, а не по памяти:** `positions.py` пинится четырьмя запечатанными записями
и трогать его не нужно (все 16 полей уже есть); thinking OFF и batch 1 — структурные в `local_llm.py`;
`loop.py` уже несёт четыре вотермарка и три прохода, так что `make tick` — проводка, не машинерия.
Верификатор: **1 failed · 4 129 passed · 2 skipped**, единственный красный — предсуществующий
`test_the_silence_check_fires_on_the_LINE_ledger_too` (его чинит S0 как долг шага 0 из STATUS).


### `MEMORY.md` сжат по слову оператора

Было: 213 строк / 29 033 юнита — доезжали **186 строк, 25 указателей из 201 не грузились вовсе**.
Стало: **196 строк / 23 917 юнитов, доезжает всё**, запас 1 083 юнита. Boot tax 9.5K → **9.2K**.

Как: (1) хуки урезаны до мнемоник — не пересказ, а ключ; (2) 16 пар-близнецов сведены на общие
строки, **обе ссылки живы** (`- [A](a.md) · [B](b.md) — общий хук`) — семейства «пустого класса»
(4 в одну), ставок (3), баланса/чтения (3), движущегося дерева (3), цитат, счёта, форматтера,
capability-гейтов, report-only поля; (3) шапка ужата. **Не выселен никто:** 201 ссылка до, 201 после,
дубликатов нет, битых нет, и те же **51** файл без указателя, что и раньше — то есть множество
адресуемых файлов не изменилось.

Побочно найдено: два РАЗНЫХ файла делили заголовок «A capability gate is not a theme gate» (один про
сток GPU против того, где может жить том; второй про «источник МОЖНО собрать» против «НУЖНО ли») —
теперь они на одной строке и различимы. И `stop_collecting_is_not_delete_the_row` стоял в Reference,
хотя это `feedback` — переставлен.

Мерить только `context-census.py::loaded_memory` (он возвращает БАЙТЫ загруженного среза, не юниты —
25 243 в прошлом замере не были сравнением с потолком 25 000) → [[the_wrong_unit_points_at_the_wrong_lever]].
## Checkpoint 17:14 — «go» на $0-срез: план поправлен, S0/S1a закрыты, S1b/S6/S7/S8 в дереве

Ревью приняло план (`docs/reviews/2026-08-30-plan-promo-pulse-1.md`) и ответило на всё: SP-0 q1–q3,
SP-1 («Go на $0, деньги — на стопе S3»), SP-5 («принято с поправками», семь штук). Пять правок плана
внесены **одним коммитом** `9d1aee7`.

**Пересчёт A1 — 8 в реестре / 10 нет, и это СПИСОК, а не итог.** «7 из 17» в плане было арифметикой
списка, из которого ATB уже выпал. Правильно: 17 строк §3 + дискуссионная группа ФАНАТИКа = **18
входов**, 8 несут строку реестра. Новый §5.3a печатает все 18 по одной и три отождествления, на
которых держится восьмёрка: `@foraINFO` ≡ `@forainfo` (только регистр), `@kop1chat` ≢ `@kopiyochka1`,
Маркетопт `+Ejz…` ≢ `@marketopt_promo` → [[count_in_prose_is_not_the_enumeration]]. Заодно выметены
все «16» в S1a/S1b/S2/S4/§5.2a.

**S0 (`d42b513`, `c74c9ce`).** Файлы тимлида закоммичены по пути, включая само ревью — оно было
untracked, а срез на него ссылается. Долг `test_the_silence_check_fires_on_the_LINE_ledger_too`
закрыт: проверка утверждала про строки **линейного** леджера, а `silent_paid_runs` ходит по
**шаговым** (`results/spend_*.json`). `spend_cycle2.json` — живой файл, и он вырастил две строки,
за которыми шагового леджера нет и не будет: **lora-c-run r3** (26.08, под `otq63mmt7s2uf1`, KILL —
ПЛАТНЫЙ шаг, чей единственный леджер линейный: `spend_lora_c_r3.json` не писался никогда) и чтение
гарда при удалении тома 27.08 → [[a_sealed_reports_checker_reads_a_live_file]]. Claim сужен до
шаговых прогонов, две записи перечислены и утверждены в обе стороны — третья такая уронит тест, а не
расширит отговорку. Негативный контроль: с живым леджером `silent` = 0, с обнулённым — 51 прогон
на 43 балансах. **4 130 passed, 2 skipped** (было 4 129 + 1 красный).

**S1a (`58ff037`).** `_HANDLE` стал union: `@username` ∪ `+inviteHash` ∪ голый chat id. Шесть
отказов, каждый на один символ мимо своей ветки (`@four`, `+Ejz…MT` 15 симв., `19258` 5 цифр).
`Source.collect: bool = True` — строгий по типу, как `watch`. `registry.py` не пинится ничем
(проверено по `results/`, `docs/`, `config/`). K2: `sources: 66 — collected 66`.

**S1b — ревизия r2 отказывается ЧЕТЫРЬМЯ гардами, не одним.** §5.2b нашёл `segment_for`. Есть ещё три,
и каждый падает **за $0, на втором шаге**: `window_summary_5c2.registry_through_the_seal`
(байтовое равенство с печатью — через него к реестру ходят **ДЕСЯТЬ** продюсеров),
`build_dashboard.through_the_provenance` (экран не собирается) и `run_5c2.preflight`
(«pinned inputs have MOVED»). У последнего **в собственном docstring уже лежал прецедент** для
`docs/SPEC.md`: «сырая проверка идентичности живёт ровно до одной поправки». Плюс
`registry_before_the_latin_aliases` перестал доставать `PRE_13B` — а на нём висят 6 тест-модулей и
4 продюсера, пересчитывающих запечатанные бары.

Механизм: цепочка ревизий в `market_pulse.registry` — `registry_before_r2` снимает помеченные строки
и блок, `registry_revision_reaching(pin, path)` отвечает на единственный вопрос всех потребителей.
Три ревизии достижимы и прибиты к шам: live `83fee82d…`, **r1 `d4e3b237…` (7 записей)**,
**pre-(13)(b) `920c7f20…` (8 записей)**. Никто не перепиннен. Гарды переведены на неё; сдвиг байтов
`window_summary_5c2.py` заявлен через собственную идиому репо — `SEALED_AT_R2 = 58ff037`,
`MOVED_BY_R2` — и 22 «rebuild byte-identical» теста закрыты **одной** условной поблажкой в
`moved_pins.assert_only_the_prompts_pin_moved` плюс `with_r2_put_back` там, где сравниваются байты.

**Найдено при этом:** три `collect: false` сперва легли ПОСЛЕ комментария-разделителя, то есть вне
своей записи — ровно та форма, ради которой в репо живёт
`test_a_field_the_block_does_not_have_yet_is_inserted_inside_it` (он и отказал). Вставка
переписана на «после последней строки-поля блока», обе развёртки снова точны.

**r2 на диске:** 66 → **74** источника, корзины `A 18 · B 17 · PAUSED 39`, 39 строк с
`collect: false` + `paused_at` + `paused_by`. `@ATB_FANatik` + группа `1925810730` и `@kop1chat` —
**вторые каналы своих строк**, из-за чего §3 и сходится: 17 строк / 18 входов. `source_type` взят из
`retail_chains.json :: is_aggregator`, `audience` — как у всех строк A, имена — из census.

**Расхождение, которое НЕ моё:** выводимая корзина A после r2 = **18 строк против 17 у §3**. Лишний —
`marketopt_promo` (публичная Толока): §3 назвал Маркетопт *private* и не сказал снимать публичный.
Оба остаются в сборе; вопрос тимлиду.

**S6/S7/S8 в дереве, 47 своих тестов зелёные.** Шесть таблиц (`attribution/signal/evidence/digest/
unsure/rollup`), `window_id` **ни в одной идентичности**, кроме недели `rollup` — проверено обеими
сторонами через `PRAGMA`, и «две жалобы в одном треде о разных субъектах» проверена самой SQLite.
S7: `draw_promo_threads.py` — популяция **678 = 229 + 449** (непересекающиеся, исчерпывающие),
сид **на страту** (`.claude/rules/registrations-and-draws.md`), тест меряет **ранги**, не id.
Правило очереди SPEC 3.19 замерено: **2 808 бессловесных из 4 718**, и **190 тредов из 678 не несут
ни одного текстового коммента** — размечать нечего, остаётся **488** (182 currency / 306
decimal-only), обе страты берут свои 40 с запасом. S8: новый модуль промпта (кодбук рендерится
ВНУТРЬ, `extractor_version` = ша рендера, не файла), четыре хука (отказ — **считаемая строка**),
два грейдера на синтетическом голде.

**S3 сухим прогоном — вилка SP-1 уже видна.** Ценз по стору ДО докачки: **609 медиа-постов →
1 050…5 901 страниц** (границы измерены: страниц в сторе нет, они в манифестах на 367 постов, и
самый большой альбом там — 10). Гард перечитан живьём: **REMAINING $2.4200**. Проекция:
warm-up 1.729 → $0.56…3.13 · fitted middle 4.2421 (ИНТЕРПОЛЯЦИЯ, `measured: false`) → $1.37…7.68 ·
**realised 10.408 → $3.35…18.84**. То есть на реальной ставке одна только страничная нога C2 не
влезает **ни одним концом**. Строки `vision_seconds_per_page` в `measurements.jsonl` по-прежнему нет,
и K4 её отсутствие **объявляет**, а не падает на нём.

## Checkpoint 18:32 — $0-срез закоммичен, S2 остановлен на SP-4: гард нашёлся ПОСЛЕ того, как я его обошёл

**Четыре красных из 22 остались после центральной починки, и все четыре — одна семья.** Убитый
верификатор дал только позиции точек; имена восстановлены из `--collect-only` по индексу
(9-я строка ×72 + 14 и 22, 25-я + 11 и 32) — приём стоит запомнить, он дешевле, чем ждать прогон.

1. **`src/market_pulse/aggregates.py` сдвинулся от S6 и не был ничем заявлен.** Он — ПРОДЮСЕР
   `results/dashboard_data_w1.json`, единственная запись в репо, которая его пинит, и пинит один
   раз. Семантический дифф доказал, что цифры НЕ поехали: свежая сборка отличается от
   закоммиченной **ровно четырьмя** provenance-шами и больше ничем. Заявлено как
   `MOVED_BY_THE_PROMO_TABLES` — **своя группа** на печати r2 (`58ff037` — последнее дерево с тем
   модулем), потому что расширить `MOVED_BY_R2` до трёх значило бы сделать её докстринг ложным.
   Свидетель `PROMO_TABLES`: 0 в печати, 1 на диске.
2. **Два лейбл-пака ждали НАБРАННЫЙ руками сет** `{producer.borrowed…prompts.py}`, а оба ещё
   заимствуют `window_summary_5c2.py`. Вынес `moved_pins.r2_paths_in()` — вторая половина ожидания
   теперь ВЫВОДИТСЯ из пересборки и опустеет сама, когда реестр вернут.
3. **Две мои же обёртки `with_r2_put_back(..., at_least=0)` удалены.** Замерил: ни один из трёх
   рендеров не несёт ни одной сдвинувшейся ши — своп ничего не находил и никого не оправдывал.
   Голое побайтовое сравнение и короче, и строже.

Попутный факт для отчёта: **24 закоммиченные записи цитируют запечатанную шу `window_summary_5c2.py`**
— вот она, цена одного байт-равенства → [[a_byte_equality_guard_has_a_caller_count]].

**Верификатор 4 190, срез закоммичен по пути:** `952faeb` (S1b+S6 — **одним** коммитом, Dv1: их
заявки живут в одних и тех же трёх хелперах, любой порядок даёт красный чекаут, а `git add -p` тут
недоступен), `765552b` (S7), `f914579` (S8), `e89a4a5` (инструменты S2/S3).

**S2 не запустился — и хорошо.** Два дефекта в моём же `scripts/collect_r2.py`, оба найдены
**запуском**, не чтением; скрипт уехал в `e89a4a5` вообще без тестов.

- `make_provenance(None, …)` вынесен над обоими циклами записи. Provenance — ПОСОСТОЧНАЯ
  (`source_type`, `comments_enabled` — свойства источника), все остальные вызыватели строят её
  внутри цикла. Умер на первой строке → [[the_entry_points_preamble_is_untested_code]].
- **Нет гарда на пиненные файлы.** `results/raw_v1_baseline.sha256` пинит шесть файлов стора, все
  шесть сходятся сегодня, и **четыре** канала A1 ложатся на них: `@atb_market_official`,
  `@silposilpo`, `@VARUS_channel`, `@msuaaaa`. Это ровно те инкумбенты, которые S2 велено докачать
  — и ровно поэтому `collect_5c1` отказывает им на уровне канала, и ровно поэтому сиблинг вообще
  написан. **Отказ с ним не приехал.** `data/` в gitignore: дозапись невидима для `git status` в
  обе стороны и неоткатываема.

Замер, которым SP-4 оценивается: за пиненными файлами **92.4 % постов и 98.1 % комментов**
(6 057 / 11 338 против 497 / 223 у шестнадцати свободных). У свободных всего **37 тредов с
комментами**, значит вне заморозки может лежать максимум 37 из 678 ценовых тредов.

Починено в `b11109a`: `refuse_pinned` **импортирует** `collect_5c1.protected()` (вторая копия
списка была бы копией, держащей писателя), отказывает **до** `build_client`, пять тестов в обе
стороны (`@VARUS_channel` отказан, `@znishkom` — нет). Негативный контроль замерен: все пять падают
на байтах `e89a4a5`. **4 195 passed.** Урок в память →
[[a_fork_written_to_pass_a_guard_drops_it]].

**K7 НЕ запущен намеренно** (Dv7): его популяция — 678 ценовых тредов, живущих почти целиком в двух
пиненных комментных сторах; вырастет она или нет — это и есть предмет SP-4. Заморозить выборку из
корпуса, который вот-вот изменится, значило бы [[widening_a_sample_must_not_resample_it]] задом
наперёд. K10 не запускается по другой причине: **в Makefile нет цели `tick`** (есть `check`,
`check-stamped`, `fmt`, `preflight`, `baselines`).

K1 после r2: **32 из 38 пиненных путей сходятся**. Из шести расхождений два — r2 (реестр
`9718aa52…`, `window_summary_5c2.py` `2de23c07…`, оба достаются через цепочку ревизий и не
перепиннены), четыре предшествуют фазе: `git log 58ff037..HEAD` не трогает ни один из них (0
коммитов). K2: `sources: 74 — collected 35, not collected 39`.

Отчёт: `docs/reports/promo-pulse-1-scaffold.md` (`76014ba`). Ценз и проекция названы в нём **сухим
прогоном** по стору ДО докачки, лежат в scratchpad, а не в `results/` — §5.1 фиксирует якорь окна
ПОСЛЕ S2, поэтому `results/promo_census_c2.json` не существует, и отчёт говорит это, а не показывает
файл, похожий на ответ.

Предыдущий `/save` дозакрыт: hot.md переписан (40 строк ровно, оба грепаемых литерала целы), кэш
обновлён, `check-wikilinks` — **ни одной битой**.

## Checkpoint 22:30 — слайс `promo-pulse-1-s2s3`: SP-4 разрешён, смок измерил ставку, стоп снят

Принят тимлидом **GREEN на `ee23a13`** (его чтения: `make check` 4 205 · `shasum -c` 6/6 ·
`serverless list` и `pod list -a` пусты · гард $2.3202 · дро пересчитано из файла). Дальше — фаза
`--join`/`--comments`, догнанная после приёмки, и аддендум к отчёту.

**SP-4 → [[one-live-raw-root-and-an-opt-in-union]].** Девять потребителей посчитаны с колонкой
«какой корень». Дро — единственный, кто обязан читать ТОЛЬКО v1: его популяция заморожена, и union
там сдвинул бы 678/488 молча. Отказ переехал из списка каналов коллектора в `RawStore.append`.

**Реестр r2, три правки одним коммитом** (`134eaba`): `@znishkom` вне категории, 17 полтавских
строк отложены в фазу B, `@marketopt_promo` остался. Проверено НА КОПИИ до правки файла: r1-ша
`d4e3b2373c43…` не двигается, потому что каждая добавленная строка несёт маркер `# (r2)`, который
снимает `registry_before_r2`. 24 запечатанные записи достают свой пин как достали.

**S2 — докачка в живой корень.** +1 340 постов (17 из 19 каналов; шесть были пустыми, `@kop1chat`
576, `@rrozetka` 170), свежесть 2026-08-08 → **2026-08-30**. Потом 7 вступлений с пейсингом 900 с
и **+1 190 комментов** по шести каналам. `shasum -c raw_v1_baseline` — 6/6 OK до, между и после.

**Смок — единственный платный шаг дня, и его пришлось СОБРАТЬ, а не запустить.** Эндпоинт и шаблон
5c2 удалены 08.08, выжил том. `run_5c2.py` привязан к запечатанной преригистрации 5c2 — гонять его
значит перекупать замороженный экзамен, поэтому написан `scripts/smoke_vision_c2.py`: транспорт
импортирован, 30 страниц из ДРУГИХ манифестов, 159 страниц `post_media_5c1.json` исключены по имени
→ [[a_smoke_drawn_from_the_exam_is_not_a_rate_sample]]. Рунг 0 ($0.2442 ≤ кап $0.35) закоммичен ДО
создания. `info` отдал POSITIONS на пиненной ревизии до единой страницы; 30 страниц → 30 ответов;
теардаун сразу, доказан листингом с положительным контролем (том и чужой шаблон на месте).

**Ставка и то, что она чуть не сказала неправильно.** Ледж��р получил `vision_seconds_per_page`
**7.872 с** (n=30) — и это дало $2.4674 против остатка $2.3299, то есть «не влезает». Но бут сидел
ВНУТРИ ставки, а платный проход платит его один раз: маргинал ограничен **2.623…3.369 с/стр.**
из полей самой записи, и C2 у пола стоит **$0.87…$1.10 — влезает**. Ставка ошибалась в 2.5× и
ответ менял знак → [[a-boot-inclusive-rate-over-a-small-n]].

**Два теста подвинуты рулингом, оба не ослаблены:** счёт 39 → 57 разложен по трём `paused_by`
(39 + 17 + 1), гард ретаргета гоняется с корнем в архиве и получил третье направление.
**`/code-review` (свежий сабагент) на S1b+S6:** FINDINGS 1 — `collect_5c1 :: collectable` не читает
`source.collect`; смягчение проверено (его корень — архив, `RawStore.append` отказывает), починка
заведена тимлидом в `PROMPT-c2-pagecount` п.2.

**Отклонения Dv8–Dv12.** Артефакты: `docs/reports/promo-pulse-1-s2s3.md` (30 строк, с аддендумом),
`results/{smoke_vision_c2,prereg_smoke_vision_c2,promo_census_c2,promo_projection_c2,promo_threads_draw,collect_r2,joins_r2}`.
Одна ошибка, пойманная до того, как её прочитал оператор: STOP-файл обосновывал остановку через
`runpodctl pod list -a`, который перечисляет ПОДЫ и про серверлес-эндпоинты не говорит ничего;
правильная команда `serverless list` существует. Исправлено в `2830120` до приёмки
→ [[the-wrong-listing-answers-a-neighbouring-question]].

## Sessions
- 07:35: session ended (auto)
- 07:43: session ended (auto)
- 09:53: session ended (auto)
- 10:43: session ended (auto)
- 11:06: session ended (auto)
- 11:55: session ended (auto)
- 12:28: session ended (auto)
- 13:08: session ended (auto)
- 13:45: session ended (auto)
- 13:55: session ended (auto)
- 14:22: session ended (auto)
- 14:23: session ended (auto)
- 14:27: session ended (auto)
- 14:29: session ended (auto)
- 14:33: session ended (auto)
- 14:37: session ended (auto)
- 14:47: session ended (auto)
- 14:56: session ended (auto)
- 15:04: session ended (auto)
- 16:48: session ended (auto)
- 16:54: session ended (auto)
- 16:54: session ended (auto)
- 17:01: session ended (auto)
- 17:11: session ended (auto)
- 17:12: session ended (auto)
- 17:18: session ended (auto)
- 17:31: session ended (auto)
- 17:36: session ended (auto)
- 17:49: session ended (auto)
- 17:54: session ended (auto)
- 18:10: session ended (auto)
- 18:25: session ended (auto)
- 18:27: session ended (auto)
- 18:35: session ended (auto)
- 19:31: session ended (auto)
- 19:31: session ended (auto)
- 20:14: session ended (auto)
- 20:18: session ended (auto)
- 20:18: session ended (auto)
- 20:31: session ended (auto)
- 20:46: session ended (auto)
- 20:58: session ended (auto)
- 21:01: session ended (auto)
- 21:16: session ended (auto)
- 21:18: session ended (auto)
- 21:31: session ended (auto)
- 21:46: session ended (auto)
- 21:48: session ended (auto)
- 21:48: session ended (auto)
- 21:49: session ended (auto)
- 21:49: session ended (auto)
- 21:50: session ended (auto)
- 21:51: session ended (auto)
- 21:52: session ended (auto)
- 21:53: session ended (auto)
- 21:54: session ended (auto)
- 21:54: session ended (auto)
- 21:55: session ended (auto)
- 21:55: session ended (auto)
- 21:56: session ended (auto)
- 21:56: session ended (auto)
- 21:56: session ended (auto)
- 21:56: session ended (auto)
- 21:58: session ended (auto)
- 22:18: session ended (auto)
