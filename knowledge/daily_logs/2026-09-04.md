---
type: daily_log
date: 2026-09-04
tags: [daily]
---

# 2026-09-04

## Summary

Семь сессий исполнителя (10–16), из них **одна платная — $0.223850**. День закрыл вилку
`admin_anon_ids`, построил A(1) и «A-rest» целиком, вскрыл пятидневный дефект резолва инвайта,
купил итерацию 2 — и вечером починил транспортный дефект, из-за которого она вышла красной.
Две остановки, **обе предвидимые файлом фазы**; PHASE вырос до v5, чтобы их классы больше не стоили
остановки.

**Сессия 10 — стоп ДО первой правки.** A(1) по букве рулинга (f) клал `admin_anon_ids` в
`config/registry.yaml` — файл, который пинят **21 запечатанный рекорд** в четырёх семействах sha
(r1 `d4e3b237…` 7 · pre-(13)(b) `920c7f20…` 8 · signed-screen `c82d0cff…` 5 · ЖИВЫЕ байты
`eff8ba5b…` 1). Обе строки рулинга — r1-эпохи, ВНЕ скобки r2
([[the-law-grows-inside-marked-blocks]]), значит любая добавленная строка попадает в КАЖДУЮ
реконструкцию. Померено запуском: `run_promo_c2.preflight` проходит на HEAD и отказывает после
ОДНОЙ строки. Четвёртая ревизия (`R3_MARK`) восстанавливает все четыре семейства ТОЧНО — и всё
равно недостаточно: preflight сравнивает сырые байты и ревизий не обходит.
→ [[the-sealed-file-does-not-grow-it-gets-a-sibling]]

**Сессия 11 — A(1) построен** (`c7a63a7`). Рулинг (g) выбрал опцию (b): сиблинг-файл
`config/channel_admins.yaml`, ни один пин не двигается. Единственный читатель —
`promo_prompts.admin_ids`; метка `[admin] ` по ID, поэтому 28 VARUS-комментов с `sender_anon_id:
null` не совпадают ни с чем. Числа из файлов: бестекстовых сброшено **68** = `n_wordless`
жеребьёвки; меток **23** = 82 − 59. Чекер пода принимает 56/56. `make check` 4 313 / 2 skipped.

**Сессия 12 — «A-rest» остановлен ДО первой правки, два измерения за $0.** (1) У A(2) была ложная
предпосылка: алиасов у `Source` НЕТ, «Варус» встречается в реестре **0 раз**. Свёртка по
`id`/`name`/хэндлам = **+0 строк**, новый носитель = **+1**.
→ [[measure_what_the_normalisation_would_fix]] (2) У пятой категории примеров не было ни одного
чистого: все четыре из §6 — тексты эталонных тредов. **Побочная находка:** отгруженный
`promo_prompts.CODEBOOK` дословно цитировал **11 эталонных комментов dev-40** — итерация 1 мерилась
инструментом, видевшим 7.9 % своего знаменателя; холдаут ЧИСТ.
→ [[the_instruments_examples_came_from_the_exam]], ср. [[the-codebook-that-quoted-the-answer-key]]

**Сессия 13 — «A-rest» СДЕЛАН целиком** (`c2234a4` код+регистрация, `d64faf7` пак, `3f75106`
слой). `config/chain_aliases.yaml` из ИМЁН сетей: **+1 строка, и она названа** —
`@VARUS_channel:6216:8865` («VARUS» против «Варус»), 0 сломанных, 114/140 → **115/140 = 0.8214**.
Утечка закрыта — 11 цитат ушли из закона, **v1.2** `a587e0d6…`; `--leak-check` берёт литералы ИЗ
ОТГРУЖЕННОГО модуля: 27 + 8 против **328** комментов → **CLEAN**. Два примера тимлида заменены,
потому что не проходят его же чек: «Ну да» — буквально холдаут `@VARUS_channel:8647:15419`.
Незаказанные находки: `promo_dev40_prep.json` протух с A(1)
([[a_fixture_on_disk_pins_yesterdays_schema]]), и **кап шага упал $2.50 → $2.4469** за сутки
всегда-включённого тома ([[a_balance_delta_is_not_a_per_leg_cost]]). Верификатор поймал то, чего не
поймала предпроверка: `test_the_layer_reads_nothing_and_parses_nothing` парсит AST и требует, чтобы
`aggregates.py` НИКОГДА не звал `open`.
→ [[a_module_can_have_a_test_about_what_it_may_not_do]]

**Сессия 14 — «sources-r3: Маркетопт», $0-нога** (`449cab1`). Первый глагол пункта оказался уже
выполненным: `results/joins_5c1.jsonl:33` пишет `already_member` от 30.08, и живой
`CheckChatInviteRequest` (чтение) вернул `ChatInviteAlready`, чат `1255265634`. Пять дней «вступил,
но не собрано» были одним проглоченным дефектом: Telethon читает `+`-форму только после `t.me/`, и
ValueError уходил в `except Exception`. Собрано **+33 поста, 04.08 → 04.09, все с медиа**, 0
комментов. → [[the-invite-hash-is-resolved-at-the-call-site]]
**Цена этой $0-записи:** максимум корпуса стал Маркетопт **2026-09-04**, а
`promo_census_c2.anchor_of()` считает `max(дата)+1` ИЗ СТОРА — якорь 2026-08-31 пересчитался бы в
2026-09-05 и перепиннил все 19 строк. → [[a_collection_moves_every_anchor_computed_from_the_store]]

**Сессия 15 — B, платная итерация 2 ($0.223850). Красный бар и ОТКРЫТЫЙ СТОП.** Под
`ej1dxfcpe0etff`, RTX 4090 EU-RO-1 по зарегистрированным $0.74/ч, 14:42:45Z → 15:00:54Z: **1089 с**,
56/56 юнитов, каждый ответ balanced, ОДИН буст (167.0 с с тёплого тома) через GO. Смок намерил
**38.816 с/тред** против 81.212 у итерации 1, `--project` дал GO. Три числа объявлены ДО смока и все
выдержали: кап B **$1.8826** (правило самой регистрации строже замороженного $1.9215), KILL против
него, а не против всего капа шага, и `--terminate-after` 90 мин как наименьшее из трёх.
**K8: сигналы 0.8854 ✅ (бар 0.75) · субъект 0.7500 ❌ (бар 0.80)** против 0.8214 у итерации 1 по
ТОМУ ЖЕ голду. Причина — транспорт, и это весь провал целиком: `@VARUS_channel:6216` 18 → 0 строк.
→ [[the-fence-defeats-the-dispatch-that-fixes-the-array]]
**Моя ошибка дня:** гонял `make check` параллельно с платной сессией — красный ERROR в файле,
которого сессия не трогала. Пока верификатор идёт, дерево обязано быть неподвижным.

**Сессия 16 — стоп закрыт рулингом (m), починка написана и зелёная, $0** (`b220acc` файлы тимлида,
`f872a53` починка, `65ae76c`+`6aabc3c` PROGRESS). `make preflight` — **0 затронутых путей не пиннуты
ни одной записью**. Дискриминирующий греп ДО правки: ни один тест не утверждает старое поведение
`parse` на массиве, значит это не «тест пришлось бы ослабить». Починка ровно как написал (m)2:
`balanced_prefix` читает забор через `promo_prompts.unfence` ПЕРВЫМ и диспатчит по форме ВТОРЫМ,
возвращая по-прежнему префикс эмитированного текста с забором (это персистит `run` и это же `parse`
расфорит на Маке); `promo_prompts.fold` читает голый массив как строки, которыми он и является, а
элемент-не-объект по-прежнему считается «not an object», а не пустым классом. **ОДИН тест, в обе
стороны** (PHASE §4 v5) — и он же $0-дрилл §6.5: объект · массив × с забором · без забора, прогнан
через `close_arrays_too` + `stops_here`, ту проводку, которую применяет `StoppingCriteria`.
Суита поймала настоящую ловушку: `pod.main` ставит патч на `reader_v5` и не снимает, поэтому
«отгруженное правило» для контроля пришлось брать из reload.
→ [[guard_selftest_negative_control]] · [[identity_field_stops_covering_the_change]]
**Рулинг (n) принял s16 по диффу ($0)** и подтвердил чтение исполнителя: `extractor_version`
транспортную починку нести не может, поэтому переиздание пиннит ОБА файла — это и есть «sha раннера
среди пинов» из (m)5, а не новый пин. Следующая сессия — итерация 3 целиком.

## Decisions
- [[the-sealed-file-does-not-grow-it-gets-a-sibling]] — рулинг (g), опция (b).
- [[the-invite-hash-is-resolved-at-the-call-site]] — резолв чинится в точке вызова.
- [[the-fence-defeats-the-dispatch-that-fixes-the-array]] — механизм красного бара итерации 2;
  **закрыт рулингом (m)**: не дефект под старым номером, а новый инструмент = итерация 3.
- Рулинги тимлида за день: (g) сиблинг-файл · (h) A(1) принят · (i) «sources-r3» перед B · (j)
  `chain_aliases.yaml` из имён, 11 цитат уходят · (k) «A-rest» принят целиком · (l) сбор принят,
  B → c3, параметры вместо сиблингов, экран — все окна · **(m) итерация 2 стоит как измерена;
  починка = новый `extractor_version` = итерация 3, покупается в той же сессии; плато нет
  (счётчик 0 из 2); нога B закрыта двумя `[]`** · **(n) s16 принят по диффу; переиздание пиннит
  оба файла починки; следующая сессия — итерация 3 целиком**.
- **PHASE v5** (файл тимлида): §4 «asked for once» — пойманный дефект продукта получает ОДИН тест в
  обе стороны без авторизации; §6.5 «валидность платного чтения» — `parse_failures = 0`, и до первой
  покупки нового инструмента раннер и парсер дриллятся при $0 на всех формах ответа.
- **Новое правило дня (слово оператора):** каждая остановка = рулинг + разбор паттерна + правка
  правил. Разбор четырёх остановок дня — `docs/reviews/2026-09-04-stop-patterns.md`.

## Next
**Итерация 3 — ОДНОЙ сессией ((m)5), старт с переиздания регистрации:** `iteration: 3`, sha раннера
среди пинов, три денежных числа объявлены ДО смока, кап = остаток шага, как его печатает гард →
покупка → K8 → таблица ошибок с названным диффом ((c)3). Субъект ≥ 0.80 → НОТИС прережки холдаута
как открытый стоп, END; красный → таблица ошибок, END. Потом «c3» одной сессией по (l) 2–4.
Раннбук уже уведён с платных `…_iter1.*` — репоинтить `_iter2`-пути и юниты смока `7187/9006/6009`.
Параллельно за $0 и на критическом пути S1: разметка позиций тимлидом по 46 страницам.

## Blockers
**Открытого стопа нет.** Названо, не построено: **`extractor_version` = `sha256(rendered prompt)`
и идентичность итерации 3 нести не может** — переиздание обязано пиннуть `promo_prompts.py` РЯДОМ с
sha раннера, потому что половина починки лежит в каждом, а `check_law` не покрывает ни одного;
`pod.main` не снимает свой патч с `reader_v5`; `committed_registration()` не смотрит
`pinned_inputs`; юнит-теста на `resolvable()` с `+`-хэндлом ещё нет (разрешён (l)1); `project()`
меряет KILL против ВСЕГО капа шага. Лег B закрыта — 16 из 16 постов `[]` в обеих итерациях. Голд
покрывает 140 из 208 комментов, значит меньше строк = выше процент. Near-quote «Шикарно…» и
кодбук-документ ждут заморозки закона ((k)2). ⚠ Не гонять `make check` во время платной сессии.
⚠ Длинный прогон — только отцепленным (`os.setsid`). ⚠ K4 не перезапускать. ⛔ `1925810730`
неадресуем. ⛔ `aggregates.py` не смеет звать `open`.

**Деньги за день: $0.223850** (одна платная сессия из семи). Шаг `promo-dev-loop` ОТКРЫТ на
**$0.7493 из $2.4469** — гарду `--close` не звать, итерации 3–5 обязаны влезть. Цикл 3: **$4.5265 из
$7.00, REMAINING $2.4735** (дельта баланса, пессимистично; биллинг отстаёт на 30–40 мин). Кап c3 =
min($0.50, REMAINING − $0.30) = **$0.50**; холдаут $0.30 цел.

- 16:01: session ended (auto)
- 16:40: session ended (auto)
- 16:48: session ended (auto)
- 16:51: session ended (auto)
- 16:54: session ended (auto)
- 17:07: session ended (auto)
- 17:21: session ended (auto)
- 17:34: session ended (auto)
- 17:57: session ended (auto)
- 18:15: session ended (auto)
- 18:20: session ended (auto)
- 18:49: session ended (auto)
- 18:59: session ended (auto)

## Checkpoint 19:03 — s17: весь $0-слой итерации 3 готов, платная половина упёрлась в стоп

**Позиция «next» из `docs/plans/promo-pulse-1.PROGRESS.md` выполнена наполовину: $0 потрачено, под не создан.**
Стартовый ритуал: файлов тимлида модифицированных не было; хвост s16 в `knowledge/` закоммичен
отдельно (`83f90d9`), чтобы дерево было чистым до бандла.

**Переиздание регистрации (`8efb0c8`, `48537b9`, `1962ee5`).** `ITERATION = 3`. `pinned_inputs`
получил `scripts/promo_dev_pod_runner.py` `102fa524…` и `src/market_pulse/promo_prompts.py`
`dd260cb5…` — это и есть пин из (m)5, подтверждённый (n)2. Лег B закрыта по (m)4: `by_channel`
пустой (именно эту карту итерирует `build_pack` — пустота и есть то, что держит посты вне пода),
`posts` 0, 16 id остались НАЗВАННЫМИ под `not_bought`. `make preflight` по трём тронутым скриптам:
0 из 21 пути не запиннут ни одной записью.

**Доказательство, ради которого пины и понадобились:** все 40 leg-A `rendering_sha256` побайтово
равны паку итерации 2 (0 сдвигов), а sha модуля ушла `8d52ee79…` → `dd260cb5…`. `extractor_version`
= `sha256(rendered)` физически не может назвать итерацию 3 — см. [[the-fence-defeats-the-dispatch-that-fixes-the-array]].

**Три числа ДО смока ((m)5):** (1) итерация 3 имеет **$1.4146** = cap $2.1638 − потраченные шагом
$0.749250 (4 сегмента из `promo_dev_loop_run.json`); (2) `project()` читает KILL против ВСЕГО капа —
прочитан руками против $1.4146: dear $1.3100 = −7.4% (собственная строка рекорда даёт −39.5%);
(3) `--terminate-after` = min(runway 114.7 мин, зарегистрированные 90) = **90 мин = $1.1100**, и это
ниже dear-угла, то есть реально связывающая граница.

**СТОП: тест, который пришлось бы менять (PHASE §8 (j)).** `make check` на `1962ee5` — **1 failed,
4319 passed, 2 skipped**. `test_the_pack_pins_exactly_what_the_pod_re_derives` держит популяцию
итерации 2: `len(items) == 56` (стр. 302), 16 leg-B (стр. 303), «56» прозой в docstring (стр. 287).
Содержательное утверждение прошло — `check_requests` перевывел все 40 sha и не отказал ни одному;
расходятся только счётчики. Соседний `test_leg_b_pins_ids_and_not_a_count` (стр. 218) гоняет саму
функцию, а не рекорд, и зелён без правок. Тест НЕ тронут, под НЕ создан.

**Деньги:** за чекпойнт **$0.00**. Шаг `promo-dev-loop`: $0.749250 из переизданного капа **$2.1638**
(упал с $2.4469 — гард показал REMAINING **$2.4638**, это всегда-включённый том, не прогон), осталось
**$1.4146**. Цикл 3: $4.5362 из $7.00. Леджер-строка НЕ бралась.

Уроки дня (в native memory, не в vault): «a wrapper's exit code is not the command's» —
уведомление о фоновой задаче отдало exit 0, а `make` вышел с Error 1; «a shrunk population is a
test change» — рулинг законно уменьшил популяцию, и счётчик в тесте превратил это в стоп.
- 19:10: session ended (auto)
- 19:18: session ended (auto)
