---
type: daily_log
date: 2026-09-04
tags: [daily]
---

# 2026-09-04

## Summary

День закрыл вилку `admin_anon_ids` и построил A(1). Две сессии исполнителя, **обе за $0**, пода не
существовало ни минуты.

**Сессия 10 — стоп ДО первой правки.** Рулинг 03.09 (f) принят и закоммичен по путям (`7675946`):
кодбук **v1.1**, три строки эталона (8044 / 20083 / 407 → `post`), STATUS; плюс knowledge сессии 9
(`5fcecad`). A(1) по букве (f) клал `admin_anon_ids` в `config/registry.yaml` — файл, который пинят
**21 запечатанный рекорд** в четырёх семействах sha: r1 `d4e3b237…` 7 · pre-(13)(b) `920c7f20…` 8 ·
signed-screen `c82d0cff…` 5 · ЖИВЫЕ байты `eff8ba5b…` 1 (`prereg_promo_c2.json`). Обе строки,
которые называет рулинг (`:36`, `:45`), — r1-эпохи, ВНЕ скобки r2
([[the-law-grows-inside-marked-blocks]]), поэтому любая добавленная строка попадает в КАЖДУЮ
реконструкцию. Проверено запуском: `run_promo_c2.preflight` проходит на HEAD и отказывает после
ОДНОЙ строки; `build_aggregates.py:294` достаёт печать C2 через `tests/test_export_dashboard_data.py`
→ `make check` краснеет, а это собственное условие выхода сессии A. Четвёртая ревизия (`R3_MARK`)
восстанавливает все четыре семейства ТОЧНО — и всё равно недостаточно: тот preflight сравнивает
сырые байты и ревизий не обходит. Продолжение [[5c2-closed-the-sitting-and-the-shelf-life-redesign]].

**Рулинг 04.09 (g) — опция (b).** Тимлид выбрал сиблинг-файл: id живут вне реестра, ревизии r3 нет,
ни один пин не двигается. Плюс: бестекстовые комменты уходят из рендера; регистрация B
переиздаётся под sha v1.1 с новым `template_sha256`; чтение v1.1 — в отдельный файл; `--out` по
номеру итерации. Закоммичен по пути (`5d6aecf`).

**Сессия 11 — A(1) ПОСТРОЕН** (`c7a63a7`). `config/channel_admins.yaml` — новый файл; каждый
префикс из кодбука разрешается ровно в один `sender_anon_id` ровно в одном канале по всему
`data/raw/comments/` (VARUS `2fa2b73f…`, msuaaaa `58805a36…`). Единственный читатель —
`promo_prompts.admin_ids`. Метка `[admin] ` по ID, поэтому `null`-отправитель (28 комментов VARUS)
не совпадает ни с чем; бестекстовые комменты выброшены. Флаг едет **в пакете**: `comments` теперь
`[msg_id, text, sender_anon_id]`, под называет колонки через `zip(strict=False)` — старый пакет пар
всё ещё рендерится, ни одна фикстура не сдвинулась.

**Числа, все из файлов:** сброшено бестекстовых **68** = сумма `n_wordless` жеребьёвки **68**;
меток `[admin]` **23** = админских коммента с текстом **23** (= 82 − 59). Пакет пересобран: чекер
пода принимает **56/56**, при расхождении yaml — ОТКАЗ на `@VARUS_channel:2537` (непинованный
конфиг закрыт по-юнитным sha — показано, не заявлено). Против пакета, с которого рендерилась
итерация 1: leg B 16 sha не двинулись, leg A `(msg_id, text)` идентичны, сдвинулись 26 из 40
рендеров. `promo_dev40_prep.json` = `7971ed66…` — пин регистрации на месте.
**`make check` 4 313 passed, 2 skipped, exit 0** (681.84 s), счёт тот же, что на прошлом HEAD.

**Одна правка теста названа явно.** `tests/test_promo_hooks.py`: две ассерции `[103]` перенаправлены
на рулинг (g) п.2 (теперь требуют ОТСУТСТВИЯ бестекстового коммента) + добавлены обе стороны метки.
Ничего не удалено и не ослаблено; авторизация — (f) A(5) («stub tests of A's code» в этой же
сессии). Названо в PROGRESS, в сообщении коммита и оператору: один revert, если тимлид не согласен.

## Decisions
- [[the-sealed-file-does-not-grow-it-gets-a-sibling]] — рулинг (g), опция (b): почему ревизия r3
  восстанавливает четыре семейства и всё равно не проходит байтовый preflight.
- Ранее сегодня: [[the-fence-is-not-the-answer]] и
  [[terminate-after-is-the-cap-less-what-the-step-spent]] (артефакты сессии 9, закоммичены утром).

## Next
**Рулинг 04.09 (h) пришёл под конец дня: A(1) ПРИНЯТ по диффу, а A(2)–A(5) плюс переиздание
регистрации — ОДИН пункт «A-rest» в одной сессии** (у них общий чек, поэтому дробить нечего):
`promo_key` канонизация сетей → блок примеров в TEMPLATE (≤12 строк, ДОСЛОВНО из кодбука v1.1
§3–§6, каждая фраза со ссылкой на §, ни одного треда dev-40/холдаута, блок — в PROGRESS) → чтение
v1.1 за $0 в `errors_iter1_v1.1.json` → stub-тесты → регистрация переиздана под sha v1.1 с
`template_sha256`, в пакете `"iteration": 2` → `make check` зелёный. Дальше сразу B, **без
отдельного чтения рендера тимлидом** — слова кодбука его собственные ((h) п.3): раннбук с
`--out results/promo_dev40_iter2.jsonl`, файлы `*_iter2.*`, ≤ $1.9746.
Параллельно за $0: разметка позиций тимлидом ПО СТРАНИЦАМ (46 страниц) — критический путь S1.

## Blockers
Нет открытого стопа. Долги названы в `docs/plans/promo-pulse-1.PROGRESS.md`, не построены:
`committed_registration()` не переверяет `pinned_inputs`, а они уже сдвинулись под коммитом тимлида
(`302113b8…`/`6a74bc81…` против `2303ea43…`/`23610346…`); `check_law` сверяет только
`codebook_version`, поэтому рендерная правка оставляет sha закона прежним до `template_sha256`;
`--score --iteration 1` перезаписывает платный `errors_iter1.json`; `--out` в раннбуке —
специфичен для итерации 1. Первые два закрывает «A-rest».

Деньги: **$0 за день**. Шаг `promo-dev-loop` $0.5254 из $2.50 ($1.9746 осталось);
цикл 3 — $4.0631 из $7.00, REMAINING $2.9369; холдаут $0.30 не тронут.
- 10:11: session ended (auto)
