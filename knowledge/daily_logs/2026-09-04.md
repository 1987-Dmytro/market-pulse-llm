---
type: daily_log
date: 2026-09-04
tags: [daily]
---

# 2026-09-04

## Summary

День закрыл вилку `admin_anon_ids` и построил A(1). Две сессии исполнителя, **обе за $0**, пода не
существовало ни минуты.

**Сессия 10 — стоп ДО первой правки.** Рулинг 03.09 (f) принят и закоммичен по путям (`7675946`):
кодбук **v1.1**, три строки эталона (8044 / 20083 / 407 → `post`), STATUS; плюс knowledge сессии 9
(`5fcecad`). A(1) по букве (f) клал `admin_anon_ids` в `config/registry.yaml` — файл, который пинят
**21 запечатанный рекорд** в четырёх семействах sha: r1 `d4e3b237…` 7 · pre-(13)(b) `920c7f20…` 8 ·
signed-screen `c82d0cff…` 5 · ЖИВЫЕ байты `eff8ba5b…` 1 (`prereg_promo_c2.json`). Обе строки,
которые называет рулинг (`:36`, `:45`), — r1-эпохи, ВНЕ скобки r2
([[the-law-grows-inside-marked-blocks]]), поэтому любая добавленная строка попадает в КАЖДУЮ
реконструкцию. Проверено запуском: `run_promo_c2.preflight` проходит на HEAD и отказывает после
ОДНОЙ строки; `build_aggregates.py:294` достаёт печать C2 через `tests/test_export_dashboard_data.py`
→ `make check` краснеет, а это собственное условие выхода сессии A. Четвёртая ревизия (`R3_MARK`)
восстанавливает все четыре семейства ТОЧНО — и всё равно недостаточно: тот preflight сравнивает
сырые байты и ревизий не обходит. Продолжение [[5c2-closed-the-sitting-and-the-shelf-life-redesign]].

**Рулинг 04.09 (g) — опция (b).** Тимлид выбрал сиблинг-файл: id живут вне реестра, ревизии r3 нет,
ни один пин не двигается. Плюс: бестекстовые комменты уходят из рендера; регистрация B
переиздаётся под sha v1.1 с новым `template_sha256`; чтение v1.1 — в отдельный файл; `--out` по
номеру итерации. Закоммичен по пути (`5d6aecf`).

**Сессия 11 — A(1) ПОСТРОЕН** (`c7a63a7`). `config/channel_admins.yaml` — новый файл; каждый
префикс из кодбука разрешается ровно в один `sender_anon_id` ровно в одном канале по всему
`data/raw/comments/` (VARUS `2fa2b73f…`, msuaaaa `58805a36…`). Единственный читатель —
`promo_prompts.admin_ids`. Метка `[admin] ` по ID, поэтому `null`-отправитель (28 комментов VARUS)
не совпадает ни с чем; бестекстовые комменты выброшены. Флаг едет **в пакете**: `comments` теперь
`[msg_id, text, sender_anon_id]`, под называет колонки через `zip(strict=False)` — старый пакет пар
всё ещё рендерится, ни одна фикстура не сдвинулась.

**Числа, все из файлов:** сброшено бестекстовых **68** = сумма `n_wordless` жеребьёвки **68**;
меток `[admin]` **23** = админских коммента с текстом **23** (= 82 − 59). Пакет пересобран: чекер
пода принимает **56/56**, при расхождении yaml — ОТКАЗ на `@VARUS_channel:2537` (непинованный
конфиг закрыт по-юнитным sha — показано, не заявлено). Против пакета, с которого рендерилась
итерация 1: leg B 16 sha не двинулись, leg A `(msg_id, text)` идентичны, сдвинулись 26 из 40
рендеров. `promo_dev40_prep.json` = `7971ed66…` — пин регистрации на месте.
**`make check` 4 313 passed, 2 skipped, exit 0** (681.84 s), счёт тот же, что на прошлом HEAD.

**Одна правка теста названа явно.** `tests/test_promo_hooks.py`: две ассерции `[103]` перенаправлены
на рулинг (g) п.2 (теперь требуют ОТСУТСТВИЯ бестекстового коммента) + добавлены обе стороны метки.
Ничего не удалено и не ослаблено; авторизация — (f) A(5) («stub tests of A's code» в этой же
сессии). Названо в PROGRESS, в сообщении коммита и оператору: один revert, если тимлид не согласен.

## Decisions
- [[the-sealed-file-does-not-grow-it-gets-a-sibling]] — рулинг (g), опция (b): почему ревизия r3
  восстанавливает четыре семейства и всё равно не проходит байтовый preflight.
- Ранее сегодня: [[the-fence-is-not-the-answer]] и
  [[terminate-after-is-the-cap-less-what-the-step-spent]] (артефакты сессии 9, закоммичены утром).

## Next
**Рулинг 04.09 (h) пришёл под конец дня: A(1) ПРИНЯТ по диффу, а A(2)–A(5) плюс переиздание
регистрации — ОДИН пункт «A-rest» в одной сессии** (у них общий чек, поэтому дробить нечего):
`promo_key` канонизация сетей → блок примеров в TEMPLATE (≤12 строк, ДОСЛОВНО из кодбука v1.1
§3–§6, каждая фраза со ссылкой на §, ни одного треда dev-40/холдаута, блок — в PROGRESS) → чтение
v1.1 за $0 в `errors_iter1_v1.1.json` → stub-тесты → регистрация переиздана под sha v1.1 с
`template_sha256`, в пакете `"iteration": 2` → `make check` зелёный. Дальше сразу B, **без
отдельного чтения рендера тимлидом** — слова кодбука его собственные ((h) п.3): раннбук с
`--out results/promo_dev40_iter2.jsonl`, файлы `*_iter2.*`, ≤ $1.9746.
Параллельно за $0: разметка позиций тимлидом ПО СТРАНИЦАМ (46 страниц) — критический путь S1.

## Blockers
Нет открытого стопа. Долги названы в `docs/plans/promo-pulse-1.PROGRESS.md`, не построены:
`committed_registration()` не переверяет `pinned_inputs`, а они уже сдвинулись под коммитом тимлида
(`302113b8…`/`6a74bc81…` против `2303ea43…`/`23610346…`); `check_law` сверяет только
`codebook_version`, поэтому рендерная правка оставляет sha закона прежним до `template_sha256`;
`--score --iteration 1` перезаписывает платный `errors_iter1.json`; `--out` в раннбуке —
специфичен для итерации 1. Первые два закрывает «A-rest».

Деньги: **$0 за день**. Шаг `promo-dev-loop` $0.5254 из $2.50 ($1.9746 осталось);
цикл 3 — $4.0631 из $7.00, REMAINING $2.9369; холдаут $0.30 не тронут.
- 10:11: session ended (auto)
- 10:36: session ended (auto)

## Checkpoint 11:54 (сессия 12, $0)

Ритуал: файлы тимлида закоммичены по путям — рулинги **(h)** и **(i)** + переписанный `STATUS.md`
(`ec1b55a`); незакоммиченные артефакты с11 (`1590ab8`). Дерево чистое, пода не было.

**Пункт сессии — «A-rest». Остановлен ДО первой правки**, стоп в PROGRESS (`d002967`).
Два измерения за $0, оба в транскрипте:

1. **У A(2) ложная предпосылка.** Рулинг (f)(б) сводит имена сетей «через names/aliases реестра» —
   алиасов НЕТ: у `Source` девять полей и ни одного alias-поля (алиасы есть только у
   `WatchlistBrand.display_names`), «Варус» встречается в `config/registry.yaml` **0 раз**, а
   рулинг (g)1 запрещает двигать этот файл. Померил, а не предположил: из 5 промахов chain-vs-chain
   трое (`додаток`, `програма лояльності`, `сертифікати` — все в одном треде `@VARUS_channel:5186`)
   лечатся блоком примеров A(3), не свёрткой; `msuaaaa`→`Roshen` не лечится ничем. Ветви:
   свёртка по `id`/`name`/хэндлам = **+0 строк** (обе стороны уже схлопываются в `promo_key`:
   NFC+lower), новый носитель алиасов = **+1**, транслитерация = **+1**. Поле `aliases` в `Source`
   обнулило бы живой пин `eff8ba5b…` из `prereg_promo_c2.json`, и `make check` остался бы ЗЕЛЁНЫМ.
   → [[measure_what_the_normalisation_would_fix]]
2. **У пятой категории примеров нет чистого примера.** (h)3 требует примеры дословно из кодбука
   §3–§6 и «никогда из dev-40 или холдаута». Прогнал все примеры кодбука против текстов 80
   разыгранных тредов: 13 фраз — дословный текст тредов, 11 из них — строки эталона. Для четырёх
   категорий чистые примеры есть (`програма лояльності`; `в якому Ашані?`/`де купити?`/`до коли
   діє?`/`що в боксі?`; `напишіть у чат @varusua_bot`; `канал не о скидках`/`на фото неправильна
   ціна`), для «голая реплика → post» — **ни одного**: все четыре из §6 это 8044, 20083, 8049 и ещё
   одна.

**Побочная находка — долг, не постройка.** Уже отгруженный `promo_prompts.CODEBOOK` дословно
цитирует **11 эталонных комментов dev-40** (из 27 литералов ≥8 символов): «Розуміємо вас», «Фото вже
прибрали», «так працює система», «Працює 5%», «Або з 9 числа», «Торгівля повітрям»,
«Потужно-незламний», «ціна на фото вірна», «передали», «У Вінниці є ваш магазин?», «Нових знижок атб
ще не виклали?». Итерация 1 мерилась инструментом, видевшим **7.9 %** своего знаменателя.
**Все 11 — dev, холдаут ЧИСТЫЙ** — настоящая заявка цела. Кодбук — закон тимлида.
→ [[the_instruments_examples_came_from_the_exam]], ср. [[the-codebook-that-quoted-the-answer-key]]

**Сверка числа тимлида независимо:** перечтение итерации 1 под кодбуком v1.1 даёт ровно
**114/140 = 0.8143** subject, 26 промахов — то самое «0.814» из рулинга (f). В файл НЕ писал:
нужный out-path сам входит в остановленный пункт (`--score --iteration 1` перезаписал бы платный
`errors_iter1.json`).

**Вопросы тимлиду в стопе:** Q1 — A(2) это (a) новый носитель алиасов, (b) свёртка только по тому,
что реестр уже несёт, или (c) транслитерация? Q2 — «голая реплика → post» едет правилом без
примера, или тимлид даёт свой?

## Next (обновлено рулингом (i))
После «A-rest» и ПЕРЕД платной B встаёт **«sources-r3: Маркетопт»**: вступить в
`marketopt_private`, собрать посты, ценз, страницы листовок через ту же машинерию S4 под
собственной регистрацией `results/prereg_promo_c3.json` **кап $0.50**, позиции в ТРЕТЬЕ окно,
`make promo-screen` показывает строки Маркетопта рядом с w2. Только потом B (≤$1.9746).

## Blockers (обновлено)
**Открытый стоп есть** — два вопроса выше, `docs/plans/promo-pulse-1.PROGRESS.md`. Долги там же.
Деньги за день: **$0**. Шаг `promo-dev-loop` $0.5254 из $2.50; цикл 3 — $4.0631 из $7.00.

### Ответ пришёл в ту же сессию — рулинг 04.09 **(j)** (в рабочем дереве, НЕ закоммичен)
Тимлид ответил на оба вопроса и расширил «A-rest». Коротко:
1. **Q1 → ветка (a), но по принципу, а не по промахам.** `config/chain_aliases.yaml` (шаблон
   `channel_admins`, реестр не двигается): на каждый id сети — её именительные написания латиницей и
   кириллицей, выведенные из ИМЕНИ сети (`varus: [VARUS, Varus, Варус]`, `silpo: [Сільпо, Silpo]`,
   `atb: [АТБ, ATB]`, `mcdonalds: [McDonald’s, McDonald's, Макдональдс]`) — **никогда синоним,
   вытащенный из промаха**. `promo_key` сводит `chain`-строки через него, эталон и предсказание
   одинаково. +1 строка на dev сегодня, один id на сеть для витрины завтра.
2. **Утечка признана и закрывается СЕЙЧАС, за $0.** 11 дословных dev-комментов в
   `promo_prompts.CODEBOOK` заменяются парафразами той же формы; пары старое→новое — в PROGRESS;
   проверка: ни одна новая строка (и ни один пример A(3)) не является подстрокой ни одного коммента
   dev-40/холдаут-40. `codebook_version` двигается — **закон v1.2**; сам кодбук тимлид подтянет на
   следующей приёмке. Итерация 1 остаётся как измерена, с пометкой в STATUS; бар на dev заявляется
   только с итерации 2, холдаут не тронут.
3. **Q2 → примеры пятой категории даёт тимлид, синтетические:** «Ну да» · «Ок, зрозуміло» · «+1» ·
   «І що?» → `post`. (h)3 поправлен: примеры — синтетические парафразы в форме кодбука, со ссылкой
   на §, никогда текст из стора.
4. Порядок прежний: A-rest (с пп. 1–3) → «sources-r3: Маркетопт» → B.

**Состояние на момент чекпоинта:** `docs/STATUS.md` и файл рулингов изменены тимлидом в рабочем
дереве и НЕ закоммичены (коммиты — отдельно, по слову оператора; начало следующей сессии их
закоммитит по путям ритуалом). В `src/ tests/ scripts/ config/ results/` не двинуто ничего.
- 12:01: session ended (auto)

## Сессия 13 — «A-rest» СДЕЛАН целиком, за $0

Ритуал: `(j)` + `docs/STATUS.md` закоммичены по путям (`1ae3c40`). Дальше — ровно один пункт
PROGRESS: «A-rest» = A(2)–A(5) + переиздание регистрации + пп. 1–3 рулинга (j). Коммиты `c2234a4`
(код + регистрация) и `d64faf7` (пак).

**A(2) — свёртка написаний сети.** `config/chain_aliases.yaml`: написания из ИМЕНИ сети (id реестра
`official_retail` + `mcdonalds`, четыре примера (j)1 дословно). `aggregates.chain_key` — единственный
читатель; `subject_id` и грейдер сводят через него ТОЛЬКО `chain`-строки. Одно написание, заявленное
двумя id, — отказ, а не last-wins (`marketopt_promo` / `marketopt_private` — реальный случай).
Выигрыш: **ровно +1 строка, и она названа** — `@VARUS_channel:6216:8865`, эталон «VARUS», модель
«Варус». 0 сломанных. 114/140 → **115/140 = 0.8214**.

**(j)2 — утечка закрыта.** 11 дословных dev-комментов ушли из `promo_prompts.CODEBOOK`, закон v1.2.
Чек — `--leak-check`, и он берёт строки ИЗ ОТГРУЖЕННОГО модуля, а не из списка рядом: 27 литералов
«…» кодбука (≥8 знаков) + 8 литералов блока примеров (без порога) против **328** комментов
(140 dev + 188 холдаут) → **CLEAN**. В сьюте прогоняется в обе стороны на подсаженном корпусе.

**Две правки к словам тимлида, обе названы в PROGRESS.** Примеры «Ну да» и «+1» из (j)3 **не
проходят чек, который требует (j)2**: «Ну да» — это буквально холдаут-коммент
`@VARUS_channel:8647:15419` («Ну да,кожен сам вирішуює»), «+1» сидит внутри dev'ского «по акції 1+1».
Заменены на «Ага, ясно» и «Плюсую». Альтернатива — ввести порог минимальной длины, то есть новый
threshold, чего план не разрешает. Откат — одна строка на каждый.

**Две находки, которых никто не заказывал.**
1. `results/promo_dev40_prep.json` был **протухшим с A(1)** (`c7a63a7`): он всё ещё мерил рендер, в
   котором были бессловесные комменты. Регистрация пинит этот файл, а правило дымового набора
   («самый короткий / медиана / самый длинный по его же chars») читает его цифры. После
   перегенерации самый короткий юнит переехал `@msuaaaa:6523` → `@msuaaaa:7187` (у 7187 два
   коммента, один бессловесный). Правило то же — вход починен. [[a_fixture_on_disk_pins_yesterdays_schema]]
2. **Кап шага УПАЛ $2.50 → $2.4469** при переиздании, потому что REMAINING цикла упал $3.3305 →
   $2.7469 за сутки всегда-включённого тома — не за ран. Значит **у B $1.9215, а не $1.9746 из (h)4**.
   Цифра рулинга родилась до падения. [[a_balance_delta_is_not_a_per_leg_cost]]

**Красное, которое было предсказано:** правка TEMPLATE протухила 40 `rendering_sha256` пака →
`test_the_pack_pins_exactly_what_the_pod_re_derives`. Закрыто переизданием пака, тест не тронут.
Порядок принудительный: код+регистрация в один коммит → `--pack` (иначе `committed_registration()`
отказывает) → пак вторым коммитом.

Деньги за день: **$0**. Шаг `promo-dev-loop` $0.5254 из $2.4469; цикл 3 — $4.2531 из $7.00.
