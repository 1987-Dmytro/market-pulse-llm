---
type: daily_log
date: 2026-09-04
tags: [daily]
---

# 2026-09-04

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->

## Sessions
- 06:34: session ended (auto)
- 07:33: session ended (auto)

## Checkpoint 07:40 — сессия 10 (исполнитель, $0): рулинг (f) принят, A(1) остановлен ДО первой правки

**Сделано.**
- Файлы тимлида закоммичены по путям (`7675946`): рулинг 03.09 (f), кодбук **v1.1**, три строки
  эталона (8044 / 20083 / 407 → `post`), STATUS. Плюс knowledge сессии 9 (`5fcecad`).
- `make check` зелёный на этом HEAD: **4 313 passed, 2 skipped, exit 0** (710.95 s).
- `docs/plans/promo-pulse-1.PROGRESS.md` переписан: done / next / **open stop** (15 строк) +
  «названо, не построено». Файл 59 строк, коммиты `a552a98` → `3ab9038`.
- Полные админские id разрешены из стора однозначно: VARUS `2fa2b73f…`, msuaaaa `58805a36…`.

**Стоп — «где могут жить `admin_anon_ids`».** Рулинг (f) A(1) кладёт ключ в `config/registry.yaml`.
Этот файл пинят **21 запечатанный рекорд** в четырёх семействах sha (посчитано по `results/`):
r1 `d4e3b237…` 7 · pre-(13)(b) `920c7f20…` 8 · signed-screen `c82d0cff…` 5 · ЖИВЫЕ байты
`eff8ba5b…` 1 (`results/prereg_promo_c2.json`, аддендум рулинга 02.09 (d)). Обе строки, которые
называет рулинг (`:36`, `:45`), — r1-эпохи, ВНЕ скобки r2 ([[the-law-grows-inside-marked-blocks]]),
поэтому любая добавленная строка попадает в КАЖДУЮ реконструкцию.

**Проверено запуском, не рассуждением.** `run_promo_c2.preflight` проходит на HEAD и отказывает
после ОДНОЙ добавленной строки; `build_aggregates.py:294` достаёт печать C2 из
`tests/test_export_dashboard_data.py` → `make check` краснеет, а это собственное условие выхода
сессии A. Четвёртая ревизия (`R3_MARK` + `registry_before_r3` под `registry_before_r2`)
восстанавливает все четыре семейства ТОЧНО — и всё равно недостаточно: тот preflight
(`:523-535`, живой на `:979`) сравнивает сырые байты, обхода ревизий в нём нет, и его же автор
пишет «If the registry moved, that is a refusal for the team lead, not a rewrite here». Ровно
продолжение [[5c2-closed-the-sitting-and-the-shelf-life-redesign]]: цепочка ревизий спасает СВОИХ
ходоков, но не байтовый гард — и они по разные стороны `make check`.

**Вопрос тимлиду (три ветки).** (a) санкционировать r3 в `registry.py` И научить `preflight`
обходу по образцу `run_5c2.py:145-148`; (b) id живут вне реестра — ни один пин не двигается, путь
render/pack идентичен; (c) объявить сбор C2 закрытым, тогда нужна только первая половина (a).

**Отдельно, для денег сессии B.** Пины регистрации dev-петли УЖЕ сдвинулись под собственным
коммитом тимлида: `prereg_promo_dev_loop.json` держит эталон `302113b8…` и кодбук `6a74bc81…`,
на HEAD `2303ea43…` / `23610346…`. `committed_registration()` проверяет только, что запись
отслеживается и не изменена — `pinned_inputs` не переверяет НИКТО, оба сдвига проходят молча.

**Ещё названо, не построено:** 59 из 82 админских комментов в dev-40 БЕЗ текста (наивный префикс
сделает их видимыми строками, которых нет в эталоне); `@kopiyochka1` — третий такой канал, и его
источник несёт два хэндла на один id; 28 комментов VARUS с `sender_anon_id: null` (recall 874/902);
`--score --iteration 1` перезаписывает принятый `errors_iter1.json` на месте, а A(4) требует
«printed beside the paid number, never replacing it»; `check_law` на поде сверяет только
`codebook_version`, поэтому чисто рендерная правка оставляет sha закона тем же.

**Инструмент.** Находки — состязательный аудит из 32 агентов (4 измерения × find → refute),
`wf_178dcd11-ffb`; каждое число, попавшее в PROGRESS, перепроверено мной руками.

Деньги: **$0** в сессии. Шаг `promo-dev-loop` $0.5254 из $2.50; цикл 3 — $4.0631 из $7.00.
- 07:45: session ended (auto)
