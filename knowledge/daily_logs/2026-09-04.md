---
type: daily_log
date: 2026-09-04
tags: [daily]
---

# 2026-09-04

## Summary

Пять сессий исполнителя (10–14), **все за $0**, пода не существовало ни минуты. День закрыл вилку
`admin_anon_ids`, построил A(1), сделал «A-rest» целиком, а под конец вскрыл пятидневный дефект
резолва и остановился перед платной ногой «sources-r3».

**Сессия 10 — стоп ДО первой правки.** A(1) по букве рулинга (f) клал `admin_anon_ids` в
`config/registry.yaml` — файл, который пинят **21 запечатанный рекорд** в четырёх семействах sha
(r1 `d4e3b237…` 7 · pre-(13)(b) `920c7f20…` 8 · signed-screen `c82d0cff…` 5 · ЖИВЫЕ байты
`eff8ba5b…` 1). Обе строки рулинга — r1-эпохи, ВНЕ скобки r2
([[the-law-grows-inside-marked-blocks]]), значит любая добавленная строка попадает в КАЖДУЮ
реконструкцию. Померено запуском: `run_promo_c2.preflight` проходит на HEAD и отказывает после
ОДНОЙ строки. Четвёртая ревизия (`R3_MARK`) восстанавливает все четыре семейства ТОЧНО — и всё
равно недостаточно: preflight сравнивает сырые байты и ревизий не обходит.
→ [[the-sealed-file-does-not-grow-it-gets-a-sibling]]

**Сессия 11 — A(1) построен** (`c7a63a7`). Рулинг (g) выбрал опцию (b): сиблинг-файл
`config/channel_admins.yaml`, ни один пин не двигается. Единственный читатель —
`promo_prompts.admin_ids`; метка `[admin] ` по ID, поэтому 28 VARUS-комментов с `sender_anon_id:
null` не совпадают ни с чем. Числа из файлов: бестекстовых сброшено **68** = `n_wordless`
жеребьёвки; меток **23** = 82 − 59. Чекер пода принимает 56/56. `make check` 4 313 / 2 skipped.

**Сессия 12 — «A-rest» остановлен ДО первой правки, два измерения за $0.** (1) У A(2) была ложная
предпосылка: алиасов у `Source` НЕТ, «Варус» встречается в реестре **0 раз**. Свёртка по
`id`/`name`/хэндлам = **+0 строк**, новый носитель = **+1**.
→ [[measure_what_the_normalisation_would_fix]] (2) У пятой категории примеров не было ни одного
чистого: все четыре из §6 — тексты эталонных тредов. **Побочная находка:** отгруженный
`promo_prompts.CODEBOOK` дословно цитировал **11 эталонных комментов dev-40** — итерация 1 мерилась
инструментом, видевшим 7.9 % своего знаменателя; холдаут ЧИСТ.
→ [[the_instruments_examples_came_from_the_exam]], ср. [[the-codebook-that-quoted-the-answer-key]]

**Сессия 13 — «A-rest» СДЕЛАН целиком** (`c2234a4` код+регистрация, `d64faf7` пак, `3f75106`
слой). `config/chain_aliases.yaml` из ИМЁН сетей: **+1 строка, и она названа** —
`@VARUS_channel:6216:8865` («VARUS» против «Варус»), 0 сломанных, 114/140 → **115/140 = 0.8214**.
Утечка закрыта — 11 цитат ушли из закона, **v1.2** `a587e0d6…`; `--leak-check` берёт литералы ИЗ
ОТГРУЖЕННОГО модуля: 27 + 8 против **328** комментов → **CLEAN**. Два примера тимлида заменены,
потому что не проходят его же чек: «Ну да» — буквально холдаут `@VARUS_channel:8647:15419`.
Незаказанные находки: `promo_dev40_prep.json` протух с A(1) ([[a_fixture_on_disk_pins_yesterdays_schema]]),
и **кап шага упал $2.50 → $2.4469** за сутки всегда-включённого тома, значит у B **$1.9215**, а не
$1.9746 ([[a_balance_delta_is_not_a_per_leg_cost]]). Верификатор поймал то, чего не поймала
предпроверка: `test_the_layer_reads_nothing_and_parses_nothing` парсит AST и требует, чтобы
`aggregates.py` НИКОГДА не звал `open` — чтение уехало в `registry.chain_spellings()`.
→ [[a_module_can_have_a_test_about_what_it_may_not_do]]. `make check` **4 319 / 2 skipped**,
прогон только ОТЦЕПЛЕННЫМ (`os.setsid`): фоновые задачи харнеса дважды убивались на 26 %.

**Сессия 14 — «sources-r3: Маркетопт», $0-нога** (`449cab1`, PROGRESS `97880de`). Первый глагол
пункта оказался уже выполненным: `results/joins_5c1.jsonl:33` пишет `already_member` от 30.08, и
живой `CheckChatInviteRequest` (чтение; `ImportChatInvite` не звался) вернул `ChatInviteAlready`,
`left: False`, чат `1255265634`. §3 PHASE и STATUS правы ОБА — пять дней «вступил, но не собрано»
были одним проглоченным дефектом: Telethon читает `+`-форму только после `t.me/`, и ValueError
уходил в `except Exception`. Собрано **+33 поста, 04.08 → 04.09, все с медиа**, 6 голов альбомов,
9 текстов с ценой, 0 комментов. `--only` схлопывает запись 19 строк → 1, поэтому `--plan` пересобрал
все строки из стора: диффом — четыре поля Маркетопта плюс `phase`.
→ [[the-invite-hash-is-resolved-at-the-call-site]]

**Цена этой $0-записи, названная в PROGRESS:** максимум корпуса был VARUS 2026-08-30, стал Маркетопт
**2026-09-04**, а `promo_census_c2.anchor_of()` считает `max(дата)+1` ИЗ СТОРА — якорь 2026-08-31
пересчитался бы в 2026-09-05 и перепиннил все 19 строк. `walk_window` не имеет верхней границы, ни
один инструмент дерева не собрал бы разрешённое окно, не сдвинув максимум; `data/` в gitignore.
→ [[a_collection_moves_every_anchor_computed_from_the_store]]

## Decisions
- [[the-sealed-file-does-not-grow-it-gets-a-sibling]] — рулинг (g), опция (b).
- [[the-invite-hash-is-resolved-at-the-call-site]] — резолв чинится в точке вызова; реестр и ключ
  стора не двигаются.
- Утром закоммичены артефакты сессии 9: [[the-fence-is-not-the-answer]],
  [[terminate-after-is-the-cap-less-what-the-step-spent]].
- Рулинги тимлида за день: (g) сиблинг-файл · (h) A(1) принят, A(2)–A(5) — один пункт · (i)
  «sources-r3» перед платной B, кап $0.50 · (j) `chain_aliases.yaml` из имён, 11 цитат уходят,
  примеры синтетические · (k) «A-rest» ПРИНЯТ целиком · **(l) сбор принят; B → c3; параметры, не
  сиблинги; экран — все окна, один id** (порядок (i) отменён: c3 уехал ЗА B).

## Next
**Рулинг 04.09 (l) пришёл в конце дня и закрыл стоп, ответив на оба вопроса и ПЕРЕСТАВИВ порядок.**
1. **Собранное принято** (`449cab1`): дефект найден живым чтением и починен только в аргументе
   резолва; 33 поста, все с медиа, $0. Юнит-тест на `resolvable()` с `+`-хэндлом РАЗРЕШЁН.
   Сдвинутый якорь признан безвредным, **пока ни один C2-продюсер не перезапускается**: правда C2 —
   в его запечатанных result-файлах, а не в максимуме стора.
2. **Q1 → параметры C2-продюсерам, НЕ сиблинги:** `--out`, `--channels`, `--anchor` (пиннится в
   регистрации), `--prereg`, `--step`, `--cap`. Все файлы C2 остаются побайтово теми же, c3 пишет
   `results/*_c3.json` под `results/prereg_promo_c3.json` со своим якорем (день после последнего
   поста) и **своим леджером `promo-c3`** — `STEP promo-pulse-1` закрыт, так что леджер обязателен.
3. **Q2 → экран рендерит ВСЕ окна** (`tick.py --window all`, дефолт, если тест не пинит `w2` — тогда
   флаг несёт строка раннбука), и оба канала берут ОДИН существующий id `marketopt_promo`:
   `chain_aliases.yaml` получает `chain_of_channel: {marketopt_private: marketopt_promo}`,
   читаемый тем же единственным читателем ДО написаний. Ничего запечатанного не двигается.
4. **Порядок: сначала B** (готова, критический путь) — `*_iter2.*`, **≤ $1.9215**, с
   перенаправлением `--score --suffix` и `--out` раннбука K8 с ПЛАТНЫХ `…_iter1.*`. **Потом «c3»
   ОДНОЙ сессией**: $0-сборка и платная нога вместе, кап = **min($0.50, REMAINING − $0.30), как
   гард напечатает ПОСЛЕ B**; если места меньше $0.15 — СТОП за словом оператора. `@ON_LINE_MO` —
   после c3, если ценз покажет цены.
Параллельно за $0: разметка позиций тимлидом по 46 страницам — критический путь S1.

## Blockers
**Открытого стопа нет.** Долги названы, не построены: `committed_registration()` не переверяет
`pinned_inputs`; `check_law` сверяет только `codebook_version`; ветка `--comments` всё ещё передаёт
голый хэндл (мертва только из-за `comments_enabled: false`); юнит-теста на `resolvable()` ещё нет.
Near-quote «Шикарно…» и кодбук-документ ждут заморозки закона ((k)2). ⚠ Длинный прогон — только
отцепленным. ⚠ K4 не перезапускать. ⛔ `1925810730` неадресуем.

Деньги: **$0 за день**, пять сессий подряд. Шаг `promo-dev-loop` $0.5254 из $2.4469 (B ≤ $1.9215);
цикл 3 — $4.2531 из $7.00, REMAINING $2.7469 (том ~$0.24/день); холдаут $0.30 не тронут. Кап c3
больше не число сегодняшнего дня: он берётся из гарда ПОСЛЕ B ((l)4).
## Сессия 15 — B, платная итерация 2 ($0.223850). ОТКРЫТЫЙ СТОП: «iteration 2 read»

**Что куплено.** Под `ej1dxfcpe0etff`, RTX 4090 EU-RO-1 по зарегистрированным $0.74/ч, создан
14:42:45Z, удалён 15:00:54Z — **1089 с, $0.223850**. 56/56 юнитов, каждый ответ balanced, finish
stop, ОДИН буст (167.0 с с тёплого тома) через GO. Смок намерил **38.816 с/тред** (макс 88.237)
против 81.212 у итерации 1, `--project` дал **GO** на $0.4097 (среднее) и $0.8465 (макс).

**Что до денег.** Три числа объявлены в PROGRESS ДО смока и все выдержали: (1) кап B — **$1.8826**,
не $1.9215, потому что правило самой регистрации `min($2.50, REMAINING − $0.30)` при сегодняшнем
REMAINING $2.7080 даёт $2.4080; замороженная запись не тронута, применено более строгое число.
(2) KILL читался против $1.8826, а не против всего капа $2.4469, как это делает `project()`.
(3) `--terminate-after` — 90 мин, наименьшее из трёх (денежная дорожка 152.6 мин, правило капа,
и зарегистрированный `gates.terminate_after_minutes`), поэтому напечатанный `--open`
`usd_at_the_backstop $1.11` — правда об этом поде, а не о другом.

**Результат K8.** Сигналы **0.8854** ✅ (бар 0.75, было 0.8792). Субъект **0.7500** ❌ (бар 0.80),
против **0.8214** у итерации 1 по ТОМУ ЖЕ v1.1-голду — то есть регресс, а не плато.

**Причина — транспорт, не закон, и это весь провал целиком.** `@VARUS_channel:6216`: 18 → 0 строк.
Ответ пришёл ARRAY внутри ```json-забора; `promo_dev_pod_runner.balanced_prefix` диспатчит массив
по `text.lstrip().startswith("[")`, забор делает это False (`fenced_answers` = 40 из 40), сработало
правило объекта — а оно же стоит на СТОПЕ ГЕНЕРАЦИИ: 213 символов, 5.9 с, `finish stop`. Остальные
17 ответов НЕ БЫЛИ СГЕНЕРИРОВАНЫ, поэтому починка парсера их не вернёт. Собственный докстринг
правила утверждает, что лег A «can never begin with `[`» — про сырой текст, который всегда
начинается с забора.

**Проверено, а не предположено** (срезанный ответ, который всё же парсится, читается как «модель
сказала меньше»): массивный ответ ровно **1 из 40**; единственные другие сдвиги числа строк —
`9014` (11→1) и `9999` (6→5) — целые закрытые объекты на тредах с 1 и 5 голд-комментами, стоят
**0** голд-строк. На 122 комментах вне 6216 итерация 2 промахивается **17 раз против 25**.

**Вопрос тимлиду (открытый стоп).** Починка диспатча — ДЕФЕКТ (тот же `extractor_version`,
итерация 2 перекупается) или НОВЫЙ ИНСТРУМЕНТ по рулингу 03.09 (c) 3, то есть итерация 3? От этого
зависит, что куплено за $0.2239: чтение ЗАКОНА или чтение ТРАНСПОРТА. И второй: рост или плато?

**Моя ошибка дня.** Погнал `make check` параллельно с платной сессией — красный ERROR в
`tests/test_zero_spend_45g5.py`, файл которого сессия не трогала. На тихом дереве после teardown —
**4 319 passed / 2 skipped, exit 0**. Пока верификатор идёт, дерево обязано быть неподвижным.

**Прочее, названо и не построено.** Лег B в ОБЕИХ итерациях вернул `[]` на всех 16 постах — S4
третьей покупкой купит тот же пустой массив. Голд покрывает 140 из 208 комментов в 40 тредах, так
что модель, пишущая меньше строк, может показать больший процент. `pinned_inputs` регистрации
сверены руками (все пять держат), потому что `committed_registration()` их не смотрит.

Деньги: шаг `promo-dev-loop` ОТКРЫТ на **$0.7493 из $2.4469** (гарду `--close` не звать — итерации
3–5 обязаны влезть), цикл 3 — **$4.5265 из $7.00, REMAINING $2.4735**; кап c3 = $0.50, холдаут цел.
Одна леджер-строка за сессию, взята ДО создания пода.

- 16:01: session ended (auto)
- 16:40: session ended (auto)
- 16:48: session ended (auto)
- 16:51: session ended (auto)
- 16:54: session ended (auto)
- 17:07: session ended (auto)
