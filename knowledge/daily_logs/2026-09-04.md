---
type: daily_log
date: 2026-09-04
tags: [daily]
---

# 2026-09-04

## Summary

Десять сессий исполнителя (10–19), из них **две платные — $0.717594**. День закрыл вилку
`admin_anon_ids`, построил A(1) и «A-rest» целиком, вскрыл пятидневный дефект резолва инвайта,
купил итерацию 2 красной, **починил транспорт и купил итерацию 3 — оба бара взяты**. Пять
остановок, все предвидимые файлом фазы; PHASE вырос до v6, чтобы их классы больше не стоили
остановки. День закрывается на первом за фазу зелёном субъекте.

**Сессия 10 — стоп ДО первой правки.** A(1) по букве рулинга (f) клал `admin_anon_ids` в
`config/registry.yaml` — файл, который пинят **21 запечатанный рекорд** в четырёх семействах sha
(r1 `d4e3b237…` 7 · pre-(13)(b) `920c7f20…` 8 · signed-screen `c82d0cff…` 5 · ЖИВЫЕ байты
`eff8ba5b…` 1). Обе строки рулинга — r1-эпохи, ВНЕ скобки r2
([[the-law-grows-inside-marked-blocks]]), значит любая добавленная строка попадает в КАЖДУЮ
реконструкцию. Померено запуском: `run_promo_c2.preflight` проходит на HEAD и отказывает после
ОДНОЙ строки. Четвёртая ревизия (`R3_MARK`) восстанавливает все четыре семейства ТОЧНО — и всё
равно недостаточно: preflight сравнивает сырые байты и ревизий не обходит.
→ [[the-sealed-file-does-not-grow-it-gets-a-sibling]]

**Сессия 11 — A(1) построен** (`c7a63a7`). Рулинг (g) выбрал опцию (b): сиблинг-файл
`config/channel_admins.yaml`, ни один пин не двигается. Единственный читатель —
`promo_prompts.admin_ids`; метка `[admin] ` по ID, поэтому 28 VARUS-комментов с `sender_anon_id:
null` не совпадают ни с чем. Числа из файлов: бестекстовых сброшено **68** = `n_wordless`
жеребьёвки; меток **23** = 82 − 59. Чекер пода принимает 56/56. `make check` 4 313 / 2 skipped.

**Сессия 12 — «A-rest» остановлен ДО первой правки, два измерения за $0.** (1) У A(2) была ложная
предпосылка: алиасов у `Source` НЕТ, «Варус» встречается в реестре **0 раз**. Свёртка по
`id`/`name`/хэндлам = **+0 строк**, новый носитель = **+1**.
→ [[measure_what_the_normalisation_would_fix]] (2) У пятой категории примеров не было ни одного
чистого: все четыре из §6 — тексты эталонных тредов. **Побочная находка:** отгруженный
`promo_prompts.CODEBOOK` дословно цитировал **11 эталонных комментов dev-40** — итерация 1 мерилась
инструментом, видевшим 7.9 % своего знаменателя; холдаут ЧИСТ.
→ [[the_instruments_examples_came_from_the_exam]], ср. [[the-codebook-that-quoted-the-answer-key]]

**Сессия 13 — «A-rest» СДЕЛАН целиком** (`c2234a4` код+регистрация, `d64faf7` пак, `3f75106`
слой). `config/chain_aliases.yaml` из ИМЁН сетей: **+1 строка, и она названа** —
`@VARUS_channel:6216:8865` («VARUS» против «Варус»), 0 сломанных, 114/140 → **115/140 = 0.8214**.
Утечка закрыта — 11 цитат ушли из закона, **v1.2** `a587e0d6…`; `--leak-check` берёт литералы ИЗ
ОТГРУЖЕННОГО модуля: 27 + 8 против **328** комментов → **CLEAN**. Два примера тимлида заменены,
потому что не проходят его же чек: «Ну да» — буквально холдаут `@VARUS_channel:8647:15419`.
Незаказанные находки: `promo_dev40_prep.json` протух с A(1)
([[a_fixture_on_disk_pins_yesterdays_schema]]), и **кап шага упал $2.50 → $2.4469** за сутки
всегда-включённого тома ([[a_balance_delta_is_not_a_per_leg_cost]]). Верификатор поймал то, чего не
поймала предпроверка: `test_the_layer_reads_nothing_and_parses_nothing` парсит AST и требует, чтобы
`aggregates.py` НИКОГДА не звал `open`.
→ [[a_module_can_have_a_test_about_what_it_may_not_do]]

**Сессия 14 — «sources-r3: Маркетопт», $0-нога** (`449cab1`). Первый глагол пункта оказался уже
выполненным: `results/joins_5c1.jsonl:33` пишет `already_member` от 30.08, и живой
`CheckChatInviteRequest` (чтение) вернул `ChatInviteAlready`, чат `1255265634`. Пять дней «вступил,
но не собрано» были одним проглоченным дефектом: Telethon читает `+`-форму только после `t.me/`, и
ValueError уходил в `except Exception`. Собрано **+33 поста, 04.08 → 04.09, все с медиа**, 0
комментов. → [[the-invite-hash-is-resolved-at-the-call-site]]
**Цена этой $0-записи:** максимум корпуса стал Маркетопт **2026-09-04**, а
`promo_census_c2.anchor_of()` считает `max(дата)+1` ИЗ СТОРА — якорь 2026-08-31 пересчитался бы в
2026-09-05 и перепиннил все 19 строк. → [[a_collection_moves_every_anchor_computed_from_the_store]]

**Сессия 15 — B, платная итерация 2 ($0.223850). Красный бар и ОТКРЫТЫЙ СТОП.** Под
`ej1dxfcpe0etff`, RTX 4090 EU-RO-1 по зарегистрированным $0.74/ч, 14:42:45Z → 15:00:54Z: **1089 с**,
56/56 юнитов, каждый ответ balanced, ОДИН буст (167.0 с с тёплого тома) через GO. Смок намерил
**38.816 с/тред** против 81.212 у итерации 1, `--project` дал GO. Три числа объявлены ДО смока и все
выдержали: кап B **$1.8826** (правило самой регистрации строже замороженного $1.9215), KILL против
него, а не против всего капа шага, и `--terminate-after` 90 мин как наименьшее из трёх.
**K8: сигналы 0.8854 ✅ (бар 0.75) · субъект 0.7500 ❌ (бар 0.80)** против 0.8214 у итерации 1 по
ТОМУ ЖЕ голду. Причина — транспорт, и это весь провал целиком: `@VARUS_channel:6216` 18 → 0 строк.
→ [[the-fence-defeats-the-dispatch-that-fixes-the-array]]
**Ошибка дня:** гонял `make check` параллельно с платной сессией — красный ERROR в файле,
которого сессия не трогала. Пока верификатор идёт, дерево обязано быть неподвижным.

**Сессия 16 — стоп закрыт рулингом (m), починка написана и зелёная, $0** (`b220acc` файлы тимлида,
`f872a53` починка, `65ae76c`+`6aabc3c` PROGRESS). `make preflight` — **0 затронутых путей не пиннуты
ни одной записью**. Дискриминирующий греп ДО правки: ни один тест не утверждает старое поведение
`parse` на массиве, значит это не «тест пришлось бы ослабить». Починка ровно как написал (m)2:
`balanced_prefix` читает забор через `promo_prompts.unfence` ПЕРВЫМ и диспатчит по форме ВТОРЫМ;
`promo_prompts.fold` читает голый массив как строки, которыми он и является. **ОДИН тест, в обе
стороны** (PHASE §4 v5) — и он же $0-дрилл §6.5: объект · массив × с забором · без забора, прогнан
через `close_arrays_too` + `stops_here`. Суита поймала настоящую ловушку: `pod.main` ставит патч на
`reader_v5` и не снимает, поэтому «отгруженное правило» для контроля пришлось брать из reload.
→ [[guard_selftest_negative_control]] · [[identity_field_stops_covering_the_change]]

**Сессия 17 — весь $0-слой итерации 3 готов, платная половина упёрлась в стоп** (`8efb0c8`,
`48537b9`, `1962ee5`). `ITERATION = 3`; `pinned_inputs` получил `promo_dev_pod_runner.py`
`102fa524…` и `promo_prompts.py` `dd260cb5…` — пин из (m)5, подтверждённый (n)2. Лег B закрыта по
(m)4: `by_channel` пустой (именно эту карту итерирует `build_pack`), 16 id НАЗВАНЫ под `not_bought`.
**Доказательство, ради которого пины и понадобились:** все 40 leg-A `rendering_sha256` побайтово
равны паку итерации 2 при сдвинутой sha модуля `8d52ee79…` → `dd260cb5…`.
**СТОП (PHASE §8 (j)):** `make check` 1 failed / 4319 — `test_the_pack_pins_exactly_what_the_pod_re_derives`
держал популяцию итерации 2 (`len(items) == 56`). Содержательное утверждение прошло, разошлись
только счётчики. Тест НЕ тронут, под НЕ создан.

**Сессия 18 — стоп (o) закрыт: тест читает регистрацию, а не пиннит её популяцию ($0)** (`ec16e82`
файлы тимлида, `2f2b62c` тест). Счёт из `leg_a.threads + leg_b.posts`, множество из `leg_a.order` +
`leg_b.by_channel`, «ровно» в ОБЕ стороны + «ни одного id дважды», лег B — цикл по юнитам записи.
Перед сравнением проверяется `pack["registration"]["sha256"] == sha256_of(PREREG)`: без якоря два
чтения могли быть двумя популяциями. (o)2 отказал синтетическому юниту. **`make check` 4320/2/exit 0.**
**Негативный контроль за $0:** выброшенный зарегистрированный юнит — ОТКАЗ, незарегистрированный
отгруженный — ОТКАЗ, отгруженный дважды — ОТКАЗ.

**Сессия 19 — ИТЕРАЦИЯ 3 КУПЛЕНА, ОБА БАРА ВЗЯТЫ ($0.493744)** (`b93c997`, `ca0dfdb`, `01a6916`).
Под `cd918wet7sea5b`, RTX 4090 EU-RO-1 по зарегистрированным $0.74/ч, 17:59:53Z → 18:39:55Z:
**2402 с**, 40/40 leg-A юнитов, каждый ответ balanced/finish stop, **0 parse failures**, 140
gold-строк. Гейт ПЕРЕД create: `make check` 4320/2/exit 0 на `edfe12f` — правка `docs/STATUS.md`
названа в десяти тестах и ЧИТАЕТСЯ четырьмя (`lora_c_{run,prep,armb}` через `quoted()`,
`think_zero_shot`), так что «дифф только по докам» было недоступной отговоркой. Порт ssh на 20 с из
500, бандл побайтово равен на обеих сторонах, boot **210.7 с** с тёплого тома, второго буста через
GO не было. Смок 3/3 → `--project` **GO-THEN-STOP** ($0.8420 по среднему углу).
**K8: субъект 0.8714 ✅ (было 0.7500 ❌) · сигналы 0.9104 ✅.**
**Дифф против итерации 2 — ровно транспортная починка и ничего больше:** 122 строки итерации 2
стоят НЕИЗМЕННЫМИ, добавилось 18, 0 сдвинулось, 0 пропало; страта `currency` идентична построчно
(48/61, субъект 0.7869), весь прирост в `decimal_only` **57/79 → 74/79**. Все 40 ответов пришли в
ФЕНСЕ, и `unfence` прочёл объект как он есть — то есть красный бар итерации 2 был ровно забором.
**KILL впервые подошёл вплотную и не сработал.** REMAINING подтёк $2.4638 → $2.4540, правило капа
дало $2.1540, остаток шага **$1.4048** вместо объявленного $1.4146; измеренный **max-угол $1.8425
оказался ВЫШЕ остатка** — впервые за три смока. Это не KILL по двум независимым причинам:
`--terminate-after` физически держит под на **$1.1098**, то есть счётчик до $1.8425 дойти не может,
а 201.967 с, которыми этот угол считается, — это `@VARUS_channel:6009`, самый длинный рендер из
всех 40, УЖЕ отвеченный в смоке. Связывающим был вопрос ВРЕМЕНИ, и он посчитан ДО GO: 37 × 88.772 с
= **54.7 мин** против **78.4 мин** жизни пода. → [[a_bound_the_meter_cannot_reach]]
**Мимо раннбука, но в тему:** `hot.md` всё ещё звал купить итерацию 3 — файл инжектится на
SessionStart, это был риск дублирующей траты, переписан. Два фоновых наблюдателя убиты локально;
отцепленный `setsid`-раннер их пережил и досчитал ([[remote_job_outlives_its_watcher]]). `scp` с
`$SCPOPT` схлопнулся в один аргумент — zsh не делает word-splitting на неквотированном разворотe;
файл на 40 строк уцелел только потому, что fetch идёт ДО delete ([[fetch_before_you_delete]]).

## Decisions
- [[the-sealed-file-does-not-grow-it-gets-a-sibling]] — рулинг (g), опция (b).
- [[the-invite-hash-is-resolved-at-the-call-site]] — резолв чинится в точке вызова.
- [[the-fence-defeats-the-dispatch-that-fixes-the-array]] — механизм красного бара итерации 2;
  **подтверждён покупкой s19**: забор снят → 18 строк вернулись, ни одна старая не двинулась.
- [[a_bound_the_meter_cannot_reach]] — новое: потолок, до которого счётчик не доходит, не может быть
  тем, с чем сравнивают KILL.
- Рулинги тимлида за день: (g) сиблинг-файл · (h) A(1) принят · (i) «sources-r3» перед B · (j)
  `chain_aliases.yaml` из имён, 11 цитат уходят · (k) «A-rest» принят целиком · (l) сбор принят,
  B → c3 · **(m) итерация 2 стоит как измерена; починка = новый `extractor_version` = итерация 3;
  плато нет; нога B закрыта двумя `[]`** · **(n) s16 принят по диффу; переиздание пиннит оба файла
  починки** · **(o) s17 принят; тест переписывается на чтение записи; заимствованная ставка уходит
  перед итерацией 4** · **(p) s18 принят по диффу; следующая сессия — покупка и ничего кроме**.
- **PHASE v5 → v6** (файл тимлида): §4 «asked for once» · §6.5 «валидность платного чтения»
  (`parse_failures = 0`, дрилл всех форм ответа при $0 до покупки) · **§6.6** — тест, у которого
  инвариант держится, а расходятся ЛИТЕРАЛЫ записи, переписывается на чтение записи тем же
  коммитом и это НЕ стоп; ослабление инварианта остаётся стопом.
- **Правило дня (слово оператора):** каждая остановка = рулинг + разбор паттерна + правка правил.
  Разбор пяти остановок дня — `docs/reviews/2026-09-04-stop-patterns.md`.

## Next
**СТОП, решение оператора: НОТИС ХОЛДАУТА.** §8 (e) — холдаут это ОДИН выстрел: предрегистрация в
закоммиченной записи, STOP-нотис, потом одна трата. Субъект взял 0.80 на 3-м прогоне из 5
разрешённых, значит это единственная оставшаяся клауза критерия. Заморозка закона (near-quote,
кодбук-документ v1.2) и holdout-40 — **зависимость ТИМЛИДА (§7)**, не исполнителя; ничего из
холдаута не предрегистрировано и я не написал ни строки. Потом «c3» одной сессией по (l) 2–4.
Параллельно за $0 и на критическом пути S1: разметка позиций тимлидом по 46 страницам.

## Blockers
**Открытого стопа нет** (открыт только вопрос к оператору выше). Названо, не построено:
**замена заимствованных 135.232 с ИЗМЕРЕНА и она БОЛЬШЕ** — смок итерации 3 дал третью строку
`promo_dev40_seconds_per_thread` **max 201.967 с** (mean 88.772, n=3), выше и 178.298 с, которые
(o)4 назвал собственным измеренным максимумом; по (o)4 борроу уходит ПЕРЕД переизданием итерации 4
на собственном максимуме, а на нём dear-угол в остаток НЕ влезает → применяется ветка «средний угол,
кап как жёсткий стоп». **Страта `currency` = 0.7869 субъекта и не сдвинулась НИ НА СТРОКУ** через
починку — бара на страте нет, но currency-тяжёлый холдаут ляжет у самой черты, и это стоит знать
тому, кто предрегистрирует holdout-40. Прежние долги живы: `items[:3] == pack["smoke_ids"]` —
пак против пака · `pod.main` не снимает патч с `reader_v5` · `committed_registration()` не смотрит
`pinned_inputs` (s19 сверил все 7 РУКАМИ до create) · `check_law` сравнивает только
`codebook_version` · `project()` меряет KILL против ВСЕГО капа · `resolvable()`-тест с `+` разрешён
(l)1, не написан · голд покрывает 140 из 208 комментов · «Шикарно…» и кодбук-документ ждут заморозки
закона ((k)2). ⚠ Не гонять `make check` во время платной сессии. ⚠ Длинный прогон — только
отцепленным (`os.setsid`). ⚠ K4 не перезапускать. ⛔ `1925810730` неадресуем. ⛔ `aggregates.py` не
смеет звать `open`. ⛔ **`MEMORY.md` стоит на пороге 25 000 UTF-16** — новая строка требует ужимания.

**Деньги за день: $0.717594** (две платные сессии из десяти: s15 $0.223850 + s19 $0.493744). Шаг
`promo-dev-loop` ОТКРЫТ на **$1.242994 из $2.1638**, осталось **$0.920806** — гарду `--close` не
звать, итерации 4–5 обязаны влезть. Цикл 3: **$5.0299 из $7.00**. Кап c3 = min($0.50, REMAINING −
$0.30) = **$0.50**; холдаут $0.30 цел. Леджер-строка бралась ОДИН раз за сессию, до создания пода.
