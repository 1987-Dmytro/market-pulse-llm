---
type: daily_log
date: 2026-08-03
tags: [daily]
---

# 2026-08-03

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->

## Sessions
- 09:49: session ended (auto)
- 10:52: session ended (auto)

## Checkpoint 12:48 — 4.5g2: половина «немых» постов оказалась опросами

Исполнен `docs/PROMPT-4.5g2.md` — одиннадцать атомарных коммитов `0b2b397 → 49efac9`, 520 тестов,
$0.1791 из $0.75. ADR: [[45g2-captions-and-quiz-rulings]]. Дальше — то, что стоит помнить.

**Квиз 11/20 при планке 18/20 — автопочинка отклонена, и это правильно.** Применено ровно две
вещи: 11 совпавших строк (авторитет `operator-quiz-45g`) и одна механическая семья, которую
подтвердили 10 из 11 taste-предложений — v1 ровно `["taste"]`, текст ≤30 символов (это
`build_sitting_pack.SHORT_CHARS`, порог уже зарегистрирован стратами, а не выдуман под правило),
ни одного бренда из вотчлиста, у родительского поста нет своего текста. Совпало 17 строк: 6 уже
были в квизе, 2 держит вердикт 4.5f, **9 закрыты правилом, и их оператор не видел**. Переписано
15 строк, 5 уже несли метку. Девять разошедшихся НЕ применены: их судили вслепую, а слепота —
это и есть дефект, ради которого фаза существует.

**Главное открытие фазы: «медиа-пост» — это не одно состояние, а несколько.** Из 41 родителя без
текста картинок только 21. **16 — опросы**, и их вопрос с вариантами всё это время лежал в
`message.poll`; коллектор пишет `message.raw_text`, а он у опроса пустой. Посты `7146`
(«Вареники з якою начинкою смакують більше?») и `7249` («Млинці з якою начинкою…») — родители 8 из
20 строк квиза и всей подтверждённой taste-семьи. Подписать картинки и на этом остановиться —
значило бы оставить слепым ровно тот класс, ради которого всё затевалось. Вопрос транскрибируется
из уже скачанного сообщения: новых сборов нет, `data/raw/posts` только читается, модель не
участвует, и в файле подписей у такой строки нет ни модели, ни хэша промпта. В промпт это входит
как `[poll] …`, а не `[image description] …` — транскрипт не описание.

**Что суррогат купил, с разбивкой и с полом дисперсии.** Переспрошены 424 медиа-строки тем же
промптом и тем же эндпоинтом; изменилось 153. По тому, что реально стояло в `<post>`:
`[опрос]` 140/322 = **43%**, `[картинка]` 13/77 = **17%**, ничего 0/25 = **0%**. Последние 25 —
бесплатный негативный контроль: у них отрендеренный промпт байт-в-байт совпал с 4.5g, и не
сдвинулось ни одного поля. Значит 153 — это инструмент, а не недетерминизм декодирования, и
честный знаменатель 399, а не 424. Из 300 гейтовых строк метку сдвинуло у 36.

**Пакет пересобран ДО первого вердикта, и это записано числом.** `verdicts_present: 0` замерен во
время сборки. Те же 300 id — не рассуждением, а ассертом против `stratum_of` старого манифеста.
`emptied40.csv` суперседед: лежит на диске байт-в-байт, чтобы хэш, который пришпилил
`results/sitting_45g_manifest.json`, по-прежнему сходился; старый манифест не редактируется и не
удаляется, новый его называет. `emptied_redo.csv` — 75 строк, и у него есть колонка `post`,
которой нет в списке брифа: 71 из 75 висит под текстовым постом, и без неё оператор судил бы их
вслепую.

**Модель подписей выбрана чтением вывода, а не репутацией.** Три кандидата на одних и тех же
картинках: mistral отдал 429 трижды, gemini на одном посте выродилась в перечень стран, а на
другом притащила маркетинговый текст, который промпт запрещает; qwen3.5-flash дал заголовок,
даты, бренды, цены и итоговую фразу. У него ещё и пиннится `fp8`, а у gemini квантизация
`unknown`. Сам промпт правился дважды по замеренному выводу: первая редакция требовала
«транскрибируй весь текст» и «не больше двух предложений» — для альбома из шести листовок это
противоречие, ответ уходил за 600 токенов и обрывался на полуслове, так и не дойдя до итоговой
фразы.

**Состояние.** `make check` 520 passed, `ruff format --check` чисто, 58 из 58 чисел перевыведены
скриптом. Дерево чистое. Пакет оператора: `data/annotation/sitting_45g/` — 300 гейтовых + 75 redo
+ 14 нечитаемых, рядом `media_map.csv` и 426 картинок в `posts_media/`.

**Открыто и не решено:** опросы — дефект уровня корпуса (16 из 41 здесь, сколько по всему стору —
неизвестно, и должен ли коллектор читать `message.poll` — вопрос брифинга Phase 5); 9 redo-строк
без колонки модели (устойчиво `{}`, те же id); 4 поста без суррогата вовсе — видео, голосовое и
два розыгрыша, у последних структура в API есть, я её не читал.
- 12:52: session ended (auto)

## Checkpoint 13:00 — сессия вердиктов вернулась, читаю её (старт 4.5g3)

`PROMPT-4.5g3` пришёл вместе с тремя файлами тимлида, которых у меня в дереве ещё не было:
`docs/quiz-sitting-45g-log.md` (весь захват), `docs/quiz-sitting-45g-check20.md` (контрольная
двадцатка) и сам промпт. Пакет из [[45g2-captions-and-quiz-rulings]] заполнен целиком.

**Захват сменил исполнителя, и это зарегистрировано до вердиктов, а не после.** Оператор сам
отсудил 36 гейтовых строк, дальше Amendment 2 делегировал остальные тимлид-LLM с двумя
предохранителями: оператор судит только то, что тимлид пометил как contested, и слепая проверка
НА тимлиде — 20 его «correct» строк, планка 18/20. Проверка прошла 20/20, вердикты тимлида
устояли. Провенанс из-за этого переименован: это **не** калибровка оператора по SPEC §8, а
«team-lead-LLM triage with operator adjudication and operator spot-check» — во всех записях ниже
по течению.

**Оператор вынес семь правил на 11 спорных строках, и они уже закон гайдлайна**: off-topic шутка
→ `unclear`; неподписанный support-бойлерплейт — это корпоративный голос, список маркеров в
гайдлайне индикативный, а не исчерпывающий; голая похвала поведению ритейлера → `["service"]`
(симметрично жалобе); прямое обвинение ритейлера перевешивает адресата-комментатора; шутка про
еду → `["taste"]`; издевательская цитата приложения → `sarcasm`. Плюс самопоправка оператора по
12621: mock-elevation несёт и флаг сарказма.

**redo-75 и unreadable-14 закрыты полностью**, с картинками: два вердикта решены буквально
картинкой (4009 — «4 шт» на коробке моти → packaging; 8612 — логотип Zernari → packaging), у
9078 родитель без текста и суррогата — названо честно в notes.

Дальше по 4.5g3: гейты по стратам считает скрипт, числа в отчёт попадают только из его вывода.


## 4.5g3 — три страты не прошли, а лекарство оказалось хуже болезни

**Гейт: 88 / 89 / 81 при планке 90 — FAIL по всем трём.** Считает
`scripts/read_sitting_returns.py`: пересобирает запечатанный пакет из батча, который пришпилен
манифестом, воспроизводит его sha256 и только потом сверяет вернувшиеся строки по семи
замороженным колонкам. Файл целиком не сходится по хэшу и не может — две колонки заполнены;
идентичность доказывается с другой стороны. Пересчёт 258/42 сошёлся с логом захвата, иначе был
зарегистрированный стоп. Итог 86% стоит рядом со стратами и ничего не решает: среднее по трём
знаменателям проходит, пока один из них 81.

**Из 42 отказов заметка называет `unclear` в 22, `intents` в 17, `sarcasm` в 3, `sentiment` в 1.**
Поэтому v2.1 — не заплатка по интенциям.

**Слилось только то, что не гейтится.** 75 строк redo (45 сменили метку, 11 — `[]`) и 14
нечитаемых, вставленных на позицию своего исходника. Из 1912 — ноль. Путь приёма страты
намеренно не написан: ни один из 1912 id не лежит ни в одном source-файле, и что именно делать с
принятой стратой — решение, а не строка кода; если страта когда-нибудь пройдёт, скрипт
остановится и скажет это.

**Баг, который стоит записать: `old` в истории — это не «что там было», а «к чему откатывает
читатель этой истории».** У 27 из 75 строк на диске лежал ответ более позднего фикса, а не
переразметчика; `reversals` берёт последний фикс на id, и популяция 97 выродилась в 70. Все
записи были верны — поле означало не то, чем его заполнили.

**А дальше — отрицательный результат, и он ценнее прохода.** Весь пласт (1912) переразмечен под
`precheck_v2.1_with_post`: 1912 ответов (17 со второго прохода), $0.7429 из $1.50. И стало хуже:
`service` 689 → 138, строк без интенции 809 → 1366, `price` 110 → 313, `sarcasm` 87 → 197.
Сменили поле 1153 строки (60%), **из них 172 из 258 тех, что сессия признала верными**, против 25
из 42 отвергнутых — соотношение ровно обратное правильному. На 29 строках, где вердикт прямо
назвал правильный ответ (разобраны из заметок скриптом, а не руками), промпт верен на 10. Решения оператора тут ни при чём — виновата форма: восемь строк
сплошных отрицаний, стоящих последними перед форматом ответа и кончающихся словами «carries no
intent».

**Исправить в этой фазе нельзя.** Повторный прогон пласта оценивается в $0.8492 при остатке
$0.7571 — не влезает, а это зарегистрированный стоп. Пакет второй волны собран, как требует бриф,
но несёт замер: `batch_health` в манифесте и предупреждение первым абзацем в README.

**Урок:** ревизия промпта — это смена инструмента, и её надо покупать сначала на сотне строк
(≈4 цента), а не сразу на всём пласте. Smoke-прогон доказывает путь записи и такого не видит.

**Два guard'а поправлены по дороге.** Оценка стоимости считалась по всему объёму, а не по тому,
что реально будет куплено, — и остаток в 17 строк был отвергнут оценкой на 1912. Теперь цена
считается по `pending`. И рамка пакета второй волны исключает строки, на которые прогон не
ответил: такая строка несёт метки прежнего промпта, и одна такая внутри сотни судила бы старый
промпт под именем нового. Сегодня таких нет, и это записано числом.

- 13:47: session ended (auto)

## Checkpoint 17:59 — 4.5g3 закрыт; ревью нашло три guard'а, которые мерили не то

С прошлого чекпойнта выполнен весь `PROMPT-4.5g3` (детали и числа — в разделе выше и в
[[45g3-sitting-gates]]). Что стоит помнить отдельно — не результат, а разбор после.

**Ревью нашло четыре вещи, две из которых дошли бы до оператора.** (1) Рамка пакета второй волны
исключала только 300 отсуженных строк, но не строки, на которые прогон не ответил, — такая строка
несёт метки прежнего промпта, и одна такая внутри сотни судила бы старый промпт под именем нового
(при планке, которая решается разницей 88 против 90, это процентный пункт). (2) В шаблоне README
три числа были вбиты руками — «верен на 5 из 16», стоимость прогона и остаток; следующая же
пересборка после исправленного прогона отгрузила бы новый sha со старыми долларами. Это
`[[corrections_break_derivations]]` наоборот: рукописное число внутри генерируемого файла.

**Число «5 из 16» вообще было получено разовым heredoc'ом с руками набранным списком.** Заменено
на разбор заметок вердиктов регуляркой: 29 строк прямо называют правильный ответ, промпт верен на
10. Оно теперь считается скриптом при каждой пересборке — то есть переживёт исправленный прогон,
который как раз и захочется сравнить.

**И собственный guard остановил безобидный resume.** Оценка стоимости считалась по всему объёму
(1912) и сравнивалась с остатком кэпа, хотя реально прогон купил бы 17 строк за $0.008. Кэп — это
ограничение на то, что запуск потратит, а не на то, из чего он выбирает; теперь цена считается по
`pending`. Второе следствие того же захода: колонка `annotator` добавилась в скрипт уже после
старта фонового прогона, поэтому батч ушёл без неё, а запись её утверждала — перегенерировано
через resume, а не правкой руками.

**Состояние.** `make check` 563 passed, `ruff format --check` чисто, 77 из 77 чисел перевыведены,
дерево чистое на момент коммита `29a1d68`.

- 18:01: session ended (auto)
- 18:31: session ended (auto)

## Checkpoint 19:02 — 4.5g4: v2.2 куплена сотней строк и убита собственным гейтом

`PROMPT-4.5g4` исполнен целиком. Пять коммитов: свип дерева → v2.2 → предзарегистрированный
план → раннер (до прогона) → результаты; плюс шестой с правкой ADR.

**Результат — KILL, но провалилась не переписка.** preserved 37/58 при планке 55, fixed 9/29 при
24. На тех же строках v2.1 давал 20/58 и 4/29. Несмешанный срез (только 58 принятых, где любое
движение — потеря): v2.1 теряет 38 строк, v2.2 — 21. Все три строки P5 теперь `["service"]` там,
где v2.1 отвечал `[]`. Утвердительная форма работает; до планки, которую поставили вердикты самой
сессии, не дотягивает. Task 4 не выполнялся, `wave2_45g3/` не открывался. ADR
[[45g4-v22-affirmative-rewrite]].

**Главное, что стоило этой фазы, — не число, а место отказа.** Из 20 неприземлившихся рулингов 15
называют `unclear`. Дальше две вещи, каждая проверяемая:

1. `UNCLEAR_RULE` говорит **последним** — смещения 2410 / 3642 / 4240 — и закрывается фразой «do
   not use it to avoid a decision you can make». Позицию держали фиксированной намеренно (она была
   в v2 с её 86%), и теперь она названа следующей переменной. Стоит $0.04: выборка, эталоны,
   скорер и пороги уже лежат, `v22_probe_rows.jsonl` делает сравнение парным на входе.
2. **P6 промпту недоступен вообще.** Правило доходит до модели дважды — вторая строка v2.2 и
   `UNCLEAR_RULE` ещё с v2 — и три из четырёх строк всё равно `unclear: false`. В их текстах
   ритейлера не опознать. А дискриминатор лежал одной директорией в стороне: все четыре несут один
   `sender_anon_id` `2fa2b73f617b…`, 876 комментариев против 81 у следующего — аккаунт поддержки,
   стабильный под HMAC и никогда не деанонимизируемый. В размечаемую строку поле не попадает.
   Недостающий признак, а не недостающая фраза — и это меняет, чей это вопрос: реестра и
   пайплайна, не промпта.

**Что поймали проверки до траты.** Юнит-тест: неотвеченная строка падала с `KeyError` вместо
незачёта по своему знаменателю. Smoke: колонка «до» читала v2.1 из *исходного* батча плана — v2
против себя, отрапортованные как безупречные 58/58 для промпта, который набирает 20. Advisor нашёл
третье уже в ADR: «2.4% от прошлой стоимости» делило на кэп, а не на $0.7429 — единственное
вбитое руками отношение в фазе, и ассерта на него не было. Теперь есть.

**Дисциплина, которая тут появилась.** План гейта коммитится **до** первого запроса, и раннер
проверяет это `git ls-files` + `git diff HEAD` — отказывает и на нетракнутом, и на изменённом
(вторая ветка — та, которую поверхностная проверка пропускает). Раннер закоммичен до прогона,
чтобы запись называла чистое дерево: попытка одна, переснять нельзя.

**Состояние.** `make check` 587 passed, `ruff format --check` чисто, 71 из 71 числа перевыведено,
дерево чистое на `3c946d4`. Деньги: $0.7794 из общего кэпа $1.50, остаток $0.7206; якорь
`3.163552735` записан `--dry-run`'ом до первого completion-запроса.
- 19:04: session ended (auto)
