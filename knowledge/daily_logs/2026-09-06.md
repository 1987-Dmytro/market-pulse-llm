---
type: daily_log
date: 2026-09-06
tags: [daily]
---

# 2026-09-06

## Summary

**День закончился ЗЕЛЁНЫМ баром.** Три сессии исполнителя: **s29** ($0, prep итерации 5), **s30**
($0, починка §0a ранбука по укусу верификатора) и **s31** — **ПЛАТНАЯ итерация 5, пятая и
последняя по PHASE §2**, которая ответила вопрос S2.

**Результат s31:** `--score --arm dev40` (БАР) — **subject 0.8857 ≥ 0.80 · signal 0.8667 ≥ 0.75**,
139 строк из **40 из 40** юнитов ноги A, **0 unparsed**; `grade_promo_signals.py` перевыводит оба
числа из файла. `--arm dev2` (ЧТЕНИЕ рядом) — subject 0.8883 · signal 0.8296.

**Рулинг (w) отвечен, и это был СЕРВИНГ, а не инструмент.** Рендер `@VARUS_channel:8647`,
14 281 символ, на котором итерация 4 умерла по CUDA OOM в `gemma4._norm`, вернулся за **313.9 с**,
`balanced True`, `finish stop` — на 32 ГБ карте с `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`.
**80/80 юнитов отвечены**, так что это НЕ неполное чтение PHASE §6.5. Ни один пин не двинулся.

**Деньги.** Под `xt4c5osyr36ew0` (RTX PRO 4500 Blackwell, 32 ГБ, $0.72/ч, EU-RO-1), 11:20:39Z →
12:38:57Z = 4698 с = **$0.9396 из капа $1.40**. Гард после прогона: `PROMO-ITER5 SPENT $0.9495 из
$1.40`. Цикл-3 **SPENT $7.3933 / REMAINING $1.6067** из $9.00. Новая целопрогонная скорость для
следующей ноги: **53.6254 с/тред (max 313.867, n=80)**.

## Decisions

- **[[the-registration-opens-the-line]]** (рулинг 06.09 **(y)** → PHASE **v14 §6.1**, PROCESS
  «Деньги» **v2.1**) — записано сегодня ADR'ом. `--register --step` читает гард, и это чтение
  СОЗДАЁТ и якорит леджер линии, значит регистрация и есть ОТКРЫТИЕ линии: она живёт в ПЛАТНОЙ
  сессии, за минуты до create. (x)2 отозван; (i) отклонён как правило на часах оператора, (iii) —
  «леджер не удаляют». **Проверено в тот же день:** якорь 11:06Z → пост-рановый `--note` 12:39Z =
  **H 1.55 ч**, дрейф `0.0079·1.55/(0.9396 + 0.0079·1.55)` = **1.29 %** против 5 % полосы; у
  итерации 4 все её 2.73 % были ровно этим членом за 2.45 ч.
- **PROCESS «Деньги» v2.1** несёт новый общий закон (тимлид): «где рулинг и эта секция спорят о
  МЕХАНИКЕ, побеждает секция — следовать ей, назвать противоречие в PROGRESS, без остановки».
  Применено сегодня дважды (см. ниже).

## s29 ($0) — prep итерации 5: карта и бэкстоп стали ПОЛЯМИ записи

`39022ac`, `31a6bc5`. **(x)3:** `CARD = "…RTX 4090"` был литералом, который `offered_price` сверял
руками — «≥ 32 GB» было рулингом, которому ни одна строка кода не могла подчиниться. Теперь `CARDS`
в порядке (w)2, берётся ПЕРВАЯ с ≥ `MIN_VRAM_GB`, стоком в EU-RO-1 и дорогим оффером ≤ $0.90/ч; оба
имени из ОДНОЙ строки листинга, `displayName` сверяется **ТОЧНО** (`RTX PRO 4500 SE` — другая карта
по той же цене в другом облаке). См. [[a_shifted_constant_has_physical_consumers]].
**(x)3 бэкстоп = минуты КАПА** — **116** против заимствованных у v5b **90**, которые убили бы
FITS-ран на $1.08. **(x)4:** ERROR-ответ читается везде, где читают ответы — рваная ПОСЛЕДНЯЯ строка
это гонка scp и **WAITING**, а не traceback на команде, решающей судьбу платящего пода.

## s30 ($0) — §0a: укус верификатора и срезы, которые сами себя проверяют

`c5a22f3` (тимлидовские по путям), `65d1285`, `4b996af`. **Главное:** `--pack` стоял МЕЖДУ
`--register` и коммитом, а `build_pack()` открывает запись через `committed_registration()`
(`git ls-files --error-unmatch` + `git diff HEAD --quiet`) — пак отказал бы на файле, который
`--register` только что переписал, **за $0, но с уже живым якорем**. Теперь ДВА коммита.
**Второй, более тихий дефект того же класса:** §0a просил оператора СЛОЖИТЬ три числа — ровно форма
провала 05.09, где три зелёные строки дали 3900 из 4321 и пропавший хвост ничего не сказал. Теперь
и покрытие, и пол — команды; негативный контроль `'146,200p'` печатает «SLICES DO NOT COVER THE
LIST — STOP». См. [[guard_selftest_negative_control]], [[make_check_exceeds_one_call]].

## s31 (ПЛАТНАЯ) — как прошли гейты, каждый показан

`--dry-run` → **листинг прочитан READ-ONLY ДО `--register`** ($0, ничего не создаёт): гард якорит
линию РАНЬШЕ, чем `offered_price()` читает предложение дня, так что ушедшая карта оставила бы живой
якорь без записи. Карта совпала с записью точно. `--register --step promo-iter5 --cap 1.40` →
**FITS на среднем углу $0.8470 (−39.5 %)**, dear $4.8633 над капом и НАЗВАН. Запись проверена ДО
коммита: cap 1.4 · `terminate_after_minutes` **116** · Blackwell · «iteration 5». Коммит 1 —
регистрация (`d598573`), коммит 2 — пак (`782bb7f`): исправление s30 сработало на живых деньгах.
`make check` на ОБОИХ HEAD: `ruff` чист, SLICES COVER, **1000+2030+1293 = 4323 passed / 2 skipped**
≥ 4266; §8 (l) пуст. §1 `STOP_AT` 13:16:14Z (связывают зарегистрированные 116 мин, а не остаток);
gpu-id ЧИТАЕТСЯ из записи; рунг 1 **GO**, бэкстоп $1.392 ≤ кап. §2 dead-man 500 с из записи, порт
на **27 с**. §3 HEAD пода == HEAD Мака, том тёплый. §5 `--smoke` **exit 0**. §6 fetch 80, delete,
оба листинга `[]`, `--close-segment`, ПОСТ-РАНОВЫЙ `--note`.

**Два отхода от буквального пастинга, оба названы в PROGRESS:** листинг read-only до якоря (выше) и
**живость по PID** — `pgrep -f promo_dev_pod_runner.py` ловит и раннер, и обёртку запуска, и
**собственную удалённую оболочку зонда**, поэтому счётчик никогда не падал ниже 1: гард, который не
может выстрелить, на платящем поде. Заменено на `[ -d /proc/184 ]` после `cat /proc/184/cmdline`.
Урок в память: [[long_run_watch_the_process]].

## s32 ($0) — «holdout-2 prep»: закрытие promo-iter5, потолок $10, нога holdout2, две починки, ранбук, dry-run

**Стартовый ритуал.** Тимлидовские по путям (`0896ba3`; потом `1e89ff3` — **(z) addendum 2 пришёл ПОСРЕДИ
сессии**), остатки s31 в `knowledge/` (`312ec89`). **Потолок $10.00** ((z) addendum): `CYCLE3_CAP_USD = 10.00`
+ нота в `spend_cycle3.json`, якорь не тронут (`6fa0a7e`). **`promo-iter5` ЗАКРЫТА $0.9450** (`ac22645`):
read-only обход ПЕРВЫМ, потом `--close` — **0.47 %** от пост-ранового $0.9495; строка гарда: **CYCLE 3 SPENT
$7.4179 of $10.00, REMAINING $2.5821** (14:21Z) — единственная леджерная строка сессии.
**Эталон holdout-2** появился на маунте в 16:33 и закоммичен по пути с shasum (`2bb9da7`): **112 строк /
40 тредов, `5525ddf1…`**. Не редактировался.

**Код (`42dfd97`).** `--part holdout2` = `HOLDOUT2_FILES` (draw-2, эталон, линия `promo-holdout2`, файлы
`promo_holdout2_*`); `use_part` биндит и `DRAW`, а `dev_threads` читает его В ВЫЗОВЕ — дефолтный аргумент
`path=DRAW` заморозил первую жеребьёвку при импорте, и holdout-2 читался бы из той самой жеребьёвки, от
которой он отделён ([[a_default_argument_freezes_the_constant_it_names]]). `by_part(part, **texts)`: каждое
решающее поле записи — по имени ноги, нога без текста отказывает (§4 v8); `if PART == "dev" else …` отдал бы
третьей ноге слова holdout-40, включая `--cap 1.10` рулинга (r)5 как authority
([[a_two_way_branch_on_a_three_way_parameter]]). Укус верификатора: без `--cap` нога holdout2 уходила в
dev-правило `min($2.50, REMAINING − $0.30)` под `floor_rule` «это число оператора» — теперь отказ ДО гарда.
**(y)4 риск 2:** `died(row)` = `"exception" in row or truthy error` — одно написание в пяти читателях.
**(y)4 риск 3:** `close_segment` суммирует сегменты ОТ ЯКОРЯ линии (`line_anchor` — леджер, который называет
запись); `spent_all_segments_usd` → `spent_this_line_usd` + `line`/`line_anchored_at`/`segments_of_this_line`;
нет леджера или закрываемый сегмент раньше своего якоря → отказ. Тест (w)3 расширен: (e) тихая смерть во всех
читателях, (f) обе стороны фильтра + два отказа, (g) отказ без `--cap`. Фикстура `staged` несёт поле `ledger`,
которое есть у каждой настоящей записи — инвариант не двигался. Число тестов НЕ выросло: 4323.

**Dry-run (`b05bd12`)** — перепрогнан ПОСЛЕ коммита эталона (первый, 16:29, писал «gold missing» за четыре
минуты до эталона; поймал верификатор — [[a_reading_that_outlived_its_state]]): 40 тредов, самый длинный
рендер 10 529 (< 14 281 итерации 5), эталон запиннен, темп 53.6254 с/тред (n=80). Rung 0 read-only на $0.72/ч
при капе $0.90: cheap **$0.5170**, priced $0.5728, dear **$2.8109** → FITS на mean, hard stop **4500 с =
75 мин** — цифра уведомления §6.2 (сошлось с ≈ $0.52 / ≈ $2.81 тимлида). `--register` НЕ вызывался.
**Ранбук (`d4bc013`)** переведён на выстрел; проверка четырёх пинов `d598573` — командой (прогнана: HOLD).
**Верификация:** воркфлоу 6 линз + по 2 опровергателя на находку (42 агента): 18 находок, 5 подтверждены
(дыра `--cap`; незакрытая кавычка в §0a; неверная фраза «без `--part` отказывает» — на деле dev-нога под линией
holdout-2; prep до эталона; ложный пример в докстринге) — все исправлены до коммитов, дешёвые ниты тоже.
`make check` на `d4bc013`: ruff чист, 1000+2030+1293 = 4323/2. Открытых стопов у исполнителя нет.

## Next

1. **Свежий верификатор → чтение тимлида** (`results/promo_holdout2_prep.json` + HEAD `d4bc013`); уведомление
   оператору по §6.2 — цифра выше — ДО открытия платной сессии.
2. **ПЛАТНАЯ сессия «holdout-2»**, впереди §0a ничего: `--register --part holdout2 --step promo-holdout2 --cap
   0.90` (FITS, четыре пина) → коммит → `--pack` → коммит → срезы → §0–§7 → `--score --part holdout2`
   (оба бара, ОДНА рука) → закрытие. Потом c3 ($0.50), потом том `mp-srv2` ((z)5).

## Blockers

- **`--step promo-holdout2` БЕЗ `--part holdout2` = dev-нога (80 тредов) под линией holdout-2:** `--register`
  заякорил бы `spend_promo_holdout2.json` и перезаписал бы замороженную запись итерации 5 — до того, как
  `committed_registration()` откажет паку ($0, живой якорь, `git checkout`). Отказ на чужой `--step` НЕ построен
  (~3 строки, не просили) — назван в PROGRESS; `--part holdout2` — несущий флаг, в ранбуке пишется всегда.
- Наивный `--created-at` без `Z` → TypeError на `--close-segment` (недостижимо: все восемь штампов с `Z`). ·
  Импортное `ARMS = ("dev",)` ≠ два плеча `DEV_FILES` до `use_part` (было и раньше; через `main` безвредно).
- **Запас:** REMAINING $2.5821 − $0.90 (забор) − $0.50 (c3) = **$1.18** до капели тома ≈ $0.24/день.
- **⛔ `promo-dev-loop` ОТКАЗЫВАЕТ** (над своими $2.50 с 05.09), закрыть нельзя ((r)4) — больше никогда не
  называется. · **`prereg_promo_holdout.json` ЗАПЕЧАТАН** под старой sha раннера. · Сюита ~12 мин на
  229 файлов — только срезами; тесты ГРЕПАЮТ `hot.md` и PROGRESS — не править, пока срезы идут.

## Sessions
- 11:32: s29 ended
- 12:59: s30 ended
- 15:0x: s31 ended — платная итерация 5, GREEN
- ~17:30: s32 ended — «holdout-2 prep», $0, до committed dry run

## Updated files
`docs/plans/promo-pulse-1.PROGRESS.md` · `results/` (прогон, обе руки, оценки, леджеры) ·
`knowledge/hot.md` · `knowledge/decisions/the-registration-opens-the-line.md` + `INDEX.md` ·
`scripts/runbook_promo_dev_1.md` (s30) · тимлидовские — по путям, не редактировались.
s32: `scripts/promo_dev_pass.py` · `tests/test_promo_dev_pass.py` · `scripts/runpod_guard.py` ·
`scripts/runbook_promo_dev_1.md` · `results/promo_holdout2_prep.json` (новый) · `results/spend_cycle3.json` ·
`results/spend_promo_iter5.json` (закрытие) · `docs/labels-promo-holdout2.jsonl` (тимлидовский, по пути).
- 16:02: session ended (auto)
- 16:03: session ended (auto)
- 16:35: session ended (auto)
- 16:56: session ended (auto)
- 17:02: session ended (auto)
- 17:07: session ended (auto)

## Checkpoint 17:34
Сессия s32 ВСЯ закоммичена, HEAD `dec4e74`, дерево чистое (кроме операторской `Claude outputs/`). Пункт
«holdout-2 prep» ((z)4 + addendum 2) закрыт на committed dry run: `--register` не вызывался, линия
`promo-holdout2` не открыта, пода нет. Сделано с прошлого чекпойнта (его не было — первый за день):
ритуал (тимлидовские по путям ×2, потолок $10.00, `promo-iter5` ЗАКРЫТА $0.9450), эталон holdout-2 по
пути (`5525ddf1…`), нога `--part holdout2` + две починки (y)4 в ОДНОМ тесте (w)3, dry-run с эталоном,
ранбук выстрела, adversarial-верификация (42 агента, 5 подтверждённых находок исправлены), `make check`
4323/2 на `d4bc013`. Память: [[a_default_argument_freezes_the_constant_it_names]],
[[a_two_way_branch_on_a_three_way_parameter]]; дописаны [[verbatim_quotes_must_be_grepped]] и
[[a_reading_that_outlived_its_state]]; `MEMORY.md` ужат до 15.5K единиц двумя тематическими индексами.
Открытых стопов нет; дальше — свежий верификатор → чтение тимлида → уведомление §6.2 → платный выстрел.
- 17:35: session ended (auto)
- 17:36: session ended (auto)
- 18:39: session ended (auto)
- 18:57: session ended (auto)
- 19:24: session ended (auto)

## Checkpoint 19:47 — s33: платный выстрел «holdout-2» куплен ОДИН раз и прочитан ПОЛНОСТЬЮ
Ритуал: (aa) / PROCESS v2.2 / PHASE v17 / STATUS по путям (`1bf1942`). Правка ранбука по (aa)3 (`215f80f`): гейт четырёх
пинов по ФАЙЛАМ НА ДИСКЕ, exit 1 ДО `--register`, одна цепочка `&&` до коммита регистрации; повтор регистрации = новая
линия `promo-holdout2-r2` (СТОП) или остаток линии; край жёсткого стопа закрывается по walk. Три read-only рефутера ($0):
0 опровержений, дешёвые находки в коммите. §0a: регистрация (`3193858`, якорь $7.04 в 16:19:45Z, FITS $0.5170, hard stop
4500 с), пак (`d519ad4`), `make check` 4323/2, пред-подовая note $0.0000 (`c4e4827`). Create ДВАЖДЫ отклонил классификатор
auto-режима — снято словом оператора «запускай» ([[the_paid_create_needs_the_operators_word_in_session]]). Под
`p3krn2lwhhcyyi` 16:45:21Z → 17:20:37Z, 2116 с = **$0.4232**; дым 12.6 / 26.8 / 219.4 с; 40/40, 0 unparsed; темп 40.8653
с/тред (n=40, max 219.4). **Чтение (`0c5f488`): signal 0.8021 HOLDS · subject 0.7054 RED (79/112)**; страты currency
0.766 / 0.7125, decimal_only 0.6615 / 0.8917; 33 промаха subject, семь из первых десяти — `chain` эталона. Пост-рановое
чтение $0.4107, цикл-3 REMAINING $2.1520; `--close` ОТКАЗАЛ («no billing rows yet») — повтор в следующем start-ритуале.
Отклонения: zsh не разбил `$SSHO` на слова, ≈ $0.03 пода ([[zsh_does_not_word_split_a_variable]]); монитор молчал из-за
`grep -c` ([[a_checker_whose_failure_is_silence]]); зависший ssh-запуск убит системой, раннер пережил через `setsid`
([[long_run_watch_the_process]]). PROGRESS `69b7db1`, 55 строк; открытых стопов нет; следующее слово — оператора.
- 19:50: session ended (auto)
- 20:31: session ended (auto)
- 20:42: session ended (auto)
