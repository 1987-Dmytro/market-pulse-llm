---
type: daily_log
date: 2026-09-06
tags: [daily]
---

# 2026-09-06

## Summary
<!-- what was done (concrete results), decisions, blockers; links — [[wikilinks]] -->

## Sessions
- 11:27: session ended (auto)

## Checkpoint 11:29 — s29: ruling (x)'s $0 prep, done except the registration

**Один айтем, $0, ничего не куплено и ни одной линии не открыто.** Новейший рулинг — **(x)**:
«next» перестал быть покупкой, итерация 5 идёт двумя сессиями (prep → чтение записи тимлидом →
платная). Посреди сессии пришёл **PROCESS «Money» v2** — он независимо формулирует три из четырёх
правок ниже как общий закон.

### Что сделано (`39022ac`, `31a6bc5`; тимлидовские по путям — `d2060b9`, `26582d8`, `e61ce6b`)
- **(x)3 карта — ПОЛЕ записи, не константа.** `CARD = "…RTX 4090"` был литералом, который
  `offered_price` сверял руками, — «≥ 32 GB» было рулингом, которому ни одна строка кода не могла
  подчиниться. `CARDS` в порядке (w)2; берётся ПЕРВАЯ с ≥ `MIN_VRAM_GB`, стоком в EU-RO-1 и дорогим
  оффером ≤ $0.90/ч. Оба имени из ОДНОЙ строки листинга: `displayName` сверяется **ТОЧНО**
  (`RTX PRO 4500 SE` — другая карта по той же цене, в другом облаке), `gpuId` идёт в `pod create`
  и `--open --card`. См. [[a_shifted_constant_has_physical_consumers]].
- **(x)3 бэкстоп = минуты КАПА** при зарегистрированной цене: **116 мин** против заимствованных у
  v5b **90**, которые убили бы FITS-ран на $1.08. В записи оба числа и то, какое живое.
- **(x)4 ERROR-ответ читается везде, где читают ответы.** `reply_rows` идёт через собственный
  `whole_lines` пода: рваная ПОСЛЕДНЯЯ строка (гонка scp с ЖИВОГО пода) — это **WAITING**, а не
  traceback на команде, решающей судьбу платящего пода. `answered_rows` выкидывает мёртвый юнит, и
  ставка `--close-segment`, `--project` и `--score` считают его **НЕОТВЕЧЕННЫМ** — денежный гейт
  пишется РАНЬШЕ строки ставки. Смерть — не ошибка парсинга: `dead_units` её называет.
- **`ITERATION = 5`**, `re_emission` раскрывает карту, её gpu-id, цену и `expandable_segments`.
  **Ранбук перенаправлен** с `_holdout` на итерацию 5 целиком; gpu-id **читается из записи**
  (`prereg_promo_dev_loop.json :: rung_0.price.card`), а не набирается со страницы — иначе
  ре-эмиссия в день, когда PRO 4500 нет в стоке, даёт KILL на рунге 1 корректно созданному поду.

### Проверки
`runpodctl gpu list` за $0: **`RTX PRO 4500`, 32 GB, `NVIDIA RTX PRO 4500 Blackwell`, $0.72/ч
secure, EU-RO-1 сток High** (A6000 48 GB $0.53 — сток `none`; L40S — не в EU-RO-1).
Рунг 0 посчитан за $0, ничего не записывая: cheap **$0.8470** (−39.5 %) · priced $0.9028 · dear
$4.8633 → **FITS на среднем углу**, hard stop **7000 с**. `ruff` чист; `pytest` **4323 passed,
2 skipped, exit 0** двумя срезами (§8 (j) хочет ≥ 4266). Один тест добавлен (бэкстоп, обе стороны),
единственный тест (w)3 РАСШИРЕН четырьмя чтениями (x)4.

### Открытый стоп — якорь, а не код
`--register --step promo-iter5` **создаёт** `results/spend_promo_iter5.json` сегодняшним балансом.
(x) ставит это на сессию раньше пода, PROCESS v2 открывает линию «ПЕРЕД подом» в §0 платной.
Закрытие считает `own_resources` (always-on кинды ВНЕ), а его допуск сверяется с пост-рановым
`--note` — балансовой дельтой, где они ВНУТРИ. Дрейф = **возраст якоря**, и это измерено:
`promo-iter4` $0.7143 − $0.694815 = **$0.019444 — ровно его строка `network-volume`**; все 2.73 %
были томом за 2.45 ч. При $0.0079/ч и $0.85 пода **5 % достигается за ≈ 6 ч**, на дешёвом ране —
раньше. Три средства расписаны в PROGRESS; выбор — оператора/тимлида.
Урок в память: [[a-settlement-and-its-reference-measure-different-kinds]].
- 11:32: session ended (auto)

## Checkpoint — s30: стоп закрыт рулингом (y), §0a ранбука исправлен ($0)

**Один айтем, $0, ни пода, ни вызова гарда, ни строки леджера.** Рулинг **(y)** ответил вчерашнему
стопу вариантом **(ii)**: `--register` читает гард с `--step`, и это чтение **СОЗДАЁТ и якорит**
`results/spend_promo_iter5.json` — значит **регистрация и есть ОТКРЫТИЕ линии**, и она живёт в
ПЛАТНОЙ сессии, за минуты до create. (x)2 («prep заканчивается закоммиченной регистрацией») отозван.
(i) отклонён как правило на часах оператора, (iii) — «леджер не удаляют».

### Что сделано
- **Тимлидовские по путям (`c5a22f3`):** PHASE **v14** §6.1, PROCESS «Деньги» **v2.1**, STATUS,
  рулинг (y) и строка паттернов 06.09. v2.1 несёт новый общий закон: **«где рулинг и эта секция
  спорят о механике, побеждает секция — следовать ей, назвать противоречие в PROGRESS, без остановки»**.
- **Айтем (`65d1285`) — §0a ранбука, пять мест.** Главное — **укус верификатора**: `--pack` стоял
  МЕЖДУ `--register` и коммитом, а `build_pack()` открывает запись через `committed_registration()`,
  который гоняет `git ls-files --error-unmatch` и `git diff HEAD --quiet` по
  `results/prereg_promo_dev_loop.json` и падает `SystemExit` на любом. То есть пак отказал бы на
  файле, который `--register` только что переписал, — **за $0, но с уже живым якорем линии**.
  Теперь **ДВА коммита**: запись первой, пак вторым. Плюс: заголовок §0a («первые минуты ПЛАТНОЙ
  сессии», а не «$0-половина»), авторитет (+(y), §6.1 v14, PROCESS v2.1), `make check` того HEAD
  расписан как `ruff` + три среза с **ОТКРЫТЫМ хвостом** (`'146,$p'`), и §6-я фраза «прочитай
  рулинг, ЕСЛИ регистрация и под не в одной сессии» — ветка, которая больше не может выстрелить.

### Проверки — отказ ПОКАЗАН, а не заявлен
Запись сделана грязной ровно так, как её оставляет `--register` → `build_pack()` **exit 1**,
«differs from HEAD … Commit it before the pack»; восстановлена побайтно (sha `208b227a…`),
`git status --porcelain results/` пуст. `grep -rln runbook_promo_dev_1 tests/` пуст — правка доки
не может покраснить сюиту; 12 тестовых файлов, читающих изменённые сегодня доки: **318 passed**.
Полная сюита ЗАКРЫТЫМИ срезами **60+85+84 = 229** файлов: **1000+2030+1293 = 4323 passed,
2 skipped**, `ruff` чист (§8 (j) хочет ≥ 4266). **Ни теста, ни пина, ни гарда, ни леджера не добавлено.**

### Противоречие, названное по v2.1 (без остановки)
`knowledge/hot.md` нёс «⚠️ `make check` НЕ в платной сессии»; (y)3 и PROCESS v2.1 ставят его
ВНУТРЬ §0a, до create. Секция побеждает — hot.md исправлен. Оттуда же удалена «карта дня»
($0.72/ч, gpu-id): это ЗАПОМНЕННОЕ число, а §0 перечитывает листинг и §1 берёт gpu-id из записи.
Уроки на завтра (названы, не построены): (y)4 риск 2 — исключение с ПУСТЫМ текстом читается как
ОТВЕЧЕНО; (y)4 риск 3 — `close_segment` суммирует все сегменты партии. Оба — первый $0-пункт ПОСЛЕ
итерации 5. Следующая сессия — **платная итерация 5, кап $1.40, пятая и последняя**.
- 12:35: session ended (auto)

## Checkpoint 12:56 — s30 (продолжение): срезы §0a теперь сами себя проверяют

После первого чекпоинта нашлась вторая, более тихая версия того же класса, что и укус верификатора.
**§0a просил оператора сложить три числа** («the three `passed` lines sum to ≥ 4266») — ровно форма
провала 2026-09-05, где три зелёные строки дали 3900 из 4321, а пропавший хвост ничего не сказал.
Просьба об арифметике стоит в единственной точке, где **якорь линии уже живой**, а пода ещё нет.

**`4b996af`:** покрытие срезов стало командой — `test "$(ls tests/test_*.py | wc -l)" -eq "$(…сумма
диапазонов…)"`, а пол — вычисляемой суммой трёх хвостов против 4266. Обе прогнаны здесь: COVER на
сегодняшних 229 файлах, сумма **4323** из настоящих строк-хвостов, FLOOR HOLDS; **негативный
контроль** `'146,200p'` (перечислённый хвост — форма s25) печатает «SLICES DO NOT COVER THE LIST —
STOP». См. [[guard_selftest_negative_control]], [[make_check_exceeds_one_call]].
Заодно проверено для платной сессии: `promo_dev40_pack.json` — один и тот же путь в коммите §0a, в
аргументе раннера §4 и в `promo_dev_pass.PACK` (без суффикса `_iter5`, в отличие от prep/replies).
PROGRESS ужат обратно до 60 строк **удалением строки, а не переливкой** ([[to_cut_lines_delete_lines]]):
первая попытка перелила и дала те же 61.

**Итог дня:** две сессии исполнителя (s29, s30), обе $0. Стоп s29 закрыт рулингом (y) вариантом (ii).
Ничего не куплено, гард не вызывался, `results/spend_promo_iter5.json` не существует.
Следующая — **платная итерация 5**, кап $1.40, пятая и последняя.
- 12:59: session ended (auto)
