<!-- AUTO-GEN START (refreshed by scripts/refresh-hot-cache.py) -->

# Hot Cache

**Auto-refreshed:** 2026-08-25 22:24:29 (every SessionStart)
**Branch:** `main`

## 🔀 Recent commits (top 5)

```
76aba89 docs(report): the closing reading -- 3 905/2 green, and the span stops being typed
0b18366 docs(report): lora-c-migrate-r2 -- 79 of 79 re-derived, and the KILL is rung 5's own
a004ba6 feat(migrate-r2): D2 -- the volume is built and the model loads from it in 40.22 s
997a2a2 chore(migrate-r2): the step ledger anchored at $13.02, before the volume
4475907 test(migrate-r2): every command of the paid session, driven at $0
```

## 📋 Recent decisions

- `lora-c-the-card-cannot-train-at-3072.md` — 32 GB cannot train this line at 3 072 — and the base columns say the prompt already moved
- `INDEX.md` — Decision records
- `lora-c-arm-b-rendering-and-the-two-leg-pass-2.md` — A synthetic row is a v3 QUERY — and pass 2 runs for the arms only

## 📅 Recent daily logs

- `2026-08-25.md`
- `2026-08-24.md`
- `2026-08-23.md`

<!-- AUTO-GEN END (everything below preserved across refreshes) -->
# Hot Cache — curated

**Last update:** 2026-08-25 22:40 **`lora-c-migrate r2` — ПЕРЕЕЗД СОСТОЯЛСЯ, $0.287267 из $1.50.**
Том **`mp-lora-c` (`soymlju8q0`, 100 GB, CA-MTL-3)** создан, веса на нём (62.58 GB, 0 неполных
блобов, снапшот = запиненная ревизия), и **модель загружена с него на A100 PCIe 80 GB за 40.22 с,
17.046 GiB из 79.138** штатным `local_llm.load`. Под удалён, 744 с. Ноль шагов, ноль эвалов, попытка
r2 цела, `mp-srv2` не тронут. Рунг 5 всё равно дал **KILL** — своим же правилом (см. Footguns).
Отчёт `docs/reports/lora-c-migrate-r2.md`, 79/79 перевыведено.

> Этот блок курируется руками. Археология закрытых контрактов живёт в отчётах, ADR и логах дня — не
> восстанавливать сюда то, у чего есть дом. Ценз буста мерить ДО и ПОСЛЕ каждой правки.

## 🔥 What's Hot

**📏 ПЕРВЫЕ ЗАМЕРЫ СТАДИЙ НА ЭТОЙ ЛИНИИ (под A100, CA-MTL-3, 25.08).** ssh **35.3 с** из 500 ·
**venv 132.0 с** из 900 (первый замер стадии в репе вообще: `python -m venv` + `pip install -e
'.[dev,gpu]'` + импорт torch — три вещи в одном числе, не разложены) · закачка **62 580 183 517 B за
184 с** из 1200 (**340.1 MB/s**; собственные часы `hf` — 02:32, т.е. 411.7 MB/s) · **загрузка
40.22 с** из 600. Четыре стадии съели **12.2%** отпущенного, под — **20.7%** хард-стопа.

**🎯 A6000 ЗА $0.53 СНОВА ДОСТИЖИМА — В US-KS-2.** В 18:01Z её не было ни в одном томопригодном ДЦ
(на этой таблице писался рулинг (с)); в 19:48Z она `Low` в US-KS-2 — как и **L40 $0.82**, **RTX 6000
Ada $0.84** и **та же A100 $1.39**. Четыре карты ≥48 GB на один том. Оператору показано с числами до
создания тома, выбран **CA-MTL-3, как в рулинге**. Сток мерцает поминутно (три чтения A100 за 17
минут — три разные строки); связывает только сам create.

**💸 ЧТО ОСТАТОК r2 ($3.2394) ПОКУПАЕТ.** Только **$0.53 → 102.84 с/шаг** проходит единственный
замер репы 61.047. $0.82 → 50.96 · $0.84 → 48.71 · **$1.39 (наша A100) → 12.08**. То есть r3 на
A100 требует свежего капа (**вилка $5–6.5**), r3 на A6000 мог бы влезть в уже зарегистрированный
остаток — **но 48 GB на 3 072 не мерил никто**, а 32 GB отказали дважды.

**✅ ЗАКРЫТО, живёт в отчётах/ADR:** потолок **3 072** · протечка элементов E (22 → 0) · линия B не
тронута · вайтлист `check-stamped` + `knowledge/index.md`, `hot.md` снаружи поимённо · у арма B
датасет 666 строк ([[lora-c-arm-b-rendering-and-the-two-leg-pass-2]]) · базы на E: **v2 61/98 ·
v3 66/98**, заряд v3 = **9.20 с/звонок** (рулинг (п)).

## ⏭️ Next

1. **КОНТРАКТ r3 ПИШЕТ ТИМЛИД** — смок на A100 покупает с/шаг на 3 072, дальше кап словом
   оператора на этих числах. Том тёплый: `hf/` 59 GiB, `venv/` 1.8 GiB, `repo/` 146 MB на
   `997a2a2`, три пака и оба SFT внутри. Повторной закачки НЕ НУЖНО.
2. **`money-anchors` ($0)** — девять открытых шагов. `lora-c` ЗАКРЫТ, `lora-c-vramprobe` ОТКАЗАН
   структурно и остаётся открытым (см. Blockers), `lora-c-migrate-r2` открыт.
3. **prep-b** — коллекция Сільпо/Varus/Маркетопт ($0), после читательского тракта.

## 🚧 Blockers

**⛔ ДВА ТОМА ТЕПЕРЬ КАПАЮТ ~$0.4667/СУТКИ.** `mp-srv2` (EU-RO-1, сервинг) + `mp-lora-c` (CA-MTL-3,
обучение), по ~$0.2333 каждый. Цикл-2: **$9.7306 из $20.00, остаток $10.2694** — это ~**22 дня**
простоя обоих. Останавливается только удалением тома, и это решение оператора.

**⛔ 32 GB НЕ ТЯНУТ 3 072 — ИЗМЕРЕНО ДВАЖДЫ, ВТОРОЙ РАЗ С ЛЕКАРСТВОМ.** Ноль шагов оба раза,
`micro_batch` понижен до 1 самим трейнером. Понижение потолка НЕ ПОМОЖЕТ: `max_seq_len` — гард на
энкоде, `collate` паддит по длиннейшей строке В МИКРОБАТЧЕ. **48 GB не проверялись вообще.**

**⛔ `make fmt` ОБНУЛЯЕТ ПИНЫ ПРОДЮСЕРОВ — ОБА РАСФОРМАТИРОВАННЫХ ФАЙЛА ЗАПИНЕНЫ.**
`scripts/write_lora_c_prereg.py` → `a4eb2893…` в `results/prereg_lora_c.json::producer`;
`scripts/build_lora_c_marker_census.py` → `a947947e…` в `results/lora_c_marker_census_pack.json`,
который сам запинен внутри замороженной регистрации. `make check` гоняет `ruff check`, а не
`ruff format --check`, и не видит ни того, ни другого. Форматировать ТОЛЬКО по путям.

**⛔ ОТНОСИТЕЛЬНЫЙ ДОПУСК ГАРДА НЕДОСТИЖИМ НА ДЕШЁВОМ ШАГЕ.** Обе записи линии отстают от settled
примерно на одну АБСОЛЮТНУЮ величину — $0.031152 (r2) и $0.026229 (проба), — что даёт 4.2% и 53.0%.
При допуске 0.07 нужен шаг дороже ~$0.37–0.45. Проба переспрошена через 5 ч 06 м — те же два числа
до цента, значит это не задержка расчёта. n = 2; полосу не трогали — не наша область.

**⛔ БАР 3 КРАСНЫЙ, И v3 ЕГО НЕ ЧИНИТ** (`@VARUS_channel:10366`, 2 сигнала против 0). Прирост v3
весь в `не_наш_рынок`. **ПРОХОД-1 НЕ БЕРЁТ БАР НИКАК:** ни базой, ни LoRA (9/14), ни v2 (+7 из +10).
Арм B не измерен. Плюс две недостижимости: девять строк `@VARUS_channel:10367` не рендерятся, у арма
A ноль таргетов `молочный_бренд` (у B — 32).

**⛔ preflight СЛЕП К `config_sha256`.** У поля нет соседа-пути, поэтому `config/qlora.yaml` не
попадает в реестр пинов: старые пины читаются 38 → 38 через смену ревизии. Проверять перечисленным
диффом записи, не preflight'ом.

**🧹 BOOT TAX ~10.8K ПРОТИВ 10.7K — РЫЧАГ НЕ В ЭТОМ ФАЙЛЕ.** По файлам: `MEMORY.md` **24.4 KB**
(обрезан окном при сырых 26.2 — записи теряются) · `hot.md` ~11.2 · CLAUDE.md 4.6 + 2.0. **~57%
сбора — индекс памяти.** Консолидация `MEMORY.md` — настоящий рычаг и отдельное решение.
Мерить `context-census.py` ДО/ПОСЛЕ.

## 🔫 Footguns этого файла

**⛔ РУНГ 5 МИГРАЦИИ ГРЕЙДИТ БУХГАЛТЕРИЮ КЭША КАК ДОКАЧКУ.** Он требует, чтобы `du -sb
/workspace/hf` не сдвинулся за загрузку. Сдвинулся на **40 байт**: `refs/main` (40 B) + четыре
ПУСТЫХ маркера `.no_exist`. Блобов записано **0**. Правило зарегистрировано ДО чтения и после
чтения не правилось — KILL записан как свой; инвариант для следующего контракта: **не растут
БЛОБЫ**, а не каталог кэша. Разбор — `results/lora_c_migrate_r2_load_bytes.json`.

**⛔ `knowledge/hot.md` грепается как ЦЕНОВОЙ ВХОД.** `scripts/volume_calc_5c1.py` берёт отсюда
**один** литерал через `quoted(HOT, …)`, и его отсутствие роняет девять тестов
`tests/test_volume_calc_5c1.py`. `make check` — после правки ЭТОГО файла, не только кода.

- **Литерал живой и нагруженный:** 100 GB сетевого тома стоят **~$0.24/day** — это ЦЕНА ЗА ТОМ, а
  не состояние счёта. Тот том фазы 4a в CA-MTL-3 был удалён; сегодня в CA-MTL-3 стоит новый
  (`mp-lora-c`) и капает по этой же ставке, плюс `mp-srv2`. Литерал ОБЯЗАН стоять ровно так,
  по-английски со слэшем: `scripts/volume_calc_5c1.py :: quoted(HOT, "~$0.24/day", 0.24)` грепает
  его ИЗ ЭТОГО ФАЙЛА как вход. Перевод слова `day` уже уронил девять тестов один раз (12.08).
- **A stopped pod with no network volume still bills its container disk.** 80 GB is about what the
  100 GB network volume costs per month. Если под нужен на одну сессию — **delete**
  (`runpodctl pod delete`, `pod terminate` не существует), не stop. Остановленный под — не
  остановленный счёт, и `runpodctl pod list` показывает только running: смотри `pod list -a`.
