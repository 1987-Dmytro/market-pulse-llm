<!-- AUTO-GEN START (refreshed by scripts/refresh-hot-cache.py) -->

# Hot Cache

**Auto-refreshed:** 2026-09-09 15:42:16 (every SessionStart)
**Branch:** `main`

## 🔀 Recent commits (top 5)

```
ff33267 docs(progress): s39 — «c3-prep-2» DONE at $0, no open stop. All six sub-items of (gg)6: the issued harness file in by Write (`diff` 0, sha `fae771af…`) with the v2.6 check shown MISSING/1 before and OK/0 after; the runbook's §0 cut to gate 1 `&&` the fields check, the create-prefix gate and its §2 note deleted rather than weakened, both gates run both ways; the projection's `rel()` derives every named file from the path `build()` read, so the c3 record stops saying `promo_pagecount_c2.json` over 30 pages it took off the c3 file and `reads.pagecount` names it. The control ran to a scratch `--out` so nothing under `prereg_promo_c2.json` moved, and it is stricter than the one (gg)5 asks for: my edit's only delta against the parent commit's own producer is the single added `"pagecount"` line. Deviation `stale-live-input`: «the default path re-writes C2's projection byte-identical» is unreachable as worded and was already unreachable with my change absent — that pinned record embeds a live guard reading ($4.8000, 2026-09-01) and an 11-row ledger against $1.4762 and 31 rows today, 40 differing lines. c3 re-written `fc015d45…` → `5ea83a55…` with the four expected moves and every priced number unmoved; the dry run re-run at the cap derived from the live guard line — min($0.50, $1.4762 − $0.30) = $0.50 — reproduces 30 pages + 8 posts, `pagecount: match`, dear corner $0.1864, FITS at −62.7%. §4 gains the volume delete after the listings and only on a complete run record, syntax off the CLI's own help. `make check` green at `2004216`: ruff clean, 4326 passed / 2 skipped in five foreground slices whose union over the 230 files on disk is proved (overlap 0, missing 0) with a negative control that refuses. hot.md's curated block restamped: the two closed blockers replaced by the rule they leave behind (Read tool for team-lead files, Write tool for the harness), the unreproducible C2 projection named, HEAD and REMAINING moved. Next: the fresh verifier → the team lead's reading → «c3» (paid) in another fresh process
2004216 s39(c3-prep-2): the allow rules leave the harness and the runbook, and the projection stops naming a file it did not read — $0, no pod, no registration
bd27cb1 knowledge(save): s38's checkpoint — hot.md restamped on ruling (gg)'s eve, the 09.09 log and the Stop-hook index, committed by path at the next start
2c1d63c docs(team-lead): ruling 09.09 (gg) + the harness file it issues — s38 «c3-prep» ACCEPTED ($0); (l)3 RULED as its own $0 item «chain-fold» after c3 (shape (b)); the two allow rules RETIRED as inert in bypass; `harness-permission` closed from the docs; «c3-prep-2» ($0, a FRESH process) precedes «c3»; PROCESS v2.6, PHASE v23
f26c286 s38(c3-prep): the c3 leg BUILT and PRICED at $0 — four records, the runbook with its three §0 gates, the dry run FITS at −62.7%
```

## 📋 Recent decisions

- `INDEX.md` — Decision records
- `a-published-number-has-one-reader.md` — A published number has ONE reader, and a missing source is a refusal by name
- `the-registration-opens-the-line.md` — The registration OPENS its line, so §0a lives in the PAID session

## 📅 Recent daily logs

- `2026-09-09.md`
- `2026-09-08.md`
- `2026-09-06.md`

<!-- AUTO-GEN END (everything below preserved across refreshes) -->

# Hot Cache — curated
**Last update:** 2026-09-09 s40 «c3-prep-3»: ранбук и регистрация ЧИСТЫ, $0; открытых стопов НЕТ.
Этап 1 `SPEC-v2`, карта `STATUS.md`, цикл v3, фаза **v24**, процесс **Money v2.6**. Руками, ≤40 строк.

## 🔥 What's Hot — нога c3 готова к ПОКУПКЕ по кап $0.80 (слово оператора, (hh)3)
**Отгружаемое (петля хуки + P1, `grade_promo_loop_readings.json`):** dev-3 **0.7411 (83/112) ❌ 0.80**
· сигналы **0.7937 ✅ 0.75**; dev-40 0.8857 / 0.8667, dev-2 0.9043 / 0.8296 — одинаковы обоими путями.
Чтения рядом как «reading» (0.7500 / 0.8021, сырьё 0.7054, holdout-40 0.7181), НЕ переписываются;
ничьи dev-3 — **16 из 28**; holdout-3 — РЕЗЕРВ. **c3 за $0:** якорь 2026-09-05, 29 медиапостов,
**30 страниц ТОЧНО**, 30/30 на диске; dry run 30 стр. + 8 постов, `pagecount: match`, дорогой угол
**$0.1864** против капа **$0.80** — **ВЛЕЗАЕТ (−76.7%)**. Верификатор s39 читал NOT SAFE при капе
$0.50 (in-run pack gate кончал ногу ДО первой страницы) — **кап $0.80 и есть починка**, код денег НЕ
трогали. Ранбук `promo_c3_paid_leg.md`: §0 = ключ API → штамп → поля v2.6 → **КАП КОМАНДОЙ**
(min($0.80, REMAINING − $0.30), вниз до цента, пол $0.15 → exit 1), §1 — ХВОСТ того же вызова,
§3 — `nohup` + PID-файл + короткие поллы (НЕ `| tee`), §5 — закрытие с `--expect-ms` из записи,
**том удаляется ПОСЛЕ закрытия**.

## ⏭️ Next — узкий верификатор по диффу s40 → чтение тимлида → «c3» (платно) в свежем процессе
Кап c3 = min($0.80, REMAINING − $0.30) — **берётся гардом В ТОЙ сессии**, не отсюда. §0 → §5.
Дальше: «chain-fold» ($0, форма (b), (gg)2 — сдвигает отгруженный P1, число перемеряется функциями
продукта) → e2e на чистом клоне + `draw_truth_20` → гейт **12.09**.

## 🚧 Blockers / долги (названы, не построены)
**Файлы тимлида — только Read-tool; харнес-файл — только Write-tool, `diff` = пруф.**
**⚠️ `promo_projection_c2.json` НЕ ВОСПРОИЗВОДИМ производителем** (живое чтение гарда + растущий
ledger внутри sha-закреплённой записи): пруф такой записи — её пин, не перепрогон.
**Деньги: гард печатает `REMAINING $1.4664`** (том пьёт ≈$0.24/day); запас над капом $0.6664.
`make check` **4326/2** на `474eaf0` (пять срезов, объединение доказано); HEAD `6065c04`, подов НЕТ,
$0 за сессию. · K12 читает настоящий `results/`, `tick.REGISTRY` — константа.
· `tick.py --window` дефолт `w2` — ранбук несёт `--window all`. · **⛔ `promo-dev-loop` ОТКАЗЫВАЕТ.**
**⚠️ zsh НЕ разбивает `$SSHO` · ⚠️ НЕТ `timeout` на macOS · ⚠️ НЕТ `setsid` на macOS (локально —
`nohup`; «ТОЛЬКО setsid» = правило УДАЛЁННОГО раннера) · ⚠️ живость по PID · ⚠️ K4 НЕ ПЕРЕЗАПУСКАТЬ ·
⛔ `1925810730` · ⛔ `aggregates.py` и `open`**.
## 🔫 Footguns этого файла
**⛔ ЭТОТ ФАЙЛ ГРЕПАЕТСЯ КАК ВХОД — ДВА ЛИТЕРАЛА.** `scripts/volume_calc_5c1.py :: quoted()` берёт обе строки ниже посимвольно; пропажа любой роняет девять `tests/test_volume_calc_5c1.py`.
- 100 GB сетевого тома стоят **~$0.24/day** — цена ЗА ТОМ, а не состояние счёта; с 27.08 том один,
  `mp-srv2`. Литерал обязан стоять ровно так, по-английски со слэшем.
- **A stopped pod with no network volume still bills its container disk.** 80 GB is about what the
  100 GB network volume costs per month. Под на одну сессию — **delete** (`runpodctl pod delete`),
  не stop; `runpodctl pod list -a` показывает только running, смотри `pod list -a`.
