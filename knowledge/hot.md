<!-- AUTO-GEN START (refreshed by scripts/refresh-hot-cache.py) -->

# Hot Cache

**Auto-refreshed:** 2026-08-19 11:00:40 (every SessionStart)
**Branch:** `main`

## 🔀 Recent commits (top 5)

```
3529bad docs(report): vault-dream -- the porcelain block says which moment it is, and names the hook that moves it
921f2a8 docs(report): vault-dream -- the ceiling came from the loader's own constants, and a citation is not a record
cf01503 brain(vault-dream): the index is consolidated 197->147 with 51 proven homes, and hot.md carries what stayed open
1be0ba2 docs(team-lead): STATUS accepts pass1-data-prep and registers the MEMORY.md blocker ruling; PROMPT-vault-dream queued
e629569 chore(brain): the vault tail of the pass1-data-prep session -- the third session's log and the index
```

## 📋 Recent decisions

- `INDEX.md` — Decision records
- `sitting-b-line-b-and-the-team-lead-labels.md` — The sitting rules line B — labelled `subject_type` + LoRA, and the TEAM LEAD does the labelling
- `guard-until-the-tolerance-that-was-a-window-bug.md` — `guard --until` — the tolerance did not re-derive because the table measured its own window bug, and the operator closed three by ruling

## 📅 Recent daily logs

- `2026-08-19.md`
- `2026-08-18.md`
- `2026-08-17.md`

<!-- AUTO-GEN END (everything below preserved across refreshes) -->
# Hot Cache — curated

**Last update:** 2026-08-19 `/save`, поверх `vault-dream` — **день трёх контрактов, и каждый закончился отказом, который
оказался поставкой.** `pass1-probe-b` ИЗМЕРИЛ проход-1 и ПРОВАЛИЛ бар P1 (9 из 14 при пороге 12) →
STOP. `guard-until` — гейт СРАБОТАЛ, контракт не закрыл НИЧЕГО, затем оператор постановил полосу 7%
и три леджера ЗАКРЫТЫ. **Сидение выбрало линию B**, и `pass1-data-prep` собрал пак разметки:
**500 юнитов под seed, экзамен вынесен с доказательством.** Отчёты `pass1-probe-b.md` (Dv490–499),
`guard-until.md` (Dv500–517), `pass1-data-prep.md` (Dv518–523).
`make check` **2 879 → 2 911 → 2 927 → 2 959 / 2 skipped**. Детали — [[2026-08-18]].
Четвёртым — `vault-dream` ($0): MEMORY.md **197 → 147 строк**, boot tax **14.5K → 13.1K**.

> Этот блок курируется руками. Археология закрытых контрактов живёт в отчётах и ADR — не
> восстанавливать сюда то, у чего есть дом.

## 🔥 What's Hot

**🔄 ТЫ ЗДЕСЬ.** Линия промпт-инженерии читателя закрыта своим stop-правилом, архитектура **D**
выбрана, её микро-замер СДЕЛАН и ОТРИЦАТЕЛЕН, и сидение 18.08 выбрало **линию B**: размеченные
`subject_type` + LoRA, **разметку делает ТИМЛИД**. Пак построен. Следующий ход — **разметка (сессия
тимлида)**, потом **LoRA-регистрация**. [[sitting-b-line-b-and-the-team-lead-labels]]

**🏷️ ПАК РАЗМЕТКИ ГОТОВ — ЧИТАТЬ И РАЗМЕЧАТЬ.**
`docs/label-pack-pass1-r1.md` (278 694 знака, 120 тредов ЦЕЛИКОМ, 500 целей помечены `⬛ TARGET`) ·
машинный `results/pass1_label_pack_r1.json` · слепые 40 `docs/label-pack-pass1-r1-blind40.md`
(опция оператора, НИЧЕГО от неё не зависит). Метки — **только `subject_type`**, четыре чтения плюс
JSON `null`, и `null` это ОТВЕТ («комментарий ни о ком»), а не пропуск. Писать в
`docs/labels-pass1-r1.jsonl`, проверять:
`PYTHONPATH=src python3.11 scripts/validate_pass1_labels.py docs/labels-pass1-r1.jsonl`
(шесть именованных отказов, зелёный путь печатает распределение и НЕ ПИШЕТ ничего).
⚠️ **`docs/labels-pass1-r1.jsonl` — файл ТИМЛИДА** с момента появления: исполнитель валидирует и
коммитит дословно, ни строки не правит и метку не генерирует.

**🎲 ПАК ДЕТЕРМИНИРОВАН И ПЕРЕСОБИРАЕТСЯ ПОБАЙТОВО.** `SEED = 20260818` модульной константой (не
флагом — опция seed это опция собрать другой пак) · вес `w = min(payable, CAP)` · наибольший остаток
с ТОТАЛЬНЫМ ключом тай-брейка · **CAP = 14 это 90-й перцентиль**, пере-выводимый при каждом запуске.
Достижимость посчитана ДО регистрации: при CAP=9 потолок **483 < 500** и пак было бы нечем набрать.
Крупнейший тред (125 платных) взял **12 из 500 = 2.4%** вместо 65 без капа.
⚠️ **Продюсер пинит сам себя** — `producer.sha256` это sha скрипта, поэтому ЛЮБАЯ правка
`scripts/build_pass1_label_pack.py` (хоть комментарий) краснит тест до пересборки трёх файлов.
Пересобирать, НЕ перепинивать: seed делает это безопасным.

**🚫 ЭКЗАМЕН ВЫНЕСЕН, И ЭТО ДОКАЗАНО.** Исключены **7 тредов**, несущих любой из 64 пробных юнитов
(шире, чем «14 голд-строк и их треды» сидения). 64 юнита — **в точности платное множество** этих
семи, поэтому 1 032 − 64 = **968** точное, а не оценочное; цель `min(500, 968) = 500`.
Четыре пустых списка в записи, причём голд-id проверяются по **ОТРЕНДЕРЕННОМУ тексту**, не по
списку юнитов: тред печатается целиком, id может быть на странице, не будучи выбранным.
⚠️ Кодбук цитирует закон атрибуции v5 и F2a **теми же байтами** из модуля, а рулинг r2 — с
**объявленной купюрой**: его поле `reading` называет голд-строку, её голд-значение и два барных
числа. [[the_codebook_quoted_the_answer_key]]

**🎯 ЧТО ИМЕННО ПРОВАЛИЛОСЬ У ПРОХОДА-1 — НЕ ТРАНСПОРТ И НЕ СЛОВАРЬ.** Все пять потерь бара P1 —
`subject_type`; `stance` прав на всех трёх строках, где считается. **Две потери — модель отказалась
типизировать вообще** (`subject_type: null` при чистом парсе), и ценз говорит то же в масштабе:
**25 из 50** соседних строк без типа **при нуле отказов на 64 вызовах**. Воздержание — это ОТВЕТ.
Симметричный коллапс — **no-op**. Это и есть аргумент за B. [[an-abstention-is-an-answer]]

**💵 ЦЕНА ПРОХОДА-1 ИЗМЕРЕНА: 5.162 с/вызов.** Окно: 1 032 платных комментария × 5.162 с =
**$1.0950** генерации как **ПОЛ** — без бута, без стейджинга, без второго пода.

**💰 ДЕНЬГИ СЕЙЧАС: цикл-2 $1.8146 из $20.00, свободно $18.1854.** Якорь `$22.5097614388`
(`2026-08-16T12:14:48Z`). Фаза 4 ЗАКРЫТА на **$32.4708 из $33.00** и НЕ перескоривается.
Закрытые шаги: probe-b $0.313162 · reader-v3 $0.393577 · reader-v4 $0.236118 · reader-v5 $0.150095 ·
reader-v5b $0.312592 · **45h2 $8.217558 · srv2b $0.939957 · srv2d $1.163191** (три последних — 18.08,
по рулингу оператора о полосе 7%).

**🧰 ГАРД УМЕЕТ КОНЕЦ ОКНА.** `--until <ISO>` бортит ОБА читателя обхода. Два гейта ДО записи:
**полнота** (`--expect-ms` против `timeBilledMs`, полоса `MS_BAND` 1% ДВУСТОРОННЯЯ) и **доллар**
(`--tolerance`, **обязательный, без дефолта**). Измерено: `--bucket-size` **не меняет итог**;
`--until` бортит ОБХОД, но не balance-delta — у дельты нет окна, поэтому открытый шаг дрейфует
всегда, и остановит его только ЗАКРЫТИЕ. Шаг цени ПО БИЛЛИНГУ: на старом леджере дельта
ОТРИЦАТЕЛЬНА (счёт пополняли), так что «два чтения, пессимистичное биндит» на закрытии недоступно.

**🔎 ПРЕФЛАЙТ — ИНСТРУМЕНТ, А НЕ ПРИВЫЧКА.** `make preflight ARGS='<имена>'` (пилот H1): греп-хиты по
корням · проза, цитирующая ЗНАЧЕНИЕ · реестр пинов (**1 466** путей), сджойненный с хитами запроса ·
sha256 против запиненного. Три формы пина: `{"sha256","module"}`, карта `producer.borrowed`
(путь = КЛЮЧ) и `frozen_when_the_pod_exists` (заморозка без дайджеста).
Гонять ДО правки того, что трогаешь: он уже нашёл, что сам гард разошёлся со своим пином.

**🧬 ИДИОМЫ, КОТОРЫЕ ДЕРЖАТ.** Продюсер зовёт продюсера и отказывается, пока тот не пересоберёт
замороженную запись байт в байт · перечисленный дифф утверждается в ОБЕ стороны · записи ворот
APPEND · пре-регистрация + one-attempt + stop-rules · парный прогон в `--outdir` как доказательство
детерминизма · право исполнителя на отказ.
⚠️ Непрозрачный диапазон в перечисленном диффе закрывается руками:
[[a-hash-is-not-the-claim-it-carries]].

**⚙️ ТРАНСПОРТ ПРОХОДА-1 ЧИСТ И ПЕРЕИСПОЛЬЗУЕМ.** Промпт `pass1_comment_gm4_v1` (`5a4a3cb6…`) несёт
закон атрибуции **по ссылке** (`READER_ATTRIBUTION_LAW_V5` — те же байты, которыми спрашивали
читателя), домен — ЧЕТЫРЕ чтения. Запись `prereg_pass1_probe_b.json` (`a29e85ad…`) + пак
(`bbbe6420…`), 64 юнита = 14 голд + 50 ценз по 7 тредам. Скорер ЗОВЁТ
`scorer.reader_comment_agreement`, ничего не переписывая.
⚠️ `bars.…scored_fields` в записи ПУСТ, настоящее правило — в `scored_fields` каждой строки голда
(Dv494): названо, не чинится — объектно равно замороженной записи.

**💥 ОДИН ФЛАГ — ДВЕ КОМАНДЫ.** `--gate` маршрутизируется по СОСТОЯНИЮ, а не по флагу: пусто на
диске → ветка бут-дедлайна, есть строки → ветка проекции полного прохода. Обе в суите.
[[one-flag-two-branches]]

**🧾 РЕГИСТРАЦИЯ — ЭТО ВХОД ДЛЯ ЧУЖОГО КОДА.** Транспорт читает запись ПО ИМЕНАМ полей; у
`pass1-probe` не было `money.arithmetic.boot_deadline_rule`, и `--deadlines` упал `KeyError` при
работающем метре. Гнать запись через ВСЕ читающие её команды за $0 до заморозки.
[[a-frozen-record-is-an-input-to-shipped-code]]

**⚙️ Карта RTX 4090, EU-RO-1, цены 16.08:** 4090 $0.74 (Low) · RTX PRO 4500 Blackwell $0.72 (High) ·
RTX PRO 4000 Blackwell $0.57 (Medium) · L4 $0.49 (Low). **Цену читать в ДЕНЬ прогона** из `costPerHr`
ответа `create`, а не из записи (Dv448 — закон).

**📊 Ячейка ценза читателя: 129 тредов · 1 032 платных коммента**
(`results/gate_census_w1_reader.json`, narrow · varto off · plus-spam/scam on). ⚠️ **129 — это ТАКЖЕ
thread-count `narrow|silencers_off`**; различает только платный счёт (1 032 против 1 116).
Самый большой тред — **125** платных. За вычетом семи экзаменационных: **122 треда · 968 платных**.

## ⏭️ Next

1. **РАЗМЕТКА — сессия ТИМЛИДА, параллельно следующему контракту.** Вход — `docs/label-pack-pass1-r1.md`,
   выход — `docs/labels-pass1-r1.jsonl`, гейт — `scripts/validate_pass1_labels.py`. См. карточку пака
   выше.
2. **LoRA-регистрация линии B** — после меток: конфиг тренировки, кап и прогноз цены ОТ ЗАМЕРОВ
   фазы 4 (H6-формулы), бар — тот же **запечатанный gold r2 ≥12/14**, и **аблация меток тимлида
   (train with/without) — закон этой регистрации**, названный заранее. Заморозка provenance-копии
   пака в `results/` (`labelled_by`, дата, кодбук, seed, sha пака) — шаг того же контракта.
3. **Денежная гигиена (`money-anchors`, $0)** — правая часть закрытия шага, судьба зависшего walk'а
   `pass1-probe`, дисциплина `anchored_at` для 19 отказанных леджеров. По слову оператора.
4. **Boot tax ≤9K — теперь РУЛИНГ, а не консолидация.** `vault-dream` исполнен; 9 323 Б трёх
   `CLAUDE.md` + 24 583 Б `hot.md` = **8.30K ДО первого байта MEMORY.md**, поэтому ≤9K достижимо
   только если ЭТОТ файл упадёт до ~8.1K Б (−67%). Что резать здесь — слово оператора.

**Кандидаты цикла-2 в порядке ценности:** окно-2 ≈$4.8 · батч-пробник $2.13
(`results/batch_cycle2_projection.json`: единственный кандидат **batch 2**, влезет ли — НЕ ИЗМЕРИМО
без живого пробника; риск не память, а ОТВЕТЫ — при batch 16 разошлись 6 строк из 666) · сбор
листовок Сільпо / Varus / Маркетопт (бесплатно) · докачка корпуса · история 11 143 (ждёт батча И
денег) · serving-латенси ОТЛОЖЕНО ([[serving-latency-deferred]]).

**Долги-входы следующих регистраций:** цена страницы листовки **10.408 с/стр (n=159)**, старые записи
НЕ перескориваются. Ценз `results/gate_census_w1.json`: narrow|silencers_on = **111 тредов, 912
платных комментариев** (популяция ПРОБ — отдельный файл от читательской ячейки). Два вопроса из §5
отчёта fix-a и метка типа поста для окна-1. Мелочи: заголовок `t5_depth`, валюта ₴ в шапке
промо-таблицы, fix-b(2) «две колонки — одно число».

## 🚧 Blockers

**⛔ `pass1-probe`: ОБХОД ЗАВИС на 300 000 из 427 000 мс** — без движения больше 12 часов. Если
биллинг не допостит недостающие 127 000 мс, закрыть шаг нечем и **нужен рулинг**, чем закрывать.
Открытое чтение дрейфует **$0.2075 → $0.4305** при капе $0.20 — это капель тома на плече
balance-delta, НЕ перерасход прогона (ресурс шага $0.087772). [[a-settlement-is-a-reading-too]]

**⛔ `pass1-probe-b`: обход ПОЛНЫЙ (+0.104%), а закрытие отказано ЧЕСТНО.** Правило сверяет settled
($0.135538) с записанным чтением леджера ($0.0853), а оно **отстающее — 64.9% правды**: `$0.0853189556`
это в точности тот же обход, обрезанный на 09:52:00, за семь минут до снятия баланса. **Нужен рулинг
о ПРАВОЙ ЧАСТИ** закрытия: чтение леджера или запись прогона.
[[a-lagged-reading-is-not-a-second-opinion]]

**⛔ 19 старых леджеров отказаны по именованным причинам** — нет якоря (10), нет окна (5), плюс
`5c1_vis` (ключ якоря не матчится по имени шага) и `probe_a` (окно нулевой ширины). Полосой это не
чинится: форма следующего денежного контракта — **дисциплина `anchored_at`** плюс правая часть.
И толеранс из контрольной таблицы pass1-probe **не выводится** — таблица мерила баг ОКНА, а не
разброс биллинга. [[a-spread-that-is-really-a-window]]

**⛔ ПРОХОД-1 НЕ БЕРЁТ БАР В НЫНЕШНЕМ ВИДЕ — 9 из 14 при пороге 12.** Ответ сидения — линия B, и
он куплен НЕ результатом, а правом попробовать: двинет ли супервизия `subject_type` — измерит
LoRA-регистрация против того же запечатанного бара.

**⛔ БУТ НА ЭТОМ СТЕКЕ — НЕ КОНСТАНТА.** 192.1 / 237.2 / [267, 293] с на одной карте, одном томе и
одних весах — **1.53×**, и ничто в прогонах этого не объясняет. Заряжать по МАКСИМУМУ замеров,
потолок ставить над ним — и не приписывать подъёму потолка результат, которого он не производил
(18.08: старый потолок 300 с прошёл бы с запасом 57.8 с). [[the-fix-that-was-not-a-fix]]

**⛔ ЗАПАС НА ПРОВИЖЕН НАДО ПОКУПАТЬ, А НЕ НАДЕЯТЬСЯ.** Четыре бута на этом стеке расходятся в
**6.7×** (179 · 373 · 175 · >1 200 с). Дедлайн вида `usable − projection` — не запас, а ставка на
платформу. [[a-negative-pre-generation-budget-is-a-forecast]]

**✅ СУИТА СНОВА ЗЕЛЁНАЯ: 2 960 / 2 skipped** (было 2 957 / 2 FAILED). Метки дословно, фриз
`results/labels_pass1_r1.jsonl` с provenance-пином и ОБА переворота тестов легли ОДНИМ коммитом
**`8d2e1ba`**: свежий clone не видит `docs/labels-pass1-r1.jsonl` без тестов, которые его
пломбируют. Тест, утверждавший отсутствие, — это часы, а не верификатор.
[[the-absence-test-is-a-clock-and-flips-with-its-artifact]]

**⛔ BOOT TAX: ЦЕЛЬ ≤9K ПРИОСТАНОВЛЕНА (рулинг 2, 19.08) — `hot.md` ради числа НЕ РЕЖЕМ.**
MEMORY.md **148 строк / 18 587 Б** = **74.0% и 74.3%** потолка загрузчика **200 / 25 000**, обе оси
под баром 75%; карта «что ушло → где лежит» — `docs/reports/vault-dream.md`. Ось census теперь ось
загрузчика (Dv526, сдвиг −1 Б), `TARGET_KTOK` и текст предупреждения НЕ двигали: «> 9.0K» читается
как «цель на пере-регистрации». Совместное сидение стартует с таблицы горячее/холодное (`hot.md`
поблочно + три `CLAUDE.md`, 9 323 Б) в `docs/reports/labels-boot-audit.md`; пере-регистрация — от
измеренного высокосигнального пола, владелец — оператор.
[[boot-tax-target-suspended-and-the-census-axis]] **Dv521 всё ещё ждёт дома** («блок записи, чьи
поля пришли из разных строк: два `max()` по разным ключам развязались на тринадцати ничьих»).

## 🔫 Footguns этого файла

**⛔ `knowledge/hot.md` грепается как ЦЕНОВОЙ ВХОД.** `scripts/volume_calc_5c1.py` берёт отсюда
**один** литерал через `quoted(HOT, …)`, и его отсутствие роняет девять тестов
`tests/test_volume_calc_5c1.py`. `make check` — после правки ЭТОГО файла, не только кода.

- **Recorded rather than open:** том CA-MTL-3 удалён, его **~$0.24/day** больше не капают — литерал
  нагруженный, не украшение, и он ОБЯЗАН стоять ровно так, по-английски со слэшем:
  `scripts/volume_calc_5c1.py :: quoted(HOT, "~$0.24/day", 0.24)` грепает его ИЗ ЭТОГО ФАЙЛА как вход.
  Перевод слова `day` уже уронил девять тестов один раз (12.08).
- **A stopped pod with no network volume still bills its container disk.** 80 GB is about what the
  100 GB network volume costs per month. Если под нужен на одну сессию — **delete**
  (`runpodctl pod delete`, `pod terminate` не существует), не stop. Остановленный под — не
  остановленный счёт, и `runpodctl pod list` показывает только running: смотри `pod list -a`.

**⛔ Суита НЕ идёт в голом `git worktree`** (Dv492, подтверждено 18.08): `data/*` в gitignore кроме
`data/frozen/`, и `tests/test_train_qlora.py` падает на СБОРЕ. И `[ -e ] || cp -R` не чинит —
`data/annotation` (**252 МБ из 313**) частично трекается, поэтому проверка «уже есть» промахивается
мимо 250 МБ медиа. Рабочий способ: чекаут в ОСНОВНОМ дереве на чистом `git status --porcelain`,
прогон там, возврат на `main`; пины проверять через `git show`. [[a-commit-must-run-its-own-suite]]

**🔫 ПРОДЮСЕР — НЕ ВЕРИФИКАТОР (Dv474, стоило рулинга оператора).** Запуск `volume_calc_5c1.py`
«чтобы проверить литерал» ПЕРЕЗАПИСАЛ его же результат и стёр операторский блок `executed`.
Проверять вход — ЧИТАТЕЛЕМ (`calc.quoted(...)`, `calc.inputs()`), никогда `main()`; после любой
команды, которая может писать, смотреть `git status --porcelain`. [[the-producer-is-not-the-verifier]]

**Деньги — три числа, которые легко перепутать.** Шаг цени ПО БИЛЛИНГУ с исключением всегда-включённых
видов ПО ВИДУ. Фазу и линию — пессимистичным максимумом двух чтений. Дельта баланса меряет СЧЁТ, а не
шаг, растёт после закрытия вечно и на старом леджере бывает ОТРИЦАТЕЛЬНОЙ (счёт пополняли). Остаток
ВЫВОДИТЬ (`CAP - spent_usd`), а не читать из `sessions[-1].remaining_usd`.
