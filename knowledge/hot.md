<!-- AUTO-GEN START (refreshed by scripts/refresh-hot-cache.py) -->

# Hot Cache

**Auto-refreshed:** 2026-08-18 19:53:53 (every SessionStart)
**Branch:** `main`

## 🔀 Recent commits (top 5)

```
a401cff docs(report): pass1-probe-b -- pass 1 is MEASURED, bar P1 is 9 of 14, and the run cost $0.135050
4e78cd0 run(pass1-probe-b): 64 of 64 read for $0.135050, and bar P1 FAILS at 9 of 14
dfdf636 anchor(pass1-probe-b): the step counter, before the first pod of this attempt exists
f880977 docs(report): pass1-probe-b -- the walk is still partial, and the registration was driven before it froze
9b9e7a5 prereg(pass1-probe-b): the same instrument, one boot constant, before the pod exists
```

## 📋 Recent decisions

- `INDEX.md` — Decision records
- `reader-programme-closed-and-the-architecture-sitting-17-08.md` — The reader's prompt-engineering programme is CLOSED by its own stop-rule, and the sitting of 17.08 rules architecture D
- `reader-v4-closed-and-sitting-17-08.md` — reader-v4 accepted as an honest negative, and the sitting of 17.08 rules instrument v5

## 📅 Recent daily logs

- `2026-08-18.md`
- `2026-08-17.md`
- `2026-08-16.md`

<!-- AUTO-GEN END (everything below preserved across refreshes) -->
# Hot Cache — curated

**Last update:** 2026-08-18 `/close` — **`pass1-probe-b` ИСПОЛНЕН: ПРОХОД-1 ИЗМЕРЕН, И БАР P1
ПРОВАЛЕН — 9 из 14 при пороге 12. ВЕРДИКТ STOP, ОГОВОРКА ВОЗВРАТА К СИДЕНИЮ ВЗВЕДЕНА.**
Под `zn3chzmo1xricf` прочитал **64 из 64 за 657.0 с = $0.135050** из капа $0.20: гейт-0 GO за
**14.5 с** (третий подряд), бут **237.2 с**, `DONE` сам, **ноль отказов**, все 64 распарсены,
`finish_reason: stop` везде. Отчёт `docs/reports/pass1-probe-b.md`, девиации **Dv490–Dv499**,
`make check` **2 879 → 2 911 / 2 skipped**. Детали — [[2026-08-18]].

**🎯 ЧТО ИМЕННО ПРОВАЛИЛОСЬ — ЭТО НЕ ТРАНСПОРТ И НЕ СЛОВАРЬ.** Все пять потерь — `subject_type`;
`stance` считается на трёх строках и прав на всех трёх. **Две потери — модель отказалась
типизировать вообще** (`subject_type: null` при чистом парсе), три — другое прочтение. Разрез:
`категория_личное` **5 из 9** · прочие типы **3 из 4** · `stance` **3 из 3**. Симметричный коллапс —
**no-op**: голд r2 пишет `категория_личное`, текст прохода-1 предлагает ровно это слово, пять строк
на нём и сошлись. Ценз: **25 из 50** соседних строк без типа, 24 без стенса, **и ни одного отказа** —
воздержание это ОТВЕТ, а не сбой парсера. [[an-abstention-is-an-answer]]

**💵 ЦЕНА ПРОХОДА-1 ИЗМЕРЕНА: 5.162 с/вызов** — **0.81×** зарегистрированной границы (граница
выдержала) и **1.76×** подгонки (подгонка недооценила; гейт на ней был бы оптимистичен во столько
же). Окно: 1 032 платных комментария × 5.162 с = **5 327 с = $1.0950** генерации как **ПОЛ** — без
бута, без стейджинга, без второго пода.

**🚨 БУТ ИЗМЕРЕН ТРЕТИЙ РАЗ — 237.2 с, И ПОДЪЁМ ПОТОЛКА СЕГОДНЯ НИЧЕГО НЕ СПАС.** Три замера
на одной карте, одном томе и одних весах: **192.1 / 237.2 / [267, 293] с — разброс 1.53×**, и ничто
в прогонах этого не объясняет. При СТАРОМ потолке 300 с первый ответ (308.2 с create-elapsed против
дедлайна 366 с) прошёл бы с запасом **57.8 с**. Рулинг всё равно верен — дедлайн покупают против
худшего случая, — но приписывать ему результат нельзя. Заряд 300 с оказался консервативен на 62.8 с;
потолок 420 с не был затронут. [[the-fix-that-was-not-a-fix]]

**🎯 ИТОГ ФАЗЫ ЧИТАТЕЛЯ: 3 бара из 5, и оба целевых провалены дважды подряд.**
Вердикт `results/reader_v5b_verdict.json` (`8c975558b8da31cf…`), плечо A — те же 23 треда:

| бар | v5b | v4 | порог |
|---|---|---|---|
| `1_flagships` | **FAIL 2 из 5** | FAIL 2 из 5 | 5 из 5 |
| `2_entity_cases` | PASS **4 из 4** | PASS 4 из 4 | 4 из 4 |
| `3_noise` | PASS | PASS | 0 сигналов |
| `4_per_comment_agreement` | **FAIL 0.4286** (6/14; по отвеченным 6/10 = 0.600) | FAIL 0.500 (7/14; 7/11 = 0.636) | ≥ 0.80 |
| `5_time_and_cost` | **OPEN** — обход не постнулся | PASS $0.236118 | кап |
| плечо B `m1`–`m4` | **ВСЕ PASS** — чанкинг работает с первого раза | — | механические |

**Инструмент v5 НЕ сдвинул бар 4 ни в одном чтении.** Эхо-обязанность при этом выполнена
**117 из 117, ноль absent**; потолок 4000 токенов ни разу не сработал (максимум 2056 = 51%);
26 из 26 ответов сбалансированы, стоп срезал 62 символа на 19 юнитах.
⚠️ **Три из четырёх отказов парсинга — сам транспортный стоп**, взявший сбалансированный объект,
который не был ответом; четвёртый (`malformed JSON`) унёс 4 строки из знаменателя бара 4.
[[a-structural-stop-accepts-a-structural-non-answer]]

**💰 ЛИНИЯ ЦИКЛА-2: потрачено $1.6769 из $20.00, свободно $18.3231** (чтение 2026-08-18
09:59 UTC). Якорь `$22.5097614388` (`2026-08-16T12:14:48Z`). Фаза 4 ЗАКРЫТА на $32.4708 из $33.00.
Шаги: probe-b **$0.313162** · reader-v3 **$0.393577** · **reader-v4 $0.236118** ·
**reader-v5 $0.150095** · **reader-v5b ЗАКРЫТ $0.312592** (обход постнулся ЦЕЛИКОМ) ·
**pass1-probe $0.087772 — ШАГ НЕ ЗАКРЫТ** · **pass1-probe-b $0.135050 — ШАГ НЕ ЗАКРЫТ**.
⚠️ **Закрывать только по ПОЛНОМУ обходу.** Закрывающий путь гарда берёт обход ОДИН, без
пессимистичного максимума против дельты баланса: в 17:23Z обход по v5b предлагал $0.0917 —
**29% правды** — и заморозил бы её навсегда. Дискриминатор: `timeBilledMs` против `billed_seconds`
в записи прогона. Сейчас: `pass1-probe` **300 000 из ~427 000 мс**, `pass1-probe-b` **обход пуст**
при ~657 000 мс. И пока шаг открыт, его delta-плечо копит каплю сетевого тома: `pass1-probe`
читает уже **$0.2075 из своих $0.20** и отказывает — это том, а не под.

**⏭️ С ЧЕГО НАЧАТЬ.** (1) **ПРОВЕРИТЬ ОБА ОБХОДА** и закрыть те шаги, что сошлись:
`runpodctl billing pods --pod-id <rx89zok35e5ca6|zn3chzmo1xricf> --grouping podId --bucket-size hour`
против 427 000 / 657 000 мс — **на частичном обходе НЕ закрывать**; (2) **гард `--until`** +
перезакрытие 22 старых леджеров ($0) — money-path, отдельный контракт, и у него теперь два новых
клиента; (3) **сидение по проходу-1** — вход `docs/reports/pass1-probe-b.md`.

## 🔥 What's Hot

**🧪 ПРОХОД-1: ИНСТРУМЕНТ ЖИВ, ОБЪЕКТНО РАВЕН И ПЕРЕИСПОЛЬЗУЕМ.** Промпт
`pass1_comment_gm4_v1` (`5a4a3cb6…`, 2521 симв.) несёт закон атрибуции **по ссылке** —
`READER_ATTRIBUTION_LAW_V5`, те же байты, которыми спрашивали читателя, поэтому находка о ФОРМЕ
ЗАДАЧИ, а не о переформулированном законе. Домен — **ЧЕТЫРЕ** чтения (в 195 `per_comment` строках
v5b слово «категория» не встречается ни разу). Записи: `prereg_pass1_probe.json` `76bbf561…` (не
измерена) и **`prereg_pass1_probe_b.json` `a29e85ad…`** (измерена) + пак `bbbe6420…`. **64 юнита =
14 голд + 50 ценз** по 7 тредам, перечислены из файлов; контекст сущностей — из УЖЕ КУПЛЕННЫХ
вердиктов. Скорер ЗОВЁТ `scorer.reader_comment_agreement`, ничего не переписывая.

**🧬 ИДИОМА «ПРОДЮСЕР ЗОВЁТ ПРОДЮСЕРА» ОТРАБОТАЛА ТРЕТЬЕ ПОКОЛЕНИЕ.**
`write_pass1_prereg_b.py` вызывает `write_pass1_prereg.build()` БЕЗ подмен и отказывается, пока тот
не пересоберёт замороженную запись байт в байт; только потом строит второй раз под подменой
модульных глобалов. Весь дифф — **28 перечисленных путей, утверждённых в ОБЕ стороны** (сдвинулся
неперечисленный → отказ; перечисленный не сдвинулся → тоже отказ), три негативных контроля стреляют.
⚠️ **Непрозрачный диапазон в перечисленном диффе закрывается руками:** таблица гейта пропущена как
ОДИН путь, поэтому порядок юнитов, каждый из 64 вердиктов и угол без бута сравниваются напрямую, с
контролем, который переворачивает один вердикт. [[a-hash-is-not-the-claim-it-carries]]

**💥 ОДИН ФЛАГ — ДВЕ КОМАНДЫ (главный урок дня).** `--gate` маршрутизируется по СОСТОЯНИЮ, а не по
флагу: пусто на диске → ветка бут-дедлайна, есть строки → ветка проекции полного прохода, и вторая
читает платные счётчики пака, остаток капа на сегмент и usable-секунды, которых первая не касается.
До 18.08 она **ни разу не прогонялась до пода**. Теперь прогнана дважды за $0 и обе в суите
(`tests/test_pass1_transport_b.py`, на `tmp_path`). [[one-flag-two-branches]]

**🧾 РЕГИСТРАЦИЯ — ЭТО ВХОД ДЛЯ ЧУЖОГО КОДА.** Транспорт читает запись по именам полей.
`read_threads_reader_v4.deadlines` берёт `money.arithmetic.boot_deadline_rule` — у `pass1-probe` его
не было, и `--deadlines` упал `KeyError` при работающем метре на записи, которую уже нельзя чинить.
В b-записи ключ **добавлен**, регрессия закреплена с двух сторон (на старой обязан бросить, на новой
вернуть правило). Правило Dv486 из урока стало ПОСТАВКОЙ: гнать запись через все читающие её команды
за $0 до заморозки. [[a-frozen-record-is-an-input-to-shipped-code]]

⚠️ **`bars.…scored_fields` в записи ПУСТ, а настоящее правило живёт в голде** (Dv494): считаются
поля, названные КАЖДОЙ строкой голда — 11 строк `subject_type`, 2 `subject_type`+`stance`, 1 только
`stance`. Не чинится: объектно равно замороженной записи. `which_binds` печатает читательскую прозу
на пас-1 записи (Dv495) — числа верны, предложение нет; `read_threads_reader_v4.py` запинен по sha.

⚠️ **Суита freeze-коммита НЕ идёт в голом worktree** (Dv492): `data/*` в gitignore кроме
`data/frozen/`, и `tests/test_train_qlora.py` падает на СБОРЕ. Симлинки делают хуже — часть
`data/annotation/` трекается, git читает её как удалённую. Рабочий способ: оставить основное дерево
на том HEAD, доказать пустой `git status --porcelain` и гонять там; пины проверять отдельно через
`git show` в throwaway-worktree.

**🧮 ПАЧКА 22 ЛЕДЖЕРОВ: ЗАКРЫТО НОЛЬ, И ЭТО ФАКТ КОДА.** У `closing_record()` нет ветки «закрыть на
записанном чтении», а гард не передаёт `--end-time` (хотя `runpodctl billing` его принимает), так
что `--close --since <старое окно>` посчитал бы шаг по всему, что билось **с той даты и до сейчас**:
$86.49 «расчётных» против ~$13.4 записанных на семи леджерах, `srv2d` в **10.26×**. Ограниченный
обход сходится с записями в 6–7% — это контроль. Обе ветви отказа проверены хэшем по всем 29
леджерам, ни один байт не сдвинулся. Долг передан С ФОРМОЙ: **научить гард `--until` и перепрогнать
таблицу** — это money-path с тестами, отдельным контрактом.

**🔐 ВТОРОЙ МОМЕНТ ЗАПЕЧАТЫВАНИЯ: `SEALED_AT_PASS1 = e657056`** (дерево, под которым под v5b читал
26 юнитов). Регистрация текста в `prompts.py` двигает модуль, а его старые байты пинят ЧЕТЫРЕ
закоммиченных записи (прережки и паки v5/v5b) — они НЕ перепиниваются. Побочка: гард объектного
равенства в `write_reader_prereg_v5b.py` перестал собираться; сужен до **двух перечисленных путей**,
дыра закрыта `the_readers_law_is_unmoved()` (четыре текста читателя + четыре домена сравниваются
напрямую), обе половины драйвятся на руками собранных парах.

**🛑 КЛАСС «ДВА ОБЪЕКТА» УБИТ ТРАНСПОРТОМ, А НЕ ПРОМПТОМ.** `reader_v5.balanced_prefix` считает
глубину скобок ВНЕ строковых литералов (экранирование обработано) и режет генерацию на первом
сбалансированном top-level объекте; сохраняется РОВНО испущенный префикс, рядом `cut_chars` и
`balanced`. Строка промпта и refuse-on-conflict парсера остаются обороной, но лечение — здесь.
**Что доказано:** срез — на всех 23 оплаченных ответах v4 (парс префикса даёт вердикт, идентичный
парсу целого). **Что НЕ доказано на этой машине:** сама ЭКОНОМИЯ — `stop_at_balanced` требует GPU,
несёт `# pragma: no cover` и впервые сработает на поде.

**🧮 БЮДЖЕТ — НЕ ELAPSED (Dv462).** Таблица бутов v4 считала `usable − pre_generation_budget − boot`;
при большей проекции бюджет ушёл в **−44.8 с** и вычитание начало ПРИБАВЛЯТЬ. Таблица v5 работает на
ИЗМЕРЕННОЙ ноге v4 (39.0 с от `pod create` до первой строки генерации), а отрицательный бюджет
опубликован отдельно и означает конкретное: **affordability-дедлайн 675.2 с связывает раньше
двенадцати минут, всегда**. [[a_budget_is_not_an_elapsed]]

**🔢 ДВА ЗНАМЕНАТЕЛЯ (Dv463).** `tokens_per_requested_row` — токены по РАСПАРСЕННЫМ тредам
(15 299 / 111 = **137.83**, опубликованное число v4), а не по всем оплаченным (18 637 / 111 = 167.9);
скорость же токен/с — наоборот, по всем, потому что отказавший ответ всё равно стоил секунд. Смешать —
переоценить ногу B на 22%, и вниз по течению этого не видно.

**🔥 ГЕЙТ ДОЛЖЕН СТОЯТЬ НА ТОМ, ЧТО ТРАТИТ — подтверждено двумя прогонами в один вечер.**
v3: go/no-go мерил прогрев, прогрев не наступил, serverless-бут съел $0.3918 при капе $0.35 —
**ни одна ветка инструмента выстрелить не могла**. v4: под, кап в СЕКУНДАХ, два дедлайна на одной оси,
`--gate` возвращает **3=WAIT / 2=KILL / 0=GO**. Бут 175 с и виден всю дорогу. **23 из 23 за $0.2361
против $0.3936 за ноль.** [[a-gate-downstream-of-the-spend]]

**💸 ЦЕНУ КАРТЫ ЧИТАТЬ В ДЕНЬ ПРОГОНА (Dv448 — закон).** $0.74/ч в записи v5 помечено как
**ПРИМЕР**, а метром служит `costPerHr` из ответа create; всякая цифра пересчитывается ИЗ НЕГО до
старта генерации. Что было в EU-RO-1 16.08: 4090 $0.74 (Low) · RTX PRO 4500 Blackwell $0.72 (High) ·
RTX PRO 4000 Blackwell $0.57 (Medium) · L4 $0.49 (Low).

**📐 ПРОЕКЦИЯ ОСТАТКА — ДВЕ НОГИ, СВЯЗЫВАЕТ ПЕССИМИСТИЧНАЯ.** В v5 ЮНИТ — это запрос (тред ноги A
или чанк ноги B), ноги проецируются отдельно и читаются в порядке A→B: стоп внутри B оставляет A
целой и её четыре бара посчитанными. Порог публикуется в секундах; доллары на границе печатают одно
и то же. [[a-published-ratio-is-not-the-gates]]

**🗃️ ЗАПИСИ ВОРОТ ТЕПЕРЬ APPEND.** `results/reader_v5_run.json` держит `gates` СПИСКОМ, каждый
снимок WAIT/GO/KILL дописывается. У v4 первый GO-снимок вытеснился ре-гейтом на двенадцати тредах,
арифметику пришлось перепроверять руками по jsonl — сошлось, но это везение, а не свойство.
⚠️ И `--gate` теперь ЧИТАЕТ ПАК (проекции нужны платные счётчики на юнит): пак обязан лежать по
`results/reader_v5_pack.json`, отсутствие — названный отказ, а не traceback на живом поде (Dv468).

**⚠️ `--gate` читает КОПИЮ файла, который пишет под.** «Ответ не пришёл» — утверждение о ФАЙЛЕ.
scp partial-jsonl обязан идти ПЕРЕД каждой проверкой; в записи ворот есть `read_from` с путём и
временем копии.

**📐 ЧЕТЫРЕ зарегистрированных текста читателя, номера 1, 2, 3, 5 — и v5 OPT-IN.**
v1 `b272115637f784ad…` · v2 `9d281bc80f18c91b…` · v3 `22533644cf35420e…` (6 630 симв.) ·
**v5 `c529d279321269e1…` (9 779 симв.)**. **v4-текста НЕТ и не было**: reader-v4 зарегистрировал
ТЕКСТ v3 на поде, номер идёт за контрактом, и пробел — честное имя этому.
`reader_messages_gm4` держит дефолтом **v2**, и `part=None` рендерит байт-в-байт (доказано на 23 из 23
зарегистрированных запросов v4 после сдвига sha `prompts.py` `dfa7a79f…` → `be5a8164…`).

**🧬 ПРИМЕРЫ В ПРОМПТЕ — ТОЛЬКО СИНТЕТИЧЕСКИЕ, И ЭТО КРАСНЫЕ ВОРОТА.** Пять предложений пар 1–4
проверены на **129 файлах / 43 871 545 символов** (оба стора комментов, посты, inferences, оба голда,
эталон) — в двух чтениях (как есть и нормализованное). **С негативным контролем:** фраза ИЗ стора
находится теми же двумя сравнениями, иначе «не найдено» могло бы быть свойством матчера.

**🔧 Парсер терпим к КОНТЕЙНЕРАМ и строг к ДОМЕНАМ — ровно три ремонта, и v5 их НЕ ТРОГАЕТ.**
(1) два top-level объекта сливаются, **и ключ в обоих с разными значениями = ОТКАЗ**; (2) `{}` вместо
списка → `[]`; (3) map по **msg_id** → список (ключи ВЫБРАСЫВАЮТСЯ). Не чиним: `aspect: null`,
`from_post`-сигнал без `evidence`, голый фрагмент. У v4 не сработал НИ ОДИН из девяти ремонтов;
refuse-on-conflict, наоборот, снял два ответа.

**🎚️ БАР 3 — ПЯТЬ ТРЕДОВ, N1 ИСКЛЮЧЁН С ПРИЧИНОЙ.** Предикат `bar_three_over_answers` ИМПОРТИРУЕТСЯ
из продюсера v3. Отказ никогда не ноль; без единого вердикта бар **UNREACHABLE**, что не проход.
N3 (`@sashafitnesslife:3939`) несёт **0 платных комментов** — бар двигают ЧЕТЫРЕ треда из пяти.

**🔤 СКЛЕЙКА СЛОВАРЯ — БАР, И ОНА СИММЕТРИЧНА.** «категория» ≡ «категория_личное» с ОБЕИХ сторон.
В ТЕКСТЕ v5 «категория» больше не предлагается (пара 5), но `prompts.READER_SUBJECT_TYPES` **не
двигается**: 26 оплаченных вердиктов и голд r2 валидировались против него.

**⚙️ Карта: RTX 4090. probe-b (serverless): 31.6376 с/тред. v4 (под): 39.461 с/тред, бут 175.1 с,
жизнь пода 1 143 с, 20.53 ток/с в среднем и 18.52 на самом медленном треде.** Единицы РАЗНЫЕ.
**probe-b больше НЕ пол для проекции** — v5 считает от измеренного v4.

**📊 Ячейка ценза читателя: 129 тредов · 1 032 платных коммента**
(`results/gate_census_w1_reader.json`, narrow · varto off · plus-spam/scam on). ⚠️ **129 — это ТАКЖЕ
thread-count `narrow|silencers_off`**; различает только платный счёт (1 032 против 1 116).

**🪟 ОКНО СПРОГНОЗИРОВАНО, НО НЕ ОТКРЫТО.** Самый большой тред ячейки — **125** платных комментов;
при 137.83 токена на запрошенную строку это ≈**17 200 токенов вывода на ОДИН тред**. Гиганты окна
(125/108/105) идут окну ПОСЛЕ того, как нога B докажет механизм чанкинга.

**🔄 ТЫ ЗДЕСЬ: линия промпт-инженерии закрыта stop-правилом, архитектура D выбрана, её РЕШАЮЩИЙ
микро-замер СДЕЛАН — и он ОТРИЦАТЕЛЬНЫЙ: бар P1 9 из 14 при пороге 12, вердикт STOP.** Инструмент,
транспорт и скорер при этом чисты (64/64, ноль отказов), так что следующий ход — не промпт, а
сидение (B: данные + LoRA · C: другая база). `reader-v5b` закрыт ($0.312592); открыты ДВА номера —
закрытие шагов `pass1-probe` и `pass1-probe-b`, оба ждут полного обхода. Фаза 5 закрыта, Фаза 4
закрыта, линия цикла-2 открыта и заякорена.

## ⏭️ Next

1. **ЗАКРЫТЬ ДВА ШАГА, КОГДА ИХ ОБХОДЫ СОЙДУТСЯ.** `pass1-probe` — **300 000 из ~427 000 мс**;
   `pass1-probe-b` — обход отвечает **`[]`** при 657 000 мс в записи, balance-delta $0.0853 против
   записанных $0.135050. Закрытие на любом из этих чтений заморозит цифру НИЖЕ правды.
   Дискриминатор: `runpodctl billing pods --pod-id <ID> --grouping podId --bucket-size hour` против
   `billed_seconds` в записи прогона. Пока открыты — копят каплю тома на delta-плече.
2. **ГАРД `--until` + ПЕРЕЗАКРЫТИЕ 22 СТАРЫХ ЛЕДЖЕРОВ ($0).** Уже в очереди у оператора, и теперь у
   него два новых клиента (выше). Money-path, отдельный контракт, с тестами.
3. **СИДЕНИЕ ПО ПРОХОДУ-1 — решение оператора.** Зарегистрированная оговорка называет только два
   выхода: **B** (размеченные данные по `subject_type` + LoRA) и **C** (другая база). Итерации
   промпта НЕТ. Цифры, которых у сидения не было:
   - вызов стоит **5.162 с** — 0.81× зарегистрированной границы и 1.76× подгонки;
   - окно на 1 032 платных комментария — **$1.0950 генерации как ПОЛ**;
   - инструмент **воздерживается на половине** соседних строк при **нуле отказов**;
   - провал целиком в `subject_type` на строках вида `категория_личное` (**5 из 9**), при
     `stance` **3 из 3** и прочих типах 3 из 4.
   Вход — `docs/reports/pass1-probe-b.md`; программа промпт-инженерии читателя закрыта своим же
   stop-rule, ADR [[reader-programme-closed-and-the-architecture-sitting-17-08]].

**Кандидаты цикла-2 в порядке ценности:**

1. **Деньги — $19.1467 свободно.** Влезает всё решённое (окно-2 ≈$4.8 · читатель v5 ≤$0.45 ·
   батч-пробник $2.13 · том ≈$2/нед); история 11 143 — не влезает.
2. **Батч-паритет — СПРОЕКТИРОВАН, решение за оператором.**
   `results/batch_cycle2_projection.json`. Шаг с batch 16 требовал **23.99 ГиБ** сверх весов, у 4090
   их **3.99** — короче в **6.02×**, единственный кандидат **batch 2**, и влезет ли он —
   **НЕ ИЗМЕРИМО** без живого пробника. Риск не память, а ОТВЕТЫ: при batch 16 разошлись **6 строк
   из 666**. Пробник: фикс **$0.1520** + **$0.0013025**/строка; арм 758 с контролем — **$2.1266**.
3. **Сбор листовок Сільпо / Varus / Маркетопт** — 3.18 (7)(d) + **3.20 (6)**. Бесплатно, в `prep-b`.
4. **Докачка корпуса** — сборка стоит с 08.08.
5. **История 11 143** — вход бесплатен, ~13 ч, ЖДЁТ батча И денег.
6. **Serving-латенси** — лечение ОТЛОЖЕНО до «готовим прод» ([[serving-latency-deferred]]).

**Долги-входы следующих регистраций:** цена страницы листовки **10.408 с/стр (n=159)** — старые
записи НЕ перескориваются. Ценз `results/gate_census_w1.json`: narrow|silencers_on = **111 тредов,
912 платных комментариев** (популяция ПРОБ; читательская ячейка — отдельный файл). Два вопроса из §5
отчёта fix-a и **метка типа поста для окна-1**. Мелочи step-0.5: заголовок `t5_depth`, валюта ₴ в
шапке промо-таблицы, fix-b(2) «две колонки — одно число»; пачка закрытий 22 старых шаговых леджеров.

## 🚧 Blockers

**⛔ ДВА НЕЗАКРЫТЫХ ЛЕДЖЕРА, И ОБА ДОРОЖАЮТ.** `pass1-probe` (обход 300 000 из 427 000 мс) и
`pass1-probe-b` (обход пуст при ~657 000 мс). Закрывающий путь гарда селится на обходе ОДИН, без
пессимистичного максимума против дельты, поэтому частичный обход заморозил бы ложь. Пока шаг открыт,
его delta-плечо копит каплю сетевого тома: `pass1-probe` читает **$0.2075 из своих $0.20** и
отказывает — это ТОМ, а не под (пробег пода $0.0617 прочитан / $0.087772 записан). Разблокирует
контракт гарда `--until`. [[a-settlement-is-a-reading-too]]

**⛔ ПРОХОД-1 НЕ БЕРЁТ БАР В НЫНЕШНЕМ ВИДЕ — 9 из 14 при пороге 12.** И по зарегистрированной
оговорке это вопрос СИДЕНИЯ, а не следующего промпта. Транспорт при этом чист: 64 из 64 прочитаны,
ноль отказов, `finish_reason: stop` везде, потолок 256 токенов не сработал. Провал — в содержании
чтения `subject_type`, и половина его это ВОЗДЕРЖАНИЕ, а не ошибка.

**⛔ БУТ НА ЭТОМ СТЕКЕ — НЕ КОНСТАНТА, И ТРЕТИЙ ЗАМЕР ЭТО ПОДТВЕРДИЛ.** 192.1 / 237.2 / [267, 293] с
на одной карте, одном томе и одних весах — **1.53×**, и ничто в прогонах этого не объясняет. Заряд
300 с (максимум замеров) оказался консервативен на 62.8 с и это правильная форма; потолок 420 с не
был затронут, а СТАРЫЙ потолок 300 с сегодня прошёл бы с запасом 57.8 с. Урок не «потолок мал», а
«бут — распределение»: заряжать по максимуму, потолок ставить над ним, и не приписывать подъёму
результат, которого он не производил. [[the-fix-that-was-not-a-fix]]

**⛔ ЗАПАС НА ПРОВИЖЕН НАДО ПОКУПАТЬ, А НЕ НАДЕЯТЬСЯ.** `pre_generation_budget_seconds` = **−44.785**
стояло в регистрации v5 ДО пода и означало ровно то, что случилось: слака до генерации нет, и любой
медленный провижен съедает попытку. Четыре бута на этом стеке расходятся в **6.7×** (179 · 373 · 175 ·
>1 200 с) — вариативность измерена и на записи, и всё равно не была куплена. Дедлайн вида
`usable − projection` — не запас, а ставка на платформу. [[a-negative-pre-generation-budget-is-a-forecast]]

**⛔ STOP-RULE ПРОГРАММЫ ВЗВЕДЁН И НЕ СРАБОТАЛ.** Его условие — «**если прогон ДОХОДИТ ДО КОНЦА** и
бары 1 и 4 не взяты ОБА». v5 не дошёл: ноль строк, ни одного бара. Значит вопрос «закрывать ли
промпт-инженерию» ровно так же открыт, как был до пода, и правило ждёт прогона, который завершится.

**🟠 Шаг `reader-v5` НЕ ЗАКРЫТ, но обход УЖЕ ПОСТНУЛСЯ — закрыть в шаге 0 v5b.** В сессии `--close`
был ОТКАЗАН («no billing rows yet») и **$0.1406** ушло в отчёт нижней границей (метр пода 727.9 с ×
$0.74/ч = $0.1496); тимлид перечитал позже и обход отвечает **$0.1501 pods-only**. Команда:
`python3.11 scripts/runpod_guard.py --step reader-v5 --step-cap 0.45 --close --note "reader-v5
settled"`. Цикл 2 на момент удаления: **$1.0133 из $20.00**, свободно **$18.9867**.

**🔴 ДВА ЗАЗОРА В МОЕЙ ЖЕ ПОЧИНКЕ, ЖДУТ RED-FIRST В ШАГЕ 0 v5b.** (1) `already_answered` парсит
`json.loads(line)` наголо — рваная строка валит РЕЗЮМ, то есть тот самый путь, ради которого
механизм и писался, а защиту от неё я поставил только в `raw_rows` того же файла;
(2) `projection()` считает `len(rows)`, а не уникальные id. Механизм резюма делает файл
ПЕРЕЧИТЫВАЕМЫМ, и после этого каждый счёт строк — вопрос про идентичности.
[[the-hardening-did-not-reach-the-sibling-reader]]

**🔫 ПРОДЮСЕР — НЕ ВЕРИФИКАТОР (Dv474, стоило рулинга оператора).** Запуск
`scripts/volume_calc_5c1.py` «чтобы проверить, что литерал вернулся» ПЕРЕЗАПИСАЛ
`results/volume_calc_5c1.json`: сдвинул `generated_at` на десять дней и **стёр блок `executed`** —
рулинг оператора 07.08 с id, размером и датацентром удалённого тома. Поймала суита, откат
`git checkout --`. Проверять вход — ЧИТАТЕЛЕМ (`calc.quoted(...)`, `calc.inputs()`), никогда
`main()`; после любой команды, которая может писать, смотреть `git status --porcelain`.
[[the-producer-is-not-the-verifier]]

**🟢 Dv455 СНЯТ.** Шаг `reader-v4` закрыт на **$0.236118** settled; биллинг постнул.

**⚠️ Worktree этого репозитория НЕ отвечает за весь suite.** Стор под `data/` в gitignore, а часть
`data/` ТРАКАЕТСЯ — так что worktree создаёт настоящую директорию `data/`, и симлинк надо ставить на
`data/derived` и `data/raw` по отдельности. Так проверены все десять коммитов 17.08 (287 → 379
проходов по 13 файлам, **ни одного красного**). [[a-commit-must-run-its-own-suite]]

**Деньги — три числа, которые легко перепутать.** Шаг цени ПО БИЛЛИНГУ с исключением всегда-включённых
видов ПО ВИДУ: probe-b **$0.313162**, reader-v3 **$0.393577**, reader-v4 **$0.236118**. Фазу и линию —
пессимистичным максимумом двух чтений. Дельта баланса меряет СЧЁТ, а не шаг, и после закрытия шага
растёт вечно. Остаток ВЫВОДИТЬ (`CAP - spent_usd`), а не читать из `sessions[-1].remaining_usd`.

## 🔫 Footguns этого файла

**⛔ `knowledge/hot.md` грепается как ЦЕНОВОЙ ВХОД — два литерала обязаны стоять здесь дословно.**
`scripts/volume_calc_5c1.py` читает их через `quoted(HOT, …)`, и отсутствие любого роняет **девять**
тестов `tests/test_volume_calc_5c1.py`. Курирование этого блока 17.08 выронило оба и сделало ровно то,
о чём предупреждала снесённая заметка. `make check` — после правки ЭТОГО файла, не только кода.

- **Recorded rather than open:** том CA-MTL-3 удалён, его **~$0.24/day** больше не капают — литерал
  нагруженный, не украшение, и он ОБЯЗАН стоять ровно так, по-английски со слэшем:
  `scripts/volume_calc_5c1.py :: quoted(HOT, "~$0.24/day", 0.24)` грепает его ИЗ ЭТОГО ФАЙЛА как вход.
  Перевод слова `day` уже уронил девять тестов один раз (12.08).
- **A stopped pod with no network volume still bills its container disk.** 80 GB is about what the
  100 GB network volume costs per month. Если под нужен на одну сессию — **delete**
  (`runpodctl pod delete`, `pod terminate` не существует), не stop. Остановленный под — не
  остановленный счёт, и `runpodctl pod list` показывает только running: смотри `pod list -a`.
