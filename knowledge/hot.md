<!-- AUTO-GEN START (refreshed by scripts/refresh-hot-cache.py) -->

# Hot Cache

**Auto-refreshed:** 2026-08-25 20:15:50 (every SessionStart)
**Branch:** `main`

## 🔀 Recent commits (top 5)

```
e925ab7 docs(report): lora-c-migrate -- STOP at $0, the ruling's datacenter takes no volume
aa9d946 test(migrate): 37 tests -- the refusal parsed, the intersection, the four findings
9003c0b chore(money): step 0.5 -- r2 settles at $0.7650, the vramprobe close REFUSES
a609b0e docs(migrate): D1 -- the registration, marked EVIDENCE and not law
42a0ccb feat(migrate): the free reading that refutes the ruling -- EU-SE-1 takes no volume
```

## 📋 Recent decisions

- `lora-c-the-card-cannot-train-at-3072.md` — 32 GB cannot train this line at 3 072 — and the base columns say the prompt already moved
- `INDEX.md` — Decision records
- `lora-c-arm-b-rendering-and-the-two-leg-pass-2.md` — A synthetic row is a v3 QUERY — and pass 2 runs for the arms only

## 📅 Recent daily logs

- `2026-08-25.md`
- `2026-08-24.md`
- `2026-08-23.md`

<!-- AUTO-GEN END (everything below preserved across refreshes) -->
# Hot Cache — curated

**Last update:** 2026-08-25 20:15 **`lora-c-migrate` — STOP на $0.0000 из $1.00: ДЦ, который
называет рулинг (р), НЕ ДЕРЖИТ СЕТЕВОЙ ТОМ.** Отказанный create (бесплатный) перечисляет **21** ДЦ,
где том возможен, — **EU-SE-1 среди них НЕТ**, как нет US-TX-1, EUR-IS-2 и US-PA-1: всех четырёх,
что назвал §5 прошлого отчёта. И сама A6000 `Low` ТОЛЬКО в EU-SE-1, `none` во всех трёх «томовых»
ДЦ, где она есть. Ни тома, ни пода не создано; замороженная регистрация побайтово та же, попытка
цела. Отчёт `docs/reports/lora-c-migrate.md`; предыдущий шаг —
[[lora-c-the-card-cannot-train-at-3072]].

> Этот блок курируется руками. Археология закрытых контрактов живёт в отчётах, ADR и логах дня — не
> восстанавливать сюда то, у чего есть дом. Ценз буста мерить ДО и ПОСЛЕ каждой правки.

## 🔥 What's Hot

**🖥 СПИСОК КАРТ И СПИСОК ТОМОВ — РАЗНЫЕ СПИСКИ (чтение 25.08 18:01Z, $0).** Том живёт только
в СВОЁМ ДЦ, и `network-volume create --data-center-id NOPE` перечисляет 21 подходящий. Пересечение
{≥48 GB} × {в наличии} × {том возможен} = **19 карт**, дешевейшая **PRO 6000 MIG 48GB $1.09/ч
US-NE-1**. Оба листинга (`datacenter list` '' и `gpu list` 'none') сверены: 137 пар, 0 расхождений.
Запись `results/lora_c_migrate_stock.json`, продюсер `scripts/probe_lora_c_migrate_stock.py`.

**🎯 ПЕРЕЕЗД НЕ НУЖЕН, ЧТОБЫ ДОСТАТЬ ПОДХОДЯЩУЮ КАРТУ.** В EU-RO-1 — ДЦ существующего тома — сегодня
есть **RTX PRO 6000 96 GB $2.09/ч `Low`**: ровно та, что рулинг (п) уже признал невлезающей (~$11
за сессию). Всё дешевле неё требует ВТОРОГО тома: 59 GB весов заново + вторая рента ~$0.24/сутки,
не оценено ни одной регистрацией.

**💸 ОСТАТКА КАПА r2 НЕ ХВАТАЕТ НИ ПРИ ОДНОЙ ДОСТУПНОЙ ЦЕНЕ.** $3.2394 / 150 шагов / фикс-часть
6 577.2 с (заряд v3 = 9.20): $1.09 → **27.48 с/шаг**, $2.09 → **−6.65** (кап кончается ДО первого
шага). Замер репы — **61.047** на строках ≤1 222 против здешних до 2 975. Даёт его только **$0.53,
и она недоступна**. Держится и на вдвое более быстрой карте (>1.65× разброса): на $2.09 — 12.61.
Таблица целиком — `docs/reports/lora-c-migrate.md` §5.

**✅ ЗАКРЫТО, живёт в отчётах/ADR:** потолок **3 072** · **протечка** элементов E (22 → 0) ·
линия B не тронута, пины на ревизии 1 · вайтлист `check-stamped` + `knowledge/index.md`, `hot.md`
снаружи поимённо · у арма B датасет 666 строк, маркер в хедере назван
([[lora-c-arm-b-rendering-and-the-two-leg-pass-2]]).

**📉 КУПЛЕНО:** обе базовые ноги на E — **v2 61/98 · v3 66/98**, 0 отказов парсера. Заряд v3 =
**9.20 с/звонок** во всех выводах (рулинг (п)), не 6.14.

## ⏭️ Next

1. **СЛОВО ОПЕРАТОРА: ПОДНЯТЬ КАП, УРЕЗАТЬ ПЛАН ИЛИ ЖДАТЬ A6000** — переезд как таковой больше
   не вопрос: ДЦ с A6000 тома не держат, а подходящая карта есть дома. Таблица в
   `docs/reports/lora-c-migrate.md` §5; ни один вариант здесь не выбран. Контракт r3 пишет тимлид.
2. **`money-anchors` ($0)** — девять открытых шагов. `lora-c` ЗАКРЫТ 25.08 ($0.764952, 4.2% от
   своей записи). `lora-c-vramprobe` ОТКАЗАН и остаётся открытым: settled $0.075729 верен (0.7% от
   часов пода), не проходит записанное $0.0495 — дельта баланса, снятая через 21 с после удаления.
3. **prep-b** — коллекция Сільпо/Varus/Маркетопт ($0), после читательского тракта.

## 🚧 Blockers

**⛔ 32 GB НЕ ТЯНУТ 3 072 — ИЗМЕРЕНО ДВАЖДЫ, ВТОРОЙ РАЗ С ЛЕКАРСТВОМ.** Ноль шагов оба раза,
`micro_batch` понижен до 1 самим трейнером. Понижение потолка НЕ ПОМОЖЕТ: `max_seq_len` — гард на
энкоде, `collate` паддит по длиннейшей строке В МИКРОБАТЧЕ. Дешёвых проб больше нет.

**⛔ `make fmt` ОБНУЛЯЕТ ПИНЫ ПРОДЮСЕРОВ — ОБА РАСФОРМАТИРОВАННЫХ ФАЙЛА ЗАПИНЕНЫ.**
`scripts/write_lora_c_prereg.py` → `a4eb2893…` в `results/prereg_lora_c.json::producer`;
`scripts/build_lora_c_marker_census.py` → `a947947e…` в `results/lora_c_marker_census_pack.json`,
который сам запинен `c632d850…` внутри замороженной регистрации. Проверено sha-грепом, не на глаз.
`make check` гоняет `ruff check`, а не `ruff format --check`, и не видит ни того, ни другого.

**⛔ ОТНОСИТЕЛЬНЫЙ ДОПУСК ГАРДА НЕДОСТИЖИМ НА ДЕШЁВОМ ШАГЕ.** Обе записи отстают от settled примерно
на одну и ту же АБСОЛЮТНУЮ величину — $0.031152 (r2) и $0.026229 (проба), — что даёт 4.2% и 53.0%.
При допуске 0.07 нужен шаг дороже ~$0.37–0.45, иначе close невозможен при любой правильности.
n = 2; полосу не трогали — не наша область.

**⛔ БАР 3 КРАСНЫЙ, И v3 ЕГО НЕ ЧИНИТ** (`@VARUS_channel:10366`, 2 сигнала против 0). Прирост v3
(+5 холдаут) весь в `не_наш_рынок`; дозамер тимлида за $0: не чинит ни 580124 (F2a), ни бар 3 — те
же 4 ложных «сеть». Ответ на «нужен ли линии адаптер» — **ДА** (рулинг (п)). **ПРОХОД-1 НЕ БЕРЁТ
БАР НИКАК:** ни базой, ни LoRA (9/14), ни v2 (+7 из +10). Арм B не измерен.

**⛔ ДВЕ НЕДОСТИЖИМОСТИ (3.25 (3) принял STOP 1 как есть):** девять строк `@VARUS_channel:10367`
не рендерятся, включая единственную реальную `молочный_бренд`; у арма A ноль таргетов
`молочный_бренд` (у B — 32), значит бар 1 почти недостижим для A.

**⛔ preflight СЛЕП К `config_sha256`.** У поля нет соседа-пути, поэтому `config/qlora.yaml` не
попадает в реестр пинов: старые пины читаются 38 → 38 через смену ревизии. Проверять перечисленным
диффом записи, не preflight'ом.

**⛔ ssh И БУТ — НЕ КОНСТАНТЫ, ЗАРЯЖАТЬ ПО МАКСИМУМУ.** ssh 14.5…262.5 с, бут 139.5…353 с
(по девять чтений; списки — в отчётах подов). Проход-2 заряжается 97 с/тред против измеренных
23.760 — безопасно и дорого; разброс подов 1.008 при **n = 1**, цитировать только парой с 200/200.

**🧹 BOOT TAX 10.8K ПРОТИВ 10.7K — РЫЧАГ НЕ В ЭТОМ ФАЙЛЕ.** По файлам: `MEMORY.md` **24.4 KB**
(обрезан окном при сырых 26.2 — записи теряются) · `hot.md` 11.2 · CLAUDE.md 4.6 + 2.0. **~57%
сбора — индекс памяти**; влезть можно только вырезая живое состояние ради обрезанного индекса.
Консолидация `MEMORY.md` — отдельное решение и настоящий рычаг. Мерить `context-census.py` ДО/ПОСЛЕ.

## 🔫 Footguns этого файла

**⛔ `knowledge/hot.md` грепается как ЦЕНОВОЙ ВХОД.** `scripts/volume_calc_5c1.py` берёт отсюда
**один** литерал через `quoted(HOT, …)`, и его отсутствие роняет девять тестов
`tests/test_volume_calc_5c1.py`. `make check` — после правки ЭТОГО файла, не только кода.

- **Recorded rather than open:** том CA-MTL-3 удалён, его **~$0.24/day** больше не капают — литерал
  нагруженный, не украшение, и он ОБЯЗАН стоять ровно так, по-английски со слэшем:
  `scripts/volume_calc_5c1.py :: quoted(HOT, "~$0.24/day", 0.24)` грепает его ИЗ ЭТОГО ФАЙЛА как вход.
  Перевод слова `day` уже уронил девять тестов один раз (12.08).
- **A stopped pod with no network volume still bills its container disk.** 80 GB is about what the
  100 GB network volume costs per month. Если под нужен на одну сессию — **delete**
  (`runpodctl pod delete`, `pod terminate` не существует), не stop. Остановленный под — не
  остановленный счёт, и `runpodctl pod list` показывает только running: смотри `pod list -a`.
