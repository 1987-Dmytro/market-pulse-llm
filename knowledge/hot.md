<!-- AUTO-GEN START (refreshed by scripts/refresh-hot-cache.py) -->

# Hot Cache

**Auto-refreshed:** 2026-08-20 21:24:41 (every SessionStart)
**Branch:** `main`

## 🔀 Recent commits (top 5)

```
317762e chore(vault): hook tail -- three session stamps and the index timestamp
1577d3a chore(docs): STATUS.md -- the team lead accepts the addendum, verbatim
46d58c6 chore(vault): hook tail -- the addendum commits reach the cache, the index and the log
669ca18 docs(report): lora-b-run ADDENDUM -- the Deviations table Dv575-Dv586, re-derived
2cbd11f verdict(lora-b): Dv579 -- re-score over the FINAL run record, one leaf moves
```

## 📋 Recent decisions

- `lora-b-red-and-line-b-closes.md` — The LoRA registration answered RED at 9 of 14 — line B is CLOSED and the question goes to sitting C
- `INDEX.md` — Decision records
- `boot-tax-re-registered-from-the-measured-floor.md` — The boot tax is de-bloated in all five groups, and the target is re-registered from the measured floor

## 📅 Recent daily logs

- `2026-08-20.md`
- `2026-08-19.md`
- `2026-08-18.md`

<!-- AUTO-GEN END (everything below preserved across refreshes) -->
# Hot Cache — curated

**Last update:** 2026-08-20, конец `pass1-fewshot`. **D0 сдан целиком и зелёный; платная сессия
НЕ состоялась — два 4090 в EU-RO-1 убиты рунгом 2 (ssh dead-man 180 с), пересоздание клаузы
восстановления ИСПОЛЬЗОВАНО. Попытка НЕ потрачена.** Ни одного токена не сгенерировано.
Линия B до этого закрыта красным баром 9/14. Ждёт слова оператора по рунгу 2.

> Этот блок курируется руками. Археология закрытых контрактов живёт в отчётах и ADR — не
> восстанавливать сюда то, у чего есть дом. Вытеснено 19.08 → mapping-таблица «блок → дом»
> в `docs/reports/boot-debloat.md`.

## 🔥 What's Hot

**🔄 ТЫ ЗДЕСЬ.** `pass1-fewshot` D0 **закоммичен и зелёный** (suite **3 239/2**, было 3 169/2):
холдаут-100, промпт `pass1_comment_gm4_v2`, dev-200 в двух парных ногах, 64 голда под v2, гейт с
блокирующим `--watch`, прережка с H6-отказом на шаге 1, скорер D2. **D1 закрыт рунгом 2 —
попытка НЕ потрачена**, вопрос о v2 открыт. Отчёт `docs/reports/pass1-fewshot.md`, ADR
[[pass1-fewshot-closes-at-the-ssh-dead-man]].

**⛔ РУНГ 2 (180 с) НЕ ДЕРЖИТСЯ НА ЭТИХ ПОДАХ.** Под 1 `7spsy61lpjumrz` убит **моим инструментом**:
цикл опроса грепал `"host"` — ключ, которого `runpodctl ssh info` не эмитит (`"ip"`/`"port"`), — был
слеп 240 с, и `--gate0` после него утверждал, что эндпоинта нет, когда он был. KILL остаётся:
гейт пишет то чтение, которое ему ДАЛИ. Под 2 `juupgp6y77jvuz` убит **подом**: ограниченный опрос по
`"port"`, всё ещё `pod not ready` на **231.9 с**. probe-b брал этот же рунг на этой же карте 18.08 →
**180 с — чтение той ночи, а не свойство стека**, ровно как бут (146.8 … 353 с).
**Пересоздание израсходовано: третьего пода под этой регистрацией НЕЛЬЗЯ.**

**🔴 `lora-b` ИЗМЕРЕН И ЗАКРЫТ — его числа в отчёте, не здесь.** Дом:
`docs/reports/lora-b-run.md` + [[lora-b-red-and-line-b-closes]].

**🧠 ГЛАВНОЕ ДЛЯ СИДЕНИЯ C: девятка — НЕ «адаптер ничего не сделал».** Строка в строку таблица
базы, но 21601 и 48283 ушли с `null` на другие (и всё равно неверные) классы, а ценз-50 сдвинулся
резко: `None` **25 → 9**, `не_наш_рынок` **8 → 23**. Файн-тюн выучил **приор** обучающего набора
(340 из 650 = **52%**), а не различение; лосс 0.689 → 0.0095 на голове в один-два токена.
**Взвешенный сэмплер `w_c = N/(5·n_c)` cap 8.0 этого не удержал** — это вход любого следующего
файн-тюна, а не деталь. [[an-identical-count-is-not-an-identical-model]]

**⛔ ПРОСТОЙ 9 778 с = $1.4396 И АРМ B.** Урок уже В ФОРМЕ: рунг живости с дедлайном от
ПОСЛЕДНЕГО события, и в `pass1-fewshot` его держит скрипт `--watch`, а не привычка.
[[no-rung-watches-an-idle-pod]] · разбор — `docs/reports/lora-b-run.md` §11.

**🧬 ИДИОМЫ, КОТОРЫЕ ДЕРЖАТ.** Продюсер зовёт продюсера и отказывается, пока тот не пересоберёт
замороженную запись байт в байт · перечисленный дифф утверждается в ОБЕ стороны · записи ворот
APPEND · пре-регистрация + one-attempt + stop-rules · парный прогон в `--outdir` · право
исполнителя на отказ. Новое за 20.08: **гард, который не видит сдвига своей же границы, — не
гард** (`load_sft` принимал обе границы), и **отказанный `pod create` бесплатен и ЕСТЬ тест на
сток** [[a-stock-window-needs-the-create-not-a-poll]].

**💸 ДЕНЬГИ ЦИКЛА-2: $5.8992 из $20.00, остаток $14.1008.** За день: `reader-topup` **$1.2259**,
`lora-b` **$2.3624**, `pass1-fewshot` **$0.117783** (573.0 с × $0.74/ч по часам гейта; дельта
баланса гарда читает $0.0906 и ещё оседает). Все поды удалены, каждое удаление доказано тремя
листингами с томом как позитивным контролем.

## ⏭️ Next

1. **Слово оператора по рунгу 2.** Либо ждать ДЦ (прецедент `lora-b`: рулинг «ждать», окно
   открылось через ~1 ч), либо **пере-регистрировать** dead-man по двум сегодняшним чтениям. Правка
   этой регистрации запрещена: она закоммичена, кумулятивный стоп и единственное пересоздание не
   обнуляются. Вход: `docs/reports/pass1-fewshot.md`, ADR
   [[pass1-fewshot-closes-at-the-ssh-dead-man]], прогон `results/pass1_fewshot_run.json`.
2. **Приёмка `pass1-fewshot` D0** — всё лежит и зелёное; вопрос о v2 НЕ измерен.
3. **Ступень простоя** уже в форме: `--watch` держит скрипт. Стоила $1.43 и один арм в `lora-b`.
4. **Денежная гигиена (`money-anchors`, $0)** — правая часть закрытия шага, зависший walk
   `pass1-probe`, дисциплина `anchored_at` для 19 отказанных леджеров. По слову оператора.

**Кандидаты цикла-2 в порядке ценности:** окно-2 ≈$4.8 · батч-пробник $2.13
(`results/batch_cycle2_projection.json`: единственный кандидат **batch 2**; риск не память, а
ОТВЕТЫ — при batch 16 разошлись 6 строк из 666) · сбор листовок Сільпо / Varus / Маркетопт
(бесплатно) · докачка корпуса · история 11 143 (ждёт батча И денег) · serving-латенси ОТЛОЖЕНО
([[serving-latency-deferred]]).

**Долги-входы следующих регистраций:** цена страницы листовки **10.408 с/стр (n=159)**, старые
записи НЕ перескориваются. Ценз `results/gate_census_w1.json`: narrow|silencers_on = **111 тредов,
912 платных комментариев**. Два вопроса из §5 отчёта fix-a и метка типа поста для окна-1. Мелочи:
заголовок `t5_depth`, валюта ₴ в шапке промо-таблицы, fix-b(2) «две колонки — одно число».

## 🚧 Blockers

**⛔ ПРОХОД-1 НЕ БЕРЁТ БАР НИ БАЗОЙ, НИ LoRA — 9/14 обоими способами**, линия B закрыта, и
**арм B так и не измерен** (контекст r2 за $1.2259 не проверен на баре — по часам, не по деньгам).
Обе строки — вход любой следующей регистрации. [[lora-b-red-and-line-b-closes]]

**⛔ ВЗВЕШЕННЫЙ СЭМПЛЕР НЕ УДЕРЖАЛ ПРИОР.** 52% одного класса, `cap 8.0`, и ценз всё равно уехал на
мажоритарный (`не_наш_рынок` 8 → 23). Любой следующий файн-тюн начинается с этого факта.

**⛔ ЧТО ВООБЩЕ ДОСТАЁТСЯ В EU-RO-1 (замер 20.08, 21:00Z).** Под потолком $0.80/ч со
стоком в ДЦ: **4090 24 GB $0.74 (Medium)** — карта probe-b, ею и создавались оба пода сегодня; RTX
PRO 4500 32 GB $0.72 (High); L4 24 GB $0.49 (Low). **A6000 48 GB $0.53 — `none`.** Для ИНФЕРЕНСА
24 GB доказаны probe-b; **35.13 GB — это ТРЕНИРОВКА** (`results/train/45h2-arm-b`), и она в
EU-RO-1 сейчас не покупается под потолком.

**⛔ БУТ НА ЭТОМ СТЕКЕ — НЕ КОНСТАНТА.** 146.8 / 192.1 / 237.2 / [267, 293] / 353 с на одной карте и
одном томе. Заряжать по МАКСИМУМУ замеров и не приписывать подъёму потолка результат, которого он не
производил. [[the-fix-that-was-not-a-fix]]

**⛔ ЗАПАС НА ПРОВИЖЕН НАДО ПОКУПАТЬ, А НЕ НАДЕЯТЬСЯ.**
[[a-negative-pre-generation-budget-is-a-forecast]]

**🧹 BOOT TAX — НЕ ЗАДАЧА, А ЧИСЛО.** Ценз **9.8K** при цели **10.7K**. Замерено ДО и ПОСЛЕ
правки 20.08: `pass1-fewshot` добавил блоки → 10.3K, вытеснение археологии `lora-b` (у неё есть дом
в отчёте и ADR) вернуло **9.8K** — ниже, чем было утром. MEMORY.md **160/200 строк ·
~20.1K/25 000 юнитов**. ⚠️ Мерить до и после каждой правки этого файла.
[[boot-tax-re-registered-from-the-measured-floor]]

## 🔫 Footguns этого файла

**⛔ `knowledge/hot.md` грепается как ЦЕНОВОЙ ВХОД.** `scripts/volume_calc_5c1.py` берёт отсюда
**один** литерал через `quoted(HOT, …)`, и его отсутствие роняет девять тестов
`tests/test_volume_calc_5c1.py`. `make check` — после правки ЭТОГО файла, не только кода.

- **Recorded rather than open:** том CA-MTL-3 удалён, его **~$0.24/day** больше не капают — литерал
  нагруженный, не украшение, и он ОБЯЗАН стоять ровно так, по-английски со слэшем:
  `scripts/volume_calc_5c1.py :: quoted(HOT, "~$0.24/day", 0.24)` грепает его ИЗ ЭТОГО ФАЙЛА как вход.
  Перевод слова `day` уже уронил девять тестов один раз (12.08).
- **A stopped pod with no network volume still bills its container disk.** 80 GB is about what the
  100 GB network volume costs per month. Если под нужен на одну сессию — **delete**
  (`runpodctl pod delete`, `pod terminate` не существует), не stop. Остановленный под — не
  остановленный счёт, и `runpodctl pod list` показывает только running: смотри `pod list -a`.
