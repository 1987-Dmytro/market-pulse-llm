<!-- AUTO-GEN START (refreshed by scripts/refresh-hot-cache.py) -->

# Hot Cache

**Auto-refreshed:** 2026-09-08 20:29:17 (every SessionStart)
**Branch:** `main`

## 🔀 Recent commits (top 5)

```
2a51900 docs(progress): s35 — the final verifier's stamped tail, 4326 passed / 2 skipped at 4d5e351 with porcelain [] on both sides; the three placeholders filled
4d5e351 docs(progress): s35 — the stop's blast radius measured on all three sets (dev-2 170/188 identical, so the one row of holdout-2 is the whole of it); the workflow's «refuted» bucket named for what it is (22 of 26 unverified after 40 agents died, not cleared); two working-tree couplings and the README writer's missing caller named, not built
72eac52 s35(p1-ship): the README block's two bars were typed in its header prose — now read off the record like every cell beside them; the file's bytes do not move (3cd8c502… twice), which is the proof the derivation equals what was typed
4719937 docs(progress): s35 — «p1-ship» DONE at $0: P1 in make tick over every reader row (K10 holds), the S2 block on the screen and in README from three named result files with a by-name refusal, the tie count given a producer (numbers untouched, two runs byte-identical), the Makefile comment repaired; OPEN STOP: the loop feeds P1 the SCREENED rows while the reading fed the raw answer — one holdout-2 row moves, the loop reproduces 0.7411 where the published reading is 0.7500; the C3 record's shape is the team lead's ruling, not the executor's
5ffe09b s35(p1-ship): the docstring said parity, the replay says one row — corrected, code unchanged. The reading fed P1 the RAW answer's rows (promo_dev_pass.predicted_rows); a record here is the answer AFTER promo_hooks.screen, so a hook-failed signal row's type never reaches P1. Replayed read-only over both packs: 3 quote_is_a_substring failures on holdout-2, 4 on dev-40, exactly ONE P1 outcome moves — @VARUS_channel:5119/5987 cites its comment lower-cased, R3 does not fire, the row stays sku — so the LOOP reproduces 83/112 = 0.7411 on dev-3 where the published reading is 84/112 = 0.7500; dev-40 unchanged 124/140. Named as the open stop, not repaired: widening the C3 record is the team lead's ruling. Makefile's «NOTHING else» comment, which this item's S2 block invalidated, now names the four files
```

## 📋 Recent decisions

- `INDEX.md` — Decision records
- `a-published-number-has-one-reader.md` — A published number has ONE reader, and a missing source is a refusal by name
- `the-registration-opens-the-line.md` — The registration OPENS its line, so §0a lives in the PAID session

## 📅 Recent daily logs

- `2026-09-08.md`
- `2026-09-06.md`
- `2026-09-05.md`

<!-- AUTO-GEN END (everything below preserved across refreshes) -->

# Hot Cache — curated
**Last update:** 2026-09-08 (закрытие дня 06.09 + рулинг 08.09 **(dd)**: стоп s35 закрыт веткой (c), дальше «s2-loop»).
Этап 1 `SPEC-v2`, карта `STATUS.md`, цикл v3, фаза **v20**, процесс **Money v2.3**. Руками, ≤40 строк.

## 🔥 What's Hot — S2 отгружен, s35 принят, стоп ЗАКРЫТ рулингом 08.09 (dd) веткой (c)
**Чтения (запись бара, не переписываются):** holdout-2 с P1 субъект **0.7500 (84/112) ❌ бар 0.80** ·
сигналы **0.8021 ✅ бар 0.75**; сырьё 0.7054 / 0.8021; holdout-40 0.7181 / 0.7958 — экран и README
печатают их ОДНИМ ридером `s2_readings`, пропажа файла/блока = отказ ПО ИМЕНИ. P1 стоит в `make
tick` (K10 держит). Дро holdout-3 — РЕЗЕРВ. Ничьи dev-3 — **16 из 28** (правило файла победило).
**Стоп s35 (PHASE v20 §2/§6.7):** вход P1 в петле — строки ПОСЛЕ хуков, в замере — сырой ответ;
**петля dev-3 = 0.7411 / 0.7937** (моя строка по субъекту + 3 signal-строки, которых я не назвал),
dev-40 и dev-2 идентичны. Закон: **отгружаемое число меряется на конвейере ПРОДУКТА целиком
(хуки + P1) его же функциями**; запись C3 НЕ расширяется.

## ⏭️ Next — «s2-loop» ($0), одна проверка = таблица; потом «c3-prep»
(i) нога петли в `promo_p1_apply.py`: те же сырые ответы → `parse` → `promo_hooks.screen` → запись
формы `tick.signal_records` → `tick.p1_rows` → `predicted_rows` → K8 v2, **ВЫЗЫВАЯ функции тика**, →
`results/grade_promo_loop_readings.json`, два прогона байт-в-байт (ждём dev-3 0.7411 / 0.7937 —
решает файл). (ii) строка «holdout-2 · loop (hooks + P1) — shipped» ПЕРВОЙ в `S2_SOURCES`, старые
три — как «reading», README тем же ридером. (iii) `make promo-screen` зовёт
`build_readme_results.py`. (iv) два докстринга `tick.py`. (v) `make check` ≥ 4326, без новых тестов,
пинов, гардов. Потом «c3-prep» как в (cc) + контракт: `channel` записи C3 — с `@`, иначе отказ.

## 🚧 Blockers / долги (названы, не построены)
`make check` **4326/2** на `4d5e351`; HEAD `2a51900`; подов НЕТ; цикл-3 **REMAINING $2.1178** из
$10.00. **⛔ Классификатор auto-режима БЛОКИРУЕТ `runpodctl pod create`** — снимает только слово
оператора В СЕССИИ. · **Связки с деревом:** K12 читает настоящий `results/`, `tick.REGISTRY` —
константа; у `build_readme_results.py` нет вызывающего (чинит (iii)). · `promo_post.owner()` требует
`@` — контракт записи C3 в (dd). · **⛔ `promo-dev-loop` ОТКАЗЫВАЕТ.** · Сюита ~12 мин — срезами;
тесты ГРЕПАЮТ `hot.md`/PROGRESS.
**⚠️ zsh НЕ разбивает `$SSHO` · ⚠️ ТОЛЬКО `setsid` · ⚠️ живость по PID · ⚠️ K4 НЕ ПЕРЕЗАПУСКАТЬ ·
⛔ `1925810730` · ⛔ `aggregates.py` и `open`**.
## 🔫 Footguns этого файла
**⛔ ЭТОТ ФАЙЛ ГРЕПАЕТСЯ КАК ВХОД — ДВА ЛИТЕРАЛА.** `scripts/volume_calc_5c1.py :: quoted()` берёт обе строки ниже посимвольно; пропажа любой роняет девять `tests/test_volume_calc_5c1.py`.
- 100 GB сетевого тома стоят **~$0.24/day** — цена ЗА ТОМ, а не состояние счёта; с 27.08 том один,
  `mp-srv2`. Литерал обязан стоять ровно так, по-английски со слэшем.
- **A stopped pod with no network volume still bills its container disk.** 80 GB is about what the
  100 GB network volume costs per month. Под на одну сессию — **delete** (`runpodctl pod delete`),
  не stop; `runpodctl pod list -a` показывает только running, смотри `pod list -a`.
