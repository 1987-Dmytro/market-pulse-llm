<!-- AUTO-GEN START (refreshed by scripts/refresh-hot-cache.py) -->

# Hot Cache

**Auto-refreshed:** 2026-08-23 09:00:44 (every SessionStart)
**Branch:** `main`

## 🔀 Recent commits (top 5)

```
97548df chore(vault): /close за 22.08 -- три контракта, два ревью-гейта стоят
20a9b88 docs(adr): the leak onto the gating bar, and two numbers the record refuted
6a2f407 fix(lora-c): all five lenses returned; the worst finding is a leak onto the bar
bad6b66 fix(lora-c): lens 1 returned 95 minutes late with eight findings; all eight hold
96ae7b1 fix(lora-c): a fourth reachability block, and the executor flags 15 of its own rows
```

## 📋 Recent decisions

- `INDEX.md` — Decision records
- `lora-c-rationale-supervision-and-the-shared-pool.md` — Rationale supervision, one shared neighbour pool — and four things the design cannot reach
- `pass-2-in-the-readers-schema-and-strict-authority.md` — pass 2 answers in the reader's schema under STRICT authority, and the fourteen keep their sixth look off a bar

## 📅 Recent daily logs

- `2026-08-23.md`
- `2026-08-22.md`
- `2026-08-21.md`

<!-- AUTO-GEN END (everything below preserved across refreshes) -->
# Hot Cache — curated

**Last update:** 2026-08-23 `/close` за 22.08. **`lora-c-prep` ИСПОЛНЕН за $0 — данные и
регистрация линии «рассуждение→метка» готовы, и работа стоит на ДВУХ ревью-гейтах тимлида.** До них
не тронуто ничего: 675 строк с `reviewed: false`, регистрация — DRAFT. За день ещё два контракта
прохода-2: `pass2-signals` (STOP на смоке, $0.108122) и `pass2-signals-r2` (ИСПОЛНЕН, $0.42365) —
слой сигналов окна-1 собран, все три бара красные. Верификатор 3 658 passed / 2 skipped, подов нет.

> Этот блок курируется руками. Археология закрытых контрактов живёт в отчётах, ADR и логах дня — не
> восстанавливать сюда то, у чего есть дом. Ценз буста мерить ДО и ПОСЛЕ каждой правки.

## 🔥 What's Hot

**🛑 ТЫ ЗДЕСЬ: ждём ДВА ВЕРДИКТА ТИМЛИДА. Пока их нет — D0 стоит и переписывать нечего.**
`docs/reviews/lora-c-rationales-verdict.md` и `docs/reviews/lora-c-synthetic-verdict.md` не
существуют. Выборки готовы: `docs/reviews/lora-c-rationales-sample.md` (40 «наших» + 40
пограничных) и `docs/reviews/lora-c-synthetic.md` (все 160). Отчёт `docs/reports/lora-c-prep.md`,
ADR [[lora-c-rationale-supervision-and-the-shared-pool]].

**🩸 ГЛАВНОЕ ЧИСЛО ДЛЯ РУЛИНГА: 22 из 198, из них 5 на платном баре.** Элементы E показывают им же
самим их собственный коммент байт в байт, с меткой, внутри их же блока примеров. Пять — холдаут, то
есть пять из 98, на которых считается `холдаут-100 ≥ 64`. Механизм точный: правило «не свой тред»
блокирует ТРЕД, а `neighbours` максимизирует Jaccard, и идентичная строка даёт 1.0 — близнец
выбирается первым и детерминированно. **Нейтрально к «арм против базы» и к абляции, решающе для
абсолютного числа.** Не починено: правило отбора предписано контрактом.

**⚖️ ЧЕТЫРЕ НЕДОСТИЖИМОСТИ, каждая с арифметикой, ни одно средство не взято.** (1) в пуле ОДНА
строка `молочный_бренд` — 9 обучающих и 2 оценочных не рендерятся; (2) **ни одна из 506 строк v3 не
влезает в `max_seq_len` 1408** при любом измеренном отношении — при минимальном самая короткая
требует 1419 против 1408, и **никакое число соседей это не закрывает** (лучший случай 62 сверху);
(3) **у арма A ноль таргетов `молочный_бренд`**, у B — 32, значит бар 1 почти недостижим для A;
(4) запиненный `train_qlora.py` отказывает v3-датасету двумя гардами.

**📏 ЧТО ГОТОВО К ЦЕНЕ для `lora-c-run`.** 506 строк арм A · 666 арм B · шаги 62/82 (и `planned`
64/84 — это другое число, оно ведёт LR-расписание) · 198 вызовов на легу · 4 леги · 11 тредов
прохода-2 · токенов на строку 1694 медиана / 2759 макс. Кап **$4.00**. **Цена не зарегистрирована
и не должна быть** — три ставки заимствованы, две через смену инструмента, у каждой записано
условие, которое её отменяет.

**🔬 РЕВЬЮ: пять линз, ни одной за 95 минут, потом все пять.** Три нашли то, чего не нашли ни я, ни
инструменты. Второго скептического прохода не было — нет коммита фиксов, который бы он читал.

## ⏭️ Next

1. **Два вердикта тимлида** — переписываю ТОЛЬКО названные строки и строки под названным паттерном,
   ставлю `reviewed`, записываю счётчики.
2. **Рулинг оператора:** протечка в E · четыре недостижимости · 15 синтетических строк против
   кодбука (знаменатель 21 из 37) · нужен ли второй скептический проход.
3. **`lora-c-run`** — первый $0-деливерабл: **sibling-тренер**. Редактировать `train_qlora.py`
   нельзя, он запинен `prereg_lora_b.json` И `lora_b_verdict.json`.
4. **`money-anchors` ($0)** — ПЯТЬ открытых шагов: `pass1-window`, `pass1-window-r2`,
   `pass2-signals`, `pass2-signals-r2`, `lora-c-prep` ($0, но шаг есть).
5. **Коммит хвоста вальта** — лог дня и `knowledge/index.md`, первым коммитом следующей сессии.

## 🚧 Blockers

**⛔ D0 СТОИТ НА ДВУХ ГЕЙТАХ.** Ничего ниже них не трогать — это правило контракта, не удобство.

**⛔ 15 СИНТЕТИЧЕСКИХ СТРОК ПРОТИВ КОДБУКА, знаменатель 21 из 37.** Тимлид кладёт немолочный товар
у сети в `сеть_ритейлер`; синтетика — в `не_наш_рынок`. **Дефект только в синтетике** — среди 515
реальных строк такой формы ноль. Претензия вписана в файл гейта 2, строки не переписаны.

**⛔ СЕДЬМОЕ СОСТОЯНИЕ ПОЛЯ `rationale` СУЩЕСТВУЕТ, И ОНО ЛЕКСИЧЕСКОЕ.** ASCII-кавычка или сырой
перевод строки делают ответ невалидным JSON: строка теряет метку и `rationale_state` не пишется
вовсе. Из 515 рационалей ни одна не несёт ASCII-кавычки — **арм A безопасен, открыта база v3**.

**⛔ БАР 3 КРАСНЫЙ** (`@VARUS_channel:10366`, 2 сигнала против 0) — вопрос к фильтру прохода-1 или к
эталону, решается только рулингом.

**⛔ ПРОХОД-1 НЕ БЕРЁТ БАР НИКАК:** ни базой, ни LoRA (9/14), ни v2 (+7 из +10). Арм B не измерен.

**⛔ ЗАРЯД ПО МАКСИМУМУ × РАЗБРОС — БЕЗОПАСЕН И ДОРОГ.** 97 против измеренных 23.760. Разброс подов
на этом декоде измерен прямо и равен **1.008**, но **n = 1 и это самый короткий из пяти ответов** —
цитировать только парой с 200/200 прохода-1.

**⛔ ssh — СЕМЬ ЧТЕНИЙ.** 14.5 · 23 · 29 · 38 · ≤50 · 231.9 · 262.5.

**⛔ БУТ — НЕ КОНСТАНТА.** 139.5 / 140.6 / 142.7 / 146.8 / 164.9 / 192.1 / 237.2 / [267, 293] /
353 с. Заряжать по МАКСИМУМУ.

**⛔ A6000 48 GB В EU-RO-1 — `none`.** 4090 24 GB $0.74 берётся с первой попытки, доказан пятью
подами подряд.

**🧹 BOOT TAX 10.1K при цели 10.7K.**

## 🔫 Footguns этого файла

**⛔ `knowledge/hot.md` грепается как ЦЕНОВОЙ ВХОД.** `scripts/volume_calc_5c1.py` берёт отсюда
**один** литерал через `quoted(HOT, …)`, и его отсутствие роняет девять тестов
`tests/test_volume_calc_5c1.py`. `make check` — после правки ЭТОГО файла, не только кода.

- **Recorded rather than open:** том CA-MTL-3 удалён, его **~$0.24/day** больше не капают — литерал
  нагруженный, не украшение, и он ОБЯЗАН стоять ровно так, по-английски со слэшем:
  `scripts/volume_calc_5c1.py :: quoted(HOT, "~$0.24/day", 0.24)` грепает его ИЗ ЭТОГО ФАЙЛА как вход.
  Перевод слова `day` уже уронил девять тестов один раз (12.08).
- **A stopped pod with no network volume still bills its container disk.** 80 GB is about what the
  100 GB network volume costs per month. Если под нужен на одну сессию — **delete**
  (`runpodctl pod delete`, `pod terminate` не существует), не stop. Остановленный под — не
  остановленный счёт, и `runpodctl pod list` показывает только running: смотри `pod list -a`.
