<!-- AUTO-GEN START (refreshed by scripts/refresh-hot-cache.py) -->

# Hot Cache

**Auto-refreshed:** 2026-08-26 09:36:03 (every SessionStart)
**Branch:** `main`

## 🔀 Recent commits (top 5)

```
9fd506e docs(report): the venv is bound to /workspace/repo, and the reading covers the inputs
16d302d docs(report): the commit table names the two commits that closed the page
b6f8de4 chore(vault): lora-c-migrate r2 -- the volume is warm and the r3 premise moved
76aba89 docs(report): the closing reading -- 3 905/2 green, and the span stops being typed
0b18366 docs(report): lora-c-migrate-r2 -- 79 of 79 re-derived, and the KILL is rung 5's own
```

## 📋 Recent decisions

- `INDEX.md` — Decision records
- `lora-c-the-volume-moves-to-ca-mtl-3.md` — The training line's volume moves to CA-MTL-3 — and the cheaper cards arrived 107 minutes late
- `lora-c-the-card-cannot-train-at-3072.md` — 32 GB cannot train this line at 3 072 — and the base columns say the prompt already moved

## 📅 Recent daily logs

- `2026-08-26.md`
- `2026-08-25.md`
- `2026-08-24.md`

<!-- AUTO-GEN END (everything below preserved across refreshes) -->
# Hot Cache — curated

**Last update:** 2026-08-26 **`lora-c-run r3` ИСПОЛНЕН — KILL по рунгу проекции, отчёт ждёт
приёмки.** Линия обучается на **89.961 с/шаг при 3 072** (A100 PCIe) — первый замер этой ставки,
который вообще существует. План при нём даёт 21 284.8 с против стопа 18 000 и **$8.1271 против капа
$7.00**. Под пробил **1 152 с = $0.4448**, шесть гейтов: пять GO и KILL. **Единственная попытка
замороженной регистрации НЕ потрачена.** Разбор — [[lora-c-the-line-trains-at-90-seconds-a-step]],
отчёт `docs/reports/lora-c-run-r3.md` (73 из 73 числа пере-выводятся, чекер гоняет суита).
**СЛЕДУЮЩЕЕ — слово оператора о цене: план как написан ≈$8.25, арм A в одиночку $4.03, оба арма на
одной эпохе $5.72 (но `epochs: 2` — замороженный закон).**

> Этот блок курируется руками. Археология закрытых контрактов живёт в отчётах, ADR и логах дня — не
> восстанавливать сюда то, у чего есть дом. Ценз буста мерить ДО и ПОСЛЕ каждой правки.

## 🔥 What's Hot

**🔥 СТАВКА ИЗМЕРЕНА: 89.961 с/шаг НА 3 072.** `provenance.json::run.seconds_per_step`, вся стена
цикла (539.8 с) на шесть шагов. **Не разогрев:** строка шага 6 держит ОДИН шаг и всё равно делит на
`log_every`, поэтому обратное умножение даёт **87.207 с** — стартовые издержки 16.6 с из 539.8
(3.1%). Против двух приоров репы (61.047 зарегистрирован lora-b, 68.442 измерен на её арме A, оба
при ≤1 222 токенах) это **1.47× и 1.31×**. Модель отношения, снятая с потолков поправкой 3.26 (3),
снята теперь и со СТАВОК.

**☠️ ГЕЙТ, КОТОРЫЙ СКАЗАЛ БЫ GO.** `gate_lora_b.measured_step` берёт `max(среднее, последнее)` по
loss-строкам, а каждая делит на `log_every` независимо от окна: на реальном логе этой сессии он даёт
**53.690** против цикла в **89.961** — занижение 40.3%, и оно ниже 68.05 с/шаг, которые кап
позволял. Оба арма ушли бы в terminate-флаг за $6.95 без адаптера, эвала и бара, и попытка с ними.
Обе половины дефекта лежали в леджере как **Dv813/Dv814** с 25.08 и не были починены.

**📏 ЗАМЕРЫ СТАДИЙ 26.08 (A100, CA-MTL-3).** ssh **24.3 с** · стейджинг (scp 14 MB + fetch/checkout
поверх `/workspace/repo` + печать стека) **111 с**, venv НЕ пересобирался (`pyproject.toml` не
двигался — доказано диффом) · **загрузка холодная 138.5 с, тёплая 13 с** в одном поде, миграция
мерила 40.22 — **загрузка не константа, разброс 10.7×** ([[a_rate_is_a_property_of_the_pod]]).

**✅ 48 GB ЗАКРЫТЫ ИЗМЕРЕНИЕМ, за $0.4448.** Пик аллокации смока **49.54 GiB**, `nvidia-smi` 57 297
MiB при 100% и 304.72 Вт. У карты на 48 GB всей памяти меньше, чем этот прогон берёт на пике, — все
три дешёвые альтернативы из таблицы стока опровергнуты, а не отговорены.

**🎯 СТОК МЕРЦАЕТ ПОМИНУТНО** — связывает только сам create; таблица альтернатив и второй переезд
(он стоил бы третьего тома) — в [[lora-c-the-volume-moves-to-ca-mtl-3]].

**✅ ЗАКРЫТО, живёт в отчётах/ADR:** потолок **3 072** · протечка элементов E (22 → 0) · линия B не
тронута · вайтлист `check-stamped` + `knowledge/index.md`, `hot.md` снаружи поимённо · у арма B
датасет 666 строк ([[lora-c-arm-b-rendering-and-the-two-leg-pass-2]]) · базы на E: **v2 61/98 ·
v3 66/98**, заряд v3 = **9.20 с/звонок** (рулинг (п)).

## ⏭️ Next

1. **ОТЧЁТ r3 ЖДЁТ ПРИЁМКИ В НОВОЙ ТИМЛИД-СЕССИИ** (`docs/reports/lora-c-run-r3.md`). Вопрос к
   оператору один и он про цену, не про бары: **план как написан ≈$8.25** (21 284.8 с при 89.961
   с/шаг), **арм A в одиночку $4.03** (62 шага, одна нога эвала, одна нога прохода-2, но у арма A
   ноль реальных таргетов `молочный_бренд`), **оба арма на одной эпохе $5.72** — против того, что
   `epochs: 2` замороженный закон. Дешёвого железа нет: 48 GB закрыты замером, community $1.19 —
   другой cloud type, чем SECURE-датацентр тома.
2. **`money-anchors` ($0)** — девять открытых шагов; `lora-c-vramprobe` отправлен сюда тимлидом.
3. **prep-b** — коллекция Сільпо/Varus/Маркетопт ($0), после читательского тракта.

## 🚧 Blockers

**⛔ ДВА ТОМА КАПАЮТ ~$0.47/СУТКИ.** `mp-srv2` (EU-RO-1, сервинг) + `mp-lora-c` (CA-MTL-3, обучение).
Цикл-2 по чтению гарда на закрытии r3: **$10.4726 из $20.00, остаток $9.5274** — ~**20 дней**
простоя обоих. `mp-lora-c` удаляется по закрытии линии, слово оператора. Окно-2 ($4.8) не влезает.

**✅ 48 GB БОЛЬШЕ НЕ БЛОКЕР — ОПРОВЕРГНУТЫ ЗАМЕРОМ 26.08.** Пик аллокации обучения **49.54 GiB**
(`nvidia-smi` 57 297 MiB при 100%): у карты на 48 GB всей памяти меньше, чем берёт пик. 32 GB
отказали дважды, второй раз с `expandable_segments` — дефицит ВЫРОС до 0.64 GiB. Понижение потолка
не поможет: `max_seq_len` — гард на энкоде, `collate` паддит по длиннейшей строке В МИКРОБАТЧЕ.
Работает только 80 GB.

**⛔ `make fmt` ОБНУЛЯЕТ ПИНЫ ПРОДЮСЕРОВ — ОБА РАСФОРМАТИРОВАННЫХ ФАЙЛА ЗАПИНЕНЫ.**
`scripts/write_lora_c_prereg.py` → `results/prereg_lora_c.json::producer`;
`scripts/build_lora_c_marker_census.py` → `results/lora_c_marker_census_pack.json`, который сам
запинен внутри замороженной регистрации. `make check` гоняет `ruff check`, а не
`ruff format --check`, и не видит ни того, ни другого. Форматировать ТОЛЬКО по путям.

**⛔ ДОПУСК ГАРДА 7% НЕДОСТИЖИМ НА ДЕШЁВОМ ШАГЕ.** Обе записи линии отстают от settled примерно на
одну АБСОЛЮТНУЮ величину ($0.031152 и $0.026229) → 4.2% и 53.0%. Проба переспрошена через 5 ч 06 м —
те же два числа до цента, значит это не задержка расчёта. n = 2; полосу не трогали. Долг маршрутизован
в `money-anchors` (`implementation-notes.md`, 26.08). **Третья точка от r3: дельта гарда $0.4461
против часов пода $0.4448 — 0.29%**, то есть на шаге такого размера полоса грейдит нормально.

**⚠️ РУНГ 2 НЕ ВИДИТ ОБУЧАЮЩУЮ СТАДИЮ.** Стадия обучения не печатает ничего до шага `log_every`: на
89.9 с/шаг смок молчал **448 с** при дедлайне 600, GPU при этом на 100% и 304 Вт. Ни строки лога, ни
роста байтов — зарегистрированные сигналы живости не покрывают это окно. Рунг не гнули; ремедь —
следующему контракту: читать GPU/процесс как сигнал ([[no_rung_watches_an_idle_pod]]).

**⛔ БАР 3 КРАСНЫЙ, И ПРОХОД-1 НЕ БЕРЁТ ЕГО НИЧЕМ** (`@VARUS_channel:10366`, 2 сигнала против 0):
ни базой, ни LoRA (9/14), ни v2 (+7 из +10). Арм B не измерен. Плюс две недостижимости: девять строк
`@VARUS_channel:10367` не рендерятся, у арма A ноль таргетов `молочный_бренд` (у B — 32).

**⛔ preflight СЛЕП К `config_sha256`** — у поля нет соседа-пути, поэтому `config/qlora.yaml` не
попадает в реестр пинов: старые пины читаются 38 → 38 через смену ревизии. Проверять перечисленным
диффом записи, не preflight'ом.

**🧹 `MEMORY.md` УПЁРСЯ В ОБА ПОТОЛКА, ВТОРОЙ НЕ ЗАКРЫТ.** Загрузчик
(`scripts/context-census.py::loaded_memory`): trim → первые **200 строк** → отрез по **25 000 UTF-16
units**. Сейчас: строк ровно 200 из 200, units **26 848 из 25 000** — перебор **1 848 ≈ 14 строк**,
и **13 указателей не доезжают** (файлы уроков целы, невидим только указатель). Рычаг один — убрать
~14 СТРОК; укорачивание записей почти не даёт ничего. Это консолидация и отдельное решение.
Выживание записи проверять `loaded_memory`, **НЕ `head -c`** — не та единица
([[the_wrong_unit_points_at_the_wrong_lever]]). Ценз буста мерить ДО/ПОСЛЕ.

## 🔫 Footguns этого файла

**⛔ ИНВАРИАНТ РУНГА 5 — НА БЛОБАХ, НЕ НА КАТАЛОГЕ КЭША.** `du -sb /workspace/hf` сдвигается за
обычную загрузку (`refs/main` + нулевые маркеры `.no_exist`), блобов при этом пишется 0. Ремеди
назначен в step 0.5 контракта r3; разбор — `results/lora_c_migrate_r2_load_bytes.json`
([[an_invariant_on_the_container_not_the_payload]]).

**⛔ `knowledge/hot.md` ГРЕПАЕТСЯ КАК ВХОД — ДВА ЛИТЕРАЛА, НЕ ОДИН.** `scripts/volume_calc_5c1.py`
берёт отсюда **`~$0.24/day`** (строка 107) И **`80 GB is about what the`** (строка 136); пропажа
любого роняет девять тестов `tests/test_volume_calc_5c1.py`. Сегодня это и случилось: при
переписывании блока я сократил вторую фразу, грепнул только первый литерал — и получил 9 из 10
красными. **Грепать оба, и гонять `pytest tests/test_volume_calc_5c1.py` после КАЖДОЙ правки этого
файла**, не только после правки кода ([[fact_is_the_rule_when_the_law_keys_on_it]]).

- **Литерал живой и нагруженный:** 100 GB сетевого тома стоят **~$0.24/day** — это ЦЕНА ЗА ТОМ, а не
  состояние счёта; сейчас таких тома два. Литерал ОБЯЗАН стоять ровно так, по-английски со слэшем:
  `scripts/volume_calc_5c1.py :: quoted(HOT, "~$0.24/day", 0.24)` грепает его ИЗ ЭТОГО ФАЙЛА как
  вход. Перевод слова `day` уже уронил девять тестов один раз (12.08).
- **A stopped pod with no network volume still bills its container disk.** 80 GB is about what the
  100 GB network volume costs per month — фраза нагруженная, `volume_calc_5c1.py` грепает её
  посимвольно. Если под нужен на одну сессию — **delete** (`runpodctl pod delete`, `pod terminate` не
  существует), не stop. Остановленный под — не остановленный счёт, и `runpodctl pod list` показывает
  только running: смотри `pod list -a`.
