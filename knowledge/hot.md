<!-- AUTO-GEN START (refreshed by scripts/refresh-hot-cache.py) -->

# Hot Cache

**Auto-refreshed:** 2026-08-23 09:00:44 (every SessionStart)
**Branch:** `main`

## 🔀 Recent commits (top 5)

```
97548df chore(vault): /close за 22.08 -- три контракта, два ревью-гейта стоят
20a9b88 docs(adr): the leak onto the gating bar, and two numbers the record refuted
6a2f407 fix(lora-c): all five lenses returned; the worst finding is a leak onto the bar
bad6b66 fix(lora-c): lens 1 returned 95 minutes late with eight findings; all eight hold
96ae7b1 fix(lora-c): a fourth reachability block, and the executor flags 15 of its own rows
```

## 📋 Recent decisions

- `INDEX.md` — Decision records
- `lora-c-rationale-supervision-and-the-shared-pool.md` — Rationale supervision, one shared neighbour pool — and four things the design cannot reach
- `pass-2-in-the-readers-schema-and-strict-authority.md` — pass 2 answers in the reader's schema under STRICT authority, and the fourteen keep their sixth look off a bar

## 📅 Recent daily logs

- `2026-08-23.md`
- `2026-08-22.md`
- `2026-08-21.md`

<!-- AUTO-GEN END (everything below preserved across refreshes) -->
# Hot Cache — curated

**Last update:** 2026-08-23 `lora-c-apply` ИСПОЛНЕН за $0. **Оба ревью-гейта ЗАКРЫТЫ** — 515
рационалей и 160 синтетических строк с `reviewed: true`, ни одна метка в гейте 2 не сдвинулась.
Поправка 3.25 применена целиком. **И проверка реальности, которую она же приказала, ОСТАНАВЛИВАЕТ
линию: настоящий токенизатор даёт 2 975 на широчайшей строке против порога 2 800 и против самого
`max_seq_len` 2 816.** Верификатор 3 668 passed / 2 skipped, дерево чистое, подов нет.

> Этот блок курируется руками. Археология закрытых контрактов живёт в отчётах, ADR и логах дня — не
> восстанавливать сюда то, у чего есть дом. Ценз буста мерить ДО и ПОСЛЕ каждой правки.

## 🔥 What's Hot

**🛑 ТЫ ЗДЕСЬ: ждём СЛОВО ОПЕРАТОРА по потолку. Всё остальное по контракту сделано.**
`docs/reports/lora-c-apply.md` — отчёт, ADR обновлён, регистрация DRAFT v2.

**🩸 ГЛАВНОЕ ЧИСЛО: 2 975 (2 991 со слэком) против 2 800 и против 2 816.** Токенизатор Gemma-4 на
запиненной ревизии, все 506 строк, за $0. **Две строки за порогом по ИСТИННОМУ счёту, три со слэком** (слэк правил оценку по символам, которую токенизатор уже считает; `@matusi_ukr:22327#580336` лежит между читками — 2 785 сырых / 2 801 со слэком). **Две строки за самим `max_seq_len` 2 816 при ОБЕИХ читках** — STOP инвариантен к вопросу. Строки:
`@tarilka_malyuka:746#83` (2 975) · `@klopotenkofood:6060#21366` (2 963) · `@matusi_ukr:22327#580336` (2 785). Модель
отношения, из которой выведено 2 816, предсказывает 2 763 и пропускает ВСЁ — недооценка **228
токенов** на строках втрое длиннее тех, на которых её мерили. **Это не следствие пересборки:** на
`97548df` тот же счёт даёт 2 974 и две строки за порогом при ОБЕИХ читках — ответ по истинному
счёту не двигался вовсе, сдвинулась только читка со слэком (2 → 3), на один токен на одной строке. Следующая ступень 3.25 (1) — **3 072** —
покрывает 2 991 с запасом 81, и взять её может только оператор.

**✅ ПРОТЕЧКА ЗАКРЫТА, и доказывает это 0.9875.** Элементы E, видящие свой же коммент: 22 → 0, на
платном баре 5 → 0, обучающие строки 13 → 0. Максимальная похожесть где-либо теперь ровно то
значение, которое линза 3 назвала наибольшим строго ниже 1.0. **Ни один запрос не потерял пятого
соседа** — новых экземпляров STOP 1 нет. Отказ реализован ПРЕ-ФИЛЬТРОМ: `build_pass1_fewshot_packs.py`
запинен десятью записями.

**⚙️ `config/qlora.yaml` — РЕВИЗИЯ 2 (1 408 → 2 816), правкой на месте.** Три запечатанных артефакта
lora-b пересобираются БАЙТ В БАЙТ: её длиннейшая строка 1 222 токена. Пин lora-b описывает ревизию 1
и не перепинивается.

**📏 ЧТО ГОТОВО К ЦЕНЕ.** 506 строк арм A · 666 арм B · шаги 62/82 (`planned` 64/84 ведёт LR) · 198
вызовов на легу · 4 леги · 11 тредов прохода-2. Кап **$4.00**. **Цена не зарегистрирована и не
должна быть.**

## ⏭️ Next

1. **Рулинг оператора по потолку:** 3 072 или другое решение. До него `lora-c-run` не стартует.
2. **Приёмка `lora-c-apply` тимлидом** — отчёт `docs/reports/lora-c-apply.md`.
3. **`lora-c-run`** — первый $0-деливерабл: **sibling-тренер**. `train_qlora.py` запинен
   `prereg_lora_b.json` И `lora_b_verdict.json`, редактировать нельзя.
4. **`money-anchors` ($0)** — ШЕСТЬ открытых шагов: `pass1-window`, `pass1-window-r2`,
   `pass2-signals`, `pass2-signals-r2`, `lora-c-prep`, `lora-c-apply` (все $0, но шаги есть).

## 🚧 Blockers

**⛔ ПОТОЛОК ПОСЛЕДОВАТЕЛЬНОСТИ НЕ ЗАКРЫТ.** 2 816 не хватает трём строкам. Это единственное, что
стоит между линией и первым платным шагом.

**⛔ ТРИ ОСТАВШИЕСЯ НЕДОСТИЖИМОСТИ (3.25 (3) принял STOP 1 как есть).** (1) девять строк
`@VARUS_channel:10367` не рендерятся, включая единственную реальную `молочный_бренд`; (2) **у арма A
ноль таргетов `молочный_бренд`**, у B — 32, значит бар 1 почти недостижим для A; (3) запиненный
`train_qlora.py` отказывает v3-датасету двумя гардами — лечится sibling-тренером.

**⛔ БАР 3 КРАСНЫЙ** (`@VARUS_channel:10366`, 2 сигнала против 0) — вопрос к фильтру прохода-1
или к эталону, решается только рулингом.

**⛔ ПРОХОД-1 НЕ БЕРЁТ БАР НИКАК:** ни базой, ни LoRA (9/14), ни v2 (+7 из +10). Арм B не измерен.

**⛔ preflight СЛЕП К `config_sha256`.** У поля нет соседа-пути, поэтому `config/qlora.yaml` вообще
не попадает в реестр пинов: старые пины читаются 38 → 38 через смену ревизии этого файла. Проверять
перечисленным диффом записи, не преflight'ом.

**⛔ ЗАРЯД ПО МАКСИМУМУ × РАЗБРОС — БЕЗОПАСЕН И ДОРОГ.** 97 против измеренных 23.760. Разброс подов
на этом декоде измерен прямо и равен **1.008**, но **n = 1 и это самый короткий из пяти ответов** —
цитировать только парой с 200/200 прохода-1.

**⛔ ssh — СЕМЬ ЧТЕНИЙ.** 14.5 · 23 · 29 · 38 · ≤50 · 231.9 · 262.5.

**⛔ БУТ — НЕ КОНСТАНТА.** 139.5 / 140.6 / 142.7 / 146.8 / 164.9 / 192.1 / 237.2 / [267, 293] /
353 с. Заряжать по МАКСИМУМУ.

**⛔ A6000 48 GB В EU-RO-1 — `none`.** 4090 24 GB $0.74 берётся с первой попытки, доказан пятью
подами подряд.

**🧹 BOOT TAX 10.3K при цели 10.7K.**

## 🔫 Footguns этого файла

**⛔ `knowledge/hot.md` грепается как ЦЕНОВОЙ ВХОД.** `scripts/volume_calc_5c1.py` берёт отсюда
**один** литерал через `quoted(HOT, …)`, и его отсутствие роняет девять тестов
`tests/test_volume_calc_5c1.py`. `make check` — после правки ЭТОГО файла, не только кода.

- **Recorded rather than open:** том CA-MTL-3 удалён, его **~$0.24/day** больше не капают — литерал
  нагруженный, не украшение, и он ОБЯЗАН стоять ровно так, по-английски со слэшем:
  `scripts/volume_calc_5c1.py :: quoted(HOT, "~$0.24/day", 0.24)` грепает его ИЗ ЭТОГО ФАЙЛА как вход.
  Перевод слова `day` уже уронил девять тестов один раз (12.08).
- **A stopped pod with no network volume still bills its container disk.** 80 GB is about what the
  100 GB network volume costs per month. Если под нужен на одну сессию — **delete**
  (`runpodctl pod delete`, `pod terminate` не существует), не stop. Остановленный под — не
  остановленный счёт, и `runpodctl pod list` показывает только running: смотри `pod list -a`.
