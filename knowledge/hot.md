<!-- AUTO-GEN START (refreshed by scripts/refresh-hot-cache.py) -->

# Hot Cache

**Auto-refreshed:** 2026-08-24 20:38:27 (every SessionStart)
**Branch:** `main`

## 🔀 Recent commits (top 5)

```
aec3b11 docs: hot.md carries the closing reading, and Dv802 says what it actually costs
cb0af81 chore(vault): the day log records the closing reading and the two takes before it
1cd3e95 docs(report): the closing reading, taken after the instrument refused its own first one
8ef994e chore(vault): the Stop hook's own output, landed so the closing reading has a clean tree
b659dea fix(verify): the stamps bracket the suite instead of arriving after it
```

## 📋 Recent decisions

- `INDEX.md` — Decision records
- `lora-c-arm-b-rendering-and-the-two-leg-pass-2.md` — A synthetic row is a v3 QUERY — and pass 2 runs for the arms only
- `lora-c-rationale-supervision-and-the-shared-pool.md` — Rationale supervision, one shared neighbour pool — and four things the design cannot reach

## 📅 Recent daily logs

- `2026-08-24.md`
- `2026-08-23.md`
- `2026-08-22.md`

<!-- AUTO-GEN END (everything below preserved across refreshes) -->
# Hot Cache — curated

**Last update:** 2026-08-24 20:34 (checkpoint) `lora-c-armb` ИСПОЛНЕН за **$0** и ЖДЁТ ПРИЁМКИ — подов нет,
попытка цела, регистрация всё ещё DRAFT. **Арм B стал ФАЙЛОМ** (666 = 506 префиксом байт в байт +
160 отрендеренных синтетических), **проход-2 = 2 ноги по рулингу (н) → break-even вырос 48.47/58.61
→ 63.29/73.43 с/шаг, и замер lora-b 61.047 теперь ВЛЕЗАЕТ при обеих ценах**. Находка, которую
контракт не заказывал: **все 160 синтетических несут маркер, которого нет ни в одной из 506 строк**
— и на них же все 32 таргета `молочный_бренд`. Верификатор **3 701/2 на `8ef994e`**, «reading
HOLDS», дерево не двинулось; `ruff format --check` 438 файлов. Отчёт —
`docs/reports/lora-c-armb.md`.

> Этот блок курируется руками. Археология закрытых контрактов живёт в отчётах, ADR и логах дня — не
> восстанавливать сюда то, у чего есть дом. Ценз буста мерить ДО и ПОСЛЕ каждой правки.

## 🔥 What's Hot

**✅ ПОТОЛОК ЗАКРЫТ РУЛИНГОМ 23.08: «поднимай max_seq_len до 3072».** Та ступень, которую поправка
3.25 (1) назвала заранее. Широчайшая из 506 строк — **2 975** (2 991 со слэком), за потолком **0 при
обеих читках**, запас **97 по истинному счёту / 81 со слэком**.

**⚠️ ЗАПАС ТОНКИЙ — меньше 4 % потолка — НО ЭТО СЧЁТ, А НЕ ПРОЕКЦИЯ.** В этом вся разница с тем, как
ставили 2 816: модель отношения недооценивает широчайшую строку на **228 токенов**, и рулинг этой
находки не трогает. Любая будущая правка длины строк должна перемеряться токенизатором, а не
отношением.

**✅ ЗАКОН ДОГНАЛ РУЛИНГ — ПОПРАВКА 3.26.** Блок написан тимлидом, посажен в реестр срезки
(`lora-c-close`): 3.26 (1) потолок 3 072 · (2) порог STOP 2 800 читается ИСТИННЫМ счётом, без
`TEMPLATE_SLACK` · (3) ratio-модель для потолков в отставке. Регистрация больше не УТВЕРЖДАЕТ, что
блока нет, — она его **цитирует** через `quoted_spec`, который падает, если текст ушёл из SPEC.
Грep за действующим потолком возвращает 3 072 из закона.

**✅ ПРОТЕЧКА ЗАКРЫТА, и доказывает это 0.9875.** Элементы E, видящие свой же коммент: 22 → 0, на
платном баре 5 → 0, обучающие 13 → 0. Максимальная похожесть теперь ровно то значение, которое линза
3 назвала наибольшим строго ниже 1.0. **Ни один запрос не потерял пятого соседа.** Отказ реализован
ПРЕ-ФИЛЬТРОМ: `build_pass1_fewshot_packs.py` запинен десятью записями.

**⚙️ ЛИНИЯ B НЕ ТРОНУТА ДВАЖДЫ, и это измерено.** Все три запечатанных артефакта пересобираются байт
в байт и при ревизии 2, и при ревизии 3; её пины остаются на ревизии 1.

**📏 ЦЕНА ПЕРЕВЫВЕДЕНА ПОД РЕАЛЬНЫЙ ОБЪЁМ (`lora-c-armb`, $0) — И ТЕПЕРЬ ОНА СХОДИТСЯ.** Рулинг
(н) фиксирует проход-2 на ДВУХ ногах, не четырёх: база v2 сквозной уже измерена (r2: 4/5 · 2), база
v3 бара не берёт. Это ЕДИНСТВЕННЫЙ сдвинувшийся член — минус 2×11×97 = **2 134 с = $0.4742**.
Фикс-часть = бут 500 + загрузка 300 + базы 2×198×6.14 + оценки адаптеров 2×198×6.14 + проход-2
2×11×97 + смок 6×181.5 = **8 885.88 с**. При $0.80/ч (рунг 1 lora-b, худшая цена, при которой
create разрешён) = $1.9746 → **63.29 с/шаг** на 144 шага; при $0.74/ч (что платили последние пять
подов) = $1.8265 → **73.43**. Замеры репозитория — **61.047** и **68.442**: первый влезает при
обеих ценах, второй только при $0.74. **Ни одна s/step по-прежнему не зарегистрирована** — оба
чтения сняты на ≤1 222 токенах против здешних 1 445–2 975, и покупает её смок. По ИЗМЕРЕННЫМ
сиблинг-ставкам порог **93.43–103.57**. Вердикт опубликован ПОЛЕМ
(`money.pre_pod_arithmetic.readings_under_the_break_even`), а не только фразой.

**⚠️ МАРКЕР В ХЕДЕРЕ — находка, которой не было в контракте.** Пять строк хедера несут синтетические
запросы и НИ ОДНА из 506: `NO_POST_TEXT` (реальных строк с ним — **0 из 506**) и четыре тега
`<thread channel="synthetic" post_id="<класс ошибки>">`. Помечены все 160 — а на них ВСЕ 32 таргета
`молочный_бренд` (у арма A ноль). Значит адаптер может привязать класс к маркеру, которого eval
никогда не покажет, и «синтетика не помогла» станет неотличимо от «модель выучила маркер». Блок
примеров чист: в пуле ровно ОДНА строка `молочный_бренд`, её видит каждый запрос линии.

**📐 ЦЕНЗ КОДИРОВКИ АРМА B: 666 из 666, 0 отказов**, мин 1 445 / медиана 1 603 / макс 2 975 при
3 072, запас **97**, широчайшая строка РЕАЛЬНАЯ. `class_weights` арма B — **ПЯТЬ** классов,
`молочный_бренд` **4.1625**. Изоляция — шесть чтений, все нули; подробности в отчёте.

**🖥 СТОКОВАЯ ПРОБА (read-only, 24.08 08:24Z, ничего не создано).** В EU-RO-1 — дата-центре самого
тома — **RTX A6000 48 GB `none`**, A40 там вообще не каталогизирован. Со стоком 7 из 47 карт:
RTX 2000 Ada $0.24 Low · L4 $0.49 Low · RTX PRO 4000 $0.57 Medium · **RTX PRO 4500 32 GB $0.72
High** · RTX 4090 $0.74 Low · RTX PRO 6000 $2.09 Low · B200 $6.79 Low. Перечислено и НЕ выбрано:
карта — слово оператора рядом с капом, а листинг это не create.

## ⏭️ Next

1. **ТРИ РЕШЕНИЯ по отчёту `lora-c-armb`** (§«What the operator is being asked»): (1) **кап** —
   при $0.74/ч $4.00 покрывает 73.43 с/шаг и оба чтения репозитория, при $0.80/ч только 61.047;
   ставка теперь защитима, но решает её всё равно смок; (2) **карта** — A6000 `none` в
   дата-центре тома, 4090 $0.74 Low, RTX PRO 4500 32 GB $0.72 High; (3) **маркер в хедере
   (Dv795)** — принять с названным конфаундом / зарулить другой хедер / убрать неправду строки
   topic. Рендеринг — решение тимлида, исполнитель его не берёт.
2. **Приёмка отчёта `lora-c-armb`** тимлидом — `docs/reports/lora-c-armb.md`; за ней
   `lora-c-run` r2 с капом от вывода.
3. **`money-anchors` ($0)** — ВОСЕМЬ открытых шагов: `pass1-window`, `pass1-window-r2`,
   `pass2-signals`, `pass2-signals-r2`, `lora-c-prep`, `lora-c-apply`, `lora-c-close`,
   `lora-c-run` (все $0, но шаги есть).

## 🚧 Blockers

**⛔ ВАЙТЛИСТ `check-stamped` ПРОТИВ STOP-ХУКА (Dv802, ждёт тимлида).** Хук бьёт по ПРОСТОЮ и
пишет ДВА пути: `daily_logs/` (в вайтлисте) и `index.md` (нет). Значит **сессия, тихо ждущая свой
десятиминутный верификатор, не получит цитируемое чтение** — 24.08 первый прогон вернул VOID при
ЗЕЛЁНОЙ суите, третий удержался только потому, что сессию держали занятой. Не расширял: список
исключений верификатора — гейт. Обход: не уходить в простой под прогоном.

**✅ У АРМА B ТЕПЕРЬ ЕСТЬ ДАТАСЕТ.** `results/pass1_sft_v3_arm_b.jsonl`, 666 строк:
506 реальных БАЙТ В БАЙТ префиксом (перерендерены и сверены с ОТГРУЖЕННЫМ файлом до записи;
расхождение — STOP) + 160 синтетических, отрендеренных ТЕМ ЖЕ рендерером на ТОМ ЖЕ пуле 515 по
рулингу (н). `population.train` называет ОБА файла с шами, `frozen_when_the_pod_exists` вырос ровно
на два имени. Осталось нерешённым: **маркер в хедере** — см. What's Hot.

**⛔ ДВЕ ОСТАВШИЕСЯ НЕДОСТИЖИМОСТИ (3.25 (3) принял STOP 1 как есть).** (1) девять строк
`@VARUS_channel:10367` не рендерятся, включая единственную реальную `молочный_бренд`; (2) **у арма A
ноль таргетов `молочный_бренд`**, у B — 32, значит бар 1 почти недостижим для A. Третья —
запиненный `train_qlora.py`, отказывавший v3-датасету двумя гардами, — **ЗАКРЫТА**:
`scripts/train_qlora_v3.py` переносит ровно эти два гарда и всё остальное ВЫЗЫВАЕТ; тест
подтверждает, что сам `train_qlora` по-прежнему отказывает.

**⛔ БАР 3 КРАСНЫЙ** (`@VARUS_channel:10366`, 2 сигнала против 0) — вопрос к фильтру прохода-1
или к эталону, решается только рулингом.

**⛔ ПРОХОД-1 НЕ БЕРЁТ БАР НИКАК:** ни базой, ни LoRA (9/14), ни v2 (+7 из +10). Арм B не измерен.

**⛔ preflight СЛЕП К `config_sha256`.** У поля нет соседа-пути, поэтому `config/qlora.yaml` вообще
не попадает в реестр пинов: старые пины читаются 38 → 38 через смену ревизии этого файла. Проверять
перечисленным диффом записи, не преflight'ом.

**⛔ ЗАРЯД ПО МАКСИМУМУ × РАЗБРОС — БЕЗОПАСЕН И ДОРОГ.** 97 против измеренных 23.760. Разброс подов
на этом декоде измерен прямо и равен **1.008**, но **n = 1 и это самый короткий из пяти ответов** —
цитировать только парой с 200/200 прохода-1.

**⛔ ssh — СЕМЬ ЧТЕНИЙ.** 14.5 · 23 · 29 · 38 · ≤50 · 231.9 · 262.5.

**⛔ БУТ — НЕ КОНСТАНТА.** 139.5 / 140.6 / 142.7 / 146.8 / 164.9 / 192.1 / 237.2 / [267, 293] /
353 с. Заряжать по МАКСИМУМУ.

**⛔ A6000 48 GB В EU-RO-1 — `none`.** 4090 24 GB $0.74 берётся с первой попытки, доказан пятью
подами подряд.

**🧹 BOOT TAX 11.8K при цели 10.7K** — измерено `scripts/context-census.py` 24.08 после правки
этого файла (до правки 11.2K). Цель превышена и до `lora-c-armb`; де-блоат — отдельное решение.

## 🔫 Footguns этого файла

**⛔ `knowledge/hot.md` грепается как ЦЕНОВОЙ ВХОД.** `scripts/volume_calc_5c1.py` берёт отсюда
**один** литерал через `quoted(HOT, …)`, и его отсутствие роняет девять тестов
`tests/test_volume_calc_5c1.py`. `make check` — после правки ЭТОГО файла, не только кода.

- **Recorded rather than open:** том CA-MTL-3 удалён, его **~$0.24/day** больше не капают — литерал
  нагруженный, не украшение, и он ОБЯЗАН стоять ровно так, по-английски со слэшем:
  `scripts/volume_calc_5c1.py :: quoted(HOT, "~$0.24/day", 0.24)` грепает его ИЗ ЭТОГО ФАЙЛА как вход.
  Перевод слова `day` уже уронил девять тестов один раз (12.08).
- **A stopped pod with no network volume still bills its container disk.** 80 GB is about what the
  100 GB network volume costs per month. Если под нужен на одну сессию — **delete**
  (`runpodctl pod delete`, `pod terminate` не существует), не stop. Остановленный под — не
  остановленный счёт, и `runpodctl pod list` показывает только running: смотри `pod list -a`.
