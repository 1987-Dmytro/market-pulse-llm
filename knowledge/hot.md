<!-- AUTO-GEN START (refreshed by scripts/refresh-hot-cache.py) -->

# Hot Cache

**Auto-refreshed:** 2026-09-09 14:07:16 (every SessionStart)
**Branch:** `main`

## 🔀 Recent commits (top 5)

```
f26c286 s38(c3-prep): the c3 leg BUILT and PRICED at $0 — four records, the runbook with its three §0 gates, the dry run FITS at −62.7%
99ae2b4 s38(c3-prep): the C2 producers get PARAMETERS, not siblings — the c3 leg is these five scripts re-pointed ((l) item 2, (dd) item 5)
6fb78c4 s38(c3-prep): the mode STAMP goes in — the issued harness file by path, `.claude/session_mode` gitignored, the hook drilled in three directions ((ff) item 4)
5d3a7e2 knowledge(save): s37's checkpoint — hot.md's curated block restamped on ruling (ff), the 09.09 log written, committed by path at the next start
f567f4c docs(team-lead): ruling 09.09 (ff) + the harness file and the pattern pass it issues — s37 «harness-fields» ACCEPTED ($0); the session MODE becomes a field where the platform reads it
```

## 📋 Recent decisions

- `INDEX.md` — Decision records
- `a-published-number-has-one-reader.md` — A published number has ONE reader, and a missing source is a refusal by name
- `the-registration-opens-the-line.md` — The registration OPENS its line, so §0a lives in the PAID session

## 📅 Recent daily logs

- `2026-09-09.md`
- `2026-09-08.md`
- `2026-09-06.md`

<!-- AUTO-GEN END (everything below preserved across refreshes) -->

# Hot Cache — curated
**Last update:** 2026-09-09 s39 «c3-prep-2»: нога c3 ЧИСТА и ОЦЕНЕНА, $0; открытых стопов НЕТ.
Этап 1 `SPEC-v2`, карта `STATUS.md`, цикл v3, фаза **v23**, процесс **Money v2.6**. Руками, ≤40 строк.

## 🔥 What's Hot — число ПРОДУКТА отгружено; нога c3 готова к покупке
**Отгружаемое (петля хуки + P1, `grade_promo_loop_readings.json`):** dev-3 **0.7411 (83/112) ❌ 0.80**
· сигналы **0.7937 ✅ 0.75**; dev-40 0.8857 / 0.8667, dev-2 0.9043 / 0.8296 — одинаковы обоими путями.
Чтения рядом как «reading» (0.7500 / 0.8021, сырьё 0.7054, holdout-40 0.7181), НЕ переписываются; ничьи dev-3 —
**16 из 28**; holdout-3 — РЕЗЕРВ. **c3 за $0 (s38):** якорь **2026-09-05** выведен, 29 медиапостов,
**30 страниц ТОЧНО** (0 недостижимых), 30/30 на диске; dry run 30 стр. + 8 постов,
`pagecount: match`, дорогой угол **$0.1864** против капа **$0.50** — **ВЛЕЗАЕТ (−62.7%)** (перепрогон s39).
Ранбук `promo_c3_paid_leg.md`: §0 = ДВА гейта через `&&` (штамп, поля v2.6), оба показаны в обе стороны.

## ⏭️ Next — свежий верификатор → чтение тимлида → «c3» (платно) в ЕЩЁ ОДНОМ свежем процессе
Кап c3 = min($0.50, REMAINING − $0.30) — **берётся из гарда В ТОЙ сессии**, не отсюда. §0 → §5, том удаляется
в §4 ТОЛЬКО при полной записи прогона. Дальше: «chain-fold» ($0, форма (b), (gg)2 — сдвигает отгруженный P1,
число перемеряется функциями продукта) → e2e на чистом клоне + `draw_truth_20` → гейт **12.09**.

## 🚧 Blockers / долги (названы, не построены)
**✅ Закрыто в s39:** allow-правила СНЯТЫ (инертны в bypass — доки), третий гейт ранбука вместе с ними;
`harness-permission` — это 12 deny-правил, достающих до Bash-файловых команд (`cat`/`sed`/`cp`/редиректы) в ЛЮБОМ
режиме, НЕ классификатор. **Файлы тимлида — только Read-tool; харнес-файл — только Write-tool, `diff` = пруф.**
**⚠️ `promo_projection_c2.json` НЕ ВОСПРОИЗВОДИМ производителем** (живое чтение гарда + растущий ledger внутри
sha-закреплённой записи): 40 строк расхождения даже без правок. Пруф такой записи — её пин, не перепрогон.
**Деньги: гард печатает `REMAINING $1.4762`** (том пьёт ≈$0.24/day); запас над капом $1.1762.
`make check` **4326/2** на `2004216` (пять срезов, объединение доказано); HEAD `2004216`, подов НЕТ, $0 за сессию.
· K12 читает настоящий `results/`, `tick.REGISTRY` — константа.
· `tick.py --window` дефолт `w2` — ранбук несёт `--window all`. · **⛔ `promo-dev-loop` ОТКАЗЫВАЕТ.**
**⚠️ zsh НЕ разбивает `$SSHO` · ⚠️ НЕТ `timeout` на macOS · ⚠️ ТОЛЬКО `setsid` · ⚠️ живость по PID ·
⚠️ K4 НЕ ПЕРЕЗАПУСКАТЬ · ⛔ `1925810730` · ⛔ `aggregates.py` и `open`**.
## 🔫 Footguns этого файла
**⛔ ЭТОТ ФАЙЛ ГРЕПАЕТСЯ КАК ВХОД — ДВА ЛИТЕРАЛА.** `scripts/volume_calc_5c1.py :: quoted()` берёт обе строки ниже посимвольно; пропажа любой роняет девять `tests/test_volume_calc_5c1.py`.
- 100 GB сетевого тома стоят **~$0.24/day** — цена ЗА ТОМ, а не состояние счёта; с 27.08 том один,
  `mp-srv2`. Литерал обязан стоять ровно так, по-английски со слэшем.
- **A stopped pod with no network volume still bills its container disk.** 80 GB is about what the
  100 GB network volume costs per month. Под на одну сессию — **delete** (`runpodctl pod delete`),
  не stop; `runpodctl pod list -a` показывает только running, смотри `pod list -a`.
