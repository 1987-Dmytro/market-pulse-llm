<!-- AUTO-GEN START (refreshed by scripts/refresh-hot-cache.py) -->

# Hot Cache

**Auto-refreshed:** 2026-08-20 11:17:37 (every SessionStart)
**Branch:** `main`

## 🔀 Recent commits (top 5)

```
d05d8c6 docs(report): lora-b -- the fork is resolved, both branches executed, and D3's input is a different set
08c4ef2 docs(report): reader-topup -- the pass completed, and the review is why the last segment worked
8e3807a run(reader-topup): COMPLETE -- 132 of 132 units, every gate GO, and the training set has its context
dba059b fix(reader-topup): segment 3 starts with an empty out-file, and the alarm is the REMAINING cap
c32801c chore(reader-topup): the pack points at attempt B's registration and nothing else moved
```

## 📋 Recent decisions

- `INDEX.md` — Decision records
- `boot-tax-re-registered-from-the-measured-floor.md` — The boot tax is de-bloated in all five groups, and the target is re-registered from the measured floor
- `boot-tax-target-suspended-and-the-census-axis.md` — The ≤9K boot-tax target is SUSPENDED, and the census moves onto the loader's axis

## 📅 Recent daily logs

- `2026-08-20.md`
- `2026-08-19.md`
- `2026-08-18.md`

<!-- AUTO-GEN END (everything below preserved across refreshes) -->
# Hot Cache — curated

**Last update:** 2026-08-20, D3/D4 ЗАКРЫТЫ. **Бар КРАСНЫЙ: 9 из 14 при пороге 12 — линия B
ЗАКРЫТА, вопрос уходит на сидение C.** Одна попытка потрачена, $2.3271 из капа $6.00. Арм B
НЕ обучен — не по деньгам ($2.19 ≤ $2.50), а по часам, и причина — мой простой, а не прогон.
Отчёт `docs/reports/lora-b-run.md`, ADR `lora-b-red-and-line-b-closes`.

> Этот блок курируется руками. Археология закрытых контрактов живёт в отчётах и ADR — не
> восстанавливать сюда то, у чего есть дом. Вытеснено 19.08 → mapping-таблица «блок → дом»
> в `docs/reports/boot-debloat.md`.

## 🔥 What's Hot

**🔄 ТЫ ЗДЕСЬ.** Линия промпт-инженерии читателя закрыта своим stop-правилом, архитектура **D**
выбрана, её микро-замер СДЕЛАН и ОТРИЦАТЕЛЕН, и сидение 18.08 выбрало **линию B**: размеченные
`subject_type` + LoRA, **разметку делает ТИМЛИД**. Пак построен, метки написаны и ЗАПЛОМБИРОВАНЫ
(`8d2e1ba`), фриз и провенанс лежат в `results/`. Пак **r2 на 150 юнитов** построен, принят и
**РАЗМЕЧЕН** (валидатор зелёный) и **ЗАПЛОМБИРОВАН** (`e027d0b`, sha `8f611437…`).
`lora-b` D0–D2 исполнены: датасеты SFT **500 / 650** (арм A ⊂ арм B), пре-регистрация
`results/prereg_lora_b.json` закоммичена ДО пода. Развилка, которую открыл D1, **закрыта двумя
рулингами**: ветка C ($0) и ветка B ($1.2259).
[[sitting-b-line-b-and-the-team-lead-labels]]

**🔴 РЕЗУЛЬТАТ D3/D4.** Под `0cnth2accoyj0n`, RTX A6000 **$0.53/ч**, 15 399 с. Все ступени с
пастами: цена GO · ssh 31.8 с из 180 · бут 353 с из 450 · milestone $2.1938 из $2.50 · smoke GO.
Арм A: 62 шага, 68.44 с/шаг, пик 33.19 GB. Eval 64/64, ноль отказов. **Бар 9/14 — строка в строку
как у базы**, но модель НЕ база: две из пяти промахнутых строк отвечены иначе, а ценз-50 сдвинулся
резко — `None` **25 → 9**, `не_наш_рынок` **8 → 23**. Файн-тюн выучил ПРИОР обучающего набора
(340 из 650 = 52% `не_наш_рынок`), а не различение, и взвешенный сэмплер этого не удержал. Это и
есть вход сидения C: не «мало данных» и не «форма развалилась».

**⛔ МОЙ ДЕФЕКТ, И ОН СТОИЛ АРМА B.** Ступени 4 и 8 НЕ СРАБОТАЛИ НИ РАЗУ — я запустил арм A и
перестал опрашивать. Арм A кончился в 14:38:23Z, найден в 17:20Z: **9 720 с простоя = $1.43 из
$2.33**. Ретроспектива по восстановленному логу говорит GO на каждой строке — то есть ступени бы
и не поймали: **все kill-правила смотрят на медленное обучение, ни одно — на под, который не
обучает вообще**. Без этого простоя арму B хватало 15 104 с против нужных 6 668. Следующему
контракту такой формы нужна ступень простоя с дедлайном от ПОСЛЕДНЕЙ лог-строки, а не от create.
[[long-run-watch-the-process]]

**🔧 D3a — пять затяжек за $0, ДО пода, направление строго безопаснее.** (1) `learn_chars` идёт
через запятую ПОСЛЕ значения: маска считается по КОНЦАМ токенов, и слияние `",` уносило хвост
единственного поля, которое считает гейт. На 1 150 строках сдвинулся ровно один ключ ровно на +1,
таргеты байт в байт. (2) Часы КУМУЛЯТИВНЫ: 5.5 ч ≈ **$4.40** по всем подам, `--terminate-after`
= create + (стоп − сожжённое закрытыми подами), и `--open` ОТКАЗЫВАЕТ, если окно длиннее.
(3) Projection gate на каждом логе — арм B по ИЗМЕРЕННОМУ темпу и 0.5 ч накладных внутри проекции;
без этих двух поправок 121 с/шаг проходит и букву контракта, и watchdog. (4) Бюджет-чек
пересоздания: $2.6275 · $3.4399 ($3.8399 с накладными) · $4.40. (5) Format smoke — ОДНА обучающая
строка, не gold и не eval-пак; **попытка списывается на первом ответе по gold-строке**.
Инструменты: `scripts/gate_lora_b.py` · `scripts/score_lora_b.py` · `scripts/runbook_lora_b.md`.

**⚖️ ЧЕМ УЧИМ И ЧЕГО НЕ УЧИМ.** Тимлид разметил ОДНО поле, а парсер требует четыре: `subject_id` и
`stance` пишутся `null` и **выключены из лосса** (`learn_chars`) — обучение на них уронило бы
потолок бара до **11 из 14** при пороге 12. Это арифметика, а не вкус, и она в тесте.

**🧾 КОНТЕКСТ ОБУЧАЮЩИХ ЗАПРОСОВ КУПЛЕН (ветка B, $1.2259).** Проход читателя по 105 тредам,
132/132 юнита: **630 из 650 topic'ов — купленные саммари** (было 91), **282 из 650 строк несут
entity-блок** (было 39), маркеров многоточия ветки C осталось **6** (было 372). Прогноз обещал
~279 блоков — попал в 1%. ⚠️ **Остаток разрыва не покупается**: у гейта 12 из 14, у арма B 43%,
потому что вердикт резолвит сущности только там, где они есть (рецептные и маркетплейсные треды не
называют никого). Держать в голове при чтении результата D3.

**🧬 ИДИОМЫ, КОТОРЫЕ ДЕРЖАТ.** Продюсер зовёт продюсера и отказывается, пока тот не пересоберёт
замороженную запись байт в байт · перечисленный дифф утверждается в ОБЕ стороны · записи ворот
APPEND · пре-регистрация + one-attempt + stop-rules · парный прогон в `--outdir` как доказательство
детерминизма · право исполнителя на отказ.
⚠️ Непрозрачный диапазон в перечисленном диффе закрывается руками:
[[a-hash-is-not-the-claim-it-carries]].

**📊 ЯЧЕЙКА И ДАТАСЕТ — ОДНОЙ СТРОКОЙ, дом у обоих есть.** Ячейка читателя: 129 тредов · 1 032
платных (`results/gate_census_w1_reader.json`; ⚠️ **гейт УЖЕ молочный** — правило отбирает 129 из
129, «целевой» подвыборки в нём нет, разбор в `docs/reports/pass1-redraw.md`). Датасет линии B:
650 строк, «наших» 49 (7.5%), **`молочный_бренд` = 2** — целевой класс стоит на ДВУХ строках
против бара ≥12/14, и это вход `lora-b`, а не находка после него
(`results/pass1_sft.json`, цена прохода-1 — 5.162 с/вызов — в `results/prereg_lora_b.json`).

**💸 ДЕНЬГИ ЦИКЛА-2: $3.3004 из $20.00, остаток $16.6996.** `reader-topup` списал **$1.2259** из
собственного капа $2.00 тремя сегментами (23 с + 462 с + 5 479 с); два первых купили дефекты, а не
вердикты, и это было дёшево. Все поды удалены, удаление доказано тремя листингами.

## ⏭️ Next

1. **`lora-b` ЗАКРЫТ КРАСНЫМ — ждёт приёмки тимлида** по `docs/PROMPT-acceptance-lora-b-run.md`.
   Отчёт `docs/reports/lora-b-run.md`, ADR `lora-b-red-and-line-b-closes`, вердикт
   `results/lora_b_verdict.json`. Ретрая нет и тюнинга нет — правило пре-регистрации.
2. **Сидение C — другая база.** На стол: таблица абляции база/арм A на тех же 14, сдвиг ценза-50,
   и то, что арм B остался НЕизмеренным (множественность, которую сидение приняло, была на два
   выстрела, сделан один).
3. **Денежная гигиена (`money-anchors`, $0)** — правая часть закрытия шага, судьба зависшего walk'а
   `pass1-probe`, дисциплина `anchored_at` для 19 отказанных леджеров. По слову оператора.

**Кандидаты цикла-2 в порядке ценности:** окно-2 ≈$4.8 · батч-пробник $2.13
(`results/batch_cycle2_projection.json`: единственный кандидат **batch 2**, влезет ли — НЕ ИЗМЕРИМО
без живого пробника; риск не память, а ОТВЕТЫ — при batch 16 разошлись 6 строк из 666) · сбор
листовок Сільпо / Varus / Маркетопт (бесплатно) · докачка корпуса · история 11 143 (ждёт батча И
денег) · serving-латенси ОТЛОЖЕНО ([[serving-latency-deferred]]).

**Долги-входы следующих регистраций:** цена страницы листовки **10.408 с/стр (n=159)**, старые записи
НЕ перескориваются. Ценз `results/gate_census_w1.json`: narrow|silencers_on = **111 тредов, 912
платных комментариев** (популяция ПРОБ — отдельный файл от читательской ячейки). Два вопроса из §5
отчёта fix-a и метка типа поста для окна-1. Мелочи: заголовок `t5_depth`, валюта ₴ в шапке
промо-таблицы, fix-b(2) «две колонки — одно число».

## 🚧 Blockers

**⛔ `pass1-probe`: ОБХОД ЗАВИС на 300 000 из 427 000 мс** — без движения больше 12 часов. Если
биллинг не допостит недостающие 127 000 мс, закрыть шаг нечем и **нужен рулинг**, чем закрывать.
Открытое чтение дрейфует **$0.2075 → $0.4305** при капе $0.20 — это капель тома на плече
balance-delta, НЕ перерасход прогона (ресурс шага $0.087772). [[a-settlement-is-a-reading-too]]

**⛔ `pass1-probe-b`: обход ПОЛНЫЙ (+0.104%), а закрытие отказано ЧЕСТНО.** Правило сверяет settled
($0.135538) с записанным чтением леджера ($0.0853), а оно **отстающее — 64.9% правды**: `$0.0853189556`
это в точности тот же обход, обрезанный на 09:52:00, за семь минут до снятия баланса. **Нужен рулинг
о ПРАВОЙ ЧАСТИ** закрытия: чтение леджера или запись прогона.
[[a-lagged-reading-is-not-a-second-opinion]]

**⛔ 19 старых леджеров отказаны по именованным причинам** — нет якоря (10), нет окна (5), плюс
`5c1_vis` (ключ якоря не матчится по имени шага) и `probe_a` (окно нулевой ширины). Полосой это не
чинится: форма следующего денежного контракта — **дисциплина `anchored_at`** плюс правая часть.
И толеранс из контрольной таблицы pass1-probe **не выводится** — таблица мерила баг ОКНА, а не
разброс биллинга. [[a-spread-that-is-really-a-window]]

**⛔ ПРОХОД-1 НЕ БЕРЁТ БАР В НЫНЕШНЕМ ВИДЕ — 9 из 14 при пороге 12.** Ответ сидения — линия B, и
он куплен НЕ результатом, а правом попробовать: двинет ли супервизия `subject_type` — измерит
LoRA-регистрация против того же запечатанного бара.

**⛔ БУТ НА ЭТОМ СТЕКЕ — НЕ КОНСТАНТА.** 192.1 / 237.2 / [267, 293] с на одной карте, одном томе и
одних весах — **1.53×**, и ничто в прогонах этого не объясняет. Заряжать по МАКСИМУМУ замеров,
потолок ставить над ним — и не приписывать подъёму потолка результат, которого он не производил
(18.08: старый потолок 300 с прошёл бы с запасом 57.8 с). [[the-fix-that-was-not-a-fix]]

**⛔ ЗАПАС НА ПРОВИЖЕН НАДО ПОКУПАТЬ, А НЕ НАДЕЯТЬСЯ.** Четыре бута на этом стеке расходятся в
**6.7×** (179 · 373 · 175 · >1 200 с). Дедлайн вида `usable − projection` — не запас, а ставка на
платформу. [[a-negative-pre-generation-budget-is-a-forecast]]

**🧹 BOOT TAX — НЕ ЗАДАЧА, А ЧИСЛО. Запас 0.2K, и он ПОСЛЕДНИЙ.** Ценз **10.5K** при цели
**10.7K**
(`TARGET_KTOK`, пол × 1.1, сидение 19.08). Что ушло и куда — mapping-таблица в
`docs/reports/boot-debloat.md`; правила с `paths:` держат 25 730 Б за **0**,
`~/dev-stack-parked.md` вне загрузки. MEMORY.md **155/200 строк (77.5%) · 19 432/25 000 юнитов
(77.7%)** — ✅ **все пять долгов индекса закрыты 20.08** (`/close` работает по лимиту загрузчика
≤200/25 000, а не по ретировавшемуся бару контракта `pass1-redraw` ≤150), и это стоило **+0.2K**,
измеренных до и после. Курация 20.08 сначала увела ценз В цель (10.7, запас ноль) — две карточки с
готовым домом вытеснены строкой с указателем, вернулось 0.2K. ⚠️ **Следующее, что растит бут,
съедает последний запас**: мерить до и после, а не предполагать.
✅ **Долги индекса закрыты 20.08 — все пять.** `a-checker-whose-failure-is-silence`,
`the-argmax-and-the-max-are-two-rows` и три урока этого дня:
`drive-the-consumer-not-only-the-producer` (гоняй потребителя, а не только то, что написал),
`a-borrowed-rule-carries-an-unstated-population` (гард приносит свои резоны, но не форму данных,
на которой они держатся), `a-retry-inherits-the-last-attempts-output` (общий том отдаёт замене
файл прошлой попытки, и «пуст ли он» меняет смысл ветки).
[[boot-tax-re-registered-from-the-measured-floor]]

## 🔫 Footguns этого файла

**⛔ `knowledge/hot.md` грепается как ЦЕНОВОЙ ВХОД.** `scripts/volume_calc_5c1.py` берёт отсюда
**один** литерал через `quoted(HOT, …)`, и его отсутствие роняет девять тестов
`tests/test_volume_calc_5c1.py`. `make check` — после правки ЭТОГО файла, не только кода.

- **Recorded rather than open:** том CA-MTL-3 удалён, его **~$0.24/day** больше не капают — литерал
  нагруженный, не украшение, и он ОБЯЗАН стоять ровно так, по-английски со слэшем:
  `scripts/volume_calc_5c1.py :: quoted(HOT, "~$0.24/day", 0.24)` грепает его ИЗ ЭТОГО ФАЙЛА как вход.
  Перевод слова `day` уже уронил девять тестов один раз (12.08).
- **A stopped pod with no network volume still bills its container disk.** 80 GB is about what the
  100 GB network volume costs per month. Если под нужен на одну сессию — **delete**
  (`runpodctl pod delete`, `pod terminate` не существует), не stop. Остановленный под — не
  остановленный счёт, и `runpodctl pod list` показывает только running: смотри `pod list -a`.
