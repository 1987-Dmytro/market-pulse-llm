<!-- AUTO-GEN START (refreshed by scripts/refresh-hot-cache.py) -->

# Hot Cache

**Auto-refreshed:** 2026-08-25 19:34:22 (every SessionStart)
**Branch:** `main`

## 🔀 Recent commits (top 5)

```
57ceb90 docs(report): the closing stamped reading, and the commit table completed
e2a2abd docs(report): the r3 ceiling is a RANGE, and the shortfall grew ten-fold
61f07d6 docs(report): lora-c-vramprobe -- the setting worked and the card still cannot train this line
01e4eb5 feat(vramprobe): the paid session -- OOM again, and the setting did work
04dd942 chore(money): the vramprobe spend anchor, committed before the create
```

## 📋 Recent decisions

- `lora-c-the-card-cannot-train-at-3072.md` — 32 GB cannot train this line at 3 072 — and the base columns say the prompt already moved
- `INDEX.md` — Decision records
- `lora-c-arm-b-rendering-and-the-two-leg-pass-2.md` — A synthetic row is a v3 QUERY — and pass 2 runs for the arms only

## 📅 Recent daily logs

- `2026-08-25.md`
- `2026-08-24.md`
- `2026-08-23.md`

<!-- AUTO-GEN END (everything below preserved across refreshes) -->
# Hot Cache — curated

**Last update:** 2026-08-25 19:28 (checkpoint) **`lora-c-vramprobe` ИСПОЛНЕН: OOM снова, карта
RTX PRO 4500 32 GB ЗАКРЫТА для линии.** 376 с = **$0.0752 из $0.30**; попытка ЦЕЛА, замороженная
регистрация не тронута. Настройка `expandable_segments` СРАБОТАЛА — фрагментация 1.33 GiB →
544 MiB — и упало на бо́льшем запросе: не хватило **0.06 GiB у r2 против 0.64 GiB здесь**. Дефицит
ограничен ТОЛЬКО СНИЗУ: шагов ноль, размер нужной карты отсюда не следует. Решение о карте и
датацентре — у оператора. Отчёт `docs/reports/lora-c-vramprobe.md`; предыдущий шаг —
[[lora-c-the-card-cannot-train-at-3072]].

> Этот блок курируется руками. Археология закрытых контрактов живёт в отчётах, ADR и логах дня — не
> восстанавливать сюда то, у чего есть дом. Ценз буста мерить ДО и ПОСЛЕ каждой правки.

## 🔥 What's Hot

**🖥 В EU-RO-1 НЕТ НИ ОДНОЙ 48 GB КАРТЫ (чтение 25.08 14:10Z, $0).** A6000, PRO 6000 и
преавторизованный фолбэк 4090 — все пустые; PRO 4500 32 GB `Medium` и обучать эту линию не может.
Где 48 GB есть: **A6000 $0.53/ч `Low` в EU-SE-1 и US-TX-1**, MIG-срез PRO 6000 2g.48gb $1.09 в
EUR-IS-2 и US-PA-1. Другой ДЦ = ДРУГОЙ ТОМ: 59 GB весов заново плюс вторая аренда — не оценено ни
одной регистрацией. Листинг датирует вопрос, а не отвечает на него.

**💸 ОСТАТКА КАПА r2 СКОРЕЕ ВСЕГО НЕ ХВАТИТ И НА ПОДХОДЯЩЕЙ КАРТЕ.** $3.2394 = 16 197 с при
$0.72/ч. r3 влезает при **56.5–64.1 с/шаг** — диапазон, потому что маркер-ценз (40 звонков × 9.20 =
368 с) и проход-2 по его же границе **15** тредов вместо заряженных 11 (+776 с) лежат ВНЕ фикс-части
6 577.2 с. Единственный замер репы — **61.047** на строках ≤1 222 токенов против здешних до 2 975,
то есть **+3.1 … −4.5**. Поднять кап или урезать план — слово оператора.

**✅ ЗАКРЫТО и живёт в отчётах/ADR:** потолок **3 072** (поправка 3.26; широчайшая 2 975, запас 97,
счёт токенизатором) · **протечка** элементов E, видящих свой коммент (22 → 0, на баре 5 → 0) ·
**линия B не тронута дважды**, её пины на ревизии 1 · вайтлист `check-stamped` расширен на
`knowledge/index.md`, `hot.md` остаётся снаружи поимённо · у арма B есть датасет 666 строк, маркер
в хедере принят и назван ([[lora-c-arm-b-rendering-and-the-two-leg-pass-2]]).

**📉 ЧТО КУПЛЕНО И ОСТАЁТСЯ:** обе базовые ноги на E — **v2 61/98 · v3 66/98** на холдауте, 0
отказов парсера. Заряд v3 = **9.20 с/звонок** во всех будущих выводах (рулинг (п)), не 6.14.

## ⏭️ Next

1. **СЛОВО ОПЕРАТОРА ПО КАРТЕ И ДАТАЦЕНТРУ** — единственное, что разблокирует линию. Варианты и их
   цены в `docs/reports/lora-c-vramprobe.md` §5; ни один не выбран здесь.
2. **`money-anchors` ($0)** — девять открытых шагов. Долг: `--close` у гарда по ОБОИМ шагам
   (walk отстаёт: r2 $0.0602 против $0.7606, проба $0.0495 против $0.0752 по часам пода).
3. **prep-b** — коллекция Сільпо/Varus/Маркетопт ($0), после читательского тракта.

## 🚧 Blockers

**⛔ 32 GB НЕ ТЯНУТ 3 072 — ИЗМЕРЕНО ДВАЖДЫ, ВТОРОЙ РАЗ С ЛЕКАРСТВОМ.** `micro_batch` понижен до 1
самим трейнером по правилу `config/qlora.yaml`, ноль шагов оба раза. Понижение потолка НЕ ПОМОЖЕТ:
`max_seq_len` — гард на энкоде, `collate` паддит по длиннейшей строке В МИКРОБАТЧЕ. Дешёвых проб
больше нет: `expandable_segments` отработала штатно и не хватило. `PRO 6000` $2.09/ч признан
невлезающим (~$11 за сессию).

**⛔ `make fmt` ОБНУЛЯЕТ ПИН ПРОДЮСЕРА ЗАМОРОЖЕННОЙ ЗАПИСИ.** `scripts/write_lora_c_prereg.py`
запинен `a4eb2893…` в `results/prereg_lora_c.json::producer`; `ruff format` двигает его в
`6807d264…`. Два файла расформатированы прямо сейчас и оставлены как есть. `make check` гоняет
`ruff check`, а не `ruff format --check`, и не видит ни того, ни другого.

**⛔ ПРИРОСТ v3 ИДЁТ МИМО КРАСНЫХ ЦЕЛЕЙ (рулинг (п)).** +5 холдаут-строк целиком в `не_наш_рынок`;
дозамер тимлида за $0: v3 не чинит ни 580124 (F2a), ни бар 3 (те же 4 ложных «сеть» в 10366).
Ответ на «нужен ли линии адаптер» — **ДА**.

**⛔ БАР 3 КРАСНЫЙ** (`@VARUS_channel:10366`, 2 сигнала против 0) — вопрос к фильтру прохода-1 или к
эталону, решается только рулингом. **ПРОХОД-1 НЕ БЕРЁТ БАР НИКАК:** ни базой, ни LoRA (9/14), ни v2
(+7 из +10). Арм B не измерен.

**⛔ ДВЕ НЕДОСТИЖИМОСТИ (3.25 (3) принял STOP 1 как есть):** девять строк `@VARUS_channel:10367` не
рендерятся, включая единственную реальную `молочный_бренд`; **у арма A ноль таргетов
`молочный_бренд`**, у B — 32, значит бар 1 почти недостижим для A.

**⛔ preflight СЛЕП К `config_sha256`.** У поля нет соседа-пути, поэтому `config/qlora.yaml` не
попадает в реестр пинов: старые пины читаются 38 → 38 через смену ревизии. Проверять перечисленным
диффом записи, не preflight'ом.

**⛔ ssh И БУТ — НЕ КОНСТАНТЫ, ЗАРЯЖАТЬ ПО МАКСИМУМУ.** ssh: девять чтений, 14.5…262.5 с (проба
дала 29.4). Бут: девять, 139.5…353 с. Списки целиком — в отчётах подов. Проход-2 заряжается 97 с/тред
против измеренных 23.760 — безопасно и дорого; разброс подов на этом декоде измерен как 1.008 при
**n = 1**, цитировать только парой с 200/200 прохода-1.

**🧹 BOOT TAX** — мерить `scripts/context-census.py` ДО и ПОСЛЕ каждой правки этого файла; цель
10.7K превышена с 24.08 и де-блоат остаётся отдельным решением. `MEMORY.md` вырос до 25.98 KB при
окне загрузки ~24.4 — индекс усекается, консолидация назрела.

## 🔫 Footguns этого файла

**⛔ `knowledge/hot.md` грепается как ЦЕНОВОЙ ВХОД.** `scripts/volume_calc_5c1.py` берёт отсюда
**один** литерал через `quoted(HOT, …)`, и его отсутствие роняет девять тестов
`tests/test_volume_calc_5c1.py`. `make check` — после правки ЭТОГО файла, не только кода.

- **Recorded rather than open:** том CA-MTL-3 удалён, его **~$0.24/day** больше не капают — литерал
  нагруженный, не украшение, и он ОБЯЗАН стоять ровно так, по-английски со слэшем:
  `scripts/volume_calc_5c1.py :: quoted(HOT, "~$0.24/day", 0.24)` грепает его ИЗ ЭТОГО ФАЙЛА как вход.
  Перевод слова `day` уже уронил девять тестов один раз (12.08).
- **A stopped pod with no network volume still bills its container disk.** 80 GB is about what the
  100 GB network volume costs per month. Если под нужен на одну сессию — **delete**
  (`runpodctl pod delete`, `pod terminate` не существует), не stop. Остановленный под — не
  остановленный счёт, и `runpodctl pod list` показывает только running: смотри `pod list -a`.
