# Competitor registry — competitor -> Telegram channels (docs/SPEC.md §5).
#
# PLACEHOLDERS. The real competitors are picked in Phase 2, and only after the
# entry check that their channels actually have comments enabled (SPEC §8);
# naming them here would front-run that gate.
#
# MVP scope: 2-3 direct competitors, Telegram only.

competitors:
  - id: competitor-a
    name: Placeholder Competitor A
    telegram_channels:
      - "@placeholder_channel_a"

  - id: competitor-b
    name: Placeholder Competitor B
    telegram_channels:
      - "@placeholder_channel_b"
      - "@placeholder_channel_b_news"
